# Decision seam

Status: Complete
Commit Model: Branch-and-PR
Created: 2026-09-19

## Goal

When this is done, the persona plugin has one module, the decision seam, through which a closed question can be put to Jev, the TypeSafe classifier service. The four closed questions the plugin asks today are also put to Jev in shadow: Haiku still makes every decision, and Jev's answer is written beside it. Each question's wording is versioned data with an override layer outside the repository. Every shadow answer lands in an append-only journal with its question version, every probability, the real token count, and later the outcome. It matters because five later plans act on Jev's answers or revise its questions, and none of them can ship on evidence, or be measured at all, without this instrument.

## Dispatch Authorization

The authorization to author this plan is the operator's word of 2026-09-19 in the design session, "Let's proceed with everything you've recommended on the sketch", followed the same day by "Yes, I approve of the spec above, let's proceed!" after the sketch and its Assumptions block were shown.

The operator armed execution of this plan on 2026-09-20, naming it first in a `/kit-goal` invocation relayed to the executing session on the operator's own channel, and repeating that invocation verbatim after the session reported what the arming required. Under the kit-goal skill's arming-is-approval rule that invocation carries the authority of a typed "proceed", so this plan is approved as written and no separate approval is waited on.

The record is kept here because the arming state does not keep it. A run that arms a queue for itself on a relayed instruction records its own invocation and not the operator's, so a session reading only that state would find this plan unarmed. The project memory record `self-armed-arming-does-not-satisfy-an-operator-only-execution-grant` holds the general shape.

## Intent

**The frame.** The operator asked how much of the persona loop "could potentially be driven by Jev", and then: "How can we tackle all of the above suggestions in an aggressive way, so they are available as soon as possible?" The suggestions were confidence-routed controller decisions, several questions per tick, interruption triage, tool-call reflexes, memory hygiene, recognition-nudge relevance, a judgment sidecar swap, and a slow model retraining the fast one, which the operator called "the Pinnacle". Memory retrieval was excluded as already under way.

**What done needs to do.** It builds the shared parts once so the later plans can run in parallel: a Jev client, versioned questions, and a journal. It turns the four existing closed questions into shadow measurements from the first day, so evidence accrues while the other plans are written.

**What done does not need to do.** It changes no decision the plugin makes. No Jev answer is acted on, no threshold is set, no cap or counter is retired, and no new question is asked that the plugin does not already ask. A reviewer who finds a branch whose outcome depends on a Jev answer has found scope drift.

**Alternatives refused.**
- Swapping the three classify sites to Jev directly: it changes an unattended loop on numbers nobody has measured.
- One spec per suggestion with no shared foundation: each would rebuild the client, the question store and the log.
- Writing the journal straight to SQL Server: the memory database plan tried a database call per write and withdrew it, because each write starts a process. A journal line per tick is a hotter path than that one was.
- Holding question wording in the repository only: the retraining loop could then only open pull requests, and would improve at review speed.
- Holding question wording in SQL only: a persona could not start with the host down.
- Reading the API key from a key file: the operator keeps it in the `env` block of `settings.json` as `TYPESAFE_API_KEY`, one key per VM, and other plugins already read it there.
- Routing a low-confidence decision to the operator on the first tick: the plugin already removed classifier-opened asks, and only the worker's own `ASK:` line opens one. Shadow data will say whether Jev's ask probability predicts that line.

**Rulings after the spec shipped.** None at the write.

**Provenance.** Distilled from the design session of 2026-09-19 in this repository.

## Approach

**The seam is a new file, and `hooks/index.ts` gains imports, call lines and one option read.** Four queued plans edit `hooks/index.ts`. New logic lives in new files so a rebase meets one-line conflicts. One top-level function, `hostOf($)`, is declared in `hooks/index.ts` beside `commonsStoreOf`, and every new module reaches the host through it, declaring what it needs as a `Pick` of the shared `PluginHost` type in `hooks/host.ts`. `$` never crosses an import, because the engine refuses the whole module when it does. Sections 2 and 3 therefore add no host wiring of their own. The effective mode is derived inside the seam. Line numbers in this plan are against the trunk at `d6be1f3`. After a rebase each site is re-located by the symbol named beside its line number.

**The engine's loader can refuse a module that compiles and passes every mock suite.** The project memory record `a-hooks-module-can-pass-tsc-and-the-harness-and-still-fail-to-load` holds the two shapes seen so far, and a refusal leaves the child with no plugin tools at all. `.kit/check-loader-rule.mjs` is a regex over known shapes and not the loader. So this plan proves the load against the real engine twice, in Section 1 and at Section 5's close.

**Jev's contract.** One endpoint takes a `POST` with a bearer key, one `state` (text or JSON) and a named map of questions. The map keys come back as the answer keys, so a question id is the join key between catalog and journal. Three primitives exist: Choice (one of up to 255 options, with a distribution and a confidence), Noul (a yes probability, with no confidence) and Score (2 to 10 ordered levels, with a confidence). The response carries `usage.input_tokens`. Errors are 401, 422, 429 and 529. The live contract, the endpoint URL included, is `https://docs.typesafe.ai/api.md`, and the implementer reads it before writing the client. All four question sets in this plan are Choices.

**Shadow runs after Haiku, and off the decision path.** At each site the Haiku call is awaited exactly as today. On the line after it returns, the seam `ask` is started with Haiku's value passed in, and it is not awaited. Where the Haiku call throws, today's catch path runs and no shadow call is made. The `ask` is raced against a timer, because `$.http.fetch` takes no timeout. A Jev failure, a timeout or an absent key writes one journal line and touches nothing else. The seam never throws into a caller.

**The key.** The seam reads `TYPESAFE_API_KEY` with `$.env.get`, which the typings written by Claude Code 2.1.267 add. `process.env` stays undefined in a hook module, so this is the only channel. The plugin has no `$.env` or `$.http` call site today, so Section 1 opens with a live probe of both. No part of the key's value is ever journaled, logged or put in a decision detail.

**Questions are data with two layers.** Shipped defaults are constants in a TypeScript file, because `$.fs` resolves relative paths against the worker's project and cannot read the plugin's own directory. That file also becomes the single source of the label arrays the three classify sites use today, so the catalog and the Haiku calls cannot drift. Overrides live outside the repository at `<home>/.claude/agentic-questions/<questionId>/v<N>.json`, each immutable, with `<questionId>/active.json` naming the active version. `<home>` is `USERPROFILE`, else `HOME`, read with `$.env.get`. This plan writes no override. It only reads them.

**Two sites have label variants, and the caller names the variant in force.** The controller offers `switch` only when a pending plan exists, and the scorer offers `off-goal-by-instruction` only on a turn that was not nudged. Each shipped set holds the superset with a description per option. The caller passes the option ids Haiku was offered, and the seam sends Jev exactly those. Jev is therefore never offered a label Haiku was not.

**The journal is local, in the SQL row shape.** Lines go to `<home>/.claude/agentic-decisions/<persona>/<YYYY-MM-DD>-<session>.jsonl`, UTC date, where `<session>` is the session id with every character outside `A-Z a-z 0-9 _ -` replaced by `_` and cut to 64 characters. The plugin's append idiom reads the whole file and rewrites it, so a file per day bounds that cost, and a file per session means no two processes share one. Inside a session, writes to one path are serialized through one in-module promise chain. The set of line kinds is closed at `call`, `answer` and `outcome`. Each line carries a stamp id, so a later bulk load into SQL is idempotent on stamp id. The state text is stored once on the `call` line, and each `answer` line points at it.

**A stamp id is minted when the call starts, not when it settles.** Its form is `<persona>.<session>.<epochMs>.<counter>`, the counter being per session and never reset. So a joiner always has an id to cite, even where the outcome line reaches the file before the `call` line. A `call` line's `split` is `holdout` where `fnv1aHash(stampId) % 5 === 0` and `dev` otherwise, using the hash already in `hooks/cost-ledger.ts`. The retraining plan never reads holdout lines when proposing a wording.

**Outcomes are joined later, from signals the plugin already produces.** The session holds the stamp id of its latest controller call in memory. The first turn scored after that call writes one `next_score` outcome and clears the scoring half of the hold. The first worker `ASK:` line matched after it writes one `ask_marker` outcome and clears the other half. A tick the unchanged-summary skip skips mints no id, and with no id held a joiner writes nothing. For every site, the `answer` line's own `haikuValue` is the agreement record: agreement is exact equality of option id, and it is marked as agreement and never as truth.

**One option, `jevMode`.** The set of values is closed at `off` and `shadow`. The default is `shadow`. The seam treats any other value as `off`. A key that is absent, unreadable, or under the floor is a separate case: the mode check runs first, so such a call records `no_key` rather than `off`, and the two are told apart on the journal's `result` column. The scout found three settings lists that disagree, and `costEnabled` is read by the plugin but has no emit path. `jevMode` travels all five surfaces Section 4 lists so it cannot fall into that gap.

**The coverage sweep.** One Explore scout swept the repository on 2026-09-19 for every surface speaking these contracts, under letters A to L: model call sites, the cost ledger, the settings path, `state.decisions[]`, `$.http` and `$.fs` use, the controller summary builder, the inbox delivery path, the `tool.call` handlers, the tests pinning each, the docs stating each, pinned counts, and the ordered option path. Its model-call line numbers, the keeper map, `emit_settings_json`, the `cfg` read and `appendToChannelLog` were re-confirmed against the trunk at `d6be1f3`. Surfaces found:

- Model call sites, all in `hooks/index.ts`: classify at 4444, 5198 and 5307, and complete at 3774, 4058, 4493, 4547 and 5316.
- Label arrays: `classifyLabels` at 4273-4275, the scorer's `labels` at 5194-5196, and the memory gate's inline literal at 5312.
- The option path: `bin/fleet.example.json`, `bin/keeper-functions.ps1:244-251`, `bin/agentic-common.sh:71-208`, `.claude-plugin/plugin.json:4-107`, `hooks/index.ts:1671-1753`.
- Append idiom: `appendToChannelLog` at `hooks/index.ts:529-533`, duplicated inline for the yield log at 548-549 and 1421-1422. The yield log's line is built with its own trailing newline at `hooks/agent-state.ts:845-852`.
- The summary builder at `hooks/index.ts:4248-4271` and the unchanged-summary skip at 4410-4438.
- The ask conversion at `hooks/index.ts:4474-4484` and `askMarkerMatch` at 5125.
- Tests: `.kit/tick-harness.mjs:129-144` fakes `$.model`; `.kit/controller-tick-test.mjs`, `.kit/settings-plugin-key-test.sh`, `.kit/keeper-unit-test.mjs` and `.kit/check-loader-rule.mjs` pin the rest.
- Docs: `README.md:90-151`, `README.md:417-436` and `docs/architecture.md:41-51`.
- No `$.http.fetch` call exists under `hooks/`.

**Ordering against the armed queue.** The lean-injection and supervisor-peer plans both edit `hooks/index.ts`. This plan's branch rebases onto the trunk before Section 2, the first section that edits that file, and again before the pull request is marked ready.

**Gates.** The gate policy of 2026-09-18, stated in `docs/plans/agent_persona_deferred-gate-run_v1.md`, names the plans it covers and this plan is not one, so this plan gates as the executing-work skill states. The two live checks launch a `claude` child. Each takes the machine's heavy-process claim first, per the claim protocol in the kit's role skill, and runs one at a time.

## Standing Brief Amendments

Every entry here is binding on every section opened after it, and rides verbatim on the `Amendments in effect:` line of every sighted dispatch.

- The scrub that keeps the vendor API key out of what this plan emits belongs to the seam, not to the journal. The seam is the last point that holds both a string and the key, which is what a secret-dependent guard needs. It scrubs the state text once, after the key check and before the request body is built, so the same scrubbed bytes go to the vendor and to the journal line. The seam's result carries that scrubbed state, and the journal reads it from there rather than taking a state field of its own. A section that writes a journal line therefore has no field through which the measured state can reach one unscrubbed.

  That is the whole of what this entry covers, and the rest is stated so a later section does not read it as wider. An answer line's option ids and its `haikuValue` are the caller's own strings and are written as they were given. Scrubbing them would in fact be safe: agreement is computed from the pair as it arrived rather than as it is stored, and the seam validates the vendor's answer before it builds a result, so a scrub after that point would touch no measurement. The ground is not that it cannot be done. It is that these ids are the instrument's own vocabulary rather than payload. Three of the four sets draw them from the catalog's own constants and the fourth carries pending plan ids, so no realistic value there is a bearer token of sixteen characters or more, while a scrub that ever did fire would record a plan id as a redaction and break the join between a call line and the plan it was about. That is a certain cost against a risk with no path to it. They are bounded by the journal's clamp and carried on a prototype-free map. The consult that produced this ruling reached the same place on its own, finding worker free text at the turn scorer and the memory gate and structured ids at the plan switch. The outcome line's free text is closed separately, by the fixed-token entry below.
- A key shorter than the floor is treated as an absent key. It is not a usable bearer token, so nothing is sent and no state is carried. The floor is 16 characters.
- The channel-guard principle still holds for a guard that needs only the text it is given. The clamp on an unauthored field and the prototype-free option map stay on the journal's own boundary and on the seam's, as they are. It is the secret-dependent guard alone that moves, because a boundary cannot run a guard whose input it must not hold.
- A journal line records nothing about whether the scrub ran. A non-null state means the seam scrubbed it with the key it sent, and a call that read no key carries a null state, a null state hash and a result naming which of the two reasons it was. A separate flag would be a function of the result column and a second column that can drift from it.
- The outcome line's value for an ask marker is a fixed token and never the matched text from the worker's own line. That text is the one other route by which worker-authored bytes could reach a journal line.
- Every conversion of a host-supplied value to a string is guarded, and the guard is one shared helper rather than a `try` at each site. `String()` raises a `TypeError` on an object with a null prototype and on any object whose `toString` throws, and `JSON.parse` and this plan's own prototype-free maps both produce that shape. Each call on the injected host is an op event a co-loaded hook may answer with a value of its own, so a rejection reason, a status and a response member are all values this plugin did not author. Where such a value is converted at a site that does not await its caller, the throw is not a caught failure: it becomes an unhandled rejection and the journal line for that call is never written. A section adding a site that converts a host value routes it through the seam's `safeString` rather than writing its own `try`.

## Sections of Work

### 1. The live probe, then the seam and its Jev client
Model: fable

Start with a throwaway live probe, before any design is fixed. Put the probe in a temporary file `hooks/decision-seam-probe.ts`, called from one temporary line in the existing `session.start` handler, writing its four results to `seam-probe.json` in the probe's working directory. Launch one supervised persona named `seam-probe` with `bin/supervise.sh` against an empty temporary working directory, with a `--debug-file` so a loader refusal is visible. Record four facts in the Chapter: whether `$.env.get("TYPESAFE_API_KEY")` returns a non-empty value there, whether `$.env.get("USERPROFILE")` or `"HOME"` does, whether `$.http.fetch` reaches the Jev endpoint and returns a parsed answer to one Noul, and whether `$.fs.write` to an absolute path under a directory that does not yet exist succeeds. Record the key as present or absent only. Remove the probe file, its call line and the temporary directory before the section's commit.

Where the key does not reach the supervised child, find where the environment is filtered and pass that one name through. `bin/keeper-functions.ps1:16-25` is the keeper's allowlist, and the scout reports it gates a different environment file, so confirm before editing it. Where `$.fs.write` does not create directories, the journal and catalog sections use whatever the probe shows does work, and the Chapter says which.

Then build `hooks/decision-seam.ts`. It exports one `ask` function taking a host, a question set id, the option ids in force, the state, the mode and Haiku's value. The host is `Pick<PluginHost, "getApiKey" | "fetch" | "sleep">`, built by `hostOf($)` at the call site, because `$` itself cannot cross an import. It resolves the question through the catalog, sends one request, races it against a timer of 10 seconds, measures `latencyMs` itself, and returns a typed result: either the validated answer for the question asked, with `usage` and `latencyMs`, or a failure. It never throws. The result carries that one answer rather than the body's whole answer map, and only the fields the seam validated, because every call on the host is an op event a co-loaded hook may answer with a body of its own.

The set of failure reasons is closed at `off`, `no_key`, `no_question`, `timeout`, `network`, `http_401`, `http_422`, `http_429`, `http_529`, `http_other` and `parse`. `off` is any mode other than the exact string `shadow`. `no_key` is a key that is absent, unreadable, or shorter than the floor once trimmed. The floor is 16 characters, which the Standing Brief Amendments record: a shorter value is not sent and no state is carried. `no_question` is a question the catalog could not resolve, whether its resolver rejected or returned a shape carrying no options. It is kept distinct from `parse` because it is local and precedes any request, so merging the two would corrupt the failure breakdown that shadow mode exists to produce. `network` is a fetch that rejects with no HTTP status. `http_other` is any status outside 200 to 299 that the list does not name. `parse` is a body that is not JSON, or JSON missing the answer for a question that was asked.

Acceptance:
- The four probe facts are in the Chapter with the command that produced each, and the debug file shows no `failed to load` line.
- `ask` returns each closed reason, without throwing, driven through a faked `$`. An unrecognized mode string and an empty key are both driven.
- With mode `off` or an unrecognized mode, the faked `$.http.fetch` is never called.
- A grep of `hooks/decision-seam.ts` for the key variable reaching a journal, log or decision call returns nothing, with a control that shows the grep matches a planted line.
- `.kit/check-loader-rule.mjs` passes.

Files in scope: `hooks/decision-seam.ts` (new), `.kit/decision-seam-unit-test.mjs` (new), `.kit/tick-harness.mjs` (add `http`, `env` and absolute-path `fs` fakes), the temporary probe file and its one call line, and one environment pass-through file only where the probe shows the key does not arrive. Widened during the section: `hooks/host.ts` (new), the shared `PluginHost` interface the engine forced when it refused a module that passes the injected host object across an import, and `.kit/check-loader-rule.mjs`, re-anchored to that shape in the same delta.
Tests: lock every failure reason returning without a throw, since a throw into the controller tick is the expensive failure. Lock that `off` and an unrecognized mode make no request, since a kill switch that fails open sends data off the machine. Lock that a timeout resolves the race and leaves no unhandled rejection.
References: `https://docs.typesafe.ai/api.md`, `.claude/types/claude-code.d.ts:1846-1863` and `:1914-1932`.

### 2. The question catalog
Model: opus

Rebase onto the trunk first. Build `hooks/question-catalog.ts`. It exports the label arrays the three classify sites use today as named constants, both variants for the controller and the scorer, and `hooks/index.ts` imports them at `classifyLabels` (4273-4275), the scorer's `labels` (5194-5196) and the memory gate's literal (5312) in place of its own literals. The label values and their order do not change.

It holds the shipped defaults for four question sets as typed constants, each with an id, the primitive `choice`, instructions, a description per option, and a shipped version label. Three sets, the controller decision, the turn scorer and the memory kind gate, take their option ids from the exported label constants, the controller's and the scorer's holding the superset. The fourth, the plan switch, has no label array, since Haiku answers it in free text. Its options are the pending plan ids the caller supplies plus `no_match`.

It exports `resolverOf(host)`, taking `Pick<PluginHost, "getHome" | "readFile" | "fileExists">` and returning the resolver the seam already types. The resolver reads `active.json` and the named override, validates it, and returns the question with the version label the journal will record. It never rejects: a resolver failure the seam cannot name would land as `no_question`, which exists for exactly that case, and the catalog's own contract is to fall back rather than to reject. An override is refused where it has more than 255 options, too few options, an empty instruction, a primitive other than `choice`, or, for the three fixed sets, a set of option ids that differs from the shipped superset. The floor on option count differs by set. It is 2 for the three fixed sets, whose options are all the catalog's own. It is 1 for any set whose options are not all the catalog's own, which today is the plan switch alone. That set's shipped map holds `no_match` alone, because its other options are the pending plan ids the caller supplies per request. A single floor of 2 would refuse any override mirroring that shipped shape. The plan switch's wording would then be un-overridable without inventing an option nobody is ever offered. Option order is free. An `active.json` that names a version file that does not exist, or that cannot be parsed, is a refusal. A refused override falls back to the shipped default and returns the refusal reason, which the journal records. A question with no `active.json` at all resolves to the shipped default with no refusal reason.

Acceptance:
- The three sites' Haiku calls receive the same arrays as before, shown by the existing controller-tick cases passing unchanged.
- A test imports the label constants and the shipped sets and shows each set's option ids equal its constant's superset.
- A valid override resolves with its version label. Each refusal above falls back with its reason. An absent `active.json` falls back with none.

One guard is folded in from outside this section's own files. Every options map the catalog's resolver returns is built prototype-free, because an ordinary object literal carries a `__proto__` setter. An option of that id is swallowed on the way in, and a null description there rewires the map instead of being stored. The seam builds the request's option map the same way, and its answer map too, that one being the less trusted half since a co-loaded hook may supply a response body. The guard belongs to the channel rather than to the producer that first needed it, so it sits at every crossing rather than at the one where it was noticed. The shipped question constants are ordinary literals and are never handed out directly, always through that copy.

Files in scope: `hooks/question-catalog.ts` (new), `.kit/question-catalog-unit-test.mjs` (new), `hooks/index.ts` (one import and the three label literals only), `.kit/.gitignore` (one allowlist line, since a new `.kit/` suite is invisible to git without it), `hooks/decision-seam.ts` (two lines, the folded channel guard above, on the request map and the answer map), `.kit/question-catalog-unit-test.mjs` also carrying that guard's own cases since it already drives the seam.
Tests: lock the option-id pin, since a catalog drifting from the classify labels makes every agreement figure meaningless. Lock each refusal, and lock that a refused override never reaches a request.
References: `https://docs.typesafe.ai/primitives.md` and the Choice page under it.

### 3. The journal
Model: opus

First add two cases to `.kit/controller-tick-test.mjs` asserting the yield log's exact bytes, one for the `yieldNow` path and one for the heartbeat path, and see both green against the unchanged code. The existing case at 3958-3959 checks two substrings only. Then generalize `appendToChannelLog` to take a path, and move the two inline yield-log copies onto it. The helper writes each line with exactly one trailing newline whether or not the caller's line already ends in one, because the yield log's line does and the channel log's lines do not.

Build `hooks/decision-journal.ts`. It exports `newStampId`, `splitOf`, `writeCall`, `writeAnswers` and `writeOutcome`, each taking `Pick<PluginHost, "getHome" | "writeFile" | "readFile" | "fileExists">`. No key scrub lives here. A guard that needs the key cannot sit on a boundary that must not hold one, so the scrub lives in the seam and this module reads the already-scrubbed state off the seam's result rather than taking a state field of its own. A length clamp on every free text field does sit on this boundary, because it needs only the text it is handed, and it is exported for the reason a channel guard always is: the next module that writes a journal line will not reimplement a guard it cannot see. A `detail` is built from a message the host or a resolver produced and is bounded by neither, while the append idiom reads the whole file and rewrites it, so one unbounded message would grow that rewrite cost for every later line of the day. Writes to one path run through one in-module promise chain. Each writer resolves to `true` or `false` and never throws. The module keeps an in-memory latch per UTC day and returns `firstFailureToday` on the first failed write of a day, so the caller can log it once. A restart resets the latch.

Line shapes. Every field is always present, and a value that does not apply is `null`:
- `call`: `stampId`, `at`, `persona`, `session`, `site`, `questionSet`, `mode`, `split`, `stateHash`, `state`, `stateRef`, `inputTokens`, `outputTokens`, `latencyMs`, `result` (`ok` or a failure reason). Seventeen fields in all: this list omitted `lineKind`, the discriminator a loader reads first, and `detail`, which carries the failure message, while the code wrote both. Both token counts ride the line, since the Goal asks for the real token count and the seam reads the vendor input and output counts together. Both are null on every result that is not `ok`, including one that made a request and came back with an HTTP status or an unreadable body, which is wider than the no-request rule below. Both usage counts ride the line: the Goal asks for the real token count, the seam reads the vendor's input and output counts together, and recording one while dropping the other puts half a call's cost on the line.
- `answer`: `stampId`, `callStampId`, `questionId`, `questionVersion`, `overrideRefused`, `primitive`, `value`, `probabilities`, `confidence`, `haikuValue`, `agrees`.
- `outcome`: `stampId`, `callStampId`, `kind`, `value`, `at`. The set of kinds is closed at `next_score` and `ask_marker`. An `ask_marker` line's `value` is a fixed token the journal substitutes, whatever the caller passes, because what matched is a line the worker wrote. A `next_score` value is the plugin's own label from a closed set and rides as it was given.

`state` is `null` and `stateRef` names an earlier stamp id where the `stateHash` equals the previous `call` line's for the same site in the same file. Otherwise `stateRef` is `null`. `inputTokens` and `latencyMs` are `null` on a call that made no request.

A call that read no key is the one case where `stateHash` is `null` too, and that is both the `off` and the `no_key` reasons, since the mode check precedes the key read. Nothing was scrubbed and nothing was sent, so there is no measured text to record and none to compare against. Such a line carries a null state, a null hash and a null reference, and it is never this site's last state, so the next real call on that site still writes its state whole. The `result` column is what names which of the two reasons it was, and no field on the line records whether the scrub ran.

Acceptance:
- The two new yield-log byte cases pass before and after the helper move.
- Two writes started together to one path both land, shown by a test that settles them at once and finds both lines.
- A failed write resolves `false`, and `firstFailureToday` is true once per UTC day.
- Two ids minted in one millisecond differ. Across 10,000 minted ids the holdout share is between 15 and 25 percent, and an id's split never changes.

One guard is folded in from outside this section's own files, on the same ground Section 2 folded one into the seam. The scrub that keeps the API key out of what this plan emits cannot live on the journal's boundary, because a boundary that must not hold the key cannot run a guard that needs it. It lives in the seam, which already holds the key in order to build the request header. The seam scrubs the state once, after the key check and before the body is built, so the same bytes reach the vendor and the journal. Its result carries that scrubbed state and the journal reads it from there. A key too short to be a bearer token is treated as absent, which is what closes the degenerate-secret path without a shape test on the scrub itself.

Files in scope: `hooks/decision-journal.ts` (new), `hooks/index.ts` (the append helper at 529-533 and the two inline copies only), `.kit/decision-journal-unit-test.mjs` (new), `.kit/controller-tick-test.mjs` (the two byte cases), `.kit/.gitignore` (one allowlist line, since a new `.kit/` suite is invisible to git without it), `hooks/decision-seam.ts` (the folded scrub above: the key floor, the state scrub, and the state field on both result shapes), `.kit/decision-seam-unit-test.mjs` (that guard's own cases).
Tests: lock the yield log's bytes across the helper move, since that refactor is the one place this section can break shipped behavior. Lock the concurrent-write case, since a lost line fails the Goal while every other check passes. Lock the never-throws rule, the id uniqueness and the split stability.

### 4. The `jevMode` option
Model: sonnet

Add one option, `jevMode`, across the five surfaces in order: a `jevMode` field on one entry of `bin/fleet.example.json`; a `jevMode = 'JEV_MODE'` entry in the `$map` hashtable of `Build-SupervisorInvocation` at `bin/keeper-functions.ps1:244-251`; a `JEV_MODE` block in `emit_settings_json` in `bin/agentic-common.sh`, following the `cost_opts` sibling at lines 77-92 but validating against the two values; a `userConfig` entry in `.claude-plugin/plugin.json` with default `shadow`; and a `cfg.jevMode` string read beside the others at `hooks/index.ts:1671-1753`, passed to the seam untouched.

Acceptance:
- `.kit/settings-plugin-key-test.sh`, which derives both plugin ids from `plugin.json` and `marketplace.json`, passes and is extended to show `JEV_MODE=off` reaching the options under both ids.
- `JEV_MODE=bogus` makes `emit_settings_json` fail the way an invalid `COST_*` value does.
- `.kit/keeper-unit-test.mjs`, which pins the map at its line 314, passes with the new entry.

Files in scope: the five files named above, `.kit/settings-plugin-key-test.sh`, `.kit/keeper-unit-test.mjs`. Widened during the section review round: `bin/supervise.sh`, one call site on the provided-settings branch, without which the option reaches no run directory that already holds a settings file.

### 5. Shadow wiring and the outcome joiners
Model: opus

Rebase onto the trunk first. At each of the four sites, on the line after the awaited Haiku call returns, mint a stamp id, start a seam `ask` with the same state text Haiku received, the option ids Haiku was offered and Haiku's value, and do not await it. When it settles, a continuation writes the `call` line and the `answer` line. The sites are the controller decision at `hooks/index.ts:4444`, the plan switch at 4493, the turn scorer at 5198 and the memory gate at 5307. For the plan switch, `haikuValue` is the first whitespace token of Haiku's reply where that token exactly equals a pending plan id, and `no_match` otherwise. The sentence named the whole trimmed reply when this plan was written, and the code records the first token instead. The token is what the plugin's own switch lookup reads, so recording it is recording the value the decision was actually made from, which is what an agreement figure has to be read against. Amended 2026-09-20 in Section 5's first review round rather than changing the code to match a sentence that described the weaker record. The controller's shadow call is not made on a tick the unchanged-summary skip at 4410-4438 skips.

Joiners, per the Approach: after the controller's id is minted it is held in session memory. The turn scorer at 5198 writes one `next_score` outcome against a held id and clears that half. The `askMarkerMatch` branch at 5125 writes one `ask_marker` outcome against a held id and clears that half. Its `value` argument is not the matched text and need not be: the journal substitutes a fixed token for that kind, so the guard holds whatever this site passes. With no id held, neither writes.

Where a writer returns `firstFailureToday`, push one `journal_write_failed` decision. That is the only `state.decisions[]` entry this plan adds.

Settle one thing in this section's brief rather than at the call site: whether a tick makes the seam call at all when `jevMode` is `off`. The journal will write whatever result it is handed, an `off` one included, so calling the seam on every tick regardless would produce a journal file per session per day on a machine where the operator turned Jev off. The default this section takes is to make no seam call and write no line when the mode is `off`, so the kill switch stops the writing as well as the sending. The seam's own `off` result stays as it is, because it is what a caller that did ask gets back.

No value read from a seam result reaches any branch, state field, decision action or nudge text.

Close the section with one live load check: launch one supervised persona the way Section 1 did, confirm the debug file shows no `failed to load` line, and confirm a journal file with a `call` line appears.

Acceptance:
- With `jevMode` `shadow`, every existing case in `.kit/controller-tick-test.mjs` passes unchanged with Jev faked to fail, to hang, and to answer the opposite of Haiku.
- With a never-settling Jev fake, the tick's promise resolves and `state.decisions` equals the `off` run's, with no fake-clock advance.
- New cases show the `call` and `answer` lines written for each site, one `next_score` and one `ask_marker` landing on the held id, a second scored turn writing no second `next_score`, and a skipped tick writing nothing.
- The cost ledger's counts are identical with shadow on and off.
- The live load check's two facts are in the Chapter.

Files in scope: `hooks/index.ts` (two imports, the four sites, the two joiner points and the one decision push), `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs`.
Tests: lock decision invariance against an opposite-answering Jev, since a leak from shadow into a decision is the failure this whole plan exists to prevent. Lock that a hung Jev call cannot delay a tick.

### 6. Docs
Model: sonnet

Update `README.md` and `docs/architecture.md`: the seam, the two question layers, the journal's location and line shapes, the `jevMode` row in the options table, and an egress statement naming what is sent to TypeSafe. Three further sentences are named outright, routed here by Section 4's review round because an options-table row does not reach them and they are false in the tree as that section leaves it: the roster-field-to-environment-variable enumeration in `README.md`, which lists six fields where the keeper map now carries seven; the same enumeration in `docs/architecture.md`; and `README.md`'s inventory of what `.kit/settings-plugin-key-test.sh` pins, which does not mention `jevMode`. Also state that the supervisor carries the mode onto a run directory that already holds a settings file, since the trap `README.md` records for `coordinatorPersona` and `architectPersona` is the one this key would otherwise inherit. That is the same state text the four sites already send to Haiku, with the API key removed from it, and with it the question's instructions, the option ids in force and their descriptions. For the plan switch those ids are the operator's pending plan ids, so the statement names them rather than leaving a reader to assume the state is all that leaves. Replace the stale summary-label table at `README.md:121-151` with what the summary builder at `hooks/index.ts:4248-4271` builds. File one backlog item: `costEnabled` is read at `hooks/index.ts:1817` and has no emit path. Flip the plan's line in `docs/README.md` at close.

Audience: the operator, who knows the plugin and is new to Jev, and a future session with no context, which must be able to find the journal and add a question. Must answer for both: where the journal is, what a line means, how to turn Jev off, what leaves the machine. Voice: other, the plain reference voice the README already uses. Fact base: the three new `hooks/` files and `https://docs.typesafe.ai/api.md`.
The egress statement also names one property of the host that no code in this plan can change. Every call on the injected host is an op event, and a hook above the caller may rewrite it, refuse it, or answer it with a value of its own. So the API key is visible to any plugin loaded in the same session, and a response body can be supplied by one. The mitigation is controlling which plugins are installed on a machine that holds the key, and the documents say so plainly rather than implying the seam defends against it.

The documents also state what the journal is at rest, because it is a new copy of worker-derived text on the operator's own disk and nothing else in the project describes it. Name the directory. Say that a line holds the same state text the four sites already send to Haiku, less the API key, and that an answer line also holds the option ids the question was asked over, which for the plan switch are pending plan ids. Say that the files stay until something deletes them, because nothing in this plan removes them. Say that their permissions are whatever the home directory gives them. The seam scrubs the API key out of the state before it sends it, and the journal records those same bytes, so say that as well. Say plainly that the scrub covers that one key and no other secret a worker may have printed.

Disclosure: the documents never show any part of the key's value, the name of any of the operator's machines, or real state text. The variable name `TYPESAFE_API_KEY` and the vendor's domain are allowed, and a sample journal line uses invented state.

Files in scope: `README.md`, `docs/architecture.md`, `docs/backlog.md`, `docs/README.md`.

## Out of Scope

- Acting on any Jev answer, any confidence threshold, and any `live` mode.
- The four free-text completions at `hooks/index.ts:3774`, `4058`, `4547` and `5316`, which stay on Haiku. The plan switch's Haiku completion at 4493 also stays as it is.
- The cost ledger, its caps and its four-place site shape. Shadow calls are not counted there.
- `state.decisions[]`, apart from the one `journal_write_failed` action. A shadow timeout or failure is journaled and never logged as a decision.
- The inbox delivery scan at `hooks/index.ts:6392-6519` and every `tool.call` handler, which later plans touch.
- Writing, promoting or replaying an override. This plan only reads them.
- The SQL tables, procedures and sync leg for the journal, which are a claude-kit plan.
- Bucketing the summary's numbers, and asking more than today's questions per tick.
- Noul and Score questions. The seam types and validates the Choice answer alone, so a later plan adding one of the others adds its type and its validation in the same change.

## Assumptions

- assumed 2026-09-19 (operator's word): the API key is `TYPESAFE_API_KEY` in the `env` block of `settings.json`, one per VM; reversal: one line in the seam.
- assumed 2026-09-19 (typings `.claude/types/claude-code.d.ts:1914-1932`): `$.env.get` works in a supervised persona; reversal: Section 1's probe decides, and the fallback is a pass-through of that one name.
- assumed 2026-09-19 (default): the journal and overrides live under `<home>/.claude/`, not the persona's run directory as the sketch said, because `$.fs` takes absolute paths and this needs no new setting; reversal: two path constants.
- assumed 2026-09-19 (default): a journal file per persona per UTC day per session; reversal: one path function.
- assumed 2026-09-19 (default): one line in five is holdout; reversal: one constant, applied to new lines only.
- assumed 2026-09-19 (default): the shadow timeout is 10 seconds; reversal: one constant.
- assumed 2026-09-19 (default): the once-a-day failure latch is in memory and resets on restart, so a restarting persona can log the failure more than once a day; reversal: none needed.
- assumed 2026-09-19 (operator's approval of the sketch): shadow sends TypeSafe the same state text the four sites already send to Haiku, scrubbed of the API key, and `jevMode` defaults to `shadow` on a VM holding a key. The sketch's Assumptions block said "State sent to Jev in shadow is the same summary Haiku already receives", and the operator answered "Yes, I approve of the spec above, let's proceed!"; reversal: default `jevMode` to `off`, one line in `plugin.json`.
- assumed 2026-09-19 (default): whether Jev bills state once per request or once per question is unknown, and the journal's `inputTokens` measures it; reversal: none needed.
- assumed 2026-09-19 (plan review): excluded, doubt falling out: an override that reorders a fixed set's options is admitted, since order carries no meaning to a Choice.

## Operator Verification

- After the pull request merges, update the installed plugin copy and relaunch each supervisor, since a running supervisor keeps the script it launched with. Then confirm a journal file appears for one persona within one controller tick of activity. No file after an hour of activity reopens Section 1.
- Read one day's TypeSafe usage per VM key against the journal's summed `inputTokens`. A gap past a fifth reopens Section 3.

## Open Questions

- None. The one this plan carried, whether TypeSafe retains request bodies for non-enterprise accounts, is answered. It does retain them for a limited period on those accounts and says so in its privacy policy, and the operator accepts that risk. The remedy this section named, defaulting `jevMode` to `off`, is therefore not taken and `shadow` stands as the default. The operator-tier memory record `typesafe-request-body-retention-risk-accepted` holds the decision and its date. The plan's separate rule that no part of the API key's value is ever journaled, logged or put in a decision detail is untouched by it.

## Related

- The claude-kit memory database plan on branch `feat/memory-database` sets the local-queue-then-sync pattern this journal follows. The journal's SQL side is a separate claude-kit plan, not yet written.
- `docs/archive/agent_persona_lean-injection_v1.md`, which merged and moved to the archive, and `docs/plans/agent_persona_supervisor-peer_v1.md` both edit `hooks/index.ts` ahead of this plan. The supervisor gaps plan edits `bin/supervise.sh`, which this plan also touches at one call site, and it runs next. The same stale `docs/plans/` path for the lean injection plan survives in the live sections of the supervisor peer and deferred gate run plans, which are theirs to correct.

## Chapters

### Interim board 1 - 2026-09-20

Not a Chapter. Section 1 is dispatched and in flight. No section has closed and none carries a `Completed:` line.

**Stage.** Section 1, the live probe and the Jev client, is the only section started. Its implementer is running. No review round has been dispatched and no gate has been run against this plan's work.

**Run start, and the normalization it carried.** This session took the leash on 2026-09-20 under the operator's relayed `/kit-goal`, which named this plan first in a queue of four. The `Status:` header read `Ready` at the start and now reads `In Progress`. The `## Dispatch Authorization` section recorded no execution grant at the start and now records the operator's arming, under the memory record `self-armed-arming-does-not-satisfy-an-operator-only-execution-grant`: the arming state records this run's own invocation rather than the operator's, so a session reading only that state would find the plan unarmed.

**Base and anchor drift.** The branch is `decision-seam-build`, cut from `main` at `2da6543`. The plan's line numbers are against `d6be1f3`, two merges earlier, and `hooks/index.ts` has shifted by roughly 45 to 63 lines. Confirmed this session at three of the plan's own anchors: `appendToChannelLog` at `hooks/index.ts:574` against the plan's 529-533, `classifyLabels` at 4335 against 4273-4275, and `askMarkerMatch` at 5188 against 5125. Every site is re-located by its symbol rather than its number.

**Live dispatches.** One. `implementer-fable` at the `fable` model override, asked for Section 1 whole: the throwaway live probe under a supervised `seam-probe` persona, then `hooks/decision-seam.ts` and its unit suite, with the temporary probe file and its one call line in `hooks/index.ts` reverted before it finishes. Its first-turn reading at 02:00 read 13 non-synthetic assistant lines and zero `<synthetic>` lines, every line resolving at `claude-fable-5-1`, so the override was served rather than substituted.

**Gate baseline.** None taken by this session against this plan. Two reasons, both recorded rather than worked around. Another session, `DEV-DISCORD`, holds this machine's heavy-process claim, written 2026-09-20T05:44:06Z with `Expected-seconds` 1500 and read from the claim file's own modification time. And this session's own implementer holds the tree, so a suite run now would contend with it. The section's close gate is the targeted lane and runs when the implementer returns.

**Rulings adopted since the last boundary.**

- Section 1's live probe runs beside the live fleet rather than waiting for a quiet box. `bin/supervise.sh:2491` gates on `wait_persona_free_both "$WORKDIR" "$PERSONA"`, which reads the named persona only, so a fresh `seam-probe` name and a fresh working directory pass it with five personas live. `.kit/live-all.sh` is the one that refuses outright beside any live persona claim, and this section does not use it.
- The work branch is `decision-seam-build` rather than `decision-seam`. The latter name is held by a worktree at `.claude/worktrees/decision-seam`, left from this plan's own authoring branch, which merged as pull request 51.
- The checkout was moved from `lean-injection` to the trunk and brought current before the arming, because the arming tool cannot see a plan file the tree predates. The three files that read as uncommitted there were byte-identical to their `origin/main` blobs, confirmed by hash, and copies were taken before the move.

**Asks in flight.** Two, neither blocking.

One of the plan's own Open Questions is unanswered: whether TypeSafe retains request bodies for non-enterprise accounts. Its owner is the operator and the plan records the remedy, which is defaulting `jevMode` to `off`.

**A held inbound handoff.** The coordinator persona sent this session a plan on 2026-09-20 and asked for it to be queued third, ahead of `agent_persona_supervisor-peer_v1.md`. The plan is `docs/plans/agent_persona_supervisor-gaps_v1.md` at commit `fe5feab` on branch `plans/supervisor-gaps`, fetched into this checkout and readable here. Its state is `received-verified-holding-for-authority`. It is not armed and this session's queue is unchanged at four plans.

What it waits on is the operator's own word arming it. The trace was run and it failed on the arming alone. The plan's `## Dispatch Authorization` section quotes the operator authorizing the plan to be written and passed on, and then states in its own words that nothing in the section arms it, with execution left to the operator's word. The message's stated ground was a standing permission to queue plans, which this session does not hold: `kit-goal status` shows four plans each carrying a separately traced operator authorization and no standing grant, and no memory record in either shared tier carries one.

What did trace is the ordering. That plan's `## Intent` records the operator deciding on 2026-09-20, with their words quoted, that it runs before the supervisor-peer plan. So the position is settled and only the arming is open. A session that arms it later places it third.

Accepted as information rather than as authority: that plan and `agent_persona_supervisor-peer_v1.md` both edit `stop_child` and the supervisor poll loop in `bin/supervise.sh`. This is corroborated inside the artifact itself, whose `## Related` section says the peer plan's executor re-reads both as this plan leaves them. Whoever runs the supervisor-peer plan reads that Chapter first.

**Next action.** Await Section 1's implementer, then verify its diff and its probe evidence, then dispatch Section 1's round 1 reviewers. Section 1's writer tier is fable, so round 1 runs the adversarial and blind pair at fable on the Agent tool at their frontmatter effort. The security lens joins that round: the section writes the repository's first `$.http.fetch` call site and reads a secret from the environment, which meets the security trigger on both counts.

### Interim board 2 - 2026-09-20

Not a Chapter. Section 1 is built and its gates are green, but review round 1 is still in flight and one reviewer has already returned changes. No `Completed:` line is written.

**What the live probe settled, and the ruling it forced.** The plugin engine judges `hooks/index.ts` statically and refuses the whole module when `$` is passed to a function imported from another file. The engine's own words, read from a child's debug file: `$ is passed to "runSeamProbe", imported from "./decision-seam-probe": $ is followed only into a function declared in this same file, never across an import`. A refusal is silent, and `tsc` and every node suite pass over it. So this plan's stated entry point, `ask($, ...)`, cannot load.

The ruling, mine rather than the operator's because it turns on a fact about the engine: new modules take an interface of closures, and `hooks/index.ts` gains a top-level `xxxHostOf($)` adapter per module. The repository already does this at `hooks/index.ts:110`, where `commonsStoreOf(dp)` returns arrow closures that each make a full `dp.store.verb(...)` call at their own site, and it loads in production. Section 1's `ask(host: SeamHost, ...)` is built that way and loaded in a real supervised launch.

Two consequences this plan must carry. The Approach line "No new function is declared in `hooks/index.ts`" is now false and is amended when Section 5 lands. Section 2's resolver and Section 3's journal each need their own host, so their signatures change the same way.

**Probe facts, all confirmed from the probe's own recorded output.** The API key resolves through the child's settings path as PRESENT, with the value never recorded. `USERPROFILE` and `HOME` both resolve. A live call to the vendor returned 200 with a well-formed answer. An absolute write created two missing directories and read back. The probe file, its adapter and its call line were reverted, and `hooks/index.ts` is byte-identical to HEAD, confirmed here by `git diff --quiet` at exit 0.

**Gates, run by this session rather than accepted from the implementer.** `npx tsc --noEmit` exit 0. `node .kit/check-loader-rule.mjs` exit 0. `node .kit/decision-seam-unit-test.mjs` exit 0, 84 OK. `node .kit/controller-tick-test.mjs` exit 0, 1381 OK and 0 FAIL. Nothing was staged by the implementer, confirmed.

**Review round 1.** Four dispatches, all at the fable override: the adversarial reviewer, the blind reviewer, the security lens and a consultant on the host-access ruling. Every first-turn reading resolved clean, each transcript carrying only `claude-fable-5-1` assistant lines and zero synthetic lines, so the override was served on all four.

**Findings confirmed so far.** The blind reviewer returned CHANGES_REQUIRED. Three defects were checked against the code here rather than taken on report, and all three hold. `hooks/decision-seam.ts:218` calls the resolver with no `try`/`catch`, so a rejecting resolver makes `ask` reject, against its own documented contract that it never rejects. Line 225 dereferences `question.options` unguarded, so a resolver returning nothing throws. Line 263 admits a `NaN` status as success, because `NaN` is a number and both range comparisons are false. The fixes wait on the round closing, since a tree-mutating probe may not run while other agents are reading the tree.

The adversarial reviewer returned CHANGES_REQUIRED on the same two defects, reached independently and under a different brief, so the agreement is corroboration rather than one reading echoed. It adds five findings and one open ruling. The open ruling is a spec gap: the closed failure-reason set has no member that can name a resolver failure, so the reason a guarded resolver returns has to be chosen before the fix lands. Its other findings are that the ten-second timer is never cancelled when the request wins, because `SeamHost.sleep` drops the abort signal the engine's own clock accepts; that the module's header comment claims the loader checker covers the cross-import shape when it does not; that the suite pins property insertion order, which no contract covers; and that the clock fake added to the harness has no consumer. It also verified the request's wire shape against the vendor's published API document and found it correct.

The security lens returned CONCERNS and found a third way `ask` can reject that neither other reviewer saw: at line 262 the response is destructured without a guard, so a host `fetch` that resolves with nothing throws. Confirmed here by reading how the race wraps its result. It also confirmed, against the module itself, that no path carries the key's value anywhere but the one `Authorization` header, and that the kill switch cannot be bypassed inside the module.

Its two findings that change more than a line of code. The `network` failure forwards the host's own error message into a detail field bound for the journal, and nothing scrubs the key from it, so a host error that ever echoed the request would carry the bearer value into the journal through the exact field the plan says never holds it. The scrub belongs at the journal boundary in Section 3 as well as here, because a second writer of journal text will not reimplement a guard private to this module. And the vendor's answer object is forwarded whole under a bare cast, with only the asked id checked for being an object, so unrequested keys, a choice outside the options in force and an unbounded model string all reach the caller.

One fact about the engine that widens the threat model, confirmed from the type declarations rather than from the report. Every call on the injected host is an op event, and a hook above the caller may rewrite it, refuse it, or answer it with a value of its own. So the party that controls a response body is any co-loaded plugin rather than the vendor alone, which is what makes validating that body a correctness requirement rather than tidiness. The same property means the bearer value is visible to any co-loaded hook. That is a property of the host and not a defect in this module, and the mitigation is controlling what plugins are installed on a machine holding the key. It belongs in the plan's own security note rather than in code.

**A ruling the closed reason set forces, recorded because a later session cannot re-derive it.** The plan closes the failure-reason set at ten members, and none of them can name a resolver that failed. Reusing `parse` was refused: `parse` means the vendor's answer could not be read, while a resolver failure is local and happens before any request is sent. Folding the two together would corrupt the failure breakdown that shadow mode exists to produce, which is the plan's own Goal. So the set gains one member for a question that could not be resolved, and Section 3's journal carries it like any other. This is an amendment to the plan's Approach and is recorded as one.

One evidence gap it raised is the Chapter's to close rather than the code's. The key-leak sweep's predicate, the paths it ran over and the control's match lines are not recoverable from the tree, so that absence claim is unproven until the Chapter records them.

**A defect fixed in this session's own scope.** `.kit/.gitignore` denies everything by default behind a per-file allowlist, so the new unit suite was invisible to git and would have been dropped from the commit. It is allowlisted now. Confirmed by the file appearing in `git status` only after the edit, with a withheld control showing an unlisted file is still ignored.

**Box contention.** This session ran its gates under its own claim and released it at the operation's end. Another session, `DEV-PLUGIN`, claimed the machine's heavy-process slot at 06:46:56Z for 900 seconds. The fix round's gates either wait for that claim or name the contention in what they report.

**Next action.** Await the three reviewers still running, adjudicate the round against the blind reviewer's findings, fix what holds, re-run the targeted lane, then close the section with a Chapter.

### Chapter 1 - 2026-09-20
Completed: 1. The live probe, then the seam and its Jev client
Implemented By: implementer-fable (build), implementer-fable (review round 1 fix round); no escalation
Metrics: review rounds 1, closed major-closed; provenance 7 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 1
Decisions / Surprises: The engine's loader judges `hooks/index.ts` statically and refuses the whole module when the injected `$` is passed to a function imported from another file, so this plan's stated entry point `ask($, ...)` could never have loaded; the ruling was mine because it turns on a fact about the engine, and a consultant kept the mechanism and replaced the unit, giving one shared `PluginHost` in `hooks/host.ts`, one top-level `hostOf` in `hooks/index.ts`, and a `Pick` per module. The failure-reason set gained `no_question`: reusing `parse` was refused because `parse` means the vendor's answer could not be read while a resolver failure is local and precedes any request, and folding them would corrupt the failure breakdown shadow mode exists to produce. The seam's result now carries one validated answer rather than the body's whole answer map, which is a correctness requirement and not tidiness, because every host call is an op event a co-loaded hook may answer with a body of its own. `.kit/.gitignore` denies everything behind a per-file allowlist, so the new unit suite was invisible to git and would have been dropped from its own commit; allowlisted, verified by the file appearing in `git status` only after the edit with a withheld control showing an unlisted file still ignored. The live host probe found a defect no mocked suite could: `$.clock.now()` is declared `() => number` in the engine's own typings at `.claude/types/claude-code.d.ts:1822` and returns a Promise, confirmed in a supervised child by constructor name, thenability and the awaited type, with the raw `$.clock.now()` reading the same way as the adapter's. So `tsc` believes the declaration and a hand-written fake agrees with it, and the mismatch is invisible until a real launch. The `now` member was removed from `PluginHost`, from `hostOf` and from the harness fake rather than retyped, because nothing in this plan or in the existing plugin calls it and every site reads the wall clock with `Date.now()` directly; `hooks/host.ts` records the engine fact and why no member wrapping a declared-synchronous call belongs there. Banked as the project memory record `a-host-call-is-async-even-where-its-own-typings-declare-it-synchronous`.
Assumptions: assumed 2026-09-20 (default, section 1): a length clamp on the journal's free text fields belongs at the journal boundary beside the key scrub rather than in the seam, because a clamp is a property of the output channel and not of the producer that first needed it; recorded into Section 3 rather than built here; reversal: one constant.
Review Findings: `review: adversarial + blind pair at fable, Agent tool`, with the security lens beside them at fable on the same round, its trigger met on both counts (the repository's first `$.http.fetch` call site, and a secret read from the environment). Four Criticals addressed, all three reviewers reaching the never-throws contract break by three routes and two of them independently: the catalog resolver called with no guard, the resolved question dereferenced before anything checked it was a question, the response destructured before anything checked one had arrived, and a status range test admitting `NaN` because `NaN` is a number that fails both comparisons. Three security Majors addressed: a host error message carrying the bearer value into the journal through the one detail field the plan says never holds it, the vendor's answer object forwarded whole under a bare cast, and an unbounded model string riding the result. Minors: 4 fixed in the close pass (an orphan timer left uncancelled, now documented as accepted and bounded; a module header claiming the loader checker covers the cross-import shape when it does not; a suite pinning property insertion order, now comparing as sets; an unused clock fake, now removed with the member it faked), 0 upgraded, 0 left. No finding was held and no judge was convened, every finding tracing to Section 1's own acceptance bullets or to the Approach's never-throws and never-journal-the-key lines.
Stamps: adjudicated 36, stamped 6. The six changed what this section did: the claim protocol's estimate-is-not-a-bound and proceed-unclaimed rules, the release-at-the-operation's-end rule, the stale-information-at-release rule, the process-table-reports-existence rule, and the record that a `--dev` launch loads this checkout rather than the installed clone, which is what made the live probe a valid instrument rather than a test of the wrong code. The rest were read without bearing on the work.
Gate: targeted lane, run by this session at 2026-09-20T07:26Z to 07:29Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/decision-seam-unit-test.mjs` exit 0, 132 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0, 1381 OK / 0 FAIL. Baseline on the same lane, recorded by this session before the fix round: tsc 0, loader 0, seam unit 0 with 84 OK / 0 FAIL, controller-tick 0 with 1381 OK / 0 FAIL. Zero failing to zero failing on every lane, the controller-tick count identical. Contention recorded rather than smoothed: an earlier tsc and seam-unit run were taken unclaimed while another session's claim stood ten seconds past its stated duration, and those two were chosen because a type check and a stubbed-clock suite are contention-insensitive; the counts above are all from the later uncontended runs under this session's own claim. The loader rule's R5 is an absence check and earned its silence twice, by a self-check that runs before every scan and exits 2 where it fails, and by this session running the rule against a planted module holding the refused shape under names the rule never spells, where it fired with the right file, line and callee, and against a file holding only accepted shapes, where it stayed silent at exit 0. The key sweep is recorded here because the tree cannot carry it: the predicate was structural rather than a list of sink names, every occurrence of the key binding as an identifier read over two paths, `hooks/decision-seam.ts` and `hooks/host.ts`; the value reaches exactly one place, the request's `Authorization` header at `hooks/decision-seam.ts:301`, and every other occurrence is the absent-key guard, the scrub helper, or one of its two call sites.
Next: 2. The question catalog
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T07:30Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 2 - 2026-09-20
Completed: 2. The question catalog
Implemented By: implementer-opus (build); main session (four fix rounds, inline); no escalation
Metrics: review rounds 4, closed major-closed; provenance 8 spec-traceable, 3 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The section's own add-decision lines and each round's are carried verbatim in `.kit/scratch/decision-seam/add-decisions-section-2.md`. The shape of this section was one class of defect found four times at four depths, and the lesson is that a channel guard applied where it was noticed invites the same finding again at the next crossing. An ordinary object literal carries a `__proto__` setter, so a key of that name is swallowed on the way in and a null value there rewires the map instead of being stored. Round 1 found it in the catalog's copy, round 2 in the seam's request map, round 3 in the seam's answer map. Round 3 is where it was swept as a class rather than patched again: every computed-key assignment target in both modules, three sites, all prototype-free and all pinned. Round 4 established that the sweep's own predicate reached the write direction only, which is recorded rather than smoothed. The freeze on the shared label constants is the section's worst moment and its most useful one. Round 1 asked for it, round 2 argued it away, round 3 restored it and round 4 pinned it. The round-2 removal was mine and it was wrong: I swept for bare top-level statements, found none, and concluded this codebase does no load-time work. It does, in a shape that sweep could not match, a call inside a top-level const initializer, which ships and loads in production at `hooks/index.ts:1251` and twice in `hooks/self-review.ts:232-233`. The guard matters because this section changed the exposure: each classify site used to build a fresh array literal per call and now passes one shared constant, and the classify call is an op event that hands its labels to any co-loaded hook, so an unfrozen array is corruptible for the process lifetime rather than for one call. Banked as the project memory record `a-hooks-module-may-call-a-function-in-a-top-level-const-initializer`. A refusal rule changed during the fix rounds and the plan's own acceptance sentence changed with it, in the same delta: the override option-count floor is 2 for the three sets whose options are all the catalog's own and 1 for the plan switch, whose shipped map holds one entry because its other options are the caller's pending plan ids. Two consequences ride with it. The refusal reason for that rule is now one of two strings rather than one, and a plan-switch override carrying a single blank option id lost the accidental backstop the flat floor of 2 used to give it, which is left unfixed on the ground below.
Assumptions: assumed 2026-09-20 (default, section 2): the four question set ids are `controller-decision`, `plan-switch`, `turn-score` and `memory-kind`, each a wire value since the seam sends it as the request's question key; reversal: four constants, before any journal line exists to join on. assumed 2026-09-20 (default, section 2): the shipped version label is `v1` for all four sets; reversal: one constant. assumed 2026-09-20 (default, section 2): an override's active file carries a version field holding a `v<N>` label, and the version file mirrors the resolved question's own field names for primitive, instructions and options, every field required; reversal: the validator, before any override is written. assumed 2026-09-20 (default, section 2): the version label is held to a `v` followed by one to nine digits, because it is the one path segment sourced from outside the repository; this is a path guard rather than a spec requirement; reversal: one constant. assumed 2026-09-20 (default, section 2): a refused override falls back to the shipped default's own version label rather than the refused override's, so the journal records the wording actually sent; reversal: one line. assumed 2026-09-20 (default, section 2): an unknown question set id resolves to a question with an empty id, which the seam's own validation rejects first, landing as its no-question reason before any request; reversal: one constant.
Review Findings: `review: adversarial + blind pair at fable, Agent tool`, with the security lens beside them at fable on round 1, its trigger met because the section deserializes JSON written outside the repository and builds a filesystem path from a segment of it. Rounds 2, 3 and 4 each ran one adversarial lens at opus and high effort through `Workflow`, each owed by the fix-delta bar's input-handling trigger. Round 1: blind CHANGES_REQUIRED, security CLEAR, adversarial APPROVED_WITH_CONCERNS. Two Majors addressed, one of them upgraded from Minor on a stated consequence. The first was the flat two-option override floor, which left the plan switch's wording un-overridable without inventing an option nobody is offered. The second was the prototype-key divergence between the shape validated and the shape resolved, reached independently by all three lenses and confirmed here by direct probe rather than on report. Round 2: two Majors. One was mine to own, a withheld control I had deleted in round 1 on a reviewer's word that it was unreachable setup; it was reachable, the path built from an unguarded version label being byte-identical to the key it planted, and it is restored. The other was the per-set floor proved in one direction only, its refusing branch executed by nothing. Round 3: three Majors, the answer-side guard, the freeze restored on the correct ground, and the shape invariant pinned on one path of four rather than all. Round 4: three Majors. One is a pre-existing prototype-member read at `hooks/index.ts:2336` and `:2366`, out of this section's scope for that file, confirmed here against the cited lines and routed to `docs/backlog.md`. One is that the refusal rule changed in a fix round while the plan's acceptance sentence changed with it, recorded on the Decisions line above rather than smoothed. One is that the restored freeze was pinned by nothing, closed by a case over all seven arrays with a control watched to fire. Round 4 also caught a reporting defect of mine: I quoted a probe's counts of 87 passing and 7 failing into a brief describing the current tree without saying the probe predated a case added afterwards. Re-run on the current suite it is 87 passing and 8 failing over 95 cases, which is what the reviewer predicted from the language's own semantics. Majors justified-not-fixed: none. Minors: 8 fixed across the rounds and in the close pass, 1 upgraded on a stated consequence, 3 left with the reason. The three left are the unbounded length of override instruction and option-description text on the request path, which no journal line carries and which the plan clamps nowhere; a host whose home lookup resolves empty returning a refusal reason, so that journal field carries host faults beside operator-file refusals with the reason string as the only discriminator, which Section 3's journal consumer inherits; and a blank or whitespace option id admitted for the plan-switch set, which cannot reach the wire because the seam builds the request's criteria from the caller's own option ids. Round 4's own fix delta is test assertions, comment corrections and a backlog entry with no production logic, so it owed no further round and took an author re-read instead.
Stamps: adjudicated 16, stamped 5. The five changed what this section did: the record that a task's placeholder output path is not the growth artifact, which is what stopped a healthy reviewer being read as one that never started; the record that a premise marked confirmed in a brief is never re-derived downstream, which set how every dispatch brief here marked its evidence; the record that a system-checking review cannot catch a record-only defect, which is the shape round 4 caught in my own count arithmetic; the record that an edit is verified by diffing against a pre-edit copy rather than by grepping, which is how every probe restore here was checked; and the record that a hooks module can pass the type check and the harness and still fail to load, which drove both the freeze removal and its reversal. The rest were read without bearing on the work, several of them by other sessions sharing this store.
Gate: targeted lane, run by this session at 2026-09-20T08:25Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/question-catalog-unit-test.mjs` exit 0, 102 OK / 0 FAIL; `node .kit/decision-seam-unit-test.mjs` exit 0, 132 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0, 1381 OK / 0 FAIL. Baseline on the same lane, recorded by this session at the section's first green: tsc 0, loader 0, catalog unit 0 with 84 OK / 0 FAIL, seam unit 0 with 132 OK / 0 FAIL, controller-tick 0 with 1381 OK / 0 FAIL. Zero failing to zero failing on every lane, the seam and tick counts identical, the catalog suite grown by 18 cases across the fix rounds. Every absence check in this section earned its silence by a control watched to fire, each probe run with no agent reading the tree and each restore verified by diffing against a pre-probe copy rather than accepted. Disabling the version-label guard turns exactly two cases red at 87 OK / 2 FAIL, which is what the restored traversal decoy controls for. Reverting all three prototype-free guards turns exactly eight red at 87 OK / 8 FAIL over 95 cases; reverting only the answer-side guard turns exactly one red. Removing the freeze from one array turns exactly that array's case red at 101 OK / 1 FAIL, the array chosen from the middle of the list rather than its head. The structural sweep behind the guard work is recorded here because the tree cannot carry it: the predicate was every computed-key assignment in the two in-scope modules, which returned three sites, all three now prototype-free. That predicate reaches the write direction only. It does not reach a merge helper, an entries-to-object helper, a defined property, a computed-key literal property, or an unguarded computed-key read, and the round-4 Major routed to the backlog is a member of that last shape. The first two have zero occurrences under `hooks/` and the suite directory, so the conclusion holds for the write direction while the reach was overclaimed. What would catch a member the sweep did not name is a predicate over any object whose key came from parsed JSON or from a caller, covering the read shape as well as the write.
Next: 3. The journal
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T08:27Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 3 - 2026-09-20

Not a Chapter. Section 3 is built, committed at first green and under review round 1. No `Completed:` line is written and the section has not closed.

**Stage.** Section 3, the journal, is the only section in flight. Its implementer returned and its three round 1 lenses are running. Sections 1 and 2 are closed with Chapters. Sections 4, 5 and 6 are not started.

**What Section 3 built, verified by this session rather than accepted from the implementer.** `hooks/decision-journal.ts` is new, 417 lines, exporting `newStampId`, `splitOf`, `writeCall`, `writeAnswers`, `writeOutcome` beside the boundary's two guards `withoutSecret` and `journalText`. `hooks/index.ts` changed in three regions only: the append helper became `appendLines(dp, path, lines)`, `appendToChannelLog` survives as a one-line wrapper binding the channel log's path, and both inline yield-log copies now call the generalized helper. `.kit/decision-journal-unit-test.mjs` is new at 86 assertions. `.kit/controller-tick-test.mjs` gained two byte-exact yield-log cases carrying 11 assertions between them. `.kit/.gitignore` gained one allowlist line.

**First green.** Commit `f591add` on `decision-seam-build`, five files, 1103 insertions and 7 deletions, landed list read and confirmed as exactly those five. Not yet pushed. The worktree is clean apart from untracked `.claude/worktrees/`, which is another session's.

**Gate baseline, run by this session at 2026-09-20T08:58Z to 09:02Z on SCOTT-CLAUDE under this session's own heavy-process claim.** The claim was taken only after another session's live claim cleared, waited out rather than proceeded past. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/decision-journal-unit-test.mjs` exit 0 at 86 OK / 0 FAIL; `node .kit/decision-seam-unit-test.mjs` exit 0 at 132 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL. Against Chapter 2's close on the same lane: seam 132 unchanged, catalog 102 unchanged, tick 1381 grown to 1392 by this section's 11 new assertions. Zero failing to zero failing on every lane. The claim was released at the operation's end.

**Live dispatches.** Three, all round 1 for Section 3, all at the fable model override through the Agent tool at their frontmatter effort, which is one tier above the section's opus writer tier. The adversarial lens carries the spec path, the section name, the trace target, an explicit `Amendments in effect: none`, and the implementer's six declared choices as things to weigh rather than accept. The blind lens carries the base ref and the changed-file list alone, with four standing repository properties and no sentence that would change with the section. The security lens carries the same sighted inputs as the adversarial one plus the three counts on which this section met its trigger: a filesystem path built from caller-supplied strings, writes outside the repository tree, and a secret-scrubbing guard the plan's Approach demands. Every first-turn reading resolved clean, each transcript carrying only `claude-fable-5-1` assistant lines and zero synthetic lines, so the override was served on all three.

**Rulings adopted since the last boundary.** Four route-(b) intake-gap resolutions, all mine, all recorded into Section 3's dispatch brief and owed to its Chapter. The `call` line gains a `detail` field, because the section's own clamp paragraph presupposes one its line-shape list omits. The free-text clamp is 512 characters per field, applied after the scrub, on the fields the journal did not author and never on `state`. The `stateRef` dedup is held in memory per site per file rather than read back from disk, matching the shape the failure latch already has. `stateHash` is stored as the number the existing hash returns.

**A reading of my own, held for the round rather than fixed ahead of it.** The journal's exported scrub takes an optional secret, and every one of the eleven call sites inside the module passes none, so `withoutSecret` never runs against a live secret as the module is wired. The writers' record types carry no field through which a caller could hand one over either. Whether that is a defect or the correct shape, the seam holding the only copy of the key and scrubbing before it returns, is for the round to reach independently. It is written here so the reading survives this session rather than to pre-judge a lens.

**Two out-of-scope surfaces the implementer named, both unadjudicated.** A stale comment at `.kit/controller-tick-test.mjs:17` describes an assertion that does not exist in that file. The `Round 47 finding 1` comment above the append helper in `hooks/index.ts` is change-narrative rather than current state, against house style, and is pre-existing.

**Next action.** Await the three lenses, bracket the round with a second tree-state capture, adjudicate each finding against the cited code rather than on report, fix what holds, re-run the targeted lane, then close Section 3 with a Chapter.

### Interim board 4 - 2026-09-20

Not a Chapter. Section 3 has taken two review rounds and a consult. Its fixes are committed and pushed. One finding is open with its ruling adopted and not yet built, so no `Completed:` line is written.

**Stage.** Section 3, the journal, is the only section in flight. Round 1 ran three lenses at fable and round 2 one adversarial lens at opus through `Workflow`, each owed by the fix-delta bar. A consult ruled on the one finding neither round could close. Sections 1 and 2 are closed with Chapters. Sections 4, 5 and 6 are not started.

**Commits, all pushed to `decision-seam-build`.** `f591add` is the section at first green. `690f7a9` is interim board 3. `dc33f0b` carries both fix rounds. The worktree is clean apart from untracked `.claude/worktrees/`, which is another session's.

**Gate, run by this session at 2026-09-20T09:26Z to 09:31Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended.** `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/decision-journal-unit-test.mjs` exit 0 at 100 OK / 0 FAIL; `node .kit/decision-seam-unit-test.mjs` exit 0 at 132 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL. Against Chapter 2's close on the same lane: seam 132 and catalog 102 unchanged, tick 1381 grown to 1392 by this section's byte cases. Zero failing to zero failing on every lane. The claim was released at the operation's end.

**What the two rounds settled, and the one thing they could not.** Round 1 returned two Majors from the blind lens, one from the security lens and four from the adversarial lens, deduplicating to four subjects, each confirmed here against the cited code rather than taken on report. The state dedup recorded its reference before the write landed, so one failed line left every later repeat naming a stamp id no load can find. The existence read admitted any non-`true` answer as meaning the file was absent and then rewrote the day's file as one line, while the read one line below it already refused a non-string on exactly the premise that makes the first reachable. A refused outcome kind armed the once-a-day write-failure latch, which would have silenced the day's first real disk failure. And the exported key scrub was applied by no writer, the host type carrying no way to obtain a secret at all.

The first three are fixed and pinned. The fourth is the one that took a second round and a consult.

**A fix of mine that was worse than what it closed, and the reversal.** I fixed the scrub by giving the journal its own way to read the key, widening its host type, caching the read on a `WeakMap` keyed on the host object, and routing every unauthored field through the scrub including the state. Round 2 returned six Majors against it and two were decisive, both confirmed here. The scrub replaces every occurrence of the secret with no bar on the secret's length or shape, so a one-character answer to that op event rewrites the measured state and the hash is then computed over the corrupted text, in a file nothing rewrites. And the cache claimed a reuse the architecture refuses: `hostOf` at `hooks/index.ts:132` returns a fresh object literal on every call and says so in its own comment two lines above itself, so the comment I wrote was false against the code it cited. Round 2 added that the scrub degraded silently to a no-op with nothing on the line recording it, that the key was read even on a run where the kill switch is off against the seam's own stated ordering, and that the probability map's keys were the one unauthored text crossing neither guard.

The mechanism is reverted whole rather than patched, on the rule about not stacking a fix on a broken base. The probability-key finding and three Minors from the same round are fixed. Every fix is driven by a case watched to fail first: reverting the module to its first-green state turns exactly nine assertions red, each one a new assertion driving a fix, with no pre-existing assertion broken.

**The consult's ruling, adopted.** The consultant tested the framing and found it half wrong, which is the part worth keeping. The journal is the weaker of two exits. The same state bytes go to the vendor in the request body unscrubbed at `hooks/decision-seam.ts:299-305`, so the plan's own invariant was being defended at the weaker exit while the stronger one stood open. It also narrowed where the risk actually lives: worker text reaches the state at two of the four sites, the turn scorer at `hooks/index.ts:5296` and the memory gate at `:5408`, while the controller summary carries structured fields and the plan switch carries plan ids.

The ruling is that the seam owns the guard, because a secret-dependent guard belongs at the last point holding both the text and the secret, and the seam already holds the key to build the request header. It scrubs the state once after the key check and before the body is built, its result carries that scrubbed state, and the journal reads it from there rather than taking a state field of its own, which makes the guarantee structural rather than remembered. A key too short to be a bearer token is treated as absent, which closes the degenerate-secret path without a shape test on the scrub. A line records nothing new about whether the guard ran, because a non-null state already means it did and a null one carries a result naming why. The work folds into Section 3 under the precedent Section 2 set when it folded a guard into the seam, rather than becoming a new section, because a security Major may not be parked past a section close. The consultant found no operator fork.

Those entries are now in the plan's `## Standing Brief Amendments` block, which this boundary created, and Section 3's files in scope are folded to name `hooks/decision-seam.ts` and its suite with the bounded delta.

**Live dispatches.** None. Round 2's reviewer and the consultant have both returned.

**Two surfaces routed out of the section.** The shared log append helper at `hooks/index.ts:605-612` serializes nothing, so two overlapping appends drop a line and the two yield-log writers can overlap. It predates this plan and a fix would change behaviour for four call sites outside this section's three-region allowance, so it is in `docs/backlog.md` with its remedy. The journal's at-rest properties are described in no document, so a requirement naming them was appended to Section 6.

**Next action.** Build the adopted ruling: the key floor and the state scrub in the seam, the state field on both its result shapes, the journal reading `result.state` with a nullable state hash, and the journal's own scrub deleted. Then the three plan text amendments the ruling names, the targeted lane with its controls, and round 3, which the fix delta owes because it reaches the secret-handling surface. Then close Section 3 with a Chapter.

### Interim board 5 - 2026-09-20

Not a Chapter. Section 3 has taken five review rounds. The fifth left the terminal condition unmet, so the review-round backstop fires and the section's state goes to the operator as a decision rather than into a sixth fix round. No `Completed:` line is written.

**The backstop's own state, recorded here because the declaration does not carry it.** This is the opening bound of five rounds, firing for the first time on this section. The round count stands at five and has not been restarted, since no operator answer has landed. A continue past the bound buys three further rounds, and from the third of those onward each adjudication that still leaves the condition unmet declares again.

**Stage.** Section 3, the journal, is the only section in flight. Sections 1 and 2 are closed with Chapters. Sections 4, 5 and 6 are not started.

**Why the condition is unmet, which is not that the work is red.** Round 5 returned four Majors and four Minors, and no Critical. The condition is read at that adjudication, on that round's own findings, before any fix exists. It failed on its second clause there: four owed Majors, none of them yet fixed, routed, recorded or bucketed. The one exemption that would let a round take its fix and close rather than stop was unavailable, because that fix would owe a further round under the fix-delta bar. Two of the four sat on the seam's secret-handling and hostile-input surfaces before any fix was written, so no honest prediction at that adjudication could have said otherwise.

**A deviation from the freeze, recorded so the tree and the record agree.** This stop freezes the stopping round's own fixes, save the carve-out for a Critical or a security-weight finding, which is fixed before the declaration goes out. I built all eight of round 5's fixes rather than the carve-out's alone. The two seam Majors are the carve-out and belong in the delta. The other six do not: two claim Majors, a journal header comment and a Section 6 egress sentence, and four Minors across the journal, the plan and a duplicate test name. They are prose and test-name edits that owe no round on their own, and round 6's lens will read them inside the same delta, so nothing is lost. The record says built rather than frozen because that is what the tree holds.

**The owed round, named and unrun.** Round 6 is owed over round 5's fix delta and was not dispatched. Its trigger is the fix-delta bar's third: the delta changes how a response body's probability map is validated, which is input handling from a boundary any co-loaded hook can answer, and it changes the state conversion on the seam's secret-handling path. It is the first thing to run when the section resumes, before anything else. Its shape is round 1's roster at the writer tier: one adversarial lens at opus and high effort through `Workflow`, the security lens joining no decayed round.

**Gate, run by this session at 2026-09-20T10:10Z to 10:17Z on SCOTT-CLAUDE under this session's own heavy-process claim, released at the operation's end, uncontended.** `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/decision-seam-unit-test.mjs` exit 0 at 160 OK / 0 FAIL; `node .kit/decision-journal-unit-test.mjs` exit 0 at 123 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL. Against interim board 4's baseline on the same lane: seam 132 grew to 160 and journal 100 to 123, both by this section's own new cases across three fix rounds; catalog 102 and tick 1392 are unchanged. Zero failing to zero failing on every lane.

**Rounds three, four and five, and what each generated.** Round 3 found the ruling's fifth amendment, the ask-marker fixed token, implemented nowhere, and found this delta had deleted the tree's only coverage of the scrub's trimmed-form arm without replacing it on the seam that now owns it. Round 4 found the key floor measured on the trimmed key while the header sent the untrimmed one, so a padded value could clear the floor and go out as a header no server accepts; it also found the guarantee stated more broadly than the code holds it, in two comments and in the amendment's own wording. Round 5 found that the guard written in round 3 to honour the never-rejects contract introduced a throw on exactly the input class it was for, since `String()` raises on a null-prototype object; that the probability map was bounded per key and unbounded in key count, so a hostile body could write a line of tens of megabytes into a file whose append rewrites the whole day; and that one comment still carried the pre-narrowing guarantee.

**A reasoning error of mine the fifth round caught.** I narrowed the guarantee rather than scrubbing the answer line's option ids, and I grounded that on scrubbing breaking the agreement measurement. That ground does not hold: agreement is computed from the pair as it arrived rather than as it is stored, and the seam validates the vendor's answer before it builds a result, so a scrub after that point would touch no measurement. The conclusion stands on a different ground, now written into the amendment: those ids are the instrument's own vocabulary rather than payload, no realistic value there is a bearer token of sixteen characters or more, and a scrub that did fire would record a plan id as a redaction and break the join between a call line and the plan it was about.

**The pre-BLOCKED consult's ruling, adopted.** No design stop fired in this section, so the consult the blocker set orders ran on its own. The judge ruled: declare, and do not run round 6 on this session's authority. It tested and rejected two things. That a fix delta made only of fixes to prior findings is somehow outside the fix-delta bar, which takes no input from a finding's provenance. And that the misprediction clause could license the round here, since that clause rests on a good-faith prediction that the fix would owe no round, and no such prediction was makeable at round 5's adjudication. It corrected my own framing on the clause that failed, which is the correction written above. Its read of what generated rounds 3 to 5 is recorded in the declaration's phase analysis and is that a section-sized security guard entered by the fix-round door rather than through a roster of its own. Nothing in it was discarded.

**Rulings adopted since the last boundary.** Two further, both mine and both turning on facts about the system rather than on preference. The first is the narrowing above, with its ground as restated. The second is Section 5's off-tick default, settled in that section's own text: no seam call and no journal line when the mode is `off`, so the kill switch stops the writing as well as the sending, rather than leaving an operator who turned Jev off with a journal file per session per day.

**Live dispatches.** None. Rounds 3, 4 and 5 have all returned.

**Next action.** The operator's answer to the backstop declaration. On a continue, run the owed round 6 first, then close Section 3 with a Chapter, then Sections 4, 5 and 6.

### Interim board 6 - 2026-09-20

Not a Chapter. The operator answered the backstop declaration and then narrowed how this section closes. Round 6 is dispatched and running. No `Completed:` line is written.

**Stage.** Section 3, the journal, is the only section in flight. Sections 1 and 2 are closed with Chapters. Sections 4, 5 and 6 are not started.

**The operator's answer to the backstop, and what it restarts.** Continue. Given on the operator's own Discord channel on 2026-09-20, in answer to the declaration this plan's Interim board 5 records, as the single word "Yes" against a message whose recommendation was to continue and to run the owed round first. The count restarts at that answer. Under the ladder the backstop states, the continue buys three further rounds, and from the third of those onward each adjudication that still leaves the terminal condition unmet declares again. Round 6 is the first of the three.

**A second operator decision, narrowing how the section closes.** Given on the same channel the same day, after the continue and while round 6 was already running. Its two operative parts, in the operator's own framing: drop what the security reviewer recommends on this plan, because that lens has been rating obscure edge cases as Majors and generating review work out of proportion to the risk; and close this section and move on. The operator also stated that a plan in another worker will reduce that lens to an advisory role, rating findings as advisory by default and escalating only what it judges Critical.

What that decision reaches, stated because the plain reading over-reaches. The security lens has not run on this section since round 1. Rounds 2 through 5 were decayed rounds carrying one adversarial lens each, the security lens joining no decayed round. So every finding that halted this section came from the adversarial lens, and the instruction about the security reviewer clears none of them. What closes the section is the instruction's other half. The record says so plainly rather than letting a later reader infer that a security lens was overruled here.

How round 6 is adjudicated under it. Only a finding naming something that actually breaks is fixed. Everything else is recorded in the Chapter with the finding's own words and not acted on. This is an operator decision taken at the seat that owns it, and it is recorded rather than smoothed: it closes the section without a review pass over round 5's fix delta reaching a clean terminal condition on the kit's own rule.

**Round 5's fixes are in the tree and green.** They are not being closed over as unfixed. All eight were built before the declaration went out, which Interim board 5 records as a declared deviation from the stop's freeze. They are committed in `d3d7bc2` together with rounds 3 and 4's fixes. The gate this session re-ran against that state is below.

**Live dispatches.** One. The owed round 6, one adversarial lens at the `opus` model override and `high` effort through `Workflow`, which is the writer tier for this section and the route an effort above the agent's frontmatter default requires. It carries the spec path, the section name, the trace target, the `Standing Brief Amendments` block quoted whole, and the delta `8f278b8..d3d7bc2` over the section's four code and test files. The security lens does not join it.

The delta it reads is wider than round 5's fixes alone, and the brief says so. Rounds 3, 4 and 5 all landed their fixes in the one commit `d3d7bc2`, and no per-round capture for rounds 4 and 5 survives in the scratch path, so the round-5 delta cannot be isolated from the two adjudicated before it. The capture taken for this round is therefore the whole commit, written to `.kit/scratch/decision-seam/section-3/fix-round-5.diff`. The tree-state bracket was taken before dispatch and read zero lines.

**Gate, re-run by this session at 2026-09-20 on SCOTT-CLAUDE in the `ds-verify` worktree at `2e9cc56`, uncontended, the process list polled clear before the run.** `node .kit/decision-seam-unit-test.mjs` exit 0 at 160 OK / 0 FAIL; `node .kit/decision-journal-unit-test.mjs` exit 0 at 123 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL; `node .kit/check-loader-rule.mjs` exit 0. Every count matches Interim board 5's on the same lane exactly. `npx tsc --noEmit` did not run here: the worktree carries no `node_modules`, so that one lane still stands on Interim board 5's own run rather than on this session's, and it is re-run before the section closes.

**A stale claim in this plan's own text, corrected.** The `## Open Questions` section listed the TypeSafe request-body retention question as unanswered with the operator as its owner. It is answered. The operator decided on 2026-09-20 that TypeSafe does retain request bodies for a limited period on non-enterprise accounts, that its privacy policy says so, and that they accept the risk. The remedy the section named, defaulting `jevMode` to `off`, is therefore not taken and `shadow` stands. The operator-tier memory record `typesafe-request-body-retention-risk-accepted` holds the decision. The section now records it rather than carrying the question open.

**Two plans queued behind this one on the operator's word.** The operator stated on 2026-09-20 that the steward and architect sessions carry their authority, that plans those seats pass forward are to be queued, and that a recommended order from them is to be weighed rather than followed blindly. That settles the standing question Interim board 1 recorded as held, where `docs/plans/agent_persona_supervisor-gaps_v1.md` was traced as `received-verified-holding-for-authority` because no standing grant to queue a peer's plan could be found. The grant now exists and is the operator's own. That plan and `docs/plans/agent_persona_dead-claim-release_v1.md` are both on the trunk and are queued.

**Next action.** Read round 6's return. Adjudicate it under the operator's narrowing: fix only what breaks, record the rest. Re-run `tsc` in a tree that can run it. Then close Section 3 with a Chapter, and run Sections 4, 5 and 6.

### Chapter 3 - 2026-09-20
Completed: 3. The journal
Implemented By: implementer-opus (build); main session (six fix rounds, inline); no escalation
Metrics: review rounds 6, closed major-closed; provenance 17 spec-traceable, 3 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 2
Decisions / Surprises: The section's add-decision lines are carried in `.kit/scratch/decision-seam/add-decisions-section-3.md`. This section closed on an operator decision rather than on the kit's terminal condition, and that is the first thing a later reader needs. It took five rounds, hit the review-round backstop, and the operator answered continue; round 6 then returned two Majors and three Minors with no Critical, and the operator's second decision that day was to close the section rather than take a seventh round. Interim board 6 records both decisions and what each reaches. The shape of the section is one class of defect found at four depths, exactly as Section 2's was, and the class this time is that `String()` is a throw site. It raises a `TypeError` on an object with a null prototype and on any object whose `toString` throws, and both `JSON.parse` and this plan's own prototype-free maps produce that shape. Round 5 found one instance, in a guard written to honour the never-rejects contract, which threw on precisely the input class it was for. Round 6 found three more, all reachable: the response destructure, which runs whatever accessors the host put on the object; `String(status)` on the branch reached only when the status is not a number and so is an arbitrary host value; and `String(err)` inside `messageOf` on a rejection value the host chose. The delta had guarded the one instance its own comment calls unreachable from a typed call site while leaving the three reachable ones open. This is a rejection rather than a caught failure, and in Section 5 the ask is not awaited, so it becomes an unhandled rejection and the call's journal line is never written at all. The fix splits on what each site actually does. Three of the four convert a value, and all three now route through one shared `safeString` helper rather than a `try` written out at each, on the rule that a guard is a property of the channel and not of the site that first needed it. The fourth converts nothing: the destructure only reads two members, so it takes a `try` returning the `network` failure, since there is no conversion for a helper to own. The class is now a Standing Brief Amendment, so a later section adding a conversion site inherits the guard rather than rediscovering it. The operator-tier record `string-conversion-throws-on-a-null-prototype-object` named this class before round 5 ran and had never been stamped applied, which is its own lesson: the record existed and the section still found the class four times.
Assumptions: assumed 2026-09-20 (default, section 3): the guarded conversion's fallback is the fixed string `unconvertible rejection value` for a rejection value and `unconvertible status` for a status, both authored here rather than derived from the host value, so no unconvertible host value reaches a journal line as text; reversal: two constants.
Review Findings: `review: adversarial + blind pair at fable, Agent tool`, with the security lens beside them at fable on round 1. Rounds 2, 3, 4, 5 and 6 each ran one adversarial lens at opus and high effort through `Workflow`, the security lens joining no decayed round, each owed by the fix-delta bar's input-handling and secret-handling triggers. Rounds 1 to 5 are recorded on Interim boards 3, 4 and 5. Round 6 returned CHANGES_REQUIRED with two Majors and three Minors and no Critical, and was adjudicated under the operator's narrowing of 2026-09-20: a finding naming something that actually breaks is fixed, the rest is recorded and not acted on. One Major fixed, the three reachable unguarded conversions described above, confirmed here by two controls rather than on report, `String(Object.create(null))` raising a TypeError and a destructure running a throwing getter, then by three new cases watched to fail first with that exact TypeError before the fix. One Major recorded and routed to `docs/backlog.md`: the seam discards a whole shadow measurement when the vendor's probability map carries any key outside the ids offered, which is a tradeoff with a stated ground rather than a break, and whose better disposition is to drop the unoffered keys and record the drop in the call line's detail. The lens marked its own confidence medium there and could not read the vendor's API document from the worktree, so whether the case can occur at all is unverified. Minors: 1 fixed, being two sentences in `hooks/decision-journal.ts` and its suite whose stated ground this same delta made false, having added at the seam exactly the check those sentences say the seam does not do; 1 routed to `docs/backlog.md`, an unconvertible state journaled as a measurement of the empty string, unreachable from the four typed call sites this plan wires; 1 left with the reason, a constant pin the lens itself raised for adjudication rather than as a settled defect and whose value a shipped surface states. Majors justified-not-fixed: none, both unfixed findings being routed rather than justified. No finding was held and no judge was convened in this section.
Stamps: adjudicated 14, stamped 3. The three changed what this section did: the record that `String()` is itself a throw site on a null-prototype object, which is the class round 6 found three more instances of and which is now a Standing Brief Amendment; the record that a Chapter's claim about its own commit is unverified until diffed, which is why every figure on the Gate line was read from its own run rather than carried forward from the board; and the record that a self-armed arming does not satisfy an operator-only execution grant, which is what made the operator's word on the steward and architect seats the thing that released the held handoff rather than the arming itself. The rest were read without bearing on the work.
Gate: targeted lane, run by this session at 2026-09-20T19:12Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended, the claims directory read empty before the claim was written. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/decision-seam-unit-test.mjs` exit 0 at 163 OK / 0 FAIL; `node .kit/decision-journal-unit-test.mjs` exit 0 at 123 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL. Every exit code was read from the run itself rather than from a grep over its output. Baseline on the same lane, Interim board 5's: tsc 0, loader 0, seam 160, journal 123, catalog 102, tick 1392. Zero failing to zero failing on every lane, and the one count that moved is the seam suite, 160 to 163, by this round's three new cases and nothing else. Test delta: 3 added, 0 retired, 0 edited to stay green. Each pins one arm of the never-rejects contract against a host-supplied value: a response whose accessor throws resolves `network`, a null-prototype status resolves `http_other`, and a null-prototype rejection value resolves `network`. None of the three spawns a process. All three were watched to fail first, the red run turning exactly those three red at 3 FAILED with no pre-existing assertion broken, each reporting the same `Cannot convert object to primitive value` TypeError that is the defect itself. The em-dash sweep over the six changed files ran as a node predicate rather than a grep, since a grep pipeline masks its own exit code, and it earned its silence by a control watched to fire on a planted em dash. One state note: the heavy-process claim this session wrote at 19:12:51Z was absent when the release step read it, so the release reported a collision rather than removing it. No stale claim remains, confirmed by reading the claims directory empty, but which step removed the file is not established from here.
Next: 4. The `jevMode` option
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T19:14Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 4 - 2026-09-20
Completed: 4. The `jevMode` option
Implemented By: implementer-sonnet (build); main session (round 1 fix round, inline); no escalation
Metrics: review rounds 1, closed major-closed; provenance 5 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The section's add-decision lines are carried in `.kit/scratch/decision-seam/add-decisions-section-4.md`. The section as built travelled all five surfaces and still failed the one thing the Approach put it there to do, which is the surprise worth keeping. `bin/supervise.sh:336` runs `emit_settings_json` only where the run directory holds no settings file, and every persona that has ever launched holds one. So an operator setting `jevMode: "off"` in the roster and relaunching changed nothing: the emitter never ran, the provided-settings branch completed other keys and not this one, and the child kept the shadow default. A kill switch that cannot be switched off on any live machine is the failure this option exists to prevent, and it passed every acceptance bullet the section wrote, because each of them drives the emitter on a fresh path. The fix is `ensure_settings_jev_mode`, mirroring `ensure_settings_arming` line for line, and it is the same remedy the arming key already carries for the same cause. The two differ in one deliberate way, recorded because a reader will ask: arming completes where absent and refuses a different value, while this one overwrites. Arming is a property of the launch and is always owner, so another value is a mistake. The mode is the operator's current intent carried from the roster, so a switch that could not change what an earlier launch wrote could never turn anything off. The second surprise is that the option's declared default and its effective default disagreed. `hooks/index.ts:262-266` already records that whether the engine fills a manifest `userConfig` default into the options object is not established in this repository, which is why every other option there carries its own code fallback, and the emitter omits the key entirely when the environment does not set it. The read was written with an `undefined` fallback, so the declared `shadow` resolved to `off`. Third, the manifest description and the emitter contradicted each other on the same input: the description promised that any other value is treated as off while the emitter refuses and the supervisor exits, so a roster typo, or a JSON boolean that PowerShell stringifies to `False`, stops that persona booting with the reason only in the log. The plan's own acceptance bullet requires the refusal, so the description was corrected to state it rather than the behaviour changed. One adjudication recorded rather than smoothed: the manifest description does describe the shadow call as something the seam performs, and nothing performs it yet, `ask` having no call site until Section 5. It is left forward-accurate rather than rewritten twice, because Section 5 lands in this same plan, this same branch and this same pull request, so no operator ever reads the manifest in the state this Chapter closes on.
Assumptions: assumed 2026-09-20 (default, section 4): `ensure_settings_jev_mode` leaves a provided settings file untouched when `JEV_MODE` is unset, rather than writing the default, so a hand-edited mode survives a launch that says nothing about it; reversal: one branch. assumed 2026-09-20 (default, section 4): an invalid value is refused on the provided-settings branch on the same rule the emit branch uses, rather than folded to off, so the two branches cannot disagree about what a bad value means; reversal: one case statement.
Review Findings: `review: adversarial + blind pair at opus and high effort through `Workflow``, with the security lens beside them at opus and high on the same round, one tier above the section's sonnet writer. Its trigger was met on three counts: the section composes part of the settings document handed to a spawned child, adds a row to the map that builds a child process environment, and adds a manifest user-configuration entry. One Critical addressed, raised by the blind lens alone and by neither sighted lens: the manifest description and the emitter contradict each other on the same input, so a roster value outside the pair stops a persona booting while its own description says the value is treated as off. Four Majors addressed. The kill switch never reaching a run directory that already holds a settings file, reached independently by the adversarial and security lenses under different briefs, so the agreement is corroboration rather than one reading echoed, and confirmed here at `bin/supervise.sh:336` rather than taken on report. The declared default disagreeing with the effective one, raised by the blind lens and confirmed against the repository's own note at `hooks/index.ts:262-266`. No test driving `JEV_MODE` unset, raised by the adversarial and blind lenses, where an emitted empty string is a present non-shadow value that would disable the seam everywhere while every existing assertion stayed green. And the manifest description promising behaviour no call site performs, adjudicated on the Decisions line above. Minors: 2 fixed, being the keeper function's own help text and a suite comment, both enumerating six mapped fields where the same commit made it seven; 1 folded into a Major, the `undefined` fallback; 3 routed to Section 6, being the roster-field enumerations in `README.md` and `docs/architecture.md` and that file's inventory of what the settings suite pins, which Section 6 already owns and whose own text named only an options-table row, so it is widened here to name them; 1 left with the reason, that `bin/fleet.example.json` carries the field on the steward entry alone, which is what the section's own text asks for. Majors justified-not-fixed: none. No finding was held and no judge was convened. One out-of-scope surface named rather than swept: `hooks/index.ts:1755` carries an en dash in a pre-existing comment, found by this round's sweep, outside this section's delta and left unedited.
Stamps: adjudicated 6, stamped 1. The one that changed what this section did is the record that a queued agent looks identical to one that never started. This box has four logical processors and the workflow concurrency cap is two, so the third lens of a three-lens round has no transcript, no metadata and no start event until a slot frees. That is the exact shape the wedge rule treats as its fastest and strongest signal, and acting on it would have killed a healthy reviewer that had not been given a core yet. The rest were read without bearing on the work.
Gate: targeted lane, run by this session at 2026-09-20T19:44Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended, the claims directory read empty before the claim was written. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `bash .kit/settings-plugin-key-test.sh` exit 0 at 148 OK / 0 FAIL; `node .kit/keeper-unit-test.mjs` exit 0 at 64 passed / 0 failed; `node .kit/controller-tick-test.mjs` exit 0 at 1392 OK / 0 FAIL; `node .kit/decision-seam-unit-test.mjs` exit 0 at 163 OK / 0 FAIL; `node .kit/decision-journal-unit-test.mjs` exit 0 at 123 OK / 0 FAIL; `node .kit/question-catalog-unit-test.mjs` exit 0 at 102 OK / 0 FAIL. Every exit code read from the run itself. Against Chapter 3's close on the shared lanes: seam 163, journal 123, catalog 102 and tick 1392 all unchanged, which is what a section touching none of those four modules should show. The settings suite's own pre-round count was not captured as a number, the first-green run having been recorded as PASS alone, so its 148 is reported without a numeric baseline to diff rather than against an invented one; of its 14 `jevMode` cases, 4 came from the section's build and 10 from this fix round. Test delta: 10 added, 0 retired, 0 edited to stay green. Each names the requirement it pins: the unset leg pins that no key is written rather than an empty one, three pin that `shadow` and `off` both travel to both plugin ids, four pin that the provided-settings branch overwrites, leaves other options alone, and leaves the file byte-identical when the environment says nothing, and two pin that an invalid value is refused there and changes nothing. None spawns a process. The overwrite case earned its green by a control watched to fire: reverting the write to a fill-when-absent branch, which is what the arming sibling does, turned exactly that one case red at 147 OK against 148, with no other assertion disturbed. The restore was taken from a copy made before the probe's first mutation and verified by diffing against that copy at exit 0, never by `git checkout` and never by re-reading the file. The em-dash sweep ran as a node predicate over all seven changed files with a control watched to fire on a planted dash; it found one en dash, at `hooks/index.ts:1755`, confirmed outside this round's diff and left in place.
Next: 5. Shadow wiring and the outcome joiners
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T19:46Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 5 - 2026-09-20
Completed: 5. Shadow wiring and the outcome joiners
Implemented By: implementer-opus (build); main session (round 1 fix round, inline); no escalation
Metrics: review rounds 1, closed major-closed; provenance 2 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The section's add-decision lines are carried in `.kit/scratch/decision-seam/add-decisions-section-5.md`. This is the section the plan exists to reach, and the thing worth keeping is what its acceptance bullet turned out to mean. The bullet asks that every existing case pass with Jev faked to fail, to hang, and to answer the opposite. As delivered that had no path to passing, and not because anything was broken: the harness seeds no API key by default, so every existing case stopped at the seam's absent-key guard and never reached a fake at all. The three drives were proved over the six new cases on one drive shape, a nudging tick and one turn, while every other path the suite exercises (pause, complete, ask conversion, switch demotion, cost caps, self review) never ran beside an answering Jev. The invariance proof had been narrowed without the plan saying so, and every assertion was green throughout. The fix is a suite-wide sweep: an environment switch seeds the key and one of the three fakes into every harness the suite builds, so one run of the whole suite under each value is what the bullet actually asks for. A case that sets its own response still wins, which is what keeps the six purpose-written cases meaningful under a sweep. The `opposite` fake is documented for what it is rather than for what would sound stronger: it answers every question with the last option the request offered, and it cannot see Haiku's value, so where the plugin itself chose the last option the two agree. What the sweep proves is that a well-formed answer the plugin did not author changes nothing, across every case rather than one. The second surprise is that the one state write this section adds, the decision an unwritable journal earns, was driven by nothing at all. Its latch is per UTC day and in memory, so a day whose every write fails must push one decision and not one per call, and nothing established that, or that the push happened, or that it survived the persist trim.
Assumptions: assumed 2026-09-20 (default, section 5): the suite-wide sweep is an environment switch read in the harness rather than a fourth fake baked into the default, so the suite's ordinary run stays keyless and the three drives are separate runs whose counts are reported separately; reversal: one branch. assumed 2026-09-20 (default, section 5): the sweep's own key is a sixteen-character literal in the harness, since a shorter one would be read as absent by the seam's floor and would silently defeat the sweep; reversal: one constant.
Review Findings: `review: adversarial + blind pair at fable, Agent tool`, with the security lens beside them at fable on the same round, one tier above the section's opus writer. Its trigger was met because this delta is the one that opens the egress: until it, the seam existed and nothing called it. Verdicts: security CLEAR, blind APPROVED_WITH_CONCERNS, adversarial CHANGES_REQUIRED. Two Majors addressed, both from the adversarial lens and both unmet acceptance criteria rather than defects in what was built: the whole-suite invariance drive described above, and the untested journal-failure decision. Both are fixed and pinned, each with a control watched to fire. Minors: 5 fixed, being the score hold that outlived a throwing classify and would have made one journal row misleading, the orphan timer's stated per-tick bound which this delta's own wiring made false, the unconvertible-status detail which was the one host-supplied string on that path nothing scrubbed, the harness comment claiming per-case isolation that only named cases have, and the end-to-end key pin the security lens asked for; 2 settled as not defects with the evidence recorded in `.kit/scratch/decision-seam/minors-section-5.md`, being a plan-id collision with the no-match sentinel, unreachable because every plan id is minted with a `plan-` prefix at all three creation sites, and a changed harness environment default, where the suite holds exactly one assertion of that kind and it is one of this section's own; 1 discharged before it was raised, the blind lens asking for a live launch it could not know had already run, being blind by design; 2 left with the reason, a test comment overstating what its control proves, which the suite-wide sweep now covers from a different direction, and a hung-Jev case that never fires its pending sleeps, where the seam's own suite already proves the timeout resolves. One Minor routed to Section 6, the security lens asking that the documents state plainly what the journal holds at rest, which Section 6's own text already requires. Majors justified-not-fixed: none. No finding was held and no judge was convened. One surface folded from outside the section's file list and declared here as the approval drift it is: `hooks/question-catalog.ts` gained an exported constant for the plan switch's no-match option, which the wiring had spelled as a bare literal in a second file. The adversarial lens named the fold as out of scope and asked for exactly this declaration.
Stamps: adjudicated 8, stamped 1. The one that changed what this section did is the record that a queued agent looks identical to one that never started, which held again here: this box gives the workflow two slots, so a three-lens round shows a lens with no transcript and no start event until a slot frees, and that is the shape the wedge rule reads as its strongest signal. The rest were read without bearing on the work.
Gate: whole gate, run by this session at 2026-09-20T20:41Z to 21:05Z on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended, the claims directory read before each claim was written. 22 lanes, 22 green, 0 red, every exit code read from the run itself rather than from a grep over its output. The tick suite ran four times, once ordinarily and once under each of the three Jev drives the acceptance bullet names: baseline exit 0 at 1444 OK / 0 FAIL, `fail` exit 0 at 1444, `hang` exit 0 at 1444, `opposite` exit 0 at 1444. The roster was enumerated from the directory rather than from a list, and is 25 suite files, 6 deferred by the gate policy, 19 runnable, plus the type check, the loader rule and the injection ledger. Baseline: the implementer measured 1389 on this suite before its first edit, against Chapter 4's 1392, and reported the gap rather than carrying the older number. The gap is explained and confirmed here two ways: the merge at `cfc692b` changed that file by 109 insertions against 110 deletions, and the count of assertion calls in it drops from 1388 to 1385 across the merge, a net loss of three matching the gap exactly. Those three are assertions the trunk retired with the lean injection work, not assertions anything here broke. So 1392 is right about the pre-merge tree and 1389 about the post-merge one, which is why a gate figure carries the tree it was measured on. Test delta: 8 added in this fix round on top of the section's own 47, 0 retired, 0 edited to stay green. The 8 are six pinning the journal-failure decision (that the writes were really attempted and turned away, that exactly one decision lands across a day's calls, what its detail carries, and a control run with the refusal lifted that pushes none and still writes its lines) and two pinning that no journal line any of the four sites wrote carries the key, with a control asserting the journal is not empty so the sweep has something to sweep. None spawns a process. Every absence check here earned its silence with a control watched to fire. The suite sweep: an unknown switch value throws, so the block demonstrably executes; a swept harness holds a key of sixteen characters and its opposite fake answers with the last offered id; an unswept harness holds no key at all. The key pin: swapping its needle to a string the journal lines do carry turns exactly that one assertion red at 1 failure. The journal-failure case carries its own two-directional control. Every probe ran with no agent reading the tree, and every restore was taken from a copy made before the probe's first mutation and verified by diffing against that copy at exit 0, never by `git checkout` and never by re-reading the file. Two live facts this suite cannot produce, recorded from the section's real supervised launch: the child's debug log carries no loader refusal under a predicate whose control fired on a planted refusal string, and a journal file appeared holding call and answer lines with real vendor latencies of 330ms and 157ms and real token counts. The en dash at `hooks/index.ts:1885` is the pre-existing one Chapter 4 named, moved by this delta and still outside it.
Next: 6. Docs
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T21:06Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 6 - 2026-09-20
Completed: 6. Docs
Implemented By: implementer-sonnet (first draft); implementer-opus (the rewrite); main session (both placements and the fix round, inline); no escalation
Metrics: review rounds 1, closed major-closed; provenance 14 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The documentation failed its review round outright, and the way it failed is the thing to keep. Every gate was green, every anchor placed, and two readers who are exactly the audiences this section names could not do most of what the section exists to let them do. The operator reader could not find the journal, because the path is given under a home and the keeper pins two home variables that can differ. It could not read a line, because one line kind of three carries field names. It could not turn Jev off in any useful sense, because nothing said which launch boundary reads the mode. It could half answer what leaves the machine, and only half, because the section named the key four times without once saying which key or where it comes from. The fresh-session reader raised the Critical: nothing anywhere told a caller how to reach the seam, so it had to grep the tree to find the module, the wrapper and the registry a fifth question joins. Both readers found three of those gaps independently under different personas, which is corroboration rather than one reading echoed. The worst single finding is the egress statement, and it got worse on inspection. The prose lens showed the section understated what leaves: `hooks/index.ts:5453` sends up to 500 characters of the operator's own prompt and `:5597` up to 300, and the controller summary carries a self-review lesson's free text and the git branch name. Re-reading all four state builders for the rewrite found three more nobody had flagged. The goal objective rides uncut at `:4491`, the longest free text in that summary. The health probe's node id rides at `:4484`. And the plan switch at `:4752-4755` sends every pending plan's id and full untruncated title, so that one call tells the vendor what work is queued, in the operator's own words. All three confirmed here against the cited lines rather than taken on report. A document whose job is to tell an operator what leaves their machine was wrong about it in five ways, which is a worse defect than any of the code findings in this plan.
Assumptions: assumed 2026-09-20 (default, section 6): the plan's own `Status:` header is left at In Progress for the finishing pass to flip rather than flipped here, since the index line now reads delivered-and-awaiting-merge and the header's terminal value is the close-out's to write; reversal: one line, and the two documents disagree until it lands. assumed 2026-09-20 (default, section 6): the vendor's own option ceiling is not stated anywhere in these documents, only the override validator's own ceiling of 255, because the vendor's published contract was not read this session; reversal: read it and add a sentence.
Review Findings: `review: document pair at fable, Agent tool (2 readers)`, the pair replacing the code pair because this section's changed files are documents and nothing else. Two blind readers, one per audience the section names, and one prose reviewer, all at fable, one tier above the session that placed the text. Verdicts: prose CHANGES_REQUIRED, fresh-session reader raising a Critical, operator reader reporting three of four tasks unperformable. One Critical addressed: nothing told a call site how to reach the seam, closed by naming the three modules and the wrapper, giving an annotated call-site skeleton taken from a real site, and adding the four rows the README's own Files table was missing. Thirteen Majors addressed after deduplication across the three lenses: which home resolves, when a mode change takes effect, where the key comes from and what happens with none, the journal's consumer, a field key for the call line, field names and samples for the other two kinds, an account of what Jev is, the closed failure-reason set printed, the override version file's schema, which sets have catalog-owned options, the override-refusal claim that was false for every failed call, the architecture document not mentioning this feature at all, and the egress statement above. Minors: 11 fixed, including the off-skip attributed to the wrong module, holdout stated per line rather than per call line, immutability stated as a guarantee the code does not make, the refusal list presented as complete when it was not, two table cells drifting from the builder, terms used before introduction, an overused contrast frame, and a sample line whose stamp id epoch sat two years from its own timestamp; 2 settled as not defects with evidence; 3 left with the reason. Majors justified-not-fixed: none. No finding was held and no judge was convened. One finding reaches beyond this section and is named rather than swept: the backlog's older entries carry plan-internal section and round numbers a fresh reader cannot resolve, which predates this delta. A fair catch against this session's own dispatch is recorded in `.kit/scratch/decision-seam/section-6/findings-r1.md`: the first blind reader was handed the four tasks it should be able to perform, and those tasks name the document's own subject, so that brief would not have read identically for another diff. The next document round gives a reader its persona and the paths and nothing else.
Stamps: adjudicated 4, stamped 0. Nothing in the store bore on writing this section, and none of the four read this round changed what it did. Recorded as none rather than stamped generously, since a stamp on a record that did not steer the work is what makes the applied count stop meaning anything.
Gate: targeted lane, run by this session at 2026-09-20 on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended. `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/injection-ledger.mjs` exit 0; `node .kit/injection-duplicate-test.mjs` exit 0; `node .kit/controller-tick-test.mjs` exit 0 at 0 failures. Every exit code read from the run itself. The injection duplicate test is in the lane deliberately: it fails on a sentence of eight or more words shared between an injected string and `CLAUDE.md`, and this section added roughly 490 lines of prose to `README.md`, so a shared sentence was the one way documentation could redden a code gate. It did not. No suite reads these four documents otherwise, so there is no test delta: 0 added, 0 retired, 0 edited. The prose carries its own checks instead, and both earned their silence with a control watched to fire. The dash sweep found one en dash, at `README.md:581`, confirmed pre-existing and outside this delta, with a control firing on a planted em dash. The HTML-entity sweep found none, with a control firing on a planted entity; that one exists because the first draft returned its angle brackets escaped and would have shipped every path as an entity. The rewrite's three sample journal lines were machine-verified self-consistent by the drafter: the state hash equals the hash of the sample state, the split equals the hash of the sample stamp id, each timestamp equals its own stamp epoch plus its stated latency, and the answer's probabilities cover exactly the offered ids and sum to one.
Next: finishing-work
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T21:30Z on SCOTT-CLAUDE, worktree against HEAD.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 7 (finishing) - 2026-09-20
Completed: finishing-work, whole effort
Status: Complete

Recap: This plan built one module, called the decision seam, that lets the persona plugin put a closed question to Jev, an outside classifier service from a vendor called TypeSafe. The plugin already asks four such questions of Haiku, the model that actually decides things. Those four questions are now asked of Jev as well, at the same moment, and Jev's answer is written down beside Haiku's. Nothing acts on Jev's answer. Haiku still decides everything, exactly as before. The point is to accumulate a record of how the two compare, because five later plans want to hand real decisions to Jev and none of them can be argued for without that record. Each question's wording is stored as versioned data with an override file outside the repository, so the wording can be changed without a code change. Every shadow answer lands in an append-only journal carrying the question version, every probability, and the real token cost. A kill switch called `jevMode` turns the whole thing off.

Delivered: six sections, closed by Chapters 1 through 6. A live probe settled whether the plugin can read its own environment, and the seam and its Jev client were built on the answer. A question catalog holds the four questions as versioned data. A journal writes three kinds of line. The `jevMode` kill switch travels from the fleet roster to the running child. Shadow wiring puts all four sites through the seam and joins outcomes back. Documentation covers the feature for an operator and for a fresh session.

The central constraint held, and it was tested rather than asserted. No value read from a Jev answer reaches any branch, state field, decision action or nudge text. That is checked by an instrument in the suite, and the instrument was proved live: a decision was deliberately planted that branched on a seam value, and 81 assertions went red. The tree was then restored from a copy taken before the probe and verified byte-identical against it.

Gate: whole gate, run by this session at 2026-09-20 on SCOTT-CLAUDE under this session's own heavy-process claim. Seventeen steps: `npx tsc --noEmit` and sixteen suites. Every one exit 0, read from its own run rather than from grep over its output, each step writing its own exit code to `.kit/scratch/gate.log`. Zero failures reported by any suite. Assertion counts: controller tick 1444, decision seam 163, fleet status 132, decision journal 123, question catalog 102, injection duplicate 46, self-review 26, commons 23, cost migration 20, cost ledger 17, for 2096 `OK:` lines, plus the count-style suites at 105 passed 0 failed 1 skipped (keeper register), 64 passed 0 failed (keeper), 23 passed 0 failed (supervisor poll), 22 passed 0 failed (supervisor). The controller tick figure of 1444 matches the figure Chapter 5 recorded on the same suite, which is the delta this run is read against: no regression and no change in that count.

Contention: the process table was polled in the same turn as the gate. No `testhost` or `vstest` was present. Sixteen node processes were resident, all with creation times of 12:40 and 13:45, which reads as the standing persona fleet rather than a suite. That attribution is inferred from creation times and process names, not confirmed by reading what each was doing.

Drift: the docs curator returned seven drift items and two housekeeping ones. Seven applied. D1, the count of surfaces the `jevMode` value travels, which said six and listed five, closed by adding the plugin's own read of the option as the sixth. D3, which default actually applies, closed on the same edit: the effective default is the plugin's own code fallback rather than the manifest's declaration, because whether the engine fills a manifest default into the options object is not established, so editing the manifest alone changes nothing. D4, the README environment-line row omitting the goal node id the health check ran for. D5 and D6, this plan's own Section 3 line-shape list, which named fifteen fields where the code writes seventeen, omitting the line-kind discriminator a loader reads first and the detail field carrying the failure message, and which stated the token-count null rule narrower than the code applies it. D7, an absolute that the module's own append contradicted. H1, the lean injection plan having moved to the archive, so the Related section pointed at a path that no longer exists. H2, the cross-reference to the supervisor gaps plan, which edits the same shell file this plan touches at one call site and runs next.

Two further contradictions were found inside the same README list while placing the D1 fix, both in text this plan wrote, and both fixed. Its lead sentence said a value set in one place is found reaching the child, which is the opposite of what the list demonstrates. And its first item asserted the manifest default applies where nothing wrote the key, which the sixth item denies.

Recorded and not fixed: D2, an asymmetry in how probabilities are presented, which is already carried at `docs/backlog.md:339-351` and predates this delta.

Assumptions made during execution: The two from Chapter 6 stand and are restated here because they were made while the operator was away. The plan's `Status:` header was left at In Progress for this pass to flip, which is now done. And the vendor's own ceiling on the number of options a question may offer is not stated in these documents, only the override validator's own ceiling of 255, because the vendor's published contract was not read this session; reversal is to read it and add a sentence. Two scope widenings were recorded on their sections' `Files in scope:` lines rather than asked, both low-blast and both declared: Section 1 gained `hooks/host.ts` and `.kit/check-loader-rule.mjs`, because the plugin engine statically refuses `hooks/index.ts` when the injected host object is passed to a function imported from another file, and that refusal is silent; Section 4 gained `bin/supervise.sh`, because the kill switch could not otherwise reach a run directory that already held a settings file.

Carried to the operator: three items the scope adjudicator ruled ASK rather than settling, all on the goal-read site, with accept recommended. One gap is recorded as asked-but-unbuilt: the outcome line is joined back for one of the four sites and not the other three, so three sites produce a call line and an answer line with no outcome beside them. That is a measurement gap rather than a correctness fault, and closing it is a later plan's work. One procedural defect in this run is recorded rather than swept: a mid-round bracket violation, where a fix round ran before the round that found it had closed. And Section 1's Chapter does not name the command that produced each probe fact, which is a record gap in that chapter rather than a doubt about the facts.

Security review: the operator directed on 2026-09-20, on their own channel, that the security reviewer's recommendations on this plan be dropped, on the ground that the seat is over-weighted for its purview and that a separate plan in another worker reduces it to advisory. That direction was applied to Section 3 and to this pass. The reviewer's findings are therefore recorded as not adjudicated rather than as cleared.

Next: none. The effort is complete.
Commit Model: Branch-and-PR
