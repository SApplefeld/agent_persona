# Direct lines to the architect

Status: Complete
Commit Model: Branch-and-PR
Created: 2026-09-22

This is a plan shell. The DEV-PERSONA worker, which owns the agent_persona clone, drafted it because the architect holds no clone of this repository. The architect reviewed the first draft on 2026-09-22, and this revision carries that review, the fresh-context plan review that followed it, and the architect's own second read.

## Goal

When this is done, a worker can put a design question straight to the architect, and the architect can answer that worker straight back. Any session that owns a named persona can send a record to the architect persona with `agentic_say`. It can read its own records there with `agentic_inbox`. Today both work only with the coordinator persona. The architect treats a worker's design ask as its own work item. It answers the worker with `agentic_say`, over a narrow line that reaches only a persona whose record to the architect it has taken delivery of and not yet resolved. Workers are told the line exists.

Direct lines do not remove the architect's clone gate. The architect still clones only a remote URL the operator wrote on the architect's own channel. A worker's ask about a repository the architect holds no clone of ends in an answer returned in the record, or waits on the operator's URL.

It matters because workers report they cannot reach the architect. Every design question a worker has now goes through the coordinator, who re-sends it and relays the answer back.

## Dispatch Authorization

The operator asked for this on 2026-09-22 on the architect persona's Discord thread. The architect quoted the operator's words from its own channel in its review of the first draft. DEV-PERSONA has that quote from the architect's record and not from the thread itself.

> I believe that the sessions should be able to talk to each other in full, and most especially talk to the steward and talk to the architect... at the very least I think that every session should be able to talk directly to you and to the steward... If we have an `in the fleet.json` named who the coordinator and architect sessions are, there should be direct lines of communication available to those sessions.

Execution waits on the architect setting this plan `Ready` and on the operator's word to run it.

## Intent

The operator's frame is the quote above. Sessions should be able to talk to each other, the architect and the steward most of all. Where the fleet's configuration names the coordinator and architect seats, lines to those seats should exist.

Done means four things. A worker reaches the architect without the coordinator in between. The architect works a worker's design ask as its own. The architect answers that worker directly. The worker's own instructions say the line exists and what it is for.

Done does not need a new store, a new tool or a new configuration file. The existing inbox, its existing labels and the two existing settings carry all of it. Done does not change who may read fleet state or restart a persona. It does not widen what the architect may clone.

Alternatives refused:
- Lines between every pair of sessions, which is the operator's wider wish. Deferred rather than refused. The rule that keeps a session on the `default` persona out of every inbox (`hooks/operator.ts:606-611`) exists so that not every plugin-loaded session on the machine can write into a seat's inbox. Widening that deserves its own plan.
- A new fleet file naming the seats. Refused, because the `coordinatorPersona` and `architectPersona` settings already name both.
- Giving the architect the coordinator's leg to any persona. Refused, because that leg is what carries the coordinator's steering standing, and the architect answers rather than steers.
- The architect answering only through `agentic_resolve`'s note. Refused outright, because the note is capped at 2,000 characters (`FREE_TEXT_MAX` in `hooks/index.ts`, a bound the cut lane shares), and a plan review or a consult routinely runs past that. It is not a fallback either: a section that cannot build the answer leg returns to the architect rather than shipping the note as the answer.

Rulings:
- 2026-09-22, the architect, answering the first draft's open questions. The architect answers a worker over a narrow leg. Sessions that own no named persona get no line. The coordinator gets no automatic copy of direct traffic. The steward in the ask is the coordinator persona, which this fleet names STEWARD.
- 2026-09-22, the architect: the coordinator keeps sight of every ask that becomes work, with no copy needed. A plan reaches a worker's queue only through the coordinator, a rule the architect's charter already states.
- 2026-09-22, the architect, on the plan review: there is no resolve-note fallback for the answer leg. The worker's steer sentences and the nudge line exist only where an architect is named, as the coordinator's design clause does today. The coordinator's two relay legs stay.

Provenance: drafted by DEV-PERSONA on 2026-09-22 from the coordinator's relay of the ask and from the code at `origin/main` `dda8afb`. Revised the same day on the architect's review, which reached DEV-PERSONA through the coordinator, and again by the architect on a fresh-context plan review (one Critical, five Majors, three Minors, all applied).

## Related plans

- `docs/archive/agent_persona_steward-architect_v1.md` created the architect seat, its `architectPersona` setting and its charter. That plan's Standing Brief Amendments are cited at `hooks/index.ts:3241`.
- `docs/plans/agent_persona_supervisor-peer_v1.md`, `docs/plans/agent_persona_goal-levels_v1.md` and `docs/plans/agent_persona_keeper-park_v1.md` edit the same priming write in `bin/supervise.sh`, `hooks/index.ts` or `.kit/injection-ledger.json`, so whichever merges second rebases onto this plan's charter text and ledger.
- `docs/plans/agent_persona_deferred-gate-run_v1.md` owns the live suites this plan's gates could not run while the fleet was up.

## Approach

**What exists today.** Each point below was read at `origin/main` `dda8afb` on 2026-09-22. Line numbers move as other plans merge, so find each site by the text quoted or the function named.

- The inbox reach rule has three legs, in `deliveryGroundIn` at `hooks/operator.ts:636-663`, documented at 596-629. A writer reaches a target when it holds a reader claim on that target. It also reaches it when it owns the coordinator persona. Finally, it reaches the coordinator persona when it owns a named persona other than `default`. That last one is the worker leg, and it names the coordinator alone.
- An inbox record lives at `inbox:<persona>:<writerSessionId>:<seq>`, with `from` naming the writer's session and `status` one of `pending`, `delivered`, `answered`, `skipped` or `resolved` (`hooks/operator.ts:9,20,24-39`).
- The architect seat is named in configuration. `.claude-plugin/plugin.json:89-94` declares `architectPersona` beside `coordinatorPersona` at 83-88. Its description says the supervisor is its only reader. `bin/supervise.sh:414-427` reads it and refuses one persona named for both seats.
- `deliveryGroundIn` takes `coordinatorPersona` as a parameter. In `hooks/index.ts`, `agentic_say` reaches it through `mayReachPersona` at 7074, and `agentic_inbox` does the same at 7116. The three delivery sites call it directly at 3826, 3927 and 7427. `fleet_status` at 7179 and `fleet_restart` at 7222 call it with the coordinator as the target.
- Three charters state today's routing, and all three must change together.
  - The architect's charter (`ARCHITECT_ROLE_INSTRUCTION`, `bin/supervise.sh:2987`) says a record from any persona but the coordinator "is information rather than an ask". It also says the architect answers the coordinator "and you never address a worker directly".
  - The coordinator's charter (`bin/supervise.sh:2924`) says "The architect answers you with a record addressed to your persona and never addresses a worker itself, so you relay its answer to the worker that escalated".
  - The worker's steer text (`COORDINATOR_STEER_INSTRUCTION`, `bin/supervise.sh:2822`) names no architect. The idle nudge a worker reads (`hooks/index.ts:5152`) points a genuine fork at an `ASK:` line and nowhere else.
- A worker already reads a `[WORKER:<persona> ...]` prompt as carrying no delegated authority (`bin/supervise.sh:2822`). An answer labelled `WORKER:<architect persona>` therefore grants the architect no standing to steer. That same sentence tells the worker to treat such a prompt as information it cannot verify and to take any act it asks for to the operator first, so a worker reading the architect's answer under it alone would escalate the answer instead of using it.
- `COORDINATOR_STEER_INSTRUCTION` is built unconditionally (`bin/supervise.sh:2819-2822`), while the coordinator's own design clause is appended only where `ARCHITECT_PERSONA` is non-empty (`bin/supervise.sh:2923`). `architectPersona` has no default. The injection ledger (`.kit/injection-ledger.mjs`) counts each instruction variable's assignments and fails when the count moves (`docs/architecture.md`, the paragraph opening "The ledger fails closed").
- The charter sentences are pinned by `.kit/channel-reply-instruction-test.sh` (`DESIGN_ARCHITECT_RELAY_CONTROL`, `DESIGN_ARCHITECT_OPERATOR_RELAY_CONTROL`, `ARCH_OTHER_PATH_CONTROL`), not by `.kit/supervisor-natural-exit-test.sh`, which pins no charter sentence.
- The comment above `ARCHITECT_ROLE_INSTRUCTION` (`bin/supervise.sh:2980-2981`) says the supervisor is `architectPersona`'s only reader and the plugin never reads the key, which Section 1 makes false.
- `README.md:58`, describing the steward's duties, ends "so the architect never addresses a worker directly", apart from the inbox passage at `README.md:585-591`.

**The design.**

1. The plugin reads `architectPersona` from its options, under the same name rule and `default` refusal `coordinatorPersona` takes at `hooks/index.ts:2095-2099`. A value equal to the coordinator persona reads as unset, the pair the supervisor already refuses.
2. The worker leg widens from "the target is the coordinator persona" to "the target is the coordinator persona or the architect persona". The record is labelled `WORKER:<persona>`, as a worker's record to the coordinator is today.
3. A fourth leg, the answer leg. The writer owns the architect persona. The target persona has a live owner claim, and that owner's session wrote a record to the architect persona whose status is `delivered` or `answered`. Such a record has reached the architect and has not been resolved. The record is labelled `WORKER:<architect persona>`. The leg grants nothing beyond that label, which a worker already reads as a peer's answer. It closes when the architect resolves the record, so the architect sends its answer first and resolves second.
4. `fleet_status` and `fleet_restart` stay gated on the coordinator persona alone, so a `WORKER` ground stays refused there (`hooks/index.ts:7184`).
5. The three charters and the nudge change as Section 2 states.

## Sections of Work

### 1. The worker leg reaches the architect, and the architect answers back
Model: opus

`hooks/index.ts` reads `architectPersona` beside `coordinatorPersona`. `deliveryGroundIn` and `mayReachPersonaIn` in `hooks/operator.ts` take the architect name. The worker leg admits it as a target, and the answer leg in design point 3 is added. The answer leg reads the inbox records addressed to the architect, so `deliveryGroundIn`'s callers pass those records, or a reading form fetches them beside the claims. Every caller passes the architect name. The descriptions of `agentic_say` and `agentic_inbox` name the architect as reachable. The `coordinatorPersona` and `architectPersona` setting descriptions say what the plugin reads, and the latter no longer says the supervisor is its only reader. Each description stays under the length test's bound, and the injection ledger and `docs/architecture.md`'s totals are refreshed.

There is no fallback inside this section. If reading the record store inside the reach rule proves too invasive, the section stops on a `BLOCKED:` and the plan returns to the architect, because an answer through `agentic_resolve`'s note would contradict the Goal, Section 2(b) and the Intent's refused list, and that note's cap is `FREE_TEXT_MAX`, which the cut lane shares. The comment above `ARCHITECT_ROLE_INSTRUCTION` in `bin/supervise.sh` no longer says the supervisor is the setting's only reader.

Acceptance (draft):
- A session that owns a named persona sends to the architect persona, and the record is delivered labelled `WORKER:<persona>`. The same send with `architectPersona` unset is refused with today's reason.
- That session reads its own records at the architect with `agentic_inbox` and the persona argument set to the architect.
- A session on `default` is refused when sending to the architect.
- The architect's owner sends to a worker whose record to the architect has status `delivered` or `answered`, and the record reaches the worker labelled `WORKER:<architect persona>`. The same send after the architect resolves that record is refused. So is a send to a persona that holds no such record.
- `fleet_status` and `fleet_restart` still refuse a `WORKER` ground.
- An `architectPersona` value equal to the coordinator persona leaves both architect legs closed.
- The existing reach-rule tests pass, edited only where they pin the worker leg's old text.

Files in scope: `hooks/operator.ts`, `hooks/index.ts` (the options read, the reach-rule callers, the `agentic_say` and `agentic_inbox` registrations), `bin/supervise.sh` (the `architectPersona` reader comment above the architect charter), `.claude-plugin/plugin.json`, `.kit/controller-tick-test.mjs`, the reach-rule unit tests under `.kit/`, `.kit/injection-ledger.json`, `docs/architecture.md`, `README.md` (the `agentic_say`, `agentic_inbox` and Trust boundary passages that state the reach rule).

### 2. Three charters and the nudge name the line
Model: opus

The architect's charter at `bin/supervise.sh:2987` changes in three places.
- (a) A prompt labelled `[WORKER:<persona> id=<record id>]` carrying a design ask is the architect's own work item, worked as a coordinator record is.
- (b) The architect answers such a record with `agentic_say` to that worker, before closing it with `agentic_resolve`. It still answers the coordinator's records to the coordinator.
- (c) The clone rule stands as written. A repository named in a worker's record is reported and not cloned. A plan the architect writes for a worker still reaches the worker's queue only through the coordinator.

The coordinator's charter at `bin/supervise.sh:2924` loses only the clause saying the architect never addresses a worker itself. The sentence's two relay legs stay: the coordinator keeps relaying the architect's answer to the operator where the ask was the operator's own, and to a worker whose ask it forwarded itself. It keeps routing its own design asks, and the operator's, to the architect. As built, it also gains a third relay leg: an architect answer that names a worker's persona and quotes that worker's record id, which reaches the coordinator where the architect's direct send was refused, is relayed to that worker as a coordinator record.

The worker's steer text at `bin/supervise.sh:2822` gains two sentences, appended only where `ARCHITECT_PERSONA` is non-empty, as the coordinator's design clause is at `bin/supervise.sh:2923`. The first names the architect persona as the seat for a design question the worker's plan does not cover: a spec gap, an approach fork, a plan review or a consult. Such a question goes by `agentic_say` with the persona argument set to the architect. Everything else keeps the coordinator ask shape. The second says that a `[WORKER:<architect persona>]` prompt answering a question this session sent is that question's answer, verifiable by the record id it carries, so the existing no-delegated-authority sentence does not send the worker to the operator with the architect's answer. The idle nudge at `hooks/index.ts:5152` names the same line beside its `ASK:` instruction, only where the plugin holds an architect name. The gated append is a second assignment of `COORDINATOR_STEER_INSTRUCTION`, so `.kit/injection-ledger.mjs`'s assignment-count rule for that variable is updated to the new count, and the ledger is refreshed.

`README.md` describes the widened reach rule where it describes the inbox (`README.md:585-591`), and names both architect legs. Its steward paragraph (`README.md:58`) no longer says the architect never addresses a worker directly.

Acceptance (draft):
- The three charters agree with each other and with Section 1's code. No charter, and no `README.md` paragraph describing a seat, still says the architect never addresses a worker, and none says a worker's record is information only. The coordinator's two relay legs are still stated.
- On a launch with no `ARCHITECT_PERSONA`, the steer text and the nudge carry no architect sentence.
- `.kit/channel-reply-instruction-test.sh`'s charter pins pass, edited only where they pin the sentences this section changes (`ARCH_OTHER_PATH_CONTROL`, and the relay controls only if their wording moves).
- The Chapter records each changed ledger entry's character delta against the committed baseline in `.kit/injection-ledger.json`. The refreshed ledger is the record of that growth, not the gate, since the size test compares live text to whatever baseline is committed.

Files in scope: `bin/supervise.sh` (the three charters), `hooks/index.ts` (the nudge text), `hooks/operator.ts` (the worker-leg label to the architect, folded at review round 1), `.kit/channel-reply-instruction-test.sh`, `.kit/controller-tick-test.mjs` where it pins the nudge, `.kit/injection-ledger.mjs` (the assignment count), `.kit/injection-ledger.json`, `README.md`, `docs/architecture.md`.

## Out of Scope

- Any new configuration file. The two existing settings name the seats.
- Lines between every pair of sessions. The Intent defers them to their own plan.
- Any change to who may read fleet state or restart a persona.
- Any change to the architect's clone rule.
- Any change to delivery timing, urgency or the break-in bound.

## Assumptions

- assumed 2026-09-22 (the repository's other plans): the commit model is Branch-and-PR; reversal: one header line.
- assumed 2026-09-22 (the architect's ruling): a record that is `answered` but not `resolved` still opens the answer leg, because an answered record is one the architect replied to in a turn and has not yet closed; reversal: one status in the leg's test.
- assumed 2026-09-22 (the architect): the answer leg matches the record's writer session against the target persona's current owner session. A worker restarted between its ask and the answer holds a new session id, so the leg is closed for it, and the architect answers that record the way it answers today, through the coordinator, then resolves it. Reversal: match on the persona the writer owned at the write, which the record does not carry today.
- assumed 2026-09-22 (the brainstorming skill): the blind read and gating litmus were not run on this plan, since a worker drafted it and the architect read it twice. The plan review ran at fable and effort high and its findings are applied.

## Operator Verification

- Write the threat model. The finishing security review found no `docs/security-model.md`, so it reviewed against the README's Trust boundary section. `docs/backlog.md` carries the item as "The keeper's boot-time execution surface has no security model document". This plan adds one surface to it: any session that owns a named persona can now place a record in front of the credential-holding architect seat, contained by instruction text alone. Done when the document exists and names that surface.
- Walk one live exchange on the fleet. A worker sends the architect a design question with `agentic_say`, the architect answers it, and the worker receives `[WORKER:<architect persona> id=...]` and uses it within its plan. The suites mock the plugin host, so only the running fleet shows the delivery end to end. A failure reopens this plan as a new round.
- Run the live suites under `docs/plans/agent_persona_deferred-gate-run_v1.md`. `.kit/live-all.sh` refused to start beside the fleet's live persona claims, as it does by design.

## Chapters

### Chapter 1 - 2026-09-22
Completed: 1. The worker leg reaches the architect, and the architect answers back
Implemented By: implementer-opus for the build and fix round 1; the README and docs/architecture.md edits and the close pass inline in the main session; no tier escalation
Metrics: review rounds 2, closed major-closed; provenance 3 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 1 declared, 0 asked); advisory: 1 findings, 1 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises:
- section open (2026-09-22): Section 1 makes the plugin read architectPersona, widens the worker leg of the inbox reach rule to admit the architect persona as a target, and adds the answer leg: the architect's owner reaches a worker whose owner session wrote it a record still delivered or answered, and at delivery also one resolved at or after the answer's write. Serves the Goal sentences "Any session that owns a named persona can send a record to the architect persona with agentic_say ... read its own records there with agentic_inbox" and "It answers the worker with agentic_say, over a narrow line", with design points 1 to 4. Adds mechanism: yes, the answer leg and the architect read, both named by the section. Size: about 60 to 100 lines across hooks/operator.ts and hooks/index.ts, two descriptions, two settings descriptions, one comment, and eight or more harness cases. Cost of not building: every design question and every answer keeps going through the coordinator's relay.
- r1 fix, adversarial Major + blind Major 2 (a sent architect answer is skipped at delivery once the worker's record leaves the architect's inbox or the worker's session changes) (2026-09-22): the send gate stamps an answer admitted on the answer leg with the id of the worker's record that opened it, and delivery honours that stamp while the writer still owns the architect persona, in place of re-reading the architect's inbox at delivery; the declared resolved-at-or-after-the-answer clause goes with the re-read. Serves Section 1's acceptance bullet "The architect's owner sends to a worker whose record to the architect has status delivered or answered, and the record reaches the worker labelled WORKER:<architect persona>". Adds mechanism: yes, a stored field on the answer record and a delivery branch that reads it. Size: one optional field, about 15 lines across hooks/operator.ts and hooks/index.ts, the delivery-time architectLineRecords calls removed, and three or four harness cases. Cost of not building: an answer the send gate admitted is marked skipped when the channel window or the 24-hour sweep rolls the worker's record out, or when the worker relaunches before its next quiet tick, and neither seat sees it.
- The header read `Status: Ready` at the start of this run and now reads `Status: In Progress`.
- Design stop: the fix for round 1's delivery-time Major adds a stamp on the answer record. The scope adjudicator ruled it accept-and-declare under Section 1's fourth acceptance bullet, whose form is a gate judged at send with an admitted send delivered labelled. The declared bound: one optional field, `answersRecord`, written only by the send gate on the answer leg and read only at the delivery sites; no read of the architect's inbox at delivery; the writer-still-owns-the-architect check kept at delivery; no change to delivery timing, urgency, the break-in bound, the channel window or the TTL sweep.
- The resolved-at-or-after-the-answer clause declared at the section's start was replaced by that stamp, so it no longer runs.
- An architect owner holding a reader claim now answers labelled `WORKER:<architect>` unless it reads the target, where `READER:<target>` stands and the answer is still stamped.
- Scope fold: `README.md` joined the section's Files in scope. The security lens found its Trust boundary passage and the `agentic_say` and `agentic_inbox` refusal lists stating three reach grounds after the code had four, and a security document stating what the code does not do is fixed rather than deferred. That edit also delivers Section 2's README bullet on the inbox passage, which Section 2 now verifies rather than writes. `README.md:58`, the steward paragraph, stays Section 2's.
- Section 2 inputs from this section's reviews: the delivered bracket carries the answer's own id, not the question's, so the architect's charter should have the answer quote the id of the worker's record it answers, and the worker's steer sentence should name that id and the `agentic_inbox` status check rather than the bracket id alone.
Assumptions:
- assumed 2026-09-22 (the executing-work intake gap check): `direct-lines-build` is cut from `goal-tree-curation-build` at c81b60e rather than from origin/main, because PR #71 edits the same `hooks/index.ts` regions and waits only on review with auto-merge armed; reversal: rebase onto origin/main once #71 merges (section 1).
Review Findings: design stop: a send-time stamp on the architect's answer, ruling accept-and-declare by the scope adjudicator. review: adversarial + blind + security + performance at fable, Agent tool, round 1; review: adversarial at opus, Workflow, effort high, round 2. Round 1 had three Majors from correctness lenses. The adversarial Major and the blind lens's second Major were one cause: delivery re-judged the answer against the architect's inbox, so a record rolled out by the channel window or the 24-hour sweep, or a worker relaunched before its next quiet tick, dropped an answer the send had admitted. It was fixed in fix round 1 within the ruled form, 11 checks seen red against 4a7376e. The blind lens's first Major, the three charters still forbidding the new lines, was traced by the orchestrator to Section 2's first acceptance bullet and is justified-not-fixed here, since Section 2 rewrites those charters. Advisory: the security Major on README's three-ground passages was fixed (honesty route); performance CLEAR. Round 2's Major, that a worker has nothing to match an answer to its question with, is justified-not-fixed here and carried to Section 2 as a charter and steer wording input above. Minors: 5 fixed (the reader-leg answer now stamped, probed red then green with the restore compared by cmp; `mayReachPersonaIn` removed with no caller left and its README mention; the skip detail naming a missing stamp; README and the `agentic_inbox` shape naming the stamp; the label order, upgraded from Minor on its Section 2 consequence and fixed in fix round 1), 1 upgraded, 3 left with the reason (two store.keys() scans on an architect send, bounded in-memory; the per-record inbox reads, moot once delivery stopped reading the inbox; the channel-window and sweep Minors, closed by the stamp). The close pass owes no round: no outward action, no new module, and its one behavior change is exercised directly by a check probed red.
Stamps: adjudicated 3, stamped 0, over a 3h window covering the section. All three were read by reviewer subagents rather than by this session, and none shaped a choice here.
Gate: targeted lane, SCOTT-CLAUDE, 2026-09-22T21:42Z, the worktree at aee165e plus the uncommitted close pass in hooks/operator.ts, hooks/index.ts, README.md, docs/architecture.md, .kit/controller-tick-test.mjs and .kit/injection-ledger.json. Lanes, each exit code read from its own run: npx tsc --noEmit 0; node .kit/controller-tick-test.mjs 0 at 2533 OK and 0 FAIL; node .kit/check-loader-rule.mjs 0; node .kit/injection-duplicate-test.mjs 0; node .kit/tool-description-length-test.mjs 0; node .kit/fleet-status-unit-test.mjs 0 at 201 OK; node .kit/self-review-unit-test.mjs 0; bash .kit/channel-reply-instruction-test.sh 0 at fix round 1. Delta against the 2489 baseline on the tick lane at c81b60e: +44 at 0 FAIL, all direct-lines checks. Tests added: five caseDirectLines_* cases in .kit/controller-tick-test.mjs, pinning the worker leg to the architect set and unset, the worker's inbox read at the architect, the `default` refusal, the send gate's answer leg open, answered, resolved, unasked and stale-writer, the send-time stamp including a reader claim on the target, delivery of a stamped answer with the architect's inbox emptied, across an owner change and under a reader claim elsewhere, the skip of an unstamped, forged or ownerless answer, the urgent and ask-answer sites, and an unusable architect setting reading as none; one fleet_status case and an architect caller in the fleet_restart refusal list in .kit/fleet-status-unit-test.mjs, pinning that a WORKER ground stays refused there. None spawns a process. Tests retired: none. Tests edited: `seedNamedOwnerHarness` gained an optional options argument. Wall clock about 65 s for the tick lane, against about 65 s at baseline. No foreign runner was up at the section's polls.
Next: 2. Three charters and the nudge name the line
Commit Model: Branch-and-PR

### Interim board 1 - 2026-09-22

Section 2 is in fix round 3. Commits on direct-lines-build: ea07ff3 (first green), 077ab88 (fix round 1), d966e5b (fix round 2), all pushed. Review rounds run: round 1 (adversarial, blind and security at fable), round 2 and round 3 (adversarial at opus, Workflow, effort high).

Live dispatch: implementer-opus on fix round 3 (brief .kit/scratch/direct-lines/brief-section-2-fix3.md): the worker's answer sentence keys on the plugin-made opening WORKER:<architect persona> bracket, with the quoted id and the agentic_inbox read only matching the answer to its question, so an answer delivered after a relaunch is still recognised; a nudge control for the architect's own seat; two test titles restated. The main thread has, uncommitted, restated docs/backlog.md's architect-containment entry (a worker's record now reaches the architect directly) and noted in docs/architecture.md that the worker and coordinator rows are upper bounds.

Gate baseline: targeted lane at d966e5b on SCOTT-CLAUDE, 2026-09-22, tick suite 2546 OK 0 FAIL, charter suite 157 OK, every lane exit 0, with another session's node --test running beside it.

Rulings adopted: the scope adjudicator ruled accept-and-declare on labelling a worker-leg send to the architect WORKER:<owned persona> whatever else the writer reads (Section 1's first acceptance bullet); it moves no bullet. Round 3's exact-wording-pin Major is justified-not-fixed: the whole charter test file pins wording this way, pre-existing controls included, so loosening it is a file-wide decision rather than this section's.

The next action: read fix round 3's diff, run the targeted lane, place the docs anchor and total refresh, commit, then decide whether round 3's fix delta owes a round 4 (below the fix-delta bar: no outward action, no new module) before the Minor close pass and Chapter 2.

### Chapter 2 - 2026-09-22
Completed: 2. Three charters and the nudge name the line
Implemented By: implementer-opus for the build and fix rounds 1 to 3; the docs/architecture.md and docs/backlog.md edits and the plan doc edits inline in the main session; no tier escalation
Metrics: review rounds 3, closed major-closed; provenance 6 spec-traceable, 3 fix-introduced, 0 new-requirement, rulings (0 refused, 1 declared, 0 asked); advisory: 0 findings, 0 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises:
- Section 2 open: rewrites the architect, coordinator and worker charter sentences in bin/supervise.sh, adds a gated nudge sentence in hooks/index.ts, and updates the README steward paragraph; serves Section 2's first two acceptance bullets and the Intent's "the worker's own instructions say the line exists"; adds no mechanism, since the only new branch is the gated append the plan names, mirroring the coordinator's existing ARCHITECT_PERSONA gate; size about 6 charter sentences, 1 nudge branch and 1 README sentence; not building it leaves the charters forbidding the lines Section 1 opened.
- r1 Major (blind, orchestrator-made trace to Section 1's first acceptance bullet and the Intent's "a worker reaches the architect without the coordinator in between"): changes deliveryGroundIn in hooks/operator.ts so a worker-leg send whose target is the architect persona is labelled WORKER:<owned persona> even where the writer holds reader claims on other personas; serves that bullet and that Intent clause; adds a mechanism: yes, a target-specific exception to the documented label order that README.md:600 states as "a writer holding a reader claim anywhere is never labelled WORKER"; size about 3 lines of code, 2 doc passages (the operator.ts comment and README.md:600) and 1 test case; not building it leaves a worker that also reads another persona reaching the architect under a READER label, which the architect's charter raises with the coordinator rather than working.
- r1 Major (adversarial, trace: Section 2 "the three charters agree with each other"): adds one clause to the coordinator's design clause in bin/supervise.sh saying an architect answer to a worker's own record, sent there because the direct send was refused, is relayed to that worker as a coordinator record; serves that bullet; adds no mechanism, since it is a charter sentence naming a relay the coordinator already performs for its own forwarded asks; size 1 sentence; not building it leaves the architect's fallback answer with no stated handler.
- r2 Majors (adversarial, opus): rewords four charter sentences in bin/supervise.sh and their pins: the architect's and the worker's recognition sentences also name the tool-result forms (`, waited` and `, urgent`) of the same record; the worker's exemption from the operator rule is bounded to using the answer as input to its approved plan, with any act outside that plan still going to the operator; the architect's fallback answer to the coordinator names the worker's persona and quotes its record id; the worker's sentence recognises a prompt that opens with the bracket, as the coordinator's does; serves the Goal's "treats a worker's design ask as its own work item", the Intent's refused "the architect answers rather than steers", and Section 2's "three charters agree with each other"; adds no mechanism, since each is a charter sentence; size four sentences and their pins; not building them leaves a waited ask read as information, an answer with steering standing, an unroutable fallback and a forgeable answer bracket.
- r3 Major (adversarial, relaunched worker): rewords the worker's answer sentence in bin/supervise.sh so the plugin-made opening WORKER:<architect persona> bracket is the recognition key and the quoted id and the agentic_inbox read only match the answer to a question, with a clause for an answer whose question the worker cannot list; serves the Intent's "The architect answers that worker directly"; adds no mechanism, a charter sentence; size one sentence and its pins; not building it sends an answer delivered after a relaunch to the operator.
- r3 Major (adversarial, claim on docs/backlog.md's architect-containment entry): repoints its anchor and restates that a worker's own record reaches the architect directly as one of its three ask channels, beside the steward's relay; serves Section 2's first acceptance bullet and the doctrine's nothing-untrue-ships rule on a security record; adds no mechanism; size one paragraph; not building it leaves a security record describing less exposure than exists.
- Scope fold: `hooks/operator.ts` joined the section's Files in scope at review round 1, for the worker-leg label to the architect; it sits in the same directory as `hooks/index.ts`, needed no acceptance bullet the plan lacked, and the section's gate covers it.
- Spec deviation: the coordinator's charter does more than lose the never-addresses-a-worker clause. It gains a third relay leg for the architect's fallback answer, which the plan's third Assumption needs, and Section 2's text now says so.
- The widened reach the security lens named is recorded here: before this plan a local session could place work in front of the architect only by holding the coordinator persona or through the steward's relay; now any session that owns a named persona can, and `docs/backlog.md`'s architect-containment entry states that path. The Intent's rationale for the `default` exclusion reads wider than the fence README's Trust boundary states, and it is left as the approved text.
- A new backlog entry records that the steward's own worker sentence names only the prompt form of a worker's record, not the `, waited` and `, urgent` tool-result forms. The gap predates this plan.
- Acceptance bullet 4, each changed ledger entry against the committed baseline at 082c54c: `COORDINATOR_STEER_INSTRUCTION` 1,420 to 2,796 (+1,376); `COORDINATOR_ROLE_INSTRUCTION` 5,153 to 5,376 (+223); `ARCHITECT_ROLE_INSTRUCTION` 5,132 to 6,464 (+1,332); `NUDGE_TEXT_idle_gap_converted` 521 to 697 (+176); all entries 30,222 to 33,329.
Assumptions:
- assumed 2026-09-22 (Chapter 1's Section 2 inputs): the architect's answer quotes the id of the worker's record it answers, and the worker's steer sentence names that quote and the agentic_inbox check rather than the bracket id; reversal: two charter sentences (section 2).
- assumed 2026-09-22 (the implementer, confirmed at hooks/index.ts where agentic_inbox lists a caller's records whatever their status): the worker's check is that agentic_inbox lists the quoted record among its own at the architect, with no status test, because the architect resolves the record right after answering; reversal: one clause in the steer sentence (section 2).
- assumed 2026-09-22 (a low-blast default): the worker sentence names only the WORKER:<architect> label, not the READER:<target> label an architect holding a reader claim on that worker would produce, since nothing in the architect's charter has it take a reader claim on a worker (inferred from the charter text, not from a fleet reading); reversal: one clause (section 2).
- assumed 2026-09-22 (the plan's third Assumption): the architect's charter answers through the coordinator where its send to the worker is refused; reversal: one charter sentence (section 2).
Review Findings: design stop: labelling a worker-leg send to the architect WORKER:<owned persona> whatever else the writer reads, ruling accept-and-declare by the scope adjudicator (grounds: Section 1's first acceptance bullet; it moves no bullet and is surfaced on the next board recap). review: adversarial + blind + security at fable, Agent tool, round 1; review: adversarial at opus, Workflow, effort high, rounds 2 and 3. Round 1: the blind lens's Major (a worker holding a reader claim elsewhere reached the architect labelled READER, which the new charter reads as information; trace orchestrator-made to Section 1's first bullet) was fixed through the design stop above, the label change seen red then green; the adversarial Major (the architect's fallback answer had no handler in the steward's charter) was fixed with a relay clause. Security CLEAR with three Minors. Round 2's four Majors were charter wording: the `, waited` and `, urgent` tool-result forms of a worker's record and of the answer; the worker's unbounded exemption from the operator rule, now bounded to input within its approved plan per the Intent's refused "the architect answers rather than steers"; a fallback the steward could not route, which now names the worker's persona and quotes its record id; and "labelled" against the coordinator's "opens with". All four were fixed with pins seen red. Round 3's Majors: a worker relaunched after the send could not recognise a delivered answer, fixed by keying recognition on the plugin-made WORKER:<architect persona> bracket; and docs/backlog.md's architect-containment entry understated the new direct path and cited a moved anchor, fixed. Round 3's third Major, that the new charter pins are exact-wording pins in testing-discipline's retire class, is justified-not-fixed: the whole charter test file pins wording this way, pre-existing controls included, so loosening that idiom is a file-wide decision rather than this section's. Round 3's fix delta owed no round under the fix-delta bar (no outward action, no new module, and the pins read the rewritten sentence directly); the main thread's author re-read of that sentence against deliveryGroundIn in hooks/operator.ts and the own-records filter at hooks/index.ts:7569 stands for it. Minors: 0 fixed in the close pass, since all fourteen fixed ones landed in fix rounds 1 to 3; 1 upgraded (the blind lens's fallback-handler Minor, merged into round 1's adversarial Major); 4 left with the reason (the nudge owner gate is moot because the controller tick returns for a non-owner at hooks/index.ts:3286; the test file's six-block count was checked and is right; the plan Intent's default-exclusion rationale is approved text, recorded above; an answer labelled READER where the architect holds a reader claim on the worker is a declared assumption); the spec-deviation Minor raised in rounds 2 and 3 is declared above and in Section 2's text.
Stamps: adjudicated 2, stamped 0, over a 5h window covering the section; both were read by reviewer or implementer subagents and shaped no choice here. Separately stamped docs-write-guard-blocks-subagent-doc-writes applied, from the recall at the section's open, since it kept the docs/ edits in the main thread.
Gate: targeted lane, SCOTT-CLAUDE, 2026-09-22T23:05Z, the worktree at d966e5b plus the uncommitted fix round 3 and docs edits in bin/supervise.sh, README.md, docs/architecture.md, docs/backlog.md, .kit/channel-reply-instruction-test.sh, .kit/controller-tick-test.mjs and .kit/injection-ledger.json. Lanes, each exit code read from its own run: npx tsc --noEmit 0; node .kit/controller-tick-test.mjs 0 at 2548 OK; node .kit/check-loader-rule.mjs 0; node .kit/injection-duplicate-test.mjs 0; node .kit/tool-description-length-test.mjs 0; node .kit/fleet-status-unit-test.mjs 0 at 201 OK; node .kit/self-review-unit-test.mjs 0; bash .kit/channel-reply-instruction-test.sh 0 at 157 OK; the regenerated ledger byte-identical to the committed one. Delta against the Section 1 close baseline at df6da2e, tree-identical to 082c54c: tick 2533 to 2548 OK (+15, all direct-lines checks), 0 FAIL both; charter suite 128 to 157 OK (+29). Tests added: caseDirectLines_idleNudgeNamesTheArchitectLine, pinning that the nudge names the architect line for a named worker with an architect set and not when unset, on default, on the coordinator or on the architect's own seat; caseDirectLines_workerLabelToTheArchitectHoldsOverReaderClaimsElsewhere, pinning that a worker reading another persona reaches the architect labelled WORKER:<owned persona>, the coordinator leg keeps READER, a reader of the architect keeps READER:<architect>, and the architect's answer to that worker is admitted and stamped; in .kit/channel-reply-instruction-test.sh the STEER_ARCH_ class (the worker's architect sentences present on a named worker with an architect and absent on every other launch, the tool-result bracket forms, the label as proof, the unlisted-question case, the plan bound), the ARCH_WORKER_ pins (a worker record as a work item in prompt and tool-result form, answer before resolve with the id quoted, the any-refusal fallback naming the persona and id, the plan route through the coordinator), DESIGN_ARCHITECT_REFUSED_RELAY and its key pin, and a retired-phrase sweep with a control over the four removed routing phrases, which sweeps those named phrases and not the class. None spawns a process. Tests edited: ARCH_ASK_CONTROL from two ways to three, SPLICE_SITE_COUNT 4 to 5, and one tick case title scoped to the coordinator target. Tests retired: none. Wall clock about 81 s for the whole targeted lane against about 83 s at the section's first green. Contention: another session's npm test and node --test from a different checkout ran through this and every earlier section run, polled before each run; every run finished with full counts.
Next: finishing-work
Commit Model: Branch-and-PR
Delta: SCOTT-CLAUDE, 2026-09-22T23:08Z, the worktree at d966e5b with this section's uncommitted close edits.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 3 - 2026-09-22 (finishing pass and close)
Completed: the finishing pass over both sections; the plan is Complete.
Implemented By: implementer-opus for finishing fix rounds 1 and 2; the drift fixes, the Minor close pass, the docs edits and the plan close inline in the main session; no tier escalation
Metrics: finishing review rounds 3 (the wave, then two fix-delta reads); provenance 0 spec-traceable, 1 fix-introduced, 1 new-requirement, rulings (0 refused, 1 declared, 0 asked); advisory: 1 findings, 1 fixed, 0 deferred, 0 refused; goal read: 3 declared, 1 asked, 0 unbuilt; NEEDS_CONTEXT 0; escalations 0; consults 0
Recap: The plan's Goal, verbatim: "When this is done, a worker can put a design question straight to the architect, and the architect can answer that worker straight back. Any session that owns a named persona can send a record to the architect persona with `agentic_say`. It can read its own records there with `agentic_inbox`. Today both work only with the coordinator persona. The architect treats a worker's design ask as its own work item. It answers the worker with `agentic_say`, over a narrow line that reaches only a persona whose record to the architect it has taken delivery of and not yet resolved. Workers are told the line exists."; What the tree does now: a worker session can send a design question straight to the architect seat and read its own questions there, and the architect can answer that worker directly while the question is open, the answer arriving marked as the architect's so the worker uses it inside its own approved plan; each seat's launch instructions say so, a worker whose question sits untaken for five minutes takes it to the steward instead, and where the plugin refuses the architect's direct answer the steward relays it marked as the architect's answer with no steering authority of its own; Refinements during the run: the answer carries a stamp naming the question it answers, so an answer admitted at send is delivered even after the architect closes the question (Section 1 design stop, replacing the resolved-at-or-after clause declared at Section 1's open); a worker that also reads another persona still reaches the architect labelled as a worker (Section 2 design stop); the steward gained a third relay leg for a refused direct answer (Section 2 deviation); that relay is marked as the architect's answer with no steer, and a worker gained a five-minute fallback to the steward (finishing fixes); the example roster's workers now name the architect (finishing drift fix); Operator-pending: write the threat model; walk one live worker-to-architect exchange on the fleet; run the live suites under the deferred-gate-run plan.
Decisions / Surprises:
- base ref (2026-09-22): 4b61108, the merge-base with origin/main and origin/main's head, since PR #71 merged before this pass. Scope check: only docs/backlog.md fell outside both Files in scope lines, an edit Chapter 2 records.
- step 1 (2026-09-22): qa-verifier PASS at 451b7f0. The whole gate ran 26 lanes, all exit 0 from their own markers, 2026-09-22T23:11:45Z to 23:32:36Z: tick 2548 OK and 0 FAIL, fleet-status 201 OK, charter suite 157 OK, each equal to the Section 2 close baseline. Every acceptance bullet of both sections PASS with a named green case. Contention lane: `.kit/live-all.sh` exit 10, refusing beside the live persona:STEWARD claim, as designed. The tree was unchanged across the round.
- steps 2 and 3 (2026-09-22): security, performance and adversarial at fable, effort high, through Workflow; every assistant turn resolved claude-fable-5-1 (32, 23 and 30), so no substitution. Security opened "threat model: absent", no Disclosure list; one Major and two Minors. Performance CLEAR with two Minors. Adversarial APPROVED_WITH_CONCERNS with one Major and four Minors.
- the security Major (advisory): the steward's third relay leg relayed an architect fallback answer to a worker as a plain steward record, which a worker reads as the operator's delegated authority, for an ask the steward never saw. The scope adjudicator's relevance ruling was CONFIRM on the Intent's refused alternative "the architect answers rather than steers". Fixed in finishing fix round 1: the relay says it relays the architect's answer, quotes the record id and disclaims a steer, and the worker reads such a record as the answer.
- the adversarial Major (new-requirement, held): a worker cannot read fleet state, so it had no way to tell an absent architect from a busy one. The scope adjudicator ruled accept-and-declare on the Goal's "Workers are told the line exists"; declared bound: one worker steer sentence, no nudge change. Fixed in fix round 1.
- fix-delta read 1 (adversarial, fable, high, Workflow): CHANGES_REQUIRED. Fix round 1's liveness sentence fired on a live idle architect, because the drain takes one record per thirty-second tick, only between turns, and `deferred` exists only inside a turn (hooks/index.ts:2211, 4011-4029, 7595). Fix round 2 bounded it to two reads five minutes apart with a hedged meaning, and keyed the relay recognition on the no-steer disclaimer with the agentic_resolve clause (a Minor upgraded into the same sentence).
- fix-delta read 2: APPROVED_WITH_CONCERNS, every claim checked true against hooks/index.ts, the totals and anchors correct, three Minors. The fix path closed there. Commit 43304a4 carries both fix rounds and the Minor close pass.
- step 4 goal read (scope adjudicator, fable, Agent tool, charter effort high): ASKED-BUT-UNBUILT none. BUILT-BUT-UNASKED: the refused-send fallback through the steward, the steward prompt-form backlog entry and the docs/architecture.md anchor refreshes, each accept-and-declare; the answersRecord stamp, ASK with a recommendation to accept, carried to the operator in the close-out since it holds nothing. Section 1's design stop had ruled that stamp accept-and-declare under Section 1's fourth bullet.
- step 5 docs curator: wrote docs/architecture.md (each session reads architectPersona from its own settings, the upper-bound clause, two failure-mode rows) and docs/README.md (the plan listed); the diff was read line by line. D1 (README said the plugin never reads architectPersona, and its options table omitted it), fixed. D2 mistake: the example roster's worker entries named no architect, so a fleet copied from it gives workers no line; pre-change read: bin/fleet.example.json unchanged since 4b61108 and hooks/index.ts:2229-2234 reads the key per session, so the basis survived; resolved before the PR by naming the architect on both worker entries and in README's launch paragraph. D3, D4 and D5 deviations, recorded here. D6 mistake: a test comment said a fourth shape is a fifth interpolation beside SPLICE_SITE_COUNT=5; pre-change read: the count was 4 at 4b61108, so this changeset made it stale; fixed. D7 deviation, an unverified pre-change claim: stale hooks/index.ts anchors in older backlog entries, carried to the backlog. H1, the Related plans link to an archived plan, fixed; H2 and H3 answered by two Related plans lines.
- close (2026-09-22): the plan gained an Operator Verification section; docs/backlog.md gained the entry for the seven deferred Minors; no backlog item is older than 90 days, the oldest dated 2026-09-12.
- Ledger against the committed baseline at 082c54c, as shipped: COORDINATOR_STEER_INSTRUCTION 1,420 to 3,700; COORDINATOR_ROLE_INSTRUCTION 5,153 to 5,528; ARCHITECT_ROLE_INSTRUCTION 5,132 to 6,464; NUDGE_TEXT_idle_gap_converted 521 to 697; all entries 30,222 to 34,385.
Assumptions:
- assumed 2026-09-22 (the executing-work intake gap check): `direct-lines-build` is cut from `goal-tree-curation-build` at c81b60e rather than from origin/main, because PR #71 edits the same `hooks/index.ts` regions and waits only on review with auto-merge armed; reversal: rebase onto origin/main once #71 merges (section 1).
- assumed 2026-09-22 (Chapter 1's Section 2 inputs): the architect's answer quotes the id of the worker's record it answers, and the worker's steer sentence names that quote and the agentic_inbox check rather than the bracket id; reversal: two charter sentences (section 2).
- assumed 2026-09-22 (the implementer, confirmed at hooks/index.ts where agentic_inbox lists a caller's records whatever their status): the worker's check is that agentic_inbox lists the quoted record among its own at the architect, with no status test, because the architect resolves the record right after answering; reversal: one clause in the steer sentence (section 2).
- assumed 2026-09-22 (a low-blast default): the worker sentence names only the WORKER:<architect> label, not the READER:<target> label an architect holding a reader claim on that worker would produce, since nothing in the architect's charter has it take a reader claim on a worker (inferred from the charter text, not from a fleet reading); reversal: one clause (section 2).
- assumed 2026-09-22 (the plan's third Assumption): the architect's charter answers through the coordinator where its send to the worker is refused; reversal: one charter sentence (section 2).
- assumed 2026-09-22 (finishing, the drift adjudication): the example-roster mistake is resolved with a config and doc fix before the PR rather than held for a ruling, since the fix restores the Goal's "Any session that owns a named persona can send a record to the architect persona" on the shipped example and changes no code; reversal: two roster fields and one README sentence (finishing).
Review Findings: `review: security, performance, adversarial at fable, Workflow, effort high` over 4b61108..451b7f0; then `review: adversarial at fable, Workflow, effort high` over fix round 1, and again over fix round 2. Correctness Criticals 0; Majors 2, 1 new-requirement fixed on an accept-and-declare ruling and 1 fix-introduced fixed; advisory Major 1, fixed on a CONFIRM ruling. Goal read: 3 declared, 1 asked (answersRecord), 0 unbuilt. Minors: 1 upgraded into fix round 2, 2 fixed in the close pass (README.md:600, README.md:606), the rest left with the reason in .kit/scratch/direct-lines/finishing/minors.md and the seven with a remedy carried to docs/backlog.md.
Stamps: adjudicated at the close, recorded in the close-out.
Gate: whole gate, SCOTT-CLAUDE, 2026-09-23T00:14:35Z to 00:37:37Z, at 43304a4 plus this close's uncommitted edits (README.md, bin/fleet.example.json, docs/README.md, docs/architecture.md, docs/backlog.md, .kit/channel-reply-instruction-test.sh and this plan's archive move and Chapter); this Gate: line is the one edit after it. 26 lanes, each exit code read from its own marker, all 0: tsc clean, tick 2548 OK and 0 FAIL, fleet-status 201 OK, charter suite 160 OK, and the other 22 lanes exit 0. Delta against the step 1 baseline at 451b7f0 (2026-09-22T23:11:45Z to 23:32:36Z): tick 2548 to 2548, fleet-status 201 to 201, charter suite 157 to 160 (+3, the finishing fix pins), every lane 0 FAIL both runs. Contention lane: `.kit/live-all.sh` exit 10, refusing beside the live persona:STEWARD claim, as designed; its live suites belong to docs/plans/agent_persona_deferred-gate-run_v1.md. No foreign test runner was up at the gate's own poll.
Next: none; the plan is complete. The pull request follows.
Commit Model: Branch-and-PR
