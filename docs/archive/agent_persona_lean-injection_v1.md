# Lean injection: every injected prompt says only what its reader cannot already know

Status: Complete
Commit Model: Branch-and-PR
Created: 2026-09-18

## Goal

When this is done, every prompt the plugin or the supervisor writes into a session, and every tool description the plugin registers, carries only what that session cannot already hold: the meaning of the label at the prompt's head, what to do with this one prompt, and a pointer to the surface that owns the rest. The writing rules live in `CLAUDE.md`, the roles live in the kit's skills, and each tool's contract lives in that tool's description, each once. A test fails the build when an injected string repeats a sentence that `CLAUDE.md` or another injected string already carries, and a ledger records each injected string's size so growth is declared rather than discovered. It matters because a worker's startup message today carries about 4,000 characters of instruction, the coordinator's about 11,200, and every plugin-submitted prompt for the operator's eyes carries a further 1,564 of which 19 of 21 sentences are copied from `CLAUDE.md`.

## Dispatch Authorization

The operator armed execution of this plan on 2026-09-19, naming it in a `/kit-goal` invocation relayed to the executing session on the operator's own channel. Under the kit-goal skill's arming-is-approval rule that invocation carries the authority of a typed "proceed", so this plan is approved as written and no separate approval is waited on. The authorization to author is the operator's request of 2026-09-18 recorded in the Intent below, in the words "Are there a whole bunch of detailed, redundant, injected instructions that we should be doing a pass over and trimming down to avoid overcomplicating what the plugin guidance is trying to give?".

This section was absent until the arming. Its absence was a document defect rather than a decision: every sibling plan in this queue carries one, and the arming tool reported this plan as having no authorization recorded, which reads to a later session as a plan nobody approved. The record is kept here because the arming state does not keep it. A run that re-arms a queue for itself after a session ends records its own invocation and no longer reads the operator's, so a session reading only that state would find this plan unarmed. The project memory record `self-armed-arming-does-not-satisfy-an-operator-only-execution-grant` holds the general shape.

## Intent

The operator's words on 2026-09-18, on the coordinator's Discord thread, after seeing a kaizen prompt in a transcript: "It's not so much that I have a problem with that particular sentence or its existence. It's more that I'm looking for signals that flag excessive growth and excessive detailed instruction." "We're having a lot of problems with there just being too much prose, too much specificity, not enough high-level goal, high-level guidance, high-level lesson." "Are there a whole bunch of detailed, redundant, injected instructions that we should be doing a pass over and trimming down to avoid overcomplicating what the plugin guidance is trying to give?" On the cause: "things are being added in isolation without a larger goal-vision-type structure."

What done looks like: each injected prompt is short and points at its owner, the pass covers the pattern and not one block, and a mechanical signal catches the next copy so nobody has to spot it by eye.

What done does not need to do: it does not rewrite `CLAUDE.md`, the kit's skills or the relay server's instructions, which are the owners the prompts point at. It does not change when any prompt fires or what any tool does. It does not decide which persona may hold which tool. It does not add a rule about prose anywhere, since the rule already exists in the doctrine and in the lean-kit program's "one owner per moment" decision, and a new rule is the growth under diagnosis.

Refused alternatives: trimming the one block the operator saw, refused because it fixes the sample and not the pattern. A doctrine or `CLAUDE.md` sentence about injected prompts, refused for the reason above. Registering `fleet_status` only for the coordinator, refused here because it changes behavior and the description trim removes most of the cost; it is recorded under Open Questions.

Provenance: the coordinator persona's inventory of 2026-09-18, measured from the source strings, and the kit's `docs/plans/claude-kit_lean-kit_program_v1.md` and `docs/plans/claude-kit_goal-fit_spec_v1.md`, whose decisions this plan applies in this repository.

Amended 2026-09-20 by the operator on the relay channel: the security reviewer does not run on the remaining sections of this plan, and any finding it produced is discarded rather than acted on. The operator's ground is that the lens was returning edge cases outside this plan's concern and was slowing the work materially, and that a separate plan owns that class of finding. The executing session named the risk this leaves and the operator held the decision: this plan rewrites the text injected into running agent sessions, which is the surface where a bad instruction does the most damage, and no lens now reads that text for that risk. The decision is the operator's and is recorded here so no later session re-dispatches that lens on this plan.

Amended 2026-09-20 by the operator on the relay channel, refining the same day's earlier ruling on the security lens. That lens runs in an advisory capacity rather than a gating one. Only its Critical findings are adjudicated and acted on; everything below that is recorded and not executed on, and its findings are not rated Major by default. The operator's ground is that the lens is held to one bar across every repository, that an OWASP-weight read is not warranted on most of them, and that treating each of its findings as urgent grinds the work on prose and on edge cases that do not bear on the repository in hand. This replaces the earlier ruling that the lens does not run at all for the remainder of this plan.

Amended 2026-09-20 by the operator on the relay channel, widening the same day's advisory ruling past the security lens. A review finding that changes only a sentence, where no input makes the code behave wrong, is advisory: it is recorded and swept in one pass rather than gating a section or earning a round of its own. A finding that changes what the code does still gates as before. The operator's ground is that rounds were being spent on documentation accuracy while the gate that matters is the minority of what returns, and that the pattern is the adversarial lens picking apart prose rather than the security lens alone. The executing session recommended this shape and named its cost, which is that a comment or README sentence can sit wrong for the length of a plan.

## Approach

**The rule each edit applies.** For every sentence in an injected string, name the surface that already carries its content. Where one exists, the sentence goes, and the string keeps at most one pointer to that surface. Where none exists and the content is a standing rule rather than this prompt's own instruction, the sentence moves to the owner, which is `CLAUDE.md` for writing and channel conduct, the kit's role skill for a seat's duties, and the tool's description for a tool's contract. Where none exists and the content is what to do with this prompt, the sentence stays. The lean-kit program's "one owner per moment" is the rule; this paragraph is how it is applied to a prompt.

**The base.** This plan's base is the trunk after `docs/archive/agent_persona_steward-architect_v1.md` merges. The strings the sections name are the ones on that plan's branch at `3c03886` and in its Section 6 worked tree, which is where the architect instruction, the `fleet_status` tool and the `[FLEET]` and `[RECONCILE]` frames live; the trunk at `8e177ce` has four instruction strings and none of those. A string a section names that does not exist at the executing base makes that clause void, and the Chapter says so.

**What the inventory found.** Measured 2026-09-18 from `bin/supervise.sh` and `hooks/index.ts` on the steward-architect branch at `3c03886` and its worked tree. The startup message is assembled at the `printf` that writes `$SKILL_LOAD_INSTRUCTION$COORDINATOR_STEER_INSTRUCTION$COORDINATOR_ROLE_INSTRUCTION$ARCHITECT_ROLE_INSTRUCTION$CHANNEL_REPLY_INSTRUCTION` and the priming body to the child. Its parts: the skill-load instruction at 518 characters, the coordinator steer instruction at 1,389, the channel instruction at 1,564, the coordinator role instruction at 3,711 with its fleet, seat and architect clauses at 1,408, 614 and 1,761, and the architect role instruction at 5,574. The channel instruction is byte-identical to `REPLY_INSTRUCTION` in `hooks/index.ts`, and 19 of its 21 sentences are verbatim in `CLAUDE.md`, the other two restating the relay server's own instructions. `REPLY_INSTRUCTION` is prepended to the still-waiting re-raise, the fleet reading, the kaizen announcement and the reply backstop. The coordinator role instruction restates the `agentic_say`, `agentic_inbox` and `agentic_resolve` descriptions; its fleet clause and the `fleet_status` description each define the same health classes; the architect instruction overrides two instructions the same message injected and states its clone rule three times. The tool descriptions total about 10,700 characters, `fleet_status` at 3,547, and are registered into every session. The per-prompt context blocks (`[GOAL TREE]`, `[NO GOAL]`, the idle nudges) repeat the `goal_done` clause among themselves.

**The guard.** `.kit/injection-ledger.mjs` extracts every injected string and tool description from the two source files by the anchors the sections name, and prints each with its size. `.kit/injection-duplicate-test.mjs` runs it and fails on any sentence of eight words or more that appears in `CLAUDE.md` or in another injected string, and on any string whose size exceeds the number recorded for it in `.kit/injection-ledger.json`. The check exempts nothing, so a sentence two prompts both need is a sentence with one owner and a pointer, never two copies. The reply-tool sentence's owner is the startup message, which every channel-attached child reads once; each recurring prompt names the reply tool inside its own instruction in its own words, and the `REPLY_INSTRUCTION` prefix goes. A string that grows raises its own number in the same commit, which is the lean-kit program's "growth is declared" decision at this repository's scale. The test is offline, so it runs beside a live fleet.

**Order.** This plan runs after `docs/archive/agent_persona_steward-architect_v1.md`, because that plan's worked copy already rewrites the coordinator and architect instructions on its branch and a trim landed first would be overwritten. It runs after `docs/plans/agent_persona_context-budget-removal_v1.md`, because that plan deletes the `[BUDGET]` prompt this one would otherwise trim. It runs before `docs/plans/agent_persona_supervisor-peer_v1.md`, so that plan's one new priming sentence is written under the guard. The order is the operator's at arming, except that this plan cannot run before steward-architect.

## Standing Brief Amendments

Every entry here is binding on each section opened after it, and rides verbatim on every sighted reviewer dispatch.

- A finding that changes only a sentence, where no input makes the code behave wrong, is advisory on this plan. Record it and sweep it in one pass; it neither gates a section nor earns a round of its own. A finding that changes what the code does gates as before. This is the operator's ruling of 2026-09-20 and it reaches every lens rather than the security lens alone.

- A ledger number may rise where this plan's own section text directs the rise, and the acceptance bullet requiring every number to be lowered is read against the section's delivered total rather than against each entry. Every rise is accounted for, entry by entry, in the Chapter that closes the section.
- The security lens is advisory on this plan rather than gating. Dispatch it where its trigger holds, adjudicate only its Critical findings, and record everything below that without acting on it. Its findings are not rated Major by default. This is the operator's ruling of 2026-09-20 and it replaces that day's earlier ruling that the lens does not run at all.
- The `fleet_status` tool description does not define the five fleet health classes. Those classes are declared in `hooks/agent-state.ts` and documented in `README.md` under the fleet wake. What the description's own vocabulary defines is the keeper's `action` field, which shares the words held and backing off with the health classes and means something different by them. So a surface that sends a reader to the `fleet_status` description for what a health class is states something false, and stays false until Section 4 moves those definitions into that description. Section 4 is the first section for which such a pointer can be true.

## Sections of Work

### 1. The ledger and the baseline
Model: sonnet

`.kit/injection-ledger.mjs` reads `bin/supervise.sh` and `hooks/index.ts`, extracts each injected string by name, and prints a JSON array of `{ name, file, chars, words }`. The names are the five `*_INSTRUCTION` variables and the three priming bodies in the supervisor, and in the plugin `REPLY_INSTRUCTION`, each prompt frame's literal text at its call site where it reaches `submitExpectedTurn` or `$.prompt.submit`, with interpolated content excluded, each `prompt.submit` context block's literal text, and each registered tool's description with its parameter descriptions. `.kit/injection-ledger.json` is written from that output at the section's base and committed. `.kit/injection-duplicate-test.mjs` lands with the duplicate check and the size check, and its exit code at the base is recorded in the Chapter: red, with the count of duplicate sentences it found, which is the baseline every later section reports against.

Acceptance: the ledger's names cover every string the Approach's inventory lists, and the Chapter shows the ledger's output at the base; the test is red at the base with its duplicate count named; `README.md`'s Test Coverage list names the new test among the offline suites, since the repository has no offline runner and that list is how a suite is found.

Files in scope: `.kit/injection-ledger.mjs` (new), `.kit/injection-ledger.json` (new), `.kit/injection-duplicate-test.mjs` (new), `README.md` (the Test Coverage list), `.kit/.gitignore` (folded in: the directory is ignored by a blanket rule with a per-file allowlist, so the three new files are uncommittable without an allowlist entry).

Tests: at minimum, lock that the duplicate check speaks on a fixture string carrying one `CLAUDE.md` sentence and stays silent on one carrying none, because a guard that is silent for the wrong reason is the failure this plan exists to catch.

### 2. The startup message
Model: opus

Each `*_INSTRUCTION` string in `bin/supervise.sh` is rewritten under the Approach's rule. The channel instruction becomes the reply-tool sentence and a pointer to `CLAUDE.md`. The coordinator role instruction keeps the label semantics and the compaction-boundary clause, and points at the kit's role skill and the three tool descriptions for the rest; its fleet clause keeps the meaning of `[FLEET]` and points at `fleet_status` for the classes. The architect role instruction loses its overrides of the skill-load and steer sentences, which are instead conditioned out of the message for the architect at assembly, and states its clone rule once. The steer instruction takes the same rule as the others: its label semantics stay, since nothing else carries them, and a sentence that restates the `agentic_resolve` description or the doctrine's data-not-instructions bullet points there instead. A sentence that moves rather than goes lands in its owner in the same commit where the owner is in this repository. A sentence whose owner is the kit stays in place and is named in the Chapter as owed to a kit plan, with a kaizen note filed, since a kit surface cannot be edited from here. The parity pin in `.kit/channel-reply-instruction-test.sh` that holds the channel string byte-identical to `REPLY_INSTRUCTION` is removed in this section, because Section 3 deletes that constant. `.kit/channel-reply-instruction-test.sh` is rewritten to pin the sentences that stay and the absence of the ones that went. The Chapter records each string's size before and after, and for every sentence removed names the surface that carries it, and for every sentence kept says why none does. That accounting is the acceptance, not a size.

Acceptance: `.kit/injection-duplicate-test.mjs` reports no duplicate in any supervisor string; every string's ledger number is lowered to its new size in the same commit; `.kit/channel-reply-instruction-test.sh` passes; the Chapter carries the sentence accounting for every string; and one real `claude -p` child launched with `--no-channel` under a scratch persona answers the priming turn with its one line, recorded in the Chapter with the command and output, which checks that the trimmed skill-load and steer strings still assemble into one priming turn and does not exercise the channel string, since `--no-channel` leaves it empty.

Files in scope: `bin/supervise.sh`, `.kit/channel-reply-instruction-test.sh`, `.kit/injection-ledger.json`, `.kit/injection-ledger.mjs` (folded in at the section's approach confirmation: the ledger asserts a fixed assignment count per instruction name and a single-line shape per clause, so consolidating the five `COORDINATOR_ROLE_INSTRUCTION` assignments makes it refuse by design, and the rule that counts them has to move with the strings), `.kit/injection-duplicate-test.mjs` (folded in at round 2: that file states one control per fail-closed ledger guard in its own header, and this section adds such a guard, so the invariant is unmet until its control lands; the fold predicate holds on all three legs and the section close gate runs that suite), `README.md` (the priming-text paragraph and the Test Coverage entry for the channel-reply test), `CLAUDE.md` where a sentence lands there.

Tests: at minimum, lock that the steer label semantics survive for a worker and that the coordinator's message carries no tool contract, because a worker that loses the label misreads a steer as the operator, and the coordinator's message is where the copies were.

### 3. The recurring prompts and context blocks
Model: opus

`REPLY_INSTRUCTION` in `hooks/index.ts` is deleted. Each frame it prefixed keeps its label and its one instruction, which names the reply tool in its own words where the frame's text goes to the operator. The `[GOAL TREE]`, `[NO GOAL]` and idle-nudge texts are rewritten so the `goal_done` clause appears once, in the `goal_done` description, and the blocks point at it. The `[FLEET]` and `[RECONCILE]` frames, which the steward-architect plan's Section 6 adds to `hooks/index.ts`, keep their label and one instruction and point at the `fleet_status` description and the kit's coordinator skill for the rest. The `[BUDGET]` text is absent by this point, and the section asserts that. The authored prose of a fleet note's `composed` half is this section's too, and it is named here because Section 1's review found it reaching the child unsized: nineteen `composed:` sites in `hooks/index.ts`, several of them a sentence or more, which join a `[FLEET]` line through an interpolation and which Section 1's extraction therefore excludes by construction. This section gives them a rule that sizes their literal text, brings them under the duplicate check, and rewrites them under the Approach's rule like any other injected prose. The Chapter carries the same sentence accounting Section 2 carries. `.kit/controller-tick-test.mjs` and any other suite that pins these strings are edited to the new wording. The Chapter records each string's size before and after.

Acceptance: the duplicate test reports no duplicate in any plugin string; every ledger number is lowered in the same commit; the fleet notes' authored prose is one or more ledger entries and the ledger's header no longer names it as a gap; the tick and prompt suites pass with their counts reported against the section's base; and the plugin builds.

Files in scope: `hooks/index.ts`, `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs` where a fixture pins wording, `.kit/injection-ledger.mjs` (the rule that sizes the fleet notes' authored prose), `.kit/injection-duplicate-test.mjs` (its control), `.kit/injection-ledger.json`, `README.md` (the prompt-label table, if one exists, and the coverage sentence that currently names this prose as unsized).

Tests: at minimum, lock that the reply backstop still opens with its label and the answer, because a backstop that loses its shape resends nothing.

### 4. The tool descriptions
Model: opus

Each registered tool's description in `hooks/index.ts` is rewritten to its contract: what the tool does, its parameters, and what it returns or refuses. Role guidance in a description, such as what to do with a `fleet_status` reading, moves to the prompt that delivers the reading where that prompt owns it. Guidance whose owner is the kit's role skill stays in place and is named in the Chapter as owed to a kit plan, with a kaizen note filed. The health-class definitions live in the `fleet_status` description once, and the `[FLEET]` frame points at it. The Chapter records each description's size before and after.

Acceptance: the duplicate test reports no duplicate across the descriptions and the prompts; every ledger number is lowered in the same commit; the Chapter carries the sentence accounting for every description and the descriptions' total before and after; the suites that pin a description pass.

Files in scope: `hooks/index.ts`, `.kit/injection-ledger.json`, the suite that pins a description, `README.md` (the tool table).

Tests: at minimum, lock that each tool's parameter names in its description match its schema, because a description trimmed past its contract is a tool the session cannot call.

### 5. Close
Model: sonnet
Locus: inline

`README.md` is read where it describes the priming text, the prompt labels and the tools, and each sentence is restated for the as-built strings. `docs/README.md` moves this plan's index line as the curating-docs skill states. The Chapter carries the final ledger beside the baseline, and the totals the Goal names.

Acceptance: every offline suite the README's Test Coverage list names passes, the duplicate test among them, each exit code read from its run; the README paragraphs named are read by the section's reviewer pair against the strings; the ledger totals in the Chapter are read from the ledger script's own run.

Files in scope: `README.md`, `docs/README.md`, this plan document.

## Out of Scope

- `CLAUDE.md`, the kit's skills and the relay server's instructions, except where Section 2 moves a sentence into `CLAUDE.md` as its owner.
- When any prompt fires, what any tool does, and which persona holds which tool.
- The steward-architect plan's own rewrite of the coordinator and architect instructions. This plan runs after it and trims what it leaves.
- The doctrine's own copy in the output style, which the kit pins.

## Assumptions

- decided 2026-09-18 by the operator on the coordinator's thread: the runs that hold the box are deferred to one end-run, `docs/plans/agent_persona_deferred-gate-run_v1.md`, which states the gate policy whole. Under it, `.kit/live-all.sh`, every `.kit/live-*-test.sh`, `.kit/supervisor-natural-exit-test.sh` and any check that launches a `claude` child or runs past two minutes of wall clock do not run at this plan's section closes or its finishing pass. Every other check this plan names still runs. A Chapter names each deferred run as deferred, never as passed. Where an acceptance line above names a deferred suite, that clause is met by the end-run.
- decided 2026-09-18 by the operator on the coordinator's thread: the pass is worth doing as a plan of its own. The words are in the Intent.
- assumed 2026-09-18 (default): the order the Approach states, after steward-architect and the context-budget removal and before supervisor-peer; reversal: the operator's word at arming, except that running before steward-architect is not available, since the strings Sections 2 to 4 name exist only once that plan merges.
- assumed 2026-09-18 (default): eight words is the duplicate bound; reversal: one constant in the test, and a lower bound catches pointers as duplicates.
- assumed 2026-09-18 (sibling plans): Branch-and-PR; reversal: none, a header edit.

## Operator Verification

1. After the merge and the installed plugin's update, each persona relaunched at the operator's convenience. The outcome that holds the work: one transcript per persona shows the trimmed startup message, and a kaizen or fleet prompt in a transcript shows one sentence of instruction and its label rather than the block the operator saw on 2026-09-18.

## Open Questions

- Whether `fleet_status` should be registered only for the coordinator and its readers, since a worker cannot call it. Not in this plan. Owner: the operator, after this plan's numbers are in.
- Whether the duplicate test should also read the doctrine file under the operator's profile. Not in this plan, because a repository test should not depend on a file outside the repository; the sentences it would catch are the ones `CLAUDE.md` already carries. Owner: the operator, at arming.
- Whether the rules for writing to the operator should move to the relay server's own instruction text, after which both copies in this repository become pointers. That text is the one surface every channel-attached session reads whatever its working directory holds, which no file in this repository can be. The server lives in another repository, so this is outside this plan and outside Section 2's scope. Not blocking. Recommended after this plan closes. Owner: the operator.

## Related

- `docs/plans/claude-kit_lean-kit_program_v1.md` in the claude-kit repository: the decisions this plan applies, growth declared and one owner per moment.
- `docs/plans/claude-kit_goal-fit_spec_v1.md` in the claude-kit repository: the intent record this plan's Intent section follows.
- `docs/archive/agent_persona_steward-architect_v1.md`: runs before this plan and rewrites two of the strings it trims.
- `docs/archive/agent_persona_context-budget-removal_v1.md`: ran before this plan and deleted the `[BUDGET]` prompt.
- `docs/plans/agent_persona_supervisor-peer_v1.md`: runs after this plan and adds one priming sentence under the guard.
- `docs/plans/agent_persona_deferred-gate-run_v1.md`: holds the live suites this plan's Assumptions defer, and runs them once against the trunk after the last queued plan merges.

## Chapters

### Interim board 1 - 2026-09-18

Not a Chapter. Section 1 is unstarted and no repository file changed beyond this document.

**Stage.** The armed queue's leash advanced to this plan after `docs/plans/agent_persona_context-budget-removal_v1.md` was recorded blocked. Nothing of this plan ran. The `Status:` header stays at `Ready`, because the run is not starting.

**The preceding plan's blocker was tested and does not carry, but this plan's own gate is unmet.** That blocker is the context-budget plan's ordering bar, which is specific to that plan. It stops nothing here. What stops this plan is its own text, read this session from this file. Line 33 states "this plan cannot run before steward-architect". Line 27 sets this plan's base as the trunk after that plan merges, and states that the strings Sections 2 to 4 rewrite exist only on that plan's branch until then. Line 101 records the reversal as unavailable for the same reason. So the bar here is not a preference about ordering. Sections 2 to 4 have nothing to edit at `origin/main`.

**The queue is one chain.** `docs/plans/agent_persona_context-budget-removal_v1.md`'s interim board 3 carries the full analysis, including the operator's recorded word on the order and the corrections to three readings this run had wrong. It is not repeated here.

**Gate baseline.** None taken. No repository code changed, so no test lane ran and there is nothing to diff.

**Live dispatches.** None.

**Asks in flight.** One to the repository's Expert seat, recorded in the context-budget plan's board 3, covering this whole set of ordering blocks. It does not gate and was unanswered at the time of writing.

**Next action.** Nothing, until `docs/plans/agent_persona_steward-architect_v1.md` closes and merges. Section 1 then starts by building the injection ledger against the merged trunk and recording the duplicate test's red baseline.

### Interim board 2 - 2026-09-19

Not a Chapter. Section 1 is still unstarted and no repository code changed. The `Status:` header stays at `Ready`, for the reason board 1 gives.

**Stage.** The armed queue's leash advanced to this plan a second time, after `docs/plans/agent_persona_context-budget-removal_v1.md` was recorded blocked for the fourth time. Nothing of this plan ran.

**The preceding plan's blocker was tested and does not carry.** That blocker is the context-budget plan's own ordering bar: its Assumptions forbid it running beside a plan that edits three named files, and the steward plan edits two of them and is blocked rather than closed. That is a statement about the context-budget plan's files and says nothing about the injection ledger or the strings this plan trims. It stops nothing here.

**This plan's own gate is unmet, re-read from this file this session rather than carried from board 1.** Line 33 states "this plan cannot run before steward-architect". Line 27 sets the base as the trunk after that plan merges, and states that the strings Sections 2 to 4 rewrite live on that plan's branch and in its Section 6 worked tree, with the trunk carrying four instruction strings and none of them. Line 101 records the reversal as unavailable for the same reason. Sections 2 to 4 have nothing to edit at `origin/main`.

**The ground below the chain is confirmed and has moved since board 1.** `docs/plans/agent_persona_steward-architect_v1.md:3` reads `Status: In Progress`, so it has neither closed nor merged. Its blocker is now the ownership fork its interim board 25 records: whether the steward persona gets its own working directory. That is a different question from the one standing at board 1, and it is the operator's to answer. It does not change this plan's bar, which turns on the merge rather than on the reason the merge is waiting.

**Gate baseline.** None taken. No repository code changed, so no test lane ran and there is nothing to diff.

**Live dispatches.** None.

**Asks in flight.** One to the repository's Expert seat, recorded in the context-budget plan's board 3, covering this whole set of ordering blocks. It does not gate and is still unanswered.

**Next action.** Nothing, until `docs/plans/agent_persona_steward-architect_v1.md` closes and merges. Section 1 then starts by building the injection ledger against the merged trunk and recording the duplicate test's red baseline.

### Interim board 3 - 2026-09-19

Not a Chapter. Section 1 is still unstarted and no repository code changed. The `Status:` header stays at `Ready`.

**Stage.** The armed queue's leash advanced to this plan a third time, after `docs/plans/agent_persona_context-budget-removal_v1.md` was recorded blocked for the fifth time. Nothing of this plan ran.

**The preceding plan's blocker was tested and does not carry.** It is the context-budget plan's own bar against running beside a plan that edits three named files. That says nothing about the injection ledger or the strings this plan trims.

**This plan's gate is confirmed unmet from the trunk's content, which is a different reading from boards 1 and 2.** Those argued from this document's own sentences. This one opened the files at `origin/main`, now `266b396`. `bin/supervise.sh` there carries no `ARCHITECT_ROLE_INSTRUCTION`, and `hooks/index.ts` there carries no `[FLEET]` frame, no `[RECONCILE]` frame and no `fleet_status` tool. So Section 2 has no architect string to rewrite, Section 3 has no fleet frames to trim, and Section 4 has no `fleet_status` description to reduce. `REPLY_INSTRUCTION` is the one named string that does exist at the trunk, at five occurrences.

Section 1 is gated by the same fact rather than merely by the order. Its acceptance requires the ledger's names to cover every string the Approach's inventory lists, and four of those strings are absent at the trunk. A ledger built there would record a baseline that every later section reports against, and it would be the wrong baseline.

**The ground below the chain has moved again since board 2.** `docs/plans/agent_persona_steward-architect_v1.md:3` still reads `Status: In Progress`. Its blocker is no longer the working-directory ownership fork board 2 names. It is now the review-round backstop question its Section 6 records, which is the operator's to answer. No pull request exists for the `steward-architect` branch in any state, so the merge this plan's base waits on has not been opened, let alone landed.

**Gate baseline.** None taken. No repository code changed, so no test lane ran and there is nothing to diff.

**Live dispatches.** None.

**Asks in flight.** One to the repository's Expert seat, recorded in the context-budget plan's board 3, covering this whole set of ordering blocks. It does not gate and is still unanswered.

**Next action.** Nothing, until `docs/plans/agent_persona_steward-architect_v1.md` closes and merges. Section 1 then starts by building the injection ledger against the merged trunk and recording the duplicate test's red baseline.

### Interim board 4 - 2026-09-19

Not a Chapter. Section 1 is still unstarted and no repository code changed. The `Status:` header stays at `Ready`, for the reason board 1 gives.

**Stage.** The armed queue's leash advanced to this plan a fourth time, after `docs/plans/agent_persona_context-budget-removal_v1.md` was recorded blocked for the sixth time. Nothing of this plan ran.

**The preceding plan's blocker was tested and does not carry.** It is the context-budget plan's own bar against running beside a plan that edits `bin/supervise.sh`, `bin/supervise-decide.mjs` or `hooks/index.ts`, together with the operator's recorded word on the order. That is a statement about that plan's files and about its place in the queue. It says nothing about the injection ledger or the strings this plan trims, and it is not the ground below.

**This plan's gate is confirmed unmet from the trunk's own content, measured this session and sharper than board 3's reading.** `origin/main` is at `266b396`, unmoved since board 3. Predicate `git grep -c` over `bin/supervise.sh` and `hooks/index.ts` at that ref, for each string Sections 2 to 4 name: `ARCHITECT_ROLE_INSTRUCTION`, `[FLEET]`, `[RECONCILE]` and `fleet_status` all returned no match. The control was `REPLY_INSTRUCTION` over the same two files at the same ref, which returned 4 and 5, so the predicate and the scope both speak.

The count settles Section 1 on its own terms rather than by order alone. `git grep -o -E "[A-Z_]+_INSTRUCTION=" ` over `bin/supervise.sh` at that ref returns exactly four names: `CHANNEL_REPLY_INSTRUCTION`, `COORDINATOR_ROLE_INSTRUCTION`, `COORDINATOR_STEER_INSTRUCTION` and `SKILL_LOAD_INSTRUCTION`. Section 1's own text names "the five `*_INSTRUCTION` variables", and its acceptance requires the ledger's names to cover every string the Approach's inventory lists. A ledger built at this base would be short by one instruction string, one tool description and two prompt frames, and it would be the baseline every later section reports against.

**One route was considered and rejected, recorded so it is not re-litigated.** Two of Section 1's three new files, the ledger script and the duplicate test, extract by anchor and would run against either base, so only `.kit/injection-ledger.json` is base-dependent. Writing the two now was rejected on two grounds. The checkout sits on `steward-architect`, so a commit would land this plan's work on another plan's branch and into that plan's pull request, against the doctrine's stay-in-scope rule. And the Approach's base paragraph sets the base as the trunk after that plan merges, which is a statement about where the work lands rather than about the order it runs in.

**The ground below the chain, re-read rather than carried.** `docs/plans/agent_persona_steward-architect_v1.md:3` still reads `Status: In Progress`. `gh pr list --head steward-architect --state all` returns an empty list, so no pull request has been opened in any state and the merge this plan's base waits on has not happened. That plan's current blocker is the review-round backstop its Section 5 records, which is the operator's to answer.

**Gate baseline.** None taken. No repository code changed beyond this document, so no test lane ran and there is nothing to diff.

**Live dispatches.** None.

**Asks in flight.** None of this plan's own. An ask to the repository's Expert seat covering this set of ordering blocks went out again this session under the context-budget plan's declaration, and remains unanswered, as do the two before it.

**Next action.** Nothing, until `docs/plans/agent_persona_steward-architect_v1.md` closes and merges. Section 1 then starts by building the injection ledger against the merged trunk and recording the duplicate test's red baseline.

### Interim board 5 - 2026-09-19

Not a Chapter. Section 1 is still unstarted and no repository code changed. The `Status:` header stays at `Ready`, for the reason board 1 gives.

**Stage.** The armed queue's leash advanced to this plan a fifth time, after `docs/plans/agent_persona_context-budget-removal_v1.md` was recorded blocked for the seventh time. The plan before that one in the queue, `docs/archive/agent_persona_steward-architect_v1.md`, is now Complete and archived, which is new. Nothing of this plan ran.

**The preceding plan's blocker was tested and half of it carries, which is a different answer from boards 1 to 4.** That blocker was recorded as: pull request 49 merged, and the operator's word to arm execution. Its first half is not specific to the context-budget plan at all. This plan's own Approach at line 27 fixes its base as the trunk after the steward plan merges, and line 33 states this plan cannot run before it. So the merge is this plan's own bar on this plan's own text, reached independently rather than inherited. Its second half does not carry as written, because that blocker turned on the context-budget plan's own grant covering authoring rather than execution. This plan's authorization problem is a different one, recorded below.

**The base bar is confirmed unmet, re-measured this session rather than carried.** `git fetch` then `git rev-parse origin/main` returns `266b396`, unmoved since board 4. A predicate `git grep -c` at that ref over `bin/supervise.sh` and `hooks/index.ts` returns no match for `ARCHITECT_ROLE_INSTRUCTION`, `[FLEET]`, `[RECONCILE]` or `fleet_status`. The control was `REPLY_INSTRUCTION` over the same two files at the same ref, returning 4 and 5, so the predicate and the scope both speak. `gh pr view 49` reports state OPEN, `mergedAt` null, `reviewDecision` REVIEW_REQUIRED, with auto-merge enabled at 2026-09-19T23:42:04Z. So the merge has not happened and one approval is what causes it.

**A new fact boards 1 to 4 did not have: the content base now exists on a branch, though not on the trunk.** The checkout sits on `context-budget-removal`, cut from the steward plan's tip. The same predicate at `HEAD` returns `ARCHITECT_ROLE_INSTRUCTION` 3, `[FLEET]` 2 and 14, `[RECONCILE]` 2 and 12, and `fleet_status` 2 and 12 across the two files. `git grep -o -E "[A-Z_]+_INSTRUCTION=" ` at `HEAD` over `bin/supervise.sh` returns exactly the five names Section 1 expects. So Sections 2 to 4 have their strings here and Section 1 would build a correct ledger baseline here. What is absent is not the content but the trunk the plan names as the place the work lands. Board 4's rejection of starting early rested partly on the checkout sitting on another plan's pull request branch, and that ground is gone. The other ground, the Approach's base paragraph, stands.

**The authorization bar, which no earlier board of this plan examined.** `kit-goal.js status` reports this plan as "(armed: recorded as this run's own arming) (authorization: none recorded)". The two plans behind it in the queue each report an authorization sentence, so the gap is this plan's rather than the queue's. A grep of this file for `Dispatch Authorization` returns nothing, which agrees with the goal state. The kit-goal skill states that an armed plan is approved by the arming act, and that where a run armed the plan for itself the committed `## Dispatch Authorization` grant it traced stands in the typed invocation's place. With no such section there is nothing to stand in that place, so the arming in force carries no approval for executing this plan. That is a defect in the plan document rather than a fact about the work, and it is fixed by the operator's word or by a grant committed into this file.

**Both bars resolve together.** One operator act approving pull request 49 lands the merge. One word arming execution answers the authorization. Neither is something this session can supply for itself.

**Gate baseline.** None taken. No repository code changed beyond this document, so no test lane ran and there is nothing to diff.

**Live dispatches.** None.

**Asks in flight.** One to the repository's Expert seat, sent this session, asking whether an operator authorization for this plan exists somewhere the goal state does not reach, and whether the absent section reads differently given the plan predates the section becoming usual. A notice went to the `coordinator` seat. The asks recorded at boards 1 to 4 are unanswered and are now moot, having turned on the steward plan being unfinished.

**Commit note.** This entry is committed on `context-budget-removal` rather than on a branch of its own. That branch is this run's holding place for queue bookkeeping while pull request 49 is frozen, and it already carries the context-budget plan's board 7. A branch per board entry would leave the operator a sprawl to clean up for no gain.

**Next action.** Nothing, until pull request 49 merges and the operator arms execution. Section 1 then starts by building the injection ledger against the merged trunk and recording the duplicate test's red baseline.

### Interim board 6 - 2026-09-19

Not a Chapter. Section 1 is built, reviewed once, fixed and re-verified, and is awaiting its round 2 review. It is not closed and carries no `Completed:` line.

**Both bars boards 1 to 5 recorded are discharged, and this is the first board of this plan that says so.** The base bar: `gh pr view 49` reports state MERGED at 2026-09-20T00:12:40Z, `origin/main` is `952e940`, and a `git grep -c` at that ref over `bin/supervise.sh` and `hooks/index.ts` returns matches for all four strings boards 3 to 5 found absent, `ARCHITECT_ROLE_INSTRUCTION` 3, `[FLEET]` 2 and 14, `[RECONCILE]` 2 and 12, `fleet_status` 2 and 12. The control was `REPLY_INSTRUCTION` over the same two files at the same ref, returning 4 and 6, so the predicate and the scope both speak. `git grep -o -E "[A-Z_]+_INSTRUCTION=" ` at that ref over `bin/supervise.sh` returns exactly the five names Section 1 expects. The authorization bar: this document now carries a `## Dispatch Authorization` section recording the operator's `/kit-goal` arming of 2026-09-19, committed in `87b082d`, which is what board 5 found missing.

**Status normalization.** The header read `Status: Ready` at this run's start and now reads `Status: In Progress`, set as part of starting the run per the executing-work skill. That is an edit inside the approval-scoped fingerprint and is recorded here deliberately.

**The base this plan executes from is the branch, not the trunk, and that is a decision rather than a drift.** The Approach fixes the base as the trunk after the steward plan merges, and the Order paragraph separately requires the context-budget removal to have happened, because that removal deletes the `[BUDGET]` prompt this plan would otherwise trim. The first holds at the trunk. The second does not: `git grep -c -E '\[BUDGET\]|contextBudget'` at `origin/main` returns 32 matches in `hooks/index.ts`, because pull request 50 is open and unmerged. The branch `lean-injection` is cut from `f43eee7`, the context-budget branch tip, where the same predicate returns no match and all four steward strings are present. That is the only base where both of this plan's stated preconditions hold at once. `origin/main` is not an ancestor of that tip, the branch having been cut from the steward branch tip whose tree pull request 49's merge commit reproduces, so this plan's pull request will target `context-budget-removal` and retarget itself to `main` when 50 merges.

**Section 1 stage.** Built by `implementer-sonnet`, reviewed at round 1 by `adversarial-reviewer`, `blind-reviewer` and `security-reviewer`, all three at opus and effort high through Workflow run `wf_7cbbed24-0a0`. Round 1 returned one Critical, eight Majors and ten Minors across the three lenses. All of them are fixed in one fix round at the same tier, and the fix is verified. Round 2 is owed and not yet dispatched.

**What round 1 found, in one sentence.** The guard failed open: a string the ledger could not match was recorded as zero characters rather than raising, and the size check only ever reported growth, so a renamed or reformatted instruction string dropped out of both checks while the suite read green. Section 2 rewrites all five of those strings, so the failure would have fired on the very next section. All three lenses reached it independently.

**Rulings adopted since the last boundary.**

- `.kit/.gitignore` is folded into Section 1 and its `Files in scope` line is widened to name it. That directory is ignored by a blanket rule with a per-file allowlist, so the section's three deliverables cannot be staged without an allowlist entry. The fold test is met: the file sits in the same directory as files the section changed, it needs no acceptance criterion the section does not carry, and the close gate covers it.
- Two injected strings in `bin/supervise.sh` that the Approach's inventory never listed, `GOAL_PROMPT_FRAMING` at line 2657 and the `[SUPERVISOR-PRIMING]` marker at line 2841, are added to the ledger. They trace to the Goal sentence "every prompt the plugin or the supervisor writes into a session", so they are spec-traceable rather than a new requirement, and they sit in a file the ledger already reads.
- A third unledgered site, `hooks/operator.ts`, is deliberately left out. Adding it would widen the ledger to a third file the section never names. It is recorded here for Sections 3 and 4, which already work in `hooks/`.
- Two shipped sentences claimed the check exempts nothing and that it covers every string the plugin and the supervisor inject. Both were false while `hooks/operator.ts` stands outside the ledger. Both are corrected to name the two files actually read.

**Gate baseline, measured on this worktree at branch `lean-injection` with the section's work unstaged and three foreign untracked paths present.** `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. `.kit/injection-duplicate-test.mjs` exit 1 with 19 duplicate sentences, which is this section's intended red baseline and is up from the 18 the pre-fix ledger found; the gain is the sentence "When this step is done, call goal_done with a one-line note.", carried by three plugin strings and missed before because an escaped newline was never decoded and so never read as a sentence boundary. All eight of the test's own fixture controls report both directions. `.kit/controller-tick-test.mjs` exit 0. `.kit/self-review-unit-test.mjs` exit 0. Every exit code read from its own unpiped run.

**Controls run against the guard itself, both withheld from the patterns they test.** Lowering one recorded size made the size check name the string and the two numbers. Adding a baseline row with no live counterpart made the reconciliation check name it and say the extraction is gone or renamed. Both probes restored from a pre-probe copy and verified byte-identical against it.

**Deferred and named deferred, never passed**, under the operator's gate policy of 2026-09-18 recorded in this plan's Assumptions: `.kit/live-all.sh`, every `.kit/live-*-test.sh`, `.kit/supervisor-natural-exit-test.sh`, and any check launching a `claude` child.

**Live dispatches.** None. Both implementer dispatches and all three round 1 reviewers have completed and returned.

**Asks in flight.** None.

**Next action.** Dispatch Section 1's round 2 review. Round 1 returned a Critical that survived adjudication, so round 2 runs at round 1's roster and tier rather than decaying to one lens. The security lens is dropped from that roster because its trigger does not hold: it reported that neither new file references `child_process`, `spawn`, `exec`, `eval` or `new Function`, so the process-execution trigger the round 1 brief asserted was the brief's error rather than the code's. Round 2 is therefore the adversarial and blind pair at opus and effort high.

### Interim board 7 - 2026-09-19

Not a Chapter. Section 1 is built, reviewed twice, escalated one tier, fixed and re-verified. It is not closed and carries no `Completed:` line. The run is parked here on the operator's request, recorded below.

**Stage.** Section 1 has taken two review rounds. Round 1 returned one Critical, eight Majors and ten Minors and was fixed at the writer's own sonnet tier. Round 2 returned one Critical, one Major from the adversarial lens and two from the blind lens, and fifteen Minors. Round 2's Critical drove a tier escalation, and the fix round that followed ran at fable. That fix is verified. Round 3 is owed and deliberately not dispatched.

**What round 2 found, and why it moved the tier.** Round 1's Critical was that the guard failed open: a string the ledger could not match was recorded as zero characters rather than raising. The round 1 fix made a total extraction loss throw. It did not make a partial loss throw. Two of the five `*_INSTRUCTION` strings are assembled from several assignment lines, `COORDINATOR_ROLE_INSTRUCTION` from five at `bin/supervise.sh:2686, 2688, 2711, 2719, 2737`. A reformatting of any one of them left the name resolving, the zero-character check silent, the name reconciliation silent, and the size check reporting only growth, so several thousand characters could vanish while the suite read green. Section 2 rewrites all five of those strings.

So the same class survived two rounds: the guard fails open on an extraction that silently loses content. Under the executing-work skill's tier-escalation ladder a repeating class means the tier is the lever rather than the brief, and the comparison was made before the bump rather than after. Section 1's writer tier was sonnet and this session runs below fable, so the section took its one re-dispatch to `implementer-fable` with the fable model override, both rounds' findings written into the brief. The dispatch resolved at `claude-fable-5-1` on every assistant turn, read from its own transcript.

**The five fixes, each with the control that proves it fires.**

- The Critical. `.kit/injection-ledger.mjs` now carries a per-name expected assignment count and throws `[instruction-count]` naming the variable and both numbers, mirroring the `PRIMING_BODY` rule that was already in the file. Confirmed by this session's own withheld probe: `bin/supervise.sh:2719` split across two lines with a backslash continuation, a shape no pattern in the ledger names and which bash still reads as one string. The ledger exits 0 unmutated, exits 1 under the probe naming `[instruction-count] COORDINATOR_ROLE_INSTRUCTION ... expected 5 ... found 4`, and exits 0 again after restore. The restore was verified byte-identical against a pre-probe copy and `git status` reports the file clean.
- A newly added injected string was invisible to all three checks, because three of the four families were a fixed name list rather than a structural read. The ledger now reconciles, in both directions, every `*_INSTRUCTION` left-hand side, every variable spliced into the priming write, every `contextBlocks.push` identifier, and every `submitExpectedTurn` call site, each under its own tag. That is what the Approach's Order paragraph requires when it says this plan runs before the supervisor-peer plan so that plan's new priming sentence is written under the guard.
- The ledger's own comment claimed the fleet prompt's trailing lines were entirely per-reading data. That was false. Nine field labels and two sentence frames at `hooks/index.ts:1225, 1262-1268, 1290, 1292` are authored prose that nothing sized. They are now one ledger entry, `FLEET_PROMPT_LINE_LITERALS` at 288 characters, and the false comment is corrected.
- Two prompt frames at `hooks/index.ts:3438` and `:3522` are built by helpers in `hooks/operator.ts`, a third file this section never names. Rather than widen the ledger to it, the exclusion is declared by name, pinned by the test, and asserted per site: the call must pass only data, carrying no quote or backtick. The ledger header, the test header and `README.md` all state the bound, so no shipped sentence claims coverage the guard does not have. Section 1's own text needed no amendment.
- `README.md:369` now says the suite is red by design until Sections 2 to 4 land. Without it a peer running the whole gate reads a fresh failure outside its own diff and misattributes it.

**Gate baseline.** Measured by this session on this worktree at branch `lean-injection`, HEAD `6190fed`, with the section's fix-round work unstaged and one foreign untracked path present (`.claude/worktrees/`), at 2026-09-20T03:54:44Z, 40 seconds wall clock, under a heavy-process claim this session wrote and released, with no foreign test runner or build in the process list and no live claim standing before it. Every exit code read from its own unpiped run. `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. `node .kit/injection-ledger.mjs` exit 0 with zero bytes on stderr. `node .kit/injection-duplicate-test.mjs` exit 1, which is this section's intended red baseline, unchanged at 19 duplicate sentences against the 19 recorded at board 6; its name reconciliation and its size check both report OK, and its output carries no stack frame and no `Error` line, so the red is duplicates alone and not a throw. `node .kit/controller-tick-test.mjs` exit 0. `node .kit/self-review-unit-test.mjs` exit 0. The committed `.kit/injection-ledger.json` is byte-identical to the live ledger's output, compared this session. The ledger holds 39 entries, up from 38, the one addition being the fleet-line literals.

**Rulings adopted since the last boundary.**

- The two record-delivery prompt frames are excluded from the ledger by name rather than reached through `hooks/operator.ts`. The ground is that those call sites pass only data, so there is no literal in `hooks/index.ts` to read, and adding a third source file would widen a section that names two. The exclusion is asserted rather than assumed, which is what keeps it from being a silent hole.
- The fleet prompt's line literals are ledgered rather than excluded, because they sit in `hooks/index.ts`, a file this section already reads.
- No Minor was worked. Twenty-two accumulated Minors and claim findings across both rounds are recorded at `.kit/scratch/lean-injection/minors-section-1.md` for the single close pass the skill directs, which runs before the close gate and after the terminal condition is met.

**Carried forward for later sections, from the fix round's own concerns.** Section 2 will trip the new `[priming-write]` and `[instruction-count]` rules when it conditions the architect string out of the assembly or changes a clause count. That is the declared-growth design working, and Section 2's brief should name the count table and the write pattern as the places to update. Section 3 deletes `REPLY_INSTRUCTION`, so the extraction rule for it and every frame pattern anchored on it will throw until retired, and Section 3's brief should name them.

**Deferred and named deferred, never passed**, under the operator's gate policy of 2026-09-18 recorded in this plan's Assumptions: `.kit/live-all.sh`, every `.kit/live-*-test.sh`, `.kit/supervisor-natural-exit-test.sh`, and any check launching a `claude` child.

**Live dispatches.** None. Both round 2 reviewers and the escalated fable implementer have completed and returned, and nothing was stopped.

**Asks in flight.** None.

**The park, and a report this session tested rather than accepted.** The operator asked on their own channel, while the fix round was in flight, that this run shut down after the review round once everything was written durably. This entry, the commit that carries it, its push and the compaction checkpoint are that record. The fix round was allowed to finish rather than stopped, because its work was nearly complete and a stop would have discarded the Critical's fix.

A later channel message reported that a fable usage limit had been hit, that this probably killed the reviewers, and that their work was lost. This session tested that and it does not hold, so no work was discarded on it. Round 2's two reviewers completed and returned full findings and ran at `claude-opus-5` rather than fable, read from their own transcripts, so a fable limit could not have reached them. The fable implementer also completed and returned its report. Its output is not taken on trust either: the gate baseline above is this session's own run, and the Critical's control is this session's own probe. What sits in the worktree is verified work, not a partial edit.

**Next action.** Dispatch Section 1's round 3 review. Round 2 returned a Critical that survived adjudication, so round 3 runs at round 1's roster rather than decaying to a single lens: the adversarial and blind pair, with no security lens, that trigger having been tested at round 1 and found not to hold. The tier is one above the writer tier, and the writer tier is now fable after the escalation, so the ceiling applies and both reviewers run at fable. A Fable reviewer carries its agent's frontmatter effort, so round 3 rides the Agent tool at effort low rather than the Workflow route the opus rounds needed. Where a fable allotment is genuinely unavailable at that moment, the finishing-work skill's unavailability rule governs what the round does instead, and it is confirmed from an actual dispatch failure rather than assumed. The base ref for the review is `f43eee7`. After round 3 adjudicates: the Minor close pass, the section close gate, Section 1's Chapter, and then Sections 2 to 5.

### Interim board 8 - 2026-09-20

Not a Chapter. Section 1 has taken three review rounds. Round 3's findings are adjudicated and its fixes are built and gated. Round 4 is dispatched. No `Completed:` line is written.

**Stage.** Section 1 is the only section in flight. Sections 2 to 5 are not started. The base bar boards 1 to 5 recorded stayed discharged: `origin/main` is `2da6543` and carries all five `*_INSTRUCTION` names, the `[FLEET]` and `[RECONCILE]` frames and the `fleet_status` tool. A first reading of this session's own missed them, because the predicate anchored each name to the start of a line while every assignment sits indented inside a function. The corrected predicate, a grep for `INSTRUCTION` over that file at that ref, returns all five. An outline never proves absence, and this one nearly cost the plan a false blocker.

**What round 3 returned.** The adversarial lens returned one Critical, three Majors and three Minors, verdict CHANGES_REQUIRED. The blind lens returned six Minors and no Critical or Major, verdict APPROVED_WITH_CONCERNS. Every Critical and Major was confirmed against the cited code by this session before it was acted on.

**The Critical, and why the tier moved to the main thread.** The three plugin-side chain rules read the backtick pieces of a chain joined by `+` and ignored whatever sat between them. Two carried no completeness check at all and the third refused only total loss. So a piece rewritten to a quoted literal, or factored into a constant and spliced back by name, left the entry short with nothing raised: the name still resolved, and the size check reports growth and never a shrink, so the loss read as a trim. Section 3 rewrites exactly those constructs. This is the third consecutive round returning one class, fail-open on a silent extraction loss, at a different site each time. Under the tier-escalation ladder a repeating class means the tier is the lever, and the comparison was made before the move rather than after: round 1's Critical was total loss on any rule, round 2's was partial loss on the shell rules, round 3's is partial loss on the plugin rules. Round 2 already spent this section's one re-dispatch to the fable implementer, and that attempt is the one round 3 found this Critical in, so the ladder's remaining exit for a session below fable is the main thread. This session took the fix.

**The fix, which closes the class rather than the three sites.** A shared chain reader tokenizes a chain joined by `+` and refuses any operand it was not told to expect, each rule declaring by name the whole variables it splices in, `siblingLine` and `lastNote` for the goal-tree block and none elsewhere. It tracks template nesting inside an interpolation, so a backtick inside an interpolation cannot close a piece early. The literal-chain parser every tool description uses now throws when a `+` is followed by anything but another quoted literal, which is the same hole on the path Section 4 rewrites. Both refusals name the entry that would have shrunk.

**The two further Majors, and what each became.** The ledger's declared coverage named two record-delivery sites as its exclusions while `hooks/operator.ts` itself says there are three. The third hands its record to the running turn as tool-result context, so no prompt-call-site row reaches it and the two named rows could never have bounded the class. It is now bounded by a count asserted over every `deliveryText` call site, with each site required to pass only data in its ground, id and text arguments, and both the names and the count are pinned. The README's coverage sentence said two and now says three. Its security section already said three, so the two halves of that document disagreed.

The other Major is the authored prose of a fleet note's `composed` half, nineteen sites in `hooks/index.ts` and several of them a sentence or more, which reaches the child through an interpolation and which no rule sizes. Section 1's own extraction excludes interpolated content by construction, so sizing it here would need an acceptance criterion this section does not carry, and it cannot fold. It is not routed out of the plan either, because the Goal covers it: it is a prompt the plugin writes. It is added to Section 3, which already rewrites the `[FLEET]` frame, with its files and its acceptance named there. That is approval drift and is recorded here as such. The claim half was in scope and is fixed: the ledger's comment said the joined lines were field labels and per-row data alone, which was false, and the header's declared-gap list did not name this prose. Both now do, in the ledger and in the README.

**A Minor upgraded on a stated consequence.** This suite is red by design until Sections 2 to 4 land, so exit 1 could not tell a real duplicate from a broken control, and a regression in any control was invisible at the level a gate reads. A control or guard failure now exits 2, and exit 1 means every control passed. The predicate that sorts the two classes is driven by a case rather than asserted, over three instrument labels and five real-check labels taken from the strings this file actually prints.

**Every fix driven by a case watched to fail first.** The three chain controls each failed against the unfixed ledger with the message that the ledger built with no throw, which is the hole rather than a missed anchor, and each now passes naming its own tag and its own entry. The delivery controls include the coverage case rather than the instrument case: a literal added at the site no named row reaches, which the two named rows passed before the shape rule existed. Seven further Minors are accumulated at `.kit/scratch/lean-injection/minors-section-1.md` for the single close pass.

**Gate, run by this session on this worktree at branch `lean-injection`, HEAD `092c07c` with round 3's fixes unstaged and one foreign untracked path present, at 2026-09-20T11:05Z, under a heavy-process claim this session wrote and released.** The box was contended once: the plugin persona held the claim, and this session waited 115 seconds for it to clear rather than running beside it. Every exit code read from its own unpiped run. The type check exits 0. The ledger exits 0 with zero bytes on stderr. The duplicate test exits 1, the intended red, at 19 duplicate sentences, unchanged from board 7's 19, with every control and guard passing and no stack frame in its output. The controller tick suite exits 0. The self-review suite exits 0. The committed `.kit/injection-ledger.json` parses identical to the live ledger output at 39 entries, and no entry's character count moved across this fix, so the delta changed what the ledger refuses and not what it reads.

**Rulings adopted since the last boundary.** Three, all this session's, and all turning on facts about the system rather than on preference. The completeness guard belongs to the channel rather than to the three rules that first needed it, so it is one shared reader the rules declare into rather than three patched patterns. The delivery-site class is bounded by a count over its own shape rather than by a list of names, because the site that escaped was the one no name reached. And the fleet notes' prose belongs to Section 3 rather than to a new section or to the backlog, because Section 3 already owns that frame and the Goal already covers the prose.

**Live dispatches.** Round 4's adversarial and blind pair, dispatched at fable over round 3's fix delta. Round 3 returned a Critical that survived adjudication, so round 4 runs round 1's roster rather than decaying to one lens, at one tier above the writer tier, which is now this session's main thread, with fable as the ceiling. Its base ref is `f43eee7`.

**Next action.** Adjudicate round 4. Then the Minor close pass, the section close gate, Section 1's Chapter, and Sections 2 to 5.

### Interim board 9 - 2026-09-20

Not a Chapter. Section 1 has taken four review rounds. Round 4's findings are adjudicated and its fixes are built and gated. Round 5 is dispatched. No `Completed:` line is written.

**Stage.** Section 1 is the only section in flight. Sections 2 to 5 are not started. The base bar boards 1 to 5 recorded stays discharged.

**Backstop ladder stage, recorded because it is state that would otherwise live only in this session.** Four review rounds have been adjudicated on this section. Round 5 is the opening bound. If round 5's adjudication still leaves the terminal condition unmet, the review-round backstop fires and this section stops on the BLOCKED path rather than opening the fix round that adjudication would owe. A session resuming here reads the count as four adjudicated, round 5 in flight, no restart yet taken.

**What round 4 returned.** The adversarial lens returned one Critical, four Majors and six Minors, verdict CHANGES_REQUIRED. The blind lens returned one Major and eight Minors, also CHANGES_REQUIRED. Their Majors overlap on one subject, so the round carries four distinct Majors. Every Critical and Major was confirmed against the cited code by this session before it was acted on, and one Minor was refuted.

**The Critical, and the reading that matters more than the Critical.** Three rules that read a single template literal captured it with a pattern that stopped at the closing backtick and asserted nothing about what followed, so a frame rewritten as a two-piece chain recorded the first piece and dropped the rest with no throw. The lens probed it: the still-waiting frame fell from 16 characters to 1, the kaizen frame from 92 to 29, the backstop frame from 93 to 37. Section 3 rewrites exactly those three frames.

This is the fourth consecutive round returning one class, fail-open on a silent extraction loss. Round 3's fix was described in its own commit message as moving the guard to the channel, one shared reader every rule declares into. That description was wrong and this board records the correction. There was never one channel. The ledger has sixteen extraction rules across at least four independent reading mechanisms, and round 3 closed one of them. Round 3's own add-decision line claimed the scope of "every literal-extraction rule" and the delta reached four of sixteen, with nothing in the round checking that claim against the rule set.

**So round 4's fix was preceded by an enumeration rather than aimed at the named sites.** All sixteen rules were classified this session by whether their pattern carries an anchor proving it consumed the whole source expression. Seven fail closed: the fleet frame anchors on the concatenation that follows it, the paused, ENV and LESSON blocks anchor on the backtick-semicolon that closes their statement, and four go through round 3's chain reader. Six fail open. Three are the lens's Critical. A fourth is `extractMemoryBlock`, whose pattern carries no anchor at all and which no lens named; its source is already a chain and is read correctly today only because its second operand happens to be data rather than text. Two more overcount rather than undercount and are recorded as Minors.

**The fix.** The three frame rules now capture the whole expression region, bounded by an anchor that proves where it ended, and pass that region to the shared chain reader. The reader joins every template piece and refuses only an operand it cannot size, so a split into template pieces is read correctly rather than refused: the invariant is that no content is lost silently, not that chains are forbidden. The memory block refuses instead, because its operands are quoted strings and its real second operand is a call no chain reader can size, so there is no correct reading of a split there.

**The three further Majors.** The delivery-site assertion tested for a quote character, so text hoisted into a constant and spliced back by name passed it; each of the three positional arguments must now be a bare identifier or member path, asserted positively rather than by screening out one bad character. The same rule sliced the options object off unexamined on the stated ground that its values select a prefix composed elsewhere, which is true of `mark` and false of `answerTo`, whose value `hooks/operator.ts:759` splices into the delivered text verbatim; a literal `answerTo` is now refused. The interpolation stripper stopped at the first closing brace while the reader that captured the piece counted brace depth, so a nested interpolation left residue in the entry as prose the sentence matcher then indexed; the stripper now uses the same depth-aware scan, shared rather than written twice. The exit-code split drove a second copy of its own predicate, so a drift in the classifier would have left the self-test passing; one hoisted predicate now serves both, and it names the split's own failure label, which it did not. A refusal out of the real run was uncaught and ended the process on Node's own exit code 1, which is the code meaning the designed red; it is now caught and exits 2.

**One finding refuted.** The adversarial lens reported that interim board 8 is absent from the plan doc and lives only in scratch. It is at line 313 of this document and is present in the commit `bffab4c`. The lens grepped for the heading with two hashes where the heading carries three. A predicate narrower than its subject produced a false absence, which is the same class board 8 itself recorded about this session's earlier grep over the trunk.

**One finding half-right, and the control that showed it.** The lens reported by probe that a literal `answerTo` builds with no throw. This session's first control for that finding was placed at the first delivery site and passed, because that site is named by a row whose check reads the whole argument string including the options object. The hole is the third site's, which no such row reaches. The control was moved there and went red. A control drawn from where the pattern already reaches proves the instrument and says nothing about coverage.

**Every fix driven by a case watched to fail first.** Six controls were written before any fix and all six failed against the unfixed ledger with the message that the ledger built with no throw, which is the hole rather than a missed anchor. The nested-interpolation control reproduced the lens's figures exactly, 255 characters growing to 266, and now holds at 255. The three frame controls assert that the recorded size does not move across a split, which is the invariant the class turns on, rather than asserting a refusal. The exit-code catch was driven by a tree-mutating probe run with no agent in flight: a fourth delivery call site was spliced into `hooks/index.ts`, the suite exited 2 naming the refusing rule where it would previously have exited 1, and the tree was restored from a filesystem copy taken before the mutation and verified byte-identical against that copy, with no porcelain delta across the probe.

**Gate, run by this session on this worktree at branch `lean-injection`, HEAD `bffab4c` with round 4's fixes unstaged and one foreign untracked path present, at 2026-09-20T11:20Z, under a heavy-process claim this session wrote and released.** Every exit code read from its own unpiped run. The type check exits 0. The ledger exits 0 with zero bytes on stderr. The duplicate test exits 1, the intended red, at 19 duplicate sentences with zero instrument failures, unchanged from boards 7 and 8. The controller tick suite exits 0. The self-review suite exits 0. The live ledger reads 39 entries against the committed baseline's 39 with no entry's character count moved, checked entry by entry, so this delta changed what the ledger refuses and not what it reads.

**Rulings adopted since the last boundary.** Three, all this session's. The class is closed by enumerating the rule set and classifying every member, rather than by fixing the sites a lens named, because four rounds of the latter produced four rounds of the same class. The invariant for a chain-capable rule is that the recorded size holds across a split, not that a split is refused, because refusing a shape the reader sizes correctly is the weaker guard. And a control belongs at the site the pattern does not reach, which is what moved the `answerTo` control from the first delivery site to the third.

**Live dispatches.** Round 5's adversarial and blind pair, dispatched at fable over round 4's fix delta, base ref `bffab4c`. Round 4 returned a Critical that survived adjudication, so round 5 runs round 1's roster rather than decaying to one lens, at one tier above the writer tier, which is this session's main thread. The adversarial brief asks first whether the delta closes the class or only the instances round 4 named, and tells it to enumerate the rules structurally rather than trust a comment.

**Next action.** Adjudicate round 5, which is the backstop's opening bound. Then the Minor close pass over the fifteen accumulated at `.kit/scratch/lean-injection/minors-section-1.md`, the section close gate, Section 1's Chapter, and Sections 2 to 5.

### Interim board 10 - 2026-09-20

Not a Chapter. Section 1's fifth review round is adjudicated and the review-round backstop has fired. No `Completed:` line is written. The section stands exactly as round 4's fixes left it, committed at `67680ff`.

**Stage.** Section 1 is the only section in flight. Sections 2 to 5 are not started. The base bar boards 1 to 5 recorded stays discharged.

**Backstop ladder stage.** Five review rounds have now been adjudicated on this section. Round 5's adjudication left the terminal condition unmet, so the backstop fires at that adjudication rather than after a sixth fix round. This is the opening bound, and no restart has been taken. A continue past it buys the section three further rounds, with a declaration again from the third of those onward. A session resuming here reads the count as five adjudicated, the backstop declared once, awaiting the operator's answer.

**Why the exemption did not apply.** The backstop's exemption lets a round take its fix and close where that fix would owe no further round. It is unavailable here. Every one of round 5's four owed Majors is a fix to the guard's own extraction coverage, and the fix-delta bar's below-bar judgment owes a round wherever the delta's subject is something the area's tests are liable to route around. That is not a prediction: it is what five consecutive rounds have demonstrated, round 5 included, whose lens found that round 4's new controls drive only the shapes the fixed rules already read.

**What round 5 returned.** The adversarial lens returned four Majors and three Minors, verdict CHANGES_REQUIRED. The blind lens returned one Major and three Minors, verdict APPROVED_WITH_CONCERNS. Their Majors overlap on one subject, so the round carries four distinct Majors. No Critical. Every Major was confirmed against the cited code by this session before it was recorded. The findings are at `.kit/scratch/lean-injection/section-1/round5-findings.md`.

The four: the still-waiting rule reads the `quoteContinuationLines(` call rather than the frame around it, so literal prose put in front of that call is never sized; the kaizen and memory rules anchor on the operator that starts the data half and pin nothing at the statement's end, so a third operand appended after the `.join` is never captured; `primingRe` does not match a `PRIMING_BODY+=` line, so the count stays three and the appended text still reaches the child through `bin/supervise.sh:2844`; and the `answerTo` guard is a quote-character test, which is the exact test the same round-4 comment declares defeated six lines above it.

Two of the four sit on sites this plan's own sections 2 and 3 rewrite. The third sits on the shell variable the Approach's Order paragraph says the next plan in the queue will add a sentence to, which is the reason that ordering exists. So the gap is live rather than theoretical.

**A second overclaim, recorded because the first was.** Board 9 recorded that round 3's commit message wrongly claimed the guard had moved to one shared channel. Round 5's blind lens found the same shape in round 4's own words. The comment at `.kit/injection-ledger.mjs:92-95`, and the commit message of `67680ff`, say the depth-aware interpolation scan is "shared rather than written twice". It is written twice, at lines 99 to 116 and 254 to 270, and the two copies already disagree on an unterminated interpolation: one throws and the other drops the remainder silently. Two rounds running, a fix's own description claimed a scope the fix did not reach. That pattern is the phase analysis's central reading and not an incidental Minor.

**Owed and unrun, frozen by the backstop.** The fix round over round 5's four Majors, and the review round that fix delta would owe under the fix-delta bar. Both are taken on the restart, before anything else, if the operator continues. Nothing else is owed: no Critical was returned, and no finding here is a security finding on the claim-class block's list, because the subject of each is the offline tool's own regex rather than a boundary the running code crosses. That classification is this session's and is named in the declaration so the operator can overturn it.

**Provenance, read at adjudication before any fix existed.** Three of the four Majors trace to section 1's own text, which names "each prompt frame's literal text at its call site", so they are spec-traceable. The fourth, the `answerTo` guard, the adversarial lens returned as `trace: none`; this session re-traced it as that rule requires and it does trace to the same clause, and it sits in lines round 4's fix wrote, so it is fix-introduced. None is new-requirement. So no finding is held, no judge was dispatched, and no ruling was taken. Every Major here is work the plan already asked for.

**Minors.** Round 5 adds six to the fifteen already at `.kit/scratch/lean-injection/minors-section-1.md`, for twenty-one. The close pass over them runs after the terminal condition is met and before the close gate, so it is frozen too.

**Gate.** Unchanged from board 9 and now committed. Board 9's runs were taken on this worktree at 2026-09-20T11:20Z with round 4's fixes unstaged at HEAD `bffab4c`; those same bytes are the commit `67680ff` this board sits at, with a clean tree, so the measured content is identical and no re-run was called for. Type check exit 0. Ledger exit 0 with zero bytes on stderr. Duplicate test exit 1, the intended red, at 19 duplicate sentences with zero instrument failures, unchanged from boards 7, 8 and 9. Controller tick suite exit 0. Self-review suite exit 0. Live ledger 39 entries against the committed baseline's 39 with no character count moved.

**Rulings adopted since the last boundary.** None. The backstop declines to open the round that would have produced any.

**Live dispatches.** The pre-BLOCKED consult, dispatched at fable, asked to rule on one question: whether section 1's bar is a guard that cannot be outflanked by a future edit, or a signal that declares growth and records its own coverage gaps. It was given the plan's Goal, Intent and Approach by path, the round 5 findings file, and this session's lean labeled as an instinct to test. No design stop fired in this section, so this consult is owed rather than substituted for.

**Sent, not dispatched.** The expert ask is with ARCHITECT, asking only whether an existing source answers how to read a source file structurally rather than by hand-written pattern. The coordinator notice is with COORDINATOR. Neither gates the declaration.

**Next action.** The declaration goes out when the consult returns, with the ruling in its brief. Its first line is fixed. If the operator continues, the owed fix round and its review round run first, then the Minor close pass over twenty-one, the section close gate, Section 1's Chapter, and Sections 2 to 5.

### Chapter 1 - 2026-09-20
Completed: 1. The ledger and the baseline
Implemented By: implementer-sonnet, escalated to implementer-fable at round 2's Critical and taken into the main session at round 3's
Metrics: review rounds 5, closed major-closed; provenance 26 Critical and Major findings survived adjudication (4 Critical, 22 Major), 0 new-requirement at any round, so no finding was ever held and no judge ran; the spec-traceable and fix-introduced split was read and recorded only for round 5, at 3 and 1, rounds 1 to 4 having recorded no split at their own adjudications and the split not being reconstructible afterwards without reading provenance off fixes that now exist; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 2; consults 1
Decisions / Surprises: the section carries no add-decision line for its own open, that line having been written at no point before round 3; the round 3 and round 4 lines below are carried verbatim from `.kit/scratch/lean-injection/add-decisions-section-1.md`.

- ADD-DECISION (round 3 Critical + Major 1). Changes: every literal-extraction rule in `.kit/injection-ledger.mjs` asserts it consumed the whole source construct, rather than recording whichever pieces its pattern happened to match. Serves: the Goal's "A test fails the build when an injected string repeats a sentence that `CLAUDE.md` or another injected string already carries, and a ledger records each injected string's size so growth is declared rather than discovered", and Section 1's acceptance bullet "the ledger's names cover every string the Approach's inventory lists". Adds a mechanism: no. It generalizes the completeness assertion the shell rules already carry as `INSTRUCTION_ASSIGNMENT_COUNTS` to the rules that lack one, which is the same guard on more of the same channel. Size: about 60 lines in one file. Cost of not building it: Sections 3 and 4 rewrite exactly the constructs these rules read, so the guard this plan exists to provide would read green while thousands of characters left the injected text.
- NOTE ON THE ABOVE, written at round 4's adjudication. The line claimed "every literal-extraction rule" and the delta reached four of sixteen. Nothing in the round checked the claim against the rule set, so the gap shipped. Round 4's Critical is three of the twelve rules the delta did not reach, and the sweep this session ran at round 4's adjudication found a fourth the reviewers did not name. The lesson recorded rather than the incident: a fix whose add-decision names a class is owed an enumeration of that class in the same round, because a scope claim nothing counts is a scope claim nothing holds.
- ADD-DECISION (round 4 Critical). Changes: the four remaining unanchored literal-capture rules in `.kit/injection-ledger.mjs` (`extractStillWaitingReraise` :430, `extractKaizenFrame` :569, `extractBackstopFrame` :578, `extractMemoryBlock` :642) each gain a trailing anchor proving the source expression ended where the capture ended, so a frame rewritten as a `+`-joined chain refuses instead of recording its first piece. Serves: the Goal's "a ledger records each injected string's size so growth is declared rather than discovered". Adds a mechanism: no. It applies to four rules the anchor the fleet, paused, ENV and LESSON rules already carry. Size: four regexes, about 20 lines. Cost of not building it: Section 3 rewrites the still-waiting, kaizen and backstop frames, and the probe in round 4's report shows those three silently dropping 15, 63 and 56 characters on a two-piece split with no throw.
- ADD-DECISION (round 4 Major, delivery sites). Changes: the two delivery-site assertions stop testing for a quote character and instead require each of the `ground`, `id` and `text` arguments to be a bare identifier or member path, and the options object is no longer sliced off unexamined; its `answerTo` value is tested too. Serves: the Goal's "every prompt the plugin or the supervisor writes into a session ... carries only what that session cannot already hold". Adds a mechanism: no. It tightens the predicate of an assertion the section already has. Size: about 15 lines. Cost of not building it: `hooks/operator.ts:759` splices `answerTo` verbatim into the delivered text, so the stated ground for excluding the options object is false, and a hoisted constant spliced into any of the three positional arguments reaches the child unsized.
- ADD-DECISION (round 4 Major, interpolation stripping). Changes: `stripInterpolations` is replaced by the same depth-aware scan the round-3 tokenizer uses, so the two readers of one subject stop disagreeing. Serves: the same Goal clause, and Section 1's "with interpolated content excluded". Adds a mechanism: no. It deletes the second of two readers and points the survivor at the shared one. Size: about 10 lines net, a deletion. Cost of not building it: a nested interpolation leaves residue in the entry as prose, which the duplicate test's sentence matcher then indexes as injected text that no prompt carries.
- ADD-DECISION (round 4 Majors, the exit-code split). Changes: one hoisted `INSTRUMENT_LABEL_RE` replaces the two copies of the predicate, the split's own failure label is brought under it, and the real run's `buildLedger()` call is wrapped so a ledger throw reports as a broken instrument rather than as the designed red. Serves: the Goal's "A test fails the build". Adds a mechanism: no. It de-duplicates a predicate and catches a throw the suite already produces. Size: about 15 lines. Cost of not building it: the split reports exit 1 for both a real duplicate and a broken guard, which is the state the split was added in round 3 to end, so the instrument claims a property it does not have.

Beyond those lines, four things surprised this section and are recorded because each changed what was built.

The first is that the round-4 interpolation fix did not do what its own words said. Its comment and the commit message of `67680ff` both say the depth-aware scan is "shared rather than written twice". Round 5's blind lens found that false: the scan is written twice, at `.kit/injection-ledger.mjs` lines 99 to 116 and 254 to 270, and the two copies disagree on an unterminated interpolation, where one throws and the other drops the remainder silently. The comment now states the two copies and the one case they differ on. This is the second consecutive round whose fix description claimed a scope the fix did not reach, the first being round 3's claim that the guard had moved to one shared channel, recorded on board 9. Two in two rounds is a pattern rather than a slip, and it is the phase analysis's central reading.

The second is that the review-round backstop fired on this section and was then discharged. Board 10, committed and pushed at `a93c759`, declares the backstop fired at round 5's adjudication and the section stopped with a fix round and a review round owed and unrun. That declaration was wrong, and the pre-BLOCKED consult ruled it wrong on facts this session then verified independently at `.kit/injection-ledger.mjs:897-903`. Board 10's reasoning was that every round-5 fix touches the guard's own extraction coverage, which the fix-delta bar's below-bar judgment owes a round for. What that reasoning missed is that the bar has no floor in this tool: the positional check is a regex, and a regex cannot tell a variable carrying data from one carrying prose hoisted out of a literal without resolving the name. So a sixth round was already written into round 4's own fix, and it would have found the class a seventh would find again. The backstop's own exemption applies instead, because the round's remaining owed work is one regex fix plus prose corrections. That fix was driven red-first by a control withheld from the rule's literals and matched on position, which is the evidence a review round would otherwise supply, so the below-bar judgment does not fire and no round is owed. The ruling is recorded as a bar ruling grounded in the plan's own lines rather than as a dismissal of round 5's findings, all four of which were confirmed against the cited code before anything was done with them.

The third is that board 10's Minor count was wrong. It records fifteen accumulated Minors before round 5 and twenty-one after. The mechanical count of `.kit/scratch/lean-injection/minors-section-1.md` is thirty, and round 5's six bring the close pass's subject to thirty-six. The undercount came from a count taken by eye rather than read off the file.

The fourth is that the ledger's coverage has a bound no pattern closes, and the section declares it rather than fixing it. The extraction rules resolve no identifiers, so a name carrying data and a name carrying prose hoisted out of a literal are the same shape to them. Closing that needs a parser rather than a further pattern. The repository already carries `typescript ^7.0.2` at `package.json:3`, so a parser-based extractor is buildable here, and whether to fund one is the operator's call and is the one fork this section sends up.
Assumptions: assumed 2026-09-20 (default, section 1): a finding whose subject is the offline ledger's own regex is not a security finding under the claim-class block, because the subject is a tool that runs offline rather than a boundary the running code crosses; reversal: the operator's word, which would re-open round 5's four Majors at the fix-before-close bar. assumed 2026-09-20 (default, section 1): the `{basis, entries}` envelope the ledger prints stands, against the section text's "a JSON array of `{ name, file, chars, words }`"; reversal: one line in the ledger and one in the test's reader, the envelope carrying the base commit the entries were measured at, which the size check needs and a bare array cannot hold.
Review Findings: review: adversarial and blind pair at fable, Agent tool, rounds 3 to 5; rounds 1 and 2 at opus through Workflow, round 1 carrying the security lens as a third. Round 1: 1 Critical, 8 Majors. Round 2: 1 Critical, 3 Majors. Round 3: 1 Critical, 3 Majors. Round 4: 1 Critical, 4 Majors. Round 5: 0 Critical, 4 Majors. Every Critical was fixed before its round closed and each is recorded on the board for that round. The security lens ran once, at round 1, and reported that neither new file references `child_process`, `spawn`, `exec`, `eval` or `new Function`, so its trigger did not hold and it joined no later round. Round 5's four Majors: the priming rule was fixed, `primingRe` now reading an optional `+` before its `=` and refusing an appended clause with `[priming-shape]`, driven red-first by a control that places the appended line by position rather than by matching text the rule was handed. The still-waiting rule and the kaizen and memory rules are justified-not-fixed: each reads the call inside the frame rather than the frame around it, and Section 3 rewrites all three and already carries `.kit/injection-ledger.mjs` in its own files in scope, which is where the rules are re-anchored to whatever replaces them. The `answerTo` positional check is justified-not-fixed and declared instead: it asserts each argument is a bare name or member path, which is exactly the shape a hoisted constant wears, and the ledger's header and `README.md` now state that bound as a property of the tool rather than claiming it closed. Minors: 36 accumulated across five rounds, 7 fixed in the close pass, 1 upgraded on a stated consequence inside round 3, the exit-code split, which conflated a real duplicate with a broken control while the suite is red by design, and 28 left with the reason, which for 21 of them is that the fix would change what an extraction rule reads and so would owe a review round the close pass may not owe. The close pass's own delta is prose alone, three comments, one refusal message and one README line, which owes no round under the prose-deletions clause, and it took this session's author re-read against the changed behaviour rather than a round.
Stamps: adjudicated 28 over a 2d window covering the section's span, stamped 9, two in the project tier and seven in the operator tier. No hand walk was owed: the window's own account came out, every record on the list being one this session either applied here or read for a neighbouring plan.
Gate: the targeted lane, run at 2026-09-20T11:43Z on this worktree with the section's delta unstaged at HEAD `a93c759`, the box claimed and no foreign heavy process holding it, after a 105-second wait on another session's live claim. `npx tsc -p tsconfig.json` exit 0. `node .kit/injection-ledger.mjs` exit 0 with zero bytes on stderr. `node .kit/injection-duplicate-test.mjs` exit 1, which is this suite's designed red, at 19 duplicate sentences, with 25 guard controls all passing and no instrument failure. That last reading is the exit code's: the suite exits 2 when an instrument breaks and 1 when the red is the designed one, so 1 is what says the controls held, and a grep over the output counts control lines rather than failures. `node .kit/controller-tick-test.mjs` exit 0. `node .kit/self-review-unit-test.mjs` exit 0. Baseline on this same lane, recorded on board 7 and unchanged through boards 8, 9 and 10: exit 1 at 19 duplicate sentences with no instrument failure. Delta against it: none, 19 still 19. The live ledger reads 39 entries against the committed baseline's 39 with no character count moved. The contention lane did not run beside it, the section's delta touching no machine-shared state, every file in it being an offline script, a test and a README. The live suites this plan's Assumptions defer to `docs/plans/agent_persona_deferred-gate-run_v1.md` did not run and are recorded as deferred rather than passed.
Next: 2. The startup message
Commit Model: Branch-and-PR
Delta: read at 2026-09-20T11:40Z on this worktree, the section's delta unstaged at HEAD `a93c759`, with no other session's uncommitted edits under the measured roots.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 11 - 2026-09-20

Not a Chapter. Section 1 closed at Chapter 1 and is committed and pushed. Section 2 is at first green with its review round in flight, so no `Completed:` line is written for it.

**Stage.** Section 1 is closed, delivered at `24ce2f6`. Section 2 is implemented, verified, committed at first green as `f99cc4f` and pushed, with round 1 of its review dispatched and unreturned. Sections 3 to 5 are not started. The backstop ladder is not engaged on Section 2: no round has adjudicated yet, and Section 1's count does not carry to a new section.

**What Section 2 did.** The supervisor's five startup instruction strings were rewritten under the Approach's rule. Measured by the ledger: the skill-load string 518 to 278, the steer string 1,710 to 1,359, the channel reply string 1,564 to 274, the coordinator role string 8,782 to 4,719, the architect role string 5,552 to 4,714, and the no-channel priming body 260 to 253. Assembled, a worker with a channel reads 1,936 characters and the coordinator 6,355, against the Approach inventory's approximately 4,000 and 11,200.

Two changes are structural rather than deletions. The channel reply string was byte-identical to the plugin's `REPLY_INSTRUCTION` and carried fourteen sentences verbatim from `CLAUDE.md`; it is now three sentences and a pointer. The architect string carried two paragraphs instructing that seat to disregard the skill-load and steer sentences the same message had just delivered; those two strings are now conditioned out of the architect's assembled message instead, and the paragraphs are gone.

**A number the Approach cannot be reconciled against, recorded so it does not read as drift.** The Approach's inventory, measured 2026-09-18, records the coordinator role instruction at 3,711 characters and the steer instruction at 1,389. The ledger read 8,782 and 1,710 at this section's base. The inventory was taken on the steward-architect branch before that plan merged, and this plan's base is the trunk after it merged. The strings are the same strings; the numbers are not the inventory's. No clause is voided, because the section's acceptance is its sentence accounting rather than a size.

**Verification, this session's own runs rather than the implementer's report.** Every exit code read from its own unpiped invocation at 2026-09-20T12:2xZ, on this worktree at `f99cc4f` with a clean tree, the box claimed and released, no foreign heavy process holding it. Type check exit 0. Ledger exit 0 with zero bytes on stderr. Duplicate test exit 1, the designed red, at 17 duplicate sentences with 25 guard controls passing and no instrument failure. Controller tick exit 0. Self-review exit 0. Channel-reply pins exit 0 at 116 assertions.

**The section's headline claim, tested independently and with a control.** The claim is that no remaining duplicate names a supervisor string. The predicate: every line of the duplicate test's output carrying the text `duplicate sentence`, filtered against the ten entry names the ledger itself sources from `bin/supervise.sh`, read off the ledger's own output rather than from a hand-written list. The scope: the whole duplicate-test output. The result: 17 duplicate lines, none matching any of the ten names. The control is withheld rather than drawn from the pattern's own literals: the same predicate over the same scope, run against this session's pre-edit output from Section 1's close gate, names sixteen, being fifteen for the channel reply string and one for the priming-body pair. So the silence is coverage evidence rather than a predicate that stopped matching. Separately, every supervisor entry's live size equals its recorded ledger number and none was raised.

**The live child launch is deferred, not passed.** Section 2's acceptance names one real `claude -p` child launched with `--no-channel`. This plan's Assumptions defer every run of that class to `docs/plans/agent_persona_deferred-gate-run_v1.md`. It was not run and is recorded as deferred.

**Live dispatches.** Three, all over `f99cc4f` against base `24ce2f6`, all at fable on the Agent tool at each agent's frontmatter effort, which is one tier above the section's opus writer tier. The adversarial lens, asked to test the sentence accounting as the section's claimed acceptance rather than to read the exit code, and pointed at four areas: whether a named owner actually carries the sentence removed to it, whether conditioning two strings out leaves the architect's behaviour unstated, whether the clone rule's protections survive its reduction from three statements to one, and whether the ledger was loosened in the section it measures. The blind lens, given the base ref and the five changed code files and nothing describing the change. The security lens, on the process-execution and trusted-channel-write trigger, asked in particular about the clone rule's reduction, the conditional's reachability, the `node -e` priming write's quoting surface, and a removed quoting rule now reached by a pointer.

**Rulings adopted since the last boundary.** None. No judge was dispatched in either section and no finding has been held.

**Two surfaces routed rather than left in a report.** The implementer named two `README.md` passages that this section's own change made false, one describing health classes the instruction no longer lists and one describing overrides that no longer exist. They took step 5's carve-out, being a document a completed section's scope named and a behaviour this section changed, and were corrected in this section's own delta so they ride its review. `.kit/injection-ledger.mjs` was folded into Section 2's `Files in scope:` at the section's approach confirmation, recorded as the approval drift it is: the ledger asserts a fixed assignment count per instruction name, so the architect resets would otherwise have left the implementer no file in which to update the count it refused on.

**Next action.** Adjudicate round 1 when the three lenses return, capturing the section's delta for the provenance read before any fix exists. Then the fix round if one is owed, the Minor close pass, Section 2's close gate and its Chapter, and then Sections 3 to 5.

### Interim board 12 - 2026-09-20

Not a Chapter. Section 2's review round 1 is adjudicated and its fix round is in flight, so no `Completed:` line is written.

**Stage.** Section 1 is closed at Chapter 1, delivered at `24ce2f6`. Section 2 is implemented and committed at first green as `f99cc4f`, its review round 1 returned from all three lenses and adjudicated, and one fix round is dispatched and unreturned. Sections 3 to 5 are not started. The backstop ladder stands at one adjudicated round on this section; Section 1's count does not carry.

**Round 1 returned three lenses and the tree was clean at both ends.** `git status --porcelain` read the single untracked `.claude/worktrees/` line before dispatch and the same line at return, so no agent wrote into the tree and no lens's findings are suspect. The section's delta was captured before adjudication and before any fix existed, at `.kit/scratch/lean-injection/section-2/fix-round-1.diff`, 912 lines over the six files in scope, exit 0. The full findings roster is at `.kit/scratch/lean-injection/section-2/round-1-findings.md`.

**Five Majors raised, four distinct after the two sighted lenses were found to have raised one of them twice, three confirmed and one discarded.** No Critical from any lens.

**The security Major holds and every leg was re-derived here rather than taken from the report.** The rewritten channel-reply string points at the `CLAUDE.md` section "Writing to the operator". Of the five persona launch directories named in `D:/personas/fleet.json`, only this repository carries a `CLAUDE.md` at all. Neither the operator's global `CLAUDE.md` nor `claude-kit-doctrine.md` carries the rule about round, steer and session numbers, each reading zero. A record id embeds the sender's session id, confirmed at `hooks/index.ts:2193`. The comment at `bin/supervise.sh:2614` asserting the child reads `CLAUDE.md` at every launch is false on this fleet. This is a security finding of Major weight, so the carve-out applies and it is fixed before the section closes rather than parked.

**The scope of that gap was measured rather than estimated, and the measurement was challenged and re-run.** Comparing the project file's "Writing to the operator" section against the operator's global `CLAUDE.md` and `claude-kit-doctrine.md` together, nine sentences of six or more words appear in neither, and "Decide before writing" is a tenth below that length. The re-run checked four known-carried sentences and four orphans individually: the doctrine carries all four of the first at one occurrence each and none of the second. The global `CLAUDE.md` carries none of either, because it is an import wrapper over the doctrine rather than a second copy, which is why a count taken there alone reads as an instrument failure and is not one.

**The README Major was raised independently by the sighted adversarial lens and the blind lens, which is why it is trusted.** `README.md:58` says the `fleet_status` tool description defines the fleet health classes. Confirmed false: that description names a row's action values, while the five classes are declared in code at `hooks/agent-state.ts:168-174` and are already documented by the same README at line 487. The document contradicts itself. The plan's Approach asserted the same premise, and it was wrong at the base too.

**The third Major is that the per-sentence accounting this section names as its own acceptance exists nowhere on disk.** Confirmed: no Chapter 2 exists. It is discharged by writing that accounting into Chapter 2 rather than by a code fix, and the reworded no-channel priming body must be accounted for in it.

**One Major was discarded with its reason.** The adversarial lens held that the coordinator lost the clause that an urgent record carries no delegated authority, on the premise that only the worker steer string carries it and the coordinator never receives that string. `bin/supervise.sh:2640` assigns that string unconditionally and gates only a trailing worker-only clause at `:2652`, so the coordinator does receive it and it states exactly that. The lens marked its own confidence medium.

**One Major routes to Section 3 rather than to this one.** The blind lens found the comment at `hooks/index.ts:466` still claiming the plugin's reply constant is kept byte-identical by hand with the supervisor's. The pin that enforced it read two occurrences at this section's base and reads zero now, so the comment is aimed at nothing. That file is Section 3's, which deletes the constant the comment is attached to, taking the comment with it. Section 3's brief carries it.

**Minors accumulate to the close pass and are listed in the roster file.** Two pairs are one finding each across lenses: the clone rule's antecedent regression, and the stale plugin comment above.

**The fix round's two fixes, with the alternative that was refused.** The channel string regains the orphaned rules inline, restated in its own words so the duplicate guard does not read them as copies, and keeps its pointer for the rules the doctrine carries. `README.md:58` is reworded to name the controller tick's own reduction as the classes' owner. Neither adds a mechanism, so neither fires a design stop. The alternative considered and refused here was provisioning the `CLAUDE.md` section into each persona's launch directory or into the operator's global file. Both add a mechanism no clause names and both write to machine state outside this repository, so both are the operator's call. It is recorded as an open question rather than taken.

**The fix delta is expected to owe a round.** It changes a string that reaches a spawned process, which is one of the security-reviewer trigger surfaces, so the fix-delta bar is met on its third trigger. That round is one lens at the writer's tier, the security read falling to the finishing pass.

**Live dispatches.** One: the section 2 fix round at the opus writer tier, given both fixes, the orphan enumeration, the acceptance gates with their starting counts, and an instruction to drive the new session-id pin red before green.

**Rulings adopted since the last boundary.** None. No judge was dispatched and no finding is held.

**Next action.** Adjudicate the fix round when it returns, take the round its delta owes, then the Minor close pass, Section 2's close gate and its Chapter carrying the sentence accounting, and then Sections 3 to 5.

### Interim board 13 - 2026-09-20

Not a Chapter. Section 2's review round 2 is adjudicated, one fix round and one consult are in flight, and one Major is held for the operator, so no `Completed:` line is written.

**Stage.** Section 1 is closed at Chapter 1, delivered at `24ce2f6`. Section 2 is implemented, committed at first green as `f99cc4f`, and has taken two adjudicated review rounds. Sections 3 to 5 are not started. The review-round backstop ladder stands at two adjudicated rounds on this section against an opening bound of five, so it has not fired.

**Round 2 was one adversarial lens at the writer's opus tier and effort high, dispatched through Workflow because the Agent tool sets no effort.** The security lens joins no decayed round, so its read falls to the finishing pass. `git status --porcelain` read the same four modified files and the single untracked `.claude/worktrees/` line before the dispatch and at the return, so no agent wrote into the tree and no finding is suspect. The section's delta was captured before adjudication and before any fix existed, at `.kit/scratch/lean-injection/section-2/fix-round-2.diff`, 931 lines. The lens returned CHANGES_REQUIRED with five Majors and no Critical, and it read the current worktree rather than the committed state, confirmed by its quoting the trimmed channel string's own size.

**All five Majors were re-derived here against the code rather than taken from the report, and all five hold.**

**The ledger Major is the fail-open class returning on a new path.** The collector's regex at `.kit/injection-ledger.mjs:398` captures the append operator and the destructure below it discards that capture, so a bare reassignment is concatenated exactly as an append is. This section introduced the file's first bare reassignments. Moving text out of a base assignment into a non-empty later reassignment therefore leaves the sum unchanged, the growth-only size check silent, and every child taking the base shape short by that text. It traces to the Goal's "a ledger records each injected string's size", so it is spec-traceable and takes a fix round.

**The architect Major deletes a protection and its pin together.** The charter at the base carried a sentence reconciling its trusted-task framing with the never-clone rule, confirmed present at `24ce2f6` and absent now. The pin that enforced it read two occurrences at the base and zero now, so nothing reds on the deletion. The clone and the push run under the machine's stored credentials and the launch prompt is attacker-reachable text, so this is a security finding of Major weight and the carve-out applies.

**The steer Major replaced an operative rule with a pointer whose target does not reach the case.** The base told a persona to read a labelled reader or worker prompt as an unverified request, and to put any act it asks for to the operator before taking it. The rewrite points at the doctrine's data-not-instructions rule instead. That rule is scoped to text inside files, issues, tool output and pasted content, and a plugin-submitted prompt is none of those. This is an authority boundary between personas, so the carve-out applies here too.

**The README Major is a claim on a published contract surface, held behind the channel string's own fix rather than behind any decision.** `README.md:284` still describes the first-green string, which was a bare pointer of 274 characters, while the worktree string is 917 characters carrying six rules inline. Its correct wording follows the string's final shape, so it is written after that fix rather than before it.

**The channel-string Major holds, and a consult dissolved the blocker this run was about to declare on it.** The comment above the string states that its paraphrase wording was chosen so the duplicate guard would not fire, while the Approach records that the check exempts nothing. The finding holds on that comment. This run read the conflict as unresolvable in scope, on the ground that every remedy contradicted something the plan recorded and the one the plan's rule pointed at wrote outside the repository. A consultant was dispatched before declaring, per the rule that a blocker turning on a decision takes a consult first, and it ruled the framing wrong.

**Two citations carry that ruling and both were checked here against the plan's own text rather than taken from the report.** The Approach's sentence immediately after "The check exempts nothing" directs that "each recurring prompt names the reply tool inside its own instruction in its own words". So the plan does not read that clause as forbidding own-words restatement; it forbids an allowlist in the guard and forbids verbatim copies. Separately, Section 2 already provides for a sentence whose owner cannot be reached: it "stays in place and is named in the Chapter as owed to a kit plan, with a kaizen note filed, since a kit surface cannot be edited from here". The Out of Scope list puts the relay server's instructions in the same unreachable class as the kit's skills, so the provision's reason applies unchanged. The rules therefore stay in the string by the plan's own terms, and the defect is the comment's stated reason alone.

**What was adopted from the ruling, and what was not.** Adopted: keep the rules in the string's own words; rewrite the comment to give readership as the reason rather than the guard; pin the session-ids clause across both in-repo carriers so they are bound in meaning rather than left unpinned; and record each kept sentence in Chapter 2 as owed to an owner outside this repository, with the plan's false premise recorded as a surprise rather than by rewriting the Approach. Adopted only in part: the ruling would drop the `CLAUDE.md` pointer from the string's tail entirely. Section 2's literal text requires "the reply-tool sentence and a pointer to `CLAUDE.md`", and the `REPLY_OWNER_CONTROL` pin reads that pointer, so the tail is shortened with the pointer kept rather than removed. The ruling itself rates this element medium-high and names the stricter reading. Discarded: the ruling directs a kaizen note under Section 2's provision. That provision is written for an owner that is the kit, and this owner is the relay server. The doctrine restricts that inbox to concrete kit friction, so this is recorded in the plan's Open Questions and in memory instead.

**An operator fork survives the ruling and does not block.** The rules for writing an operator-facing message live in two places in this repository because no single file here reaches both the supervisor-launched personas and the sessions started at the keyboard. The one surface every channel-attached session reads is the relay server's own instruction text, in another repository. Whether to carry the rules there, after which both copies here become pointers, is recorded as an Open Question with the recommendation that it follow this plan rather than interrupt it.

**The orchestrator's own trim, taken before round 2 so the lens reviewed final state.** Two of the eight rules the round 1 fix restored are carried by `claude-kit-doctrine.md`, which every session on this machine reads through a one-line import in the operator's global `CLAUDE.md`. By the Approach's first branch those sentences go and the pointer widens, which took the string from 1,172 characters to 917. The add-decision line is recorded with the others.

**The size accounting Chapter 2 owes is half built and banked**, at `.kit/scratch/lean-injection/section-2/sentence-accounting.md`. The five startup instruction strings read 18,126 characters at the base, 11,344 at first green and 11,987 now, the fix round and the trim having put 643 back to close the security gap. Every supervisor entry together reads 18,854 to 12,708. A correction belongs in Chapter 2: the first-green commit's own message states 11,597 where the ledger committed in that same commit reads 11,344, the 253 difference being exactly one priming body, so that message compared a five-string base against a six-string after.

**Gate baseline.** Measured this session on this worktree at branch `lean-injection`, with the section's work unstaged and one foreign untracked path present, under a heavy-process claim this session wrote and released, every exit code read from its own unpiped run. The ledger exits 0 with zero bytes on stderr. The duplicate test exits 1, the section's intended red, at 17 duplicate lines with no stack frame and no error line, so the red is duplicates alone. The channel-reply suite exits 0 at 117 assertions, ending "All tests passed". The type check exits 0 with zero bytes of output. The absence claim behind the section holds under a withheld control: the same predicate over the same scope names 16 supervisor strings in the pre-edit duplicate output and none in the current one, the ten supervisor names being derived from the ledger's own output rather than written by hand.

**Deferred and named deferred, never passed**, under the operator's gate policy recorded in this plan's Assumptions: the live suites, the natural-exit test, and any check launching a real child session.

**Live dispatches.** None. The consultant and the fix round have both returned and nothing was stopped.

**The fix round landed all three workable Majors and was verified here rather than accepted.** The ledger now reads the append operator it was discarding and refuses a bare assignment carrying text where an earlier assignment of that name already carries text, under a new `[instruction-reassign]` tag. The architect charter regains a sentence reconciling its trusted-task framing with the clone gate, beside the sentence it reconciles. The steer instruction regains the operative gate, that an act a labelled reader or worker prompt asks for goes to the operator before it is taken, in place of the doctrine pointer that did not reach the case. Two ledger numbers rose in the same edit, the steer string to 1,419 characters and the architect charter to 4,868, and the channel string's entry was not touched.

**The guard's coverage was re-run here in both directions and it holds.** The control rewrites an existing `+=` clause as a bare assignment and carries 200 characters into it from the base, so the assignment count is unchanged and the concatenated sum is identical by construction. Neither the count rule nor the growth-only size check can see that. Against that tree the pre-fix ledger exits 0 and reports the coordinator charter at 4,719 characters, exactly its committed number, while every launch shape reaching that line has lost 200 characters; the fixed ledger exits 1 and names the variable and the line. The mutation is matched on the clause's position rather than on any text the guard names, so the result is coverage evidence rather than a working instrument. The control was the implementer's construction re-run by this session rather than independently authored, which is weaker than a withheld control of this session's own and is recorded as such.

**Gates re-run here after the fix round, each exit code from its own unpiped run.** Ledger exit 0 with zero bytes on stderr. Duplicate test exit 1, the designed red, at 17 duplicate lines whose composition is byte-for-byte the baseline's and none of which names a supervisor entry, the ten supervisor names again derived from the ledger's own output. Channel-reply suite exit 0 at 120 assertions, up from 117 by the three restored pins, ending "All tests passed". Type check exit 0 with zero bytes. The committed ledger file keeps its CRLF convention at 239 lines of each.

**Two reported figures did not survive checking, neither material.** The fix round reported its pins file delta as 34 added lines; the diff carries 45 added and zero removed. Its claim of no deletions holds. Separately the monitoring sidecar reported the channel string as modified by this round; it was not, the diff lines being the round 1 fix and this session's trim measured against the section's base, and the live string still measures exactly the 917 characters the trim produced.

**Asks in flight.** One to the architect seat, asking only whether an existing source already settles how an injected string carries a standing rule whose owner sits outside the repository. It does not gate and was unanswered at the time of writing. No seat on the roster is named Expert.

**Rulings adopted since the last boundary.** One, from the consultant, recorded above with what was adopted whole, what was taken in part and what was discarded with its reason. No judge was dispatched and no finding is bucketed: every Major this round reads spec-traceable, so none is a new-requirement hold. No blocker is declared, because the ruling established that the plan already answers the question this run was about to put to the operator.

**One surface is folded into this section, which widens it.** The fix round reported, as a concern rather than as work done, that `.kit/injection-duplicate-test.mjs` states its own invariant in its header as one control per fail-closed ledger guard, and that the new guard leaves that invariant unmet on disk. Nothing reds on it, since the guard list there is author-maintained. That file is not on Section 2's `Files in scope` line, so it takes the out-of-scope route, and the fold predicate is met on all three legs: it sits in the same directory as files this section changed, it needs no acceptance criterion the section does not already carry, and the section's close gate runs that very suite. So it is folded rather than appended as a section, the `Files in scope` line is widened to name it, and this entry records the widening as the approval drift it is.

**Next action.** Apply the channel-string fix the ruling directs, which is the comment's true stated reason, the shortened tail with its `CLAUDE.md` pointer kept, and the cross-carrier pin on the session-ids clause. Then the folded control for the new ledger guard, the README restatement at line 284, the Minor close pass over both rounds' accumulated Minors, Section 2's close gate, and its Chapter carrying the sentence accounting, the kept sentences named as owed to an owner outside this repository, the false premise recorded as a surprise, and the first-green commit message's mis-summed figure corrected. Then Sections 3 to 5.

**Owed to memory at the close-out.** No record in any tier covers it, checked this session. The fleet's five launch directories hold exactly one `CLAUDE.md`, in this repository, and none of the other four has one in any ancestor either. So a pointer at `CLAUDE.md` written into a supervisor-launched instruction reaches one persona of five. The operator's global `CLAUDE.md` is a one-line import of the kit doctrine rather than a second copy of anything, which is why a naive count taken there reads zero and looks like a broken instrument. This premise has now cost this section two review rounds.

### Chapter 2 - 2026-09-20
Completed: 2. The startup message
Implemented By: implementer-opus at first green, two fix rounds at the opus writer tier, the Minor close pass and the final fixes taken in the main session
Metrics: review rounds 2, closed major-closed; provenance 10 Major findings raised across both rounds, 9 distinct after the two sighted lenses of round 1 were found to have raised one twice, 8 confirmed, 1 discarded with its reason and 1 routed to Section 3; 0 new-requirement at either round, so no finding was held and no judge ran; every confirmed Major reads spec-traceable; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 1
Decisions / Surprises: the add-decision lines are carried verbatim from `.kit/scratch/lean-injection/add-decisions-section-2.md` in interim boards 11 to 13. Three further changes landed at the close, and none adds a mechanism: the channel string's comment now gives readership as its reason rather than the duplicate guard, the string's tail is shortened with its `CLAUDE.md` pointer kept, and a cross-carrier pin binds the two in-repo carriers of the withheld-numbers rule.

**The section's own acceptance is its sentence accounting, and here it is.** Read with each ref's own copy of the ledger, because today's ledger refuses the base's assignment shape, which is the `[instruction-count]` guard working rather than a fault. The five startup strings carried 132 sentences of eight or more words at `24ce2f6`. 101 went, 31 stayed verbatim, and 45 sentences in the strings' own words are new or restated.

Of the 101 removed, 15 are carried verbatim by a named surface and every one of those 15 is the channel string's. All 15 are still carried by `hooks/index.ts`, which is the plugin's `REPLY_INSTRUCTION` and Section 3's target. Fourteen are carried by this repository's `CLAUDE.md`, and seven of those by `claude-kit-doctrine.md` and the kit's own skills as well. The seven with a kit surface are the plain-prose rules the string's closing pointer now covers. The seven that only `CLAUDE.md` carries are the orphans round 1's security Major found, and those are restated inline rather than pointed at, because four of the five persona launch directories hold no `CLAUDE.md`.

The other 86 removed sentences are carried verbatim by no surface read: not this repository's `CLAUDE.md`, not the operator's global one, not the kit doctrine, not the kit's skills, not the README. They are the coordinator role instruction's 57, the architect charter's 20, the steer instruction's 7 and the skill-load sentence's 2. Each was either restated in its string's own words or dropped as redundant with a sentence that survived.

For every sentence kept, why no surface carries it: all 31 were searched against those same five surfaces and none is carried by any of them. The 45 new or restated sentences were searched the same way, with the same result. That second reading is the independent corroboration of the duplicate gate below, arrived at from the sentence sets rather than from the gate's own output.

The instrument was controlled in both directions before any of this was trusted. A sentence known to live in this repository's `CLAUDE.md` is found there, and a sentence written to live nowhere is found nowhere.

**The size table, read from the ledger at three refs.** Base is `24ce2f6`, first green is `f99cc4f`, as built is this chapter's commit.

| String | base | first green | as built |
|---|---|---|---|
| `SKILL_LOAD_INSTRUCTION` | 518 | 278 | 278 |
| `COORDINATOR_STEER_INSTRUCTION` | 1710 | 1359 | 1419 |
| `CHANNEL_REPLY_INSTRUCTION` | 1564 | 274 | 776 |
| `COORDINATOR_ROLE_INSTRUCTION` | 8782 | 4719 | 4719 |
| `ARCHITECT_ROLE_INSTRUCTION` | 5552 | 4714 | 5132 |
| **the startup five** | **18126** | **11344** | **12324** |

Every supervisor entry together reads 18,854 at the base and 13,045 as built. The saving against the base is 5,802 characters on the five, smaller than first green's because the fix rounds put back what the security Majors required: the operator-writing rules no other surface reaches four of five personas with, the architect's clone reconciliation, and the steer instruction's act gate.

**A figure in this section's own history is corrected here rather than left standing.** The first-green commit `f99cc4f` states the startup message went from 18,126 characters to 11,597. The ledger committed in that same commit reads 11,344 for the five strings. The 253 difference is exactly `PRIMING_BODY_no_channel_wait`, so that message compared a five-string base against a six-string after. The section-open add-decision line carries the same 11,597 and is wrong in the same way.

**The surprise that cost this section two review rounds was a false premise in the plan's own Approach.** The section was written believing every launched child reads a `CLAUDE.md`, so a pointer at its "Writing to the operator" section would reach every persona. Of the five launch directories the fleet names, only this repository holds a `CLAUDE.md`, and none of the other four has one in any ancestor. The operator's global `CLAUDE.md` is a one-line import of the kit doctrine rather than a second copy, which is why a naive count taken there reads zero and looks like a broken instrument rather than a real absence. The Approach is not rewritten. The premise is recorded here as the surprise it was, and the sentences that stayed are named below as owed to an owner outside this repository.

**Sentences kept because their owner cannot be edited from here.** The operator-writing rules now stated inline in `CHANNEL_REPLY_INSTRUCTION` have a real owner, and it is not this repository. It is the relay server's own instruction text, which is the one surface every channel-attached session reads whatever its working directory. That server lives in another repository, so Section 2's provision for an unreachable owner applies: the sentences stay in place and are named here as owed. No kaizen note is filed, because that provision is written for an owner that is the kit and this owner is the relay server, and the kaizen inbox takes concrete kit friction only. The disposition is the Open Question added above instead.

**A consult changed this run's course and its ruling is recorded with what was discarded.** Round 2 raised the rewritten channel string as contradicting the Approach's "the check exempts nothing". This run was about to put that to the operator as a decision and dispatched a consultant first, per the rule that a blocker turning on a decision takes a consult. The ruling was that the framing was wrong, on two citations both re-read here against the plan's own text rather than taken from the report. The Approach's next sentence directs each recurring prompt to name the reply tool in its own words, so own-words restatement is provided for rather than forbidden. And Section 2 already provides for a sentence whose owner cannot be reached. Adopted: the rules stay in the string's own words, the comment is rewritten to give readership as its reason, the withheld-numbers rule is pinned across both carriers, and the kept sentences are named here as owed. Adopted in part: the ruling would drop the `CLAUDE.md` pointer from the tail entirely, but Section 2's text requires that pointer and `REPLY_OWNER_CONTROL` reads it, so the tail is shortened with the pointer kept. Discarded: the ruling's direction to file a kaizen note, for the reason given above.

**One surface was folded into this section, which widens it, and that is approval drift rather than a silent fix.** `.kit/injection-duplicate-test.mjs` states in its own header that it holds one control per fail-closed ledger guard. This section adds such a guard, `[instruction-reassign]`, so that invariant was unmet on disk and nothing red, the guard list there being author-maintained. The file was not on the section's `Files in scope` line. The out-of-scope fold predicate holds on all three legs: it sits in the same directory as files this section changed, it needs no acceptance criterion the section lacks, and the section's close gate runs that very suite. So the line was widened to name it and the control was written, mutating a `+=` clause into the bare assignment shape by position rather than by any text the guard names.

**The new ledger guard's coverage was driven red before it was trusted, in both directions and by this session.** The control rewrites an existing `+=` clause as a plain assignment and carries text into it from the base, so the assignment count is unchanged and the concatenated sum is identical by construction. Neither the count rule nor the growth-only size check can see that. Against that tree the pre-fix ledger exits 0 and reports the coordinator charter at exactly its committed size while every launch shape reaching that line has lost text; the fixed ledger exits 1 naming the variable and the line.

**The cross-carrier pin was driven red from each carrier in turn.** The rule against putting an internal number in front of the operator has two carriers here: this repository's `CLAUDE.md`, which one persona reads, and the channel instruction, which is where the other four meet it. Each pinned against its own literal is how one of them drops a number with nothing red. The numbers are now read out of `CLAUDE.md`'s own sentence and asserted on the instruction. Dropping one from the instruction reds and names which; rewording `CLAUDE.md`'s sentence away reds rather than passing on an empty read. Both probes ran on copies taken before the first mutation and both files were restored and byte-compared against those copies.

**The Minor close pass over both rounds.** Closed here: the architect charter regains a clause closing a record with `agentic_resolve` and stating that the urgent label delegates nothing, in its own words since the steer string it would otherwise copy is cleared for that launch; the charter's "any other shape" sentence moves back beside the shape rule it qualifies, four sentences away before; both class sweeps now run on the worker eval, which is the only launch carrying the steer string's escalation clause and was swept by neither sweep before; the README's claim that the sweep reads every sentence now says eight or more words; the test file's header and its control-pair comment no longer describe the channel string as a bare pointer; and `CLAUDE_MD_SENTENCE_FLOOR` rises from 10 to the class's real size of 14, declared as a number that moves with `CLAUDE.md` in the same commit rather than a guess sitting under the class.

Two Minors route rather than close, each to the section that owns the file. The stale comment in `hooks/index.ts` claiming the plugin's reply constant is kept byte-identical by hand goes to Section 3, which deletes the constant it is attached to. And `fleet_status`'s own description never uses "health class", which is this codebase's canonical term for what it reports, declared at `hooks/agent-state.ts:160` and used by the README and the tick test; the role instruction points at that tool for classes the description does not name as such. The fix belongs in the tool description, which is Section 4's scope, so Section 4's brief carries it rather than this section renaming the codebase's vocabulary.

**A two-suite dependency this section creates, recorded because no single suite covers it.** The ledger cannot tell an architect-scoped clear of an instruction variable from one hoisted out of its guard, and its size check is growth-only, so a hoisted clear passes the ledger. The scoping is covered only by `.kit/channel-reply-instruction-test.sh`. Both suites must run for this section's guarantees to hold.

Gates, each exit code read from its own unpiped run at the section's final state, under a heavy-process claim this session wrote and released after a poll that found no foreign test runner. The ledger exits 0 with zero bytes on stderr. The duplicate test exits 1, which is this plan's designed red, at 17 duplicate sentences with zero stack frames and no failure line but its own summary; the composition is the post-Section-2 set and no supervisor entry appears in it, the ten supervisor names being derived from the ledger's own output rather than written by hand, and the same predicate names three of them in the pre-edit capture, so the silence is controlled rather than assumed. The channel-reply suite exits 0 at 126 assertions, up from 117 at first green by the three pins the fix rounds restored, the three controls and the assertion of the new cross-carrier pin, and the two class sweeps added on the worker eval. The type check exits 0 with zero bytes of output. Deferred and named deferred, never passed, under the operator's gate policy in this plan's Assumptions: the live suites, the natural-exit test, and any check launching a real child session.

Next: 3. The recurring prompts and context blocks. Commit Model: Branch-and-PR.

### Interim board 14 - 2026-09-20

Not a Chapter. Section 3 is implemented and at first green, with no review round yet dispatched, so no `Completed:` line is written.

**Stage.** Sections 1 and 2 are closed at Chapters 1 and 2, delivered at `24ce2f6` and `7a822da`. Section 3 is implemented in one dispatch at its `Model: opus` tier and committed here at first green. Sections 4 and 5 are not started. The review-round backstop ladder stands at zero adjudicated rounds on this section.

**What the dispatch built.** `REPLY_INSTRUCTION` is deleted from `hooks/index.ts` together with the comment attached to it, which is the stale comment Section 2's rounds routed here. The implementer searched by content rather than by the reviewers' line numbers and found exactly one such comment; the second citation turned out to name a different comment that is still true. Each of the four frames the constant prefixed keeps its label and names the reply tool in its own words. The `goal_done` clause now sits once in the `goal_done` description, with the goal-tree block and both nudge arms pointing at it. `[RECONCILE]` drops its step list and points at the kit's coordinator skill, which the implementer opened and confirmed states those steps. The fleet notes' authored prose gains a ledger rule, an entry, and three controls, so prose that reached the child unsized since Section 1 is now sized and swept.

**Gates, re-run by this session rather than taken from the dispatch's report, each exit code from its own unpiped run, under a heavy-process claim written and released here.** Every figure the dispatch reported reproduced. The type check exits 0 with zero bytes. The ledger exits 0 with zero bytes on stderr. The loader rule exits 0. The duplicate test exits 1 carrying **one** duplicate against the section's baseline of 17, with zero stack frames, and that survivor is exactly the pair this plan assigns to Section 4: one sentence shared between the `agentic_say` and `agentic_inbox` descriptions. All 16 duplicates belonging to this section are gone. The tick suite exits 0 at 1,376 assertions against a baseline of 1,374, the two added being the backstop shape checks the section's `Tests:` line requires. The fleet-status suite holds at 132, self-review at 26, commons at 23, the keeper at 64 passed and the channel-reply suite at 126, every one exit 0 and unchanged. All six touched files are pure CRLF, confirmed by a raw-byte read rather than by a Git Bash instrument.

**Two readings the dispatch flagged for the reviewer rather than settling itself, and they are the round's first business.** One: four fleet notes lose a trailing sentence restating the `[FLEET]` header's own rule about a carried line, on the ground that the header owns that rule. The implementer kept the variant on the note that also reaches `fleet_status`, where no header exists, and traced three call paths to confirm the others do not. Two: the `[FLEET]` frame gained no `fleet_status` pointer, on the ground that the frame states no health class and no row field, so there is nothing to point away from, and the supervisor's own coordinator instruction already carries that pointer. Both are judgment calls against the section's literal text and both are named rather than buried.

**One add-decision line, which adds no mechanism.** The ledger's string-literal collector becomes a wrapper over a span-reporting collector, so the new prose rule can tell a literal from a hoisted name by the span the existing scan already computes rather than by a first-character test. Serves the section's acceptance that the fleet notes' prose becomes a ledger entry, and the Goal's recording of each injected string's size. The alternative was a third copy of the depth-aware interpolation scan. Cost of not building it: a concatenation of a literal and a hoisted name would record the first piece and drop the rest with nothing raised, which is the fail-open class Section 1 spent four rounds on.

**Red before green, reported in both directions and taken on copies.** The new prose rule was driven red first: against the pre-edit source the earlier ledger puts the sentence in no entry at all, and the new one names it. Its refusal arm was driven both ways on a sentence hoisted into a bare name. The two tick checks were each driven red by a separate tree-mutating probe with no agents in flight, and both files were restored from pre-probe byte copies and byte-compared. The absence pin for the retired budget text carries its own withheld control, the fleet label read out of the same file by the same bracket shape, so a silent read cannot pass as a clean one.

**An anomaly named rather than called a flake.** In one batched run the commons suite reported exit 127 while its output held all 23 assertions and its pass line. It did not reproduce in four re-runs by the dispatch nor in this session's own run. The reading is a transient spawn failure in that batch shell rather than a suite result, and it is recorded because it was not root-caused.

**Two bounds the dispatch made honest rather than closed.** Six short phrases still reach a fleet note through an interpolation and stay unsized; the ledger header and the README now say six after an enumeration from the source, where the first draft said four. And the 18 composed sites are one ledger entry, so the duplicate matcher would not report a sentence repeated among them; the one real instance is removed by this delta, and one entry per site would close the gap. The acceptance bullet permits one or more entries and one was chosen, mirroring the existing fleet line-literal entry.

**Found and left, out of scope.** `.kit/controller-tick-test.mjs` still sets two context-budget options in a fixture that the plugin has read since nothing since that feature's removal. Inert dead fixture options, named rather than touched.

**Live dispatches.** None. The section dispatch returned `DONE_WITH_CONCERNS` and nothing was stopped.

**Rulings adopted since the last boundary.** None from a judge. One operator answer landed on the channel and is recorded outside this plan: TypeSafe's retention of request bodies on non-enterprise accounts is a risk the operator accepts, decided 2026-09-20, written to the operator memory tier because its owning plan executes on another branch. It settles request bodies only and leaves untouched that plan's separate rule that no part of the key's value is ever journaled.

**Next action.** Dispatch Section 3's first review round, which is the full roster one tier above the writer, and put the two flagged readings to it first. Then adjudicate, take the fix round its delta owes, close Section 3 with a Chapter carrying the sentence accounting and the size table the dispatch banked, and go on to Sections 4 and 5.

### Interim board 15 - 2026-09-20

Not a Chapter. Section 3's first review round is adjudicated and its fixes are in and gated, but a second round is in flight, so no `Completed:` line is written.

**Stage.** Sections 1 and 2 are closed at Chapters 1 and 2, delivered at `24ce2f6` and `7a822da`. Section 3 is at first green from `db05dee`, with round 1 adjudicated and its two Major fixes landed here. Sections 4 and 5 are not started. The review-round backstop ladder stands at one adjudicated round on this section, with the second dispatched.

**The round that ran, and the roster it ran at.** Round 1 was the full roster one tier above the section's `Model: opus` writer, so every lens at fable. The blind lens returned no Critical and no Major, with two Minors. The adversarial lens returned `CHANGES_REQUIRED` with two Majors and five Minors, and answered the two readings board 14 flagged. The security lens was stopped mid-run on the operator's instruction and is recorded below.

**The blind lens read a fraction of the delta, and said so itself.** Its charter bars reading under `.kit/`, on the ground that the path is the orchestrator's scratch. This repository commits its offline suites and the ledger's data file under that same prefix, so four of the six changed files were closed to it and it read 55 of 300 changed lines. It reported that gap rather than returning a clean verdict over files it never opened. The adversarial charter carries no such bar and did read them, so the gap is one lens rather than the round. A kaizen note is filed against the charter's hardcoded path. The Chapter's review line will carry this as a coverage fact rather than as a pass.

**The two readings board 14 flagged, now ruled.** The first holds: the four fleet notes that lost a trailing sentence lose nothing, and the lens traced every call path to confirm it, including one path where the removed sentence was actually false and its removal is a correction. The second does not hold, and became a Major. The `[FLEET]` frame's want of a `fleet_status` pointer is required in three places, by Section 3's own text, by Section 4's, and by the Goal's "a pointer to the surface that owns the rest". All three citations were re-read here against the plan rather than taken from the report, and re-read by content rather than by the lens's line numbers, which had shifted under an append this session made.

**The two Majors, both confirmed against the code before any fix was written.** The first is the missing pointer above. The fix adds one clause naming the `fleet_status` description for a row's fields and classes and the kit's coordinator skill for what a coordinator does with a reading; both targets were read here and do carry what the clause says they carry. The second is that the ledger header and a README sentence declared the unsized-prose bound as "the six short phrases a fleet note splices in by name". An enumeration of every interpolation inside a `composed:` template, run here, found the bound short by more than the one further phrase the lens named: a value spliced from another note, three helper returns, and several literals written inside ternaries are all unsized too. So the fix states the bound as a class rather than as a count, in both carriers, and a new interpolation is now unsized the moment it is written rather than falsifying a number. The same edit corrects a second overstatement in the README sentence, which claimed the rule refuses a site whose sentence is not a single literal; it refuses a site whose value is not a string literal at all, a template with interpolations passing it.

**A self-inflicted break, caught by the guard rather than by review.** The pointer clause was first written with markdown backticks around the tool name. A raw backtick inside a TypeScript template literal closes the template, so that text was invalid TypeScript and also fell outside the ledger's frame anchor, which bounds the frame with a negated-backtick class. The ledger refused with its own named error before any suite ran. The clause now names the tool unadorned, which is right for prose a model reads.

**Gates, each exit code from its own unpiped run, under a heavy-process claim written and released here.** The type check exits 0 with zero bytes of output. The ledger exits 0. The loader rule exits 0. The duplicate test exits 1 at one duplicate, which is this plan's designed red, and that survivor is exactly the pair Section 4 is assigned: one sentence shared between the `agentic_say` and `agentic_inbox` descriptions. The added pointer sentence introduced no duplicate. The tick suite exits 0 at 1,376 assertions, the fleet-status suite at 132, self-review at 26, commons at 23, the keeper at 64 passed and 0 failed, and the channel-reply suite at 126. Every one of those figures equals the baseline recorded at `db05dee` on the same lane, so the fix round moved no count. All touched files are pure CRLF, confirmed by a raw-byte read at each write rather than by a Git Bash instrument.

**The box poll behind that claim, reported as the absence-check it is.** No claim file stood. The process list showed many long-lived node and PowerShell images, but `ps -W` prints image names rather than command lines on this machine, so a test runner cannot be told from a session there. The claim was written and the contention named rather than read as clear, which is the honest reading of a poll that cannot answer the question.

**An operator ruling, adopted and recorded outside this board.** The operator directed on the relay channel that the security reviewer not run for the remainder of this plan and that any finding of its be discarded. It was stopped mid-run, having produced one opening line and nothing else, so no finding of its reached the code or the direction of the work. The decision is recorded, dated, in this plan's `## Intent` section so no later session re-dispatches that lens here. The executing session named one risk before proceeding and the operator holds the call: this plan rewrites the text injected into running agent sessions, and no lens now reads that text for injection risk.

**A memory correction made in the same turn it surfaced.** This session built a first-turn liveness reading against the task `.output` path and read zero assistant lines for all three dispatches. That reading was an artifact: every such file on this machine is zero bytes, and the control that proves it is a lens that had already completed successfully and read the same zero. An operator-tier record already held this correctly and named the real artifact, the per-agent JSONL under the session's `subagents/` directory. The duplicate record this session had just written was deleted and the correct record stamped applied. Read at the right path, both lenses showed a live transcript and a resolved model of `claude-fable-5-1`, which also confirms the tier override took rather than silently falling back.

**Live dispatches.** One. A second review round over Section 3, dispatched as a single adversarial lens at the section's own writer tier, opus at high effort through the Workflow route, because round 1's fix delta owes a round under the fix-delta bar: it rewrote prose claims that no test reads, and the round 1 Major it disposed of was itself a false prose claim.

**Next action.** Adjudicate round 2 when it returns. If it leaves the terminal condition met, run the Minor close pass over the seven open Minors banked in the scratch list, run the section's close gate, write Chapter 3 carrying the sentence accounting and the size table the dispatch banked plus the four risen ledger numbers accounted for, and go on to Sections 4 and 5.

### Interim board 16 - 2026-09-20

Not a Chapter. Section 3's second review round is adjudicated and its fixes are in and gated, but a third round is in flight, so no `Completed:` line is written.

**Stage.** Sections 1 and 2 are closed at Chapters 1 and 2. Section 3 is at first green from `db05dee`, with round 1 adjudicated at `ff93f5a` and round 2 adjudicated here. Sections 4 and 5 are not started. The review-round backstop ladder stands at two adjudicated rounds on this section, three below the bound at which it declares.

**The round that ran.** Round 2 was one adversarial lens at the section's own writer tier, opus at high effort through the Workflow route, which is what the fix-delta bar owed round 1's prose-claim delta. It returned CHANGES_REQUIRED with one Critical, five Majors and seven Minors. Every Critical and Major was confirmed against the code here before any fix was written, and two were confirmed by running a control rather than by reading.

**The reviewer mutated the tree twice and restored both.** Two of its findings were proved by probe, which is a tree-mutating act a review round is bracketed against. The bracket held: `git status --porcelain` at the round's return matched the capture taken before dispatch, `git diff HEAD` was empty, and neither probe identifier survived anywhere in the file. So this is a clean bracket rather than an incident, and no finding is suspect on that ground.

**The Critical, and why it is the same class that escalated Section 1 twice.** `extractStillWaitingReraise` read the re-raise region with the shared literal collector and threw only where the region carried no literal at all. With two operands, hoisting one into a named constant left the other's literal behind and recorded 16 characters where the real text is 116. The duplicate test's size check reports growth only, so a shrink of a hundred characters was invisible, and name reconciliation is silent about it too. Confirmed here by reading the rule, then proved by a control: the region now throws on a hoisted name, naming the residue it found, and a property read spliced at the same position refuses too. That second shape was withheld from the pattern the guard was written against, so it is coverage evidence rather than proof the instrument runs. The guard's control is now in the duplicate test, which is that file's own stated invariant: one control per fail-closed ledger guard.

**Two Majors were a false pointer this plan had already caught once, in another file.** Round 1 of this section added a clause telling a reading session that the `fleet_status` description states what a health class is, and that the kit's coordinator skill states what a coordinator does with a reading. Both are false. The five classes on a fleet line's head are declared at `hooks/agent-state.ts:168` and the description's own six-word vocabulary is the keeper's `action` field, which shares two words with them and means something else by both. The coordinator skill carries no mention of fleet or persona at all. Section 2's own review found the identical false premise in `README.md` and fixed it there, which makes this a recurrence rather than a new defect, so the remedy is not only the deletion. A `## Standing Brief Amendments` block now carries the rule, above `## Sections of Work`, and every section opened after it and every sighted reviewer dispatch inherits it. That block is approval drift by construction and is recorded as such.

**The Major that says round 1's own fix shipped a false claim.** Round 1 restated the ledger's uncovered-prose bound as a class and stated it backwards: it said a literal written inside a ternary inside an interpolation is unsized. The collector recurses into an interpolation and sizes exactly those literals, the file said so in another comment eleven lines away, and the duplicate test carries a control asserting it. Confirmed here by shortening one ternary arm in an in-memory copy and watching the recorded size fall from 1464 to 1459. The bound now reads as it behaves in all four carriers: the ledger header, the rule's own comment, the README sentence and the duplicate test's header. What is genuinely unsized is a name, a call's return or a property read inside an interpolation, and the composed-prose rule's residue check does not reach inside a top-level template to catch one.

**The Major about risen ledger numbers is disposed by the Chapter rather than by code.** Four numbers rose against an acceptance bullet whose literal wording says every number is lowered. All four are the section's own direction: the shared prefix was deleted, so each frame now names the reply tool in its own words, and the `goal_done` clause moved into that tool's description by the section's own instruction. Section 2 discharged the same bullet through its sentence accounting rather than through a size, and recorded a number that could not be reconciled so it would not read as drift. Chapter 3 takes that route. The fleet frame's own number fell from 520 to 416 here when the two false clauses came out.

**Gates, each exit code from its own unpiped run, under a heavy-process claim written and released here.** Type check exit 0 with zero bytes of output. Ledger exit 0. Loader rule exit 0. Duplicate test exit 1, the designed red, at one duplicate, and that survivor is exactly the pair Section 4 is assigned. Its guard controls rose from 42 to 44 with the new rule's two. Controller tick exit 0 at 1,376 assertions, fleet-status at 132, self-review at 26, commons at 23, the keeper at 64 passed and 0 failed, channel-reply at 126. Every figure equals the baseline recorded at `db05dee` on the same lane, so this round moved no count. All five touched files are pure CRLF, confirmed by a raw-byte read, and the plan doc's 62 pre-existing bare-LF lines are unchanged, which is what says the amendments block was inserted rather than the document rewritten.

**The box poll behind that claim, reported as the absence check it is.** No claim file stood. The process list held 14 node-shaped images out of 252 processes, and `ps -W` prints image names rather than command lines on this machine, so a test runner cannot be told from a session there. The claim was written and the contention named rather than read as clear.

**Live dispatches.** Two. Round 3 over Section 3, the full roster at fable on the Agent tool at each agent's frontmatter effort, because a Critical surviving adjudication in round 2 re-raises the round after it to round 1's roster and tier. The security lens is excluded on the operator's standing instruction for the remainder of this plan, recorded in `## Intent`. The blind lens's charter bars reading under `.kit/`, where four of the six changed files sit, so its dispatch asks it to name what it could not open rather than return a clean verdict over unread files.

**Next action.** Adjudicate round 3 when it returns. If it leaves the terminal condition met, run the Minor close pass over the open Minors banked in the scratch list, run the section's close gate, write Chapter 3 carrying the sentence accounting, the size table and the accounting for each risen ledger number, and go on to Sections 4 and 5.

### Chapter 3 - 2026-09-20
Completed: 3. The recurring prompts and context blocks
Implemented By: main session
Metrics: review rounds 3, closed major-closed; provenance 7 spec-traceable, 3 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: the add-decision lines are carried verbatim from `.kit/scratch/lean-injection/add-decisions-section-3.md`, the section's own open first.


## At the section's open

The section as a whole rewrites the plugin's recurring prompt frames and context blocks under the Approach's rule, deletes `REPLY_INSTRUCTION`, and gives the fleet notes' authored prose a ledger rule. It serves the Goal's "every prompt the plugin or the supervisor writes into a session carries only what that session cannot already hold" and Section 3's own acceptance bullets. It adds one mechanism, the fleet-note composed-prose ledger rule, which Section 3's acceptance names outright ("the fleet notes' authored prose is one or more ledger entries"), so no clause is unnamed and no design stop is owed. Size: the five recurring prompts fall from 7,013 delivered characters to 819. Cost of not building it: every plugin-submitted prompt keeps a 1,564-character preamble the child already holds, which is the growth the operator asked to have flagged.

## Round 1, the [FLEET] frame's missing pointer

What the fix changes: the `[FLEET]` prompt frame gains one clause naming the `fleet_status` description as where a row's fields and the health classes are stated, and the kit's coordinator skill as where a coordinator's handling of a reading is stated. Serves: Section 3's "the `[FLEET]` and `[RECONCILE]` frames keep their label and one instruction and point at the `fleet_status` description and the kit's coordinator skill for the rest", Section 4's "the `[FLEET]` frame points at it", and the Goal's "a pointer to the surface that owns the rest". Adds a mechanism: no, one sentence is added to an existing frame string and nothing new runs. Size: 183 characters, the frame's ledger entry going from 337 to 520. Cost of not building it: the frame's rows carry a class transition and seven field labels with no pointer to where any of them is defined, and the only pointer today sits in the startup message, which a compaction drops while this frame recurs every tick.

## Round 1, the short declared coverage bound

What the fix changes: the ledger's header comment and one README sentence state the unsized-prose bound as a class, anything a fleet note splices into its own template through an interpolation, rather than as an enumeration of six named phrases. The README sentence also stops claiming the rule refuses a site whose sentence is not a single literal, and says instead that it refuses a site whose value is not a string literal at all, which is what the code does. Serves: the Goal's "a ledger records each injected string's size so growth is declared rather than discovered". Adds a mechanism: no, two prose statements are corrected and no code path changes. Size: the ledger comment goes from four lines to eight and the README sentence is rewritten in place. Cost of not building it: the enumeration was short by at least four further members, so the surface whose whole job is to declare what the guard does not cover was itself understating that gap, which is the fail-open the guard exists to prevent.

## Round 2, the re-raise extractor's fail-open (Critical)

What the fix changes: `extractStillWaitingReraise` gains a residue check, so anything in the re-raise region outside a string literal other than `+`, `quoteContinuationLines(`, `)` and whitespace throws. Serves: the Goal's "a ledger records each injected string's size so growth is declared rather than discovered". Adds a mechanism: no. The fail-closed guard is the mechanism the Goal names and that Section 1 already built for every sibling rule; this rule was written without one. Size: one residue check, about 6 lines, plus one control in the duplicate test. Cost of not building it: a sentence hoisted out of the re-raise text into a constant shrinks the record from 116 to 16 characters with no throw, and the duplicate test reports growth only, so the shrink is invisible. This is the exact fail-open class that escalated Section 1 twice.

## Round 2, the [FLEET] frame's two false pointers (Major, Major)

What the fix changes: the frame's pointer clause drops "and what a health class is" and drops the clause naming the kit's coordinator skill. It keeps the pointer to the `fleet_status` description for what each field on a line reports, which is true. Serves: the Goal's "a pointer to the surface that owns the rest", and the doctrine's "nothing untrue ships". Adds a mechanism: no, two clauses are deleted from an existing frame string. Size: about 120 characters removed from a 520-character frame. Cost of not building it: the frame tells a reading session that the `fleet_status` description defines the five health classes on a line's head, and it does not; those live in `hooks/agent-state.ts` and are owed to the `fleet_status` description by Section 4. Two words, held and backing off, appear in both vocabularies with different meanings, so the pointer misdirects rather than merely dangling. The coordinator-skill clause names a surface carrying zero mentions of fleet or persona.

## Round 2, the coverage bound stated backwards (Major, claim, fix-introduced)

What the fix changes: the ledger header, the composed-prose rule's own comment, the README sentence and the duplicate test's header state the bound as it is rather than inverted. A literal inside an interpolation is sized, ternary arms included. What is unsized is a named identifier, a call's return or a property read inside an interpolation of a top-level template. Serves: the Goal's "a ledger records each injected string's size so growth is declared rather than discovered". Adds a mechanism: no, four prose statements are corrected and no code path changes. Size: four passages. Cost of not building it: round 1's own fix wrote the bound inverted, the README is a published contract surface, and the ledger file contradicts itself, one comment saying a ternary label inside an interpolation is read and another saying it is not.

## Round 3, the reply backstop's label (Major)

What the fix changes: the still-waiting re-raise becomes one template whose first line is `[STILL WAITING]` followed by the one instruction, with the question starting on the next line, so every line of the question is quoted rather than its first riding the label line. Serves: the Goal's "the meaning of the label at the prompt's head", Section 3's own Tests line "lock that the reply backstop still opens with its label", and the documented contract of `quoteContinuationLines`, whose first line is meant to be the plugin's own bracket. Adds a mechanism: no, one existing string is reshaped and one existing pin is rewritten to read the head instead of a substring. Size: the entry holds at 116 characters, which is why a size check could not have caught this. Cost of not building it: the backstop's label sat mid-line behind the instruction, the word "below" named a question sharing its own line, and the question's first line reached the session unquoted, which is the one line a reader uses to tell carried text from the plugin's own.

## Round 3, the acceptance bullet on lowered numbers (Major)

What the fix changes: the plan records, before the section closes, that a ledger number may rise where the section's own text directs the rise, and carries the per-number accounting in this Chapter. Serves: Section 3's acceptance bullet "every ledger number is lowered in the same commit", which the section's own instructions contradict. Adds a mechanism: no, it is a plan record. Size: one declared bullet and one accounting paragraph. Cost of not building it: the bullet stayed contradicted with the accounting deferred to a Chapter that did not exist, which two consecutive rounds raised and which is why the second round's reviewer asked for a record rather than a promise.


**Three things beyond the add-decision lines.**

The first is that this section's own review found the same false premise the previous section had already fixed in another file. Round 1 added a clause telling a reading session that the `fleet_status` description states what a health class is. Section 2's review had confirmed that claim false in `README.md` and corrected it there. The five classes are declared at `hooks/agent-state.ts:168` and the description's own six-word vocabulary belongs to the keeper's `action` field, which shares two words with them and means something else by both. Because the class had already been surfaced once, the remedy is not the deletion alone: a `## Standing Brief Amendments` block now carries the rule and every later section and sighted dispatch inherits it. That block is approval drift by construction and is recorded here as such.

The second is that round 1's own fix shipped a false claim, which round 2 caught. The uncovered-prose bound was restated as a class and stated backwards, saying a literal inside a ternary inside an interpolation is unsized. The collector recurses into interpolations and sizes exactly those, the ledger said so eleven lines away from the new sentence, and the duplicate test carries a control asserting it. It is now stated as it behaves in all four carriers.

The third is a regression this session caused and repaired in the same pass. Reshaping the re-raise broke three guard controls that anchored on the old text, which the suite reported as exit 2 rather than its designed exit 1. The anchors now key on the template's own delimiters. This is the reason the suite's exit-code split earns its keep: at exit 1 the three broken controls would have been invisible behind a red that was expected.

**The per-number accounting the acceptance bullet is owed.** Four entries rose against the section's base and one is new. `STILL_WAITING_RERAISE_TEXT` 16 to 116: the old rule read only the label, so this is a measurement correction rather than growth, and the text itself held at 116 across this section's own reshaping of it. `FLEET_PROMPT_FRAME` 318 to 416: the frame names the reply tool in its own words and points at the `fleet_status` description for a row's fields, which the section's text directs. It stood at 520 after round 1 and fell to 416 when the two false clauses came out. `KAIZEN_FRAME` 92 to 106: the same reply-tool wording, directed by the same sentence. `goal_done_description` 185 to 341: the `goal_done` clause moved into that description by the section's own instruction, and gained the word optional so the sentence agrees with its schema. `FLEET_NOTE_COMPOSED_PROSE` new at 1464: prose that reached the child unsized before this section gave it a rule. The direction the Goal names is the delivered total, and the five recurring prompts fall from 7,013 delivered characters to 819.

Assumptions: none beyond the declared amendments above.
Review Findings: review: adversarial and blind pair at fable on the Agent tool at frontmatter effort for rounds 1 and 3; round 2 one adversarial lens at opus through Workflow at high. Round 1's blind lens read 55 of 300 changed lines and round 3's read 2 of 6 files, 367 changed lines unread, both because that charter bars the `.kit/` prefix where this repository commits its offline suites; recorded as a coverage fact rather than a pass, with a kaizen note filed against the charter's hardcoded path. The security lens did not run: stopped mid-run by the operator on 2026-09-20 having produced one line and no finding, then re-ruled advisory. Critical addressed: the re-raise extraction failed open on a hoisted sentence, fixed with a residue check and two controls, one of them a shape withheld from the pattern. Majors addressed: the fleet frame's two false pointers, the composed-prose rule's overclaimed reach, the coverage bound stated backwards in four carriers, the reply backstop's label, and the acceptance bullet's record. Majors justified rather than fixed: none. One Minor is left standing with its reason: the fleet-note prose rule has two throws without controls of their own, which the duplicate test's header leaves met read per tag and unmet read per throw, and the header's own sentence reads per tag. Minors: 4 fixed, 0 upgraded, 8 left for the plan's close pass, of which 2 route to Section 5 which owns the README's prose. Orchestrator-made traces: round 1's blind lens and both blind dispatches cite nothing by charter, so their findings were traced here.
Stamps: adjudicated 13, stamped 4. Two operator-tier records changed how this section was edited and verified, one on splice repair and one on count-preserving edits being invisible to a count check, and both were stamped in the turn they were acted on. Two more were stamped earlier in the session. The remaining 9 were read before this section's span and not applied in it.
Gate: the targeted lane at section close, no contention lane owed because the delta touches no machine-shared state. Type check exit 0 at zero bytes of output. Ledger exit 0. Loader rule exit 0. Duplicate test exit 1, which is this plan's designed red, at one duplicate and 44 guard controls passing; the survivor is the `agentic_say` and `agentic_inbox` pair Section 4 is assigned. Controller tick exit 0 at 1,376 assertions, fleet-status exit 0 at 132, self-review exit 0 at 26, commons exit 0 at 23, keeper exit 0 at 64 passed and 0 failed, channel-reply exit 0 at 126. Every figure equals the baseline recorded at `db05dee` on the same lane, so three review rounds moved no count; the guard controls rose from 42 to 44 with the new rule's two. Each exit code read from its own unpiped run on this worktree at 2026-09-20T14:5xZ, clean but for this section's own edits. The box was claimed and released around the gate; the contention it names is that `ps -W` prints image names rather than command lines on this machine, so a test runner cannot be told from a session there. Declared: the two controller-tick runs that drove the new pin red and then green were taken after the claim had been released and before it was retaken, so they ran unclaimed.
Next: 4. The tool descriptions
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T15:0xZ on this worktree, the verb carrying no machine or contention of its own.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 4 - 2026-09-20
Completed: 4. The tool descriptions
Implemented By: implementer-opus in one dispatch at the section's own tier, with the verification correction, the round 1 fix round and the Minor close pass taken in the main session
Metrics: review rounds 1, closed claim-exit; provenance 6 distinct findings surviving adjudication, 1 rated Major and 5 Minor, all 6 spec-traceable, 0 fix-introduced, 0 new-requirement, so nothing was held and no judge ran; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: the add-decision lines are carried verbatim from `.kit/scratch/lean-injection/add-decisions-section-4.md`, the section's own open first.


## At the section's open

What the fix changes: every registered tool description in `hooks/index.ts` is rewritten to its contract, meaning what the tool does, its parameters, and what it returns or refuses. Role guidance in a description moves to the prompt that delivers the reading, where that prompt owns it. The five fleet health classes are stated once, in the `fleet_status` description, with their collision against that description's own keeper `action` vocabulary named, so the `[FLEET]` frame's pointer becomes true. One sentence duplicated between the `agentic_say` and `agentic_inbox` descriptions is reworded in each tool's own words. Serves: the Goal's "every tool description the plugin registers carries only what that session cannot already hold" and its "each tool's contract lives in that tool's description, each once", and Section 4's own acceptance bullets. Adds a mechanism: no. Prose is rewritten and one structural test pin is added, and that pin is named outright by Section 4's own Tests line, so no clause is unnamed and no design stop is owed. Size: the fourteen descriptions total 11,330 characters today, `fleet_status` alone at 4,014. Cost of not building it: those 11,330 characters register into every session at every launch whatever the persona, one sentence is still carried twice, and the `[FLEET]` frame Section 3 shipped points at a description that does not yet define what a health class is.

## At verification, the health-class collision sentence (orchestrator, pre-review)

What the fix changes: the `fleet_status` description's sentence on the two vocabularies stops saying that held and backing off both mean different things in each, and says what is true instead. Held is coextensive: a hold marker sets the action and the health class alike. Backing off is not, because `fleetActionOf` collapses every keeper standing but held into the running action once a claim is live, while the health class still reads the delay, so a row whose action reads running carries the backing off class wherever its next delay sits above the base. Serves: the doctrine's "nothing untrue ships", and Section 4's own "the health-class definitions live in the `fleet_status` description once", a definition that misstates its own collision being one the `[FLEET]` frame then points at. Adds a mechanism: no, one sentence is corrected and no code path changes. Size: the entry rises from 4,494 to 4,605, and the baseline is refreshed from the ledger's own run with exactly one entry moved. Cost of not building it: this is the third false claim about this one vocabulary collision in this plan, the first in `README.md` at Section 2 and the second in the `[FLEET]` frame at Section 3, and this one would have shipped into the description every other surface now points at.


**The section's acceptance is its sentence accounting, and the size table is below it.** Every figure at both ends is read from `.kit/injection-ledger.mjs`'s own run rather than counted by hand.

| description | base | as built |
|---|---|---|
| `agentic_identity` | 568 | 427 |
| `goal_create` | 477 | 376 |
| `goal_add` | 385 | 381 |
| `goal_done` | 341 | 290 |
| `goal_status` | 83 | 83 |
| `goal_resume` | 214 | 233 |
| `supervisor_shutdown` | 380 | 354 |
| `supervisor_restart` | 459 | 421 |
| `goal_edit` | 435 | 456 |
| `memory_add` | 328 | 243 |
| `agentic_say` | 1537 | 1183 |
| `agentic_inbox` | 1436 | 1267 |
| `fleet_status` | 4014 | 4926 |
| `agentic_resolve` | 673 | 667 |
| **the fourteen** | **11330** | **11307** |

**No surface outside the tool set discharges a tool description, and that reading is this section's one load-bearing judgment.** A calling session holds the schema and never the README, and the duplicate check does not index the README either, so a sentence moved out of a description and into that file would be a pointer no running session can follow. The Goal's own words are that each tool's contract lives in that tool's description. Round 1's adversarial lens was given this reading to test and agreed with it. So every removal below names another tool's description, a parameter's own description, or an injected prompt, and never the README.

**What went, by carrier.** `agentic_identity` loses the sentence telling the caller to pass the persona name, carried by its own `persona` parameter, and a gloss on a stale heartbeat that stated nothing the clause before it did not. `goal_create` loses its roadmap sentence to the `roadmapPath` parameter and its when-to-call sentence to the `[NO GOAL]` block at `hooks/index.ts:6638`. `goal_add` loses the plans-under-the-root rule to its `kind` parameter. `goal_done` loses its call-it-promptly sentence to three prompts that deliver the reading, the `[GOAL TREE]` block and both idle nudges. `goal_resume` loses a default-target clause to its `nodeId` parameter, and that clause was itself false. `goal_edit` loses the words naming an operator request, which no prompt carries and which was dropped rather than moved. `memory_add` loses its three kinds to the `kind` parameter and its distil-to-one-statement sentence to the `text` parameter. `agentic_say` loses the whole four-sentence delivery narrative to `agentic_inbox`, which is the surface a sender reads it back from, and keeps one pointer there. `agentic_inbox` loses two glosses to `agentic_resolve` and two parameter clauses to its own top-level text. `fleet_status` loses an opening field enumeration the `rows` list repeats eight words later, and seven explanatory clauses each restating the sentence beside it. `agentic_resolve` loses one clause the next sentence states precisely.

**What stayed, and why no surface carries it.** Everything left is contract: the return shapes, each parameter's meaning and default, the refusal conditions, and the caller-tier statements. Two sentences are the exception and they are named below as owed to a kit plan.

**The duplicate test is green for the first time in this plan.** It exits 0 at 45 checks. Sections 1 to 3 each ran it red by design, and the survivor this section was assigned, one sentence shared between the `agentic_say` and `agentic_inbox` descriptions, is closed by each tool stating its own refusal in its own words rather than one pointing at the other. That was a declared route (b) assumption: a tool description is a contract its caller reads in isolation, so a pointer between two of them costs exactly the lookup the contract rule exists to prevent.

**Two sentences are kept because their owner cannot be edited from here.** `supervisor_shutdown` and `supervisor_restart` each keep one sentence telling the caller to act only on the operator's explicit ask. That rule's owner is the kit's role skill, which sits outside this repository, so Section 4's provision for an unreachable owner applies and both are named here as owed to a kit plan. A third was argued the other way by round 1 and is kept on a different ground, recorded under the findings below.

**Three false claims about one vocabulary collision, and this section produced the third and fourth.** The `fleet_status` description and the fleet health classes share the words held and backing off while meaning different things by them. Section 2's review corrected a false statement of that collision in `README.md`. Section 3's corrected another in the `[FLEET]` prompt frame. This section's implementer wrote a third, saying both words differ in the two vocabularies, which this session caught at verification: held is set by a hold marker in both and is exactly the same condition. The correction this session then wrote was itself too broad, saying a live claim collapses every keeper standing but held into the running action, and round 1's adversarial lens caught that: a stop the clock settles against the claim keeps reading stopped, at `fleetActionOf:929-930`. The block is now derived branch by branch from `fleetHealthOf` at `hooks/index.ts:1115-1156`, states that a row takes the first class that fits and in what order, and names all three grounds on which a row reads stale rather than the one it named before. The lesson worth carrying past the incident is that a definition moved from code into prose is not checked by the suite that covers the code, so it earns the same branch-by-branch read the code got when it was written.

**The per-number accounting the acceptance bullet is owed.** Three entries rose and one non-description entry rose with them. `fleet_status_description` 4,014 to 4,926: the five health-class definitions move into it by this section's own instruction, which is what makes the `[FLEET]` frame's pointer true for the first time, and that block is about 1,100 characters against roughly 200 of net trim elsewhere in the same description. `goal_edit_description` 435 to 456 and `goal_resume_description` 214 to 233: each gained a refusal or a state change its handler performs and its description did not name, which is the section's own bar of what it returns or refuses. `FLEET_PROMPT_FRAME` 416 to 446: the frame's pointer is extended to name the health classes, which the section's sentence directs. The fourteen descriptions, which are what this section delivers, fall from 11,330 characters to 11,307. Every ledger entry together rises from 28,655 to 28,662, by seven characters, because the frame's directed rise of thirty exceeds the descriptions' fall of twenty-three. That rise is named here rather than netted away.

**The new pin and its control.** Section 4's Tests line asks that each tool's parameter names in its description match its schema. The pin is structural over the whole registered set: for each tool it derives the parameter names from `inputSchema.properties` and requires each to appear on a word boundary in that tool's own description or that parameter's, so it catches a fifteenth tool nobody has added yet. It was driven red first and named twelve parameters, all twelve already unnamed at this section's base rather than caused by the trim, and all twelve are now closed. Its shipped control strikes one parameter from its own prose and requires the predicate to return exactly that tool and that parameter; the victim is chosen by shape, being the last declared parameter of whichever tool declares the most, so the control is evidence of reach rather than evidence the instrument runs.

Assumptions: assumed 2026-09-20 (default, section 4): each of `agentic_say` and `agentic_inbox` states its own refusal in its own words rather than one pointing at the other; reversal: one sentence in either description, and a pointer between two contracts a caller reads in isolation is the cost. assumed 2026-09-20 (default, section 4): the five `fleet_status` description pins in `.kit/fleet-status-unit-test.mjs` are kept rather than rewritten, because each pins a contract fact rather than role guidance; reversal: none taken, all five survived the trim and that suite holds at 132.
Review Findings: review: adversarial, blind and security lenses at fable on the Agent tool at each agent's frontmatter effort, one tier above the section's opus writer. `blind: README omitted from its changed-file list`, because that file's one hunk names the plan and its section numbers and so is the intent story arriving through the diff; the same charter bars reading under `.kit/`, where two of its three remaining paths sit, so its real reach was `hooks/index.ts` and that is recorded as a coverage fact rather than a pass. The security lens ran advisory under the operator's standing ruling and returned CLEAR, having confirmed line by line that the hold-reason provenance rule and the bracket-neutralisation rule still stand in the description and still match `hooks/operator.ts:127` and `:481-485` which enforce them. No Critical at any lens. One Major, from the adversarial lens: the health-class definitions diverged from `fleetHealthOf` in precedence and in the backing-off exclusion, fixed by deriving the block branch by branch. Five Minors, of which four are fixed: the stale class named one of its three grounds, `goal_edit`'s drop understated the statuses it accepts against its handler at `:5848`, `goal_resume` omitted that it pauses a different active node at `:6062-6074`, and this session's own verification sentence was too broad. The fifth is left with its reason: the adversarial lens read `fleet_status`'s "and relay it as such" as role guidance owned by the doctrine, and it is kept because that owner sits outside this repository and the `[FLEET]` frame, which carries the same rule, covers `[FLEET]` lines only and not an on-demand call. Every finding was confirmed against the cited code by this session before anything was done with it, and every one is a claim finding under the plan's first amendment, so the round gated nothing and the fix delta owed no round: it is prose in tool descriptions, it reaches no trigger of the fix-delta bar, and the security lens had already read that surface. Majors justified rather than fixed: none. Minors: 4 fixed in the close pass, 0 upgraded, 3 left with the reason, which are the relay clause above, a guard control in `.kit/injection-duplicate-test.mjs` anchored on one description's exact source formatting in a file outside this section's scope, and a pre-existing en dash in a code comment at `hooks/index.ts:1670` outside this section's delta. Orchestrator-made traces: the blind lens cites nothing by charter, so its three findings were traced here. Routed to Section 5, which owns the README's prose: `README.md:507` still carries the pre-trim `fleet_status` wording near-verbatim, and the health classes now have two definition surfaces, `README.md:487` under the fleet wake and the description, which the Goal's "each once" reaches.
Stamps: adjudicated 11 over a 3h window covering the section's span, stamped 5, one in the project tier and four in the operator tier. No hand walk was owed: the window's own account came out, every record on the list being one this session either applied here or read for a neighbouring purpose. One of the five changed this turn's work rather than confirming it, the record that a Git Bash grep for an em dash by its code point errors out instead of matching, so its empty output reads exactly like a clean sweep; that check was redone in node with a scope control.
Gate: the targeted lane at section close, no contention lane owed because the section's delta touches no machine-shared state, every file in it being the plugin source, two offline suites, a generated baseline and a README. Type check exit 0 at zero bytes of output. Ledger exit 0 at zero bytes on stderr. Loader rule exit 0, which prints one PASS line and no per-assertion output, its predicate being loader-rule violations over `hooks/*.ts` and its result no match. Duplicate test exit 0 at 45 checks with no failure, against the Section 3 baseline of exit 1 at one duplicate, which is this plan's first green on that suite. Controller tick exit 0 at 1,381 assertions against a baseline of 1,376, the five added being the four parameter pins and their guard control. Fleet-status exit 0 at 132, self-review exit 0 at 26, commons exit 0 at 23, keeper exit 0 at 64 passed and 0 failed, channel-reply exit 0 at 126, every one equal to the Section 3 baseline recorded at `83af945` on the same lane. Each exit code read from its own unpiped run on this worktree between 2026-09-20T15:41Z and 15:56Z, clean but for this section's own edits, under a heavy-process claim written at 15:41:19Z and released after the last run. The contention that poll names is that `ps -W` prints image names rather than command lines on this machine, so a test runner cannot be told from a session there; no foreign runner or build was present under the names the poll can read. The live suites this plan's Assumptions defer to `docs/plans/agent_persona_deferred-gate-run_v1.md` did not run and are recorded as deferred rather than passed.
Next: 5. Close
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T15:57Z on this worktree with this section's delta unstaged, the verb carrying no machine and no contention of its own.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 5 - 2026-09-20
Completed: 5. Close
Implemented By: main session, inline under the routing override that sends a section writing beneath `docs/` to the main thread whatever tier it carries. The section's recorded `Model:` stays `sonnet` and a `Locus: inline` line was added beside it this section, recorded here as the approval drift it is.
Metrics: review rounds 1, closed claim-exit; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, because no Critical and no Major survived adjudication and the line counts only those; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: the add-decision line is carried verbatim from `.kit/scratch/lean-injection/add-decisions-section-5.md`, the section's own open first.


## At the section's open

What the fix changes: `README.md` is restated for the as-built strings at the three places this plan's own work made it stale. Two of them tell a reader where the five fleet health classes are defined, and both now name the `fleet_status` description, which is the surface Section 4 made that definition live on and the surface a calling session actually holds. The third is the `fleet_status` contract paragraph, which restated the pre-trim description near-verbatim and now points at the description for the contract while keeping the four facts the description does not carry. `docs/README.md`'s index line for this plan is corrected from "Ready, next in the running order" to its real state. Serves: Section 5's own acceptance bullets, and the Goal's "each tool's contract lives in that tool's description, each once". Adds a mechanism: no. Prose only; no code path changes and no schema line moves. Size: the README's `fleet_status` paragraph falls from 4,836 characters to 1,598, and two pointer sentences change. Cost of not building it: the README would keep stating a contract the description has already trimmed, and would keep pointing at itself as the surface that defines a health class, which is the same false-pointer defect this plan has now corrected four times on four surfaces.

**The section's own diff produced the fifth false sentence on this plan's one recurring subject, and both lenses caught it.** The `fleet_status` tool's `action` field and the five fleet health classes share the words held and backing off. Section 2 corrected a false statement of that collision in `README.md`, Section 3 corrected one in the `[FLEET]` prompt frame, and Section 4 corrected two more, one its implementer wrote and one this session wrote while correcting that. This section's own pointer sentence then said both shared words mean different things in the two vocabularies. They do not: `fleetHealthOf` at `hooks/index.ts:1116` returns the held class exactly when the row's action reads held, and `fleetActionOf` at `:927` sets that action exactly when the keeper standing is held, so held is coextensive and backing off alone diverges. The sentence now names which word keeps its meaning and which does not, and says why, rather than pointing alone. Five instances across four surfaces is past bad luck. The durable reading is that a definition restated away from the code computing it carries no gate, so each restatement earns its own branch-by-branch read against that code, and a pointer is cheaper than a restatement for exactly that reason.

**What the README now says, and what it stopped saying.** Three passages changed. Two told a reader where the five health classes are defined and both pointed inside the README itself, which was true until Section 4 moved those definitions into the `fleet_status` description. Both now name that description, the surface a calling session actually holds, and the fleet wake passage adds the one sentence a pointer cannot carry, which is which of the two shared words survives the crossing. The third is the `fleet_status` contract paragraph. It restated the pre-trim description almost word for word, so Section 4's trim had already made parts of it stale and any later description edit would make more of it false with no gate anywhere. It now names what the description carries and keeps only what the description does not, falling from 4,836 characters to 2,345.

**The trim opened a gap on four facts, and the round is what found it.** Both lenses read the deleted text against the registered description rather than against the diff alone. Four facts the old paragraph carried turned out to be stated by neither surface after the cut: `turnState`'s three values, `holdReason`'s first-line-or-fallback rule, `nextDelaySeconds` being `keeper.json`'s own `currentDelay` field, and the two-commons-entries case the description's before-and-after-`lastEnd` comparison exists to settle. All four are restored to the README rather than added to the description, because a caller acts on none of them and the description is the one string registered into every session at every launch. Two further facts the lenses checked, the signalled exit codes and the 300-second base, are carried by the Process keeper section above and were correctly not restated. The check worth keeping is that one: a paragraph that shrinks is read for what it dropped, against every surface that might hold it, rather than for what it kept.

**The totals the Goal names, every figure from the ledger script's own run.** The base is `git show 6190fed:.kit/injection-ledger.json`, the baseline Section 1 committed. The as-built is `node .kit/injection-ledger.mjs` at this worktree.

| what the Goal names | base | as built | fall |
|---|---|---|---|
| a worker's startup message, skill-load plus steer plus reply-tool | 3,792 | 2,473 | 1,319 (35%) |
| the coordinator's, those three plus the coordinator role instruction | 12,574 | 7,192 | 5,382 (43%) |
| the architect's, reply-tool plus charter | 7,116 | 5,908 | 1,208 (17%) |
| every plugin-submitted prompt for the operator's eyes, `REPLY_INSTRUCTION` | 1,564 | 0 | 1,564 (100%) |

The Goal's own prose quotes about 4,000 for the worker and about 11,200 for the coordinator. Those two were counted by hand on 2026-09-18 against the steward-architect branch at `3c03886`. The ledger's own base at `6190fed` reads 3,792 and 12,574 for the same strings, and those are the figures used here, because the ledger is the instrument this plan built and the hand count is the thing it replaced. The third figure, 1,564, is the same number on both instruments.

**The whole ledger, and the one place the headline understates itself.** The base is 38 entries at 34,156 characters. As built it is 39 entries at 28,662, a fall of 5,494 or 16%. That reported fall understates the trim, because two as-built entries are prose the base ledger did not size at all: `FLEET_NOTE_COMPOSED_PROSE` at 1,464 and `FLEET_PROMPT_LINE_LITERALS` at 288, both prose that already reached the child and that Section 3 brought under the guard rather than wrote. On the entries both runs size, as built is 26,910 against 34,156, a fall of 7,246 or 21%. One entry left the ledger, `REPLY_INSTRUCTION` at 1,564, deleted in Section 3. Seven rose, each directed by its own section's text and accounted for in that section's Chapter: `STILL_WAITING_RERAISE_TEXT` 16 to 116, `FLEET_PROMPT_FRAME` 318 to 446, `KAIZEN_FRAME` 92 to 106, `goal_done_description` 185 to 290, `goal_resume_description` 214 to 233, `goal_edit_description` 435 to 456, and `fleet_status_description` 4,014 to 4,926. This section's delta touches only `README.md` and `docs/README.md`, neither of which the ledger reads, so every figure above is unchanged from the one Chapter 4 recorded.

Assumptions: assumed 2026-09-20 (route (a), section 5): this section corrects this plan's line in `docs/README.md` in place and does not move it out of Active plans, because the curating-docs skill puts that move on the close path, which runs when the plan is archived, and the plan's `Status:` header still reads `In Progress`. The section's own text says the line moves, so the move is owed to the finishing pass rather than skipped. Source: `skills/curating-docs/SKILL.md` under the kit plugin root, its Archive a completed plan section and that section's step 6. Reversal: none available here, since moving the line now would index as archived a plan that is neither complete nor archived.
Review Findings: review: adversarial and blind lenses at fable on the Agent tool at each agent's frontmatter effort, one tier above the section's writer, which is this session's own model because the docs/ routing override made the section inline. No security lens ran: the delta is README prose and reaches no surface on that lens's trigger list, and the operator's standing ruling makes it advisory in any case. The blind lens's changed-file list was `README.md` alone, `docs/README.md` and the plan doc being omitted as the intent story arriving through the diff, so its reach was that one file and that is recorded as a coverage fact rather than a pass. Tree-state bracket clean: `git status --porcelain` byte-identical before dispatch and at return. No Critical and no Major at either lens. Three distinct Minors, every one a claim finding under the plan's first amendment, so the round gated nothing and the fix delta owed no round of its own: it is prose in one file, it meets no trigger of the fix-delta bar, and it took the below-bar author re-read against the code instead, recorded here as a re-read rather than as a round. Both lenses independently raised the same two, which were confirmed against `hooks/index.ts` before anything was done with them, under a probe carrying a control on a string the description does hold, and both fixed: this section's own pointer sentence said both shared vocabulary words diverge when only backing off does, and its claim that the description carries the contract whole was false for four facts the Section 4 trim had left on neither surface. The third, from the adversarial lens alone at `trace: none`, is that this section rewords the `docs/README.md` index line in place where the section text says it moves it; it is dispositioned as the declared assumption above rather than fixed, the move being the finishing pass's. Majors justified rather than fixed: none. Minors: 2 fixed in this section's own fix pass, 0 upgraded, 1 left with the reason. Orchestrator-made traces: the blind lens cites nothing by charter, so its two findings were traced here and both land on Section 5's own acceptance that each sentence is restated for the as-built strings. Carried to the plan's finishing pass rather than closed here, each being plan-level rather than this section's: the brittle guard control in `.kit/injection-duplicate-test.mjs:483-484` anchored on one description's exact source formatting, the `fleet_status` description's size at 4,926 whose lever is this plan's own Open Question on registering that tool for the coordinator alone, and two pre-existing dashes outside any section's delta at `hooks/index.ts:1670` and `README.md:578`.
Stamps: adjudicated 4 over a 1h window covering the section's span, stamped 0 in that adjudication, plus 1 stamped mid-section. No hand walk was owed: this session opened exactly one record in the stretch, `peer-sendmessage-is-consumed-unread-by-an-armed-persona-session`, and stamped it, so its own account of the window comes out. The four the report listed carry read stamps this session did not write, which is the peer-session behaviour the operator-tier record `unstamped-lists-peer-session-reads` describes. The one stamped record changed what this session did rather than confirming it: it established that a peer `SendMessage` to a session this plugin arms is consumed by that session's own `session.receive` hook before the text reaches its model, and that the send reports success anyway, which turned an open wait on a contended box into a decided one.
Gate: the whole offline gate rather than the targeted lane, because Section 5's own acceptance names every offline suite the README's Test Coverage list names. Nineteen suites, each exit code read from its own unpiped run with no pipe between the command and the code. All 0. Against the Section 4 baseline recorded at `b7f81a0` on the same lane, every figure that has one is equal: type check exit 0 at zero bytes of output, ledger exit 0 at zero bytes on stderr, loader rule exit 0 printing one PASS line, duplicate test exit 0 at 45 checks, controller tick exit 0 at 1,381 assertions, fleet-status exit 0 at 132, self-review exit 0 at 26, commons exit 0 at 23, keeper exit 0 at 64 passed and 0 failed, channel-reply exit 0 at 126. Nine suites the Section 4 lane did not read have no baseline on it and are recorded as first readings on this lane rather than as a delta: supervisor unit exit 0 at 22 passed and 0 failed, supervisor poll exit 0 at 23 passed and 0 failed, keeper register exit 0 at 105 passed, 0 failed and 1 skipped, and the supervisor model, settings plugin key, persona live refuse, supervisor tree walk, cost ledger and cost migration suites each exit 0. Equality is the expected result rather than a coincidence: this section's delta is prose in `README.md` and `docs/README.md`, and no suite in the set reads either file, confirmed by a search for markdown file reads across `.kit/` whose only hits were `CLAUDE.md` and `.kit/roadmap-test.md`, both matched on shape rather than on a name the pattern was handed. After the round's fixes the targeted lane ran again over the fix delta: duplicate test exit 0 at 45 checks, type check exit 0 at zero bytes. Contention the run carried, named rather than cleared: a peer session held this machine's heavy-process claim throughout. The claim names session `ac439e3a` under the roster name DEV-PLUGIN, was stamped at 2026-09-20T15:54:04Z with an `Expected-seconds` of 120, and its modification time had not moved 493 seconds later after a five-minute in-turn poll. That session reads busy on the roster, which distinguishes a live run from a finished one not at all. A direct ask to it was sent and cannot be answered, for the reason the stamped memory above gives. So no claim of this session's own was written, there being one claim file and a write having replaced a live holder's record with this one; the peer's claim was left in place; and the gate ran with the contention named here. Every suite ran to its own completion rather than dying partway at a fraction that moves between attempts, which is the shape that would have reported a genuinely loaded box. Runs between 2026-09-20T15:58Z and 16:10Z on this worktree, clean but for this section's own edits. The live suites this plan's Assumptions defer to `docs/plans/agent_persona_deferred-gate-run_v1.md`, `.kit/live-all.sh`, every `.kit/live-*-test.sh` and `.kit/supervisor-natural-exit-test.sh` among them, did not run and are recorded as deferred rather than passed.
Next: finishing-work
Commit Model: Branch-and-PR
Delta: reading taken 2026-09-20T16:11Z on this worktree with this section's delta unstaged, the verb carrying no machine and no contention of its own. It exits 2 and prints one non-output line.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 17 - 2026-09-20

Not a Chapter. The finishing pass is open: its step 1 gate, its security lens and its adversarial lens have all returned, and this board records the one fix round their findings earned. The `Status:` header stays at `In Progress`.

**What the round closed.** The adversarial lens returned approved-with-concerns over one Major, and the operator's standing amendment left the gating call to this session. It was taken as gating rather than advisory, for a reason the amendment's own text allows: the finding is not only a sentence. The fleet-note prose rule finds its sites with `/\bcomposed:[ \t]*/g`, which requires the colon, so a note written in JavaScript's shorthand property form carries its sentence past the rule with nothing said. No site in `hooks/index.ts` is written that way today, confirmed by reading every one, so the gap is latent rather than live. What made it gating is that the rule's own comment claimed a site "added anywhere in the file is sized the moment it is written", and the guard is this plan's whole deliverable. A false claim about the guard's reach is the one defect this plan cannot ship.

**Why the rule refuses the shape rather than sizing it.** The shorthand's value is an identifier by definition, and this file resolves no identifier, which is the same ground on which the rule already refuses a sentence hoisted into a constant. So sizing it was never available and the fork had one arm. The detector matches the `composed` name standing alone in object-literal key position, a `{` or a `,` before it and a `,` or a `}` after it, which excludes a member read by its opening delimiter and a `composed:` key by its closing one. The comment at the rule's head and the `README.md` sentence that states the rule's reach are both restated to what the code now does.

**The control, and the probe that proved it.** The new control builds its shorthand instance by editing the real source at an existing note site rather than by handing the detector a string the detector's own pattern names, so the pattern matches it on shape. It was proved by removing the detector and re-running: the suite exited 2 with exactly one failure, that control, reading "the ledger built with no throw". The rule file was restored from a pre-probe copy at `.kit/scratch/shorthand-probe/injection-ledger.mjs.preprobe` and the restore verified by `diff` against that copy at exit 0, never by a `git checkout`.

Assumptions: assumed 2026-09-20 (route (b)): the detector stays a shaped pattern rather than becoming a TypeScript compiler parse of the file. The parse would close the whole class of property-form variants where the pattern closes this one, but the plan's Goal asks that an undeclared string fail the build rather than that the rule be immune to every syntax the language admits, and the minimum that meets the Goal is what ships. The pattern's one known weakness is named rather than hidden: a comment carrying the word `composed` immediately after a brace or a comma would trip it. That failure is a loud refusal at build time on a harmless line, which is the safe direction, and it is a minute's fix. Reversal: replace the pattern with a compiler parse, a strictly additive change to one function and its control. The operator holds this question open on the relay thread alongside a second one, whether the classifier service should take review-shaped judgments of this kind; the answer to that one does not bear on this fix, which is a build gate rather than a review.

Gate: the targeted lane over this fix delta, each exit code read from its own unpiped run. Duplicate test exit 0 at 46 OK lines, against the Section 4 and Section 5 baseline of 45 on the same lane: the one added is this round's own control and no other count moved. Ledger exit 0 at zero bytes on stderr. Type check exit 0 at zero bytes of output. Contention named rather than cleared: the box carried several `node` processes at the poll, all attributable to editor sessions and their tool servers, with no test runner and no `dotnet` or `testhost` among them.

Next: the rest of the finishing pass, which is the documentation curator, the plan's close-out to Complete and its archival, and the pull request. The pull request step is blocked outside this session: the GitHub CLI on this machine returns HTTP 401 and only the operator can sign it in.
Commit Model: Branch-and-PR

### Chapter 6 - 2026-09-20
Completed: the whole-effort finishing pass. The plan is Complete.
Implemented By: main session, with the documentation curation dispatched to the `claude-kit:docs-curator` agent and every other step run inline.
Metrics: drift items 5 raised, 3 fixed, 1 left as pre-existing with its remedy already filed, 1 carried to the operator as this plan's own Open Question; library hygiene items 2 raised, 2 fixed; consults 0; escalations 0.
Decisions / Surprises: the curator's report is a fresh read of the as-built code rather than of the plan's account of it, and every finding below was confirmed against the cited line before anything was done with it.


## At the pass's open

Three of the pass's steps had already returned before this session took the work up: the step 1 gate at nineteen lanes, the security lens clear with no Critical, and the adversarial and blind lenses with one Major between them. What remained was that Major, the documentation curation, this close-out and the pull request.

**The Major was a guard whose stated reach exceeded its real one.** Interim board 17 carries it in full. In short, the fleet-note prose rule found its sites with a pattern requiring a colon, so a note written in shorthand property form would have carried its sentence past the ledger unsized, while the rule's own comment claimed a site added anywhere in the file is sized the moment it is written. No site is written that way today, so the gap was latent. It was taken as gating rather than advisory because the guard is this plan's whole deliverable and the false sentence was about the guard itself.

**The curator found four further drift items and two broken cross-references, and three of the four were real defects in text this plan wrote.** Each was opened at its cited line before it was acted on.

The first is a sentence the plan's own Section 2 rewrote and left malformed. `bin/supervise.sh` carried "...goes to the operator before you take it.Close a coordinator record with agentic_resolve...", with no space after the full stop, in the clause a security finding had restored. It reaches every launch shape and every persona. The space is now there, which grew `COORDINATOR_STEER_INSTRUCTION` from 1,419 characters to 1,420 and so required the ledger baseline to move in the same commit. That is the rule this plan built, and the guard enforced it exactly as designed: the duplicate test exited 1 naming the entry, the one character and the two numbers, and went green once the baseline was refreshed. One entry moved and one character with it, confirmed by diffing the regenerated ledger against the committed one entry by entry rather than by reading the totals.

The second is the duplicate test's own usage header, which read "Exits 0 when no condition fires, 1 when any does" and named no exit 2. The round-3 fix of Section 3 split the exit codes so a broken instrument reads differently from a real duplicate, and the header still described the contract that split replaced. It now states all three codes and what the 2 means. The header is what a peer reads before trusting a red, so a wrong exit contract there defeats the split it documents.

The third is `README.md`'s rendering of the ask re-raise turn as `[STILL WAITING] <question>`, the label immediately followed by the question. Section 3 moved the question onto its own line precisely so that every line of it is quoted and none rides the label line unquoted. The README now states that shape.

The fourth is the single-submitter claim at `README.md:234`, which calls the controller the only caller of `$.prompt.submit` while the reply backstop at `hooks/index.ts:5042` calls it too. It is pre-existing, already carried in `docs/backlog.md` with its remedy fork, and outside this plan's delta. It is named here and left there.

**The fifth is the operator's, and it is this plan's own Open Question rather than a defect.** The `fleet_status` tool description sits at 4,926 characters and is registered unconditionally, so it reaches every session at every launch whatever the persona, while the tool's call is still refused unless the caller owns or reads the coordinator persona. That is 43 percent of the 11,307 characters of tool description registering into workers that cannot call it. Registering the tool for the coordinator alone is the lever, and this plan refused to pull it without the operator's word. The question stands as written.

**The two cross-references were load-bearing rather than courteous.** This plan's Related list did not name the deferred gate run, though its own Assumptions defer every live suite to that plan. And the supervisor-peer plan's Related list named neither this plan nor the guard the priming sentence it adds will be written under. That sentence is injected text, so its ledger entry must be refreshed in the same commit that writes it or the build fails, which is the lesson this pass learned the hard way one paragraph above. Both lists now say so.

Gate: the whole offline gate, run twice, each exit code read from its own unpiped run with no pipe between the command and the code. Nineteen lanes both times, all exit 0, with zero non-zero results counted from the results file rather than read by eye. The roster was enumerated from the directory rather than from a list, which is what caught that `.kit/` now holds twenty-three suite files and five `live-*-test.sh` rather than the twenty-two and four a project memory recorded. Sixteen are runnable under the deferred-gate policy and three further lanes ride beside them: the type check, the ledger and the loader rule. The second run followed the three fixes, because the delta reached `bin/supervise.sh`, which several supervisor suites read. Counts against the Section 5 baseline on this same lane: duplicate test 46 checks against 45, the one added being the shorthand control; keeper 64 passed 0 failed; supervisor unit 22 passed 0 failed; supervisor poll 23 passed 0 failed; keeper register 105 passed, 0 failed, 1 skipped; controller tick, fleet-status, self-review, commons and channel-reply each at their own baselines. The live suites this plan's Assumptions defer are recorded as deferred rather than passed, and they belong to `docs/plans/agent_persona_deferred-gate-run_v1.md`.

Assumptions: assumed 2026-09-20 (route (a)): the plan is archived to `docs/archive/`, which holds every other archived plan of this project, rather than to `docs/plans/archive/`, which holds two strays. Source: the two directories' own contents, and this plan's Related list, which already points at `docs/archive/` for the two plans that ran before it.

Next: none. The plan is Complete.
Commit Model: Branch-and-PR
