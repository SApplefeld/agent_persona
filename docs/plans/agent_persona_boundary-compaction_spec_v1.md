# The plugin banks a compaction boundary at each turn's end, so an unleashed persona compacts on its own

Status: In Progress
Commit Model: Branch-and-PR
Created: 2026-09-24

## Dispatch Authorization

The operator approved this plan's shape on the ASSISTANT relay thread on 2026-09-24 ("I'm good with all of the above. Please proceed as recommended." and, on the mechanism, "I'm good with everything you've suggested. Please proceed as recommended."). This is Plan A of the two persona-side plans that follow the nudge-state plan. It carries no Jev call: the judge-driven work is Plan B. Jev is TypeSafe's classifier service, which the plugin calls only in shadow today, journaling each answer and acting on none; promoting a question to a live decision is Plan B's opening section.

This plan depended on a fix in the claude-kit repository, now merged as the interactive-only plan's compaction section (PR 123). That section keys the boundary marker by session id under `~/.kit` and locates the transcript by session id, so the command records a usable position from a linked worktree, which is where every persona runs its plans, and the gate reads the marker wherever the session works. This plan reaches live use once the installed plugin cache is updated to that merged build, which is the operator's own act; the Operator Verification check is where that is observed. The running order is firm: the kit boundary section, now merged, first, then nudge-state, then this plan.

## Goal

When this ships, an unleashed persona compacts at a durable point in its work rather than running to the kit's 800,000 token safety ceiling. The plugin, not a leash and not the model, decides that point in code at the end of each turn: it runs the kit's checkpoint boundary command when the turn ended at a durable boundary and does nothing when the turn stopped mid-work. The idle nudge and the injected goal block name the plan document the persona is working, so a nudged persona re-reads the right document. A plan whose Status was flipped to Complete in the persona's own worktree is recognized as complete, rather than looping nudges on the stale launch-checkout copy until merge. The persona launch instruction and the repository's docs no longer say the model banks the boundary by hand, because the plugin does.

## Intent

The operator ruled that personas run unleashed, and the leash was the only thing that let a persona compact at a chapter boundary. Without a replacement, an unleashed persona would run its context to the kit's 800,000 token ceiling before compacting, the cost and quality regression the operator worked to avoid. This plan is the persona-side replacement: the plugin banks a compaction boundary itself, in code, at the end of every turn that reached a durable point.

The design review turned up a fact the mechanism rests on. The plugin captures the working directory only at session start, the launch checkout, and never refreshes it, while a persona works in a linked worktree. So it would read the plan document and write the marker against the wrong directory. The live directory comes from `$.session.cwd()` instead, which the plugin already uses as the session-start fallback; the stale field stays as it is for the persona store that resolves against it.

What done needs to do: read completion and Chapter progress from the live directory, run the kit's boundary command at turn end under fixed rules, name the plan document in the nudge and goal block, and stop the launch instruction and docs saying the model banks by hand. What it does not need to do: no Jev call and no per-turn goal record, which are Plan B, and it does not make the boundary command work from a worktree, which is the kit fix DEV-PLUGIN owns.

Alternatives refused:
- The plugin writes the marker file directly, in the kit's format. Refused: it makes two owners of the marker format and of the transcript-position computation, the split the operator ruled against; the kit's command owns the marker and this plan calls it.
- The persona is told to run the command through its launch instruction, with no hook. Refused: a model instruction is not reliable, and the point of a function hook is that the boundary is banked whether or not the model remembers.
- The Jev judge decides whether the turn is at a durable boundary. Refused: Jev is shadow-only by contract on main, where a branch reading a Jev answer is a checked defect; promoting a question to live is Plan B's opening section.
- The marker is written on every streaming step rather than at turn end. Refused: a marker written mid-turn declares a boundary the gate would honor mid-turn, which is the wrong moment; the boundary is a property of a turn that ended.
- The live working directory is read from the turn event's `cwd`. Refused: the turn-start and turn-complete events carry no `cwd`; only the session-start and classic-hook payloads do. `$.session.cwd()` is the per-turn source, with the `CwdChanged` hook as the fallback where that call proves not to track the worktree.
- Completion is read from the plan's feature branch with a git command. Refused: the stale-copy problem is the frozen working-directory field, not the absence of a branch read; reading from the live directory reads the worktree copy, which already carries the branch's Complete, and a git read adds a subprocess and a branch-discovery decision for nothing.
- `sess.workdir` is repointed to the live directory. Refused: it is the launch anchor the persona store and the workdir files resolve against, so repointing it would move the store lookup; the live directory is read per call and `sess.workdir` is left alone.

Rulings the operator made after the design conversation, each on 2026-09-24 via the ASSISTANT relay thread: this is Plan A, no Jev, carrying the boundary command at turn end, the plan-document naming in nudges, the completion read, and the launch instruction; Plan B carries the per-turn goal records and the Jev promotion. The kit boundary section ships before this plan.

Ruling 2026-09-25, ARCHITECT on the operator's delegation: the durability decision stays a property of the turn that ended, and the bank is performed at the start of the persona's own next turn, because the kit's moment rule lapses a declared marker on the next inbound line, and every persona turn opens with one. A turn-end bank can therefore never be honored. The refused alternative on per-streaming-step banking stands, since the bank still happens once per durable end, at the earliest moment the gate can honor it.

Applied 2026-09-25 under that ruling's own fallback clause: the bank is taken at the first main-loop tool call of the persona's next turn rather than at its `turn.start`. A live probe showed `turn.start` can fire before the turn's opening prompt line reaches the transcript file, so a marker recorded there lapsed on its own turn's opening line in one of three samples. At the first tool call the opening line was on disk in five of five samples. Every persona turn end recomputes the owed bank, so a turn with no tool call never carries a stale one into a later turn.

Provenance: distilled from the ASSISTANT brief `personas-run-unleashed-compaction-and-sequencing.md`, its "Addendum: the mechanism" section, and the ARCHITECT design session of 2026-09-24, across two plan-review rounds that found the frozen working-directory field and the empty turn-event `cwd`.

## Approach

**The live working directory comes from `$.session.cwd()`, not from a stored field or a turn event.** `sess.workdir` is captured once at session start, where it is the launch checkout, and never refreshed; it is the anchor the persona store and the workdir files resolve against, so it stays as it is. No turn event carries a `cwd`. So the completion read obtains the directory from `await $.session.cwd()`, which returns the session's current directory, the worktree once the persona has moved there. The compaction marker needs no directory: the merged kit boundary section keys it by session id under `~/.kit`. The plugin already calls it as the session-start fallback, and the engine's `CwdChanged` hook confirms the directory is tracked live; that hook is the fallback if `$.session.cwd()` is ever shown not to follow the worktree.

**Completion and Chapter progress are read from the live directory.** The plan-record reader joins the plan path onto the directory it is handed. Handed the launch checkout, it reads a copy that never gains a Chapter or a Complete status until the feature branch merges, which is the loop the Goal names. Handed the live directory, it reads the worktree copy the persona is writing, so a Chapter banked and a Status flipped to Complete are seen the same turn. This is also the Chapter-advance signal the durable-boundary check needs.

**The compaction mechanism is a fixed-rule check at a true turn boundary, calling the kit's own command.** The plugin adds one step to its `turn.complete` handler. `turn.complete` fires for a background subagent's completion too, while the persona's own turn is still open, so the step first checks that this is the persona's own turn ending: the completing id equals `currentGateTurnId`, and after this completion's own `openTurns` delete `turnIsOpen()` is false. This is the same true-boundary guard the inbox-drain plan uses, and a completion arriving inside an open turn or for a turn this session never saw start banks nothing. Then, gated on the plugin being the owner and the turn not skipped, when the turn ended at a durable boundary it records a bank as owed, and otherwise clears it. The owed bank is run as the kit's checkpoint command with the boundary verb, as a child process, at the first tool call the model makes in the persona's next turn (the Intent's dated ruling says why). A durable boundary is decided from fixed signals, never from Jev: whether the turn ended on a `BLOCKED:` or `WAITING:` lead, read with `readStatusLine(e.answer)`, whose `working` state is no lead, on every turn whatever the active entry's kind rather than from the plan-holder-only `turnLeaf.lead`; whether the plan-record read advanced the Chapter count this turn; and whether an active goal remains mid-work. The active entry's plan holder is resolved with `planHolderOf(sess.state, turnLeaf)`, so a task a worker added under a plan node has that plan as its holder. An entry is mid-work when it has a plan holder and the turn advanced no Chapter for it; a turn whose entry has no plan holder, or where `turnLeaf` is null so no entry was active, is durable when it ended on no lead. "No active goal" is `turnLeaf` null, never `noActiveRoot`, which stays true while a live plan sits under a complete root. The turn's kind is recorded in the decision and does not enter the predicate. A turn that was skipped, ended on a lead, or left a plan holder mid-section with no Chapter advance banks nothing.

**The command is located in code, and run with the live directory and the session id.** The plugin has no path to the kit CLI today; the scout found that CLI-path resolution is punted to the model through the peer-sessions skill's prose. So the plugin reads `~/.claude/plugins/installed_plugins.json`, whose `plugins["claude-kit@applefeld"]` is an array of install records each carrying an `installPath` and a `lastUpdated`; it takes the record with the greatest `lastUpdated` and runs `node <installPath>/hooks/kit-compact-checkpoint.js boundary` through `$.process.run`, passing `{ CLAUDE_CODE_SESSION_ID: <the session id> }` as the child's `env`, which the host sets over its own environment rather than replacing it, and no `cwd`. The merged kit boundary section (interactive-only PR 123) keys the marker by session id under `~/.kit` and locates the transcript by session id, so the command records a usable position from a linked worktree and the gate reads the marker wherever the session works. The caller needs no directory for the marker; that boundary section is the kit precondition named in Dispatch Authorization, now merged.

**The kit is the only decider of compaction, and its own boundary-verb gate does not apply to the plugin's call.** The plugin writes nothing the gate parses on its own; it runs the command, and the kit's gate keeps its ceiling and its single-shot honoring of the marker. The kit's peer-sessions skill states three questions that license a seat to run the boundary verb by hand (worktree edits none or handed off, decisions on disk, owed messages sent). The operator approved the plugin-in-code mechanism; that its fixed rule replaces the skill's three questions for a persona is this plan's reading of that approval, recorded here. It follows the one-owner ruling: two deciders on one moment is the drift that set the leash against the goal tree.

**Naming the plan document in the nudge and the goal block extends injected strings without changing call sites.** The idle nudge text (two arms) and the `[GOAL TREE]` block are strings the plugin submits, pinned by the injection ledger and a duplicate test that pins the template-literal chain shape. The plan path and section are added as a declared whole-variable piece inside the existing chains, beside the block's existing `siblingLine` and `lastNote` pieces, so the chain shape holds and the new copy is sized by the ledger; a bare interpolation would be stripped and never sized. The section printed is the plan holder's Chapter count plus one, printed as `Section N`, since no stored field names a section.

**The launch instruction and the docs.** The coordinator role instruction tells the model to run the boundary command by hand at each turn end; the banking step makes the plugin do it, so that instruction, the injection-ledger baseline that sizes it, and the repository docs that describe it are all updated.

**Line references.** The line numbers in the sections are as of the branch head `4e1a96f`, this plan's own base. Because nudge-state, the self-review flood and the inbox drain all merge before this plan and all touch `hooks/index.ts`, the executing session re-locates each site by its described role rather than by the number. The scout that first mapped these surfaces ran against `b9e6e28`, one merge behind the base, which moved the supervisor priming into `bin/supervise-holder.sh`; the Files-in-scope lists below are re-anchored to the base.

## Standing Brief Amendments

- The plan record is read under the live working directory alone. A directory or document unreadable there logs its reason once and leaves the plan not complete, and no second read is made under `sess.workdir`, which stays the anchor for the persona store (Section 1 acceptance bullets 1 and 3; the Intent's "read completion and Chapter progress from the live directory").
- The boundary command runs in the persona's own next turn, at that turn's first main-loop tool call, before the tool runs, not at the end of the turn it describes. Every `turn.complete` of the persona's own turn recomputes an in-memory pending-bank flag: it is set when that turn passes the durable predicate and cleared otherwise. The first `tool.call` the model makes on the main loop (no `agentId`, and `next.origin.plugin` is `"engine"`) while the flag is set runs the command and clears the flag whatever the exit. A subagent's tool call, a tool call a plugin raises through `$.tool.call` (the plugin's own reply backfill at turn end among them), and a tool call with no flag set bank nothing. The flag is not persisted, since a restart is a new session id (ARCHITECT ruling 2026-09-25 and its fallback, recorded in the Intent).

## Sections of Work

### 1. Completion and Chapter progress are read from the live working directory
Model: opus
Pass `await $.session.cwd()`, not `sess.workdir`, to the plan-record read at the tick's caller, so completion and the Chapter count are read from the worktree copy the persona writes. Confirm, with evidence, that `$.session.cwd()` returns the worktree for a persona working in one; where it returns the launch checkout instead, track the directory from the `CwdChanged` hook and use that. The reader itself is unchanged; only the directory it is handed changes. Keep every read failure a returned reason rather than a throw, as the reader already does. `sess.workdir` is not repointed.
Acceptance:
- The tick's plan-record caller is handed the live working directory rather than `sess.workdir`, and `sess.workdir` is unchanged.
- A plan whose Status is Complete under the live directory, and not under the launch anchor, reads as complete, so the controller auto-completes the goal node; a Chapter banked under the live directory is seen as a Chapter-count advance the same turn.
- A read failure logs a reason and leaves the plan not complete rather than throwing.
- `.kit/controller-tick-test.mjs` gains a case where the Complete document sits only under the live directory and not under the launch checkout, and the goal node auto-completes; the suite runs green on its own exit code.
Files in scope: `hooks/index.ts` (the `readPlanRecord` caller at ~7078 to 7080), `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs` (only if the harness must be taught to answer `$.session.cwd()` per case).
Tests: lock that the caller reads the live directory, that a Complete document present only under the live directory auto-completes the node, and that a read failure degrades to not-complete rather than a throw. The case must exercise the caller, not `readPlanRecord` alone, which already takes a directory parameter and would pass green without the caller change.

### 2. The plugin banks a boundary at turn end under fixed rules
Model: opus
The `turn.complete` handler gains a step. It runs only at a true turn boundary: the completing `e.turnId` equals `currentGateTurnId` and, after this completion's own `openTurns` delete, `turnIsOpen()` is false, so a subagent's completion or a completion for an unseen turn banks nothing. The step runs after the handler's plan-record read, so the Chapter-advance signal is settled, and is further gated on the plugin being the owner and the turn not skipped. When the turn ended at a durable boundary it sets an in-memory pending-bank flag, and otherwise clears it. The first main-loop tool call the model makes in the persona's next turn runs the kit boundary command when that flag is set, before the tool runs, and clears the flag whatever the exit. A call a plugin raises through `$.tool.call` is not the model's and takes nothing. The completion reads its boundary facts at its own `openTurns` delete. Where a newer turn has started before the recompute runs, the recompute clears the flag rather than set it, so the bank never lands after that turn's work has begun. The kit's moment rule lapses a declared marker on the inbound line that opens every turn, so a bank made at turn end is never honored (ARCHITECT ruling 2026-09-25, in the Intent). The predicate is the fixed rule in the Approach: `readStatusLine(e.answer)` returned no `blocked` or `waiting` state, and either `turnLeaf` is null, or its `planHolderOf(sess.state, turnLeaf)` is undefined, or that holder advanced a Chapter this turn; a holder that advanced no Chapter is mid-work and not durable. The command is located by reading `installed_plugins.json`, taking the record with the greatest `lastUpdated` under `plugins["claude-kit@applefeld"]`, and run via `$.process.run(["node", "<installPath>/hooks/kit-compact-checkpoint.js", "boundary"], { env: { CLAUDE_CODE_SESSION_ID: sess.mySessionId }, timeoutMs })`, with no `cwd`: the merged kit boundary section (interactive-only PR 123) keys the marker by session id under `~/.kit` and the gate reads it wherever the session works. The call is best-effort: a non-zero exit or a throw logs one decision and never fails the turn. The decision carries the exit code and the child's first stderr line, so an unpositioned marker, which the command still exits zero on, is legible in the log rather than reading as a clean bank. `sess` is the session-state object the handler already reads (`sess.isOwner` gates the existing untracked-work push).
Acceptance:
- After the persona's own durable turn boundary, the boundary command runs once, at the first main-loop tool call of the persona's next turn, with `env` carrying `CLAUDE_CODE_SESSION_ID` equal to the session id and no `cwd`, and a decision records the run, its exit code and its first stderr line. The pending flag clears whatever the exit, and a persona turn end that is not durable clears it too.
- A subagent completion arriving inside the persona's open turn, and a completion for a turn this session never saw start, neither set nor clear the pending flag. A subagent's tool call, a tool call a plugin raises through `$.tool.call` (the reply backfill at turn end among them), and a tool call with no pending flag bank nothing. A completion whose recompute runs after a newer turn has started clears the flag.
- The turn's opening inbound line is shown live to be in the transcript before the bank records its position, so the marker holds for that turn's own work. `turn.start` was shown to race that write, so the bank runs at the turn's first main-loop tool call, and the Chapter carries the probe readings.
- On a turn that was skipped, ended on a lead whatever the active entry's kind, or left a plan holder mid-section with no Chapter advance, the command does not run.
- The install path is the greatest-`lastUpdated` record's `installPath`; a missing key, an empty array, or an absent file logs one decision and skips the run rather than throwing.
- `.kit/controller-tick-test.mjs` runs green on its own exit code, with the process stub overridden so the assertion is real (see Tests).
Files in scope: `hooks/index.ts` (the `turn.complete` handler at ~6570, its `openTurns`/`currentGateTurnId` boundary check, and the `readStatusLine`, `planHolderOf` and plan-record read it already computes), `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs`.
Tests: lock both directions of the durable-boundary gate: a delivered no-goal turn, a task-entry turn, and a Chapter-banking turn each bank exactly once; a skipped turn, a lead-ended turn including a task-entry turn that ended on `BLOCKED:`, and a plan-holder-mid-section turn each bank nothing. Override the harness's `process.run` stub so the test records the child argv and env and asserts them on the banking cases and an empty record on the others, since the default stub returns a fixed exit code and would go green whether or not the bank ran. A silent bank on a mid-work turn is the expensive failure, because it licenses compaction mid-section. The bank lands at the next persona turn's first main-loop tool call, so lock that side too. A durable end followed by a main-loop tool call banks once, and a second tool call banks nothing more. A subagent's tool call banks nothing, and leaves the flag for the main loop's call. A tool call with no pending flag banks nothing. A durable end followed by a turn with no tool call and a non-durable end leaves nothing owed.

### 3. The nudge and the goal block name the plan document
Model: sonnet
Add the active plan document's path and section to the idle nudge text (both arms) and the `[GOAL TREE]` block, taking the path and Chapter count from the active entry's plan holder via `planHolderOf(sess.state, turnLeaf)`, so a task under a plan node names that plan's document; the section is the holder's Chapter count plus one, printed as `Section N`. Add it as a declared whole-variable piece inside the existing template-literal chains, beside the block's `siblingLine` and `lastNote`, so the chain-shape pins hold and the ledger sizes the new copy. Add the new piece's name to the ledger extractors' declared-name lists, and regenerate the ledger. When the active entry has no plan holder, the piece is empty so the nudge and block omit the plan line.
Acceptance:
- Both nudge arms name the plan holder's document path and section when the active entry has a plan holder, including a task under a plan node, and omit them for an entry with no plan holder.
- The `[GOAL TREE]` block names the plan document path and section, with its template-literal chain shape preserved.
- The injection ledger regenerates clean and `.kit/injection-duplicate-test.mjs` passes on its own exit code.
- `.kit/controller-tick-test.mjs`'s `[GOAL TREE]` and `[GOAL]` assertions pass.
Files in scope: `hooks/index.ts` (the nudge text at ~6234, the `[GOAL TREE]` block at ~9112), `.kit/injection-ledger.mjs`, `.kit/injection-ledger.json`, `.kit/injection-duplicate-test.mjs`, `.kit/controller-tick-test.mjs`.
Tests: lock that the plan path and section are carried when a plan is active and omitted for a task entry, in both the nudge and the block.

### 4. The launch instruction and the docs stop describing a hand-run boundary
Model: sonnet
Locus: inline
The coordinator role instruction in `bin/supervise-holder.sh` tells the model to run the boundary command by hand at each turn end. Now that the plugin banks the boundary for every owner session (section 2), remove that clause and state that the plugin banks the boundary. That instruction's size is recorded in the injection-ledger baseline, so regenerate it. Update the repository docs that describe the hand-run boundary so they match. This section is dispatched after section 2, so the instruction is removed only once the plugin banks reliably. It waits on the interactive-only plan's removal of the self-arm text from the executing-work skill: the executing session confirms that removal has landed before editing the priming, so no instruction is left pointing at a mechanism the other plan removed.
Acceptance:
- The coordinator role instruction no longer tells the model to run the boundary command by hand, and states the plugin banks it.
- The injection-ledger baseline is regenerated and `.kit/injection-duplicate-test.mjs` passes on its own exit code.
- `README.md` (the launch narrative and the no-context-reading paragraph) and `docs/architecture.md` no longer say the coordinator instruction has the child bank the boundary by hand.
- `.kit/channel-reply-instruction-test.sh` passes on its own exit code, updated where it pins the removed control string.
Files in scope: `bin/supervise-holder.sh` (the coordinator role instruction at ~253 and its comment at ~285), `.kit/injection-ledger.json`, `.kit/injection-duplicate-test.mjs`, `README.md` (the launch narrative at ~344 and the no-context-reading paragraph at ~731), `docs/architecture.md` (~16), `.kit/channel-reply-instruction-test.sh`, `docs/security-model.md` (the takeover-file list, carried from section 2's security review).

## Out of Scope
- Any call to the Jev judge, and any promotion of a shadow question to live. That is Plan B.
- Per-turn goal records beside the goals array, and the retirement of the untracked-work counter. That is Plan B.
- Making the kit boundary command record a transcript position from a worktree. That is the kit fix DEV-PLUGIN owns in the interactive-only plan's boundary section.
- Any change to the kit gate's ceiling or its marker-honoring logic.
- Repointing `sess.workdir`. It stays the launch anchor; the live directory is read from `$.session.cwd()` per call.

## Assumptions
- assumed 2026-09-24 (default): a `$.process.run` child inherits enough environment (PATH, a resolvable `node`) to run the kit command; reversal: if it does not, section 2 passes an explicit environment and its cost rises slightly.
- assumed 2026-09-24 (`git worktree list` and the ASSISTANT scout): personas run their plans in linked worktrees filed under the main checkout; reversal: if a persona ran in the main checkout, the worktree precondition would not bind and the plan could ship without waiting on the kit fix.
- assumed 2026-09-24 (`.claude/types/claude-code.d.ts:1521-1523`, and the plugin's own session-start fallback): `$.session.cwd()` returns the session's live directory, the worktree once the persona has moved there; reversal: if it returns the launch checkout, section 1 tracks the directory from the `CwdChanged` hook, which its confirmation step establishes.
- assumed 2026-09-24 (default): the executing implementer is a persona engineer familiar with the plugin's own vocabulary (the leash, the goal tree, the idle nudge and its two arms, the injection ledger, the Jev shadow seam, the kit marker's declare-and-lapse lifecycle) and resolves each term by reading the named code and the repo's README and architecture.md; reversal: a reader without that context needs those terms glossed, which the repo docs already carry.

## Operator Verification
- After the kit boundary section and this plan both merge and the supervisors relaunch, confirm on one persona that a turn ending at a Chapter boundary compacts before the context reaches the ceiling. What reopens the work: a persona running to the 800,000 token ceiling before compacting.

## Open Questions
- None open. Section 1's live-directory confirmation and section 2's process-stub test are settled in-code against the acceptance above.

## Related
- `claude-kit_kit-goal-interactive-only_spec_v1.md` (claude-kit repo): unleashes personas and adds the boundary section this plan depends on.
- `agent_persona_nudge-state_v1.md`: ships before this plan; replaces the nudge cap's pause with an ask.

## Chapters

### Chapter 1 - 2026-09-25
Completed: 1. Completion and Chapter progress are read from the live working directory
Implemented By: implementer-opus (one fix round on the same agent)
Metrics: review rounds 1, closed major-closed; provenance 5 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (1 refused, 0 declared, 0 asked); advisory: 0 findings, 0 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: section 1 open: changes the directory handed to the one readPlanRecord caller (turn.complete) from sess.workdir to the live directory from $.session.cwd(), falling back to sess.workdir when that call throws or returns no string; serves the Goal sentence "A plan whose Status was flipped to Complete in the persona's own worktree is recognized as complete" and section 1's first two acceptance bullets; adds no mechanism (the fallback keeps the existing value on a failed read, which the section's "a read failure logs a reason ... rather than throwing" bullet names); size about 10 lines of code plus one tick case; not building it leaves a worktree Complete unseen until merge, looping nudges.
  round 1, finding B1: a second plan-record read under sess.workdir when the live read is unreadable; serves the Goal sentence on a worktree Complete being recognized; adds a mechanism (a fallback read no clause names); about 8 lines and one case; design stop fired, scope-adjudicator ruled REFUSE on the form ground (bullets 1 and 3), not built.
  round 1, findings A2/A3: a failed $.session.cwd() stops falling back to a sess.workdir read and instead logs plan_record_unreadable once and reads nothing; serves Section 1 acceptance bullet 3 ("A read failure logs a reason and leaves the plan not complete"); adds no mechanism (removes the fallback read); about -3/+8 lines; not building it leaves a silent read of the stale launch copy.
  The Status header read `Ready` and now reads `In Progress`, set as this run started. The operator set the queue with this plan first on 2026-09-25, ahead of the inbox-drain plan the Approach expected to merge before it; nothing gates on that plan, so only the line references moved, and each site was re-located by role. The spec's "the tick's caller" is the single readPlanRecord caller, which sits in the turn.complete handler. The `$.session.cwd()` premise is confirmed live: a throwaway plugin (.kit/cwd-probe, gitignored) in a headless haiku session launched in D:\agent_persona, after a Bash cd into this worktree, recorded D:\agent_persona at session.start and inside tool.call, and D:\agent_persona\.claude\worktrees\boundary-compaction at turn.complete, the site this read runs at. So no CwdChanged hook was added. The plan's Assumptions bullet citing `.claude/types/claude-code.d.ts:1521-1523` for that premise points at the CommandInfo type; the session.cwd declarations sit at 6334 and 10082, and the probe above is the evidence.
Assumptions: none
Review Findings: review: adversarial + blind at fable, Agent tool (low). design stop: a second read under sess.workdir on an unreadable live read, ruling REFUSE by scope-adjudicator (form ground, Section 1 acceptance bullets 1 and 3), recorded in Standing Brief Amendments. Majors addressed: A1 the premise unproven (settled by the live probe above); A2 a failed cwd() silently read the launch copy and A3 a test pinning that silence (both fixed within the ruled form: a failed cwd() now logs plan_record_unreadable once and reads nothing); A4 a duplicated count pin (loosened to `.some`). B1, a removed worktree at turn end leaves a merged plan unread (trace orchestrator-made, Goal sentence on a worktree Complete): refused as above; whether the Goal promises that post-merge path is carried to the finishing pass's whole-changeset goal read. Majors justified: none. Minors: 0 fixed in the close pass, 0 upgraded, 4 left with the reason: the cwd guard now routes a non-string answer to the bullet-3 read-failure path; the Assumptions citation is corrected here rather than in the approval region; the pinned no-fallback case matches the ruling; the holder reference held across the new await carries the same exposure the existing fs awaits already carry, and no handler replaces sess.state mid-turn.complete short of a persona switch.
Stamps: adjudicated 18, stamped 8 (hook-modules-cannot-read-process-env, tick-harness-state-is-loaded-at-creation-drive-handlers-to-change-it, worktrees-under-the-checkout-resolve-the-main-node-modules, a-merged-plan-is-the-go-to-queue-it; operator: kit-goal-inert-when-session-cwd-is-not-project-root, a-bare-cd-in-the-bash-tool-repoints-every-later-dispatch, kit-compact-checkpoint-lapses-from-a-worktree, function-hooks-prototype-ships-behind-a-flag); the other 10 were reads by other sessions or did not steer this section. Two memories this section contradicted were corrected: the operator record kit-goal-inert-when-session-cwd-is-not-project-root (a Bash cd does move the session directory, and the boundary verb no longer depends on the directory) and the project record hook-modules-cannot-read-process-env (the plugin now reads $.env.get through hostOf).
Gate: targeted lane at SCOTT-CLAUDE, 2026-09-25T12:50Z, worktree boundary-compaction-build at ddd2d9c plus the fix round unstaged, no foreign runner on the box: `npx tsc --noEmit` exit 0; `node .kit/controller-tick-test.mjs` 3713 OK / 0 fail, exit 0 (baseline at 23c841d on this lane: 3666 OK / 0 fail, exit 0); `node .kit/injection-duplicate-test.mjs` exit 0 (baseline 0); `node .kit/check-loader-rule.mjs` exit 0 (no baseline recorded on it). Four lanes 110 s wall. Test delta: 47 checks added in three cases in .kit/controller-tick-test.mjs, 0 retired, 0 edited to stay green; the live-directory Complete case pins acceptance bullet 2 (Complete read under the live directory completes the node), the Chapter-advance case pins bullet 2's same-turn Chapter read, and the read-failure case pins bullet 3 (two unreadable documents and four cwd failures log a reason and leave the node active). None spawns a process. Contention: none. `.kit/plan-record-unit-test.mjs` was not in this lane; at baseline it prints PASS with 0 failures and exits 127 on a libuv teardown assertion, which this plan does not touch.
Next: 2. The plugin banks a boundary at turn end under fixed rules
Commit Model: Branch-and-PR
Delta: SCOTT-CLAUDE, 2026-09-25T12:51Z, no contention.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 1 - 2026-09-25
In-flight: section 2 is implemented and first-green committed (29f8908, pushed). Its review round 1 returned at 13:38Z with four lenses at fable (adversarial, blind, security, performance). The fix round is held on one finding. The blind lens's Major, orchestrator-traced to the Goal sentence on compacting at a durable point, says the boundary verb writes a declared marker, and the kit gate lapses a declared marker once an inbound line lands after its recorded position. Every persona turn opens with such a line, so a turn-end bank never reaches a compaction offer. This is confirmed against the installed kit (kit-compact-lib.js markerMomentHolds, and the section comment above it naming the turn-end case), and against .kit/compact-gate.jsonl, where a declared marker was honored only inside the turn that declared it. The spec's mechanism therefore contradicts its Goal.
Live dispatches: none. Pending: the expert ask to ARCHITECT (options A, bank at the next turn's start once the prompt line has landed; B, a kit verb writing the undeclared, age-bound marker seat-stop writes; C, the plugin writes a coordinator registry entry; lean A). If it goes unanswered by the time section 2's other review work is ready, a consult runs, then the question goes to the operator.
Adjudicated, held with the ruling: A1, an exact env key-set pin, is a spec-traceable Major for the fix round. Close pass: skip a malformed install record and keep scanning; a pending-session-id case; a plan_record_unreadable assertion in the unreadable case; reason "answer" in place of "completed"; a comment noting the child inherits the session's directory. To section 4: name installed_plugins.json in docs/security-model.md's takeover-file list. To the finishing goal read: a plan closed by goal_done reads mid-section, a no-goal turn that added a plan mid-turn banks, and a turn ending while a background subagent's turn is open never banks.
Gate baseline: targeted lane at 29f8908 (SCOTT-CLAUDE, 2026-09-25T13:17Z, clean worktree, no foreign runner): tsc 0; controller-tick-test 3829 OK / 0 fail, exit 0; injection-duplicate-test 0; check-loader-rule 0; 120 s wall.
Inbound plan held: the coordinator, relaying the operator through the Architect, asked to queue docs/plans/agent_persona_channel-log-retention_spec_v1.md at the back of the queue, pending and not resumed by hand. Its pull request 103 is open and the file exists only on plans/channel-log-retention, so the goal entry waits on that merge. goal_add with kind plan needs a turn the coordinator or operator started, which the reply to the coordinator names.
Next: on the ruling, re-brief section 2's fix round (the bank moment plus A1), then the close pass, the close gate and Chapter 2.

### Chapter 2 - 2026-09-25
Completed: 2. The plugin banks a boundary at turn end under fixed rules
Implemented By: implementer-opus (first build plus four fix rounds on the same agent); close-pass edits by the main session
Metrics: review rounds 5, closed major-closed; provenance 3 spec-traceable, 5 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, 0 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 0; consults 0 (one expert ask to the ARCHITECT, which ruled)
Decisions / Surprises: section 2 open: adds one step at the end of the turn.complete handler that, at a true turn boundary on an owner, unskipped, durable turn, reads installed_plugins.json for the kit install path and runs `node <installPath>/hooks/kit-compact-checkpoint.js boundary` with CLAUDE_CODE_SESSION_ID in env and no cwd, logging one decision per attempt or skip; serves the Goal sentences "The plugin ... decides that point in code at the end of each turn" and "runs the kit's checkpoint boundary command when the turn ended at a durable boundary and does nothing when the turn stopped mid-work", and section 2's five acceptance bullets; adds a mechanism the section names (the bank step, the install-path lookup, the skip decisions are each a named clause: bullets 1, 3 and 4); size about 60 to 90 lines of code plus about 12 tick cases; not building it leaves an unleashed persona running to the 800,000-token ceiling before compacting.
  round 1, finding B1: moves the bank from turn.complete to the persona's next own turn.start, carried by an in-memory pending-bank flag set when a turn.complete passes the durable predicate and cleared at that turn.start whatever the exit; serves the Goal sentence "an unleashed persona compacts at a durable point in its work rather than running to the kit's 800,000 token safety ceiling" and section 2's amended acceptance bullets 1 and 2 (ARCHITECT ruling 2026-09-25, Standing Brief Amendments); adds no mechanism no clause names (the flag and the turn.start bank are named by the amended section text and the ruling); size about 20 to 30 lines plus the turn.start cases; not building it leaves every banked marker lapsed on the next turn's inbound line, so personas still run to the ceiling.
  round 1, finding A1: loosens the exact env key-set pin in bank2CheckBanked to an assertion that env.CLAUDE_CODE_SESSION_ID equals the session id; serves section 2 acceptance bullet 1 ("env carrying CLAUDE_CODE_SESSION_ID"); adds no mechanism; one line; not doing it pins a choice the plan's Assumptions expects to change.
  round 2, finding R1: moves the owed bank's take from turn.start to the persona turn's first main-loop tool.call, and makes every gate-turn completion recompute the flag (set on a durable end, cleared otherwise); serves section 2 acceptance bullets 1 to 3 as amended and the Goal sentence on compacting at a durable point; adds no mechanism no clause names (the tool-boundary take is the ruling's own fallback, recorded in the Intent and the Standing Brief Amendments); about 15 to 25 lines plus re-pointed cases; not building it leaves the bank racing the transcript write, lapsing on its own turn's opening line in one of three live samples.
  round 3, finding R3-1: gates the owed bank's take on next.origin.plugin === "engine", so a plugin-raised $.tool.call (the reply backfill at turn end) neither takes nor clears it; serves amended acceptance bullet 1 ("the first main-loop tool call of the persona's next turn") and the Intent ruling; adds no mechanism no clause names (narrows the existing take to the call the amendment names); about 3 lines plus two cases; not building it lets a channel turn with no reply spend the owed bank at its own end, where the kit lapses it.
  round 3, finding R3-3: snapshots turnIsOpen() right after this completion's openTurns delete and uses it in the recompute; serves section 2's "after this completion's own openTurns delete" clause; adds no mechanism; 2 lines; not doing it can clear a durable end's bank when a next turn.start lands during the handler's awaits.
  round 4, findings R4-1/R4-2: captures completesGateTurn, turnKindAtStart and the turn-start count in the synchronous block at this completion's openTurns delete, and makes the recompute clear the owed bank when a newer turn started before it runs; serves the amendment's "every turn.complete of the persona's own turn recomputes" and "first main-loop tool call, before the tool runs" and the Intent's refusal of a mid-turn marker; adds no mechanism (reads the existing monitor turn counter; the clear is the recompute's existing not-durable branch); about 6 lines and a re-pointed case plus one new case; not doing it lets an interleaved turn start leave a stale bank or bank mid-turn.
  The spec's mechanism contradicted its Goal. The kit's boundary verb writes a declared marker, and the kit gate lapses a declared marker on the first inbound transcript line after it, which opens every persona turn. So a bank at turn end is never honored. The expert ask went to the ARCHITECT, who ruled on the operator's delegation to bank at the next turn's start, with a fallback to the first tool boundary. Live probes then settled four facts. First, turn.start can fire before the turn's opening line reaches the file: a marker recorded there lapsed on its own opening line in one of three samples. So the ruling's fallback was used: the bank is taken at the first tool call the model makes in the next turn, which held in five of five samples. Second, a plugin's own $.tool.call re-enters its tool.call hook with its own name as next.origin and no agentId, while the model's call carries origin engine. Third, a message queued mid-turn lapses the marker, which is correct. Fourth, the engine awaits the whole turn.complete chain before it starts the next turn, for a plugin submit and a waiting operator message alike (one sample on this build). Each ruling and fallback is recorded in the Intent and the Standing Brief Amendments, and section 2's body, acceptance bullets and Tests line were amended to match, which is approval drift made deliberately. Round 4's fix uses a module-level monotonic turnStartSeq rather than monitor.turnCount, which resets at hooks/index.ts ~3825 and ~8132. A plan completed by its document counts as a durable end (declared). A non-owner session clears an owed bank without running it (declared). The implementer's concern about other unconditional per-turn resets after turn.complete's awaits (hooks/index.ts ~7109, ~7119) is retired with the fourth probe fact rather than filed: no turn starts during turn.complete.
Assumptions: none
Review Findings: review: round 1 adversarial + blind + security + performance at fable, Agent tool (low; medium for security and performance); rounds 2 to 5 adversarial at opus, Workflow (high). Majors addressed: A1 an exact env key-set pin (loosened); B1 the turn-end bank is never honored (trace orchestrator-made, the Goal sentence on compacting at a durable point; moved by the ARCHITECT ruling); R2 the turn.start bank races the transcript write (moved to the first model tool call); R3 the plugin's own reply backfill re-enters tool.call and could spend the bank at turn end (gated on next.origin engine); R4-1 and R4-2 boundary facts read after awaits (captured at the delete, with a clear when a newer turn started). Majors justified-not-fixed: R5-1 (a stale bank if the handler is entered after the next turn.start) and R5-2 (the clear-on-a-newer-turn rule drops banks on back-to-back turns), both resting on a turn starting mid-completion, which the fourth live probe shows the engine does not do. The round 4 clear stays as a guard that does not fire in normal running. Minors: 7 fixed in close passes (a malformed install record ends the scan; the child's inherited directory; "persona switch" worded as ownership lost; the unreadable-record assertion; the subagent case retitled; the helper label on a non-owing case; the sibling-call scope of "before the tool"), 0 upgraded, 9 left with the reason. Those 9: a goal_done-closed plan reads mid-section, a no-goal turn that added a plan banks, and a turn ending with a subagent turn open never banks (all carried to the finishing goal read); a pending-session-id case and a non-owner take are untested (the harness cannot demote mid-case, and the kit refuses a pending id loudly); the reason value "completed" stays as the suite-wide convention; a relative installPath (the engine writes absolute ones, and the writer is outside the threat model); the bank is awaited before next(e) (the probe measured the spawn at about 0.1 s against a 15 s bound); the skipped decision logs every durable turn on a machine with no kit (DECISIONS_MAX bounds it). Advisory: security and performance found no Critical or Major. The security note to name installed_plugins.json in docs/security-model.md's takeover-file list goes to section 4.
Stamps: adjudicated 5, stamped 3 (operator: bash-tool-heredoc-collapses-backslashes; project: prompt-submit-always-waits-for-idle-so-it-cannot-reach-a-running-turn, stream-json-feed-must-gate-on-result-lines-not-sleeps); the other 2 project and 3 operator reads came from other sessions or did not steer this section. Three project memories were written: turn-start-can-fire-before-its-prompt-line-is-on-disk, the-engine-awaits-the-turn-complete-chain-before-starting-the-next-turn and a-plugins-own-tool-call-re-enters-its-tool-call-hook.
Gate: targeted lane at SCOTT-CLAUDE, 2026-09-25T15:13Z, worktree boundary-compaction-build at 3c525f9 plus the section's final close-pass edits unstaged, no foreign runner on the box: `npx tsc --noEmit` exit 0; `node .kit/controller-tick-test.mjs` 4250 OK / 0 fail, exit 0 (baseline at 39bc06b on this lane: 3713 OK / 0 fail, exit 0); `node .kit/injection-duplicate-test.mjs` exit 0 (baseline 0); `node .kit/check-loader-rule.mjs` exit 0. The four lanes took 117 s of wall clock (Chapter 1: 110 s). Test delta: 13 cases added in .kit/controller-tick-test.mjs (537 checks). Added durable-end cases pin acceptance bullet 1: no-goal, task-entry, Chapter-banking and plan-complete ends each bank once at the next model tool call, with argv, env and no cwd asserted. Added absence cases pin bullets 2 and 3: skipped, lead-ended and mid-section ends; a subagent completion; an unseen turn; overlapping turns; a turn start alone; a subagent or plugin-raised call; late-settling completions. Added install-record cases pin bullet 4: the greatest lastUpdated wins past malformed records; absent file, missing key, empty array, unparseable JSON and no usable record each skip with one decision. Added failure cases pin bullet 1's best-effort clause: a non-zero exit and a rejected run log one decision and clear the flag. 0 tests retired. 0 tests edited to stay green (three untracked-work tail checks were reshaped in the first build and restored byte-for-byte in fix round 1). 0 added tests spawn a process, because every case stubs $.process.run. Contention: none. Acceptance bullet 3 rests on the live probes under .kit/scratch/boundary-compaction/tsprobe*: a turn.start bank lapsed in 1 of 3; a first-model-call bank held in 5 of 5 with the opening line at or before the recorded position; a mid-turn queued message lapsed it as it should. The kit gate's own allow line needs a live persona near the context ceiling, so it stays under Operator Verification.
Next: 3. The nudge and the goal block name the plan document
Commit Model: Branch-and-PR
Delta: SCOTT-CLAUDE, 2026-09-25T15:22Z, no contention.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 3 - 2026-09-25
Completed: 3. The nudge and the goal block name the plan document
Implemented By: implementer-sonnet (first build); the round 1 fixes and the close pass by the main session
Metrics: review rounds 1, closed major-closed; provenance 3 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, 0 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: section 3 open: adds one module-level builder that returns "Plan document: <planPath>, Section <chapterCount+1>" for an entry whose planHolderOf holder carries a planPath and an empty string otherwise, splices its result by name into both idle-nudge arms and the [GOAL TREE] block, sizes the builder's literal once as its own injection-ledger entry, and regenerates the ledger; serves the Goal sentence "The idle nudge and the injected goal block name the plan document the persona is working, so a nudged persona re-reads the right document" and section 3's four acceptance bullets; adds no mechanism no clause names (the declared whole-variable piece and the Section N rule are the section's own text; the single builder is how one copy is sized rather than two); size about 15 lines of code, 1 ledger extractor and about 8 tick cases; not building it leaves a nudged persona told only the objective, so it can re-read a stale or wrong document.
  round 1, finding adversarial M1: drops the sentence "Re-read it before the next step." from planDocumentLine's literal, leaving "Plan document: <path>, Section N."; serves section 3 acceptance bullets 1 and 2 ("name the plan holder's document path and section") and the Goal sentence on naming the plan document; adds no mechanism (removes copy); one sentence, 33 bytes; not doing it tells a persona on every injected [GOAL TREE] block to re-read a 18 to 40 KB plan document, which spends the context this plan exists to conserve.
  round 1, finding adversarial M2: replaces the three exact-wording line checks with checks that the line directly after [GOAL] or Path: opens "Plan document: ", carries the plan path and carries the expected "Section N", and drops the duplicate plan-entry check in caseCount_bothNudgeTextsNameTheThreeLines; serves section 3's Tests line ("lock that the plan path and section are carried"); adds no mechanism; three assertions reshaped and one removed; not doing it leaves checks that go red on a copy edit while the acceptance still holds.
  The plan line is built by one helper and sized once as the PLAN_DOCUMENT_LINE ledger entry, so the nudge and the block splice `planLine` by name and the duplicate check does not read the same sentence twice. In the nudge, `planLine` is declared above `architectLine`, outside the ledger's fixed-order match on the declarations directly above `nudgeText`. The close pass added a re-test of the stored planPath against PLAN_PATH_PATTERN, the check agent-state.ts states every reader of that field owes, which also replaced a guard half that could never fire. The plan path's validation at goal_add already refuses a line break (hooks/agent-state.ts:383), so the re-test covers only a hand-edited or foreign-written store.
Assumptions: none
Review Findings: review: round 1 adversarial + blind at opus, Workflow (high). No security or performance lens: the section adds no input path (planPath is validated at write and now re-tested at read) and touches no per-tool-call, process, lock or store-query path. The blind reviewer read hooks/index.ts only, treating the `.kit/` files as barred by the brief's scratch-path line, so the extractor and test side had the adversarial lens alone. Majors addressed: adversarial M1, the plan line told the persona to re-read the whole document on every injected block (sentence dropped); adversarial M2, three checks pinned the line's exact wording (reshaped to path and section). Majors justified-not-fixed: blind M1 (orchestrator-made trace: section 3's "the section is the holder's Chapter count plus one"), the section number is wrong before a plan's first read and names a nonexistent section after its last Chapter. The formula is the plan's approved rule (Approach, "Naming the plan document..."), the pre-read state lasts one turn because that turn's end read sets the count, and with the re-read sentence gone the number is a label rather than an instruction. Minors: 3 fixed in the close pass (the planPath re-test against PLAN_PATH_PATTERN, raised by both lenses; the dead `!holder.planPath` half and its untrue comment, retired by that re-test, with the duplicate no-planPath case replaced by a stored planPath carrying a line break; a refusal control for the PLAN_DOCUMENT_LINE rule), 1 fixed in the round 1 fix (the duplicate plan-entry check), 0 upgraded, 3 left with the reason. Those 3: the Chapter-count formula (the same ground as blind M1); the `planLine` declarations are bound to planDocumentLine by name and not by shape (siblingLine, lastNote and leadHoldLine carry the same exposure, so this is the ledger's standing coverage bound); the nudge placement may leave the line unpinned (it is sized as its own entry, confirmed in .kit/injection-ledger.json). The close-pass delta took the author re-read.
Stamps: adjudicated 1, stamped 0; the one unstamped operator-tier read (subagent-can-report-a-documented-past-injection-as-a-live-one) came from another session 32 minutes before the close and did not steer this section.
Gate: targeted lane at SCOTT-CLAUDE, 2026-09-25T15:49Z, worktree boundary-compaction-build at 8cb8d8e plus the round 1 fix and the close pass unstaged, no foreign runner on the box: `npx tsc --noEmit` exit 0; `node .kit/controller-tick-test.mjs` 4268 OK / 0 fail, exit 0 (baseline at 74cf9e7 on this lane: 4250 OK / 0 fail, exit 0); `node .kit/injection-duplicate-test.mjs` exit 0 (baseline 0), its new control reading "refused by [chain-shape], naming PLAN_DOCUMENT_LINE"; `node .kit/check-loader-rule.mjs` exit 0. The four lanes took 115 s of wall clock (Chapter 2: 117 s). Test delta: two cases added in .kit/controller-tick-test.mjs. casePlanDoc3_goalTreeBlockNamesThePlanDocument pins bullet 2: the plan node and a task under it name the document and Section 3 directly after the Path: line, and a no-plan task entry and a stored planPath failing the pattern omit it. casePlanDoc3_nudgeArmsNameThePlanDocument pins bullet 1: both arms, for the plan node and a task under it, name the document and Section 3 directly after the [GOAL] line. One check was added to caseCount_bothNudgeTextsNameTheThreeLines, pinning bullet 1's omit half on both arms for a task entry. One control was added to .kit/injection-duplicate-test.mjs, pinning bullet 3's refusal when the builder's shape moves. One check was retired as a duplicate: the plan-entry line check in caseCount_bothNudgeTextsNameTheThreeLines, which casePlanDoc3_nudgeArmsNameThePlanDocument covers. 0 tests edited to stay green. 0 added tests spawn a process. The pattern-fails case was watched red against the old guard: 1 FAIL, "omits the plan document line", suite exit 1, restored byte-identical by cmp. Contention: none.
Next: 4. The launch instruction and the docs stop describing a hand-run boundary
Commit Model: Branch-and-PR
Delta: SCOTT-CLAUDE, 2026-09-25T15:52Z, no contention.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```
