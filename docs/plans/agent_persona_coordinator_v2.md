# agent_persona: a Fable-tier coordinator directing many Sonnet workers across repos, v2

Status: In Progress
Commit Model: Branch-and-PR. `main` is protected on GitHub, PRs only. Every PR opens Draft and the worker marks it ready explicitly, in words, per v1's own convention.
Created: 2026-09-12

## Section 0. Close out v1 first, plus two live worker-liveness defects a fleet cannot ship with

`agent_persona_passive-supervisor_v1.md` is still Status: In Progress; every roadmap item (1-8) has a Chapter, everything is merged to `main` at `1911a83`, and this v2 spec exists at that same clean boundary. Before any v2 section starts:

1. **The goal-less-restart defect (Reviewer Round 105 R1, memory: `a-goal-less-worker-restarts-on-every-operator-message`).** The item 2 backstop (`hooks/index.ts:2748`) backfills a one-round root and marks it complete in the same tick for *any* owner turn that did real tool work with no active root - whatever the turn's origin, coordinator-delivered included. `bin/supervise.sh` reads that `root_complete` as the goal finishing and takes `RESTART_PASSIVE`, killing a live child that was never actually done with anything; live on the `aios` worker three times in one afternoon (16:04Z, 16:10Z, 16:25:35Z). Fix, chosen from the two named in Round 105: the supervisor's restart decision ignores a root whose `notes` carry the backfill's own marker string ("Backfilled: the worker did the work without calling goal_create this turn") - one added condition in the trigger read (`bin/supervise-decide.mjs`), no change to the backfill's own accounting, and the model-facing behavior is untouched.
2. **The stop-path defect (Reviewer Round 105 R2, same memory record).** A `RESTART_PASSIVE`/`stop_child` cycle can TERM the bash wrapper while `claude.exe` survives underneath it, still holding the persona claim; the 120-second pre-gate then times out and `supervise.sh` exits 2, leaving an orphaned child and no supervisor watching it. Fix: `stop_child` TERMs the whole process tree it launched (the `--rundir` child directory already records the PID lineage needed to find it), and the pre-gate re-polls past its current ceiling with a log line rather than exiting on a bare timeout.
3. Whole-gate live run of `.kit/live-all.sh` at the tip carrying fixes 1 and 2 (hooks changed since the last whole gate at `661759c`: `742015b` the channel-reply backstop, `adfffcc` the dedupe fix, plus this section's own two), pasted verbatim into a closing Chapter. Run only once the operator has confirmed the machine is otherwise quiet (every other live `claude` process stopped) - Section 5's extended lock does not exist yet at this point in the sequence, since it is itself v2 code the whole gate is validating; this section cannot depend on it.
4. Finishing reviews (adversarial + docs-curator) per the finishing-work skill.
5. Item 6's own unfinished half: the runtime switch from `D:\DeepSeekHarness\agentic-plugin` (`--plugin-dir`) to the installed marketplace plugin as this machine's one shared plugin copy - load-bearing for v2, since every worker and the coordinator must load the same plugin code for the commons store to mean the same thing everywhere (memory: `a-second-worker-launches-in-its-own-repo-with-a-unique-persona-name`). Acceptance includes a live check that the installed-mode store actually holds every currently-live persona claim (worker and coordinator alike) before any other section trusts it as the one shared store.
6. Status: Complete, archived per `curating-docs`.

**Not done as of this spec's authoring:** this machine currently runs 4+ live `claude` processes (this worker, the Reviewer, the `aios` worker, at minimum) at 345-597 MB each. The whole-gate run takes about 25 minutes and launches many more short-lived children that all contend for the one shared commons-store file. Running it blind against that contention risks exactly the collision failures v1's own Chapters (1, 2, 7) already hit from concurrent live sessions, and items 1-2 above are net-new fixes this spec has not yet built, let alone proven. This section is deliberately not executed inline in this round. Operating rule for when it does run (Reviewer Round 105 R4): stop every worker, run the gate, relaunch the fleet - with `dev` and `aios` running all day, the gate does not get a naturally quiet window on its own.

## Goal

Today, a "worker" is a `bin/supervise.sh` launch: one Sonnet-tier `claude -p` child, one repo, one persona, one Discord thread. The operator drives each one directly. v2 adds one Fable-tier "coordinator" persona that reads status across N workers in N repos, batches steers to them without blocking on their multi-minute turns, receives their findings and escalations, reports to the operator on its own thread, and - per the Decisions section's FORK B ruling - dispatches bounded work to a worker without a human round trip per message, within a checkable bound and a judgment call the operator has explicitly delegated. The mechanism is an extension of the existing commons-store inbox path (`agentic_say`/`agentic_inbox`), not a new transport. Discord stays the operator's channel; the coordinator does not read or post into a worker's thread.

## Approach

**Grounding.** This design was pressure-tested by a two-lens Fable-tier design council (Round 1, 2026-09-12) before being written down: an operability/reliability lens and an authority/safety lens, converged by a facilitator. Every claim about the plugin's own code below was independently confirmed against `hooks/index.ts`, `hooks/operator.ts`, `hooks/commons.ts`, and `bin/supervise.sh`, both by the council and, for the load-bearing ones, spot-checked again by this session before this line was written. The full council transcript is retained at `.kit/runs/v2-council-round1/` (copied out of the workflow's own transcript dir before archiving, per the memory note `a-run-dir-is-merge-evidence-until-the-merge-lands`).

**The rejected mechanisms, and why.** Two other transports exist on this machine and both were considered and rejected as the coordinator-to-worker channel:
- The harness's own peer tools (`ListAgents`/`SendMessage`): this plugin actively consumes `session.receive` events of kind `peer`/`peer-send-message` before the model ever sees them (`hooks/index.ts:694-712`), specifically so a peer message carries no steering authority. Using it as a coordinator channel means reversing that design decision and rebuilding deferred-status, urgent break-in, reply capture, and the reader-claim trust gate on a transport this plugin does not own. Its address is a live session id, which a `supervise.sh` relaunch mints fresh every time (Chapter 8.3's own proof: child-1, then child-2), while `persona:<name>` is the thing that actually stays stable across a restart.
- Each worker's own Discord thread: inbound authority there is the operator's own Discord account against a one-account allowlist; the relay's own instructions tell a worker to treat channel text as keyboard-equivalent operator steering. A coordinator posting there either launders its own text into operator authority with no way for the worker to tell the difference, or has no channel to post through at all, since the relay never tells one process which session serves another process's thread. Read access is closed the same way. Discord stays the operator's channel, full stop; the coordinator reports to the operator on its own thread, same as any worker does today.
- `DISCUSSION.md` (the append-only file the worker and Reviewer personas use today for handoffs): turn-serial, invisible to the plugin's own trust gate, and polled by hand. It works between two co-equal live sessions watching one repo; it does not scale to a coordinator directing N workers in N repos, none of which read it. v2 retires it as a steering path; the plan doc and the coordinator's own thread are the record going forward.

**The chosen mechanism: the commons-store inbox path, extended in four small places.** `agentic_say`/`agentic_inbox` already has everything this needs except cross-repo reach: a machine-checked writer gate (only a live reader-claim holder's record reaches the model, `hooks/index.ts:3736-3740`), a deferred-status view that doesn't block on a busy worker (`:3784-3808`), an urgent break-in that reaches a worker mid-turn through the `tool.call` hook's own `context` return (`:3830-3868`), reply capture, and a decision-log trail. Item 8.3 built these for the operator-to-worker direction; the coordinator reuses them rather than inventing a second version.

Four extensions, in order of what blocks the next:
1. **Cross-repo deferred status.** `heartbeatPath` is the literal string `".agentic-heartbeat.json"`, read relative to the process's own cwd (`hooks/index.ts:408, 425, 581`). A coordinator running in one repo can never see a worker in another repo as busy; it would read its own empty or irrelevant heartbeat file. Fix: publish `turnStartedAt` and `workdir` into the session's commons entry (`hooks/commons.ts:24-28`, keyed `commons:<sessionId>`, one entry per live session, already a machine-global store). Finding the right entry for a named persona is `commonsWinner(readAllClaims(...), "persona:<target>")` (`hooks/commons.ts`), the same lookup `shouldYieldCommons` already uses for arbitration - not a lookup this design invents.
2. **Addressing N workers.** `agentic_say`/`agentic_inbox` operate on `sess.persona` only. Reaching a specific worker today means an `agentic_identity` switch, which also swaps the coordinator's own in-memory state to that persona's store, losing its own. Fix: an optional `persona` argument on both tools, so the coordinator addresses a worker by name without switching identity.
3. **Coordinator-to-worker and worker-to-coordinator addressing, both authorized without a reader claim - because a reader claim cannot cover this shape at all.** `claimReaderRole` (`hooks/index.ts:1036, 1138, 3170`, also reached at session start on lost commons arbitration) is only ever reached through `agentic_identity` switching `sess.persona`, which also releases `persona:<previous>` (`hooks/index.ts:3112`) - so a session that owns `persona:coordinator` cannot also hold `reader:<worker>` for N workers without giving up its own persona each time it addresses one. (A *reader-only* coordinator, with no persona of its own, is the live counterexample - the Reviewer session holds `reader:dev`, `reader:reviewer`, and `reader:aios` simultaneously today, and this is a real, working alternative shape. It is rejected here because the coordinator needs its own supervisor lifecycle - restarts, a goal tree, a Discord thread of its own - which only an owned persona gets; a reader has none of that. So the choice is "the coordinator owns a persona, like a worker does" plus a new authorization rule for reaching *other* personas from that position, not an appeal to impossibility.) The new rule: the caller must hold `persona:<coordinatorPersona>` (the one configured coordinator name, `cfg.coordinatorPersona`, read beside `controllerTickMs` at `hooks/index.ts:648`) to address any *other* persona, **or** the target persona must equal `<coordinatorPersona>` (so any worker can push to the coordinator without holding any special claim at all - the coordinator's own name is the address, known config rather than a credential the worker must acquire). Both `agentic_say`'s owner-only denial (`hooks/index.ts:3731-3733`) and `agentic_inbox`'s matching owner-only denial (`:3768-3771`) are relaxed by exactly this rule: refused only when the target equals the caller's own persona (self-messaging stays blocked, for everyone, no exception); allowed for any other target when either side of the "or" holds. No new reader-claim path, no new tool.

   This alone is not sufficient for a record to actually reach a worker's model: `hasLiveReaderClaim` gates the send at `hooks/index.ts:3736-3740`, the delivery-tick filter at `:1336` (a record whose writer holds no live reader claim is marked `"skipped"` at `:1345` and never submitted), and the urgent path at `:3846` - three more places that read the *old* reader-claim shape and would silently swallow a coordinator's own record. All three gates gain the same second check this authorization rule uses: a live `persona:<coordinatorPersona>` claim on the record's `from` session satisfies each of them exactly as a reader claim would. Without this, Section 4's labels are computed for records that never arrive.
4. **Provenance label.** Every delivered record today is submitted to the model as a flat `[OPERATOR] <text>` or `[OPERATOR, urgent] <text>` (`hooks/index.ts:1372, 3860`), with no distinction between the real operator (who no longer even uses this path since Discord attaches directly) and any reader session, coordinator included. Fix: label a record `[COORDINATOR] <text>` when the writer's session holds `persona:<coordinatorPersona>` in commons, computed at delivery time from the writer's live claims (not stored on the record at send, so a claim that changed hands between send and delivery is read as it stands at delivery - the same freshness `hasLiveReaderClaim` already uses elsewhere), and reserve `[OPERATOR]` for records that actually originate from the operator's own Discord channel path (untouched by this section - it never goes through this delivery code). A non-coordinator reader (the Reviewer, the relay) gets a distinct `[READER:<persona>]` label rather than either. This is a prerequisite for the FORK B decision below, not optional plumbing: without it, a worker cannot apply any coordinator-specific rule to coordinator text, because it cannot tell coordinator text from anyone else's. Round 106 is the live argument for why this matters now rather than later: the item 8.3 urgent break-in delivered a Reviewer steer into a *subagent's* tool-call result labelled `[OPERATOR, urgent]`, the subagent (correctly, per doctrine) refused to act on unverifiable injected text, and the worker reported to the operator that something on the machine could inject fake authoritative-looking messages - which was true, and is exactly the provenance gap this label closes. Section 4 below adds the further fix Round 106 asks for: the urgent break-in must never fire inside a dispatched subagent's own tool call, only the top-level session's.

**Machine-budget contention.** Confirmed live via `Get-Process` at council time: 4+ `claude` processes at 345-597 MB each, plus their node children. A coordinator adds one more process pair; each worker adds one more. The mechanism choice does not change this count. Two contention findings, both grounded in the live store rather than guessed: `listInboxRecords` and `hasLiveReaderClaim` each poll the one shared JSON file every tick per owner plus on a 5-second throttle per tool call, so K workers plus one coordinator is K+1 pollers on one file; and the store shows two separate clusters of multiple full-file `.bak` rewrites within a third of a second of each other, consistent with concurrent processes each writing a full snapshot in quick succession (confirmed as contention; not confirmed as corruption - all copies parsed as valid JSON with matching key counts). Mitigations, detailed in Section 5: the coordinator's own launch sets its tick cadence well above the worker default (a launch-time setting, no new knob); the existing closed-record roll to the append-only log stays in place; and `.kit/live-all.sh` gains a new refuse-at-start check - the whole gate does not run at all while any real fleet is live, not merely waits for it, per Section 5.

**Operating rule that falls out of the turn-serial constraint.** A worker's own turn runs 2-5 minutes; a message delivered mid-turn queues until it ends (confirmed live in this plan's own Rounds 100-103: a message sent to this worker mid-turn queued exactly this way). So: steers to one worker batch into one record per cycle rather than trickling in one at a time; `urgent` is reserved for a real stop-or-redirect, never for "what is your status"; and status is never asked of a worker in-band - the coordinator reads `agentic_inbox`, the worker's own `.agentic-personas.json` in its repo, and the commons entry, all of which are file reads with no turn cost to the worker.

## Sections of Work

### 1. Commons entry carries turn state and workdir
Model: opus
`hooks/commons.ts`'s per-session commons entry gains `turnStartedAt: number | null` and `workdir: string`, written wherever the entry is written today (session start, turn start, turn complete, heartbeat tick) using the existing `writeOwnerHeartbeat`-style call sites as the pattern to mirror. No existing reader of the commons entry breaks: these are additive fields.
Files in scope: `hooks/commons.ts`, `hooks/index.ts` (every commons-entry write site).
Tests: a harness case that a commons entry written mid-turn (at the `turn.start` handler) carries a non-null `turnStartedAt` and the entry's own `workdir`, and a control that `turn.complete` (`hooks/index.ts:2674-2676`, the same site that already clears the heartbeat's own turn stamp) clears the commons entry's copy back to null too.

### 2. Deferred status reads the commons entry, not the local heartbeat file
Model: opus
`agentic_inbox`'s deferred-status check (`hooks/index.ts:3784-3808`) reads the target persona's commons entry (Section 1's new fields) instead of the cwd-relative `.agentic-heartbeat.json`, so a coordinator in one repo correctly reads a worker in another repo as busy or free.
Files in scope: `hooks/index.ts`.
Tests: a harness case with two simulated working directories/personas proving the deferred check reads the addressed persona's own commons state, not the caller's local heartbeat file; a control proving the same-repo case (today's only working case) still passes.

### 3. `persona` argument on `agentic_say` / `agentic_inbox`, authorized without a reader claim
Model: fable
Both tools take an optional `persona` argument; when given, the write/read targets that persona's inbox rather than `sess.persona`, with no identity switch. The owner-only denial on `agentic_say` (`hooks/index.ts:3731-3733`) and the matching owner-only denial on `agentic_inbox` (`hooks/index.ts:3768-3771`) are both relaxed by the same rule: denied only when the target persona equals the caller's own persona (self-messaging stays blocked, for everyone, no exception); allowed for any other target persona when either the caller holds `persona:<coordinatorPersona>` (coordinator addressing a worker) **or** the target equals `<coordinatorPersona>` (any worker escalating to the coordinator, no special claim needed - the coordinator's own name is the address). `cfg.coordinatorPersona` is read beside `controllerTickMs` (`hooks/index.ts:648`), checked live via `readAllClaims`/`commonsWinner` (`hooks/commons.ts`) the same way the plugin already checks any other claim. The three delivery-side gates that would otherwise silently swallow a coordinator's own record - `hasLiveReaderClaim` at the send (`:3736-3740`) and the matching `agentic_inbox` check (`:3773-3777`), the tick's delivery filter (`:1336`, marks `"skipped"` at `:1345`), and the urgent path (`:3846`) - each gain the same second check: a live `persona:<coordinatorPersona>` claim on the record's `from` session satisfies the gate exactly as a reader claim would. A plain worker's owner-only denial for its *own* persona is unchanged in every other respect. This is the section carrying the real judgment call (composing the existing owner/reader gates with a new coordinator-specific one across four call sites without opening self-messaging or letting any owner spam an unrelated persona's inbox) and is why it is fable-tier, not sonnet.
Files in scope: `hooks/index.ts` (tool registrations and handlers for both tools, the three delivery-side gates named above, and `cfg.coordinatorPersona`'s read site beside `controllerTickMs`), `hooks/operator.ts` (the write/read helpers both tools call), `README.md` (the Operator channel section, which today states both tools are reader-only and refused to the owner, and states the trust boundary as "a record whose writer holds a reader claim on the same persona" - `README.md:277-287, 318` - both need updating to state the coordinator exception).
Tests: at minimum, both directions of the self-message guard (a persona cannot message itself even via the new argument, any claim held or not), both directions of the coordinator-claim requirement (an owner with no `persona:<coordinatorPersona>` claim is refused a cross-persona call to an *unrelated* third persona; the coordinator succeeds), and both directions of the worker-to-coordinator push (a worker with no special claim reaches the coordinator by name; a worker cannot reach a *different* worker by the same path) - a silent bypass of any of these is the expensive failure here.

### 4. Provenance labels: `[COORDINATOR]`, `[READER:<persona>]`, `[OPERATOR]` reserved; the urgent break-in stops crossing into subagents
Model: opus
At both delivery sites (`hooks/index.ts:1372`, `:3860`) and the ask-answer delivery (`:1323`), the label is computed at delivery time (not stored on the record at send) from the writer's live commons claims: `[COORDINATOR]` when the writer holds `persona:<coordinatorPersona>` (the config key Section 3 defines); `[READER:<persona>]` for any other reader; `[OPERATOR]` reserved for the operator's own Discord-originated channel path, which does not go through this delivery code today and is untouched by this section. Uses `readAllClaims` (`hooks/commons.ts:154`), already used elsewhere for commons arbitration.

Round 106's live incident is folded in here rather than left as a separate gap: the item 8.3 urgent break-in (`hooks/index.ts:3830-3868`) fires on the `tool.call` hook, which runs for a dispatched subagent's own tool calls exactly as it does for the top-level session's - so a Reviewer's urgent steer was delivered as `[OPERATOR, urgent]` context inside a running `implementer-opus` subagent's `Edit` call, which correctly could not verify it and refused it. Fix: the urgent break-in only fires when the `tool.call` event can be confirmed to belong to the top-level session, not a subagent's own call; if the event carries no field distinguishing the two (checked against the real `ToolCallEvent` shape at implementation time, not assumed here), the break-in is disabled entirely for that call and the record falls back to ordinary tick-based delivery at the owner's next quiet moment - a safe default over a mislabelled one.

Files in scope: `hooks/index.ts` (the label computation and the urgent break-in's session-scope check); `.kit/controller-tick-test.mjs` (the existing assertion at `:672-673` that the general drain produces exactly one `[OPERATOR]`-prefixed prompt is updated to expect `[READER:<persona>]` for a plain reader, and the assertions at `:1528` and `:4563` that also key on the literal `[OPERATOR]` string); `.kit/live-restartrequest-test.sh` (its standing instruction at `:77-81` is keyed on the literal `[OPERATOR]` prefix and is updated to match whichever label that suite's own reader now carries, or the suite's reader is given the coordinator claim, whichever keeps its restart proof honest); `.kit/live-operator-test.sh` (its assertion at `:576-600` fails a run whose reader text lacks the literal `[OPERATOR] ` prefix and needs the same update); `README.md:312, 314, 318` (documents the `[OPERATOR]` marker and the reader-claim trust boundary; updated to describe all three labels and the coordinator exception).
Tests: one case per label (coordinator claim present -> `[COORDINATOR]`; a plain reader claim, no coordinator -> `[READER:<persona>]`); a control that the operator's own Discord-origin path still carries no prefix at all (that path only sets a flag, `hooks/index.ts:3882`; it was never prefixed, and this section leaves it that way); and, for the urgent-break-in fix, a case that a subagent's own tool call never receives break-in context (falls back to tick delivery) with a control that the top-level session's tool call still does.

### 5. Contention guards
Model: sonnet
No new knob: `controllerTickMs` is already a per-launch setting (`bin/supervise.sh:119` emits it from the `TICK_MS`/`controllerTickMs` env var into the child's settings, which `hooks/index.ts:648` reads and which beats the code's own 30000 default). The coordinator's launch recipe (Section 6) simply sets it higher at launch - 60000, six times `bin/supervise.sh`'s own 10000 default - rather than inventing a second, competing knob. `.kit/live-all.sh`'s running-lock is extended: today `wait_persona_free` (`bin/agentic-common.sh:137-169`) *waits*, polling for up to a timeout, for any live claim on the literal resource `persona:default` (`:164`) to clear - it does not refuse to start, and it only ever checks that one hardcoded resource. The fix is a genuine addition, not a widening of the existing wait: a new refuse-at-start check, beside the existing `.kit/RUNNING` lock-file check, that fails the run immediately (no polling wait) if any `persona:` claim at all is currently live in the shared commons store - the whole gate cannot run at all while a real fleet is up, full stop, rather than waiting out someone else's claim and possibly starting inside its own 90-second stale window.
Files in scope: `bin/supervise.sh` (the coordinator's own `TICK_MS` at launch - a Section 6 concern, noted here for completeness), `bin/agentic-common.sh` (a new refuse-at-start check, not a change to `wait_persona_free` itself), `.kit/live-all.sh` (wiring the new check in beside `.kit/RUNNING`).
Tests: a case that the new refuse-at-start check fails immediately with one live persona claim present, and a control that it proceeds clean with zero.

### 6. Coordinator launch shape, config key, and persona/thread naming
Model: sonnet
The coordinator's persona name is read as `cfg.coordinatorPersona` beside `controllerTickMs` (`hooks/index.ts:648`'s config-reading site), default `coordinator`, settable the same way `cfg.persona` already is. The coordinator is launched exactly like a worker - `bin/supervise.sh <repo> <persona> <permission-mode> --rundir <path>` per the memory record `a-second-worker-launches-in-its-own-repo-with-a-unique-persona-name` - with `MODEL` set to the Fable model id, `<persona>` set to the same name as `coordinatorPersona` (or `coordinatorPersona` overridden to match, whichever the operator finds clearer), `TICK_MS`/`controllerTickMs` set to Section 5's higher value, and its own `--channel-name` (its own Discord thread, distinct from every worker's). No new script. The launch recipe omits `--dev`: Section 0 item 5 retires the `--plugin-dir` runtime for the installed marketplace plugin as a v2 prerequisite, and every worker and the coordinator load the same installed copy, per that section. This section also checks `bin/supervise.sh` for any assumption that a worker never messages other personas, and lifts it if found (checked against the actual script, not assumed).

Also promoted here from the backlog, per Reviewer Round 105 R5 rather than left as a separate future brainstorm: every session that loads this plugin fires its hooks and its goal-nudge reminders on every turn, including a plain interactive chat session with no persona work to do - already felt on the Reviewer's own seat today. v2 adds more sessions loading the plugin (the coordinator, more workers), which makes the problem more common, not less. Requirement: a disarmed default, armed only via `pluginConfigs` for a session that actually intends to run as a supervised persona - one settings key this section's launch recipe sets for every worker and coordinator it starts, with an unarmed interactive session seeing none of the goal-tree machinery. The exact mechanism (a `register()`-time early return on an unset config key is the natural shape) is this section's own design work, not fixed here.
Files in scope: `bin/supervise.sh` (the config emission, and read for any such assumption), `hooks/index.ts` (`cfg.coordinatorPersona`'s read site, and the new arming check at the top of `register()`), `.claude-plugin/plugin.json` (the new arming key, following the existing convention that `askReraiseWindowMs` and its siblings already use - `askReraiseWindowMs` itself does have a `plugin.json` entry at `:60`, so this section's new key is documented there the same way), `README.md` (the quickstart gains the coordinator launch shape and the arming requirement as documented patterns, per item 6 of v1).
Tests: for the arming flag, a case that an unarmed session's `register()` never wires the goal-tree tools or the reminder injections, with a control that an armed one does. For the launch shape, no harness test; acceptance is proved live: the launched process holds `persona:<coordinatorPersona>` in the commons store, its own Discord thread opens, and one `agentic_inbox` call with `persona: <a live worker's name>` returns the coordinator's own records addressed to that worker (not that worker's records generally - `agentic_inbox` filters to the caller's own sent records, `hooks/index.ts:3780`, and this section's acceptance is written against that real behavior), with `reply` and `deferred` fields populated where applicable.

## Decisions

### FORK B: coordinator steer authority - decided 2026-09-12, Option 2, autonomy with judgment

**The question.** Once Section 4 ships, a worker can tell a coordinator's record apart from anyone else's. What still needed settling was the rule for what that label is allowed to do: when a worker receives a `[COORDINATOR]` steer that would change files inside its own approved plan, does it act on it directly, or hold for the operator's confirmation first? Two positions were on record from two live sessions working this exact repo (the worker's Round 101, the Reviewer's Round 103), and the design council's authority lens (Round 1) took a position between them rather than picking one outright.

**The ruling, the operator's own words in substance (Round 107):** agree wholeheartedly with the coordinator having the ability to steer the worker. The premise is autonomy, abstracting as many decisions away from the operator as possible. It is a fine line, easy to land on the wrong side of - a sibling project (`ai-os`) did, running 30-plus review-round loops building features nobody wanted - but largely the coordinator determines what stays aligned with the goals and product vision and directs that on its own, and what is divergent or problematic and needs a conversation to realign or decide. It will not get this right every time, and that is acceptable; the closer it gets, the better the output.

**What this settles, concretely:**
1. **Option 2 is the rule.** A `[COORDINATOR]` steer inside a worker's approved plan is acted on without an operator round trip. Option 1 ("unverified until the operator confirms") is retired for coordinator records specifically; it still holds for any record that is not machine-labelled `[COORDINATOR]` (a plain reader, the Reviewer's own steers), which is exactly why Section 4's label and Round 106's subagent fix are prerequisites, not plumbing - Option 2 is only ever as safe as the label that triggers it.
2. **The bound is judgment plus a closed exclusion list, not a per-message confirmation.** The checkable parts stay from the original Option 2 proposal: the worker ties the steer to a goal node in its own tree under an approved plan, the resulting act stays within that node's scope and the plan's own recorded commit model, and the act is none of a closed exclusion list - no push beyond the plan's commit model, no deploy, no settings/CLAUDE.md edits, no write outside the plan's own scope, no onward dispatch purely on message content, no shared-tier memory-lesson promotion, no instruction to skip the machine's heavy-process claim protocol. Beyond those mechanical checks, the operator's own line - aligned-with-goals versus divergent-and-needs-conversation - is the coordinator's judgment call to make, not a further checklist item. A wrong call lands on a branch and waits at merge: `agent_persona`'s branch protection (ruleset `protect-main`, id `22911862`) is confirmed active with `pull_request` required and `autoMergeAllowed: true` (Reviewer, Round 105, `gh` GraphQL read at 16:52Z) - a confirmed fact this Decision rests on, not a reported claim any longer, and no Open Question remains about it below.
3. **The escalation criterion belongs in the coordinator's own instructions, not the worker's.** "Divergent and problematic enough to need a conversation" is the coordinator's own call to raise with the operator, not a rule the worker enforces on incoming steers. The guard against the named failure mode (the `ai-os` loop of many rounds building unwanted features) is a round cap per steer plus a scope check against the plan's own Goal - both already in this kit's vocabulary (goal-tree round caps; the scope-adjudicator seat) - rather than a human gate on every message. A new short paragraph in Section 6 (or its own small section, at execution time's discretion) states this escalation rule and round cap as part of the coordinator's own operating instructions.
4. **What stays open.** The kit's standing-grant rail rewrite (in flight as of this spec's authoring) is no longer a precondition for building Option 2 - the operator's ruling is the authority, not the rail - but it stays an alignment check: once the rewrite lands, re-read `skills/role/SKILL.md` and confirm the delegation chain it defines is still compatible with what this Decision states, and reconcile if not.

This Decision is recorded as the operator's ruling on the Reviewer's own thread (Round 107), overruling the worker's Round 101 position for coordinator-labelled records specifically. The worker's position stood as one live view weighed, not dismissed; it is recorded as considered and overruled by the operator, not by the Reviewer.

## Out of Scope

- Cross-machine coordination (the commons store is one machine-global file; a second machine is a different problem).
- A UI or dashboard for the coordinator's view of N workers; `agentic_inbox`/`goal_status`/the commons store are the interface for now.
- Automatic lesson promotion to a shared kit memory tier; the council named this as needing at least the same external-anchor bar item 8.2 already enforces plus a review gate, and it is deliberately left to a future section once the coordinator itself has run for a while and has real cross-repo lessons to promote.
- Retiring `DISCUSSION.md` as a physical file (only as a steering path going forward); its history stays as the record of v1's own build.
- Any change to the Discord relay, the broker, or `discord-channels` - the coordinator does not touch that surface at all in this design.

## Assumptions

- assumed 2026-09-12 (route b, low-blast default): the coordinator's own persona name is `coordinator` unless the operator prefers another; reversal costs a rename in Section 6's launch recipe and nothing else.
- assumed 2026-09-12 (route b, this session's own authored pick, offered rather than sourced): the commons-store inbox path, not the harness's peer tools or Discord threads, is the coordinator-to-worker transport. A design council was convened and confirmed this pick against the plugin's own code rather than inventing it, but the pick itself is this spec's, not an existing source's ruling; reversal is a different spec, not a patch, since the rejected mechanisms were rejected on trust-boundary grounds this design relies on throughout. Flagged under Open Questions below in case the operator wants to weigh in before execution starts.
- FORK B (steer authority) was asked as route (c), material, decided by the operator 2026-09-12 (Round 107); recorded under `## Decisions` above rather than here, since it is now a ruling and not an open assumption.

## Open Questions

- The transport pick itself (commons-store inbox path over the harness's peer tools or Discord threads) is this spec's own authored call, confirmed against the code by a design council rather than handed down by an existing source - the operator has not yet been asked to affirm it specifically. Low blast to revisit before Section 1 starts; expensive to change once Sections 1-4 are built on it.
- Whether the kit's standing-grant rail rewrite (in flight as of this spec's authoring) keeps a Coordinator-to-Worker chain compatible with the Decisions section's FORK B ruling - owner: re-check `skills/role/SKILL.md` once the rewrite lands; the ruling itself does not wait on this, per the operator's own word (Round 107), but the two should be reconciled if they diverge.
- What label a coordinator gets on records *it* sends to the operator (this spec only decided the worker-facing labels) - low-blast, decide at execution time, likely no label at all since the coordinator reports via its own Discord thread, not this inbox path.
- The exact `tool.call` event field (if any) that distinguishes a subagent's own call from the top-level session's, needed by Section 4's urgent-break-in fix - owner: implementation time, checked against the real `ToolCallEvent` type, not assumed by this spec.

## Chapters

(none yet)
