# Every persona turn opens, follows or closes a goal, recorded in code, so an answered question is never nudged and a closed one-turn goal is a safe compaction point

Status: Ready
Commit Model: Branch-and-PR
Created: 2026-09-25

## Dispatch Authorization

The operator ruled the mechanism on the ASSISTANT relay thread on 2026-09-24 and agreed the ARCHITECT's seven-area recommendations on the ARCHITECT relay thread on 2026-09-25. This is Plan B of the two persona-side plans that follow the nudge-state plan; Plan A is `docs/plans/agent_persona_boundary-compaction_spec_v1.md`. The operator set the running order: nudge-state, then Plan A, then the kit's interactive-only plan (merged as claude-kit PR 123), then this plan. The task-list and autonomy-dial plans sit between Plan A and this plan in the coordinator's queue, since this plan builds on both; their order relative to each other is the coordinator's to set.

This plan starts only after four plans have merged to `origin/main`, read after a fetch: `agent_persona_nudge-state_v1.md`, `agent_persona_boundary-compaction_spec_v1.md`, `agent_persona_task-list_spec_v1.md` and `agent_persona_autonomy-dial_v1.md`. The check has two halves per plan: `git ls-tree --name-only origin/main docs/plans/` no longer lists the file, and the file sits under `docs/archive/` or `docs/plans/archive/` with a `Status: Complete` header, read with `git show origin/main:<archived path>`. A worker that finds any of the four still listed, or absent from both archives, stops with a `BLOCKED:` lead naming it. The line numbers in this plan are as of `bc3c153`, this plan's own base; every one of those four plans touches `hooks/index.ts`, so the executing session re-locates each site by its described role rather than by the number. The work runs on one branch cut from the fetched trunk and lands as one pull request.

The one thing this plan makes the plugin do that the decision-seam plan barred: a branch that reads a Jev answer. The operator ruled that Jev makes the per-turn calls, and that promotion is shadow-first, one question at a time, on a recorded bar. So the contract changes from "no branch reads a Jev answer" to "no branch reads a Jev answer except a question the operator has promoted by name in the settings, and that branch falls to a stated default on any failure". The seam invariance instrument keeps its scope over every question that is not promoted.

## Goal

When this ships, every message a persona receives is held by the plugin as a turn record, in code, before the model reads it. A message with no active goal opens a record. A message that steps an active goal attaches the record to it. A message that carries on an open record continues it. At the turn's end the plugin decides whether the turn delivered what the record asked, from fixed rules first and from Jev, TypeSafe's classifier, only on what the rules leave open, and closes the record when it did. A closed record drops out of view, so the goal tree never becomes a chat log and an answered question is never nudged. An open record that ended on a `BLOCKED:` or `WAITING:` lead, an open ask or a live background agent is in flight; any other open record is a durable boundary, so Plan A's turn-end banking compacts at it. Both Jev questions ship in shadow, journaled with the state the operator's labelers need and an outcome each, and each goes live only when the operator names it in a settings list, one question at a time. A record that touched a plan document is promoted into the goal tree through the autonomy dial, and a record the operator turned into a goal or a task by the model's own tool call is marked promoted, so no record dangles beside the entry it became.

## Intent

The frame, in the operator's words on 2026-09-24: every message has a purpose, so every turn should set, follow or complete a goal, even one that lasts a single turn. The constraint: a turn-scoped goal must close automatically when the turn ends, or the idle controller nudges an answered question. "Answer delivered" must count as "goal complete", recorded deterministically rather than by the model remembering to call `goal_done`.

What done needs to do: hold every external message as a record in the store, decide at turn end whether it was delivered, keep records out of the goal tree and out of the nudge, make an idle open record a compaction boundary, ship the two Jev questions in shadow with the state and outcomes a labeling pass needs, give the operator one settings switch per question to go live, and mark a record promoted when it becomes a goal or a task.

What done does not need to do: no Haiku decision on either question, because the operator ruled that Jev makes the quick calls and Haiku only words a new goal. No automatic goal from a message, because the ruling is that a message with no active goal opens a turn record and never a real goal. No nudge on a record, because a record is not a goal entry. No automatic close of a goal entry from a delivered record, because the task-list plan's ruling keeps goal completion with the goal. No retirement of the `untracked_work` line, because the supervisor reads it on a clean exit. No labeling pass inside the plugin, because the promotion bar is judged offline from the journal.

Alternatives refused:
- Haiku decides both questions with Jev in shadow, as the four existing sites do. Refused: the operator ruled Jev makes these calls, and a Haiku decision here would add a second judge to retire later.
- A turn record is a goal-tree leaf. Refused: the operator ruled records stay out of the tree as small leaves that close and drop out of view, and a tree leaf is scored, budgeted and activated one at a time.
- The turn record is a task in the task-list tier. Refused in that plan: a task belongs to an active goal and never exists without one, while a record is an intention with no goal yet or ahead of one.
- A record that was not delivered is dropped at the next message. Refused: the operator agreed an undelivered record carries over across turns with a time-out backstop, so a follow-up lands on the intention it continues.
- Promotion sends a `[PROPOSAL]` at the propose level whenever a record touches a plan document. Refused: the architect persona authors plan documents for other workers every turn, so that branch would propose the architect's own specs to itself. Plan activity stores the plan path and sends nothing at propose; the standing block already tells a persona how to propose.
- The plugin decides compaction from the record itself. Refused: the kit gate stays the sole decider, and Plan A's banking step is the one place the persona side speaks to it; this plan only adds a rule to Plan A's predicate.

Rulings: none at the write.

Provenance: distilled from the ASSISTANT brief `D:\personas\ASSISTANT\briefs\personas-run-unleashed-compaction-and-sequencing.md`, the ASSISTANT experiment results `D:\personas\ASSISTANT\briefs\jev-turn-complete-experiment-results.md`, the ARCHITECT memory record `plan-b-goal-every-turn-design-decisions`, and the ARCHITECT design sessions of 2026-09-24 and 2026-09-25 on the relay threads, with the code read at `bc3c153` on 2026-09-25.

## Approach

**The turn record is a field beside the goals array, not a node in the tree.** `AgentState` gains `turnRecords: TurnRecord[]`, seeded empty by a version migration, capped, and reaped on load. A record carries `id`, `text`, `openedAt`, `status`, and optionally `turnId`, `goalId`, `planPath`, `taskId` and `closedAt`. Its `status` is a closed set of five: `open`, `delivered`, `superseded`, `expired`, `promoted`. At most one record is `open` at a time; a message Jev reads as a new goal or a step supersedes the open one, whether that one is bare or attached. `turnId` is absent at the open, since `prompt.submit` runs before the turn id exists, and `turn.start` stamps it on the open record for every route, from the origin reading whose text the turn opens with. `goal_status` prints the open record on one line above the tree, so the operator can see it without reading the store. A record is never a goal entry, so nothing that reads `goals`, the tick's idle branch and the nudge among them, sees it. The task-list plan's `tasks` field is the exact sibling for the schema, the migration, the cap and the load-time reap.

**Opening runs in the real `prompt.submit` hook, which fires only for a genuine external turn.** The plugin's own submits, nudges, deliveries and the idle proposal, bypass that hook, so the seam sees exactly the messages the operator, the coordinator, a peer or the harness sent. Fixed rules run first, in order, on the owner session only; a reader-armed session opens nothing. A priming or supervisor-ask turn opens nothing. A turn arriving while an ask is open is the answer to that ask: it opens one record attached to the asked entry, superseding any open record, and asks Jev nothing, since the handler already closes the ask on that same turn. Then the turn-open question, `turn-open`, a `choice` over `new-goal`, `step` and `continuation`, is put to Jev over the state `active_goal`, `open_record` and `message`. Live, `new-goal` opens a record worded by Haiku, `step` opens a record attached to the active entry, and `continuation` keeps the open record and moves its `turnId` to this turn. Not live, or on any failure, the fallback is continue-or-attach-or-bare-record: where a record is open it continues, else a record attached to the active entry where one is active, else a bare record whose text is the message's first 80 characters, and never a goal. So under fallback an open record carries over until it expires, which is the agreed carry-over. Haiku words a new goal only on a live `new-goal` verdict, through the same `$.model.complete` call shape the controller's reason call uses, bounded to one line, with the excerpt as its fallback.

**Closing runs at the persona's own turn boundary in `turn.complete`.** The step sits under the same true-boundary guard the backstop-turn-guard plan and Plan A use: the completing id equals `currentGateTurnId` and, after this completion's own `openTurns` delete, `turnIsOpen()` is false, so a background subagent's completion closes nothing. Fixed rules run first, in order, and each decides without Jev. A skipped turn, one aborted, errored, refused or with no answer, leaves the record as it was. A closing text whose first line is a `BLOCKED:` or `WAITING:` lead, read with `readStatusLine(e.answer)`, leaves the record open and in flight. A turn that opened an ask, or ends with one open, leaves the record open and in flight. A turn with a live background agent, read from `$.agent.list()` as any agent whose `status` is `running` and whose `parentId` is absent, leaves the record open and in flight. What the rules leave is the delivered-versus-mid-work residue. There the turn-disposition question, `turn-disposition`, a `choice` over `delivered`, `mid_work` and `blocked_or_waiting` with the ASSISTANT experiment's clarified descriptions verbatim, is put to Jev over the state `active_goal`, `this_turn_was_asked` (up to 1,200 characters), `agent_final_message` (up to 3,000 characters) and `turn_tool_activity`. Live, the record is delivered when the answer's `probabilities.delivered`, which every `ChoiceAnswer` carries per option, is at least `TURN_DELIVERED_THRESHOLD` (0.5), else it stays open. The rule "a turn that opened an ask" reads `sess.state.pendingAskId` after the handler's own `ASK:` marker step, which is what sets it. Not live, or on any failure, the fallback is not delivered, which keeps the record open and costs at most one nudge on a goal entry, and nothing on a record. The experiment measured this question at 95% agreement on 110 rule-free turns with one false delivered at the threshold, and its promotion bar is recorded under Operator Verification.

**`turn_tool_activity` is built by the plugin from its own `tool.call` view.** The handler already counts work tools; this plan adds a per-turn ring of the last eight tool names and a set of flags reset at turn start: a plan document under `docs/plans/` read, a plan document edited, a commit, a push, an agent dispatched, `goal_done` called, the reply tool called, and the work-tool count. A commit or push is read from a `Bash` call whose command carries `git commit` or `git push`, which is a text read of the argument and not a run. That list is the specification of the state; the ASSISTANT's experiment scripts are background and nothing in this repository depends on them.

**Jev goes live one question at a time, by name, from the settings.** The plugin reads `jevLive` beside `jevMode`: an array of question set ids, empty by default, each of which must be one of the two ids this plan ships, held in `PROMOTABLE_SET_IDS`. A question named there is asked live; any other question the plugin asks stays in shadow. The seam's one request path gains the mode `live` beside `shadow`: both send, and `live` takes its own timeout, `LIVE_TIMEOUT_MS` (2,000 ms), because a live call is awaited on the turn-end path where a shadow call is not. A new wrapper, `liveAsk`, is the one entry point for the two questions. Where the question is not in `jevLive`, or `jevMode` is not `shadow`, it calls `shadowAsk` itself and returns null. Where it is live, it awaits the seam, journals the call and its answer with mode `live`, and returns the validated answer, or null on any of the closed failure reasons. So a caller sees one shape, an answer or null, and null always means the stated default for that question. `shadowAsk` is untouched. `jevLive` reaches the child the way `jevMode` does: a roster field the keeper maps to `JEV_LIVE`, which `emit_settings_json` writes under both plugin ids and `ensure_settings_jev_live` carries onto a provided file, refusing any id outside the promotable set with no file left behind. Under `jevMode` `off`, `jevLive` is read but nothing is sent, so the fallbacks decide; the kill switch stays a kill switch.

**Each question is journaled with an outcome the plugin can observe.** `turn-open`'s outcome is `record_delivered_within`: whether the record that call opened or continued reached `delivered` within `RECORD_OUTCOME_TURNS` (3) of the persona's own turns, written once as `true` or `false`. `turn-disposition`'s outcome is `next_prompt_kind`: the verdict the next external message took on `turn-open`, one of its three option ids, or `none` where the record expired first, written once against every disposition stamp still pending on that record, since a record open across several turn ends has several. Both join by the call's stamp id the way `next_score` and `chapter_within` do. The journal's closed set of outcome kinds gains both.

**An open record is a durable boundary unless it is in flight.** Plan A's banking predicate reads the lead line and the plan holder's Chapter advance. This plan adds one rule to that predicate: a live background agent, read as above, makes the turn not durable. An open record with no lead, no open ask and no live agent is idle, and idle is durable, so Plan A banks. Nothing else in the compaction path changes, and the kit gate is not touched.

**Promotion has three routes, and the plugin drives one of them.** Route one, a record into the tree on plan activity: at the true turn boundary, before the disposition close so a plan-touching record is promoted rather than closed, where the open record has no `goalId` and this turn wrote or edited a file whose path matches `PLAN_PATH_PATTERN` after the working-directory prefix, the record takes that `planPath`. A read alone does not count, and the architect persona the settings name never takes route one, because it authors plan documents for other workers by charter. Then the autonomy level decides, read from `AgentState.autonomy`. At `propose` nothing more happens; the standing block already tells the persona to propose. Where no goal tree exists the record keeps `planPath`, no entry is added, and one decision says so, since only the operator opens a tree. At `plan-and-ask` and `plan-and-start` the plugin adds a plan entry with that `planPath` through the same path the autonomy-dial plan gives `goal_add` at that level, so the entry is paused awaiting the operator's yes at the one and active at the other, and the record becomes `promoted` with the new entry's id. Route two, the operator's word: a `goal_create` or `goal_add` the model calls in a turn holding an open record marks that record `promoted` with the new node's id, and a `task_add` in a turn holding an open record attached to the active entry marks it `promoted` with the task's id. Route three, a task into a goal: `goal_add` takes an optional `taskId`, copies that task's text as the title where none is given, and reaps the task. No route auto-opens a goal from a message, which is the ruling.

**The `[NO GOAL]` block stops asking for a goal on every request.** Today it tells the model to call `goal_create` for any request, size aside. Under this plan the plugin holds the request as a record, so the block says so and tells the model to open a goal only for an effort that outlasts this turn or that the operator asks it to track. The text is one ledger entry, regenerated.

**Scout sweep (2026-09-25, in-session, over `bc3c153`).** The store contract: `hooks/agent-state.ts` (`AgentState` at 334, `parseState` at 653, `enforceInvariants` at 898, `PLAN_PATH_PATTERN` at 386), the task-list plan's `tasks` field and reap as the sibling. The seam: `hooks/decision-seam.ts` (`send` at 548 with its mode gate, `SHADOW_TIMEOUT_MS` at 35, `ask` at 750), `hooks/question-catalog.ts` (`QUESTION_SET_IDS` at 108, `FIXED_OPTION_SETS` at 137, `SHIPPED_QUESTIONS` at 149), `hooks/decision-journal.ts` (`OUTCOME_KINDS` at 114, `writeOutcome` at 501), `hooks/host.ts`, and in `hooks/index.ts` `hostOf` at 173, `shadowAsk` at 218, `shadowOutcome` at 375, `jevMode` at 2845. The hooks: `prompt.submit` at 9024 with the `[NO GOAL]` block at 9171, `turn.start` at 6421, `turn.complete` at 6589 with the `untracked_work` block at 6719 and the lead read at 6859, `tool.call` at 7424, `isWorkTool` at 651, `openTurns` and `turnIsOpen` at 2610, `turnMayStartEffort` at 2588, the `goal_done` handler at 8143. The settings path: `bin/agentic-common.sh` (`emit_settings_json`'s `JEV_MODE` branch at 131, `ensure_settings_jev_mode` at 398), `bin/supervise.sh`, `bin/keeper-functions.ps1` (`Build-SupervisorInvocation`), `.kit/settings-plugin-key-test.sh` (the `jevMode` leg at 64 and 101). The tests: `.kit/controller-tick-test.mjs` (the seam invariance instrument `checkInvariantAgainstOff` at 19398 and the plan-health invariance at 20167), `.kit/question-catalog-unit-test.mjs` (the set-count pin at 167), `.kit/decision-journal-unit-test.mjs` (the outcome-kind pin), `.kit/tick-harness.mjs` (the fetch and classify stubs at 131 and 313), `.kit/injection-ledger.mjs`, `.kit/injection-ledger.json`, `.kit/injection-duplicate-test.mjs`. The docs: `README.md` (Decision seam at 741, the `jevMode` row at 268, the question table, the Turning Jev off section at 967), `docs/architecture.md` (External integrations at 252, Failure modes at 261), `docs/README.md`. The harness types: `.claude/types/claude-code.d.ts` (`AgentInfo` at 60, `agent.list` at 3602 and 3771, `agent.spawn`'s `background` at 230).

## Sections of Work

### 1. The turn record store
Model: opus
Add `TurnRecord` and `turnRecords: TurnRecord[]` to `AgentState` beside `tasks`, bump the store version by one from the value the trunk carries at execution, and add the migration step that seeds `turnRecords: []`. A record carries `id: string` (minted by the goal-node id generator), `text: string` (at most 80 characters), `openedAt: number`, `status`, `turnId: string`, and optional `goalId`, `planPath`, `taskId`, `closedAt`. Export `TURN_RECORD_STATUSES` as the closed set `open`, `delivered`, `superseded`, `expired`, `promoted`; `TURN_RECORDS_MAX` (20); `TURN_RECORD_TIMEOUT_MS` (24 hours). Export `openTurnRecord(state)` returning the one `open` record or null, and `reapTurnRecords(state, now)`: an `open` record whose `openedAt` is older than the timeout, or is not a finite number, becomes `expired` with `closedAt`; records that are not `open` are dropped past the cap in ascending `openedAt` order, so the oldest-opened go first. `goal_status` prints the open record as one line, `Turn record: <status> <text>`, above the tree, and prints nothing where none is open. Call the reap in `enforceInvariants` and in `persist`, the two sites the task-list reap uses. The record layer holds at most one `open` record, and `enforceInvariants` keeps the newest where a store carries two.
Acceptance:
- A store at the prior version parses to the new version with an empty `turnRecords`; a record in each of the five statuses round-trips through `serializeState` and `parseState`.
- An `open` record older than the timeout reads `expired` after `parseState` and after a `persist`; one younger stays `open`.
- Twenty-one closed records leave twenty after the reap, the oldest dropped; an `open` record is never dropped by the cap.
- Two `open` records load with the newer `open` and the older `superseded`; an `open` record with no finite `openedAt` loads `expired`.
- `goal_status` prints the open record's line when one is open and no such line otherwise.
- `.kit/controller-tick-test.mjs` runs green on its own exit code, its version pin and `.kit/tick-harness.mjs`'s state builder moved to the new version.
Files in scope: `hooks/agent-state.ts`, `hooks/index.ts` (the reap call in `persist`), `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs`, `.kit/cost-migration-test.mjs`, `.kit/fixtures/` (one fixture at the prior version), `docs/backlog.md` (the store-version sentence, where one names the version).
Tests: lock the migration, the timeout in both directions, the cap sparing an open record, and the two-open repair; a record that never expires holds a stale intention against every later message.

### 2. The `jevLive` setting reaches the child
Model: sonnet
Clone the `jevMode` path for `jevLive`. The plugin reads `cfg.jevLive` at registration as an array, trims each string member, keeps the members that are in `PROMOTABLE_SET_IDS` (section 3 exports it) and logs one `jev_live_invalid` decision naming any it dropped, a non-string member among them; a missing or non-array value reads as empty. The shell halves trim each comma-separated member the same way before the check. The bash list of promotable ids is typed once in `bin/agentic-common.sh`, and `.kit/settings-plugin-key-test.sh` pins it against `PROMOTABLE_SET_IDS` read from the catalog, so the two cannot drift. The keeper maps a roster entry's `jevLive` array to `JEV_LIVE` as a comma-separated string, set only where the entry carries the field. `emit_settings_json` writes `jevLive` under both plugin ids from `JEV_LIVE` where it is set, as a JSON array, and leaves the key out where it is unset. `ensure_settings_jev_live` overwrites a provided file's value from `JEV_LIVE` and leaves the file byte-identical where it is unset, with the same rename-replace the `jevMode` helper uses. Both refuse a value carrying an id outside the promotable set, or a quote or control character, with an `ERROR:` line and no file left behind. `.kit/settings-plugin-key-test.sh` gains the same leg it runs for `jevMode`.
Acceptance:
- `JEV_LIVE=turn-disposition` reaches `jevLive` as `["turn-disposition"]` under both ids on an emitted file and on a provided one; unset leaves the key out of an emitted file and a provided file byte-identical.
- `JEV_LIVE=turn-disposition,bogus` is refused with no file left behind; `JEV_LIVE=` (empty) is treated as unset.
- The plugin reads a settings value of `["turn-disposition","bogus"]` as `["turn-disposition"]` with one `jev_live_invalid` decision, and a missing key as empty.
- `JEV_LIVE= turn-disposition ` with surrounding spaces reaches the file as `["turn-disposition"]`.
- `.kit/settings-plugin-key-test.sh` passes on its own exit code with the bash-list pin, and `.kit/keeper-unit-test.mjs` passes with its emitted-environment pin gaining `JEV_LIVE`.
Files in scope: `hooks/index.ts` (the `jevMode` read at 2845), `bin/agentic-common.sh` (`emit_settings_json`, `ensure_settings_jev_mode` as the sibling), `bin/supervise.sh` (the call site of `ensure_settings_jev_mode`), `bin/keeper-functions.ps1` (`Build-SupervisorInvocation`), `.kit/settings-plugin-key-test.sh`, `.kit/keeper-unit-test.mjs` (the environment `deepEqual` pins at 256 and 326), `README.md` (the settings table row beside `jevMode` at 268 and the roster field list at 460).
Tests: lock that a bad id is refused in both halves and that unset leaves a provided file byte-identical; a switch that silently drops a valid id leaves a question in shadow the operator believes is live.

### 3. The seam's live path, the two questions and the contract
Model: fable
Add the two questions to the catalog. `TURN_OPEN` (`turn-open`) is a `choice` with the instructions "A message has just arrived for an autonomous persona. Given its active goal and its open turn record, what is the message?" and the options `new-goal`: "It asks for something new, not a step of the active goal and not a follow-up to the open record."; `step`: "It is a step of, or an instruction about, the active goal."; `continuation`: "It follows up, corrects or adds to the open turn record's own request." `TURN_DISPOSITION` (`turn-disposition`) is a `choice` with the instructions "How did this turn end?" and the options `delivered`: "It finished what was asked of it this turn (answered, completed and reported the action, relayed or handed off the result, or acknowledged a message needing no action) and left nothing of its own half-done. Standing by for a new request, or others now acting on something it already handed over, still counts as delivered."; `mid_work`: "It stopped with its own work unfinished: a next step narrated but not taken, an edit or commit half-done, or an intermediate status."; `blocked_or_waiting`: "It cannot finish its own current task until something else happens: a person's decision it asked for, another agent's answer, or a background task or subagent of its own still running." That wording is the one the ASSISTANT's experiment measured, carried here so the repository holds it. Both join `QUESTION_SET_IDS` and `FIXED_OPTION_SETS`. Export `PROMOTABLE_SET_IDS` as exactly those two ids. Export `TURN_OPEN_OPTIONS` and `TURN_DISPOSITION_OPTIONS` as frozen arrays, the option ids in force, and `TURN_DELIVERED_THRESHOLD` (0.5) and `RECORD_OUTCOME_TURNS` (3) beside them. Add `record_delivered_within` and `next_prompt_kind` to `OUTCOME_KINDS`.

In `hooks/decision-seam.ts`, `send` admits the mode `live` beside `shadow`: both read the key and send, any other mode is `off`. The timeout is taken from the mode: `SHADOW_TIMEOUT_MS` for `shadow`, `LIVE_TIMEOUT_MS` (2,000) for `live`. In `hooks/index.ts` add `liveAsk(host, site, questionSetId, optionIds, state, jevMode, jevLive)`: where `jevMode` is not `shadow`, or the question is not in `jevLive`, it calls `shadowAsk` with the same arguments and returns null; where it is live it awaits `ask` with mode `live`, writes the call and answer lines with mode `live`, and returns the validated `ChoiceAnswer` or null on any closed failure reason. It is the only entry point sections 4 and 5 call. The journal writers take the mode string as they take `shadow` today.

Amend the contract. The seam invariance instrument in `.kit/controller-tick-test.mjs` keeps its scope: with `jevLive` empty, every decision and state field is invariant across a Jev at each extreme, failing and hung, for every question this plan adds. Add one instrument beside it: with `turn-disposition` in `jevLive`, the record's status follows the answer (delivered at p 0.5 and above, open below), and a hung, failing or `off` Jev leaves it open, with the turn's end delayed by no more than the live timeout. `README.md`'s Decision seam section states the amended contract: Jev's answer is read by no branch except a question named in `jevLive`, that branch falls to a stated default on every closed failure reason, and the promotion bar is what earns the name.
Acceptance:
- `.kit/question-catalog-unit-test.mjs` passes with its set-count pin moved up by two from the value the trunk carries at execution, since the nudge-state plan adds a question of its own, and `.kit/decision-journal-unit-test.mjs` with its outcome-kind pin gaining the two kinds.
- `.kit/decision-seam-unit-test.mjs` locks that `live` sends and times out at `LIVE_TIMEOUT_MS`, that `shadow` still times out at `SHADOW_TIMEOUT_MS`, and that any third mode is `off`.
- The existing invariance instrument passes over both new questions with `jevLive` empty; the new instrument shows the status following the answer at both sides of the threshold and the fallback on each of hung, failing and `off`.
- A `liveAsk` on a question outside `jevLive` makes no HTTP call.
Files in scope: `hooks/question-catalog.ts`, `hooks/decision-journal.ts`, `hooks/decision-seam.ts`, `hooks/index.ts` (beside `shadowAsk` at 218), `.kit/question-catalog-unit-test.mjs`, `.kit/decision-journal-unit-test.mjs`, `.kit/decision-seam-unit-test.mjs`, `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs`, `README.md` (Decision seam at 741 and the question table).
Tests: lock both directions of the live switch, the threshold at exactly 0.5, and every fallback; a live call that can hang past its timeout delays every turn end, and a fallback that reads delivered closes a record the turn did not deliver.

### 4. Opening a record at the prompt
Model: opus
In the `prompt.submit` handler, after the open-ask close and before the context blocks, on the owner session only: apply the fixed rules in the Approach's order, then ask `turn-open` through `liveAsk` or `shadowAsk` over the state `active_goal` (the active entry's objective or empty), `open_record` (the open record's text or empty) and `message` (the prompt's first 1,200 characters). On a live `new-goal`, supersede the open record and open one worded by Haiku (`$.model.complete`, `haiku`, `maxTokens` 40, a prompt asking for one line under 80 characters naming what the message asks; the excerpt on any failure). On a live `step`, open a record with `goalId` set to the active entry's id, superseding the open record whether bare or attached. On a live `continuation`, keep the open record and set its `turnId` to the turn that opens next, which `turn.start` stamps from the reading it takes; where no record is open, treat it as the fallback. Not live, or on a null answer: continue-or-attach-or-bare-record, as the Approach states. Each act logs one decision: `turn_record_opened`, `turn_record_attached`, `turn_record_continued`, `turn_record_superseded`. The `[NO GOAL]` block is rewritten as the Approach states and the ledger regenerated. Write the `record_delivered_within` outcome: hold the stamp id on the record, and at the delivery or the third later own-turn completion, whichever comes first, write `true` or `false` once.
Acceptance:
- A message with no active entry and no open record opens a bare record with the excerpt, under fallback; a message arriving on an open record continues it under fallback; the same message under live `new-goal` opens a record worded by Haiku and supersedes the open one.
- A message with an active entry attaches under fallback and under live `step`; under live `continuation` the open record keeps its text and takes the new turn id.
- A priming turn and a supervisor-ask turn open nothing; a turn answering an open ask opens one record attached to the asked entry and makes no Jev call; a reader-armed session opens nothing.
- The `[NO GOAL]` block no longer asks for `goal_create` on every request; the ledger regenerates clean and `.kit/injection-duplicate-test.mjs` passes.
- `record_delivered_within` is written once, `true` on a record delivered within three own turns and `false` on the third completion otherwise.
Files in scope: `hooks/index.ts` (`prompt.submit` at 9024, the `[NO GOAL]` block at 9171, `turn.start` at 6421 for the turn-id stamp), `.kit/controller-tick-test.mjs`, `.kit/injection-ledger.mjs`, `.kit/injection-ledger.json`, `.kit/injection-duplicate-test.mjs`.
Tests: lock the fallback and every live verdict, the three turns that open nothing, and that a reader-armed session opens nothing; a record opened on a nudge or a priming turn would hold the plugin's own text as an intention.

### 5. Closing a record at the turn's end, and the compaction rule
Model: fable
In `turn.complete`, under the true-boundary guard, after the lead read and before Plan A's banking step: build `turn_tool_activity` from the per-turn ring and flags this section adds to `tool.call` and resets in `turn.start`; apply the fixed rules in the Approach's order, reading the live-agent rule from `$.agent.list()`; then ask `turn-disposition` through `liveAsk` or `shadowAsk` over the four-field state. Live, set the open record `delivered` with `closedAt` when p(delivered) is at least `TURN_DELIVERED_THRESHOLD`; otherwise leave it open. Log one decision per close, `turn_record_delivered`, and one per in-flight rule that held, `turn_record_in_flight` with the rule named. Expire an open record past the timeout here as well. Extend Plan A's durable-boundary predicate with the live-agent rule, so a turn with a running agent banks nothing. Write the `next_prompt_kind` outcome at the next `turn-open` verdict, or `none` at expiry.
Acceptance:
- Under live, a delivered answer at p 0.5 closes the record and one at p 0.49 leaves it open; under fallback the record stays open.
- A `BLOCKED:` or `WAITING:` first line, an open ask, and a running top-level agent each leave the record open with the rule named, and Jev is not asked.
- A subagent's completion inside the persona's open turn closes nothing.
- A turn with a running top-level agent does not run Plan A's boundary command; the same turn with the agent completed does.
- The journaled state carries the four fields with `this_turn_was_asked` cut at 1,200 and `agent_final_message` at 3,000 characters, and `turn_tool_activity` carries the eight flags and the last eight tool names.
- `next_prompt_kind` is written once against every disposition stamp pending on the record, and a record with three pending stamps gets three outcome lines carrying the same verdict.
- Route one (section 6) runs before this section's close, so a plan-touching bare record under a live `delivered` verdict reads `promoted`, never `delivered`.
Files in scope: `hooks/index.ts` (`turn.complete` at 6589, `tool.call` at 7424, `turn.start` at 6421, Plan A's banking step as landed), `.kit/controller-tick-test.mjs`, `.kit/tick-harness.mjs` (an `agent.list` stub).
Tests: lock both sides of the threshold, every fixed rule in both directions, the subagent-completion guard, and the compaction rule in both directions; a record closed on a turn that dispatched a live agent is the wrong compaction point, and one never closed nudges nothing but strands the outcome.

### 6. The promotion routes
Model: opus
Route one: at the true boundary, before section 5's disposition close, where the open record has no `goalId`, the session's persona is not the architect persona the settings name, and the turn's `tool.call` view saw a `Write` or `Edit` whose `file_path`, made relative to `await $.session.cwd()`, matches `PLAN_PATH_PATTERN`, set `planPath` on the record. Where no goal tree exists, log one `turn_record_plan_noted` decision and stop there. Then read `AgentState.autonomy`: at `propose` do nothing more; at `plan-and-ask` and `plan-and-start` add the plan entry through the shared function the autonomy-dial plan's `goal_add` handler uses at that level, so the paused-awaiting-yes shape and the `[PROPOSAL]` or `[STARTED]` record are the dial's own, and mark the record `promoted` with the entry's id. A record already carrying that `planPath` in a promoted status is not promoted again. Where the add is refused, because the record write the dial's path makes fails or the plan path fails `PLAN_PATH_PATTERN` on re-test, the plugin logs one `turn_record_promotion_refused` decision naming the cause and leaves the record open with its `planPath`, so the next boundary tries again. Route two: in the `goal_create`, `goal_add` and `task_add` handlers, after the node or task is created, mark the open record `promoted` with the id, for `task_add` only where the record's `goalId` is the active entry. Route three: `goal_add` takes an optional `taskId`; where given, the title defaults to the task's text and the task is dropped from `tasks` after the add.
Acceptance:
- A turn editing `docs/plans/x_v1.md` with a bare open record sets `planPath`; at `propose` no entry and no record are sent; at `plan-and-ask` a paused entry awaiting yes exists with that path and the record reads `promoted`; at `plan-and-start` an active entry exists.
- The same path touched under a record already promoted for it adds no second entry; a `Read` of a plan document sets no `planPath`; the architect persona's turn sets none; with no goal tree the record keeps `planPath` and no entry is added.
- Under live `delivered` on a plan-touching bare record at `plan-and-start`, the record reads `promoted` and the entry exists.
- `goal_create` and `goal_add` in a turn with an open record mark it `promoted`; `task_add` marks a step record and not a bare one.
- `goal_add` with a `taskId` copies the text and drops the task; an unknown `taskId` is refused.
Files in scope: `hooks/index.ts` (the `goal_create`, `goal_add` and `task_add` handlers, the boundary step of section 5), `hooks/agent-state.ts`, `.kit/controller-tick-test.mjs`.
Tests: lock the three levels of route one, the no-double-promotion guard, and route three's refusal; a promotion at `propose` is the architect flooding the steward.

### 7. The documents
Model: sonnet
`README.md` and `docs/architecture.md` state the behavior as it now is: the turn record, its five statuses, the opening rules and the closing rules with the fixed rules ahead of Jev, the two questions with their states and outcomes in the question table, `jevLive` and the amended contract, the promotion routes, the compaction rule, and the rewritten `[NO GOAL]` block. `docs/README.md` gains this plan's row, and its decision-seam archive row carries the `jevLive` exception. State current behavior only, no change narrative.
Acceptance: all three documents describe every item above consistently with sections 1 to 6 as built; no sentence in any of them still says Jev's answer is read by no branch without the `jevLive` exception.
Files in scope: `README.md`, `docs/architecture.md`, `docs/README.md`.
Audience: a developer of the agent_persona plugin who knows the goal tree, the store and the decision seam but not the record layer. Must-answer questions: what a turn record is and how it differs from a goal entry and a task; what opens, continues and closes one; which rules decide before Jev; how a question goes live and what the bar is; what promotion does at each autonomy level; why an idle open record is a compaction boundary. Voice: none. Fact base: the as-built `hooks/agent-state.ts`, `hooks/index.ts`, `hooks/decision-seam.ts`, `hooks/question-catalog.ts` and `bin/agentic-common.sh` from sections 1 to 6.

## Out of Scope
- Any Haiku decision on either question, and any Jev call the four existing shadow sites make.
- Retiring the `untracked_work` decision line; the supervisor reads it on a clean exit (`bin/supervise.sh` near 4718).
- Moving the idle classifier from Haiku to Jev, which the brief named as a candidate; a separate promotion on its own bar.
- The labeling pass that judges the promotion bar; it runs offline over the journal with the ASSISTANT scripts.
- Any change to the kit gate, to Plan A's boundary command call, or to the marker.
- Nudging a turn record, or completing a goal entry from a delivered record.
- The nudge-state plan's `continued_unprompted` question and its outcome, which stay as that plan ships them.

## Assumptions
- assumed 2026-09-25 (source: the operator's rulings of 2026-09-24 and 2026-09-25 as recorded in `plan-b-goal-every-turn-design-decisions`): the seven design areas are settled and this spec reopens none of them; reversal: a reopened area is a ruling appended to Intent and a Chapter drift line.
- assumed 2026-09-25 (default): at most one record is open at a time and a new-goal verdict supersedes the open one; reversal: a list of open records needs a selection rule at every read, a larger change.
- assumed 2026-09-25 (default): the timeout is 24 hours, since an idle open record is harmless (never nudged, durable for compaction); reversal: one constant.
- assumed 2026-09-25 (default): at the propose level, plan activity stores `planPath` and sends nothing, because the architect authors plan documents every turn; reversal: one branch sending a `[PROPOSAL]` record, and a way to exclude the architect.
- assumed 2026-09-25 (default): the live background agent rule reads `$.agent.list()` at turn end rather than tracking dispatches per turn; reversal: a per-turn dispatch map keyed on `agent.spawn`, if `agent.list` proves not to see a model's own background Agent call.
- assumed 2026-09-25 (default): the `[NO GOAL]` block stops asking for a goal on every request, since the plugin now holds the request; reversal: the old ledger text, one entry.
- assumed 2026-09-25 (default): `turn-disposition`'s live timeout is 2,000 ms against a measured median of about 300 ms and a 90th percentile of about 340 ms; reversal: one constant.
- assumed 2026-09-25 (source: `bin/supervise.sh` near 4718): the `untracked_work` line is left alone because the supervisor reads it; reversal: a supervisor change and its natural-exit suite case.
- assumed 2026-09-25 (default): the executing implementer is a persona engineer familiar with the plugin's own vocabulary (the goal tree, the idle nudge, the injection ledger, the Jev shadow seam, the true-boundary guard, the autonomy dial) and resolves each term by reading the named code and the repo's README and architecture.md; reversal: a reader without that context needs those terms glossed, which the repo docs carry.

## Operator Verification
- After this plan merges and the plugin cache updates, run one persona for a day with `jevLive` empty and confirm records open and expire in `goal_status`'s store, and that no answered question is nudged. What reopens the work: a nudge on a turn that answered an operator message with no goal active.
- Promote `turn-disposition` only when a labeling pass over the shadow journal meets the bar: at least 95% precision on `delivered` at p 0.5 and above, over at least 50 fresh delivered cases, with no false `delivered` on a turn whose own edits were uncommitted. The pass is the ASSISTANT's scripts under `D:\personas\ASSISTANT\.kit\jev-exp\` over the journal. What reopens the work: precision below the bar, or a false delivered on uncommitted edits.
- Promote `turn-open` on its own pass afterwards, at the same bar on `new-goal`. What reopens the work: a message attached to the wrong entry.

## Open Questions
- Whether `$.agent.list()` reports a model's own background `Agent` call as a running top-level agent from inside `turn.complete`, or only agents a plugin spawned. Owner: section 5's implementer, who confirms it against the harness and takes the reversal named under Assumptions where it does not.
- Who runs the labeling pass and how often. Owner: the operator, with the ASSISTANT holding the scripts.

## Related
- `docs/plans/agent_persona_boundary-compaction_spec_v1.md` (Plan A): the banking step whose predicate section 5 extends.
- `docs/plans/agent_persona_task-list_spec_v1.md`: the `tasks` field, the store sibling for section 1, and the promotion seam this plan owns.
- `docs/plans/agent_persona_autonomy-dial_v1.md`: the level section 6 reads and the `goal_add` path it reuses.
- `docs/archive/agent_persona_nudge-state_v1.md`: the hold and the status lines; its `continued_unprompted` question stays its own.
- `docs/archive/agent_persona_decision-seam_v1.md`: the seam whose contract section 3 amends.

## Chapters
