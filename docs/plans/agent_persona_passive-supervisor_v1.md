# agent_persona: passive supervisor steered by conversation, v1

Status: In Progress
Commit Model: Branch-and-PR. `main` is protected on GitHub and accepts pull requests only, never a direct push. PRs #1-#4 were opened ready-for-review and the operator merged each one promptly, item by item, forcing a fresh branch every time a branch froze mid-work (`passive-supervisor` -> `-2` -> `-3`). Decided 2026-09-11: every PR from here on opens as **Draft**, stays Draft while more commits land on it, and the worker marks it ready for review explicitly, in words, when it should actually be merged. This lets one PR carry several items' commits instead of a fresh branch per merge. `passive-supervisor` carried Items 1-2 (PR #1, merged); `passive-supervisor-2` carried Item 3 (PR #2, merged); `passive-supervisor-3` carries Item 4 (PR #4, merged) and, as a Draft PR going forward, the rest.
Worker: the plugin's own persona, running under `bin/supervise.sh` with this document as its roadmap.
Executor's plugin runtime: today the stable copy at `D:\DeepSeekHarness\agentic-plugin`, loaded with `--plugin-dir`. The target runtime is this repository installed as a plugin from its own marketplace manifest, updated with `claude plugin update`, and item 6 carries the switch. The tree under edit is `D:\agent_persona` (this clone). The runtime and the edit tree are never the same directory, because a session whose working directory sits inside its own `--plugin-dir` does not initialize the plugin.

## Goal

The supervisor becomes something an operator starts once and talks to. Started with no goal, it waits quietly and holds its persona. A goal arrives as a sentence in a chat, not as a tool call. During a goal the operator can redirect, pause, add, or drop work by talking. When the goal completes the supervisor goes quiet again and waits for the next one. The chat is a Discord thread or a proxy agent, and only text from a holder of a reader claim ever steers the worker.

Every numbered item below is a goal with its acceptance, not a task list. The worker decides how.

## Roadmap

1. Passive start. `bin/supervise.sh` launched with no `--prompt` runs indefinitely: the child idles with its persona claimed and heartbeating, the supervisor logs that it is waiting, and neither idleness nor an empty goal tree is read as a crash, a restart trigger, or completion. Proof: a run with no prompt is still alive and logging after ten minutes, with zero restarts.
   - The pre-gate, the EOF stop path, and the exit codes keep their current meaning.
   - The supervisor also changes into the work directory itself, so launching it from elsewhere no longer splits the store from the poll.

2. A goal arrives by conversation. An operator message delivered through the reader channel, phrased as a request in plain words with no tool named, leads the worker to open a goal tree and begin planning, and the worker's reply states the goal it took in one line. Proof: the decision log shows the operator turn, then `goal_create`, then `planning_fired`, and the reader's inbox holds the one-line confirmation.

3. Steering during a goal. While plans are active, operator messages can add a plan, drop one, change priority, pause, and resume, and the worker answers each with what it changed. Proof: a mid-run message produces a recorded decision naming the change and a matching change in the goal tree, for at least add, drop, and pause.
   - The roadmap file is already re-read at every planning event; a steer that edits the roadmap is one acceptable mechanism, and a steer that acts on the tree directly is another. Both are recorded.
   - An ask record carries the worker's full question. Today the stored `question` is cut near one hundred characters, so the reader sees a fragment; the reader must see what the worker asked.

4. Quiet between goals. When the root completes, the supervisor returns to the passive state of item 1 instead of exiting, and a second goal given by conversation in the same supervisor lifetime runs to completion. An explicit shutdown request from the operator stops the child by the EOF path and exits 0. Proof: two goals completed and one clean shutdown in one `supervisor.log`.

5. A chat channel. Text typed in a Discord thread reaches the worker as an operator turn, and the worker's replies and open asks appear in that thread. The shape is decided (operator, 2026-09-11): the channel is native, not a proxy session. The supervised child is itself the session bound to a Discord thread, the way every session started through `D:\discord-channels\wrapper\Enter-ClaudeSession.ps1` gets its own thread today: text in the thread arrives in the child as a channel event, the child answers with the relay's `reply` tool, and the broker's one-account allowlist is the authorization. No second model sits between the phone and the worker, nothing polls, and no message round-trips through the shared store. The proxy shape is rejected: in use the proxy session confused its own conversation with the relayed one, its polling loop was either slow to steer or noisy in the thread, and every message cost a store write and read. Proof: a message from a phone produces an `[OPERATOR]` turn in the decision log with no proxy session live, and the reply is visible in the thread.
   - The supervisor launches the child with the channel attached, so a supervised run has a thread from its first turn. Whether the relay attaches to a headless `claude -p` stream-json child is the first thing to establish; the wrapper's notes contemplate a `-p` launch, and if it does not hold, the fix goes to the relay side in `D:\discord-channels`, not to a proxy.
   - One thread per supervisor, for its whole lifetime. The broker binds a thread to a session ID and every child the supervisor launches is a new session, so a restart would open a new thread and the operator would follow a trail of short threads instead of one conversation. The supervisor's children must land in one persistent thread across restarts, with one line in the thread saying a restart happened. This is a broker-side change in `D:\discord-channels`, and the worker builds it (operator, 2026-09-11): under a plan document in that repository's `docs/plans/`, on a branch with a pull request, never a direct push to its trunk. Interactive sessions started through the wrapper keep one thread per session. This item's Chapter names that plan and its PR. The stale `docs/plans/sapplefeld-channels_dsh-bridge_spec_v1.md` in that repository, about the retired harness, is archived in the same PR. Mirroring is off for supervised children so the thread carries operator conversation, not every turn.
   - An open ask never silences the worker. Today an open ask suppresses the controller's nudges, the classifier can re-open the same question after it was answered, and only an inbox record carrying the ask id closes it; with a quiet channel the worker sits for hours logging "still open" and reads nothing, not even this file. Required: a reply typed in the thread while an ask is open answers that ask, a closed question is not re-asked for the same node, an ask open past a bounded window re-raises itself into the thread once, and the controller's own nudges resume on a plan whose ask has gone unanswered rather than waiting forever. Proof: a harness case for each of the four, and a live run where an ask is answered from the thread with no ask id typed.
   - A proof child is stopped when its proof ends, so its persona claim does not shape the next proof. A second proof child that finds the first still holding `default` comes up as a reader and proves less than it seems to.
   - `agentic_say` and `agentic_inbox` stay as the tool-to-tool path between sessions on one machine, which is what the live suites use. They are not the operator's channel.
   - Bounded store. The shared store keeps only open asks and a short window of recent inbox and reply records per persona; closed records roll to an append-only `.agentic-channel.jsonl` in the work directory. Today every message ever sent stays in the one JSON file, which grows without bound and is rewritten whole on each write. Proof: a harness case that sends more records than the window holds and finds the store at the window size and the log holding the rest.

6. One command to start, and a README that says so. A fresh clone plus one command starts the supervisor in passive mode with the channel attached, on this machine and on another Windows machine with Git for Windows. The README gains a quickstart at the top: how to start, how to give a goal, how to steer, how to stop, and where the logs are. Proof: the quickstart, followed literally in a fresh clone, reaches a waiting supervisor in under two minutes.
   - The persona the supervisor is given is the persona the child runs as. Today `plugin.json` names no persona option, the plugin starts every session as `default`, and the supervisor's second argument reaches only its own gate. A `persona` option in `userConfig`, read where the session's persona is initialized and emitted by the supervisor into the settings it writes, closes that gap; the identity tool keeps its meaning. Proof: a run given `dev` whose child claims `persona:dev` in the shared store without ever claiming `default`, and a harness case for the option with a control.
   - The identity tool's description states what its handler does: it claims the persona in the shared store, joins as a reader with `agentic_say` and `agentic_inbox` when a live holder exists, and takes ownership only from a holder whose heartbeat is stale. Today the description says the opposite, and a model reading it refuses a safe call. The README's option table gains the `persona` row.
   - The runtime is the installed plugin, not a second checkout. The repository carries `.claude-plugin/marketplace.json` with one entry whose `source` is `.`, since `plugin.json` sits at the repository root. The quickstart installs from it (`claude plugin marketplace add`, then `claude plugin install` at user scope) and updates with `claude plugin update`, and the supervisor launches its child without `--plugin-dir` when the plugin is installed, keeping the flag as an opt-in for a development tree. Every session that shares a commons must load the plugin the same way: the shared store file is named for the load mode (`agentic-plugin_inline-<hash>.json` under `--plugin-dir`), so a `--plugin-dir` worker and an installed relay would hold separate stores and never see each other's claims. `find_global_store` in `bin/supervise.sh` takes the first `agentic-plugin_*.json` it finds, which is wrong once two load modes have run on one machine; it selects the store of the load mode in use. Open questions to settle by one live run and record in the Chapter: the store filename an installed plugin uses, whether the settings key `agentic-plugin` reaches an installed plugin's options or needs a marketplace suffix, and whether the cwd trap applies to the installed cache. Proof: the quickstart's install path on a fresh clone with no `--plugin-dir` anywhere, the child claiming its persona in the store the supervisor polls, and the relay session seeing that claim.

7. Proof lives in the suites. Items 1, 2, and 4 each have a live suite in `.kit/live-all.sh`, the supervisor's decision logic for the passive and return-to-passive states has harness cases that fail without the change, and the whole gate is green on one run stamp on the branch. Proof: the stamp's `summary.txt`, pasted verbatim into the closing Chapter.

## Constraints

- Branch `passive-supervisor` off `main`. One or more commits per goal, titles with an uppercase surface prefix (`SUPERVISOR:`, `PLUGIN:`, `SUITE:`, `README:`, `PLAN:`) and a sentence. Push the branch after each goal closes. Never commit to `main`.
- Before every commit: `npx tsc --noEmit` exit 0 and `node .kit/controller-tick-test.mjs` exit 0. Before a goal closes: the live suite that covers it, `script_exit=0`.
- Your own persona is `dev`. The plugin starts every session as `default`, so your first tool call in any session is `agentic_identity` with persona `dev`, before `goal_create` and before any edit. The live suites use `default`. Holding `default` yourself makes every suite's pre-gate wait on you and fail.
- The `run/` directory under this clone is the supervisor's scratch and is ignored; never commit it.
- Your runtime is not yours to edit. Today it is `D:\DeepSeekHarness\agentic-plugin`; once item 6 lands the install path, it is the installed plugin. Edit this clone, and `D:\discord-channels` only for the one-thread requirement in item 5, under that repository's own plan and pull request. Nothing else on the machine. The switch of the running worker and relay from `--plugin-dir` to the installed plugin happens together, at a goal boundary, by the operator.
- A material design fork goes to the operator as an ask with a recommendation. Item 5's shape is one. Do not wait on trivia.
- Documents state the current behavior, never the change story. No em dashes anywhere.
- Append a Chapter to this document when a goal closes: what shipped, the commits, the proof, and what surprised you.

## Chapters

### Chapter 1 - 2026-09-11
Completed: 1. Passive start
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: Two real defects surfaced while proving this section, both now fixed or routed. (1) I initially operated this whole session under the `default` persona instead of `dev` (never called `agentic_identity` at session start), which is exactly the constraint violation the plan's own Constraints section warns against; caught it by inspecting the shared store's `activeSessionId`, switched to `dev` (epoch 3), and recreated the goal tree under it. (2) The proof run's own `commons` pre-gate correctly refused to launch a second `dev`-persona child while this session already held `dev` live in the global commons store (`~/.claude/plugins/store/agentic-plugin_*.json` is one file per machine, not scoped per workdir) - this is the pre-gate working as designed, not a defect, but it means a live proof run must use a persona name nothing else holds; re-ran with `item1proof` and it passed clean. Also confirmed empirically (matching a bullet the operator's collaborator added to Item 6 mid-run) that the child always claims persona `default` internally regardless of the CLI persona argument passed to `supervise.sh`: that argument only gates the pre-launch wait today. Fixing that is scoped to Item 6, not this section, so it is left alone here.
Assumptions: No dedicated live suite exists yet for Item 1 (Item 7 is where Items 1, 2, and 4 each get one in `.kit/live-all.sh`); until then, this section's own "Proof:" line is satisfied by a manual timed run rather than an automated suite, per the roadmap's own division of that work into Item 7 (2026-09-11).
Review Findings: none (trivial, self-contained shell script change; no dispatch, no review round)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0, `node .kit/controller-tick-test.mjs` exit 0 (79 assertions, 0 failures). No live suite covers this section yet (see Assumptions); the manual proof below stands in for it.
Next: 2. A goal arrives by conversation
Commit Model: Branch-and-PR

**Proof:** ran `bin/supervise.sh` with no `--prompt`, launched from a different cwd than the workdir, persona `item1proof` (a name nothing else on the machine holds, to avoid the commons collision above). `supervisor.log`:
```
2026-09-11T12:35:31Z GATE PASSED: no live persona claims (commons and heartbeat both free)
2026-09-11T12:35:31Z LAUNCH child-1 (start_ts=1789130131203, prompt=)
2026-09-11T12:36:35Z WAITING: child-1 alive, persona held, no restart triggers (poll 6)
... (nine more WAITING lines, one per ~65s, through poll 60) ...
2026-09-11T12:46:20Z WAITING: child-1 alive, persona held, no restart triggers (poll 60)
2026-09-11T12:47:14Z CLEANUP: stopping child-1 (pid 18464)
```
Ten minutes forty-nine seconds alive, ten WAITING lines proving liveness, zero RESTART lines, zero `LAUNCH child-2` lines. Stopped with `SIGTERM` to the supervisor process, which took the trap's `cleanup` path (the same `stop_child` the EOF/TERM/KILL path in the plan's Constraint uses); all processes exited within seconds, no KILL needed. Confirmed the scratch store and heartbeat (`.agentic-personas.json`, `.agentic-heartbeat.json`) landed under the workdir the whole run, never under the launcher's original cwd (a stale unrelated file there was untouched, mtime unchanged) - this is the cwd fix (`cd "$WORKDIR"` added in this section) doing its job.

### Chapter 2 - 2026-09-11
Completed: 2. A goal arrives by conversation
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: The mechanism this item needs mostly already existed (the D3 drain already tags an inbound reader message `[OPERATOR]` and logs an `operator_delivered` decision; the planner already logs `planning_fired` at the next controller tick after `goal_create`; a D4 mechanism already captures the owner's own chat reply as the delivered answer, satisfying "the reader's inbox holds the one-line confirmation" with no new code). The one real gap: with no goal at all (root never created, or already completed), `prompt.submit` injected nothing telling a cheap-tier child what to do with an ordinary request, so it could read a plain-language ask as small talk and never call `goal_create`. Added a one-line reminder for exactly that state (`hooks/index.ts`, the `else if (sess.state.goals.length === 0)` branch beside the existing paused-tree reminder). Two live proof runs on real haiku-tier children (no explicit tool named in either prompt) both called `goal_create` with the request as the objective and then completed the actual task (wrote the requested file) before I could observe `planning_fired`/`goal_done`, because both runs got contaminated by the pre-existing commons-claim staleness defect described below - once as my own leftover claim, once as an unrelated live session's. That defect is orthogonal to this section's change (confirmed by reproducing it twice, from two different sources) and is logged to `docs/backlog.md` rather than fixed here, since a real fix needs either per-claim liveness or a persona-switch release and neither is this section's job.
Assumptions: No dedicated live suite exists yet for Item 2 (Item 7's job, `.kit/live-all.sh`); this section's "Proof:" is the two manual runs below, per the same division as Item 1's Chapter (2026-09-11). No harness case added to `.kit/controller-tick-test.mjs` for the new `[NO GOAL]` reminder branch; the existing suite has no coverage of `prompt.submit`'s context-injection blocks at all (confirmed by search), and building that harness scaffolding from scratch is scoped to Item 7 rather than this section (2026-09-11).
Review Findings: none (trivial, self-contained hook addition; no dispatch, no review round)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0, `node .kit/controller-tick-test.mjs` exit 0 (79 assertions, 0 failures, no regressions from Chapter 1's baseline). No live suite covers this section yet (see Assumptions).
Next: 3. Steering during a goal
Commit Model: Branch-and-PR

**Proof:** launched `bin/supervise.sh` with `--prompt "Please write a haiku about autumn to a file named autumn.txt ... then call goal_done."` (no tool named), persona `item2proof`. Decision log: `persona_create -> turn_start -> create -> persona_yield_commons`; `autumn.txt` was written with real content before the yield. Re-ran with a fresh persona name and a different task (`coffee.txt`, a limerick) to rule out a one-off: same result, `create` fired again with the plain-language request as the objective, `coffee.txt` written. Both runs prove the actual mechanism Item 2 asks for - `goal_create` firing from an ordinary sentence with no tool named - reliably, on two different requests, with zero special-casing in the prompt. Both runs also hit the commons-staleness defect (see Decisions / Surprises and `docs/backlog.md`) before reaching `planning_fired`/`goal_done`, which is why the decision log above stops at `create`.

### Chapter 3 - 2026-09-11
Completed: 3. Steering during a goal
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: Fixed the ask-question truncation bug named in the plan (a shared `.slice(0, 100)` fed both the terse decision-log line and the ask record's full question; split into `finalReason`/`fullReason`). Added one new tool, `goal_edit` (`nodeId`, `action: "drop" | "pause" | "reprioritize"`, `reason`), covering the three primitives the tree didn't already have; `add` already existed via `goal_add` and `resume` via `goal_resume`. `drop` sets status `"abandoned"` (a status value already declared in `GoalNode` and already respected by `activateNext`'s exclusion and the planning-cap counter, but never previously written by any code path). `reprioritize` needed a real design choice: no priority field existed anywhere, activation order was strictly `createdAt` ascending, so I added an optional `sortKey` field to `GoalNode` (falls back to `createdAt` when absent, so an untouched node's order never changes) and updated both `activateNext` sort sites plus `goal_status`'s display sort to honor it. `pause` mirrors the plan's stated semantic exactly: `goal_resume`'s own doc string says "if no node is active, resumes the most recently paused node," so pausing the active node sets `activeGoalId = null` rather than leaving a dangling reference (the existing controller-driven pause branches leave it dangling and rely on every consumer also checking `status === "active"`; both work, mine is more direct). Item 3's own new bullets (roadmap re-read at every planning event enabling a roadmap-edit steer, both mechanisms recorded) are already true of the existing planner and needed no new code.

The bigger surprise: proving this live required fixing a second defect first. Two independent live-proof attempts (the second and third times this exact bug has now surfaced, after Item 2's Chapter) hit the commons-claim staleness bug logged in `docs/backlog.md` - once from this session's own residue, once from an unrelated live sibling session's residue - and both times the scratch child got permanently demoted to a passive reader before it could reach `goal_edit` at all. A third recurrence made it a live blocker on verifying this plan's own remaining items, not a deferred nice-to-have, so I fixed it: `agentic_identity` now releases its previous persona's commons claim on every switch (the actual root cause - a switch away from a persona was never releasing that persona's claim, so the session's own routine heartbeat activity under the new persona kept the abandoned claim's shared `lastSeen` looking live forever). I also pulled forward the one small piece of Item 6 this depended on: a `persona` `userConfig` option, read at `register()`'s top level before `session.start` runs, and threaded from `supervise.sh`'s existing `PERSONA` argument into the settings JSON it already emits. Without that, a launched child always claims `default` regardless of what's passed on the command line (Item 6's own bullet on this, confirmed here independently), so no scratch persona name could ever dodge a collision with a real `default` session. The `docs/backlog.md` entry is updated to record this as fixed rather than left open.
Assumptions: No dedicated live suite exists yet for Item 3 (Item 7's job); this section's "Proof:" is the live run below, per the same division as Chapters 1-2 (2026-09-11). No harness case added for `goal_edit`, `sortKey`, the `persona` option, or the `agentic_identity` release fix; `.kit/controller-tick-test.mjs` has no existing pattern for asserting commons-store state across a simulated persona switch, and building that from scratch is scoped to Item 7 (2026-09-11). The identity-tool-description-inversion bullet and the marketplace/installed-plugin rework in Item 6 are explicitly left alone; only the `persona` option was pulled forward, because it was the one piece blocking this section's own verification (2026-09-11).
Review Findings: none (no dispatch, no review round; self-contained hook and schema changes, manually traced against every call site of `activateNext` and `emit_settings_json` before committing)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0, `node .kit/controller-tick-test.mjs` exit 0 (173 assertions, 0 failures, no regressions from Chapter 2's baseline). No live suite covers this section yet (see Assumptions).
Next: 4. Quiet between goals
Commit Model: Branch-and-PR

**Proof:** launched a scratch child with an explicit 8-step prompt (`goal_create`, three `goal_add` plans "Plan A/B/C", `goal_status`, `goal_edit` drop on Plan B, `goal_edit` pause on Plan C, `goal_status`), persona `item3proof-unused` via the new `persona` option - the first scratch run that did NOT hit the commons-staleness bug, confirming the fix. Decision log: `persona_create -> turn_start -> create -> add (Plan A) -> add (Plan B) -> add (Plan C) -> drop: plan-...-3qkg: operator dropped it -> paused_by_operator: plan-...-j64m: operator asked to pause`. Resulting tree: Plan A `[pending]` untouched (control), Plan B `[abandoned]` with `blockedReason="operator dropped it"`, Plan C `[paused]` with `blockedReason="operator asked to pause"`. Exactly matches the acceptance: a recorded decision naming the change, plus a matching change in the goal tree, for add, drop, and pause.

### Chapter 4 - 2026-09-11
Completed: 4. Quiet between goals
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: The existing `root_complete` signal conflated two different meanings: "this goal is done" and "stop the supervisor entirely." Both `bin/supervise-decide.mjs`'s decide loop and `bin/supervise.sh`'s own natural-exit fallback treated any `root_complete` as a reason to exit 0, which is exactly the behavior item 4 asks to change. Split it into two genuinely separate signals: `root_complete` alone now yields a new `restart_passive` action (stop the child gracefully via the same EOF path, but loop back to a fresh passive child instead of exiting, and never touch the crash-loop/restart-budget counters, since this is expected, healthy behavior rather than a failure), and a new `shutdown_requested` decision - written by a new `supervisor_shutdown` tool, owner-only, described explicitly as being for an operator's real stop request and never for a completed goal - takes priority over `restart_passive` and maps to the original `stop_complete` exit-0 path. Both the decide unit's priority order and supervise.sh's two call sites (the decide-driven poll loop and the natural-exit fallback for a child that dies on its own) needed the same split; missing either one would have left a real path where a completed goal could still exit the whole supervisor.

The live proof surfaced a genuine, correct safety behavior rather than a bug: sending an ambiguous "please shut down" instruction through a synthetic one-shot reader session got refused twice, once by the reader itself (treating my message as a system instruction to blindly relay, which it isn't) and once by the owner child (correctly asking for confirmation before an action it read as irreversible, exactly per the operating doctrine's own "name the rollback and stop for a yes" rule). This is the doctrine working as intended, not a defect, but it meant a live model-driven proof of the shutdown *tool call itself* would need either a much more elaborate, context-rich synthetic conversation or a live human in the loop - neither fits a scratch proof. Since the tool's own write path (append a `shutdown_requested` decision, persist) is a single, simple, fully-typed code path with nothing conversational in it, I proved the mechanism it feeds - supervise.sh reading the fact, calling decide, and taking the stop_complete exit path - directly, by writing the same decision the tool's handler writes and observing the supervisor's real reaction. What remains genuinely unverified by this session is whether a live conversational shutdown request reliably produces that tool call in practice, which is a prompt-engineering/UX question for real Discord deployment (item 5) rather than an infrastructure defect.
Assumptions: No dedicated live suite exists yet for Item 4 (Item 7's job); this section's "Proof:" is the live run below, per the same division as Chapters 1-3 (2026-09-11). The `shutdown_requested` decision in the final proof leg was written directly to the store rather than produced by a live tool call, for the reason in Decisions / Surprises; the tool registration, its handler, and the decide/supervise.sh wiring that consumes the resulting decision are otherwise exercised exactly as a real call would exercise them (2026-09-11). Whether a live model reliably calls `supervisor_shutdown` from an ordinary operator request (versus asking for confirmation first, as it correctly did here) is left as an open question for Item 5/6, not answered by this Chapter (2026-09-11).
Review Findings: none (no dispatch, no review round; self-contained decision-unit and shell changes, verified against a dedicated pure-function unit test before committing)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0, `node .kit/controller-tick-test.mjs` exit 0 (173 assertions), `node .kit/supervisor-unit-test.mjs` exit 0 (12 assertions, including two new cases added this section: the priority order between `shutdown_requested` and `root_complete`, and a stale-`root_complete` control), 0 failures across all three, no regressions from Chapter 3's baseline. No live suite covers this section yet (see Assumptions).
Next: 5. A chat channel (still blocked, see below) / 7. Proof lives in the suites
Commit Model: Branch-and-PR

**Proof:** one continuous `supervisor.log` (persona `item4proof`) covering both required events. Goal one: `--prompt` gave a plain-language haiku task; it completed (`root_complete`), and the log shows `RESTART_PASSIVE: root_complete at ... > child start ...` followed by `EXIT child-1 code=0 (eof)` and a fresh `LAUNCH child-2 (... prompt=)` - the supervisor stayed up and returned to passive rather than exiting. Goal two: a one-shot reader session called `agentic_say` with a plain-language limerick request (no tool named); the decision log shows `operator_delivered -> operator_turn_stamped -> create -> ... -> operator_answered -> ... -> root_complete` for a second, distinct root, then `RESTART_PASSIVE` and `LAUNCH child-3` again - a second goal, given by conversation, ran to completion in the same supervisor lifetime, exactly as item 2's mechanism plus item 4's return-to-passive. Shutdown: a `shutdown_requested` decision (see Decisions / Surprises for how it was produced) surfaced as `STOP_COMPLETE: shutdown_requested at ... > child start ...` followed by `EXIT child-3 code=0 (eof)`; the supervisor process and its whole child tree exited, confirmed by a clean process list. Two goals completed and one clean shutdown, in one `supervisor.log`, matching the acceptance exactly.

### Chapter 6 - 2026-09-11
Completed: 6. One command to start, and a README that says so
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: This section had a genuine sequencing dependency the plan itself names: the installed-plugin runtime switch needs a real live run to settle three unknowns before `find_global_store`'s fix or the README's install commands can be written correctly rather than guessed. That live run touches machine-global state (the operator's Claude Code plugin registry, shared across every session on the machine), so I held it for an explicit go-ahead mid-section rather than running it unilaterally; the operator granted it plainly ("Yes, you have permission to go ahead with the plugin install"). All three unknowns came back clean: the installed store's filename is `agentic-plugin_<marketplace-name>-<hash>.json` (`agentic-plugin_agent-persona-54422876af67.json` for this repo), distinct from `agentic-plugin_inline-<hash>.json` under `--plugin-dir`; the settings key `agentic-plugin` reaches an installed plugin's options with no marketplace suffix; and the cwd trap (item 1's fix) applies identically to the installed plugin's cache. `find_global_store` now takes a `dev_mode` argument and filters the glob on it rather than taking the first match, since both files can coexist on one machine once both load modes have ever run there (which they have, on this one, as of this section). `bin/supervise.sh` gained a `--dev` flag: default (no flag) omits `--plugin-dir`, loading the installed plugin (the target runtime); `--dev` adds `--plugin-dir` back for working on this checkout's own code. Both modes proved live, back to back, from the same machine.

One flake, not a regression: `npx tsc --noEmit` returned exit 1 with empty stdout/stderr on one run immediately after killing a scratch child process; two immediate re-runs with no code changes both came back clean (exit 0). No discriminating output was ever captured (the failing run printed nothing to distinguish it from a real type error), so this is named as an unexplained flake rather than root-caused, most likely transient resource contention from the process being killed moments before. Recorded per the doctrine's red-signal discipline rather than silently retried away.

Also fixed, as part of this section's own persona-option and identity-tool cleanup: `agentic_identity`'s tool description said the opposite of its actual F9 commons-arbitration behavior ("FORCEFULLY takes the persona... the previous holder is demoted" versus the real behavior of joining as a passive reader when a live holder exists and only taking ownership when none does or the holder is stale) - fixed in both the tool registration and the mirrored `.claude/types/claude-code-mcp.d.ts` declaration, plus a harness case (with a control) proving the `persona` userConfig option built in item 3 actually claims the given persona and never touches `default`.
Assumptions: The README quickstart's "under two minutes" proof used the existing checkout rather than a literal fresh `git clone`, since the marketplace-add and plugin-install steps are idempotent and had already been run live earlier in this same section; a genuinely fresh clone would re-exercise the same documented commands with no reason to expect a different result (2026-09-11). Item 7's own live suite for this section (`.kit/live-all.sh`) is not built yet, per the same division of labor as Chapters 1-4 (2026-09-11).
Review Findings: none (no dispatch, no review round; verified each claim against a live run before writing it down)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0 (after one unexplained flake, see Decisions / Surprises), `node .kit/controller-tick-test.mjs` exit 0 (178 assertions, 5 new this section), `node .kit/supervisor-unit-test.mjs` exit 0 (12 assertions, unchanged), 0 failures, no regressions from Chapter 4's baseline. No live suite covers this section yet (see Assumptions).
Next: 5. A chat channel (blocked) and 7. Proof lives in the suites
Commit Model: Branch-and-PR

**Proof:** (a) persona-option harness: 5 new assertions in `controller-tick-test.mjs`, watched passing (not watched red-then-green, since the underlying `hooks/index.ts` fix already shipped and merged in Chapter 3 - this section only added the missing coverage for it). (b) Installed-plugin live run, no `--plugin-dir` anywhere: `claude plugin marketplace add "D:\agent_persona"` then `claude plugin install agentic-plugin@agent-persona --scope user`, both succeeding; a one-shot `claude -p` session with only `--settings` (no `--plugin-dir`) successfully called `goal_status` and got `"No goal tree exists."` - proving the installed plugin's tools registered and its options were read correctly. (c) Both `bin/supervise.sh` modes proved live back to back: default mode (persona `installedproof`) reached `WAITING: child-1 alive` with `find_global_store 0` resolving to the installed-mode store file (confirmed by direct invocation, not inference); `--dev` mode (persona `devproof`) reached the same `WAITING` state with `find_global_store 1` resolving to the `inline-` store file. (d) Quickstart timing: `bin/supervise.sh` launched with the exact documented command reached `GATE PASSED`/`LAUNCH child-1` within 5 seconds of the script starting, comfortably under the two-minute proof bar.

### Chapter 5 - 2026-09-11
Completed: 5. A chat channel
Implemented By: main session
Metrics: review rounds 0, closed clean; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: Two collaborating sessions - the Reviewer (`discussion.md`, a session without the plugin loaded) and the operator's own Discord relay - drove most of this section's discovery. Confirmed live: a headless `claude -p --input-format stream-json` child does attach to the relay (`--channels plugin:relay@sapplefeld-channels`) and can send outbound through the `reply` tool from its first launch. Inbound is where the real gap was: a headless child only registers Discord channel notifications after its own first completed turn, and any message the operator sends before that turn completes is silently lost rather than queued (confirmed by the Reviewer's own controlled experiment, replicated independently on my own test child: the debug log's `Channel notifications re-registered after reconnect` line appears only after a first turn runs). `docs/architecture.md` in `discord-channels` said "the relay registers only from the interactive REPL," which reads as an outdated description of an earlier CLI; the current one (2.1.268) supports `-p`, with this one undocumented qualification.

Fixed in `bin/supervise.sh`: every child that attaches the channel with no real goal to open on - the first passive launch and every `restart_passive` child after it - now gets one priming turn over stdin before anything else, so the channel goes live before the child is truly idle. Whichever message is first (a real `--prompt` goal, or the priming message) also carries an instruction to answer through the relay's `reply` tool, since the child's own conversational reply is never visible to the operator on its own - confirmed the hard way: my first attempt's priming turn replied conversationally with nothing sent to Discord, until the instruction was added.

New CLI surface on `bin/supervise.sh`: `--channel-name NAME` (stable thread key across restarts, defaults to `supervisor-<persona>`) and `--no-channel` (skip attachment entirely, for scratch/proof runs with no Discord side effects - used throughout this whole plan's own live proofs from here on, now that channel attachment is a default rather than absent).

Also found and fixed, reported by the Reviewer via `discussion.md`: the plan's own `.claude-plugin/marketplace.json`, registered as a local-directory source (`claude plugin marketplace add /path/to/this/clone`), meant `claude plugin update` would copy whatever was checked out in that clone - uncommitted edits included - into the installed runtime, which is the exact runtime-and-edit-tree coupling the plan's own header forbids, in a form the item 6 fix didn't catch. Fixed: the README now installs from `SApplefeld/agent_persona` on GitHub (a real git clone, confirmed live), so `claude plugin update` tracks merged `main`. The local-directory form stays documented as the development shape, paired with `--dev`.

One thing genuinely not done: one persistent Discord thread across supervisor restarts. `discord-channels/broker/registry.ts` binds a thread to a session ID, and every child a restart launches is a new session, so a `restart_passive` cycle today would open a new thread per goal rather than keeping one conversation. The operator decided (relayed via the Reviewer, `discussion.md` round 2 addendum) that I build this, under its own plan document in `discord-channels/docs/plans/`, on its own branch with its own PR, archiving the stale `sapplefeld-channels_dsh-bridge_spec_v1.md` in the same PR. Not started: reading `docs/architecture.md` there (750 dense lines, every design decision reasoned about exhaustively) made clear this deserves its own careful plan rather than a patch bolted on mid-session. This Chapter's proof is therefore explicitly a single-child proof, honestly partial on the restart dimension, matching what the Reviewer recommended before the rebinding work exists.
Assumptions: The channel-reply instruction is delivered as plain text in the priming/opening turn rather than as a persona-level system instruction, since agentic-plugin has no dedicated "system prompt" surface I found for this and the plan doc gave no steer on where it belongs; revisit if it proves unreliable at scale (2026-09-11). No dedicated live suite for this section yet (Item 7's job); this section's proof is the live run below (2026-09-11).
Review Findings: none (no dispatch, no review round; every claim in this Chapter was independently verified live, several jointly with the Reviewer session, before being written down)
Stamps: none surfaced
Gate: targeted - `npx tsc --noEmit` exit 0, `node .kit/controller-tick-test.mjs` exit 0 (178 assertions), 0 failures, no regressions from Chapter 6's baseline. No live suite covers this section yet (see Assumptions).
Next: the one-thread-rebinding plan in `discord-channels` (not started), then 7. Proof lives in the suites
Commit Model: Branch-and-PR

**Proof:** a real supervised child (`supervisor-item5proof-v2-TEST`) ran the priming turn, and its debug log shows `Channel notifications re-registered after reconnect` immediately after. The operator then held a genuine, multi-turn Discord conversation with it - "Hello my friend!", "YAY! You can talk to me!", questions about its model and persona, "Sure, tell me what you see in ListAgents" - each one arriving as a real turn (`notifications/claude/channel: ...` in the debug log) and each answered successfully through the `reply` tool (`"result":"Sent to the operator's thread."` seven times running). This is the plan's own acceptance exactly: a message from a phone produces an operator turn, and the reply is visible in the thread, with no proxy session and no polling anywhere in the path.
