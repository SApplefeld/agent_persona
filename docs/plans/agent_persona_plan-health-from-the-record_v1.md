# Plan health from the record

Status: In Progress
Commit Model: Branch-and-PR
Created: 2026-09-20

## Goal

When this is done, a queue entry that carries a plan document is judged from the record. The persona plugin reads done and progress from the plan document, and it reads blocked and waiting from the first line of the worker's own closing text. No plan entry is stopped by a count of turns, and a turn the operator or the coordinator persona started costs a worker nothing. The persona's store then holds the state the worker itself would report, which is what the coordinator persona reads when it judges a worker. Three Jev questions about plan health are asked in shadow and journaled, and nothing acts on them. It matters because on 2026-09-20 both workers' in-flight plans read blocked while both workers were working normally, and the controller had stopped nudging them.

## Dispatch Authorization

The operator asked for this plan on 2026-09-20 on the architect persona's Discord thread.

> Yeah, this actually sounds like quite a defect, the measure that the Steward and Supervisor are using to measure health is quite disjoint from the measure the session itself has. I'd love your thoughts on how to address this gap.

He approved the sketch, its Assumptions and its place in the running order the same day with "Yes, they're great. Please proceed." That approval covers authoring. Execution waits on the operator handing this plan to a worker by name.

This plan starts only after `docs/plans/agent_persona_decision-seam_v1.md` has merged to the trunk, because Section 5 extends that plan's code. The check is that `hooks/decision-seam.ts`, `hooks/question-catalog.ts` and `hooks/decision-journal.ts` exist on the trunk. A worker that reaches Section 5 and finds them absent stops with a `BLOCKED:` lead naming that. It never takes them from an unmerged branch.

## Intent

**The frame.** The operator's words: the health measure the coordinator and the supervisor use "is quite disjoint from the measure the session itself has". Both workers had just told him they were "proceeding fine" while their queue files said blocked. The Steward in his words is the coordinator persona.

**What happened.** The plugin gives each queue entry ten rounds. A round is one finished turn that a classifier labels on-goal, drift or complete. At ten the entry is marked blocked with the reason `Max rounds reached` and the next entry is activated. A plan takes far more than ten turns, so every plan hits it. For the `dev` persona the tenth round was spent by the operator's own check-in, which the classifier labelled drift. With no active entry the controller sends no nudge, so a worker that goes quiet in the middle of a plan is not woken.

**What done needs to do.** Make the plan document and the worker's own words the record, and have the plugin read them. Free any entry a round count has already stopped.

**What done does not need to do.** No Jev answer changes any decision, state field or nudge in this plan. The worker gains no new tool or duty for reporting progress, because the document it already keeps is the report. Entries that carry no plan document keep their round budget. The supervisor's own checks are not touched: they read three decision actions and never the round counter.

**Alternatives refused.**
- Raising the round limit to its ceiling of 50: the same wrong measure with more room. It is the stopgap the coordinator persona applies by hand until this lands.
- A progress-report tool for the worker: a second record beside the document, which would drift from it.
- Replacing the turn scorer's classifier with Jev on the same question: a cheaper wrong measure.
- Acting on the three new Jev answers now: a probability on a health reading looks like a fact, and nothing has measured these questions yet.
- Advancing to the next entry when a worker says it is blocked: the worker's tree is mid-plan, and both workers already refuse such a start in writing.

**Rulings after the spec shipped.**

- 2026-09-21, the operator: no section is added for the false-completion defect, and Section 4 gains one acceptance bullet instead. The defect is that `hooks/index.ts:5261` runs `completeLeaf` on whichever entry was active when a turn began, whenever the turn-end classifier returns `complete`. That entry is not the one the turn concerned. Sections 3 and 4 as written already close it for every entry that has a plan, a sub-task under a plan node included, because such an entry is a plan entry by the Approach's own ancestor rule. What the plan left unpinned is that rule's reach at the scorer: Section 2 states it outright for the round budget and Section 4 does not state it for scoring, so an implementer reading Section 4 alone could key on `kind` and reinstate the defect for sub-tasks. The added bullet closes that.

- 2026-09-21, the operator, on the dev persona's Discord thread, answering Section 1's review-round backstop: continue, with three further review rounds. Order of work: finish the supervisor gaps section then in flight through its review and its Chapter, then carry this plan through before returning to supervisor gaps, taking the owed work first (the test that pins the two plan path patterns against each other, the review round the security fix earned, and the enumerated run of the nine suites once the box is clear). Ruling on the text pattern: the pattern that reads a plan path out of free prose is a one-time fill for old queue entries, the queue file is written by the persona itself, a wrong path can only name another plan document in the same repository, and the reader checks the stored path again against the strict pattern and the closed folder list before using it. So from here on, a finding on that pattern's left edge that is not Critical is accepted and declared under `## Assumptions` with the shape it admits, not fixed. A Critical there is still fixed. The architect does not edit this document while the branch carries it.

**Provenance.** Distilled from the architect persona's design conversation with the operator on 2026-09-20, and from both workers' stores and decision logs read that day.

## Approach

**The parts named here.** The controller is the plugin's idle tick, which nudges a quiet worker. The scorer is the plugin's turn-end classifier call. The coordinator persona is the fleet's coordinating session, which reads workers' stores. The supervisor is `bin/supervise.sh`, which launches and restarts a persona's session. Jev is the TypeSafe classifier service the decision seam calls.

**Three fields on a queue entry.** `planPath` is the plan document's path relative to the persona's working directory, in the form `docs/plans/<name>.md`. `lead` is null or `{ state, reason, at }`, where `state` is `blocked` or `waiting`. `chapterCount` is the number of Chapters the document held at the last read. All three are stored on the entry, so they survive a restart.

**What a plan entry is.** An entry's plan is its own `planPath`, or else the `planPath` of its nearest ancestor that has one. A plan entry, everywhere in this document, is an entry that has a plan by that rule. So a task a worker adds under its plan node is a plan entry too, judged against its parent's document. An entry with no plan is called a task entry here, whatever its `kind`.

**Where `planPath` comes from.** `goal_add` takes it as an optional parameter on an entry of kind `plan` only, and refuses it on any other kind. It is refused unless it matches `^docs/plans/[A-Za-z0-9][A-Za-z0-9._-]{0,250}\.md$`. An entry of kind `plan` loaded without one gets it filled once, from the first capture of `(?<![A-Za-z0-9._/:\-])(docs/plans/[A-Za-z0-9][A-Za-z0-9._-]{0,250}?\.md)(?![A-Za-z0-9_-]|\.[A-Za-z0-9]|/)` in its `title` and then its `objective`. Live entries write the path followed by a comma or a full stop, which that expression leaves outside the capture. Both edges are guarded, so a path sitting inside a longer token is not matched: a leading `../`, a URL prefix, a Windows path or a drive-relative `D:` on the left, and a further extension such as `.md.bak` or a longer path such as `.md/notes` on the right. The coordinator persona's standing instruction tells it to pass `planPath` when it queues a plan.

**Reading the document.** At the end of each turn, for the entry that was active when the turn started, when it is a plan entry, the plugin reads `<workdir>/<planPath>` with a 256 KiB cap. The plan is complete when the first line opening with `Status:` above the first `##` heading has the value `Complete`, trimmed, as the whole value, ignoring case. A line with markup around the key, such as `**Status:**`, is not that line. The plan is also complete when no file exists at `planPath` and a file of the same name exists at `docs/archive/<name>`, `docs/archive/plans/<name>` or `docs/plans/archive/<name>`, whatever that file's status says. The set of places read is closed at those four. Completion completes the entry that holds the `planPath` itself and runs the same steps the scorer's `complete` label runs today: `completeLeaf`, `runHealth`, a `complete` decision, `activateNext` and `activate`. A file that cannot be read, is over the cap, or is found in none of the four places changes nothing that turn and logs one `plan_record_unreadable` decision per entry per session. A document with no `Status:` line is simply not complete. The Chapter count is the number of `### Chapter N` headings under `## Chapters`. A count higher than `chapterCount` stores the new count, resets `consecutiveNudgesWithoutOnGoal`, and logs a `plan_progress` decision.

**No round budget on a plan entry.** Its `completedRounds` is never incremented, at the scorer or in the `goal_done` handler. The `Max rounds reached` block never applies to it. Its `maxRounds` is left as it is and is never read. The round text changes at all four sites that write it: the two worker-facing ones, the prompt text and the status line, omit it, and the controller's idle summary and its skip-hash subset say `plan entry, no round budget` in its place. An entry of kind `plan` that gains a `planPath` at load, and whose status is `blocked` with `blockedReason` exactly `Max rounds reached`, returns to `pending` with the reason cleared, so the controller's ordinary activation picks it up in queue order.

**The worker's lead.** The kit has a worker open its closing text with `BLOCKED:` and a reason when it cannot continue without someone else, and with `WAITING:` when background work will wake it. For a plan entry, the plugin reads the first non-blank line of a turn's closing text. A line opening with the literal uppercase `BLOCKED:` sets `lead` to blocked with the rest of that line as the reason, cut at 300 characters. One opening with `WAITING:` sets it to waiting. Any other first line clears `lead` when the turn made at least one work tool call, which is the count `toolCallsThisTurn` already holds through `isWorkTool`, so a reply to the operator clears nothing. This applies at the end of every turn on the entry, whatever opened the turn. The entry's `status` does not change, the nudge counter is not touched, and the controller does not activate another entry.

While `lead` is blocked the controller skips its whole idle branch for that entry: no classifier call and no nudge. While it is waiting the controller does the same until 60 minutes after `lead.at`, and then runs the idle branch as usual. With no lead set, the idle branch runs as it does today with one change for a plan entry: a classifier outcome of `complete` completes nothing and is logged as `complete_ignored` and sent as a nudge, because done is read from the document and a worker whose text reads finished is woken rather than left idle. The classifier's question and labels are not changed. The `ASK:` line is handled exactly as it is today.

**Which turns are scored.** A turn that opened from a channel message, or from a delivered record, is never scored, for any entry. The plugin knows both facts as `currentTurnIsChannelOrigin` and a `currentTurnKind` of `delivery`, but it resets both before the scorer runs, so Section 4 captures them beside `wasNudged`, before the resets. An unaccounted turn is one the plugin did not open and that carried no channel message. For a plan entry, only a turn the controller opened with a nudge is scored. The label array passed to the classifier is unchanged. For a plan entry, `on-goal` resets the nudge counter, `complete` neither resets it nor completes anything, and no label spends a round. The three-nudge stall pause works as it does today.

**Jev in shadow.** The decision seam handles the `choice` primitive only, in its types, its validation and its answer parsing, and its journal closes the outcome kinds at two. A Choice returns one option of a fixed set with a probability for each. A Noul returns the probability that a stated condition holds. A Score returns a position on ordered levels the question itself describes, with a probability for each level. Section 5 widens the seam and the catalog to `noul` and `score`. It adds three question sets, asks them in one request at the end of every turn on a plan entry, and journals each answer with an outcome the plugin can observe later. No value from them reaches a branch.

**The sweep.** Searches run on trunk `fd8f4cb` and re-read on `c22d0fc`: `interface GoalNode`; `maxRounds|completedRounds|Max rounds reached`; the four scorer labels; `consecutiveNudgesWithoutOnGoal|pausedByNudgeCap|wasNudged`; `currentTurnKind|currentTurnIsChannelOrigin`; `ASK:|BLOCKED:|WAITING:`; `goal_add|goal_create`; the same names over `bin/`, `docs/` and `README.md`. Surfaces found: `hooks/agent-state.ts` (the `GoalNode` interface and the v2 to v3 migration); `hooks/index.ts` (the `goal_add` schema and handler, the two `completedRounds` increments, the block, the round text at four sites, the scorer, the nudge cap, the turn-origin tracking, the `ASK:` parse, the idle branch and its no-active-leaf case); `.kit/tick-harness.mjs` `makeGoalNode`; `.kit/controller-tick-test.mjs`; `.kit/injection-ledger.json` and `.kit/injection-ledger.mjs`, which pin the size and shape of every injected string, with their totals stated at `docs/architecture.md:121`; `bin/supervise.sh` (the coordinator instruction); `README.md`; `docs/architecture.md`. Nothing under `bin/` reads the round counter, confirmed by a search that matched one comment there and 38 lines under `hooks/`. No `BLOCKED:` or `WAITING:` parse exists today. On `origin/decision-seam-build`: `hooks/decision-seam.ts`, `hooks/question-catalog.ts`, `hooks/decision-journal.ts` and their three unit tests.

## Standing Brief Amendments

- A plan entry is one that has a plan by the Approach's ancestor rule, never one whose `kind` is `plan`. Every rule this plan states for a plan entry reaches a task entry under a plan node, at the scorer and the idle branch exactly as at the round budget.
- Section 2's reader re-tests a stored `planPath` against the same pattern `goal_add` enforces, before it joins that value onto the working directory. A value that fails the re-test returns `unreadable` and logs one `plan_record_unreadable` decision per entry per session, exactly as a file absent from all four places does. The store is a producer the `goal_add` validation never sees, so a value read back out of it is untrusted at the join.
- At the scorer, a plan entry's `complete` label moves nothing: no completion, no round, no nudge-counter reset. Only `on-goal` at the scorer and a Chapter count rise at the document read reset the counter on a plan entry. So the idle branch's converted `complete` nudge counts toward the three-nudge stall pause like any other nudge, which is what bounds it.
- A blocked lead also lifts on two acts beside a working turn, amended 2026-09-21 in the finishing pass to meet Section 3's Tests line that a hold never lifting is the defect this plan removes. `goal_resume` on the entry lifts a blocked lead. The controller's idle tick lifts one once an ask on the entry has closed after the lead was set, reading the `lastAskClosedAt` every ask-close route already stamps, so the `ASK:` path itself is unchanged. Each logs `lead_cleared`. A waiting lead is lifted by neither and keeps its 60-minute hold. A reply-only turn still clears nothing.

## Sections of Work

### 1. The plan path on a queue entry
Model: sonnet

Add `planPath`, `lead` and `chapterCount` to `GoalNode`, optional, with the migration leaving them unset. Add the `planPath` parameter to the `goal_add` schema and handler with the kind rule and the pattern check. Add one helper that returns an entry's plan under the Approach's rule. On store load, fill `planPath` by the text match for any entry of kind `plan` that lacks it, and return a round-limit-blocked entry that gains one to `pending`. Add `planPath` to `makeGoalNode` as an optional argument. The `goal_add` description is an injected string, so the injection ledger is regenerated with it.

Acceptance:
- `goal_add` with kind `plan` and `planPath: "docs/plans/a_v1.md"` stores it.
- `goal_add` with `planPath` of `../x.md`, `docs/plans/sub/x.md`, `C:\x.md`, `docs/plans/x.txt` or `Docs/plans/x.md` is refused with a message naming the required form, and adds no entry. So is a valid `planPath` on kind `task`.
- An entry of kind `plan` loaded with `objective` starting `Finish docs/plans/a_v1.md, which` gains `planPath` `docs/plans/a_v1.md`. So does one whose text ends `docs/plans/a_v1.md.` with a full stop. One naming no plan gains none, and an entry of kind `task` whose text names a plan gains none.
- The helper returns the parent's `planPath` for a task under a plan node, and none for an entry with no such ancestor.
- A loaded entry of kind `plan` with status `blocked`, reason `Max rounds reached` and a filled `planPath` is `pending` with no `blockedReason` after load. The same entry with no plan in its text stays blocked.
- Every existing case in `.kit/controller-tick-test.mjs` passes unchanged, and `.kit/injection-duplicate-test.mjs` passes against the regenerated ledger.

Files in scope: `hooks/agent-state.ts`, `hooks/index.ts` (the `goal_add` schema and handler and the store load path only), `.kit/tick-harness.mjs`, `.kit/controller-tick-test.mjs`, `.kit/injection-ledger.json`, `.kit/injection-ledger.mjs`.
Tests: lock the pattern refusal, because `planPath` becomes a read path in Section 2. Lock the recovery in both directions, since an entry wrongly freed would run a task past its budget.

### 2. Done and progress from the document
Model: opus

Build `hooks/plan-record.ts`: a pure parser taking a document's text and returning `{ complete, chapters }`, and a reader taking the host's file functions, the working directory and a `planPath` and returning that result, `archived`, or `unreadable`. Call the reader at turn end for the turn-start entry when it is a plan entry, and apply the Approach's completion and progress rules. Remove the round budget from plan entries at both increment sites and the block, and change the round text at the four sites as the Approach states.

Acceptance:
- A document whose header reads `Status: Complete`, or `status:   complete  `, completes the entry holding the `planPath` at that turn's end, runs `runHealth` and activates the next entry, with a `complete` decision whose detail names the document as the cause.
- `Status: Complete (archived)`, `Status: Completed`, `Status: In Progress`, `**Status:** Complete`, and a `Status: Complete` line that sits below the first `##` heading under a header reading `Status: In Progress`, do not complete it.
- A `planPath` file that is absent, with the same name present in any one of the three archive places, completes it, including when that archived file reads `Status: In Progress`. Absent in all four places changes nothing and logs one `plan_record_unreadable` decision per entry per session.
- A Chapter count rising from 2 to 3 resets the nudge counter and logs `plan_progress`. An unchanged count logs nothing.
- A plan entry scored 25 times is never blocked, and its `completedRounds` stays 0. A `goal_done` call on a plan entry also leaves it 0. A task under a plan node is treated the same way. A task entry still blocks at its budget with today's reason.
- A worker prompt and the status line for a plan entry contain no `round` text. The idle summary for one reads `plan entry, no round budget`. For a task entry all four are unchanged.
- `.kit/injection-duplicate-test.mjs` passes against the regenerated ledger.

Files in scope: `hooks/plan-record.ts` (new), `.kit/plan-record-unit-test.mjs` (new), `hooks/index.ts`, `.kit/controller-tick-test.mjs`, `.kit/injection-ledger.json`, `.kit/injection-ledger.mjs`, `.kit/.gitignore` (folded at execution: the new suite needs an allow-line), `hooks/agent-state.ts` (folded at execution: the plan-holder walk lives beside `resolvePlanPath` rather than as a copy in `hooks/index.ts`).
Tests: lock the `Complete` rule against its near misses, since a wrong completion activates the next plan while the tree is mid-work. Lock the round budget in both directions: gone for a plan entry, intact for a task entry.

### 3. The worker's BLOCKED and WAITING leads
Model: opus

Read the lead at turn end as the Approach states, beside the existing `ASK:` parse. Skip the idle branch in the controller tick while a lead holds, under the two rules, and ignore a classifier `complete` on a plan entry. Log `lead_set`, `lead_cleared` and `complete_ignored` decisions, the first two once per change. Add one sentence to the worker-facing instruction text beside the `ASK:` teaching: the controller reads a first-line `BLOCKED:` or `WAITING:` and holds its nudges. Regenerate the injection ledger for it.

Acceptance:
- A closing text whose first non-blank line is `BLOCKED: waiting on the operator's fork` sets `lead` blocked with that reason. The idle tick after it makes no classifier call and sends no nudge, the entry stays `active`, the nudge counter is unchanged, and no other entry is activated.
- `Blocked: x`, `BLOCKED x`, the same word at the start of a later line, and the word inside a sentence each set nothing.
- `WAITING:` holds the idle branch for 60 minutes of fake clock and no longer.
- A later turn with a work tool call and no lead clears it, and the next idle tick nudges. A later channel-origin turn whose only tool call is the reply tool does not clear it.
- A lead set before a simulated restart is still set after the store reloads.
- A turn carrying both a `BLOCKED:` first line and a valid `ASK:` line opens the ask as today and sets the lead.
- With no lead set, a classifier outcome of `complete` on a plan entry leaves the entry active and logs `complete_ignored`. On a task entry it completes the entry as today.
- A task entry's closing text sets no lead.

Files in scope: `hooks/index.ts`, `.kit/controller-tick-test.mjs`, `.kit/injection-ledger.json`, `.kit/injection-ledger.mjs`.
Tests: lock the hold in both directions, since a hold that never lifts is the defect this plan removes wearing a new name. Lock the first-line rule, because a worker quoting the word in prose must not silence its own nudges. Lock that a reply to the operator does not clear a blocked lead.

### 4. Which turns are scored
Model: sonnet

Capture `currentTurnIsChannelOrigin` and whether `currentTurnKind` is `delivery` beside `wasNudged`, before the resets. At the scorer, skip with a `score_skipped` decision when either captured fact is true. For a plan entry, skip as well when the turn was not opened by a nudge. For a plan entry on a nudged turn, keep the label array and apply the Approach's reading of the labels.

Acceptance:
- A channel-origin turn and a delivery turn each log `score_skipped` and leave `scores`, `completedRounds` and the nudge counter untouched, for a plan entry and for a task entry.
- A nudged turn on a plan entry labelled `on-goal` resets the nudge counter. One labelled `complete` does not reset it and does not complete the entry, so three such turns with no Chapter rise trip the stall pause. (Amended at execution on 2026-09-21: the earlier "on-goal or complete resets" formed a loop with Section 3's converted idle complete that the stall pause never bounded.)
- Three nudged turns labelled `drift` on a plan entry trip the existing stall pause.
- An unaccounted turn on a task entry is scored exactly as today. An unaccounted turn on a plan entry is not scored.
- A task under a plan node is treated exactly as its parent plan entry is, at every rule this section states: its unaccounted turns are not scored, and a nudged turn labelled `complete` does not complete it. An entry with no plan ancestor is the only one that keeps today's scoring.

Files in scope: `hooks/index.ts`, `.kit/controller-tick-test.mjs`.
Tests: lock that an operator check-in spends nothing, which is the incident. Lock that the stall pause still trips, since it is the one health signal this plan keeps.

### 5. Three shadow questions
Model: opus

Rebase onto the trunk first, and check the precondition the Dispatch Authorization states. Read the TypeSafe API page and the three primitive pages before writing the request builder, since they are the source for each primitive's request and answer shape. Widen `ResolvedQuestion`, the resolver's validation, the seam's request builder and its answer parsing to the `noul` and `score` primitives beside `choice`. Widen the journal's closed set of outcome kinds by three. Add three shipped question sets:
- `worker-blocked`, a Noul: is this worker saying it cannot continue without someone else. State: the closing text.
- `rounds-converging`, a Score with three levels written as concrete situations: each turn closes more than it opens; the turns hold steady; the turns reopen what earlier turns settled. State: the last five closing texts for the entry, each cut at 1,000 characters.
- `block-owner`, a Choice among `operator`, `coordinator`, `another-plan`, `self-resolving` and `none`. State: the closing text.

Ask all three in one request at the end of every turn on a plan entry, not awaited. Journal each answer under a stamp id from the journal's `newStampId`. Outcomes: `lead_blocked` records whether that same turn carried a `BLOCKED:` lead; `chapter_within` records whether the Chapter count rose within the next five turns on that entry; `next_speaker` records whether the next turn was channel-origin, a delivery, or neither. The closing texts and the stamp ids awaiting an outcome are held in session memory only. A restart, or the entry completing, drops them, and the outcomes they awaited are never written, which the journal's readers already tolerate.

Acceptance:
- Every existing case in `.kit/controller-tick-test.mjs` and the three seam unit suites passes unchanged with Jev faked to fail, to hang, and to answer at each extreme: a Noul of 0 and of 1, a Score at its lowest and highest level, and each Choice option.
- `state.decisions` and every `GoalNode` field are identical between a run with the three questions on and one with `jevMode` off.
- New cases show one `call` line and three `answer` lines per plan-entry turn, the three primitives recorded by name, and each outcome kind landing once against the right stamp id.
- An entry that completes two turns after a question writes no `chapter_within` outcome for it and throws nothing.
- A task-entry turn asks none of the three.
- An override of `rounds-converging` with two levels or five is refused and falls back, by the catalog's existing rule extended to Score levels.

Files in scope: `hooks/decision-seam.ts`, `hooks/question-catalog.ts`, `hooks/decision-journal.ts`, their three unit tests under `.kit/`, `hooks/index.ts`, `.kit/controller-tick-test.mjs`.
Tests: lock decision invariance against a Jev that answers at each extreme, because a leak from shadow into a decision is the failure the decision seam plan exists to prevent. Lock that a hung request cannot delay a turn's end.
References: `https://docs.typesafe.ai/api.md`, `https://docs.typesafe.ai/primitives/noul.md`, `https://docs.typesafe.ai/primitives/score.md`, `https://docs.typesafe.ai/primitives/choice.md`, `https://docs.typesafe.ai/confidence.md`.

### 6. The documents and the coordinator's instruction
Model: sonnet
Locus: inline

`README.md` states the queue entry's three new fields, the `goal_add` parameter, what a plan entry is, how one is judged, which turns are scored, the two leads, and that a plan entry has no round budget. Its passages on rounds, at `README.md:127` and `README.md:566` on `c22d0fc`, are scoped to task entries. `docs/architecture.md` names the plan document as the record the controller reads, lists `hooks/plan-record.ts`, and restates the injection ledger's totals at `:121` as the regenerated ledger gives them. The coordinator instruction in `bin/supervise.sh` gains one sentence: pass `planPath` when queuing a plan, and read an entry's `lead` and its plan document when judging a worker, never `completedRounds`. The ledger is regenerated for that sentence. The README's decision seam section lists the three shadow questions and their outcome kinds.

Acceptance:
- `README.md` names `planPath`, `lead` and `chapterCount` and states the plan-entry rule.
- A case-insensitive search of `README.md` and `docs/architecture.md` for `round` is read hit by hit. Every hit about the round budget or the scorer's rounds says it applies to task entries, and the Chapter lists each hit with its line. The same search at the base commit is run first and returns the two README passages named above, which shows the pattern reaches them.
- The totals at `docs/architecture.md:121` equal the regenerated ledger's, and `.kit/injection-duplicate-test.mjs` passes.

Files in scope: `README.md`, `docs/architecture.md`, `bin/supervise.sh` (the coordinator instruction text only), `.kit/injection-ledger.json`, `.kit/injection-ledger.mjs`, and any `.kit/` test that pins the coordinator instruction's text.

## Related

- `docs/plans/agent_persona_decision-seam_v1.md`: supplies the Jev client, the question catalog and the journal Section 5 extends. This plan starts after it merges.
- `discord-channels` repository, `docs/plans/channels_board-worker-queues_spec_v1.md`: the Discord board card over worker queues. It prefers `planPath` when present and reads a blocked `lead` as blocked. Neither plan waits on the other.
- `docs/plans/agent_persona_supervisor-gaps_v1.md` and `docs/plans/agent_persona_supervisor-peer_v1.md`: both change `bin/supervise.sh`. This plan touches only the coordinator instruction text there, so whichever lands second rebases over a text edit.

## Out of Scope

- The supervisor's decision logic, the keeper, and `fleet_status`.
- The idle classifier's question and labels, the plan switch and the memory gate. What the controller does with a `complete` outcome on a plan entry is in Section 3.
- The `ASK:` path and the ask record.
- Acting on any Jev answer, and any threshold over one.
- `docs/archive/agentic-plugin_goal-tree_v1.md`, which restates the old field list and is history.
- Reading a plan document from a branch that is not checked out in the working directory.
- A lead on a task entry.
- `docs/README.md`, which changes only at this plan's own close-out.

## Assumptions

- assumed 2026-09-20 (source: the kit's wait rule, merged to claude-kit trunk on 2026-09-20, and its `BLOCKED:` lead): a worker opens a stopped turn's closing text with `BLOCKED:` or `WAITING:`. The `worker-blocked` question measures how often it says so another way; reversal: widen the first-line rule in Section 3.
- assumed 2026-09-20 (default): a waiting hold lifts after 60 minutes, so a wake that never comes cannot silence the controller for good; reversal: one constant.
- assumed 2026-09-20 (default): a `complete` label from the scorer or the idle classifier on a plan entry completes nothing, which keeps both label arrays and the decision seam's pin on their option ids unchanged; reversal: a third label variant and a catalog change.
- assumed 2026-09-20 (source: `goal_add`'s default kind of `task` and its demotion of a plan node when a task is added under it): a task under a plan node is judged as a plan entry against its parent's document; reversal: the helper in Section 1.
- assumed 2026-09-20 (source: both workers' pause reasons, which name branch checkouts inside their working directories): the checked-out copy of the active entry's plan document is the live one; reversal: a git read, which the plugin does not do today.
- assumed 2026-09-20 (source: `hooks/index.ts` channel-reply backfill): the operator already sees a worker's `BLOCKED:` text on the worker's own channel, so a lead opens no ask of its own; reversal: route a blocked lead through the ask path.
- assumed 2026-09-20 (source: this repository's `docs/plans/archive/`, which holds two archived plans): a completed plan can sit in any of three archive places; reversal: one entry in the closed list.
- declared 2026-09-21 (source: the operator's ruling of 2026-09-21 under `## Intent`, section 1): the load-time text pattern's left edge refuses the characters its comment lists (letters, digits, `.`, `_`, `-`, `/`, `\`, `:`) and no other, so a longer token that puts any other character immediately before `docs` (a quote, a bracket, a `%`, a non-ASCII letter) still fills `planPath` with the embedded `docs/plans/<name>.md`; the fill runs once per old entry, the persona writes its own queue file, the value can only name a plan document in this repository, and Section 2's reader re-tests it before the join; reversal: widen the lookbehind's class.
- declared 2026-09-21 (source: the operator's ruling of 2026-09-21 under `## Intent`, section 1, on review round 6's Major): the same left edge also refuses this repository's own plan named in a spelling that puts `/` or `:` before `docs`, such as `./docs/plans/a_v1.md` or `D:/agent_persona/docs/plans/a_v1.md`. An entry of kind `plan` whose title and objective name its plan only that way gains no `planPath` at load, and one frozen at the round budget stays blocked until a `planPath` is set on it another way. The failure is a miss rather than a rewrite; reversal: a second lookbehind alternative admitting `./`.

## Operator Verification

- After this merges and the fleet relaunches on it, message a worker mid-plan several times and confirm on the board or in its store that the entry stays active and never reads blocked. The work reopens if any plan entry shows `Max rounds reached` again.
- After a week, read the journal's three new question sets against their outcomes. That reading decides whether a later plan may act on any of them.

## Open Questions

None.

## Chapters

### Interim board 1 - 2026-09-21

In-flight sections: Section 1 only. It is at its fix round over review round 1's findings. No section has closed, so this entry carries no `Completed:` line.

Live dispatches: one implementer at sonnet, given the five fixes below and an explicit do-not-fix list of six held Minors, so the fix delta stays reviewable. The round 1 review of three lenses has returned and is adjudicated.

Gate baseline: the controller tick suite reads 1410 assertions and 0 failures on this tree, and 1378 with 0 failures at the base commit `8df99af`, measured by cutting a scratch worktree there and running the same suite. Both readings taken 2026-09-21T01:05Z on SCOTT-CLAUDE by this session, under this session's own heavy-process claim, uncontended, with the claim released after. The rest of the targeted lane is green at exit 0: `npx tsc --noEmit`, `node .kit/check-loader-rule.mjs`, `node .kit/injection-ledger.mjs`, `node .kit/injection-duplicate-test.mjs`.

Rulings adopted since the last boundary:

- The operator ruled on 2026-09-21 that no section is added for the false-completion defect and that Section 4 gains one acceptance bullet instead. Recorded in the `## Intent` record with its reasoning, and the Standing Brief Amendments block was created for the definition that ruling turns on.
- Review round 1 returned CHANGES_REQUIRED from the blind lens with one Critical, APPROVED_WITH_CONCERNS from the adversarial lens, and CLEAR from the security lens. One Critical and five Majors enter the fix round; eight Minors accumulate for the close pass; three findings are recorded and not fixed.
- No finding was held and no judge was convened. Every Major traces to the Goal sentence "No plan entry is stopped by a count of turns", to Section 1's own acceptance, or to the Standing Brief Amendment, so none reads as new-requirement and no design stop fired. Two Minors were upgraded on a stated consequence: a text pattern that fills a different document than the one named, and an unguarded field read that turns session start into a throw.
- The security lens's finding that the path pattern guards one producer rather than the channel is routed to Section 2, whose reader owns the join. It is named there rather than fixed here, since this section owns no reader.
- The security lens's finding that the project has no security model document is out of this plan's goal and is routed to `docs/backlog.md` at the section close.

The Critical, confirmed at the code rather than taken on report: `applyPlanRecordOnLoad` returns a round-budget-blocked entry to pending without testing its root, and `isActivationEligible` deliberately exempts the root from its status test, breaking out of its ancestor walk at the root before reading status. So a store load can resurrect work under a root already marked complete, which also defeats the planning cap. The blind lens's separate Major about a recovered node with children is the same defect from another angle: such a node is activatable by nothing, since `isActivationEligible` refuses a node with children, and the planner never runs either, since `isPlanningDue` returns false while any descendant is pending. One guard answers both.

Next action per section: read the fix round's report, verify it against the recorded baseline, then run review round 2. Round 1 returned a Critical, so round 2 runs round 1's full roster at its tier rather than a single lens. Sections 2 through 6 are unstarted. Section 5's precondition is met: pull request 57 merged at 2026-09-21T00:38Z as `622c19b`, and all three decision seam modules are confirmed present on `origin/main` by a direct read rather than inferred from the merge state.

Commit Model: Branch-and-PR

### Interim board 2 - 2026-09-21

In-flight sections: Section 1 only, at its second fix round. No section has closed, so this entry carries no `Completed:` line.

Live dispatches: one implementer at opus, given three fixes and an eight-item do-not-fix list. Review round 2 of three lenses has returned and is adjudicated, and the scope adjudicator has ruled.

Gate baseline: the controller tick suite reads 1436 assertions and 0 failures on this tree, up from 1410 before fix round 1 and 1378 at base commit `8df99af`. Read from the run's own exit code of 0. Measured 2026-09-21T01:41Z on SCOTT-CLAUDE by this session, uncontended. A foreign heavy-process claim (DEV-PLUGIN, repo `D:/personas/dev-plugin/repo`, written 2026-09-21T01:44Z) went live after that reading, so the fix round's own gate runs under the claim protocol rather than uncontended. The rest of the targeted lane is green at exit 0: `npx tsc --noEmit`, `node .kit/check-loader-rule.mjs`, `node .kit/injection-duplicate-test.mjs`.

Rulings adopted since the last boundary:

- Review round 2 returned CHANGES_REQUIRED from the adversarial and blind lenses and CONCERNS from the security lens. All three independently found one Critical, the blind lens among them, which never held the spec.
- The Critical: `applyPlanRecordOnLoad` frees a round-budget-blocked entry whose intermediate ancestor is blocked, and nothing can then activate it. `activateNext` filters each level on `pending` so it never descends past the blocked ancestor, `isActivationEligible` refuses on ancestor status, and `isPlanningDue` goes false once anything is pending. Confirmed at the code rather than taken on report. The change makes such a store strictly worse than before it: with nothing pending, the planner used to recover it.
- This is the same finding class as round 1's Critical, which was the same guard reading the root alone. Round 1 covered the root and the leaf; the span between them stayed uncovered. A repeating class means the tier is the lever, so the fix round is escalated from sonnet to opus with both rounds' evidence in the brief.
- A design stop fired on the Critical's fix, because clearing a blocked ancestor mutates a node no acceptance bullet names. The scope adjudicator ruled ACCEPT-AND-DECLARE: the plan parent's block exists only because its child was stopped at the round budget, so it is itself an entry a count of turns stopped, one hop removed, and the Goal sentence "No plan entry is stopped by a count of turns" already asks for it. The ruling's grounds were checked against the plan's own text on this side before adoption. The mechanism is one the bullets already asked for, so no acceptance bullet moved and the Standing Brief Amendments block is unchanged.
- The blind lens's Major, that the round budget stops binding across restarts, is recorded and not fixed. Section 2's acceptance removes the round budget from plan entries outright, so after that section there is no count to re-free. Both sections land in the same pull request under Branch-and-PR, so no window exists where one ships without the other.
- The security lens's Major, that a plan path is shape-checked but never existence-checked, is recorded and not fixed here. Section 2's acceptance already owns it: a path absent from all four places logs one `plan_record_unreadable` decision per entry per session.
- The three lenses' shared Minor, that the fill and the recovery share one pass and so depend on array order, is not reachable in production. Every write to a goals array across all six files under `hooks/` is an empty literal, a single-element reset or an append, and the four files beyond `agent-state.ts` and `index.ts` only read it. The invariant is unstated even so, and 27 fixtures in the tick suite build the array directly, so the two-pass split rides with this fix round rather than the close pass.

Next action per section: read the fix round's report, verify it against the 1436 baseline, then run review round 3. Round 2 returned a Critical, so round 3 runs the full roster again rather than a single lens. That will be the third round, and the operator backstop fires at the fifth. Sections 2 through 6 are unstarted.

Commit Model: Branch-and-PR

### Interim board 3 - 2026-09-21

In-flight sections: Section 1 only, at its third fix round. No section has closed, so this entry carries no `Completed:` line.

Live dispatches: one implementer at opus, given four fixes and a nine-item do-not-fix list. Review round 3 of three lenses has returned and is adjudicated.

Gate baseline: the controller tick suite reads 1473 assertions and 0 failures on this tree, up from 1436 before fix round 2, 1410 before fix round 1, and 1378 at base commit `8df99af`. Read from the run's own exit code of 0. Measured 2026-09-21T01:57Z on SCOTT-CLAUDE by this session, with no claim live on the box at the reading. `npx tsc --noEmit`, `node .kit/check-loader-rule.mjs` and `node .kit/injection-duplicate-test.mjs` each exit 0.

Rulings adopted since the last boundary:

- Review round 3 returned no Critical from any of the three lenses, which is the first round with none. The blind lens returned APPROVED_WITH_CONCERNS, the adversarial CHANGES_REQUIRED, the security CONCERNS. Three Majors enter fix round 3; the Minors accumulate for the close pass.
- The round overturns a fix adopted in round 1. Round 1's leaf guard refuses recovery for any node with children, on the stated ground that such a node can be activated by nothing. That ground is false and was adopted here without being checked. `isActivationEligible` does refuse a node with children, but `activateNext`'s DFS descends through a pending parent into its children, so a freed parent with pending children is reachable. `goal_add` with an explicit `parentId` checks the parent's existence and kind and never its status, so a blocked plan node can gain a pending child. All three confirmed by reading the code. The guard therefore refuses the one shape where freeing works, and the subtree is stranded in the same permanent idle this plan exists to remove. Fix round 3 replaces the leaf test with a reachability test and must make the result independent of the order of `state.goals`.
- The kind-rule refusal on `goal_add` names no required form, and fix round 2's test asserts the form token is absent. Section 1's acceptance bullet reads "is refused with a message naming the required form, and adds no entry. So is a valid planPath on kind `task`." Ruled here that the plain reading governs: "So is" carries the whole predicate. The message gains the form and the assertion flips.
- The text pattern's right-edge lookahead excludes neither a full stop nor a slash, so text naming `docs/plans/a_v1.md.bak` fills the path with `docs/plans/a_v1.md`, a different file, silently. Same class as the left-edge defect an earlier round closed. The repair was tested here across eight cases including both acceptance bullets before being handed over.
- The comment on `PLAN_PATH_PATTERN` calls it the path-joining guard and says a refused shape never reaches the join. That is untrue: the store is a second producer and is read verbatim with no re-test. The sentence is corrected. The validation itself stays routed to Section 2, as round 1 decided, because Section 2 owns the reader that performs the join.
- Two declared assumptions from fix round 2, both accepted: the ancestor clear does not reset `completedRounds`, because a plan parent's block is not a round-budget block and its counter is never read; and a missing parent mid-chain refuses the whole recovery, because an orphaned subtree is what the activation walk cannot reach. The second had no test, and fix round 3 adds one.
- The security lens's Major is advisory under the operator's standing instruction of 2026-09-20 that the security lens gives no recommendations and is acted on only where it finds a Critical. It found none. Its subject is the stored-value producer, already routed to Section 2. The false comment is fixed on the separate ground that nothing untrue ships.

Next action per section: read fix round 3's report, verify it against the 1473 baseline, then run review round 4. Round 3 returned no Critical, so round 4 is one lens at the writer tier rather than the full roster. That will be the fourth round; the operator backstop fires at the fifth. Sections 2 through 6 are unstarted.

Commit Model: Branch-and-PR

### Interim board 4 - 2026-09-21

In-flight sections: Section 1 only, at its fourth fix round. No section has closed, so this entry carries no `Completed:` line.

Live dispatches: one implementer at opus, given five fixes and an eight-item do-not-fix list. Review round 4 of one lens has returned and is adjudicated.

Review-round stage: four rounds have run. The operator backstop fires at the fifth adjudication that still leaves the terminal condition unmet.

Gate baseline: the controller tick suite reads 1504 assertions and 0 failures, exit 0, wall clock 38 seconds. Up from 1473 before fix round 3, 1436 before fix round 2, 1410 before fix round 1 and 1378 at base commit `8df99af`. Measured 2026-09-21T02:30Z on SCOTT-CLAUDE by this session, on the main checkout with the section's five files dirty. Read from the run's own exit code. `npx tsc --noEmit`, `node .kit/check-loader-rule.mjs`, `node .kit/injection-duplicate-test.mjs` and `node .kit/injection-ledger.mjs` each exit 0, and the ledger generator leaves `.kit/injection-ledger.json` byte-unchanged.

That reading was taken under a named contention. A foreign heavy-process claim (DEV-PLUGIN, repo `D:/personas/dev-plugin/repo`, written 02:11:23Z with `Expected-seconds: 600`) was still live 17 minutes on, and a bounded 15-minute wait for it timed out without the gate running. The box measured free at the reading: no test runner, build or msbuild process, and 19.6 GB free virtual memory. So the suite was run without writing a claim, per the protocol's wait-or-name-the-contention rule, and the foreign claim was left untouched. The 38-second wall clock is consistent with an uncontended box and is not comparable to figures recorded under the machine's earlier memory configuration.

Rulings adopted since the last boundary:

- Review round 4 returned no Critical, the second consecutive round with none. One lens ran, the adversarial reviewer at opus and effort `high` through the Workflow route, because round 3 returned no Critical and the fix delta touched code rather than a deliverable document. Verdict CHANGES_REQUIRED on two Majors.
- The first Major is a pointer left aimed at nothing. The `PLAN_PATH_PATTERN` comment states as present-tense fact that Section 2's reader re-tests a stored `planPath` at the join. Section 2's acceptance bullets carry no such requirement, confirmed here by reading them; the routing existed only in a Chapter. A Section 2 implementer reading that comment ships the join unguarded, which is a file read outside `docs/plans/`. Held to the behavior bar as a claim on a security boundary and on a published contract surface. Fixed in two places: the comment is restated as a requirement, and a Standing Brief Amendment now declares the re-test so every later sighted dispatch is built from it.
- The second Major is a missing cross-surface pin. `PLAN_PATH_TEXT_PATTERN`'s capture is a third writer of `planPath`, checked against `PLAN_PATH_PATTERN` by nothing. The two agree today, confirmed by the lens over ten prose forms, so the pin is preventive: without it a later relaxation of the text pattern writes a value `goal_add` would refuse straight into the store.
- One Minor is upgraded to Major on a stated consequence. The left-edge lookbehind omits the backslash, so `D:\other_repo\docs/plans/a_v1.md` still fills `planPath` with `docs/plans/a_v1.md`, a different file. That is the exact rewrite class the left-edge guard was added to close, on the separator this Windows host uses.
- Two further fixes ride the round: an empty or whitespace-only `planPath` is refused rather than silently treated as absent, and the shipped comments stop narrating the review rounds. The bare `F1`, `F2`, `F3(a)` and `F3(b)` tokens are unresolvable by any reader who does not hold this plan, and the before-this-fix framing is the journey the doctrine keeps out of shipped text.
- The Approach's stated fill regex is corrected to the shipped one. It still named the pre-fix expression, so the plan contradicted the code it describes. Recorded as approval drift: the Approach sits inside the approval-scoped region.
- Recorded and deliberately not fixed: the recovery writes no `decisions` entry and no `updatedAt` bump. Every lens has raised it in three consecutive rounds. Building it here adds a mechanism no Goal sentence, Intent clause or Section 1 acceptance bullet names, which would fire a design stop for a Minor. The decision-log surface belongs to Sections 3 and 4, which this plan already has. Carried to Section 3's open.
- Round 4 traced the recovery's guards against `activateNext`'s DFS, `isActivationEligible`, `isPlanningDue` and `enforceInvariants` and found them sound, and found no shape the change makes worse. The fix round 3 implementer's own inferred concern, that its child test is one level deep rather than transitive, is closed on that trace. No transitive form is built.

Record recovered this boundary: round 3's Minors and fix round 3's add-decision lines were never written to their scratch files at adjudication, and a compaction took the context holding them. All three round-3 lens reports were recovered from their transcripts on disk and written to `.kit/scratch/plan-health/`, seven open Minors and two already disposed. The lesson is the doctrine's own: a hold that lives only in context dies at the next compaction.

Next action per section: read fix round 4's report, verify it against the 1504 baseline, then run review round 5. Round 4 returned no Critical, so round 5 is one lens at the writer tier. Sections 2 through 6 are unstarted.

Commit Model: Branch-and-PR

### Interim board 5 - 2026-09-21

In-flight sections: Section 1 only, at the review-round backstop. No section has closed, so this entry carries no `Completed:` line.

Backstop stage: this is the opening bound. Section 1 has taken five review rounds and round 5's adjudication leaves the terminal condition unmet, so the run declares to the operator rather than opening the fix round that adjudication would owe. Round count as it stands: 5. The ladder has not restarted, and a continue past this bound buys three further rounds.

Live dispatches: none. The scope adjudicator dispatched on a design stop has ruled, and the expert ask sent to the repository's expert seat has been answered.

Gate baseline: the controller tick suite reads 1525 assertions and 0 failures on this tree, up from 1522 after fix round 4, 1504 before it, and 1378 at base commit `8df99af`. Read from the run's own exit code of 0. Measured 2026-09-21T02:58Z on SCOTT-CLAUDE by this session, under this session's own heavy-process claim, written after the box polled free and released at completion. `npx tsc --noEmit`, `node .kit/check-loader-rule.mjs`, `node .kit/injection-duplicate-test.mjs` and `node .kit/injection-ledger.mjs` each exit 0, and the ledger is byte-unchanged across the regen.

Owed and unrun, to be taken on the re-arm before anything else: the fix for round 5's Major 2 landed in this tree and its delta owes a review round under the fix-delta bar, because it reaches input handling, which is one of the surfaces the security lens's trigger names. This stop opens no round, so that round is owed and unrun. The fix is one character in the text pattern's left-edge lookbehind plus one refusal case and one one-character control.

Not run, and named rather than reported clean: the enumerated offline suite roster. A memory records that a targeted lane on this repository naturally reaches about seven of sixteen runnable suites and that `self-review-unit-test.mjs` is the one that bites, because `hooks/self-review.ts` is edited incidentally by work aimed at `hooks/index.ts`, which this section changes. The enumeration was prepared and not run: a foreign heavy-process claim (DEV-DISCORD, repo `sapplefeld-channels`, written 2026-09-21T03:00:14Z, 900 seconds expected) went live before the spawn. The claim protocol permits waiting or naming the contention, never writing a second claim over a live one. It is named here and is owed with the round above.

Rulings adopted since the last boundary:

- Review round 5 returned no Critical, the third consecutive round with none, from one adversarial lens at the writer tier. Two Majors and five Minors. Verdict CHANGES_REQUIRED.
- Major 2, confirmed here by probe against the literal read out of the source rather than retyped: the text pattern's left-edge lookbehind omitted `:`, so `Finish D:docs/plans/a_v1.md now` filled `planPath` with `docs/plans/a_v1.md`, a file in another tree, with no signal. That is a drive-relative Windows path. It traces to the Goal sentence "judged from the record" and its subject is input handling, which makes it a security finding of Major weight. Such a finding is never held and never parked, so it was fixed before this declaration. The lookbehind gained `:`, the comment was restated, and a refusal case plus a one-character control were added.
- A shape guard on the capture cannot stand in for that left edge. Every rewrite the lookbehind refuses yields a capture that is itself well formed, so re-testing it against `PLAN_PATH_PATTERN` passes it. Confirmed by probe. This corrects an over-generalisation in the expert seat's answer, which read both Majors as one defect.
- Major 1, confirmed here by probe: the cross-surface pin added in fix round 4 is blind to the relaxation its own comment names. Relax the text pattern's body class to admit `/` and both fill fixtures still capture a well-formed value and stay green, while `finish docs/plans/sub/x_v1.md now` writes a value `goal_add` refuses into the store. The pin asserts fixture outputs, never the relation between the two patterns.
- Major 1's repair adds a guard at the fill, which is a mechanism no acceptance bullet, Goal sentence or Intent clause names. That fired a design stop. The scope adjudicator ruled REFUSE on a form ground: the declared bullet puts the one re-test of a stored `planPath` at Section 2's reader, at the join, and Section 1's own fill bullets describe what the fill writes and name no shape refusal. A guard at the fill is therefore a second re-test at a site no bullet names. Its grounds were checked here against the plan's own text before adoption, and the check was that the proposed mechanism departs from the form those clauses ask for rather than that the clauses exist. The judge named the form the fix takes instead: a test that pins the relation between the two patterns, not a runtime branch. A refuse on a form ground closes no finding, so Major 1's fix is still owed and is owed in that form. This ruling stands as the pre-BLOCKED consult, having ruled on the mechanism the phase analysis names, so no second consult was dispatched.
- The expert seat answered the ask and named an existing source that resolves the design question's ground: the operating doctrine at line 114, "A sanitizing or clamping guard is a property of the output channel, not of the producer that first needed it. So the moment a channel gains a second producer the guard moves to the shared boundary as an exported helper." Quote verified against the file here. The load-time fill is the second producer on the `planPath` channel. That makes the guard a doctrine requirement rather than an invention, which is the ground carried into the adjudicator's brief.
- The five Minors accumulate for the close pass. Two are claim findings on comments that overstate what the code does, one is a refusal message that describes an illegal value as legal, and two are store shapes only a hand-edited store produces.

Next action per section: the backstop is declared to the operator with the phase analysis and the ruling in the body. On the answer, three things are taken before anything else: Major 1's owed fix in the form the judge named, a behavioural pin over the two patterns rather than a runtime branch; the review round the security fix's delta owes; and the enumerated offline suite roster. Sections 2 through 6 are unstarted.

Commit Model: Branch-and-PR

### Interim board 6 - 2026-09-21

In-flight sections: Section 1 only, resumed past the backstop on the operator's answer. No section has closed, so this entry carries no `Completed:` line.

Backstop stage: the operator answered continue on 2026-09-21, recorded under `## Intent`. The ladder restarts at that answer: the count stands at 5 and the continue buys three further rounds, so the next declaration point is the adjudication of round 8 that still leaves the terminal condition unmet.

Resumption. The dev session that declared the backstop was restarted in the fleet-wide restart, and this session took the plan up after closing the supervisor gaps plan's Section 1 at `12c75e1` on its own branch, per the operator's ordering. The checkout moved from `supervisor-gaps-build` to `plan-health-build` at `426c9a0`, clean. The operator's ruling on the text pattern's left edge is recorded under `## Intent` and produces one declared assumption under `## Assumptions`, dated 2026-09-21, naming the shape the lookbehind admits.

Live dispatches: fix round 5, one `implementer-opus` (the writer tier since the round-2 escalation) on the brief at `.kit/scratch/plan-health/fix-round-5-brief.txt`: the pin over the two plan path patterns in the form the judge named, a structural equality of the two bodies and a behavioral corpus of shapes `goal_add` refuses, in `.kit/controller-tick-test.mjs` alone, with a scratch mutant as the control.

Owed after it, in order: the review round the security fix's delta owes (one adversarial lens at `opus`, effort `high`, through `Workflow`, since round 5 carried no Critical), and the enumerated offline suite roster, listed from the directory rather than from a memory's count. The claims directory read empty at this boundary, so the box is clear for that run.

Next action per section: read fix round 5's report, verify it against the 1525 baseline, then dispatch review round 6, then run the enumerated roster. Sections 2 through 6 are unstarted.

Commit Model: Branch-and-PR

### Chapter 1 - 2026-09-21
Completed: 1. The plan path on a queue entry
Implemented By: implementer-sonnet (first build and fix round 1), escalated to implementer-opus at fix round 2 on a repeating Critical class and held there through fix round 5 and the close pass
Metrics: review rounds 6, closed major-closed; provenance 16 spec-traceable, 1 fix-introduced, 0 new-requirement, rulings (1 refused, 1 declared, 0 asked); advisory: 2 findings, 0 fixed, 2 deferred, 0 refused; NEEDS_CONTEXT 0; escalations 1 (sonnet to opus at fix round 2); consults 0, the round-5 backstop's pre-BLOCKED consult being the design stop's own ruling
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision lines, verbatim from `.kit/scratch/plan-health/add-decisions-section-1.md`:
  ## Section open (round 0)
  - Changes: adds `planPath`, `lead`, `chapterCount` to `GoalNode`; a `planPath` parameter on `goal_add`; an ancestor-walking helper; a store-load fill and a round-budget recovery. Serves: Section 1's own acceptance bullets, all six. Adds a mechanism: yes, and every one of them is named by an acceptance bullet, so the section's own text is the authority. Size: roughly 140 lines across two source files. Not building it: Sections 2 through 4 have no plan path to read.
  ## Fix round 1 (five fixes F1-F5)
  - F1 recovery keyed on the ancestor rule, not `kind`. Serves: Standing Brief Amendment. No new mechanism. ~3 lines. Not building it: sub-tasks under a plan node stay frozen.
  - F2 `completedRounds` reset to 0 on recovery. Serves: Goal, "No plan entry is stopped by a count of turns." No new mechanism. 1 line. Not building it: a freed entry re-blocks on its next scored turn.
  - F3(a) root-liveness guard, F3(b) leaf guard. Serves: Intent, "Free any entry a round count has already stopped." No new mechanism, both predicates read off `isPlanningDue` and `isActivationEligible`. ~4 lines. Not building it: work resurrects under a completed root, and a node with children is freed that nothing can activate.
  - F4 left-edge lookbehind on the text pattern. Serves: Section 1 acceptance, the fill bullet. No new mechanism. 1 line. Not building it: the fill stores a different document than the one named.
  - F5 nullish guard on `title` and `objective`. Serves: Section 1 acceptance, the fill bullet. No new mechanism. 2 lines. Not building it: session start throws outside the fallback and the persona does not come up.
  ## Fix round 2 (design stop fired)
  - The Critical's fix changes the recovery's ancestor guard: it walks the whole chain from the entry to the root rather than testing the root alone, and where an ancestor is `blocked` with the derived reason `Child task blocked`, it returns that ancestor to `pending` as well. An ancestor blocked for any other reason, or paused, abandoned or complete, refuses the whole recovery and the entry stays blocked. Serves: Intent, "Free any entry a round count has already stopped", and the Goal's closing sentence about workers reading blocked while working normally with the controller no longer nudging them. Adds a mechanism: contested, and this is what the stop is for. The guard half adds none. The clearing half mutates the status of a node other than the one a round count stopped, and no Goal sentence, Intent clause or acceptance bullet names an ancestor as something this section frees. Size: roughly 15 lines, 3 branches. Not building it: the section converts a tree the planner could still recover into one that idles permanently, which is strictly worse than shipping nothing.
  - The adversarial lens's Major, an exact-sentence pin on a deny message, is independent of the held mechanism. Changes: one test assertion, from full-sentence equality to the prefix its own sibling case already uses. Serves: Section 1 acceptance, the refusal bullet. No new mechanism, nothing that runs changes. 2 lines. Not building it: the wording becomes a contract nobody declared, and a reworded message reads as a behavior regression.
  ## Fix round 3 (three Majors, one prose fix)
  - The leaf guard is replaced by a reachability test. Changes: a node with children is freed when at least one child is pending once the pass is applied, rather than refused outright. Serves: Intent, "Free any entry a round count has already stopped." Adds a mechanism: no. It narrows an existing guard whose stated ground was false, and the predicate it replaces the guard with is read off activateNext's own DFS. Size: roughly 20 lines, 2 branches, plus a fixpoint sweep. Not building it: the recovery refuses the one shape where freeing works, and the subtree stays in the permanent idle this plan exists to remove.
  - The chain walk also frees an ancestor blocked `Max rounds reached` that carries a plan. Serves: the Goal sentence, "No plan entry is stopped by a count of turns", directly rather than one hop removed. Adds a mechanism: no. Such an ancestor is itself a plan entry a turn count stopped, so it is the same subject the Goal names, and the round 2 design stop already ruled accept-and-declare on the weaker case (an ancestor blocked only because its child was). Size: 2 lines, 1 branch. Not building it: the acceptance bullet asking that a frozen parent and its frozen child both recover is unreachable, since the child waits on the parent and the parent waits on the child.
  - The kind refusal names the required form. Serves: Section 1 acceptance, "is refused with a message naming the required form". Adds a mechanism: no, a static string plus an exported constant. Size: 1 line, plus flipping one test assertion from absence to presence. Not building it: the acceptance bullet's plain reading is unmet, "So is" carrying the whole predicate.
  - The right-edge lookahead excludes a further extension and a path separator. Serves: Section 1 acceptance, the fill bullet. Adds a mechanism: no. Size: 1 line. Not building it: a record naming a backup file fills planPath with the plan file beside it, silently, which is the same class the left-edge fix already closed.
  - The `PLAN_PATH_PATTERN` comment is corrected. Serves: the doctrine's nothing-untrue-ships rule rather than a plan clause. Adds a mechanism: no, nothing that runs changes. Size: prose only. Not building it: a shipped comment calls the constant the path-joining guard when the store is a second, unvalidated producer, and Section 2's implementer reads that comment.
  ## Fix round 4 (two Majors, one upgraded Minor, two prose fixes)
  - The PLAN_PATH_PATTERN comment's promise about Section 2 is made true rather than deleted. Changes: the comment states the re-test as a requirement on Section 2's reader, and a Standing Brief Amendment declares that requirement so Section 2's implementer is actually bound by it. Serves: the Goal's reliance on the store being what the coordinator reads, and the doctrine's nothing-untrue-ships rule. Adds a mechanism: no, in Section 1. The re-test itself is Section 2's work and is declared, not built here. Size: prose only in Section 1, one amendment bullet. Not building it: a shipped comment tells Section 2's implementer the join is already guarded, so the join ships unguarded and a store value joins onto the working directory untested, which is a read outside docs/plans/.
  - A cross-surface pin asserts the filled value satisfies PLAN_PATH_PATTERN. Serves: Section 1's Tests line, "lock the pattern refusal, because planPath becomes a read path in Section 2". Adds a mechanism: no, a test assertion over existing fill fixtures. Size: one assertion across the fill cases. Not building it: the text pattern is a third writer of planPath checked against nothing, so a later relaxation of its body writes a value goal_add would refuse straight into the store, and each side stays green against its own literal.
  - The lookbehind gains the backslash. Upgraded from Minor on a stated consequence: a mixed-separator Windows path such as "D:\other_repo\docs/plans/a_v1.md" fills planPath with "docs/plans/a_v1.md", a different file, on this Windows host. Serves: Section 1 acceptance, the fill bullet. Adds a mechanism: no, one character added to an existing class. Size: 1 character, plus one test case and its control. Not building it: the exact rewrite class the left-edge fix was added to close stays open on the separator this box actually uses.
  - An empty or whitespace-only planPath is refused rather than treated as absent. Serves: Section 1 acceptance, "is refused with a message naming the required form". Adds a mechanism: no, it routes an existing refusal to a case that currently falls through. Size: 1 branch. Not building it: a caller that meant to pass a path and passed an empty string is told nothing, on kind task as well as plan.
  - The shipped comments stop narrating the review rounds. Changes: "Fix round 1, F4:", "Before this fix", and the bare F1/F2/F3(a)/F3(b) labels give way to present-tense statements of the invariant. Serves: the doctrine's journey ban rather than a plan clause. Adds a mechanism: no, nothing that runs changes. Size: prose only, roughly 90 lines of comment. Not building it: live code carries plan-internal tokens no future reader can resolve, and the round history sits in the one place the doctrine says it must not.
  ## Fix round 4, recorded and NOT fixed
  - The recovery writes no decisions entry and no updatedAt bump. Raised as a Minor by every lens in rounds 2, 3 and 4. Justified not fixed: writing a decision record per freed node is a mechanism no Goal sentence, Intent clause or Section 1 acceptance bullet names, so building it here would fire a design stop for a Minor. The decision surface belongs to Sections 3 and 4, which the plan already has. Recorded in the Chapter and carried to Section 3's open.
  ## Round 5 adjudication
  add-decision (fix A, DESIGN STOP - adds a mechanism):
  - changes: applyPlanRecordOnLoad tests each captured value against
    PLAN_PATH_PATTERN before assigning planPath; a capture that fails the test
    leaves the field unset.
  - serves: Goal, "a queue entry that carries a plan document is judged from the
    record". No acceptance bullet names a guard at the fill. The operating
    doctrine does, at line 114: a guard is a property of the output channel, so
    a channel gaining a second producer moves the guard to a shared boundary.
  - adds a mechanism: YES, a guard.
  - size: about 3 lines, 1 branch.
  - cost of not building it: the fill stays a second writer to a guarded field
    with no guard. Confirmed by probe: relax the text pattern's body class and
    both fill fixtures stay green while "docs/plans/sub/x_v1.md" is written into
    the store, a value goal_add refuses.
  add-decision (fix B, BUILT - no mechanism):
  - changes: the text pattern's left-edge lookbehind gains ":"; comment restated;
    one refusal case and one one-character control added.
  - serves: Goal, "judged from the record".
  - adds a mechanism: NO. One character in an existing character class.
  - size: 1 character in the pattern, 2 test cases.
  - cost of not building it: "Finish D:docs/plans/a_v1.md now" fills planPath
    with docs/plans/a_v1.md, a different tree's file, silently. Confirmed by
    probe against the source-read literal.
  ## Round 5 ruling and fix round 5
  - Fix A was refused by the scope adjudicator on a form ground and rewritten as a test: one case pins the capture body of PLAN_PATH_TEXT_PATTERN to PLAN_PATH_PATTERN's body structurally and runs a corpus goal_add refuses through the text pattern behaviourally. Serves: Section 1's Tests line and the Goal, "judged from the record". Adds a mechanism: no, a test. Size: about 80 lines of test. Not building it: the relation between the two producers of planPath is pinned by nothing, and a relaxation of the text pattern writes a value goal_add refuses into the store with every test green.
  ## Round 6 adjudication and the close pass
  - Round 6's Major, the left edge refusing this repository's own plan in `./` or absolute spellings, is accepted and declared under `## Assumptions` on the operator's ruling of 2026-09-21, with no change to either pattern. Serves: the Intent ruling. Adds a mechanism: no. Size: prose only. Not building a fix: an entry named only that way stays unfilled, which is a miss rather than a rewrite.
  - The close pass reads a non-string title or objective as absent. Serves: Section 1 acceptance, the fill bullet, and the load path's own contract that a store it can parse never throws. Adds a mechanism: no, a type test in place of an optional chain. Size: 2 lines. Not building it: a hand-edited store with a numeric title stops the session coming up.
  Further decisions and surprises. Two Criticals in the recovery's ancestor guard were the same class from two angles (round 1 read the root alone, round 2 the leaf alone, and the span between stayed uncovered), which is what earned the escalation. The text pattern's edges took five rounds to settle and then took the operator's ruling that its left edge is declared rather than fixed further. The section reached the review-round backstop at round 5 and the operator answered continue with three further rounds; round 6 was the first of those and carried no Critical and one Major the ruling routes to a declaration, so the section closes on it with the close pass's delta under an author re-read rather than a seventh round. Round 3's, round 4's and round 5's Minors were never written to the list at adjudication and were recovered from the reviewer transcripts twice, once by the earlier session and once here. The Approach's stated fill regex drifted from the shipped pattern twice and is corrected again; it sits inside the approval-scoped region, recorded here as approval drift. Both declared assumptions on the text pattern's left edge sit under `## Assumptions` as the ruling directs. The stamp script's `now` output read four hours ahead of this machine's UTC clock in the implementers' claim records, an observation left for a later reader rather than acted on. Kaizen: none captured.
Assumptions: declared 2026-09-21 (source: the executing-work skill's fix-delta bar, section 1): the close-pass delta, a type test in place of an optional chain, a refusal message clause, and comment and test edits, sits below the owed-round bar and takes this author re-read; reversal: run one adversarial round over `5f149bf..HEAD`. The two left-edge declarations are under `## Assumptions`, dated 2026-09-21.
Review Findings: review: code pair plus security lens at opus, Workflow, rounds 1 through 3; adversarial lens alone at opus, Workflow, rounds 4 through 6; blind lens findings traced by the orchestrator (orchestrator-made). Criticals addressed: the recovery freeing an entry under a completed root (round 1); the recovery freeing an entry under a blocked ancestor that nothing could then activate (round 2). Design stops: the ancestor clear, ruled accept-and-declare by the scope adjudicator (round 2); a guard at the fill, ruled refuse on a form ground, the pin written as a test instead (round 5). Majors fixed: the ancestor rule over `kind`, the round reset, the root and leaf guards, the left-edge lookbehind, the nullish guard (round 1); the exact-sentence deny pin (round 2); the reachability test replacing the leaf guard, the kind refusal naming the form, the right-edge lookahead (round 3); the re-test promise made a requirement, the cross-surface fixture pin, the backslash in the lookbehind (round 4); the colon in the lookbehind and the relation pin (round 5). Majors justified or declared: the round budget stopping binding across restarts (round 2, Section 2 removes the budget); the left edge refusing own-tree spellings (round 6, declared under `## Assumptions` on the operator's ruling). Advisory: the security lens's shape-not-existence check on a stored path and its producer finding, both deferred to Section 2's reader, which owns the join. Minors: the list at `.kit/scratch/plan-health/minors-section-1.md` carries every round's entries with a disposition each; the close pass fixed eleven (three comments, the refusal message, the type guard, a store-only chain comment, the sentence-prefix pins, the precedence fixture) and verified the earlier rounds' fixes; nine are left with the reason on the list, among them the recovery writing no decision (carried to Section 3's open, where the decision surface lives), the reader-armed recovery view, the two-root store, the v2 migration of a frozen goal (all six persona store files on this box read version 4), and the security lens's reserved device names (advisory, unconfirmed on this box, owned by Section 2's open). No claim finding left standing false.
Stamps: adjudicated 8 project-tier reads in the window and stamped 1 (`the-kit-suite-roster-is-wider-than-the-seven-obvious-suites`, which shaped the roster run); the other 7 are peer-session and review-dispatch reads that changed nothing built here; the window is the plan's whole span, since no earlier Chapter exists
Gate: targeted lane at the closing tree on 2026-09-21 on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended: `npx tsc --noEmit` exit 0; `node .kit/controller-tick-test.mjs` 1550 OK lines, 0 FAIL, exit 0, wall clock 38 s against 38 s at Interim board 4 on the same box; `node .kit/injection-ledger.mjs` exit 0 with `.kit/injection-ledger.json` byte-unchanged; `node .kit/injection-duplicate-test.mjs` exit 0. Baseline on the same lane: 1378 at base `8df99af`, 1525 at `426c9a0`, 1545 at `5f149bf`. The enumerated offline roster, nineteen lanes listed from the directory with the natural-exit suite and the five `live-` suites deferred per the 2026-09-18 policy, ran at `5f149bf` under this session's claim with every lane at exit 0 from its own marker, logs under `.kit/scratch/plan-health/roster-20260921T100027Z`; it ran before the close pass, whose delta the targeted lane above covers. Test delta over the section: assertions 1378 to 1550; over the last two rounds three cases added (the relation pin, the non-string title, the title precedence), one retired earlier in fix round 4 (class subject-moved, the leaf-guard fixture, its subject covered by the two child-reachability cases), five edited (four sentence-prefix pins moved to a token, one comment). Every added case names the bullet or rule it pins in its title. Spawning tests added: 0; the tick suite runs against a fake host in-process. Contention: none at the reading, the claims directory empty before each run. Probe set: not the kit's repository, no run called for.
Next: 2. Done and progress from the document
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 1 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 7 - 2026-09-21

In-flight sections: Section 2 only, at its first build. Section 1 closed at `a39a538` with Chapter 1; this entry carries no `Completed:` line.

Live dispatches: one `implementer-opus` on the brief at `.kit/scratch/plan-health/section-2-brief.txt`, building `hooks/plan-record.ts`, its unit suite, the turn-end reader call beside the scorer, the round-budget removal at both increments and the block, and the four round-text sites.

Section 2 open: the add-decision line and the intake gap check are at `.kit/scratch/plan-health/add-decisions-section-2.md`. Two declared assumptions ride into the brief: the host's file API has no stat, so the 256 KiB cap is measured on the text read; and the scorer's `complete` label on a plan entry stays Section 3's to ignore. The reader is told to build no refusal beyond the amendment's pattern re-test, so the round-3 device-name Minor goes to the backlog at this section's close unless the reader's open finds more.

Gate baseline: the targeted lane at `a39a538` on 2026-09-21 on SCOTT-CLAUDE reads `npx tsc --noEmit` exit 0 and `node .kit/controller-tick-test.mjs` 1550 OK, 0 FAIL, exit 0, wall clock 38 s, uncontended under this session's claim. The enumerated offline roster of nineteen lanes read exit 0 on every lane at `5f149bf`.

First green landed at `9723438` on this session's own run: tsc, the loader rule, the new unit suite at 48, the tick suite at 1645 with 0 failures in 39 s, the ledger regenerated for the shorter goal-tree block, the duplicate test, every lane exit 0. The implementer returned NEEDS_CONTEXT on one mechanism no bullet names: a plan node's live sub-tasks when its document reads Complete and the turn-start entry was a task under it. That fired the design stop. The scope adjudicator ruled ACCEPT-AND-DECLARE on the Goal sentence "The persona's store then holds the state the worker itself would report", after one NEEDS_CONTEXT return because the first brief quoted the Approach; its GROUNDS were checked here against the plan's own text. The declared bound and the ruling's record are at `.kit/scratch/plan-health/add-decisions-section-2.md`. The implementer is resumed to build it as declared, with its pins.

The declared descendant completion landed at `8dc5321`, verified on this session's own run: tsc, loader, unit 48, tick 1659 with 0 failures in 39 s, ledger byte-unchanged, duplicate test, every lane exit 0. Review round 1 is dispatched at round 1's roster one tier above the opus writer: the adversarial and blind lenses at `fable` (frontmatter effort) and the security lens at `fable`, through the Agent tool, base ref `a39a538`, with `threat model: absent` since the repository has no security model document. The performance lens is not summoned: the delta reads one small file per turn end and walks the goal array once per completion, neither of which is a trigger named for it.

Round 1 returned and is adjudicated, 2026-09-21: security CLEAR with one advisory Minor, blind APPROVED_WITH_CONCERNS with eight Minors, adversarial APPROVED_WITH_CONCERNS with one Major and five Minors. No Critical. The Major: the Chapter heading regex refuses the colon form two of this repository's archived plans use and the kit contract permits; spec-traceable, fixed by widening the regex. One Minor folds: the duplicated ancestor walk moves to one exported helper in `hooks/agent-state.ts`, which joins Section 2's Files in scope. The fix delta is read as below the owed-round bar, so the section closes on it with an author re-read; the records are at `.kit/scratch/plan-health/add-decisions-section-2.md` and `minors-section-2.md`. The tree bracket carried one delta, this session's own board edit.

Live dispatches: the Section 2 implementer resumed on fix round 1 and the close pass (the regex, the helper fold, the holder's blockedReason, the unreadable set, the detail slice, the ledger pin).

Next action per section: verify the fix on this session's own run, author re-read, close Section 2 with its Chapter, commit and push at round 1's roster one tier up from the writer (fable for the code pair, plus the security lens since the section reads a file at a path the store supplies).

Commit Model: Branch-and-PR

### Chapter 2 - 2026-09-21
Completed: 2. Done and progress from the document
Implemented By: implementer-opus (first build, the declared addition, fix round 1 and the close pass, one agent resumed twice)
Metrics: review rounds 1, closed major-closed; provenance 1 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 1 declared, 0 asked); advisory: 1 finding, 0 fixed, 0 deferred, 0 refused, a Minor left with its reason; NEEDS_CONTEXT 1 (the implementer's report raised the design stop rather than a question); escalations none; consults 0, the design stop's judge being the scope adjudicator
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision lines and its intake gap routing, verbatim from `.kit/scratch/plan-health/add-decisions-section-2.md`:
  ## Section open
  - Changes: adds hooks/plan-record.ts (a pure parser and a host-file reader), calls the reader at turn end for the turn-start entry when it is a plan entry, applies the completion and progress rules, removes the round budget from plan entries at both increment sites and the block, and changes the round text at four sites. Serves: Section 2's seven acceptance bullets and the Goal sentences "reads done and progress from the plan document" and "No plan entry is stopped by a count of turns". Adds a mechanism: yes, and every one is named by an acceptance bullet or the Approach's three paragraphs on it. Size: roughly one new module of about 120 lines, one new unit suite, and edits at six named sites in hooks/index.ts. Not building it: every plan entry keeps hitting Max rounds reached, which is the incident.
  ## Intake gap check, Section 2 (routed 2026-09-21)
  - (b) The host's file API is exists, read and write, with no stat (hooks/index.ts dp.fs usage; .kit/tick-harness.mjs:98-108). The 256 KiB cap is therefore measured on the byte length of the text read, and a file over it is read once and treated as unreadable. Declared; reversal: a stat call if the host gains one.
  - (a) The turn-start entry is turnLeafId in hooks/index.ts, which the scorer already keys on (hooks/index.ts:5215-5231). The reader keys on the same value.
  - (a) The completion steps to mirror are the scorer's complete branch at hooks/index.ts:5266-5280: completeLeaf, runHealth, a complete decision, activateNext, activate.
  - (b) The scorer's complete label on a plan entry is Section 3's to ignore, not this section's; this section leaves the label's behaviour as it is except that no label spends a round on a plan entry. Declared.
  - (a) Reserved Windows device names in a plan path (round 3 security Minor) are not refused by the reader: the Standing Brief Amendment names the one re-test, PLAN_PATH_PATTERN, and the finding is unconfirmed on this box. Left to the backlog at this section's close if the reader's open finds nothing more.
  ## Design stop at the implementer's report (ruled 2026-09-21)
  - Changes: on completion by document, where the turn-start entry is a task under the holder, the holder's live descendants (pending, active, paused) are marked complete with a note naming the document, before the holder. Serves: the Goal sentence "The persona's store then holds the state the worker itself would report, which is what the coordinator persona reads when it judges a worker." Adds a mechanism: yes, a bounded loop over the holder's descendants. Size: about 8 lines, 1 branch. Not building it: a sub-task completion by document leaves two active nodes and a pending child under a completed plan, and isPlanningDue stays false for good.
  - Ruling: ACCEPT-AND-DECLARE by the scope adjudicator (fable), after one NEEDS_CONTEXT return because the first brief quoted the Approach. GROUNDS checked here: the Goal sentence exists at the plan's line 9 and covers the store's state after a completion; Section 2's bullet "A task under a plan node is treated the same way" and the Intent ruling of 2026-09-21 put the sub-task shape inside the section. The bound: the holder's own descendants, live statuses only, one note each, before the holder; the holder's steps unchanged; nothing outside the subtree. No bullet moves; the Chapter records it as approval drift under the quoted Goal sentence.
  ## Round 1 adjudication (2026-09-21)
  - R1-A1 (adversarial, Major, spec-traceable): the fix widens the Chapter heading regex to `\d+(?!\d)` and the Chapters heading regex to a word boundary, with unit cases for the colon form and a trailing note; serves the acceptance bullet "A Chapter count rising from 2 to 3 resets the nudge counter and logs plan_progress" and the curating-docs contract it reads; adds no mechanism; 2 characters plus 2 cases; not building it leaves a worker whose plan writes the colon form (two archived plans here do) with no progress ever read.
  - Fold (round 1, adversarial Minor): `planHolderOf` in hooks/index.ts duplicates `resolvePlanPath`'s ancestor walk. The fix moves the walk to one exported helper in hooks/agent-state.ts and derives the path from it. hooks/agent-state.ts sits in the same directory as hooks/index.ts, the fold needs no acceptance the section lacks, and the tick suite covers it, so it folds; Section 2's Files in scope line gains the file and the Chapter names the fold.
  - Round 1 carried no Critical, one Major above, and Minors from all three lenses. The fix delta (a regex, a helper move, four small close-pass edits and a ledger extractor change) touches no outward action and adds no module, and each change is exercised directly by the tick or unit suite, so it is read as below the owed-round bar and the section closes on it with an author re-read. That is a prediction, recorded as such.
  Further decisions and surprises. The implementer returned NEEDS_CONTEXT on the one mechanism the bullets did not name, a plan node's live sub-tasks at completion by document, and the design stop's judge ruled it accepted and declared under the Goal sentence quoted above; the first dispatch of that judge came back NEEDS_CONTEXT because the brief quoted the Approach, which its charter refuses, and the second ruled. Approval drift, recorded as such: the declared addition, and two folds onto the section's Files in scope line (`.kit/.gitignore` for the new suite's allow-line, `hooks/agent-state.ts` for the plan-holder walk that now lives beside `resolvePlanPath`). Round 1's Major was a regex stricter than the kit's Chapter-heading contract, and the repository's own archive held the two counter-examples. The typed host declares `fs.stat` while the plugin and the fake host use only `exists`, `read` and `write`, so the cap stays on the bytes read and the reader's comment says so. The implementer's first fixture for the blocked-holder pin used `Max rounds reached` on the blocked task, which Section 1's load-time recovery freed before the pin could fire; the fixture uses a reason the recovery leaves alone, and its comment says why. The unit suite died once in the libuv teardown assertion the backlog already records for another suite, after every assertion passed, and the rerun exited 0; the backlog entry carries the second sighting. The scorer's `complete` decision reads "completed in 0 rounds" for a plan entry until Section 3 turns that label into `complete_ignored`, transient inside one pull request. The stamp script's `now` output continues to read four hours ahead of this machine's UTC clock in claim records. Kaizen: none captured.
Assumptions: declared 2026-09-21 (source: the host's file API as used, section 2): the 256 KiB cap is enforced on the byte length of the text read, since the plugin uses no stat; reversal: a stat call. declared 2026-09-21 (default, section 2): the reader runs at every turn end that has a turn-start entry, aborted and errored turns included, because the document's state does not depend on the turn's answer; reversal: gate on the scorer's skipped flag. declared 2026-09-21 (default, section 2): "one `plan_record_unreadable` decision per entry per session" is read as once per unreadable stretch, re-armed by a successful read, keyed on the holder's id; reversal: drop the re-arm. declared 2026-09-21 (default, section 2): an archived copy is the first found of the three places, and two copies are not refused; reversal: a refusal. declared 2026-09-21 (default, section 2): a `plan_record_failed` decision names the catch around the reader, mirroring `score_failed`; reversal: reuse `score_failed`. declared 2026-09-21 (source: the executing-work skill's fix-delta bar, section 2): the fix-round delta (a regex, a helper move, four small edits and a ledger extractor change), each exercised directly by the unit or tick suite, sits below the owed-round bar and takes this author re-read; reversal: one adversarial round over `8dc5321..HEAD`.
Review Findings: review: code pair at fable, Agent tool, plus the security lens at fable, Agent tool, round 1; design stop: the descendant completion, ruling accept-and-declare by the scope adjudicator (fable), after one NEEDS_CONTEXT return. Critical: none. Major addressed: the Chapter heading regex refusing the colon form (adversarial, spec-traceable, fixed with five unit cases). Majors justified: none. Advisory: the security lens's read-before-cap cost with the device-name and pipe shapes beside it, CLEAR verdict, left with the reason on the Minor list (needs a writer inside the tree, stated in the module's comment; the device-name half goes to `docs/backlog.md` under Section 1's existing entry class at the finishing pass). Minors: 14 listed on `.kit/scratch/plan-health/minors-section-2.md`; 6 fixed in the close pass (the holder's `blockedReason` cleared on completion, the unreadable set re-armed by a read, the detail sliced to 150, the duplicated walk folded into one helper, the heading regexes, the ledger pin restored to the task-entry shape at 223 chars and 37 words); 8 left with the reason: the reader running without the scorer's `skipped` gate, the `runHealth` order inherited from the scorer's branch, the unset `chapterCount` reading as lower once per node on upgrade, archive detection by name as the Approach states it, a throw after completion leaving `activeGoalId` on a complete node (inherited), the "0 rounds" wording (transient), the security Minor above, and a blocked descendant left blocked by the ruling's bound. No claim finding left standing false.
Stamps: adjudicated 0 project-tier reads in the window since Chapter 1 (none surfaced); one stamp placed in error this section, `tick-harness-fires-session-start-at-creation-so-a-refired-case-inherits-claims`, applied before its body was read and found not to bear on this section (no case re-fires `session.start`); no verb withdraws a stamp, so this line is the correction
Gate: targeted lane at the closing tree on 2026-09-21 on SCOTT-CLAUDE under this session's own heavy-process claim after a 5 s wait on a foreign DEV-DISCORD claim: `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/plan-record-unit-test.mjs` 53 OK, 0 FAIL, exit 0 on the rerun (the first run died in the libuv teardown assertion at exit 127 after printing every assertion passed); `node .kit/controller-tick-test.mjs` 1668 OK, 0 FAIL, exit 0, wall clock 39 s against 38 s at Chapter 1 on the same box; `node .kit/injection-ledger.mjs` exit 0 with `.kit/injection-ledger.json` regenerated to pin the goal-tree block at its task-entry shape, 223 chars and 37 words, the figure the section opened at; `node .kit/injection-duplicate-test.mjs` exit 0. Baseline on the same lane: 1550 at `a39a538`, 1645 at `9723438`, 1659 at `8dc5321`. Test delta over the section: the unit suite is new at 53 checks; the tick suite gained 13 case functions (1550 to 1668 assertions), 0 retired, 0 edited; every added case names the bullet or ruling it pins in its title; the two declared-addition cases include the In Progress control. Spawning tests added: 0, both suites run in-process against fake hosts. Contention: the DEV-DISCORD claim named above, waited out before the run. Probe set: not the kit's repository, no run called for.
Next: 3. The worker's BLOCKED and WAITING leads
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 2 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 8 - 2026-09-21

In-flight sections: Section 3 only, at its first build. Sections 1 and 2 closed at `a39a538` and `6887209` with Chapters 1 and 2; this entry carries no `Completed:` line.

Live dispatches: one `implementer-opus` on the brief at `.kit/scratch/plan-health/section-3-brief.txt`, building the lead read beside the `ASK:` parse, the idle-branch hold, the `complete_ignored` branch, the three decisions, the one added sentence in the nudge frames, and the ledger regeneration.

Section 3 open: the add-decision line and the intake gap check are at `.kit/scratch/plan-health/add-decisions-section-3.md`. Section 1's carried Minor, that the load-time recovery writes no decision record, names a mechanism no Section 3 bullet names, so it is routed to `docs/backlog.md` with the reason rather than built.

Gate baseline: the targeted lane at `6887209` on 2026-09-21 on SCOTT-CLAUDE reads tsc exit 0, loader exit 0, unit 53, tick 1668 OK with 0 FAIL in 39 s, ledger exit 0, duplicate exit 0, under this session's claim.

First green landed at `1e1b616` on this session's own run: tsc, loader, unit 53, tick 1746 with 0 failures in 41 s, the ledger regenerated for the added nudge sentence (the idle-gap frame 423 to 521 chars), duplicate test, every lane exit 0. The implementer returned DONE_WITH_CONCERNS with two declared choices, both route (b): a lead line always rewrites `at`, so a fresh WAITING: restarts the hold; and a marker with leading whitespace sets nothing, mirroring the ASK: regex. It also found `docs/architecture.md:121`'s ledger totals already stale before this section, which Section 6 restates. Review round 1 is dispatched: the adversarial and blind lenses at `fable` through the Agent tool, base ref `6887209`; no advisory lens, since the section reads only the worker's own answer text and spawns nothing.

Round 1 returned and is adjudicated, 2026-09-21: both lenses APPROVED_WITH_CONCERNS, no Critical. The blind lens's Major is fixed: an idle-classifier complete on a plan entry is ignored and sent nothing, so a plan whose document never flips would never be nudged again; the verdict now converts to a nudge with the ignored record kept, the shape the neighbouring branch already uses. The adversarial lens's Major, that a turn moving the active entry mid-turn stores the lead on the turn-start entry where the hold does not read it, is justified-not-fixed: the Approach names the turn-start entry, the next nudged turn restates the lead on the new entry, and writing it elsewhere is a special case no clause names. Records at `.kit/scratch/plan-health/add-decisions-section-3.md` and `minors-section-3.md`. The tree bracket carried one delta, this session's own board edit.

Live dispatches: the Section 3 implementer resumed on fix round 1 and four close-pass Minors.

Next action per section: verify the fix on this session's own run, author re-read, close Section 3 with its Chapter, commit and push.

Commit Model: Branch-and-PR

### Chapter 3 - 2026-09-21
Completed: 3. The worker's BLOCKED and WAITING leads
Implemented By: implementer-opus (first build, fix round 1 and the close pass, one agent resumed once)
Metrics: review rounds 1, closed major-closed; provenance 2 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, no advisory lens was summoned; NEEDS_CONTEXT 0; escalations none; consults 0
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision lines and its intake gap routing, verbatim from `.kit/scratch/plan-health/add-decisions-section-3.md`:
  ## Section open
  - Changes: reads a plan entry's closing text's first non-blank line for a BLOCKED: or WAITING: lead beside the ASK: parse; holds the controller's idle branch while a lead holds (blocked indefinitely, waiting for 60 minutes); ignores a controller-classifier complete on a plan entry with a complete_ignored decision; logs lead_set and lead_cleared once per change; adds one sentence to the worker-facing ASK teaching and regenerates the ledger. Serves: Section 3's eight acceptance bullets and the Goal sentence "it reads blocked and waiting from the first line of the worker's own closing text". Adds a mechanism: yes, each named by a bullet or the Approach's "The worker's lead" paragraphs. Size: about 80 lines in hooks/index.ts plus cases. Not building it: a blocked worker keeps being nudged, and a controller complete label on a plan entry completes it off the record.
  ## Intake gap check, Section 3 (routed 2026-09-21)
  - (a) The idle branch's skip for an open ask (hooks/index.ts near 4313-4320, "D5") is the sibling shape for the lead hold; the lead check sits beside it, before the classifier call.
  - (a) The controller's complete outcome is `finalDecision === "complete"` near hooks/index.ts:4788; that branch takes the plan-entry guard and the complete_ignored decision. The scorer's complete label on a plan entry is Section 4's ("the entry is not completed by the label"), not this section's.
  - (a) isWorkTool (hooks/index.ts:316) already excludes the channel's reply tool and this plugin's own tools, so toolCallsThisTurn is the count the Approach names, unchanged.
  - (a) The ASK teaching lives in the nudge frames the ledger's extractNudgeFrames pins (.kit/injection-ledger.mjs:823); the added sentence goes there and the ledger is regenerated.
  - (b) The 60-minute waiting hold is one named constant, per the plan's Assumptions. Declared.
  - Routed out: Section 1's carried Minor, that the load-time recovery writes no decision record, names a mechanism no Section 3 bullet names (Section 3's decisions are lead_set, lead_cleared, complete_ignored). It goes to docs/backlog.md at this section's close with the reason, and is not built here.
  ## Round 1 adjudication (2026-09-21)
  - R1-B1 (blind, Major, spec-traceable): the fix converts an idle-classifier complete on a plan entry into a nudge while keeping the complete_ignored record, the shape the neighbouring branch (hooks/index.ts near 4608) uses for its own converted verdict; serves the Goal sentence "the controller had stopped nudging them" and the Intent's "a worker that goes quiet in the middle of a plan is not woken"; adds no mechanism (a verdict conversion an existing branch already performs); about 3 lines; not building it leaves a plan whose closing text reads finished but whose document never flips un-nudged for good, the incident wearing a new name.
  - R1-A1 (adversarial, Major, spec-traceable on the Goal, justified-not-fixed): the lead is written to the turn-start entry, so a turn that activates a different entry mid-turn (goal_add of a task, goal_done) and closes with BLOCKED: stores the lead where the hold does not read it. Justified: the Approach states the turn-start entry by name; the next nudged turn starts on the newly active entry, where a worker still blocked restates its lead and the hold takes it, so the cost is one nudge, bounded by the three-nudge stall pause; writing the lead elsewhere is a special case no clause names, which would fire a design stop for a one-nudge cost.
  Further decisions and surprises. The blind lens found the one liveness hole the section had opened: ignoring an idle-classifier complete on a plan entry without nudging would have left a worker whose closing text reads finished, and whose document never flips, silent for good, the incident this plan exists to remove. The fix converts the verdict to a nudge the way the neighbouring branch already converts its own, and the three-nudge stall pause bounds the repeats. Bullet 7 is met at the controller's idle branch; the scorer's own `complete` label on a plan entry still completes it until Section 4, whose bullet owns that path. The implementer's repair of a shell-escaped carriage return in the test file was checked byte by byte here: no bare carriage return and no lone line feed remain. `docs/architecture.md:121`'s ledger totals were already stale before this section and are Section 6's to restate. Kaizen: none captured.
Assumptions: declared 2026-09-21 (default, section 3): a lead line always rewrites the lead's `at`, so a fresh `WAITING:` restarts the 60-minute hold; reversal: keep `at` on a same-state re-read. declared 2026-09-21 (source: the `ASK:` parse's own `^ASK:` anchor, section 3): a marker with leading whitespace sets nothing; reversal: trim before the match. declared 2026-09-21 (source: the executing-work skill's fix-delta bar, section 3): the fix-round delta, a verdict conversion the neighbouring branch already performs, a gate on the hold, a trailing carriage return stripped and two comments, each exercised directly by the tick suite, sits below the owed-round bar and takes this author re-read; reversal: one adversarial round over `1e1b616..HEAD`.
Review Findings: review: code pair at fable, Agent tool, round 1. Critical: none. Major addressed: an idle-classifier complete on a plan entry left the worker un-nudged for good (blind, trace orchestrator-made to the Intent's "a worker that goes quiet in the middle of a plan is not woken"), fixed by converting the verdict to a nudge with the ignored record kept. Major justified: a turn that moves the active entry mid-turn stores its lead on the turn-start entry (adversarial), on the Approach's named rule and a one-nudge cost the next turn repairs. Minors: 7 listed on `.kit/scratch/plan-health/minors-section-3.md`; 4 fixed in the close pass (the hold gated on a plan entry, a stray carriage return stripped, two comments); 3 left with the reason: the unbounded blocked hold (the Approach's rule and the plan's assumption that the operator sees the worker's text), the silent `at` refresh (declared), and the scorer's `complete` label (Section 4's). No claim finding left standing false.
Stamps: adjudicated 0 project-tier reads in the window since Chapter 2 (none surfaced)
Gate: targeted lane at the closing tree on 2026-09-21 on SCOTT-CLAUDE under this session's own heavy-process claim, uncontended: `npx tsc --noEmit` exit 0; `node .kit/check-loader-rule.mjs` exit 0; `node .kit/plan-record-unit-test.mjs` 53 OK, 0 FAIL, exit 0; `node .kit/controller-tick-test.mjs` 1762 OK, 0 FAIL, exit 0, wall clock 41 s against 39 s at Chapter 2 on the same box; `node .kit/injection-ledger.mjs` exit 0 with the ledger byte-unchanged since first green, where the idle-gap nudge frame moved from 423 to 521 chars for the one added sentence; `node .kit/injection-duplicate-test.mjs` exit 0. Baseline on the same lane: 1668 at `6887209`, 1746 at `1e1b616`. Test delta over the section: 10 case functions added (1668 to 1762 assertions), 0 retired, 3 edited in the fix round (the complete case gains its nudge assertion, the near-miss case gains the carriage-return check, one comment); every added case names the bullet or finding it pins. The repeat case raises the hourly nudge cap to 10 so the stall pause, not the hourly cap, is what stops it, and says so. Spawning tests added: 0. Contention: none. Probe set: not the kit's repository, no run called for.
Next: 4. Which turns are scored
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 3 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 9 - 2026-09-21

In-flight sections: Section 4 only, at its first build. Sections 1 through 3 closed at `a39a538`, `6887209` and `24ab378` with Chapters 1 to 3; this entry carries no `Completed:` line.

Live dispatches: one `implementer-sonnet` on the brief at `.kit/scratch/plan-health/section-4-brief.txt`, adding the channel-origin and delivery captures beside `wasNudged`, the scorer skips, and the plan-entry label rules, with the cases.

Section 4 open: the add-decision line and the intake gap check are at `.kit/scratch/plan-health/add-decisions-section-4.md`. The scorer's `complete` branch change also closes Section 3's recorded Minor that bullet 7 was met only at the idle branch.

Gate baseline: the targeted lane at `24ab378` on 2026-09-21 on SCOTT-CLAUDE reads tsc, loader, unit 53, tick 1762 OK with 0 FAIL in 41 s, ledger and duplicate test, every lane exit 0, under this session's claim.

First green landed at `ff4d4d6` on this session's own run: tsc, loader, unit 53, tick 1828 with 0 failures in 44 s, ledger byte-unchanged, duplicate test, every lane exit 0. One Section 2 case, which counted scores on unaccounted turns, was updated to the new rule and flagged in the implementer's report. The implementer's edit left the test file's worktree line endings at LF; the committed content is unaffected, since the index stores LF, and the worktree copy is restored to CRLF and hashes to the committed blob. Review round 1 is dispatched: the code pair at `opus`, effort `high`, through `Workflow` run `wf_ebd84821-14f`, base ref `24ab378`.

Round 1 returned and is adjudicated, 2026-09-21: both lenses CHANGES_REQUIRED, no Critical, three Majors. Two are test gaps (the plan-entry round budget and the task-entry scorer complete each lost their only scored-turn pin) and are fixed with pins. The third, from the blind lens: a plan entry's scorer `complete` resetting the nudge counter, together with Section 3's converted idle `complete`, looped a worker that believes it is done at one nudge every five minutes, with the stall pause never tripping. That fix contradicted the letter of Section 4's second bullet, so a fresh-context consultant ruled: Option A, a plan entry's `complete` at the scorer resets nothing. Adopted as a declared resolution and written before the fix dispatch: Section 4's second bullet and two Approach sentences amended in place, a third Standing Brief Amendment added, recorded as approval drift. The consultant's found item, a nudge text naming the document, is in `docs/backlog.md`. Records at `.kit/scratch/plan-health/add-decisions-section-4.md` and `minors-section-4.md`.

Live dispatches: the Section 4 implementer resumed on fix round 1: the ruling, both pins, and five Minors. Fix round 1 returned and was verified on this session's own run (tick 1844, every lane exit 0) and committed at `457e875`; review round 2 is dispatched through `Workflow` run `wf_b3b799f5-5a7`.

Queue changes, 2026-09-21. The coordinator steered the dev persona to queue `docs/plans/agent_persona_keeper-park_v1.md`, merged to trunk at `21a5ee2`; it is `plan-mub8p4og-5lgf`, paused behind goal tree curation, on its own Dispatch Authorization. The running plugin's three-nudge stall pause had paused this plan's entry while its reviews ran, since the waiting replies read as drift under the scoring this plan replaces; the entry is resumed.

Next action per section: verify the fix on this session's own run, then review round 2 over the fix delta, taken by choice below the owed-round bar because the ruling changed behaviour a bullet names and the defect was one the suite had routed around: one adversarial lens at the writer's tier (`sonnet`, effort `high`, through `Workflow`), since round 1 carried no Critical. Close on that round's adjudication.

Commit Model: Branch-and-PR

### Chapter 4 - 2026-09-21
Completed: 4. Which turns are scored
Implemented By: implementer-sonnet (first build and fix round 1, one agent resumed once); the round-2 prose Minor inline in the main session
Metrics: review rounds 2, closed claim-exit; provenance 3 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, no advisory lens was summoned; NEEDS_CONTEXT 0; escalations none; consults 1, the complete-label reset against the stall pause, adopted as a declared resolution
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision lines and its intake gap routing, verbatim from `.kit/scratch/plan-health/add-decisions-section-4.md`:
  ## Section open
  - Changes: captures the channel-origin flag and whether the turn kind is delivery beside wasNudged, before their resets; at the scorer, skips with a score_skipped decision when either is true, for every entry; for a plan entry, skips as well when the turn was not opened by a nudge; for a plan entry on a nudged turn, on-goal and complete both reset the nudge counter and complete completes nothing. Serves: Section 4's five acceptance bullets and the Goal sentence "a turn the operator or the coordinator persona started costs a worker nothing". Adds a mechanism: no beyond what the bullets name. Size: about 30 lines in hooks/index.ts plus cases. Not building it: an operator check-in is scored as drift, which is the incident, and the scorer's complete label still completes a plan entry off the record.
  ## Intake gap check, Section 4 (routed 2026-09-21)
  - (a) The captures: wasNudged at hooks/index.ts:5132 beside the currentTurnKind reset at 5133; currentTurnIsChannelOrigin is reset at 5181; the scorer runs at 5345, after both. The Approach names exactly this.
  - (a) A plan entry is resolved through isPlanEntry, per the first Standing Brief Amendment, so a task under a plan node is scored as its parent is (the added bullet from the Intent ruling of 2026-09-21).
  - (a) The scorer's complete branch is at hooks/index.ts:5395; for a plan entry it completes nothing, which also closes Section 3's recorded Minor that bullet 7 was met only at the idle branch.
  - (b) A score_skipped decision's detail names which fact skipped it (channel-origin, delivery, or not nudged on a plan entry). Declared.
  ## Round 1 adjudication (2026-09-21)
  - R1-A1 (adversarial, Major, spec-traceable): the fix drives the edited Section 2 case's 25 turns as nudged turns (or adds the asserts to a nudged Section 4 case run past maxRounds), asserting 25 scores, completedRounds 0 and no block on a plan entry; serves Section 2's bullet "A plan entry scored 25 times is never blocked" and the Goal "No plan entry is stopped by a count of turns"; adds no mechanism (a test); about 30 lines; not building it leaves both round-budget guards deletable with the suite green.
  - R1-A2 (adversarial, Major, spec-traceable): the fix runs the task-entry shape through the nudged complete leg, asserting it completes, logs complete and activates the next; serves Section 4's bullet "An entry with no plan ancestor is the only one that keeps today's scoring"; adds no mechanism (a test); about 15 lines; not building it leaves the task-entry complete branch deletable with the suite green.
  - R1-B1 (blind, Major, orchestrator-traced to Section 4's Tests line "Lock that the stall pause still trips, since it is the one health signal this plan keeps"): a plan entry's scorer complete resetting the counter, together with Section 3's idle conversion, loops a worker that believes it is done at one nudge every five minutes. The fix contradicts the letter of Section 4's second bullet, so a fresh-context consultant rules before anything is written.
  - R1-B1 ruling (consultant, fable, 2026-09-21): Option A. A plan entry's complete label at the scorer resets nothing; only on-goal at the scorer and a Chapter rise at the document read reset the counter. The fix removes `|| (label === "complete" && planEntry)` from the reset condition; adds no mechanism and removes one clause; not building it loops a worker that believes it is done at one nudge every five minutes. Adopted as a declared resolution: Section 4's second bullet, the Approach's two sentences and a third Standing Brief Amendment are amended in place, recorded as approval drift. Grounds checked here: the three plan sentences existed verbatim, the Tests line names the stall pause as the plan's one kept health signal, and the loop is on the code at the lines the ruling cites.
  Further decisions and surprises. The section's own second bullet, as written, formed a loop with Section 3's fix: an idle complete converted to a nudge, a worker answering "already done", and the scorer's complete resetting the counter, so the stall pause the Tests line calls "the one health signal this plan keeps" never tripped and the worker was nudged every five minutes. The consultant found the cause in the plan's history: the reset clause was written when an idle complete sent nothing, so it was inert until Section 3's fix gave it a nudge to reset. Approval drift, recorded as such: Section 4's second bullet and two Approach sentences amended in place, and a third Standing Brief Amendment added, all before the fix dispatch. One Section 2 case, which counted scores on unaccounted turns, lost its meaning under this section and was rewritten to drive 25 nudged, scored turns, restoring what Section 2's bullet names. The implementer's first build left the test file's worktree line endings at LF; the committed content was unaffected and the worktree copy was restored to CRLF. The running plugin's own stall pause paused this plan's entry while its reviews ran, since the waiting replies read as drift under the scoring this plan replaces, which is the incident this section fixes, seen live; the entry was resumed. The coordinator's steer queued the keeper park plan, paused behind goal tree curation on its own Dispatch Authorization. Kaizen: none captured.
Assumptions: declared 2026-09-21 (default, section 4): a `score_skipped` decision's detail names the fact that skipped it (a channel message, a delivered record, or a plan entry's turn not opened by a nudge); reversal: one detail string. declared 2026-09-21 (source: the consultant's ruling, section 4): a plan entry's `complete` label at the scorer moves nothing, now the third Standing Brief Amendment; reversal: restore the clause and revisit Section 3's conversion.
Review Findings: review: code pair at opus, effort high, `Workflow`, round 1; adversarial lens at sonnet, effort high, `Workflow`, round 2 over the fix delta, taken by choice below the owed-round bar. Critical: none in either round. Majors addressed: the round budget's lost scored-turn pin (adversarial), the task-entry scorer complete's lost pin (adversarial), and the complete-label reset loop (blind, trace orchestrator-made to Section 4's Tests line "Lock that the stall pause still trips"), the last through a consultant's ruling that amended the bullet. Majors justified: none. Minors: 7 listed on `.kit/scratch/plan-health/minors-section-4.md`; 6 fixed (the channel/delivery stub, two history comments, the re-indent, the nudge-first ordering with its pin, the narrowed check-in comment, and round 2's two history comments inline); 1 left with the reason: a channel-origin or delivery turn clears nothing on a task entry, since the Approach says such a turn is never scored for any entry. No claim finding left standing false.
Stamps: adjudicated 0 project-tier reads in the window since Chapter 3 (none surfaced)
Gate: targeted lane on 2026-09-21 on SCOTT-CLAUDE under this session's own heavy-process claim after waits on foreign claims: at `457e875`, `npx tsc --noEmit` exit 0, `node .kit/check-loader-rule.mjs` exit 0, `node .kit/plan-record-unit-test.mjs` 53 OK exit 0, `node .kit/controller-tick-test.mjs` 1844 OK, 0 FAIL, exit 0 in 47 s, `node .kit/injection-ledger.mjs` exit 0 with the ledger byte-unchanged, `node .kit/injection-duplicate-test.mjs` exit 0; at the closing tree after the inline comment edits, the tick suite again 1844 OK, 0 FAIL, exit 0 in 47 s. Baseline on the same lane: 1762 at `24ab378`, 1828 at `ff4d4d6`. Wall clock 47 s against 41 s at Chapter 3, under the same box with foreign claims cycling between runs. Test delta over the section: 7 case functions added (1762 to 1844 assertions), 0 retired, 2 edited (the Section 2 round-budget case rewritten to scored turns, restoring its bullet's meaning; the nudged-label case renamed and rewritten to the amended rule); every added case names the bullet or ruling it pins. Spawning tests added: 0. Probe set: not the kit's repository, no run called for.
Next: 5. Three shadow questions
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 4 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 10 - 2026-09-21

In-flight sections: Section 5, at its precondition. Sections 1 through 4 closed at `a39a538`, `6887209`, `24ab378` and `54bf5e4` with Chapters 1 to 4; this entry carries no `Completed:` line.

Section 5's precondition. Its body says to rebase onto trunk first and check that `hooks/decision-seam.ts`, `hooks/question-catalog.ts` and `hooks/decision-journal.ts` exist there. All three exist on `origin/main` at `21a5ee2`, read directly. This branch was cut before the decision seam merged, so it lacked all three. A rebase would rewrite the pushed `plan-health-build` and need a force push, which stays outside this run's authority, so trunk is merged into the branch instead, following the decision seam plan's own precedent (its commit `cfc692b`). Declared deviation from the body's word "rebase"; the precondition it serves holds either way. The merge conflicted in two files: `hooks/index.ts`, resolved by keeping this branch's scorer (the skips, the plan-entry gates, the re-indented block) and splicing in trunk's four scorer changes (the label constants, the shadow classifier call over the same bytes, its outcome joiner, and the joiner reset in the catch); and `.kit/controller-tick-test.mjs`, resolved as the union of both import lines. `npx tsc --noEmit` exit 0 on the resolved tree.

Live dispatches: the enumerated offline roster on the merged tree, under this session's claim, since a merge takes the whole gate.

The roster read every lane exit 0: twenty-four lanes on the merged tree, the tick suite at 1910 OK with 0 FAIL, the six live suites deferred per policy, logs under `.kit/scratch/plan-health/roster-20260921T125403Z`. The merge committed at `0e5dafa` and is pushed.

Section 5 open: the add-decision line and the intake gap check are at `.kit/scratch/plan-health/add-decisions-section-5.md`. One declared default rides into the brief: one request carries one `state`, so that state is an object with `closingText` and `recentClosingTexts` fields the three questions refer to by name, on the vendor API page's own statement that a state may be structured. The five reference pages the section names are fetched under `.kit/scratch/plan-health/s5-refs/` for the implementer, which has no web tool.

Live dispatches: one `implementer-opus` on the brief at `.kit/scratch/plan-health/section-5-brief.txt`.

Queue change, 2026-09-21. The coordinator steered the dev persona to queue `docs/plans/agent_persona_goal-levels_v1.md` (PR 61), with the operator's confirmation reported; it is `plan-mubdglgm-7yq4`, paused fifth on its own Dispatch Authorization, after this plan, goal tree curation and keeper park. The running plugin's stall pause paused this plan's entry again during the build; resumed. A coordinator addendum the same day: pull request 63 records the operator's ruling on the goal levels plan's Section 6, and the plan text on trunk still tells a worker to skip that section; the goal levels node stays paused and is not started until that pull request has merged and the plan is re-read from trunk. That pull request merged at `23aa100`, and the plan re-read from trunk there carries ruling 8 under its Intent and says Section 6 runs on it, so the hold is lifted; the node stays paused fifth by order alone.

First green landed at `57ac701` on this session's own run: tsc, loader, seam 249, catalog 147, journal 137, plan-record, tick 2014 OK with 0 FAIL in 57 s against 1910 at `0e5dafa`, ledger byte-unchanged, duplicate test, every lane exit 0; the nine files wholly CRLF with non-ASCII counts equal to HEAD's. The implementer returned DONE_WITH_CONCERNS with five declared defaults, all route (b), recorded at `.kit/scratch/plan-health/add-decisions-section-5.md`. Review round 1 dispatched at round 1's roster one tier above the opus writer: the code pair at `fable` and the security lens at `fable` through the Agent tool, base ref `5c7434b`, with `threat model: absent` and the operator's accepted body-retention decision of 2026-09-20 as a deployment fact; the performance lens not summoned, since the delta makes one non-awaited request per plan-entry turn end and none of its triggers holds.

Round 1 returned and is adjudicated, 2026-09-21: adversarial APPROVED with five Minors, blind CHANGES_REQUIRED with two Majors, security CLEAR with two Minors; no Critical. Both Majors confirmed at the code: the chapter_within record is keyed by the turn leaf while the rise is measured on the plan holder, so a sibling task's stamps miss a rise read on another sibling's turn; and the closing text rides the request and the journal's uncut state column whole while the sibling scorer cuts at 1,000. Both spec-traceable on orchestrator-made traces. Three Minors ride the fix round (the session map pruned for ended entries, the catalog's duplicate level bounds imported from the seam, a broken harness comment); the rest are left with reasons, one report-only item on the journal reader's join order. Records at `.kit/scratch/plan-health/add-decisions-section-5.md` and `minors-section-5.md`; the round's capture at `.kit/scratch/plan-health/section-5/fix-round-1.diff`. The tree bracket was clean.

Live dispatches: one `implementer-opus` on fix round 1, brief at `.kit/scratch/plan-health/section-5-fix-round-1-brief.txt`.

Next action: verify the fix on this session's own run, commit it, then review round 2 over the fix delta, owed because the closing-text cut changes the vendor request's payload: one adversarial lens at the writer's tier (`opus`, effort `high`, through `Workflow`), round 1 having carried no Critical. Close on that round's adjudication.

Commit Model: Branch-and-PR

### Interim board 11 - 2026-09-21
Section 6, the documents and the coordinator's instruction, run inline in the main thread at the session's model (opus) under the docs/ routing override, `Locus: inline` recorded on the section. First green landed at `4d63ccc`, pushed level with origin: README.md (the plan-entry section under Architecture, the rounds hits scoped to task entries, `hooks/plan-record.ts` in the Files table, the decision seam section's plan-health site, primitives, `askAll`, structured state, answer and outcome columns, the level-count rule and the egress paragraph), docs/architecture.md (the plan document as the record, `hooks/plan-record.ts`, the ledger totals 27,532 to 27,964 with the coordinator row 7,193 to 7,399 and the tool descriptions 10,176 to 10,304), bin/supervise.sh (one sentence in `COORDINATOR_ROLE_INSTRUCTION`, 4,719 to 4,925 characters), and the ledger regenerated. Targeted lane at `4d63ccc` under no claim (no suite spawned, the two shell suites and the duplicate test run in seconds): `.kit/channel-reply-instruction-test.sh` 126 OK exit 0, `.kit/settings-plugin-key-test.sh` 148 OK exit 0, `node .kit/injection-duplicate-test.mjs` exit 0, `node .kit/injection-ledger.mjs` exit 0, `npx tsc --noEmit` exit 0, `node .kit/check-loader-rule.mjs` exit 0. The rounds search at the base commit `7dc427e` reached the two passages the section names at their current lines, README.md:128 and :573. Live dispatches: review round 1, the adversarial and blind lenses at `fable` through the Agent tool over `7dc427e..4d63ccc`, no advisory lens on Section 3's precedent for an injected-sentence change; the blind brief lists README.md, bin/supervise.sh and the ledger and omits docs/architecture.md. Rulings adopted since Interim board 10: none. Section 6's add-decision line and its intake gap routing are at `.kit/scratch/plan-health/add-decisions-section-6.md`. Next action: adjudicate round 1, fix, close gate, Chapter 6, then finishing-work.

### Interim board 12 - 2026-09-21
Finishing pass in progress. Base ref `21a5ee2`, the merge-base of `plan-health-build` with `origin/main`; the changeset listing matches the union of the Files in scope lines plus the plan doc, `docs/backlog.md` (deferred findings filed by Sections 2 to 4) and the untracked `.claude/worktrees/` directories.
Step 1: the QA verifier ran the enumerated offline roster at `c955714`, 24 lanes, every lane exit 0 read from its own marker by this session (`.kit/scratch/plan-health/roster-20260921T171306Z`), tick suite 2031 OK and 0 FAIL, six `live-` and natural-exit lanes deferred per the 2026-09-18 policy; every acceptance bullet of Sections 1 to 6 PASS. It ran unclaimed after waiting out two unclaimed foreign test runs and an ARCHITECT claim for another repository, named rather than overwritten. Tree bracket unchanged.
Steps 2 and 3: security, performance and adversarial lenses at fable, effort high, through Workflow run `wf_89d7cee1-0e8`. Tree bracket unchanged. No Critical.
Major (adversarial), spec-traceable to Section 3's Tests line "a hold that never lifts is the defect this plan removes wearing a new name": a blocked lead survives `goal_resume` and the ask close, so an entry whose post-answer turn is reply-only is held idle with no bound. Add-decision: Changes: hooks/index.ts (clear `lead` with a `lead_cleared` decision on `goal_resume` and on the ask close) and two tick cases. Serves: Section 3's Tests line above. Mechanism: none new, two more clear sites for the existing field and decision. Size: about 20 lines plus two cases. Not building it: the defect the plan removes survives on one path. Dispatched to implementer-opus as finishing fix round 1.
Advisory dispositions. Security: the ungated document read, fix (folded with the adversarial Minor M1); the README recovery sentence, fix (M2); the symlinked oversize plan file read whole before the cap refuses it, refused, since the file sits in the worker's own repository and the exposure is self-inflicted and bounded to that session. Performance: the ungated document read, fix (M1); the journal's whole-file rewrite per line, now seven per plan-entry turn, deferred to `docs/backlog.md`, since it is the journal module's existing write shape and sits off the awaited path; the tick suite's real timers and uncleared 5 s race timer, deferred to `docs/backlog.md`.
Minors for the one pass after the fix path closes: `.kit/scratch/plan-health/finishing/minors.md`, six items.

### Chapter 5 - 2026-09-21
Completed: 5. Three shadow questions
Implemented By: implementer-opus (first build, one agent; fix round 1, a second agent on a fresh brief); the round-2 comment Minor inline in the main session
Metrics: review rounds 2, closed claim-exit; provenance 2 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, the security lens's two Minors on the Minor list; NEEDS_CONTEXT 0; escalations none; consults 0
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision lines and its intake gap routing, verbatim from `.kit/scratch/plan-health/add-decisions-section-5.md`:
  # Add-decision lines, Section 5
  ## Section open
  - Changes: widens ResolvedQuestion, the catalog resolver's validation, the seam's request builder and its answer parsing from the choice primitive alone to noul and score beside it; widens the journal's closed outcome kinds by three (lead_blocked, chapter_within, next_speaker); adds three shipped question sets (worker-blocked, rounds-converging, block-owner); asks all three in one request at the end of every turn on a plan entry, not awaited; journals each answer and its outcome. Serves: Section 5's six acceptance bullets and the Goal sentence "Three Jev questions about plan health are asked in shadow and journaled, and nothing acts on them". Adds a mechanism: yes, each named by a bullet or the section body. Size: about 250 lines across the three seam modules and hooks/index.ts, plus cases in four suites. Not building it: the plan's shadow measurement does not exist, and the Operator Verification's one-week reading has nothing to read.
  ## Intake gap check, Section 5 (routed 2026-09-21)
  - (a) Precondition: the three decision-seam modules exist on origin/main at 21a5ee2, read directly; trunk was merged into the branch at 0e5dafa rather than rebased, recorded on Interim board 10.
  - (b) One request carries one `state` (the API page, .kit/scratch/plan-health/s5-refs/api.md: "Evaluate a `state` against a map of typed `questions`"), while two questions read the closing text and one reads the last five closing texts. The state is one object with two fields, `closingText` and `recentClosingTexts`, and each question's instructions name the field it reads; the API page states an object can hold data the questions refer to. Declared; reversal: two requests.
  - (a) The key scrub the seam applies to every state (the decision-seam plan's ruling, commit d3d7bc2) covers the new state because it goes through the same ask path; no second scrub is written.
  - (a) The worker's closing text already leaves the machine in the turn-score state today (hooks/index.ts, the scorer's scoreState), and the operator accepted TypeSafe as the one vendor on 2026-09-20 (operator memory source-code-may-leave-the-lan-to-typesafe); the new states carry the same class of text, so no new egress class is opened. Section 6 states it in the README.
  - (b) The three outcomes are written by the plugin from facts it already holds: lead_blocked at the same turn end from the lead read, chapter_within from the Chapter count read over the next five turns on the entry, next_speaker at the next turn end from the captured origin facts. Declared.
  ## Implementer report, first build (2026-09-21, DONE_WITH_CONCERNS, first green at 57ac701)
  - Add-decision: none; every mechanism built is named by a Section 5 bullet, the section body or the brief's design constraints.
  - Declared defaults, all route (b), for the Chapter's Assumptions line:
    - (b) a turn on which the entry completes or is abandoned asks nothing and drops the hold; a skipped (aborted or errored) turn asks nothing, matching the other shadow sites' `!skipped` gate.
    - (b) `next_speaker` is written at the next turn end whatever entry that turn is on, and even when that turn is skipped, since the origin is known then.
    - (b) on a Chapter rise, every stamp still waiting gets `chapter_within: true`, each once.
    - (b) the call line's `questionSet` is the three ids joined by commas, site `plan-health`; the answer lines carry `haikuValue: null` and `agrees: null`.
    - (b) the journal imports `QUESTION_PRIMITIVES` at runtime from the seam to single-source the closed set; the loader rule passes.
  - Test-contract delta: five closed-set pins amended in place because the section widens the sets they pin (catalog Tests 2a, 2d/2e/2f, 2h; journal's closed-kinds pin, two to five). The sweep fake widened so the whole tick suite runs at each extreme (pick:0..pick:4).
  - Vendor pages: choice shape agrees with the existing parser, unchanged; a Noul answer carries no confidence (primitives_noul.md:354); Score bounds 2..10 levels keyed by level number as a string (primitives_score.md:564,595,664).
  - Section 6 wording owed (README decision seam section): the three question ids, the `plan-health` site, the three outcome kinds, the answer line's `primitive` column, the structured state fields `closingText` and `recentClosingTexts`, and the rule that a Score override keeps its level count.
  - The brief's `node kit-registry-stamp.js` needs its `now` verb; the implementer read it from the verb.
  ## Round 1 adjudication (2026-09-21; adversarial APPROVED, blind CHANGES_REQUIRED, security CLEAR; no Critical)
  - R1-B1 (blind, Major, behavior, spec-traceable, trace orchestrator-made to Section 5's bullet "each outcome kind landing once against the right stamp id", its body "chapter_within records whether the Chapter count rose within the next five turns on that entry", and the first Standing Brief Amendment): the held record is keyed by turnLeaf.id (index.ts:6007-6009) while the rise is measured on the plan holder's chapterCount (5887, 6013), so with two task entries under one holder a rise read on task A's turn is invisible to task B's pending stamps, which land chapter_within false. Confirmed at the code. Fix: each pending entry stores the holder's chapterCount at call time, and the joiner compares the holder's current count against that. Adds a mechanism: no, it changes what is compared. Size: about 4 lines plus one case. Not building it: a sub-task's chapter_within outcome is a false negative whenever a sibling's turn read the rise.
  - R1-B2 (blind, Major, behavior, spec-traceable, trace orchestrator-made to Section 5's own state bound "each cut at 1,000 characters" on the same request state and the Goal's "asked in shadow and journaled"; the security lens's first Minor is the same defect and is dispositioned covered): closingText rides the request and the journal's uncut state column whole (index.ts:6035; decision-journal.ts:395) while the sibling scorer cuts at 1,000 (5728), so a long answer grows the day file on every later append and can 422 the whole three-question call. Confirmed at the code. Fix: closingText is the same 1,000-character cut the recent list already holds (the last element of record.closingTexts). Adds a mechanism: no, an existing bound applied to the second field of the same state. Size: 1 line. Not building it: cost and a whole-call failure on a long answer. Declared (b): "the closing text" in the section's State line is read as its first 1,000 characters, matching the recent list's bound.
  - Minors fixed in the fix round rather than a later close pass, since a round follows: the session map pruned in the joiner pass for any held entry that is complete, abandoned or absent from the tree (all three lenses; the section's own clause "the entry completing drops them" names it, so no new mechanism); the catalog imports the seam's SCORE_MIN_LEVELS and SCORE_MAX_LEVELS in place of its own pair (adversarial); the harness sweep comment re-wrapped (adversarial).
  - Minors left with the reason: score epsilon above the top level (adversarial, low confidence; the vendor's score is a probability-weighted position and cannot exceed the top index); the UTF-16 surrogate at the cut (adversarial, inherited from the memory-kind site, not new here); duplicate resolved ids and empty asks in askAll (blind, unreachable from the shipped three ids, a guard no clause names); the 0-based score position (blind, low confidence; confirmed 0-based by the adversarial lens against api.md's Score answer, so disposed); outcome-before-call ordering (blind): the implementer confirms the journal reader joins outcome to call by stamp id rather than by file order and reports; if it does not, that becomes a fix.
  - Fix-delta bar: the closingText cut changes the payload of the vendor request, an outward action, so the fix delta owes a round. Round 2: one adversarial lens at the writer's tier (opus, effort high, Workflow), round 1 having carried no Critical.
  Further decisions and surprises. The section body says to rebase onto the trunk first; a rebase would have rewritten the pushed branch and needed a force push, which stays outside this run's authority, so trunk was merged in at `0e5dafa` instead, on the decision seam plan's own precedent, and the precondition the body serves held either way (Interim board 10 carries the merge and its two conflict resolutions). The blind lens, holding no spec, found both Majors: the chapter_within record was keyed by the turn leaf while the rise was measured on the plan holder, so under the first Standing Brief Amendment's ancestor rule two sub-tasks under one plan could not both see a rise; and the closing text rode the request and the journal's uncut state column whole while the sibling scorer cuts at 1,000. The fix-round implementer waited fifteen minutes on a foreign `npm test` run outside this repository that held no claim file and whose CPU never moved, then proceeded and named it. Its first claim write ran the stamp script without its `now` verb and left `Started:` empty for one run; it rewrote the claim before the second. The fix-round implementer also confirmed that no reader under `hooks/` or `bin/` joins an outcome line to its call, so an outcome written before its call line breaks nothing that exists; the test helpers that do join do so by stamp id. The vendor pages settled two shapes the seam's comments now state: a Noul answer carries no confidence, and a Score's probabilities are keyed by level index as strings with the score a 0-based position. The close gate ran under a foreign heavy-process claim from the dev-plugin persona's finishing whole gate the protocol's fifteen-minute wait timed out and the run proceeded without a claim, named on the Gate line. The coordinator's two steers during the section (the goal levels hold and its lift after pull request 63 merged at `23aa100`) are on Interim board 10. The running plugin's stall pause paused this plan's entry once more during the build, and its self-review then wrote a kaizen node (`plan-mubft0y7-gqzw`, turns past an hour) that took the active slot at the section's close, the shape the goal levels plan answers; the entry was resumed both times and the kaizen node is paused. Kaizen: none captured.
Assumptions: declared 2026-09-21 (default, section 5): one request carries one `state`, an object with `closingText` and `recentClosingTexts` fields the three questions name, on the vendor API page's statement that a state may be structured; reversal: three requests. declared 2026-09-21 (source: review round 2's claim Minor, section 5): "the closing text" in the section's State lines is read as its first 1,000 characters, the same cut the recent list carries, so the request body and the journal's uncut state column are bounded; reversal: one constant. declared 2026-09-21 (default, section 5): a turn on which the entry completes or is abandoned asks nothing and drops the hold, and a skipped turn asks nothing, matching the other shadow sites' gate; reversal: ask on the completing turn. declared 2026-09-21 (default, section 5): `next_speaker` is written at the next turn end whatever entry that turn is on, skipped turns included; reversal: gate on the same entry. declared 2026-09-21 (default, section 5): on a Chapter rise every stamp still waiting for the entry gets `chapter_within` true, each once; reversal: the newest only. declared 2026-09-21 (default, section 5): the call line's `questionSet` is the three ids joined by commas under site `plan-health`, and the answer lines carry `haikuValue` and `agrees` null; reversal: three call lines. declared 2026-09-21 (default, section 5): the journal imports the closed primitive set from the seam at runtime rather than restating it; reversal: a copy and a pin. declared 2026-09-21 (source: the executing-work skill's fix-delta bar, section 5): the close-pass delta, one comment, sits below the owed-round bar and takes this author re-read; reversal: one adversarial round over `8b893a0..HEAD`.
Review Findings: review: code pair at fable, Agent tool, plus the security lens at fable, Agent tool, round 1; adversarial lens at opus, effort high, `Workflow`, round 2 over the fix delta, owed because the closing-text cut changes the vendor request's payload. Critical: none in either round. Majors addressed: the chapter_within rise measured against the wrong count under sibling entries (blind, trace orchestrator-made to Section 5's bullet "each outcome kind landing once against the right stamp id" and the first Standing Brief Amendment); the closing text sent and journaled uncut (blind, trace orchestrator-made to the section's own 1,000-character bound and the Goal's "asked in shadow and journaled"; the security lens's first Minor is the same defect, dispositioned covered). Majors justified: none. Advisory: the security lens's CLEAR verdict with two Minors, the uncut text (covered) and the session map hold (fixed as a Minor). Minors: 14 listed on `.kit/scratch/plan-health/minors-section-5.md` across the three lenses and round 2; 5 fixed (the session map pruned for ended entries, raised by all three lenses; the catalog's level bounds imported from the seam; the harness comment; the constant's comment inline in the close pass; the uncut text, covered by the Major); 1 declared (the cut, on the Assumptions line); 8 left with the reason (the score epsilon, the UTF-16 cut boundary inherited from the memory-kind site, duplicate resolved ids and empty asks in `askAll` as guards no clause names and unreachable from the shipped ids, the 0-based score confirmed against the API page, outcome-before-call ordering with no reader that joins by order, and the two same-subject duplicates). No claim finding left standing false.
Stamps: adjudicated 15 operator-tier reads and 0 project-tier reads in the window since Chapter 4, stamped 0 at this boundary, all being peer sessions' reads of records that changed nothing built here; two records were stamped applied as they shaped the section, `the-task-output-path-is-not-the-growth-artifact` (the implementer's liveness reading after the compaction) and `typesafe-request-body-retention-risk-accepted` (the security lens's deployment facts)
Gate: targeted lane on 2026-09-21 on SCOTT-CLAUDE. At `57ac701` under this session's own claim, uncontended: `npx tsc --noEmit` exit 0, `node .kit/check-loader-rule.mjs` exit 0, `node .kit/decision-seam-unit-test.mjs` 249 OK exit 0, `node .kit/question-catalog-unit-test.mjs` 147 OK exit 0, `node .kit/decision-journal-unit-test.mjs` 137 OK exit 0, `node .kit/plan-record-unit-test.mjs` PASS exit 0, `node .kit/controller-tick-test.mjs` 2014 OK, 0 FAIL, exit 0 in 57 s, `node .kit/injection-ledger.mjs` exit 0 with the ledger byte-unchanged at md5 `83a2cf0e`, `node .kit/injection-duplicate-test.mjs` exit 0. At `8b893a0`, the same lane under this session's own claim: every lane exit 0 with the tick suite at 2031 OK, 0 FAIL in 57 s and the three seam suites unchanged at 249, 147 and 137. At the closing tree after the inline comment edit: tsc exit 0, loader exit 0, tick `node .kit/controller-tick-test.mjs` 2031 OK, 0 FAIL, exit 0 in 60 s. Baseline on the same lane: 1910 at `0e5dafa` after the trunk merge, 1844 at `54bf5e4`. The implementer's own first-build sweep ran the tick suite nine times at the closing tree of the build (plain, fail, hang, opposite, pick:0 to pick:4), every run 2014 OK, 0 FAIL, exit 0 in 56 to 58 s, logs at `.kit/scratch/plan-health/s5-tick-final-*.log`; the fix-round implementer ran it once more under pick:1 at 2031. Test delta over the section: 9 case functions added to the tick suite (7 at the build, 2 in the fix round; 1910 to 2031 assertions), 5 blocks to the seam suite (163 to 249), 3 to the catalog suite (102 to 147), 3 to the journal suite (123 to 137); 0 retired; 7 edited (five closed-set pins widened because the section widens the sets they pin: catalog Tests 2a, 2d to 2f, 2h and the journal's closed-kinds pin; catalog Test 9j reading the seam's level bounds; the one-call-three-answers case gaining the cut pin); every added case names the bullet or finding it pins in its title. Spawning tests added: 0. Contention: the closing tick run waited the protocol's fifteen minutes on a live foreign claim (the dev-plugin persona's finishing whole gate, `Expected-seconds: 2400`) and then ran without writing a claim, naming it here; the earlier runs were uncontended under this session's own claim. Probe set: not the kit's repository, no run called for.
Next: 6. The documents and the coordinator's instruction
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 5 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 6 - 2026-09-21
Completed: 6. The documents and the coordinator's instruction
Implemented By: main session (inline, opus, under the docs/ routing override; `Locus: inline` recorded on the section)
Metrics: review rounds 1, closed claim-exit; provenance 0 spec-traceable, 0 fix-introduced, 0 new-requirement, rulings (0 refused, 0 declared, 0 asked); advisory: 0 findings, 0 fixed, 0 deferred, 0 refused; NEEDS_CONTEXT 0; escalations none; consults 0
Recap: omitted from a section Chapter
Decisions / Surprises: the section's add-decision line and its intake gap routing, verbatim from `.kit/scratch/plan-health/add-decisions-section-6.md`:
  Section open (2026-09-21): Changes: README.md (the plan-entry fields, the goal_add parameter, the plan-entry rule, how a plan entry is judged, which turns are scored, the two leads, no round budget, the rounds passages scoped to task entries, hooks/plan-record.ts in the Files table, the decision seam section's three questions, site, outcome kinds, primitive column, state fields, level-count rule and the egress statement); docs/architecture.md (the plan document as the record the controller reads, hooks/plan-record.ts, the ledger totals restated); bin/supervise.sh (one sentence in COORDINATOR_ROLE_INSTRUCTION); .kit/injection-ledger.json regenerated. Serves: Section 6's three acceptance bullets and the Goal sentence "The persona's store then holds the state the worker itself would report, which is what the coordinator persona reads when it judges a worker." Mechanism: none, prose and one injected sentence; nothing new runs. Size: about 6,000 bytes of prose and one sentence of roughly 200 characters in the priming text. Cost of not building: the README and the coordinator keep describing the round budget for every entry, and the coordinator keeps judging a worker from a counter the plugin no longer moves.
  Further decisions and surprises. The rounds search at the base commit `7dc427e` reached both passages the section names, at their current lines README.md:128 (the controller summary's Node row) and :573 (the Section 13 test line); every hit about the round budget or the scorer's rounds now says it applies to a task entry, at `4d63ccc` at README.md:128, 159, 169, 171, 175, 596, 720 (scoped by 726, which leaves the shipped `ask-operator` wording as the record), 798 (the sample call line, scoped by 801) and 878; the remaining hits are the kit's review and fix rounds (314), the coordinator's steer rounds (314, 509), `rounds-converging` (637, 730, 763), round trips, round brackets, and `ground`, `background` and `around`; docs/architecture.md holds no hit on a round at all. The ledger totals the section names at `docs/architecture.md:121` sat at :143 on this tree and were already stale before the section, as an earlier Chapter recorded; they now read 39 entries, 28,021 characters, 13,309 across the ten `bin/supervise.sh` entries, 14,712 across the twenty-nine `hooks/index.ts` entries, 10,304 across the fourteen tool descriptions, and 7,456 for the coordinator launch row, each recomputed from the regenerated ledger. The coordinator sentence was reworded in the close pass: the coordinator queues nothing itself, since `goal_add` writes the caller's own tree and a worker queues its own plans on a coordinator steer, so the sentence now has the worker pass the path relative to its own repository (blind lens). The adversarial lens asked whether the document read on an aborted turn or a non-owner session is a Section 2 code gap; it is not, since `persist` returns early for a non-owner session (`hooks/index.ts:1671`) and a document read on an aborted turn reads the record the turn left, so the README now states the read's real scope. Kaizen: none captured.
Assumptions: declared 2026-09-21 (default, section 6): the plan-entry rules live in one new README subsection under Architecture, "Plan entries: judged from the record", since the README carried no `goal_add` passage to extend; reversal: move it. declared 2026-09-21 (default, section 6): a hit that quotes shipped text verbatim (the `ask-operator` option description, the sample journal line) is scoped by the sentence beside it rather than reworded, since the quoted string is the record of what ships; reversal: none needed. declared 2026-09-21 (default, section 6): the section ran inline in the main thread rather than taking a sonnet draft, since every sentence was checked against code read in the same turn and the docs/ write is the main thread's; reversal: a sonnet draft. declared 2026-09-21 (source: review round 1's claim Minor, section 6): the coordinator instruction's plan-entry duty gains one pin in `.kit/channel-reply-instruction-test.sh`, on the file's own one-case-per-duty rule, which the section's Files in scope names; reversal: delete the case.
Review Findings: review: code pair at fable, Agent tool, round 1; blind brief omitted docs/architecture.md and the blind lens left `.kit/injection-ledger.json` unread under its own charter, the adversarial lens reading both. No advisory lens: the delta spawns nothing and touches no guard, grant or boundary; its one injected sentence carries no interpolation. Critical: none. Majors: none. Minors: 11 listed on `.kit/scratch/plan-health/minors-section-6.md` across the two lenses, 9 distinct after two overlaps (the recovery sentence and the unreadable latch, each raised by both); 9 fixed in the close pass (the recovery by the ancestor rule with its two conditions and the round reset; the unreadable latch per plan holder and re-armed by a readable read; the lead read's skipped-turn and owner gates; the document read's scope, every turn end; `chapter_within` counting aborted turns; `next_speaker` not dropped by a completing entry; which question-adding steps apply to an outcome-measured question; the coordinator sentence naming the worker and its repository; the missing duty pin), 0 upgraded, 0 left. The close pass is prose plus one test case, below the fix-delta bar, and took this author's re-read of the delta against the code lines each finding cited. The pin was watched red (the sentence removed in a probe: exit 1, the case's FAIL line) and green (exit 0, 127 OK), the probe's file restored from its pre-probe copy and compared byte-identical with `cmp`.
Stamps: adjudicated 1 operator-tier read and 0 project-tier reads over `--since 1h`, the smallest window the tool takes, which covers Chapter 5's commit 20 minutes earlier; stamped 0, the one read (`kit-memory-database-host`) being a peer session's and shaping nothing built here
Gate: targeted lane on 2026-09-21 on SCOTT-CLAUDE, no claim written and none standing (no suite of minutes spawned; the lane runs in seconds). At `4d63ccc`: `bash .kit/channel-reply-instruction-test.sh` 126 OK exit 0, `bash .kit/settings-plugin-key-test.sh` 148 OK exit 0, `node .kit/injection-duplicate-test.mjs` exit 0, `node .kit/injection-ledger.mjs` exit 0, `npx tsc --noEmit` exit 0, `node .kit/check-loader-rule.mjs` exit 0. At the closing tree after the close pass: the same lane, `.kit/channel-reply-instruction-test.sh` 127 OK exit 0, settings 148 OK exit 0, duplicate test exit 0, ledger exit 0, tsc exit 0, loader exit 0. Baseline on the same lane: no run at `7dc427e`; the first-green run at `4d63ccc`, 126 and 148 with no run failing, stands as the baseline, since neither suite's own file had changed from the base there. Test delta: 1 case added to `.kit/channel-reply-instruction-test.sh`, pinning the coordinator instruction's plan-entry duty (Section 6's coordinator sentence); 0 retired; 0 edited. Spawning tests added: 0. The tick suite and the four unit suites were not run, since the section touches no file they read: `hooks/` is unchanged since `7dc427e`. Contention: none observed. Probe set: not the kit's repository, no run called for.
Next: finishing-work
Commit Model: Branch-and-PR
Delta: 2026-09-21 on SCOTT-CLAUDE, worktree at the closing tree of Section 6 before its commit, no foreign dirty files under the measured roots; the reading `node <plugin-root>/scripts/kit-size.js report --repo D:/agent_persona` prints:
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```
