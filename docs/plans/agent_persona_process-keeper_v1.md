# Process keeper: personas as scheduled tasks that come back on their own

Status: In Progress
Commit Model: Branch-and-PR
Disjoint: yes
Created: 2026-09-15

## Goal

When this is done, every persona in the machine's roster runs under a Windows scheduled task that starts at boot with no logon, restarts the supervisor on the exit codes that mean "try again" with a growing delay, holds on the exit codes that mean "stop", and leaves a log and a state file the operator can read. The supervisor itself gains the three self-heal fixes the keeper depends on: no orphaned `claude.exe` at a natural-exit relaunch, the restart and crash limits enforced on the decide path, and a rate-limited or credential-swapped child handled instead of read as a hang. It matters because today the fleet comes back only when the operator opens a terminal and runs a launcher by hand, so a reboot or a crash at night takes the fleet down until morning.

## Dispatch Authorization

The operator authorized this plan for execution on 2026-09-15 on the authoring session's own Discord thread, the relay channel the operator steers that session from, for any session that holds it, and named the dev persona as its executor. The operator's words, in order: "Yes, let's make a plan to build the process keeper", then "Can you plan this work, and hand it off to Persona-Dev to start building?". The expert session authored this plan and this section and does not execute the plan. The receiving session traces the grant to the commit that lands this file and to the operator's own word on its channel, and arms only where both hold.

## Approach

The keeper is three PowerShell scripts in this repo's `bin/`, one roster file and one environment file on the machine, and one scheduled task per persona. The supervisor (`bin/supervise.sh`) is unchanged in shape: it still runs one child, decides restart or stop, and exits with the code its exit table already defines. The keeper wraps it, and the wrapper's whole input is that exit code.

**Why a wrapper with its own loop rather than the scheduler's restart alone.** Task Scheduler's restart-on-failure setting restarts a task that ended badly, at a fixed interval, up to a count. That is the right backstop for the wrapper crashing, and the wrong policy for the supervisor: a supervisor that exits because the operator asked it to shut down must stay down, one that exits on a usage error must not be retried every minute, and one that exits on a crash loop should come back later with a growing delay. Those are three different answers to three exit codes, so the policy lives in a decision function the wrapper runs, and the scheduler only relaunches the wrapper.

**Where it lives.** In this repo's `bin/`, beside the supervisor it launches, because the exit-code contract it reads is this repo's and a change to that table changes the keeper in the same commit. The operator's decision of 2026-08-30 says machine customization converges on the kit repo and its doctor. The keeper is not customization of a session; it is this program's own launcher. What the kit should gain is a doctor health step that reads the keeper's state file, and that is a follow-on for the kit seat, recorded under Open Questions.

**The roster.** One JSON file, default `D:/personas/fleet.json`, listing the personas the machine runs. Each entry maps to the supervisor's own argument and environment contract, and the wrapper sets a variable only where the entry carries the field:

| Roster field | Reaches the supervisor as | Required |
|---|---|---|
| `name` | the `<persona>` argument | yes |
| `workdir` | the `<workdir>` argument | yes |
| `permissionMode` | the `<permission-mode>` argument | yes |
| `rundir` | `--rundir` | no, the supervisor defaults to `<workdir>/run` |
| `channelName` | `--channel-name`; absent means the supervisor's own default thread name, `supervisor-<persona>`, so an entry without it keeps the thread the persona has today | no |
| `model` | the `MODEL` environment variable | no |
| `effort` | the `EFFORT` environment variable | no |
| `controllerTickMs` | the `controllerTickMs` environment variable | no |
| `coordinatorPersona` | the `COORDINATOR_PERSONA` environment variable, by the same path the coordinator launcher uses today | no |
| `args` | any further supervisor flag the table has no row for, appended verbatim; today that is `--dev` and `--no-channel`, and `--prompt` is refused, the build function throwing on it and naming the roster path | no |
| `enabled` | whether a task is registered and enabled | yes |

The three launchers under `D:/personas` today (`aios/launch.sh`, `coordinator/launch.sh`, `dev/relaunch.sh`) are the source for the three entries. The example the repo ships, `bin/fleet.example.json`, is derived from them at Section 5 and the real roster is written from the launchers read at that moment, never from this document.

**The environment file.** Default `D:/personas/keeper.env`, `KEY=value` lines with `#` comments, the broker's `broker.env` shape. The wrapper applies an allowlist and nothing else: `KEEPER_BASH_EXE`, `KEEPER_PATH_PREPEND`, `HOME`, `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`, `TEMP`, `TMP`. The reason is the same as the broker's. S4U is the Task Scheduler logon type that runs a task as a user without a stored password and without loading that user's profile, so the user-scoped PATH (where `bash.exe`, `claude` and the npm shims live on this box) is absent and the profile variables may not resolve to the operator's profile. Every path the supervisor chain needs is pinned in this file. The application rule, which the probe in Section 1 and the wrapper in Section 2 both implement: each allowlisted key present in the file is set as an environment variable of the wrapper's own process before the supervisor starts, except `KEEPER_PATH_PREPEND`, whose value is prepended to the process `PATH` with a semicolon, and `KEEPER_BASH_EXE`, which names the bash executable to run and is not exported. Section 1 measures which of these the task actually needs; the wrapper applies whichever are present.

**The exit-code policy.** The decision function takes the supervisor's exit code, the seconds the supervisor ran, the previous delay, and the count of consecutive exit-1 runs, and returns one action:

| Supervisor exit | Meaning (README exit table) | Keeper action |
|---|---|---|
| 0 | shutdown honored, or stop_complete | write the hold marker with the reason and exit 0 |
| 1 | usage or configuration error, or a launch-time fault | relaunch after 300 seconds; after three consecutive exit-1 runs each shorter than 60 seconds, write the hold marker with the reason and the supervisor's last error line and exit 0 |
| 2 | pre-launch gate failed or timed out | relaunch after 300 seconds, delay unchanged |
| 3, 4, 5, any other | crash loop, restart budget, survivor alive, unknown | relaunch after the current delay, then double it up to 14400 seconds |
| 130, 143 | the supervisor was signaled | exit 0 without relaunching |

The delay starts at 300 seconds and resets to 300 whenever the supervisor ran for 3600 seconds or more before exiting. The hold marker is `<rundir>/keeper.hold`; when it exists at wrapper start, the wrapper logs the hold reason and exits 0 without launching, so a persona the operator shut down stays down across reboots until `Start-Persona.ps1 -Name <n> -Release` removes the marker. The state file `<rundir>/keeper.json` carries the persona name, launch count, last start and end, last exit code, current delay, and the hold reason if any. The log `<rundir>/keeper.log` gets one line per decision and rotates at 5 MB keeping one previous file.

**Registration.** `bin/Register-PersonaTasks.ps1` mirrors `D:/discord-channels/install/Register-BrokerTask.ps1`: an elevation check that fails with a message saying why, one task per enabled roster entry named `AgentPersona-<name>`, an AtStartup trigger, an S4U principal at RunLevel Limited, RestartCount 999 at a one-minute interval, no execution time limit, MultipleInstances IgnoreNew, StartWhenAvailable, and `Set-ScheduledTask` when the task exists so a re-run updates in place. The broker registers AtLogOn; this plan registers AtStartup because the goal is a fleet that needs no logon. `CswapAuto` on this box already runs S4U at boot, which is the precedent that the shape works here.

**The supervisor fixes.** Three backlog entries name defects the keeper would otherwise relaunch into. Backlog entries are named here by their heading text, since line numbers move when the file is pruned: "The natural-exit relaunch never checks for a surviving claude.exe" (a surviving `claude.exe` at a natural-exit relaunch), "The decide path relaunches once more before a restart or crash limit stops the run", and "The supervisor treats a rate-limited child as merely quiet, and a credential swap never reaches it". Section 4 fixes them with their recorded remedies, each checked against the code before it is written, with one amendment to the third: the backlog remedy keys the swap on the credentials file's modification time, and that file is also rewritten on an ordinary token refresh, which would restart a healthy child. The signal is instead the account identity the profile config names, `oauthAccount.accountUuid` in the `.claude.json` under `USERPROFILE`, the file `claude` itself reads, read at child launch and compared on each poll; a change is a swap, and a rewrite that keeps the identity is quiet. On this box the account autoswitch task is what moves accounts, and it rewrites that config in place. The supervisor never reads the credentials file itself.

**Sweep.** The surfaces that speak the launch contract were read directly in the design session rather than by a scout dispatch: `bin/supervise.sh` (usage at lines 4 and 37, argument parsing 45 to 116, environment settings 206 to 207 and 333, launch flags 1463 to 1474, exit points 1832 to 1864, 1937 to 1942, 1996 and 2002, exit table comment at 24); `README.md` (quickstart 5 to 31, the workdir sentence at 24, exit codes 300 to 312, test coverage 322 to 343, limitations and next steps 472 to 491); `docs/backlog.md` lines 3, 64, 78, 82, 94, 158; the launchers under `D:/personas`; the sibling pattern in `D:/discord-channels/install/Register-BrokerTask.ps1` and `Start-Broker.ps1`; the kit's own task in `D:/claude-kit/sidecar/install-daemon-task.ps1`; and this machine's task list, which holds `claude-kit-sidecar-daemon` (Interactive, daily), `CswapAuto` (S4U, boot) and `SapplefeldChannelsBroker` (S4U, logon). Every one of those is in a section's Files in scope or under Out of Scope.

**Order.** Section 1 first, because its measurements are the input to Sections 2 and 3. Sections 2 and 3 next in either order, then 4, then 5. Section 4 is independent of the others and may run beside 2 or 3 in a second worktree. The header's Disjoint line says this plan touches no file another in-flight plan holds, so it may run beside one.

## Standing Brief Amendments

Each entry binds every section opened after it.

- Section 2: `Read-KeeperEnvFile` and the allowlist live in `bin/keeper-functions.ps1` alone. `bin/keeper-probe.ps1` dot-sources that file for both and drops its own copies, so the probe's readings and the wrapper's behavior come from one list. `bin/keeper-probe.ps1` joins Section 2's Files in scope for that edit. The unit test pins the allowlist in both directions against the single list.
- Section 2: an allowlisted key whose value is empty or whitespace-only is recorded and not applied; a key that appears twice takes the last value and is recorded. The wrapper's log spells these `ENV empty:` and `ENV duplicate:`, in the `ENV *:` family Interim board 2 locked; the probe's out file spells the same facts `env.empty` and `env.duplicate`. This narrows the Approach's "each allowlisted key present in the file is set": `Set-Item env:` with an empty value removes the variable under Windows PowerShell 5.1 and keeps it empty under pwsh 7, so applying it would do different things on the two engines and the log would call both "applied".
- Section 2: the wrapper launches the supervisor as a plain `bash <path> ...` with no `-l`, so children resolve on the delivered PATH and not on a login profile's rewrite. The probe measures with `bash -c` for the same reason.
- Section 2: the supervisor's output capture is file-backed. The wrapper redirects the supervisor's stdout and stderr to two files under the run directory, waits on the launched process object alone rather than on its process tree, then appends both to `supervisor.out` with stdout first and takes the last non-blank line of the stderr file as the last error line. It carries no in-memory pipe pumps, no poll-derived drain window and no rule that a quiet pass marks where the supervisor's own output ended. The acceptance bullets ask that the supervisor's stdout and stderr land in `supervisor.out` and that `keeper.hold` name the supervisor's last stderr line; neither carries a time bound, so a capture whose completeness depends on a timing window is not the form they ask for.
- Every section: a refusal leg carries a test that goes red if the leg is removed, not only one showing the leg refuses when it is called directly. A gate that must run before a load is pinned on that ordering, by a fixture whose loaded file records that it ran. A test that reaches the guard only through an injected call proves the guard's logic and proves nothing about the guard being wired in.
- Section 5: the README's `## Process keeper` section states in one paragraph that the keeper reads no file permissions: the roster, the env file and the repository tree are the operator's own machine state and are trusted as such, by the operator's decision, and no keeper script refuses, warns or skips on who may write them.
- Sections 2, 3 and 5, the operator's decision of 2026-09-15: the keeper carries no permission check. No keeper script reads a file's ACL, owner or writers, and none refuses, warns or skips on such a reading. `bin/keeper-functions.ps1` holds no writers predicate and no SID helper; `bin/Start-Persona.ps1` applies the env file and reads the roster with no trust check and no directory warning, and `-Release` needs no exemption from a refusal that no longer exists; `bin/keeper-probe.ps1` records no ACL reading and skips no probe on one; `bin/Register-PersonaTasks.ps1` carries no bootstrap gate, no launcher trust sweep, no roster or env file trust reading, no refusal message cap and no elevation-forcing seam, and dot-sources nothing it does not use. The `icacls` hardening of `D:/agent_persona` and `D:/personas` is not run, and no README, comment, test or plan sentence describes a trust check the scripts do not make. The tests that pinned the check retire with it under the retired-contract class, and every case that drove a real registration path through the gate's refusal is rewritten to shadow the scheduler cmdlets, since with the gate gone nothing else stands between the suite and a real `Register-ScheduledTask`. The reason, in the operator's words: the machine is a dedicated sandbox with isolated permissions that only the operator reaches, and the check was a best practice rather than a requirement.
- Section 4, the operator's decision of 2026-09-16: fix 3's rate-limit half reads the stream and logs the park to the supervisor log and nowhere else. It suppresses nothing, carries no anchor, ceiling or clamp, and sends no Discord notification when a park starts or ends. In the operator's words, a pause for a rate limit and the resume from it need no notification each time.
- Section 4: a process tree walk accepts a process as a descendant only where its creation time is not earlier than its parent's. Windows keeps a dead parent's id in every orphan it left and hands that id out again, so a walk that follows the parent id alone adopts an older, unrelated process whose parent happened to hold the id before, and every kill downstream of that walk then kills a process the child never started. No kill in `bin/supervise.sh` reaches past a ticks-matched snapshot by re-walking the live parent chain at kill time, so no `taskkill //T` remains where the snapshot kill already covers the tree. The guard is pinned by a test against a process table holding an older process whose parent id names the walk's root, which the walk must leave out.

## Sections of Work

### 1. Prove the scheduled-task shape on this box
Model: opus

Measure, under a real scheduled task, what the wrapper will inherit, before the wrapper is written against a guess. Deliver `bin/keeper-probe.ps1`, which takes `-OutFile` and `-EnvFile`, writes to the out file the running user, the session id, the elevation state, and the delivered values of `USERPROFILE`, `HOME`, `APPDATA`, `LOCALAPPDATA`, `TEMP` and `PATH`, then applies the env file's allowlisted keys by the application rule in the Approach and records the exit code and first output line of each of `bash.exe --version`, `node --version` and `claude --version` run through that bash. The env file for the probe is one this section writes at its final path, `D:/personas/keeper.env`, which Section 5 later reduces to the pins that proved necessary, carrying all eight allowlisted keys at this box's values: bash under `C:/Program Files/Git/bin`, node under `C:/Program Files/nodejs`, `claude` under the profile's `.local/bin`, the npm shims under the profile's `AppData/Roaming/npm`, and the profile variables at the operator's profile path. This plan is for this machine: the sibling scripts under `D:/discord-channels/install`, the launchers under `D:/personas` and the `CswapAuto` task are read here as patterns and are not in this repository.

Measurements, each recorded in this section's Chapter with the artifact it came from:

1. Whether the executing session is elevated, read with `IsInRole` on the current Windows identity.
2. Whether registering an S4U task succeeds from that session. Try it with `-ErrorAction Stop`; record the error text if it fails.
3. The delivered environment under the task, from the probe's out file.
4. Whether bash, node and claude resolve and report a version with the env file applied.
5. A real supervisor run under a task, with a scratch persona named `probe`, workdir `D:/personas/probe` and `--no-channel` so no Discord thread is created, launches a child: `LAUNCH child-1` in that run's `supervisor.log` and a heartbeat file that updates across two minutes.
6. Whether `Stop-ScheduledTask` ends the whole tree: after the stop, no `bash.exe`, `node.exe` or `claude.exe` from that run survives, read from the process list by parent chain or start time.
7. If cheap, whether a wrapper that exits 3 is restarted by the scheduler within the restart interval, since that is the backstop the design leans on for the wrapper's own crash.

Acceptance: the Chapter carries all seven readings, each as measured, or as "cannot measure" with the reason. Where reading 1 is unelevated and reading 2 fails, the section still delivers the probe script, registers nothing, records readings 3 to 7 as "cannot measure: registration needs an elevated session", and Sections 2 and 3 proceed on the broker's documented S4U facts (no profile loaded, every path pinned); the Operator Verification items then produce the readings. The scratch task and the scratch persona's directory are removed before the section closes, the env file stays, and the Chapter names all three as machine state that was created, removed or kept.

Files in scope: `bin/keeper-probe.ps1` (new); reads of `D:/discord-channels/install/Register-BrokerTask.ps1` and `Start-Broker.ps1` for the pattern; `bin/supervise.sh` for the exit table and the launch chain.
References: `D:/discord-channels/install/Register-BrokerTask.ps1` for the S4U principal and settings; this machine's `CswapAuto` task for an S4U boot trigger that works.

### 2. The keeper wrapper and its decision function
Model: opus

Deliver `bin/keeper-functions.ps1`, dot-sourced, holding pure functions: `Get-KeeperDecision` (exit code, uptime seconds, previous delay seconds, consecutive exit-1 count, in; action, delay seconds, next delay seconds, next exit-1 count, reason, out) implementing the policy table in the Approach; `Read-KeeperRoster` (path, name, in; the entry, out; a missing or disabled name is a thrown error naming the roster path); `Read-KeeperEnvFile` (path, in; the allowlisted keys and a list of the ignored keys, out); and `Build-SupervisorInvocation` (entry, in; the argument list and the environment map, out) implementing the roster table in the Approach. Deliver `bin/Start-Persona.ps1 -Name <n> [-Roster <path>] [-EnvFile <path>] [-Release]`, which applies the environment, honors the hold marker, runs the supervisor through `KEEPER_BASH_EXE` with the built arguments and with its stdout and stderr appended to `<rundir>/supervisor.out`, loops on the decision function, writes `keeper.json` and `keeper.log` under the entry's run directory, and rotates the log at 5 MB keeping one previous file. Both scripts run under Windows PowerShell 5.1 (`powershell.exe`), which is what the task action names.

Acceptance:
- `Start-Persona.ps1 -Name nosuch` exits 1 with a message naming the roster path and the name.
- With `KEEPER_BASH_EXE` pointed at a stub that exits with a planned code, the wrapper's log shows one `DECIDE exit=<n> uptime=<s> action=<a> delay=<d> reason=<r>` line per exit, the actions match the policy table, the delay doubles from 300 to the 14400 cap across consecutive crashes, and an uptime of 3600 or more resets it to 300.
- The stub's stdout and stderr land in `<rundir>/supervisor.out`, read back after the run.
- An `args` entry carrying `--prompt` makes `Build-SupervisorInvocation` throw, naming the roster path, before anything runs.
- Exit 0 writes `keeper.hold` with the reason and the wrapper exits 0; a single exit 1 relaunches after 300 seconds; three consecutive exit-1 runs each under 60 seconds write `keeper.hold` with the reason, the stub's last stderr line and the path of `supervisor.out`, and the wrapper exits 0; a later start with the marker present logs `HOLD <reason>` and exits 0 without running the stub; `-Release` removes the marker, logs it, and exits 0 without launching, and where no marker exists it logs that and exits 0, which is the only case a running wrapper can present, since the marker is written only as the wrapper exits.
- A key in the env file outside the allowlist is not applied and its name appears in the log's `ENV ignored:` line; an allowlisted key is applied before the stub runs, read back from the stub's own output.
- `keeper.json` after a run carries the fields the Approach lists, as JSON `Get-Content | ConvertFrom-Json` reads.

Files in scope: `bin/keeper-functions.ps1` (new), `bin/Start-Persona.ps1` (new), `.kit/keeper-unit-test.mjs` (new), `bin/supervise.sh` (read only, for the exit table at lines 24 to 33).
Tests: at minimum, lock the policy table both directions for every row (a code that must hold never launches, a code that must launch never holds), the doubling and the cap, the reset on uptime, the hold marker present versus absent, and the allowlist in both directions, since a key that leaks through the allowlist is the same defect the broker's allowlist exists to stop. Drive the real `powershell.exe` from node so the test exercises the engine the task will run.
References: `D:/discord-channels/install/Start-Broker.ps1` for the allowlisted-env pattern; `D:/claude-kit/sidecar/daemon-task.ps1` for a log rotation already written for this box.

### 3. Task registration
Model: sonnet

Deliver `bin/Register-PersonaTasks.ps1 [-Roster <path>] [-EnvFile <path>] [-RepoRoot <path>] [-User <name>] [-Prune] [-Start] [-WhatIf]`, cloned from `D:/discord-channels/install/Register-BrokerTask.ps1` with these substitutions: the build step is a function that returns one definition per enabled roster entry, so a test can inspect the definitions with `-WhatIf` and never touch the scheduler; the task name is `AgentPersona-<name>`; the action is `<SystemRoot>/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -File <repo>/bin/Start-Persona.ps1 -Name <name> -Roster <roster> -EnvFile <envfile>` with every path absolute, `<repo>` being `-RepoRoot`, which defaults to the parent of the script's own directory and which the operator runs from the root checkout at `D:/agent_persona` and never from a worktree, since a task pointed at a worktree breaks when that worktree is removed; the trigger is AtStartup; the principal is S4U at RunLevel Limited for `-User`, defaulting to the current identity; the settings are RestartCount 999, RestartInterval one minute, ExecutionTimeLimit zero, MultipleInstances IgnoreNew, StartWhenAvailable, AllowStartIfOnBatteries and DontStopIfGoingOnBatteries. An existing task is updated with `Set-ScheduledTask`; a roster entry with `enabled` false disables its task if one exists and reports otherwise; a task named `AgentPersona-*` with no roster entry is reported, and unregistered only under `-Prune`; `-Start` starts each registered task after registration. The elevation check runs first and throws the broker's message shape.

Acceptance: `-WhatIf` against `bin/fleet.example.json`, or against a fixture of the roster shape under `.kit/fixtures/` until Section 5 lands, prints one definition per enabled entry with the name, action string, trigger class, logon type and the seven settings above, and registers nothing; the script run unelevated without `-WhatIf` throws before any ScheduledTasks cmdlet runs. The idempotence check runs only where Section 1's reading 1 found the session elevated: a first and a second run against a scratch roster naming one scratch persona `keeper-probe`, never started, register `AgentPersona-keeper-probe` once and leave its definition unchanged, read before and after, and the scratch task is unregistered before the section closes. Where the session is unelevated, that check is recorded as cannot measure and Operator Verification item 1 runs the registration twice in its place. The real roster is never registered by this section; the cutover is the operator's.

Files in scope: `bin/Register-PersonaTasks.ps1` (new), `.kit/keeper-register-test.mjs` (new), `bin/fleet.example.json` (Section 5 writes it; this section reads it, or a fixture of the same shape if Section 5 has not landed).
Tests: at minimum, lock the built definition per entry, the elevation guard in both directions with the elevation state injected, the disabled and absent branches through `-WhatIf` output, and the `-Prune` branch reporting rather than removing when the switch is absent, since an unrequested unregister is the destructive failure.
References: `D:/discord-channels/install/Register-BrokerTask.ps1`, whose `-WhatIf` return and `Set-ScheduledTask` branch are the shape to clone.

### 4. Supervisor self-heal fixes the keeper depends on
Model: opus

Three fixes across the supervisor's two halves. `bin/supervise.sh` is the outer loop: it launches the child, polls it, stops it, and relaunches. `bin/supervise-decide.mjs` is the pure decision unit the poll loop calls with the child's state, returning one action such as restart, restart_passive or stop_crash_loop. Fixes 1 and 2 land in `bin/supervise.sh`; fix 3's rate-limit reading lands in the decision unit, since the hang reading it suppresses is decided there, and its account check lands in `bin/supervise.sh` beside the other facts the poll loop reads. Each backlog entry, named by its heading text, has its recorded remedy read whole and checked against the code before it is written; where the code has moved past the remedy, the Chapter says so and the fix follows the code.

1. "The natural-exit relaunch never checks for a surviving claude.exe": resolve and store the child's Windows pid right after the coproc launch, snapshot its tree on every stop and before a natural-exit relaunch, and kill any survivor before launching the next child, so a relaunch never runs beside an orphaned `claude.exe`.
2. "The decide path relaunches once more before a restart or crash limit stops the run": the decide path's restart branch checks the crash-loop limit and the restart budget before it relaunches, so a limit stops the run at the limit rather than one launch later.
3. "The supervisor treats a rate-limited child as merely quiet, and a credential swap never reaches it": the supervisor reads the child's `stdout.jsonl` tail and logs `RATE_LIMITED until <ISO>` while the newest record in the stream is an `api_retry` carrying `error_status` 429, whose `retry_delay_ms` names the remaining wait, so the log says a park is a park rather than leaving the operator to read `WAITING` lines for hours. The newest-record rule ends the limited state by itself the moment the child writes real work. The supervisor does not suppress the hang check on that reading, because a parked child keeps stamping its heartbeat and so never reaches the hang branch at all, while a genuinely wedged child whose last record is a retry would go unrestarted if it did; and it reads `oauthAccount.accountUuid` from the `.claude.json` under `USERPROFILE`, the file `claude` itself reads, at child launch and on each poll, taking `restart_passive` when the value changes so the child picks up the swapped account, staying quiet when the file is rewritten with the same value, and reading an absent or unparsable file as no identity, which is also quiet. It never opens the credentials file.

Acceptance: each fix has a red-then-green case in the suite named below; the README exit table at lines 300 to 312 is unchanged, since no exit code changes meaning; every offline suite the README lists under The supervisor's own suites passes, namely `.kit/supervisor-unit-test.mjs`, `.kit/supervisor-natural-exit-test.sh`, `.kit/supervisor-model-test.sh`, `.kit/settings-plugin-key-test.sh`, `.kit/channel-reply-instruction-test.sh` and `.kit/persona-live-refuse-test.sh`; and `.kit/live-stopprocesstree-test.sh` passes.

A running supervisor launched from this checkout keeps executing the script it started with (the backlog entry "A running supervisor keeps the script it launched with"), so these fixes reach a persona only at its next launch, which the cutover under Operator Verification provides.

Files in scope: `bin/supervise.sh`, `bin/supervise-decide.mjs`, `.kit/supervisor-natural-exit-test.sh`, `.kit/supervisor-unit-test.mjs`, `.kit/live-stopprocesstree-test.sh`, `.kit/supervisor-fn-extract.sh`, `.kit/.gitignore`, `docs/backlog.md` (read only here; the close-out prune retires the three entries named above).
Tests: at minimum, lock a survivor left by the stub child being killed before the relaunch and a control where none is left; the decide-path relaunch refused at the limit and taken one below it; a rate-limit record suppressing the hang reading until its reset time and not past it; and an account identity that changed since launch taking `restart_passive`, with a rewrite that kept the identity and an absent file both staying quiet, since the silent direction of each is a persona that looks alive and is not, and the loud direction of the last is a healthy child restarted on every token refresh.
References: `.kit/supervisor-natural-exit-test.sh`'s stub harness for the shell-level cases; `.kit/supervisor-unit-test.mjs` for the decision-unit cases.

### 5. Roster, environment file, README, and launcher migration
Model: sonnet

Write `bin/fleet.example.json` from the three launchers under `D:/personas` read at this moment, one entry each for `coordinator`, `aios` and `dev`, carrying every argument and environment variable each launcher sets, mapped through the roster table in the Approach. Write the real `D:/personas/fleet.json` with the same content and reduce the `D:/personas/keeper.env` Section 1 wrote to the pins Section 1 measured as needed, or keep all eight allowlisted keys where Section 1 could not measure. Both files are machine state outside git and the Chapter names them. The launchers stay in place as the manual fallback.

Add a `## Process keeper` section to `README.md` stating what runs today: the roster fields, the env file keys, the policy table, the hold marker and `-Release`, the registration command and its elevation requirement, how to stop a persona (its own shutdown tool, or `Stop-ScheduledTask` where Section 1's reading 6 found it ends the tree, else the wrapper's own stop path) and start one (`Start-ScheduledTask`), and where `keeper.log` and `keeper.json` live. Add the two new suites to the Test Coverage list at lines 322 to 343 in that list's shape, add the three keeper scripts to the Key Files table at lines 314 to 321, and add the Section 4 cases to the two supervisor suite lines. Re-read Limitations and Next steps at lines 472 to 491 and remove any line this plan retires. The README states current behavior only, never the change story.

Acceptance: `Register-PersonaTasks.ps1 -WhatIf` against the written roster prints three definitions; a diff of each launcher against its roster entry, done by eye and recorded in the Chapter, finds no argument or variable dropped; the README section answers each of the questions above in its own sentence.

Files in scope: `bin/fleet.example.json` (new), `README.md`, `D:/personas/fleet.json` and `D:/personas/keeper.env` (machine state), reads of `D:/personas/aios/launch.sh`, `D:/personas/coordinator/launch.sh`, `D:/personas/dev/relaunch.sh`.

## Out of Scope

- Moving the broker's own task from AtLogOn to AtStartup. That is the `discord-channels` repo's change and the operator's call; Open Questions carries it.
- The split of today's coordinator persona into a Sonnet steward that keeps the fleet running and a Fable architect that does design work on demand. That is the next plan.
- The kit resolving a persona's instructions, memory and leash from the repo it is working in rather than its home directory. That is the plan after, for the session that maintains the `claude-kit` repository.
- The backlog's other outer-loop entries: the design direction "let the outer loops recover a session that stopped" beyond the three fixes in Section 4; "A `--prompt` goal is dropped when the first child dies before its goal turn", since the roster launches every persona passive with no prompt and a prompt field is not added; "A pending record to an unheld coordinator is lost when its writer restarts before the coordinator's first tick", which moves with the steward plan; "A reader session cannot write restart_requested when the owner is the stuck process"; and "A running supervisor keeps the script it launched with".
- A kit doctor health step reading `keeper.json`, and any change to `D:/claude-kit/sidecar`.
- Any change to the supervisor's exit codes or their meanings.
- Syncing the roster or the env file across machines.
- Cutting the running personas over to the tasks. That is the operator's act under Operator Verification, at a quiet point of the operator's choosing.

## Assumptions

- assumed 2026-09-15 (default): the keeper ships in this repo's `bin/`, not in the kit repo, because it launches this repo's supervisor and reads this repo's exit table, and the operator's 2026-08-30 convergence decision covers session customization rather than a program's own launcher; reversal: moving three scripts and two tests to the kit's sidecar directory and re-registering the tasks.
- assumed 2026-09-15 (default): the scripts target Windows PowerShell 5.1, the engine every scheduled task on this box already runs, and the tests drive that same `powershell.exe`; reversal: none, since PowerShell 7 runs the same scripts.
- assumed 2026-09-15 (default): the roster and the env file live under `D:/personas` beside the launchers they replace, and since that directory is not a git repository they are machine state named in the Chapter that writes them; reversal: both scripts already take the paths as parameters.
- assumed 2026-09-15 (default): the trigger is AtStartup alone, because the goal is a fleet that needs no logon; reversal: one trigger line per task, and a roster field if some personas should differ.
- assumed 2026-09-15 (default): the exit-code policy table in the Approach, with 300 seconds as the base delay, 14400 as the cap and 3600 seconds of uptime as the reset; reversal: three constants in the decision function and its test.
- assumed 2026-09-15 (default): exit 1 relaunches after 300 seconds and holds only after three consecutive exit-1 runs each under 60 seconds, because the supervisor exits 1 both on a configuration error that will repeat and on a launch-time fault a boot can hit once; reversal: two constants in the decision function and its test.
- assumed 2026-09-15 (source: `bin/supervise.sh` lines 24 to 33 and `README.md` lines 300 to 312): the supervisor's exit code is the keeper's whole input and the keeper reads nothing else from the run; reversal: the decision function grows an input.
- assumed 2026-09-15 (default): a persona shut down through its own tool stays down across reboots until an operator act, so exit 0 writes a hold marker the task honors at every later start; reversal: drop the marker branch, and every shutdown is followed by a relaunch at the next boot.
- assumed 2026-09-15 (source: the operator's words on 2026-09-15 that the fleet should be a few workers plus one or two homed personas): the roster is closed at the three personas that run today, `coordinator`, `aios` and `dev`, and nothing more; reversal: a roster entry.
- assumed 2026-09-15 (source: the profile config's `oauthAccount.accountUuid` key, present on this box): the account swap signal is that identity changing between child launch and a poll, not the credentials file's timestamp, because the file is rewritten on an ordinary token refresh too; reversal: the check reads a different key or file.
- assumed 2026-09-15 (default): the executing session runs on this machine, where the sibling scripts under `D:/discord-channels/install`, the launchers under `D:/personas` and the `CswapAuto` task exist to read as patterns; reversal: on another machine the Approach carries enough of the broker's shape to write Section 1's probe without them.
- assumed 2026-09-15 (default): Section 1 runs first and its Chapter is the input to Sections 2 and 3; where the executing session is unelevated and cannot register a task, Sections 2 and 3 proceed on the broker's documented S4U facts and the Operator Verification items produce the readings; reversal: a divergence found at the operator's run is a fix round on Section 2.
- assumed 2026-09-15 (default): the executing session is the dev persona, working in this repo on a feature branch under Branch-and-PR, and the expert session that authored this plan does not execute it; reversal: none.

## Operator Verification

1. Where Section 1's Chapter reports the executing session was unelevated: with the root checkout at `D:/agent_persona` on a branch that carries the merged work, from an elevated PowerShell run `D:/agent_persona/bin/Register-PersonaTasks.ps1 -Roster D:/personas/fleet.json -EnvFile D:/personas/keeper.env` twice. The first run prints three definitions and registers them; the second reports the same three definitions and changes nothing. A thrown error, a definition that names a wrong path, or a second run that changes a task reopens Section 3.
2. Cut over at a quiet point. For each hand-launched supervisor: ask the persona to shut down through its own tool or close the launcher's console, wait for the supervisor's exit line in its log, then run `Start-ScheduledTask AgentPersona-<name>`. Each persona's thread reports ready. One that does not come back within ten minutes reopens Section 2, with `keeper.log` as the evidence.
3. Reboot without logging on. Every enabled persona's thread reports ready within ten minutes of the boot. One that does not reopens Section 1 or Section 2, whichever `keeper.log` points at.
4. Ask one persona to shut down through its tool, then reboot. That persona stays down and its `keeper.log` names the hold; the others come back. Then run `Start-Persona.ps1 -Name <name> -Release` and `Start-ScheduledTask AgentPersona-<name>`, and that persona reports ready. A persona that comes back on its own after a shutdown reopens Section 2.

## Open Questions

- Whether `Stop-ScheduledTask` ends the whole process tree in session 0. Section 1 measures it. If the scheduler leaves the supervisor's children running, the wrapper needs a stop path of its own, which is a fix round on Section 2 rather than a new section.
- Whether the broker's task should also move to AtStartup so the whole machine comes up without a logon. The operator's call, in the `discord-channels` repo.
- Whether the kit's doctor gains a step that reads each persona's `keeper.json` and reports a held or backing-off persona. The kit seat's call, after this plan ships.

## Chapters

### Interim board 1 - 2026-09-15

Written before Section 1's review round, at a compaction boundary the gate asked for.

- Handoff: this plan arrived from the expert seat over the commons inbox as a reader record (writer holding `reader:dev`, confirmed in the installed store). The grant was traced to the operator's own advance yes on this session's relay thread on 2026-09-15 for the class of plans the expert would dispatch, since the expert both authored and cited the `## Dispatch Authorization` section and so could not be the citer under the author-citer rule. Armed with `kit-goal arm --self-armed` from `D:/agent_persona`, bound to this session. Handoff reply owed to the expert: armed. The reply rides the inbox record's turn answer, which lands when this turn ends.
- Branch: `process-keeper`, cut from `origin/plan/process-keeper` at cf9e903 because the expert's worktree at `D:/agent_persona-wt/process-keeper` holds that branch. The pull request will open from `process-keeper`. The branch also carries `docs/plans/agent_persona_steward-architect_v1.md`, Status Ready, parked for the operator and not run here.
- Section 1 stage: implemented by implementer-opus (DONE_WITH_CONCERNS), verified in the main thread (probe run exit 0, bash 5.3.15, node v24.19.0, claude 2.1.272 all exit 0), review round 1 not yet dispatched. Live dispatches: none.
- Section 1 readings so far: (1) unelevated, `IsInRole Administrator` false in this session and in the probe's own out file; (2) S4U registration from this session fails, `Microsoft.Management.Infrastructure.CimException: Access is denied.`, HRESULT 0x80131500, nothing registered; (3) to (7) cannot measure: registration needs an elevated session, and this box has UAC on with consent prompting and no gsudo. Sections 2 and 3 proceed on the broker's documented S4U facts per the plan's own branch.
- Machine state: `D:/personas/keeper.env` written by the main thread with all eight allowlisted keys; TEMP and TMP pinned to `D:\Temp`, the user-level value this box delivers today, rather than the profile Temp path. No scheduled task was created. The scratch persona directory `D:/personas/probe` was never created.
- Gate baseline: no suite ran for this section (the plan names no test for it). The direct probe run is the section's evidence.
- Assumptions declared this section (route b unless noted): the env file is written by the main thread rather than the implementer, since it is machine state outside the tree; new `.kit/*.mjs` suites in Sections 2 and 3 are added to the allowlist in `.kit/.gitignore`, which ignores everything not listed; the probe records `delivered.TMP` beside the six named keys, since TMP is on the allowlist.
- Found work routed out of this plan: the installed plugin copy (0.10.0 cache, `hooks/index.ts:1408`) labels every drained inbox record `[OPERATOR]` while this repo's source labels by writer. Reported to the operator on the relay thread; remedy is a plugin update and a persona relaunch, the operator's act. A backlog entry follows with the Section 1 Chapter.
- Rulings adopted: none.
- Next action: dispatch round 1 (adversarial, blind, security at fable) over `bin/keeper-probe.ps1`, adjudicate, close Section 1 with its Chapter, then Sections 2 and 3.

### Chapter 1 - 2026-09-15
Completed: 1. Prove the scheduled-task shape on this box
Implemented By: implementer-opus (DONE_WITH_CONCERNS), review fixes by the main session
Metrics: review rounds 4, closed major-closed; provenance 5 spec-traceable, 4 fix-introduced, 1 new-requirement, rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The seven readings, each with its artifact. (1) Unelevated: `elevated=false` in every probe out file under `.kit/scratch/process-keeper/s1/`, and `IsInRole Administrator` false in this session. (2) S4U registration from this session fails: `Register-ScheduledTask` with an S4U principal threw `Microsoft.Management.Infrastructure.CimException: Access is denied.` (HRESULT 0x80131500); nothing was registered, confirmed by the task list. (3) Delivered environment under a task: cannot measure, registration needs an elevated session; UAC is on with consent prompting and the box has no gsudo. What the probe records when run by hand from this session is in `gate-51.txt` and `gate-7.txt`: all six named keys plus TMP delivered, TEMP and TMP at `D:\Temp`. (4) Toolchain through the env file, measured with the final `bash -c` instrument under both engines (`gate-51.txt`, PowerShell 5.1.26100; `gate-7.txt`, pwsh 7.6.5): bash exit 0 `GNU bash, version 5.3.15(1)-release`, node exit 0 `v24.19.0`, claude exit 0 `2.1.272 (Claude Code)`. The earlier `-lc` reading on the interim board is superseded. (5) Supervisor under a task: cannot measure, same reason. (6) `Stop-ScheduledTask` tree kill: cannot measure, same reason. (7) Scheduler restart of an exit-3 wrapper: cannot measure, same reason. Readings 3 and 5 to 7 come from the Operator Verification items once an elevated session registers the task. Machine state: `D:/personas/keeper.env` created and kept, with all eight allowlisted keys and its ACL restricted in this section to LocalAdmin, Administrators and SYSTEM with inheritance off (the saved pre-change ACL is at `.kit/scratch/process-keeper/s1/keeper-env-acl-before.txt`; rollback is `icacls D:\personas /restore` on that file); no scheduled task was created, so none was removed; `D:/personas/probe` was never created, so nothing was removed. Surprise: the review found the env file writable by Authenticated Users through the `D:\` root's inherited grant, and the same grant sits on `D:\personas` and on this repository tree; hardening the directory and the tree is machine-wide state and is the operator's call, raised in the close-out. Surprise: `[string]$Value` on the recorder function coerced `$null` to empty, so the round-1 script could not tell an undelivered variable from an empty one; fixed by typing it `[object]`. Surprise: `Set-Item env:` with an empty value removes the variable under 5.1 and keeps it under 7, so the probe records `env.empty` and applies nothing. The implementer's own concern about that inference is what the review confirmed. Handoff: the expert seat's plan pointer was acknowledged `armed`; it rode the inbox record's turn answer.
Assumptions: the env file is written and its ACL set by the main thread rather than the implementer, since it is machine state outside the tree (assumed 2026-09-15, route b, section 1); new `.kit/*.mjs` suites are added to the allowlist in `.kit/.gitignore`, which ignores everything not listed (assumed 2026-09-15, route a, the file's own rule, section 1); the probe records `delivered.TMP` beside the six named keys because TMP is on the allowlist (assumed 2026-09-15, route b, section 1); the exempt set for the env file's writers is the process account, Administrators and SYSTEM by SID rather than the owner (decided 2026-09-15 in review round 2, section 1, see Standing Brief Amendments).
Review Findings: review: code pair at fable, Agent tool; security at fable, Agent tool; rounds 2 to 4 adversarial at opus effort high, Workflow. Round 1 Majors: undelivered variable read as empty (blind and adversarial, spec-traceable, fixed); out-file directory and file resolved against different roots (blind, spec-traceable, fixed); env file writable by Authenticated Users (security, spec-traceable, fixed by ACL and by Section 2 amendment). Round 2 Majors, all fix-introduced on the predicate round 1 wrote and all security: mask omitted ChangePermissions, TakeOwnership and Delete (fixed); generic access bits unread (fixed); owner exemption let a substituted file pass (fixed by a fixed exempt set plus the directory reading, and the partial-guard statement in amendment 2). Round 3 Majors: owner's implicit WRITE_DAC unread (fix-introduced, security, fixed with an owner leg and SID comparison); reading 4 measured by the superseded instrument (spec-traceable, fixed by re-measurement recorded above). Round 4 Major: the refusal predicate named no test (new-requirement, security carve-out, fixed as a sentence in amendment 2). Blind lens's Majors traced by the orchestrator. Minors: 21 fixed in the close pass, 0 upgraded, 3 left with the reason (env bash path missing does not fall back to PATH, a mistyped pin should read as a failure; DeleteSubdirectoriesAndFiles on the directory reading would owe a fifth round and is named in amendment 2 for Section 2; the dated header was first left then dropped in round 4). List at `.kit/scratch/process-keeper/minors-section-1.md`. Recurrence: none across sections yet; five Standing Brief Amendments adopted (approval drift, recorded here).
Stamps: adjudicated 29 (11 project, 18 operator), stamped 5 operator (powershell-select-first-corrupts-exit-code, powershell-binding-error-exit-code, bash-tool-heredoc-collapses-backslashes, fan-out-runs-through-workflow-under-a-session-wide-cap, git-bash-sed-i-strips-cr); the rest were read for other work in the window and did not shape this section.
Gate: targeted lane, this section's own: the probe run under PowerShell 5.1.26100 and pwsh 7.6.5 against `D:/personas/keeper.env`, the unknown-flag and missing-flag exits, and nine readings checked in each out file; 14 checks, 14 pass, 0 fail, exit 0 on the re-run (two checks first failed on the shell's backslash handling in my grep pattern, not on the probe). Baseline on this lane: none before this section. Contention lane: not run; the section's machine-shared delta is the env file's ACL, and no other process reads that file yet. Tree state for the run: worktree at a95c571 plus this section's unstaged fixes, clean of foreign files.
Next: 2. The keeper wrapper and its decision function, and 3. Task registration, which may run beside each other on disjoint files
Commit Model: Branch-and-PR
Delta: worktree against HEAD a95c571 on this checkout, 2026-09-15, no contention.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 2 - 2026-09-15

Written after Sections 2 and 3 reached first green and before their review rounds, at a compaction boundary the gate asked for.

- Section 2 stage: implemented by implementer-opus (DONE_WITH_CONCERNS), verified in the main thread (`node .kit/keeper-unit-test.mjs` 50 pass, 0 fail, exit 0 on my own run; probe re-run into a fresh out directory exit 0), first-green commit 6f0bc35 pushed. One fold: the probe's `New-Item -LiteralPath`, which Windows PowerShell 5.1 does not have, moved to `-Path`; confirmed by `(Get-Command New-Item).Parameters.Keys -contains 'LiteralPath'` reading False under 5.1 and by the probe creating a missing directory afterward. Review round 1 not yet dispatched.
- Section 3 stage: implemented by implementer-sonnet (DONE), verified in the main thread (`node .kit/keeper-register-test.mjs` 23 pass, 0 fail, exit 0 on my own run), first-green commit 16075ad pushed. The idempotence check against a scratch task is cannot measure: this session is unelevated. Review round 1 not yet dispatched.
- Live dispatches: none.
- Contracts locked by the main thread before dispatch (route b, sections 2 and 3): the roster is a top-level JSON array of entry objects; the fixture of that shape is `.kit/fixtures/fleet.fixture.json`; `.kit/.gitignore` allowlists `keeper-unit-test.mjs`, `keeper-register-test.mjs` and `fixtures/*`; the wrapper's log tokens (`DECIDE`, `HOLD`, `RELEASE`, `ENV ignored:`, `ENV empty:`, `ENV duplicate:`, `ENV refused:`, `ENV dir-writers:`, `LAUNCH`, `EXIT`) and `keeper.json`'s camelCase fields; Section 3's script is self-contained (its own `Test-IsElevated`, its own roster read) so the two sections share no file.
- Implementer route-b choices carried to the Chapters: Section 2 captures the supervisor's stdout and stderr through `Start-Process` redirects to two temp files appended to `supervisor.out` (interleaving is lost; a `2>&1` merge under 5.1 wraps stderr in ErrorRecord noise, confirmed by the implementer's probe); `-DelayScale` and `-OwnerSid` are test seams; positional argument order follows `bin/supervise.sh` line 37; a missing env file is tolerated only when `KEEPER_BASH_EXE` is already on the process environment; `keeper.json` is written fresh per wrapper process. Section 3 refuses a roster name outside `[A-Za-z0-9_-]+` and a path carrying a double quote at the action-string boundary; the battery settings print through the CIM object's inverted property names.
- Gate baseline on the targeted lane: Section 2 suite 50/50 exit 0, Section 3 suite 23/23 exit 0, both measured on this checkout at 16075ad with a clean tree, no contention (no heavy-process claim, no foreign runner).
- Rulings adopted: none.
- Next action: round 1 for both sections (code pair at fable, security at fable, Agent tool), adjudicate, fix rounds as owed, close each with its Chapter, then Section 4.

### Interim board 3 - 2026-09-15

Written after review round 1 on Sections 2 and 3, with both fix rounds in flight, at a compaction boundary the gate asked for.

- Session: the machine rebooted for a Microsoft patch and the bound session died. The operator re-armed with `/kit-goal` on the relay thread; this session holds the leash. The operator then appended `docs/plans/agent_persona_steward-architect_v1.md` behind this plan, so the queue is 2 plans and that one starts on its own when this plan completes. It carries no `## Dispatch Authorization` section; the operator's typed arming is its authorization.
- Section 2 stage: review round 1 adjudicated, fix round 1 dispatched to implementer-opus at effort high through Workflow. Round 1 ran the code pair plus security at fable through Workflow (the operator's standing fan-out instruction routes every fan-out through Workflow with model and effort named), adversarial and blind at effort low, security at medium.
- Section 3 stage: review round 1 adjudicated, fix round 1 dispatched to implementer-sonnet at effort high through Workflow. Round 1 ran all three lenses at opus effort high, one tier above the sonnet writer.
- Live dispatches: `fix-s2` (implementer-opus) was asked to fix ten findings in `bin/Start-Persona.ps1`, `bin/keeper-functions.ps1`, `bin/keeper-probe.ps1` and `.kit/keeper-unit-test.mjs`. `fix-s3` (implementer-sonnet) was asked to fix nineteen findings in `bin/Register-PersonaTasks.ps1` and `.kit/keeper-register-test.mjs`. The two sets are disjoint.
- Round 1 tree bracket: `git status --porcelain` empty before dispatch and empty at return, captured at `.kit/scratch/process-keeper/round1-pre.txt` and `round1-post.txt`. No reviewer touched the tree.
- Provenance captures: `.kit/scratch/process-keeper/s2/fix-round-1.diff` and `s3/fix-round-1.diff`, taken at round 1's return against 5b128a1.
- Criticals, both confirmed on this session's own surface rather than accepted from the lens. Section 2: `Start-Process -Wait` waits for every descendant, so a supervisor exit 5, which by definition leaves a survivor, never reaches the decision function until the orphan dies; measured here under PowerShell 5.1.26100, a `-Wait` on `cmd.exe /c start /b ping -n 9` returned after 9.1 seconds. Section 3: the task action launches `bin/Start-Persona.ps1` under an S4U boot principal and `Get-Acl` on that file returns an inherited `NT AUTHORITY\Authenticated Users: Modify, Synchronize`, so any local account rewrites what boots as the persona.
- Decision taken to the operator and answered 2026-09-15: whether the registration script refuses to register when the launcher it points at carries foreign writers. The operator chose the refusal over a warning and over documentation alone. The in-flight Section 3 brief asked for the warning with the refusal as a single later gate, so the gate and its test are added on that dispatch's return rather than by killing it. Consequence for the operator: the repository tree and `D:\personas` must be hardened before the tasks register, which the close-out will carry with the commands.
- Also raised by two lenses and folded into Section 2's fix round: the roster file is read with no foreign-writer check while the env file carries a full one, so a roster writer chooses the persona's workdir, permission mode, run directory and supervisor flags. The fix reuses `Get-ForeignWriters` rather than writing a second predicate.
- Gate baseline on the targeted lane, unchanged since Interim board 2 and re-stated as the figure the fix rounds report against: Section 2 suite 50 pass 0 fail exit 0, Section 3 suite 23 pass 0 fail exit 0, measured on this checkout at 16075ad with a clean tree and no contention.
- Rulings adopted: none. No finding was held; the one new-requirement Major carried a security subject and took the carve-out's fix-before-close route instead.
- Next action: adjudicate both fix reports, add the Section 3 refusal gate and its test on the operator's answer, run each suite, take the owed review round over each fix delta, close both Chapters, then Section 4.

### Interim board 4 - 2026-09-15

Written after Section 2's review round 2 was adjudicated, with Section 2's second fix round and Section 3's review round 2 both in flight, at a compaction boundary the gate asked for.

- Section 2 stage: fix round 1 returned DONE_WITH_CONCERNS from implementer-opus; review round 2 ran the full round-1 roster at fable through Workflow (adversarial and blind at effort low, security at medium) and returned no Critical. Fix round 2 dispatched to implementer-opus at effort high through Workflow.
- Section 3 stage: fix round 1 returned DONE from implementer-sonnet; the operator's refusal gate was added in the main thread afterward (below); review round 2 dispatched, full roster at fable through Workflow. The round is read at fable rather than opus because the section now has two writers, implementer-sonnet and this main session, and the round runs one tier above the stronger of them.
- Live dispatches: `fix2-s2` (implementer-opus) was asked to fix three Majors and four Minors in `bin/Start-Persona.ps1`, `bin/keeper-functions.ps1` and `.kit/keeper-unit-test.mjs`, and was told explicitly not to touch Section 3's two files, which a concurrent round is reading. `r2-s3-adversarial`, `r2-s3-blind` and `r2-s3-security` were asked to review the Section 3 fix delta against base 16075ad.
- The operator's Option 1, implemented in the main thread: `Register-PersonaTasks.ps1` now refuses to register when the launcher the task would run, `bin/Start-Persona.ps1` or the directory holding it, is writable outside the exempt set, naming the offending principals. The reading is a new function `Get-LauncherTrustProblems`; the gate sits in `Register-PersonaTasks` beside the elevation check rather than in the definition builder, because building a definition writes nothing and `-WhatIf` calls the builder. On the `-WhatIf` path the gate reports on stderr and still prints what a hardened tree would register, which is the run the operator makes before the cutover. The in-flight fix round had built the warning form the brief asked for; converting it was a move and a widening, not a rewrite.
- Red-then-green on that gate, run as an exclusive tree-mutating probe with no agents in flight: with the gate's condition forced false, three of the four new cases fail and the refusal case reaches `Register-ScheduledTask` itself, which is the write the gate exists to stop. Pre-probe copy at `.kit/scratch/process-keeper/probe/Register-PersonaTasks.ps1.preprobe`; the restore was verified byte-identical with `cmp` and the tree compared clean against the pre-probe `git status --porcelain`.
- Two defects found in the main thread's own work before the suite went green, both mine and both fixed: `Get-ForeignWriters` returns a semicolon-joined string rather than a list, so wrapping it in `@()` manufactures one empty element and every launcher reads as untrusted; and `return ,$array` from the new reading returned the array as a single element, which printed as `System.String[]` in the refusal message. The predicate's string return is worth a Minor in its own right, since it invites exactly that misuse at every call site.
- Suite call sites moved: the registration suite's four real-registration cases now run against a hardened scratch launcher root built in the preamble, because this checkout inherits `Authenticated Users: Modify` from the `D:\` root and so is itself an untrusted launcher. A test leaning on the repository's own tree would pass today and flip the moment the operator hardens it.
- Round 2 tree bracket for Section 2: `git status --porcelain` identical before dispatch and at return, captured at `.kit/scratch/process-keeper/round2-s2-pre.txt` and `round2-s2-post.txt`. No reviewer touched the tree.
- Provenance captures: `.kit/scratch/process-keeper/s2/fix-round-2.diff`, taken at round 2's return against 16075ad.
- Section 2 round 2 adjudication, no Critical. Three owed Majors: the post-exit drain loop renews its deadline on every read with data, so a chatty survivor keeps the wrapper from ever returning (blind, fix-introduced); the pending reads and the Process object are never disposed, leaking a thread-pool worker per relaunch (blind Major, adversarial Minor, taken at the higher, fix-introduced); and the roster refusal writes only to stderr, which a hidden boot task discards, against a Goal that promises a log the operator can read (all three lenses, spec-traceable). No design stop: round 1 was the section's first review, so its findings sat in the implementer's original code and no consecutive pair of fix-introduced rounds exists.
- One Major downgraded at adjudication and recorded as such: the adversarial lens rated the test's pin on PowerShell's localized error prose a Major, tracing to nothing. Its stated failure needs a non-English UI culture, and the plan is scoped to this machine by its own assumption, so it carries no reachable consequence here. Downgraded to Minor and folded into the fix round rather than held for a judge.
- Gate baseline, measured on this checkout by the main session on 2026-09-15 with a clean box and the heavy-process claim held and released: Section 2 suite 68 pass 0 fail exit 0, Section 3 suite 48 pass 0 fail exit 0. The plan's previous recorded baseline was 50/0 and 23/0 at 16075ad, so the delta is +18 and +25 cases with no failure either side.
- Rulings adopted: none. No finding is held.
- Next action: adjudicate Section 3's round 2 and Section 2's fix report, run each suite, take whatever round the fix deltas owe, close both Chapters, then Section 4.

### Interim board 5 - 2026-09-15

Written after Section 2's review round 3 fired a design stop and Section 3's review round 2 was adjudicated, at a compaction boundary the gate asked for.

- Section 2 stage: fix round 2 returned DONE_WITH_CONCERNS from implementer-opus at 75 pass 0 fail exit 0, against the 68/0 baseline it measured itself. Review round 3 ran one lens (adversarial at opus, effort high, through Workflow), which is the decayed roster a later round takes over an opus writer. It returned four Majors and no Critical. **Design stop fired.**
- The design stop, and why. Rounds 2 and 3 each carry at least one owed Major that traces to a bullet and sits in lines the previous fix round of this section wrote, and the two sit in one mechanism: the post-exit output capture in `Invoke-Supervisor`, `bin/Start-Persona.ps1` roughly lines 290 to 405. Round 2 said the drain loop renewed its deadline forever; round 3 says the loop *before* the drain still does, because `if ($moved) { continue }` precedes `if ($exited) { break }` and a survivor streaming on both pipes keeps `$moved` true. Round 2 said the reads and the process were never disposed; round 3 says the disposal added does not release a parked read, which the new code's own comment concedes ("no call here can end it early"). All four confirmed by the main session against the file, not taken from the lens.
- Held as one unit until the ruling lands: round 3's Majors 1, 3 and 4 (the uncapped pre-drain loop, the hold marker able to name a survivor's stderr line, the disposal that does not close the leak). No fix round runs on that mechanism meanwhile.
- Not held, on the security carve-out: round 3's Major 2, `-Release` exempted from the roster refusal but still reading `rundir` and `workdir` out of that same untrusted roster, so a foreign roster writer still steers a directory create and a targeted delete. Fixed before the section closes.
- Judge: the scope adjudicator at fable, effort high, through Workflow, dispatched at once because this stop freezes the stopping round's fixes and so leaves no other work for a window to run against. The ask also went to the live `AP: Expert` seat under the never-gates rule; its answer, if it arrives, is recorded beside the ruling in force.
- The orchestrator's own lean, recorded here and deliberately withheld from the judge: round 1's Critical was about `Start-Process -Wait` waiting for every descendant, not about file redirection, and the fix round discarded the redirection along with the wait. A redirect to files plus `WaitForExit(0)` polling would give correct exit detection with no pump loop at all.
- Section 3 stage: review round 2 ran the full round-1 roster at fable through Workflow and returned no Critical. All three lenses converged on one security Major: the launcher trust gate reads `bin/Start-Persona.ps1` and `bin` only, while the boot chain also runs `keeper-functions.ps1`, `supervise.sh`, `agentic-common.sh` and `supervise-decide.mjs`, and the repo root is unread. A second security Major: the registration script dot-sources `keeper-functions.ps1` before any gate can read it, so an elevated run executes the tree it is meant to refuse. Both confirmed against the file by the main session. Both take the security carve-out, so neither is held and neither goes to a judge.
- Two Standing Brief Amendments adopted for Section 3 on those two Majors: the trust reading becomes a structural sweep over every leaf under `bin` plus `bin` plus `-RepoRoot`, with the `-User` account added to the exempt set; and a self-contained bootstrap check, deliberately stricter than the full predicate, runs before the dot-source.
- Live dispatches: `fix2-s3` (implementer-sonnet, effort high) was asked to fix those two Majors, one upgraded Major (the suite's elevation probe treats a failure as unelevated, which in an elevated session would register real tasks against the fixture roster) and twelve Minors in `bin/Register-PersonaTasks.ps1` and `.kit/keeper-register-test.mjs`. `designstop-s2` (scope-adjudicator) holds the ruling above.
- Round brackets, all clean: `round2-s3-pre/post.txt` and `round3-s2-pre/post.txt` identical either side. No reviewer touched the tree.
- Provenance captures: `.kit/scratch/process-keeper/s3/fix-round-2.diff` and `s2/fix-round-3.diff`, both against 16075ad.
- Minor lists: `.kit/scratch/process-keeper/minors-section-3.md` written, twelve entries from round 2, the ones folded into the live fix round marked. Section 2's round-2 Minors were all dispositioned inside fix round 2; round 3's eight are not yet listed, because the design stop freezes that section's fixes.
- Gate baseline on the targeted lane: Section 2 suite 75 pass 0 fail exit 0 as the implementer measured it on this checkout on 2026-09-15 with the heavy-process claim held, which the main session has NOT yet re-run, because a live dispatch is on the box and presence is grounds for waiting. Section 3 suite 48 pass 0 fail exit 0, the figure its live fix round is measuring against. The plan's previous recorded figures were 68/0 and 48/0.
- Owed to the operator at Section 3's close, measured this session with `icacls`: `D:\agent_persona` and `D:\personas` both carry `NT AUTHORITY\Authenticated Users:(I)(M)` inherited from the `D:\` root. The exact commands and their rollback are at `.kit/scratch/process-keeper/hardening-commands.md`.
- Rulings adopted: none yet; one in flight.
- Next action: adjudicate the design stop's ruling, fix Section 2's security Major, adjudicate Section 3's fix report, re-run both suites once the box is clear, close both Chapters, then Section 4.

### Interim board 6 - 2026-09-15

Written after the design stop's ruling landed and both sections' next rounds went out, at a compaction boundary the gate asked for.

- The design stop ruled REFUSE. The judge was the scope adjudicator at fable, effort high, through Workflow. Its grounds: the acceptance bullets ask that the supervisor's stdout and stderr land in `supervisor.out` and that `keeper.hold` name the supervisor's last stderr line, and neither carries a time bound, while the mechanism as built gives the capture a time-bounded and lossy semantics through a poll interval, a drain window and a quiet-pass rule that no bullet names. The grounds were checked on this session's own surface: both bullets exist as quoted, and base 16075ad does hold the file-backed form the ruling names. The `AP: Expert` seat was asked in parallel under the never-gates rule and had not answered by the time the ruling landed; the ruling is the one in force.
- The form the ruling names, now a Standing Brief Amendment: redirect stdout and stderr to two files under the run directory, wait on the launched process object alone rather than on its tree, append both to `supervisor.out` with stdout first, and take the last non-blank line of the stderr file as the last error line. No pumps, no poll interval, no drain window, no quiet-pass rule.
- What that ruling costs and buys, recorded because it discards work: three fix rounds of repair on the pump mechanism come out. What it buys is a capture with no timing heuristic in it, and it separates the two things review round 1's Critical had welded together. That Critical was about `Start-Process -Wait` waiting on every descendant, not about file redirection; the fix round discarded the redirection along with the wait, and every defect since came from the pump loop that replaced it. The wait stays `WaitForExit(0)` on the process object.
- The design stop's consecutive-fix-introduced count restarts at this ruling, so a Major in the removal delta opens a fresh pair rather than firing the same stop again.
- Section 2's Major 2 from round 3, the security one the carve-out kept out of the hold, was fixed in the main thread: `-Release` is no longer exempt from the roster foreign-writer refusal, because the release path still read `rundir` and `workdir` out of that same untrusted roster to choose a directory to create and a `keeper.hold` to delete. That carve-out was the orchestrator's own instruction in the round-2 fix brief and the reviewer was right that it does not hold. Its test is owed and is handed to the removal round.
- Section 3's fix round 2 returned DONE from implementer-sonnet at 62 pass 0 fail exit 0, against the 48/0 baseline. The trust gate is now a structural sweep over `-RepoRoot`, `bin` and every file under `bin`, with `-User`'s SID added to the exempt set, and a bootstrap ACL check runs before the dot-source.
- One material concern from that round, adjudicated in the main thread rather than accepted: the implementer gated the bootstrap check on elevation, which the brief did not say. Unconditional is the stronger position, because the dot-source happens at load time and so an unelevated `-WhatIf` run also executes the suspect file, leaving an escalation from one unprivileged local account to another. The gating is kept anyway: unconditional would make the script refuse to load from this checkout, so the suite could not run at all until the operator hardens the tree, and hardening is the operator's act at cutover. The exposure left is the one hardening closes rather than a new one, and Section 5's README trust-boundary paragraph carries it.
- Live dispatches: `fix3-s2` (implementer-opus, effort high) was asked to perform the removal above plus three Minors and the owed `-Release` test, and was told Section 3's two files are not its own. `r3-s3-adversarial` (adversarial at opus, effort high) is reading Section 3's second fix delta, which is the round that delta owes under the fix-delta bar.
- Round brackets: `round3-s3-pre.txt` captured before dispatch. Section 3's round 2 and Section 2's round 3 brackets both closed clean.
- Gate baselines, neither yet re-run by the main session because a live dispatch holds the box: Section 2 suite 75/0 exit 0 as the round-2 fix measured it, Section 3 suite 62/0 exit 0 as the round-2 fix measured it. The main session owes both re-runs once the box clears.
- Minor lists: `.kit/scratch/process-keeper/minors-section-2.md` and `minors-section-3.md` both written.
- Rulings adopted: one, the design stop's REFUSE above, recorded as a rule in the Standing Brief Amendments block rather than as a verdict.
- Next action: adjudicate Section 3's round 3 and Section 2's removal round, re-run both suites on a clear box, close both Chapters, then Section 4.

### Interim board 7 - 2026-09-15

Written after Section 3's review round 3 was adjudicated and its fix round went out, with Section 2's removal round still running, at a compaction boundary the gate asked for.

- Section 3 stage: review round 3 ran one lens (adversarial at opus, effort high, through Workflow), the decayed roster a later round takes, over the second fix delta against base 16075ad. Six Majors, five Minors, no Critical. Fix round 3 dispatched to implementer-sonnet at effort high through Workflow. No tier escalation: the ladder keys on surviving Criticals and this section has returned none.
- No design stop for Section 3, and the reading is recorded because the shape looks like one. Round 2's owed Majors were both security findings, and the security carve-out keeps those out of the design stop's trigger entirely, so rounds 2 and 3 form no consecutive fix-introduced pair. Round 3's own non-security Major is spec-traceable rather than fix-introduced.
- The six Majors, each confirmed by the main session against the file rather than taken from the lens. (1) The launcher trust sweep reads `-RepoRoot`, `bin` and the files under `bin`, while the boot chain reaches repository code outside `bin`: `bin/supervise.sh:313` sets `PLUGIN_DIR` to the repository root and `:1443` passes `--plugin-dir <repo root>` for any roster entry carrying `--dev`, which the shipped fixture's `beta` entry does. Fix-introduced, security. (2) `Get-ChildItem -File -Recurse` never ACL-reads a subdirectory container, so `DeleteSubdirectoriesAndFiles` on a future `bin/<sub>` is unread; latent today, since `bin` has no subdirectory. Fix-introduced, security. (3) All three bootstrap-gate cases dot-source the script first and then call the gate with injected parameters, so deleting the top-level call at line 175 leaves every case green: the refusal is proven and the wiring is not. Fix-introduced, security. (4) The bootstrap gate's owner leg has no substituted-SID seam, which Section 2's amendment required on the sibling predicate for exactly this reason. Fix-introduced, security. (5) The one stub-shadowed real-write case always passes `-Prune`, so a bug letting `Unregister-ScheduledTask` run without it passes the suite. Spec-traceable, and the section's own Tests line names this branch. (6) The roster and env file are read with no trust reading while `D:\personas` carries the inherited `Authenticated Users: Modify`, so a foreign writer deleting entries makes a `-Prune` run unregister live personas' tasks. Security, so the carve-out fixes it rather than holding it as a new requirement.
- Two Standing Brief Amendments adopted on those findings, both approval drift recorded here. The launcher trust reading's scope becomes `-RepoRoot` whole, every file and every directory beneath it, with no exclusion list, since a list of what the chain cannot reach goes stale the moment the chain reaches it. And, binding every section: a refusal leg carries a test that goes red if the leg is removed, not only one showing it refuses when called directly, with an ordering leg pinned on the ordering and an owner leg pinned through a substituted-SID seam.
- Recurrence: that second amendment is the recurrence rule firing. Section 1's round 4 carried the same class, a refusal predicate that named no test, and round 3 here carried two instances of it.
- The ordering pin needs a seam the script does not have, and the brief decides its shape: the top-level gate call treats the gate as elevated when a named environment variable is set or when `Test-IsElevated` is true. The seam can only tighten the gate, never loosen it, and no input may force it false.
- Section 3 spec text corrected to match the as-built action string, which now names the qualified `<SystemRoot>/System32/WindowsPowerShell/v1.0/powershell.exe` rather than the bare `powershell.exe`. That was a claim finding this round raised.
- One Minor left with the reason: `Enable-ScheduledTask` on every enabled entry re-enables a task the operator disabled by hand. Roster-is-truth is the decision in force and the behavior stays.
- Round bracket: `round3-s3-pre.txt` and `round3-s3-post.txt` differ by one line, the plan doc moving from modified to clean, which is this session's own commit of Interim board 6 and not a reviewer's write. Nothing else moved.
- Provenance capture: `.kit/scratch/process-keeper/s3/fix-round-3.diff`, taken at round 3's return against 16075ad, 1543 lines.
- Live dispatches: `fix3-s2` (implementer-opus, effort high) performing the design stop's ordered removal in `bin/Start-Persona.ps1`, `bin/keeper-functions.ps1` and `.kit/keeper-unit-test.mjs`; its transcript was growing at the moment this entry was written, 26 minutes into the run. `fix3-s3` (implementer-sonnet, effort high) fixing the six Majors and four of the five Minors in `bin/Register-PersonaTasks.ps1` and `.kit/keeper-register-test.mjs`. The two file sets are disjoint and each brief names the other's files as off-limits.
- Gate baselines, neither yet re-run by the main session because two live dispatches hold the box: Section 2 suite 75/0 exit 0 and Section 3 suite 62/0 exit 0, both as their round-2 fixes measured them on this checkout on 2026-09-15. The main session owes both re-runs once the box clears.
- Minor lists: `minors-section-3.md` now carries round 3's five entries under a `## Round 3` heading, seventeen in total. `minors-section-2.md` holds round 3's six.
- Rulings adopted: none this boundary. The design stop's REFUSE from the last boundary is the one in force and its removal round is running.
- Next action: adjudicate both fix reports, take whatever review round each delta owes, re-run both suites on a clear box, close both Chapters, then Section 4.

### Interim board 8 - 2026-09-15

Written after both sections' fix rounds returned and both suites were re-run by the main session, with review round 4 in flight over both deltas, at a compaction boundary the gate asked for.

- Section 2 stage: the design stop's ordered removal returned DONE_WITH_CONCERNS from implementer-opus. The pump mechanism is gone: `Read-KeeperPump`, `Write-KeeperCapture`, the zero-millisecond poll loop, the bounded drain, the quiet-pass rule, the in-window flush check and the three capture constants all came out. In their place, `Start-Process` without `-Wait` redirecting to two per-launch files, a timed `WaitForExit` loop on the launched process alone, a shared-mode read, and an append of stdout then stderr to `supervisor.out`.
- What the implementer confirmed on this box, with its evidence, because these are the facts the new capture rests on. The unwaited `Start-Process` returned in 0.01 seconds and the wait loop returned at the launched bash's own exit while its descendant lived four seconds longer, so the wait is on the launched process alone. Both files already held every line the parent wrote at the moment the wait returned, because the child writes through an inherited file handle and there is no flush window. A surviving descendant holds the file open, so Windows refuses the delete; that is logged as `CAPTURE held:` and the next launch sweeps what has come free. `$process.ExitCode` reads empty unless the handle is touched while the process is alive, confirmed against six variants, which is why a `$null = $process.Handle` line sits immediately after the start.
- The removal's regression pin was proved both directions in a copy of `bin` under `.kit/scratch/redcheck/` with `-Wait` injected into the copy alone: exactly one case failed, the orphan-descendant case, at 13638 ms against its 7000 ms bound. The shared tree was never mutated.
- Two Section 2 cases retired, both as orphans: the chatty-descendant drain case, whose contract was the drain window that no longer exists, and the case pinning the hold marker against a survivor's post-exit stderr, whose contract was the quiet-pass rule the amendment replaced. One case flipped: `-Release` is now refused against a foreign-writable roster, with a withheld control removing the one grant and watching the same call succeed. One token added, `CAPTURE held:`; none removed.
- Section 3 stage: fix round 3 returned DONE from implementer-sonnet. The trust sweep is now the whole `-RepoRoot` tree, files and directories, with no exclusion list; the bootstrap gate has an ordering pin driven through a tightening-only seam and a substituted-owner-SID seam; the roster and env file have a trust reading of their own; and the destructive `-Prune` branch has a real-path case in the non-removing direction. The sweep takes 12 seconds and reports 3,489 problems on this unhardened checkout.
- One Major the orchestrator raised on that delta and fixed in the main thread, because it defeats the gate on the one run the gate exists for. The refusal joined all 3,489 sentences into a single line, so the operator's first run, the one before hardening, produced an unreadable wall of text with the decision buried in it. The message now leads with the count, shows the first ten paths whole and names how many it elided. Red proved both directions as a tree-mutating probe with no agents in flight: with the cap forced off, exactly the two new cases failed and the elision case reported eleven of eleven filler paths shown. The file was restored from a pre-probe copy and verified byte-identical with `cmp`, and the tree matched its pre-probe `git status --porcelain`.
- Gate, re-run by the main session on a clear box with the heavy-process claim held and released, which is the first time this session has measured either figure itself. Section 2 suite 73 passed, 0 failed, exit 0. Section 3 suite 77 passed, 0 failed, exit 0. Against the plan's previously recorded figures of 75/0 and 62/0: Section 2 is down two cases, which is the two retirements above, and Section 3 is up fifteen, thirteen from the fix round and two from the orchestrator's cap. No failure either side. Tree state for both runs: worktree at 78b7bb6 plus both sections' unstaged work, clean of foreign files, no contention.
- A contention note that qualifies one earlier figure. Both implementers ran their own suites while a foreign session held the box claim (`KIT: Worker`, repo `claude-kit`, 5400 seconds declared). The Section 2 implementer waited 43 minutes and then claimed it properly; the Section 3 implementer named the contention and proceeded without claiming, which the protocol allows. Its 75/0 figure was therefore measured under contention and is superseded by the main session's own 77/0 run above.
- Review round 4 dispatched: two adversarial lenses at opus, effort high, through Workflow, one per section, over the deltas against base 16075ad. Both deltas owe a round under the fix-delta bar. Section 2's changed how a process is spawned, which is an outward action; Section 3's changed an authorization surface.
- Section 3 is now at four review rounds. The operator backstop fires at five, so if round 4's adjudication leaves the terminal condition unmet, the round after it declares.
- Round brackets: `round4-pre.txt` captured before dispatch. Round 3's Section 3 bracket closed with one delta, the plan doc moving from modified to clean, which was this session's own commit.
- Minor lists: `minors-section-2.md` holds six entries, three of which fix round 3 dispositioned. `minors-section-3.md` holds seventeen across two rounds, four of round 3's five folded into the fix round. One left with the reason: `Enable-ScheduledTask` re-enabling a hand-disabled task, since roster-is-truth is the decision in force.
- Rulings adopted: none this boundary.
- Next action: adjudicate round 4 on both sections, run the Minor close pass for each, close both Chapters, then Section 4.

### Interim board 9 - 2026-09-15

Written after review round 4 was adjudicated on both sections and fix round 4 went out, at a compaction boundary the gate asked for.

- Round 4 ran one adversarial lens per section at opus, effort high, through Workflow, the decayed roster a later round takes. Section 2 returned one Major and six Minors, verdict APPROVED_WITH_CONCERNS. Section 3 returned four Majors and seven Minors, verdict CHANGES_REQUIRED. No Critical either side.
- Round bracket closed clean. `round4-pre.txt` and `round4-post.txt` are byte-identical, diff exit 0, six modified files and nothing else. Provenance captures taken at the round's return: `s2/fix-round-4.diff` 1186 lines and `s3/fix-round-4.diff` 2052 lines, both against 16075ad.
- Section 2's Major, confirmed by the main session against `bin/keeper-functions.ps1:389-393`. The owner leg is guarded by `$owner.Length -gt 0`, so an owner that reads as an empty string is treated as exempt and the leg the amendment requires is skipped. `$acl.Owner` is PowerShell's own code property over `GetOwner(typeof(NTAccount))`, which returns empty where the descriptor carries no owner or the name will not translate. A security guard that fails open on an input it cannot parse is the wrong default. Fix-introduced, security, so the carve-out fixes it rather than holding it.
- Section 3's four Majors, each confirmed by the main session rather than taken from the lens, two of them reproduced on this box. (1) Case-variant roster names register the wrong definition: `New-Object 'HashSet[string]'` takes the ordinal case-sensitive comparer, so adding `alpha` then `Alpha` both return True and the duplicate throw never fires, while `$definitionsByTaskName = @{}` is case-insensitive and collapses both to one key holding the second entry. Reproduced: hashset count 2, hashtable count 1, lookup of the lowercase key returning the uppercase entry's value. One persona launches twice under the wrong name and the other never launches, silently. (2) The bootstrap gate runs unconditionally at load time (line 208), before param handling, so an elevated run against this unhardened tree exits 1 and `-WhatIf` is unreachable elevated. Reproduced: `icacls D:\agent_persona\bin` carries `NT AUTHORITY\Authenticated Users:(I)(M)`, which the gate refuses. The gate is right and stays; what is wrong is that its refusal leaves the operator with no route to the hardening list, which only the unelevated `-WhatIf` reaches. (3) The whole-tree sweep costs about 14 seconds a call and the suite pays it about nine times, 86 percent of it avoidable: `Get-ForeignWriters` and `Get-FileWriters` each call `Get-Acl` on the same path, and `ConvertTo-Sid` translates uncached per ACE per path. (4) The refusal message's ten-path cap can omit an offending principal outright, since a single non-inherited deep grant to a principal none of the first ten name is invisible, and the operator hardens what was shown and is refused again with no new information.
- Major 3's fix lives in `bin/keeper-functions.ps1`, which is Section 2's file, so it was assigned to the Section 2 implementer and named to the Section 3 implementer as off-limits. The two file sets stay disjoint.
- No design stop for Section 3, and the reading is recorded because the shape looks like one for a second time. Round 3's owed Majors were five security findings plus one spec-traceable, and the security carve-out keeps security findings out of the trigger, so round 3 contributes no counting fix-introduced Major and rounds 3 and 4 form no pair. Of round 4's four, three are security and the fourth, the sweep cost, is the only counting fix-introduced Major in the round.
- Both sections are now at four review rounds. The operator backstop fires at five: if round 5's adjudication leaves the terminal condition unmet, that adjudication declares `BLOCKED: section <n> hit the review-round backstop; phase analysis attached` rather than opening a sixth fix round. Both fix deltas will owe round 5 under the fix-delta bar, Section 2's because it changes a security predicate and Section 3's because it changes an authorization surface.
- One claim the lens could not settle, settled by the main session instead. `Register-PersonaTasks.ps1:28-30` says `$PSScriptRoot` is not populated when a `param()` default runs under `powershell.exe -File`. A scratch script carrying `param([string]$P = $PSScriptRoot)`, invoked that way, printed the script's own directory for both the param default and the body. The stated reason is false, the code is right either way, and the sentence is corrected in this fix round.
- Operator Verification item 1 corrected in the plan doc, since Major 2 showed it is unrunnable as written. It now runs in three steps: the `-WhatIf` inspection unelevated, the hardening, then the elevated registration runs. That is approval drift and is recorded here.
- Minor lists: `minors-section-2.md` now holds twelve entries across two rounds, three of round 4's six folded into this fix round and three left with the reason. `minors-section-3.md` holds twenty-four across three rounds, six of round 4's seven folded in and one left with the reason.
- Gate baseline, unchanged since board 8 and measured by the main session on a clear box at worktree 78b7bb6 plus both sections' unstaged work: Section 2 suite 73 passed, 0 failed, exit 0; Section 3 suite 77 passed, 0 failed, exit 0.
- Live dispatches: `fix4-s2` (implementer-opus, effort high) on `bin/keeper-functions.ps1`, `bin/Start-Persona.ps1` and `.kit/keeper-unit-test.mjs`; `fix4-s3` (implementer-sonnet, effort high) on `bin/Register-PersonaTasks.ps1` and `.kit/keeper-register-test.mjs`. Each brief names the other's files as off-limits.
- Rulings adopted: none this boundary.
- Next action: adjudicate both fix reports, take review round 5 on each delta, watch the backstop at that adjudication, run the Minor close pass for each section, close both Chapters, then Section 4.

### Interim board 10 - 2026-09-15

Written at the review-round backstop firing on both Sections 2 and 3, before the declaration went to the operator.

- **Backstop stage: first firing, at the opening bound of five rounds, on both sections at once.** The round count stands at five for each and restarts at the operator's answer. A continue past this bound buys three further rounds, and each adjudication from the third of those onward that still leaves the terminal condition unmet declares again.
- Review round 5 ran one adversarial lens per section at opus, effort high, through Workflow, the decayed roster a later round takes. Section 2 returned three Majors and six Minors, Section 3 three Majors and ten Minors, verdict CHANGES_REQUIRED both. No Critical either side. Recorded as run above the rule's tier: Section 3's writer tier is sonnet, so the table would put a later round's lens at sonnet, and rounds 2 through 5 all ran at opus for continuity, on the reading that a stricter read never weakens a round.
- Round bracket closed clean. `round5-pre.txt` and the porcelain at return name the same six modified files and nothing else. Provenance captures at the round's return: `s2/fix-round-5.diff` 1453 lines and `s3/fix-round-5.diff` 2352 lines, both against 16075ad.
- All six Majors were confirmed by the main session against the code rather than taken from the lens. Section 2: the two ACL-read-failure refusals at `bin/Start-Persona.ps1:152` and `:411` carry no test, so deleting either leaves the suite green; `bin/keeper-functions.ps1:341` decides the write mask from `Test-Path -PathType Container`, which returns false both for "this is a file" and for "I could not classify this", silently dropping DeleteSubdirectoriesAndFiles on the second; `bin/Start-Persona.ps1:41` dot-sources keeper-functions.ps1 with no trust reading while the same script refuses the roster and env file on exactly that reading. Section 3: round 4's own principals fix is pinned by nothing that composes the message, so deleting the two `$detail +=` lines leaves the suite green; the bootstrap refusal's route hint promises the unelevated `-WhatIf` run prints "exactly which paths carry a foreign write grant" while the cap at `Format-TrustProblemDetail` truncates to ten of about 3,489, reached through `Get-TargetTrustProblems`'s own composition at line 725 which the listing branch also uses; six suite cases drive `Register-PersonaTasks` elevated with no `-WhatIf` and no stubs, so a gate regression turns the suite itself into a real AtStartup S4U registration.
- **Why the backstop fires rather than a sixth fix round opening.** Every one of the six is a security finding of Major weight, so the carve-out keeps each out of any hold and out of the design stop's trigger, and each would ordinarily be fixed before the section closes. The terminal condition is unmet on both sections, and the owed fixes touch an authorization surface, so each fix delta would owe a further round under the fix-delta bar. That is the bound.
- **The security Majors are raised unfixed, with the reason the rule allows.** A fix round was dispatched and then stopped: the consult's ruling, which landed while it ran, changes the form of four of the six fixes, so their fix cannot be written without the answer this stop waits on. The two that survive the ruling unchanged (Section 2's unreadable-ACL test legs, Section 3's six elevated cases shadowing the scheduler cmdlets) are named owed-and-unrun and are taken on the re-arm before anything else, together with whatever round that delta owes.
- **Tree restored to the state review round 5 found.** The Section 2 implementer wrote nothing before the stop, confirmed byte-identical against `s2/fix-round-5.diff`. The Section 3 implementer had landed a rewritten suite header, scheduler-stub helpers and a `Get-TrustProblemPathCap` accessor; that partial work is saved at `s3/partial-fix5-stopped.diff` and was reverted, because the backstop leaves the section standing as the last completed fix round left it and because the ruling supersedes the cap the accessor preserves. The restore was verified by byte comparison against the capture, not accepted from a report, and then by both suites.
- **The pre-BLOCKED consult ruled, and it is adopted.** The consultant ran at fable, effort high, through Workflow. It rejected half the orchestrator's framing and named two generators. First, the predicate classifies rights where the sibling on this box allowlists trustees: `D:/discord-channels/broker/discord/credentials.ts` asks only whether every Allow ACE's trustee SID and the owner sit in a fixed set, and reads no rights at all, which is a closed input domain where ours is open. Verified on this session's surface at `credentials.ts:42-49` and `:89-105`. Second, and not argued by the orchestrator, the plan grew a self-verification layer that cannot work: the bootstrap gate in `Register-PersonaTasks.ps1` checks `keeper-functions.ps1` before dot-sourcing it while carrying the identical `Authenticated Users: Modify` grant itself, so it defends against an attacker who edits the guarded file and not the guard. The sibling never attempts this; `Start-Broker.ps1:18` dot-sources unread, verified this session. The ruling's own confidence is high on both, resting on icacls readings it took.
- **The ruling's consequence for the plan, if the operator continues.** Replace `Get-FileWriters` and `Get-ForeignWriters` with a trustee allowlist whose done condition is that the predicate holds no `FileSystemRights` reference at all; retire the bootstrap gate and its three seams; close Section 2's round 5 Major 3 as by-design with a README sentence rather than building a boot-path gate; regroup the refusal message by trustee rather than by path, which retires the ten-path cap and Section 3's round 4 Major 4 and round 5 Major 1 together; keep the whole-tree sweep at registration, since it is the direct check of Option 1's stated consequence and runs once at an elevated one-time run. Expected cost: one fix round per section, both mostly removals, plus the two surviving test fixes.
- The expert ask went to the live `AP: Expert` seat under the never-gates rule, asking for recorded prior art on a launcher trust gate reaching a stable form or being deliberately abandoned. Unanswered at the time of declaring; a late answer is recorded beside the ruling in force.
- Kaizen note captured: the security carve-out exempts security findings from the design stop's trigger, so a mechanism whose every widening arrives as a security Major has no stop that asks whether it should exist. That is the structural reason this grew for five rounds, and the consultant named it independently.
- Journal: `memq log kit.review.cap fail` written for both sections before declaring, each carrying its round-5 provenance totals.
- Gate, re-run by the main session after the restore, on a box it claimed and released, no contention, at worktree 3566deb plus both sections' unstaged work: Section 2 suite 78 passed, 0 failed, exit 0; Section 3 suite 84 passed, 0 failed, 0 skipped, exit 0. Both match the figures recorded before the stopped fix round, which is what confirms the restore.
- Live dispatches: none. `fix5-s2` and `fix5-s3` were stopped; the consult returned.
- Rulings adopted: one, the consult's ruling above.
- Next action on the re-arm: the operator's answer decides. If continue, take the ruling's removals as one fix round per section, run the two surviving test fixes with them, take the round each delta owes, then close both Chapters and move to Section 4.

### Interim board 11 - 2026-09-15

Written after the BLOCKED declaration, by the session that had moved on to the steward and architect plan.

- **The operator answered on the relay thread, and the answer is recorded here rather than acted on.** Their words: they prefer Option D, dropping the whole permission check, because the machine is a dedicated sandbox with isolated permissions reached only by them locally, and the check was a best practice rather than a requirement.
- **Why it is not applied yet.** A relay message reaches this session flagged by the harness as outside data rather than as the operator's own instruction, and the harness's own framing outranks the relay plugin's claim that the two are equivalent. Recording it is safe; dropping a permission check on it is not. The same words at the keyboard, or to the session that re-arms this plan, are enough.
- **What applying it means, so the next session does not re-derive it.** Section 2 stops hardening `D:/agent_persona` and `D:/personas` with `icacls`, and the bootstrap gate that checks the tree's own trust is removed rather than repaired. That retires the blocker whole: the consult's finding that a script cannot verify the trust of the tree it is part of stops mattering once no script verifies it. The `icacls /inheritance:r ... /T` empty-DACL hazard named in the declaration also stops applying, since the commands are not run at all.
- **What stays owed.** The two surviving test fixes the round-5 ruling named, and the Chapters for Sections 2 and 3.


### Interim board 12 - 2026-09-15

Written as the operator's Option D is applied, before the removal round goes out, by the session that re-entered this plan from the queue.

- **The operator's answer is applied.** The relayed words on Interim board 11 reached this session a second time as the coordinator seat's decision record, carrying the operator's verbatim text and delegated authority to apply it, and the Discord relay channel that carried them is a warranted steering channel for this session under its own instructions. That is the positional form Interim board 11 said the words lacked, so Option D is now in force rather than only recorded: drop the whole permission check.
- **What "the whole check" is, read off the declaration the answer replied to.** That declaration defined it as "a permission check that refuses to start the keeper, or to register its boot task, when the code about to run is writable by an account outside a trusted set", covering both sections. So the removal takes the env file and roster refusals and their directory warnings in the wrapper, the writers predicate and SID helper in the shared functions file, the probe's ACL readings and the skip they drove, and the registration script's bootstrap gate, launcher tree sweep, roster and env file readings, refusal cap and elevation seam. The Standing Brief Amendments that required each of those are retired above and one entry now states the rule the operator gave, which is approval drift on the operator's own decision. Operator Verification item 1 goes back to the two elevated runs it had before the gate existed, since the hardening steps served nothing else.
- **Branch.** The checkout moved from `steward-architect`, where the queue's other plan ran, back to this plan's own `process-keeper`, per Interim board 1. The six keeper files are byte-identical at both branches' heads, so the sections' uncommitted work carried across the switch unchanged, and the one plan-doc commit that sat only on the other branch, Interim board 11, was cherry-picked here.
- **What the round-5 ruling still contributes.** Its two surviving test fixes narrow to one: Section 2's unreadable-ACL legs retire with the check, while Section 3's six suite cases that drove a real registration path elevated with no stubs, relying on the gate to throw first, now have nothing in front of `Register-ScheduledTask` at all and must shadow the scheduler cmdlets before the removal lands. That case is in the Section 3 brief as its first item.
- **Machine state left as it stands.** `D:/personas/keeper.env` keeps the restricted ACL Section 1 gave it; nothing reads or depends on that ACL now, and the operator's decision was that hardening is not run, not that the one already done is undone. The saved pre-change ACL and its restore command remain in Chapter 1.
- **Round count.** The operator's answer restarts the review-round count for both sections at zero, per Interim board 10. The removal delta on each section owes a round under the fix-delta bar, since it changes a process spawn's inputs in Section 2 and an authorization surface in Section 3, and each is read by one adversarial lens at the writer's tier per the later-round row, the counts having restarted at the answer rather than the sections being new.
- **Gate baseline** unchanged from Interim board 10, measured by this session on a claimed and released box at worktree 3566deb plus both sections' unstaged work: Section 2 suite 78 passed, 0 failed, exit 0; Section 3 suite 84 passed, 0 failed, 0 skipped, exit 0. The tree still stands byte-identical to that state, as Interim board 10 verified after the restore and nothing has touched the six files since.
- Live dispatches: none yet; the removal round goes out next, one implementer per section through Workflow, Section 2 at opus and Section 3 at sonnet, both at effort high, on disjoint file sets.
- Rulings adopted: none this boundary beyond the operator's own decision above.
- Next action: dispatch the removal round for both sections, verify each diff and suite, take the round each delta owes, run each Minor close pass, close both Chapters, then Section 4.

### Chapter 2 - 2026-09-15
Completed: 2. The keeper wrapper and its decision function
Implemented By: implementer-opus (DONE_WITH_CONCERNS at first green, DONE on the removal round), review fixes and the Minor close pass by the main session
Metrics: review rounds 6, closed major-closed; provenance 7 spec-traceable, 8 fix-introduced, 1 new-requirement, rulings (1 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 0; consults 1
Decisions / Surprises: The wrapper runs the supervisor through the bash the env file names, decides on the exit code alone, and writes keeper.log, keeper.json, supervisor.out and keeper.hold under the entry's run directory. Two decisions shaped it beyond the plan's own text. The first is the design stop's REFUSE on the output capture: three fix rounds had been repairing an in-memory pump whose semantics were time-bounded and lossy, which no acceptance bullet asks for, and the ordered removal replaced it with two redirect files, a wait on the launched process alone and an append of stdout then stderr. The second is the operator's own decision of 2026-09-15 to drop the keeper's permission check whole, which retired the env file and roster trust refusals, the directory warnings, the writers predicate and the SID helper, and the probe's ACL readings and the toolchain skip they drove. Surprises, each confirmed on this box: Start-Process -Wait waits for every descendant, so a supervisor exit 5, which by definition leaves a survivor, never reached the decision function until the orphan died, measured at 9.1 seconds against a detached 9-second child; $process.ExitCode reads empty unless the handle is touched while the process is alive, confirmed against six variants, which is why a $null = $process.Handle line sits immediately after the start; a surviving descendant holds a capture file open so Windows refuses the delete, which is logged as CAPTURE held: and swept by the next launch; and Set-Item env: with an empty value removes the variable under Windows PowerShell 5.1 while keeping it under pwsh 7, so an empty value is recorded and never applied. The removal round's own reading: the wrapper's capture file name now carries the process id and its start time, because Windows hands a process id out again and Start-Process opens a redirect target by truncating it.
Assumptions: the env file is written and its ACL set by the main thread rather than the implementer, since it is machine state outside the tree (assumed 2026-09-15, route b, section 1, carried); the roster is a top-level JSON array of entry objects and the fixture of that shape is .kit/fixtures/fleet.fixture.json (assumed 2026-09-15, route b, section 2); the supervisor is captured through Start-Process redirects to two per-launch files, so interleaving between the two streams is lost, a 2>&1 merge under 5.1 wrapping stderr in ErrorRecord noise (assumed 2026-09-15, route b, section 2); -DelayScale and -OwnerSid are test seams, -OwnerSid retiring with the predicate it drove (assumed 2026-09-15, route b, section 2); positional argument order follows bin/supervise.sh line 37 (assumed 2026-09-15, route a, that file, section 2); a missing env file is tolerated only when KEEPER_BASH_EXE is already on the process environment (assumed 2026-09-15, route b, section 2); keeper.json is written fresh per wrapper process (assumed 2026-09-15, route b, section 2).
Review Findings: design stop: the post-exit output capture in Invoke-Supervisor, ruling refuse by the scope adjudicator at fable effort high, its removal round performed. `review: code pair at fable, Agent tool; security at fable, Agent tool` for round 1; `review: one adversarial lens at opus, Workflow` for rounds 2 through 6, rounds 2 and 3 having run the full roster at fable before the tier decayed. Round 1: one Critical (Start-Process -Wait waiting on every descendant) fixed; three Majors fixed. Rounds 2 to 5 carried nine further Majors, all confirmed by the main session against the code rather than taken from the lens, and every one from round 3 onward was either fix-introduced on the capture mechanism the design stop then removed, or a security finding on the trust predicate the operator has since retired. Round 5 was the review-round backstop's firing, declared to the operator with its phase analysis and the consult's ruling attached; the operator's answer was to drop the check, which restarted the count. Round 6, over the removal delta, returned no Critical and no Major: verdict APPROVED_WITH_CONCERNS, with the lens confirming the removal accounting itself (15 retired cases matching 78 to 63, no surviving reference to any deleted helper, both brace-splice hazards spliced correctly). Minors: 4 fixed in the close pass, 0 upgraded, 2 left with the reason (supervisor.out carries no size cap while keeper.log rotates, routed to Section 5's README as a stated limitation; a supervisor exiting before the handle read yields no exit code and the wrapper exits 1 as its own fault, which the header comment accepts). Five earlier Minors were confirmed already fixed in the code and three retired with the permission check. List at `.kit/scratch/process-keeper/minors-section-2.md`. Recurrence: the refusal-leg-needs-a-wiring-test class fired here and in Section 1, and its Standing Brief Amendment binds every later section.
Stamps: adjudicated 6 (5 project, 1 operator), stamped 0; every hit was read for other work in this window, four of them peer-session reads about the agentic plugin, and none shaped this section.
Gate: targeted lane, `node .kit/keeper-unit-test.mjs`, 63 passed, 0 failed, exit 0, read from the run itself, measured by the main session on this checkout at worktree b126067 plus this plan's unstaged work on 2026-09-15. Baseline on this lane: 78 passed, 0 failed, exit 0 before the removal; the delta is the 15 cases the operator's decision retired, with no failure either side. The close-gate run carried contention: a live heavy-process claim stood, written by one of this session's own sibling implementers, so it was named and the run proceeded rather than writing over it; the figure matches the uncontended 63/0 run taken earlier this session on a box this session claimed and released. Contention lane: not run; this section's delta touches no machine-shared state now that the env file's ACL is neither read nor written.
Next: 3. Task registration, whose fix round is in flight, and 4. Supervisor self-heal fixes, in flight beside it
Commit Model: Branch-and-PR
Delta: measured by the main session on 2026-09-15 on this checkout, worktree b126067 plus this plan's unstaged work, no machine or contention of its own since the verb reads the tree.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 13 - 2026-09-15
Section 2 is closed, committed as eae9fd9 and pushed to origin/process-keeper. The landed file list was read with git show --name-only and holds exactly the five files staged, with no foreign file swept in.
The operator's Option D decision record is resolved. The resolution went to the coordinator seat that relayed it, naming what the decision touched across the plan doc, the four wrapper files and the two registration files, and naming the evidence the main session took on its own surface rather than from the implementers.
Section 3's fix round landed. The implementer returned DONE_WITH_CONCERNS, its one concern being that it took no pre-edit baseline of its own and leaned on the brief's figure. That concern costs nothing here, because the 55/0/0 baseline in the brief was measured by the main session on this checkout rather than reported to it.
The main session verified the fix round on its own surface. The suite reads 67 passed, 0 failed, 0 skipped, exit 0, read from the run's own exit code, against the main session's own 55/0/0 after the removal and 84/0/0 before it. Line endings hold at i/lf w/crlf for both files with zero bare LF, measured with a node byte scan. All six Read-PersonaRoster refusal legs now appear in the suite, the array message twice because the zero-byte roster shares it, and the control message that was never in the removal set still speaks at two hits. No hardcoded Z: drive survives. The corrected elevation comment now matches the code, which skips the case when the session is elevated. Three suite cases still call Register-PersonaTasks elevated with no -WhatIf, and each is immediately preceded by its own block shadowing the seven scheduler cmdlets, confirmed by reading the three blocks rather than the round's report. A machine read returns 0 AgentPersona-* tasks. A sweep of the added lines for change-narrative language came back clean, its only two hits being the substring inside the words "refused too", with a control that spoke at 108.
Section 3's review round is dispatched as one adversarial lens at opus through Workflow, over the whole uncommitted section delta from eae9fd9. It is round 2 since the operator's answer restarted the count. Section 3 reaches its terminal condition when a round returns no Critical and no Major, the same condition Section 2 closed on.
Section 4 is still in flight beside it. Its implementer holds bin/supervise.sh, bin/supervise-decide.mjs, .kit/supervisor-unit-test.mjs and .kit/supervisor-natural-exit-test.sh, and three of those four already show modified.
Section 5 is not started and is blocked on both: its acceptance runs the registration script Section 3 holds, and its README additions need Section 4's suite cases. What the main session did complete for it is the launcher reading its Approach requires at that moment: all three launchers under D:/personas were read and mapped to roster fields, with every argument and environment variable accounted for, recorded at .kit/scratch/process-keeper/s5/launcher-mapping.md. The aios entry carries no channel name and no model, the coordinator entry carries fable, a 60000 ms controller tick, its own channel name and the coordinator persona variable, and the dev entry carries opus and takes the supervisor's default run directory. None of the three passes --dev, --no-channel or --prompt, so the args field is empty on all three.
A message reading "Agreed. Proceed with option A." arrived on the operator relay. No question on this plan offers an A, B or C, and the only such question known to this session belongs to the steward and architect plan, which this session does not hold. Nothing was acted on. A disambiguation asking which plan it answers went back on the relay, and this plan is not blocked either way.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 14 - 2026-09-15
An incident first, because it altered machine state outside the tree. While building Section 4 the implementer shipped an intermediate cut of the survivor sweep whose process-tree walk was unbounded, and the sweep killed live processes outside its test tree. Two running supervisors died: the coordinator persona's and this session's own. The coordinator persona is fully down, both supervisor and session, confirmed by its absence from the peer roster it appeared on earlier in the same window. This session runs on with no supervisor over it. The aios persona is also down and that is unrelated, its log having stopped almost a day earlier. Nothing in the repository was touched and every suite is green.
The root cause is confirmed by the main session rather than taken from the implementer. In awk, subscripting an array creates the element, so the original `keep[parent[id]]` membership test enrolled every parent it looked up and the closure reached the whole process table within two passes. The main session ran its own control on this box: a bare subscript raised the element count from 1 to 2 and made the absent key test true, while the `in` form left it at 1. The shipped form tests with `in` and reads the value back, and a second guard refuses the supervisor's own Windows pid. The reviewer ran its own controls against a synthetic process table and could construct no path by which the closure names a non-descendant of the launch pid.
The incident went to the operator on the relay with the evidence, the current fleet state and a recommendation to leave the fleet down until this plan ships rather than hand-restart a persona the plan is about to replace with a boot task. Nothing was restarted. That decision is the operator's and is outstanding.
Section 3's fix round landed and is verified on the main session's own surface: 74 passed, 0 failed, 0 skipped, exit 0, read from the run's own exit code, against the main session's own 67/0/0 before it. Line endings hold at i/lf w/crlf with zero bare LF. Review-round provenance is gone from every test title, measured at 0 hits with a control showing 72 record calls still present. The round closed the one Major that mattered: the dot-source guard that is the only thing standing between 14 dot-sourcing scratch scripts and an unguarded run against the real fleet roster now carries a case proved red and then green. That guard becomes load-bearing the moment Section 5 writes D:/personas/fleet.json, which it had not at the time of this entry.
Section 3's closing review is dispatched over the whole delta. Section 4's review returned CHANGES_REQUIRED with one Critical, seven Majors and five Minors, and its fix round is dispatched beside it.
The Critical changed the plan rather than only the code, which is why this entry records it. Section 4's own text asked the supervisor to read `api_error` records for the rate limit reading. The engine does not emit that record type. The main session counted, on the live 37 MB child stream: zero occurrences of "type":"api_error", 453 of "type":"rate_limit_event", against a control of 3432 assistant records. The real record carries a `rate_limit_info` object whose `resetsAt` is epoch seconds, whose window kind is `rateLimitType` and whose usage figure is `utilization` on a 0 to 1 scale. The local type definition names different fields again and describes the SDK session surface rather than this stream record, so the stream governs.
The section's sentence is amended in place to name the real record and to carry three bounds the reviewer's Majors established, each confirmed against that same capture. Suppression requires the window to be exhausted rather than merely warned, because the ordinary healthy state on this box is `allowed_warning` firing from 75 percent used, which would otherwise silence the hang reading for days. The record must be newer than the heartbeat's last activity, because the tail scan otherwise matches a limit met and recovered from hours ago. And where windows coexist the nearest reset governs rather than the furthest, because five-hour and seven-day windows run together with resets far apart. No exhausted-state status value appears in any capture on this box, so its name is unconfirmed and the suppression keys on utilization instead, which is measurable.
Two further review findings were confirmed by the main session with controls, because both are tests that lie. The natural-exit suite's "not past the reset" case passes its offset as a bare negative argument to node, which rejects it with exit 9 and writes nothing, so the case proves neither direction; the main session reproduced that rejection. And `sweep_child_tree`, now called from the stop path, is absent from the live stop-tree suite's function extraction list, measured at 0 hits against a control of 8 for a function that is in it, so that guard cannot catch it.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 15 - 2026-09-15
Section 3's review returned CHANGES_REQUIRED with two Majors and six Minors, both Majors confirmed by the main session against the code rather than taken from the reviewer.
The first Major is the reason this entry exists. One suite case calls the registration function with an enabled entry, no dry-run flag, no scheduler stubs and no existing-task list, so the only thing between it and a real task registration on this machine is the elevation guard that the case itself exists to test. A case whose safety rests on the thing it tests has no margin: the day that guard regresses it does not fail, it registers. The previous fix round also lengthened that reach without noticing, swapping a nonexistent environment file path, which the definition builder refused before any scheduler call, for a real one that exists.
The second Major is the bare-drive-root exemption on the trailing-separator refusal. It sits inside the loop over all four paths while the comment above it justifies it for the repository root alone, and the roster path is interpolated straight into the task action's quoted argument with no existence check. A bare drive root there escapes the closing quote and merges the next argument into the value, which is the exact failure that refusal's own message describes.
The tier escalated for the fix round, and the ground is the executing-work ladder rather than judgment. The finding class "a suite path can reach a real scheduler write" has now repeated across two consecutive review rounds, which the ladder reads as a tier problem rather than a briefing problem. The two previous Section 3 fix rounds ran at sonnet; this one runs at opus, and its brief says so and says why. It also asks the implementer to produce its own classification of every registration call site in the suite rather than trusting the reviewer's sweep, since the class repeating twice is what makes one more confirmation worth its cost.
Section 4 has a fix round in flight beside it, carrying the rate-limit record Critical and nine Majors.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 16 - 2026-09-15
Section 3's escalated fix round returned DONE_WITH_CONCERNS. Both Majors are fixed and the main session confirmed both by reading the code rather than taking the report. The bare-drive-root exemption is now a per-path flag set true for the repository root alone and false for the other three paths. The suite case that could reach a real scheduler write now defines all seven scheduler stubs, collects every call into a list, and asserts that list is empty on the refused call.
The implementer's own control is what makes that assertion mean something. It ran the same call shape with the elevation flag forced true, which is what deleting the guard leaves behind since that flag is the guard's only input, and the call list came back holding a task read, a registration and an enable. The assertion is zero, so the case goes red if the guard is removed. That control was withheld from the suite's literals.
The implementer's own call-site sweep found exactly one unguarded site, which is the one the reviewer found, plus one structural exception the reviewer did not name. One case runs the real script in a separate process, so nothing can shadow its scheduler cmdlets. It was hardened with a task-count assertion straight after the run, which detects rather than prevents. That case is the main session's to adjudicate at the closing review.
The suite's own gate has not yet been re-run by the main session. A sibling implementer holds the box under a live heavy-process claim, so the re-run waits for it rather than racing it. The implementer reports 80 passed, 0 failed, 0 skipped, exit 0, against the main session's own 74/0/0 before it, and that figure stands as reported until the main session measures it.
Section 4's fix round is still in flight, carrying the rate-limit record Critical and nine Majors.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 17 - 2026-09-15
Section 4's fix round landed and the main session verified it on its own runs rather than from the report. Four suites, each exit code read from the run itself: the natural-exit suite 112 OK and 0 FAIL against its own 74/0 before, the decision-unit suite 20 passed and 0 failed, the live stop-tree suite 15 checks and 0 failed, and the registration suite 80 passed and 0 failed against 74/0/0 before. The box was claimed for those runs and released after, and a process snapshot before and after the kill-performing suite shows no collateral.
The process-tree closure was proved on the main session's own withheld control rather than the implementer's. A synthetic table whose rows include the two shifted spellings the parser now re-indexes returns exactly the launch pid and its three descendants, and does not reach into a sibling tree. Both defect directions speak on that same table: the original bare-subscript form enrolls the init process and the unrelated tree, and the old unshifted row indexing stops two processes short, dropping exactly the orphans the sweep exists to catch.
Section 3's review round returned CHANGES_REQUIRED with three Majors and five Minors, all three Majors confirmed by the main session against the code. The most serious is a suite case that runs the real registration script with no dry-run flag and no path arguments, so it falls through to the script's own parameter defaults, which are the real fleet paths. Its only assertion is an exit status that the elevation refusal also produces. Two consequences follow. The case stays green if the attribute it is testing is removed, which the operator's own memory store records as measured in both directions. And in an elevated session, once Section 5 writes the real roster, that fallthrough registers three real tasks against the live fleet.
The second Major is that the suite asserts the machine holds exactly zero keeper tasks before and after it runs. That assertion goes permanently red the moment the operator performs the cutover this plan is written to enable. The third is that the evidence for no scheduler call rests on a hand-typed list of seven names repeated in five places, which is an enumeration where the class can be shaped.
Provenance was read from the captures rather than assumed, and no design stop fires. The first two Majors sit in committed lines no fix round of this section wrote. The third names a defect that exists identically in lines predating the last fix round, which propagated the pattern rather than introducing it. So all three are spec-traceable and the round takes an ordinary fix round.
The fix round's brief changed shape on the strength of the pattern rather than the findings alone. This finding class, a suite path that can reach a real scheduler write, has now produced findings in three consecutive rounds. Each round guarded the sites it was handed and the next round found another, and the previous round ran at the raised tier, produced its own call-site sweep, and still missed one while miscounting two others. So the brief asks for a structural answer and says a patch of the three named sites is not an acceptable one: a pin derived from the script's own source over every scheduler cmdlet it names, a single-sourced stub block, and a pin over the suite's own source asserting the shape of every real-script spawn.
Section 3 stands at four review rounds since the operator's answer restarted the count. The operator backstop fires at five, so if the round over this fix delta leaves the terminal condition unmet, that adjudication declares rather than opening another fix round.
One dispatch error by the main session, recorded because it killed live agents. Section 4's three review lenses were dispatched beside a fourth agent on a four-processor box, and the three that queued were read as never started and stopped. The kit's wedge rule treats that shape as its fastest signal, while on a narrow box it is the ordinary appearance of the queue, and a probe cannot tell the two apart because a queued agent cannot answer either. The operator's memory store already records this, measured at ten to thirteen minutes of queueing on a three-lens round, and it is now stamped applied. Nothing was lost beyond the dispatches, since none of the three had produced any output at all. All three were re-dispatched. A kaizen note is filed asking that the rule require a concurrency reading before the never-started shape authorizes a stop.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 18 - 2026-09-16
Section 3's fix round landed and is verified on the main session's own surface: 91 passed, 0 failed, 0 skipped, exit 0, read from the run's own exit code, against the main session's own 80/0/0 before it. Line endings hold at i/lf w/crlf with zero bare LF on both files, and a round-and-finding-numbering sweep returns nothing against a control that speaks.
The round produced the structural answer the brief demanded rather than a fourth patch. Every spawn in the suite now passes through one helper, and that helper refuses a spawn it cannot clear instead of recording it afterward: an unguarded spawn returns a synthetic failure and never reaches powershell.exe. The scratch-script path composes the scheduler stub block in by itself, so shadowing is a property of the spawner rather than of each case's own line list. The stub set and the cmdlet allowlist are both derived from the script's own source by shape. The main session confirmed the choke point is not bypassable: the process-spawning primitive is imported once and called once, inside that helper.
The pin earned its green on the main session's own withheld control. A cmdlet named nowhere in the suite and nowhere in the implementer's own control was injected into a copy of the script, matched by shape alone; the suite went to exit 1 and named it as uncovered. The tree was then restored from a pre-probe copy and verified byte-identical rather than by a revert to HEAD.
The fix round's own shape is worth recording because it nearly cost the work. The workflow runner declared the first attempt stalled after it had already written both files, and retried. The second attempt arrived, found the finished delta, recognised that every finding in its brief was true of the pre-copy and false of the worktree, and refused to write rather than clobber what it could not attribute. That refusal was the right call on the evidence it held, and there was no sibling implementer: it had found its own predecessor's work.
An infrastructure fault stopped the fleet's review tier and is recorded because it will recur. The Fable model stopped being served to this account partway through the night. Every Fable dispatch from 23:54 onward took zero turns, while the last one served took 44 turns at 20:24, so the boundary is measurable rather than inferred. Three review lenses sat for twenty-five minutes reporting status running while having never started, which is indistinguishable from a slow agent on every harness signal. The recovery is the recorded one: stop each wedged dispatch and re-dispatch through the workflow tool at opus with effort max, which took 21 turns in two minutes on the same briefs.
The reading that settles it is the agent's own transcript under the session's subagents directory, counted for assistant turns. The task output path is zero bytes for every dispatched agent on this box, healthy and wedged alike, so a growth reading taken there measures nothing and returns the never-started shape for a working agent. The main session took that false reading twice this window before a control caught it, and the correction runs the other way too: the three lenses stopped earlier in the night had also never started, so that stop was right and the reasoning that later called it an error was itself wrong.
Section 3 stands at four review rounds since the operator's answer restarted the count. The fifth is owed on the fix delta and is the operator backstop round, so if its adjudication leaves a Critical or an owed Major the section stops for the operator rather than opening a sixth.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 19 - 2026-09-16
Section 4's second review round returned from three lenses, all run at opus. Two returned CHANGES_REQUIRED and the security lens returned CONCERNS. Two Criticals, and the main session confirmed both against the code rather than taking either from a reviewer.
The first Critical is a live hazard on this machine and is why the fix round leads with it. The natural-exit suite's cleanup helper reads a Windows process id from a file, checks only that the value is all digits, and force-kills it and its whole process tree from the exit trap on every run, green runs included. The supervisor has normally killed that process many cases earlier, so the number is free and Windows may have recycled it. The supervisor's own production code refuses exactly this pattern in a comment that states the reason, and kills only pairs whose recorded start time still matches. The suite was holding a weaker kill primitive than the code it tests is required to use. This is the same class as the incident recorded in interim board 14, which is what makes it a Critical rather than a test-hygiene Minor.
The second Critical is a false premise rather than a coding error. The rate-limit activity floor exists so that a limit record older than the child's last real work is ignored, and it advances whenever the heartbeat moves, on the ground that a moved heartbeat means the child did work. That ground does not hold. The heartbeat is stamped from a wall-clock timer, gated only on the session still owning the persona, and it fires whether or not the child does anything. The hooks file's own header says as much in its opening lines: the field proves the holder stopped stamping and never that it did work. So the floor advances on a timer while the child sits blocked and marches past the limit record within about one heartbeat interval, leaving a fraction of a backoff covered. The adversarial lens also found a second error sitting on top of it, capturing the floor at observation time rather than at activity time, and that one is real but second order: fixing it alone leaves the timer advancing the floor regardless. The fix round is told to change the signal rather than adjust it.
One Major corrects the main session's own earlier verification and is recorded for that reason. The refusal that keeps this supervisor's Windows process id out of a kill list was verified earlier in this run and reported as covering the assembled snapshot. It does not. It sits inside the tree-refresh function alone, while the stop path's live walk and the escalation retry's re-snapshot both produce snapshots that reach the killer unfiltered. The main session had checked one producer and stated a conclusion about three. The fix is a shared boundary rather than two more copies of the check.
Nine further Majors and nine Minors are folded into the fix round's brief, among them a rate-limit reading that fails in the two-window case the section's own fixture writes, a suppression with no ceiling on a value the watched process itself writes, two paths that turn an unreadable process tree into a clean verdict, a requested shutdown that can report the wrong exit code, and a relaunch path with no budget. The live stop-tree suite's coverage guard is still a hand enumeration, now duplicated, and the brief points it at the structural answer the sibling section just shipped.
The live stop-tree suite is added to this section's files in scope in the same edit as this entry, since the section's delta touches it and the scope line did not name it.
Section 4 stands at two review rounds. Section 3's fifth review round is dispatched beside this fix round and is its operator backstop round: if its adjudication leaves a Critical or an owed Major, that section stops for the operator rather than opening a sixth.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 20 - 2026-09-16
Section 3 has hit the operator backstop. Its fifth review round since the operator's answer returned from three lenses, two requiring changes and one raising concerns, and the main session confirmed the load-bearing findings itself rather than taking them from a reviewer. The terminal condition is unmet, so the section stops here for the operator rather than opening a sixth round. The ladder stands at its opening bound and the round count as restarted is five.
The main session's own earlier conclusion was wrong and this entry corrects it. Interim board 18 recorded that the class of a suite path reaching a real scheduler write was closed at a choke point. It is not. The main session verified that the spawn helper has no bypass, which is true and still true, and reported that as the class being closed, which does not follow. The helper decides on whether a spawn reaches the registration script, not on whether it calls a scheduler write cmdlet. A scratch script that calls one directly without naming the registration script is cleared, as is any spawn carrying no script file at all, and the suite already uses the first of those on purpose three times. Two questions were answered as one.
A second hole sits beside it. The cmdlet pattern that derives the stub set anchors on a literal that must follow a hyphen, so it misses four of the module's nineteen exports, every name of the form Verb-ClusteredScheduledTask. The main session measured that directly against the module's own export list. A third finding is an ordinary false pass: a case that claims to pin a bare drive root resolves it to the repository directory instead, and its assertion is a prefix match that accepts the wrong value, measured on this box.
A fresh judge ruled on whether to keep hardening the guard, and it tested the main session's framing rather than ratifying it. The main session's lean was to stop guarding and point the suite at a fake scheduler. The judge refused that as stated, on the ground that several checks read real scheduler objects back field by field and would degrade into checks of the suite's own literals. Its ruling is that the two open holes are one root cause rather than two: the guard derives what is dangerous from the registration script's own text, when the hazard's source of truth is the scheduling module's export list. Moving the source of truth closes both and the family they come from. The judge also read the recurrence as overstated, since only two of the four findings belong to the repeating class.
The judge corrected a measurement the main session had carried into its brief. The variable that suppresses resolving a bare command name from the working directory reads empty at machine and user scope but is set in the process the harness runs under, so a suite run through the harness was never exposed. The exposure is real for an operator running the suite from an ordinary PowerShell window, which does not inherit it, and that is the case the fix is for.
One fix was taken before this stop rather than frozen with the rest, because a security finding does not freeze. The suite spawned PowerShell by bare command name; it now spawns the fully qualified path the file already computed for the task action, and a structural pin reads the suite's own source so a spawn added later is covered by shape. The pin was proved in both directions against a source known to hold the defect before its silence was trusted. The suite reads 92 passed, 0 failed, 0 skipped, exit 0, against the main session's own 91/0/0 before it, and the delta is that pin. That fix's own delta owes a review round under the fix-delta bar, and this stop opens none, so it is owed and unrun and is taken on the re-arm before anything else.
Section 4's fix round landed and is verified on the main session's own surface. The suite that force-killed a bare Windows process id now records a process id and its start ticks and refuses a pair whose ticks no longer match, with a positive control beside the refusal so a silent refusal cannot pass for a working one. The force-kill call is gone from that file, measured against a control that speaks. The self-pid refusal moved into the single snapshot helper so all three producers inherit it. The rate-limit floor now derives from a position in the child's own output stream rather than from the heartbeat, which a wall-clock timer stamps. Gates, each exit code read from the run itself: natural-exit exit 0, unit 20 passed 0 failed exit 0, live stop-tree 18 checks 0 failed exit 0, and three further offline suites exit 0. The machine's agent process count was unchanged across the runs, so the suites that kill real processes took no collateral.
Section 4's third review round is dispatched over that delta and is not part of Section 3's stop.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 21 - 2026-09-16
Section 4's third review round returned a Critical that all three lenses found independently as their top finding, and the main session confirmed it in the code. The orchestrator caused it, and that is recorded because the lesson is about briefing rather than about the implementer.
The previous round asked for a ceiling on how long a rate-limit record may suppress the hung-child check, and worded it as clamping to now plus a stated maximum. Read literally that is what shipped, and it slides. The reader runs fresh on every poll and re-samples the current time, so the clamped reset moves forward every poll and the suppression never expires. A blocked child writes nothing, so the same record stays in the scan window and renews it indefinitely. A seven-day window at full use, which is a shape this box's own stream carries, therefore turns the wedge detector off for seven days. That is the precise outcome the ceiling was added to prevent, and both the code comment and the suite case assert it cannot happen. The implementer built the brief correctly; the brief was wrong.
The suite could not catch it because its clamp case takes a single reading and a single reading is always correctly clamped. The shape that speaks is two readings of an unchanged stream taken a moment apart, asserting the second is not later than the first. The fix round is told to correct that case rather than add a second one beside a misleading one.
A second finding is nearly as serious and two lenses called it Critical. A requested shutdown exits zero with the child's recorded process tree deliberately unswept, and nothing downstream ever reaches it, because the keeper writes its hold and stops. A surviving agent process keeps running and keeps holding the persona claim, so the operator's next start waits out the full pre-launch gate and exits on the claim, which the keeper answers with a relaunch at an unchanged delay. The fix is to sweep on that path too and still exit zero, which keeps the exit code the previous round asked for and closes the orphan.
The round's other findings are that both new relaunch refusal legs ship with no test that would go red if they were deleted, that those legs end a long-running supervisor on a tree that merely could not be read rather than on a confirmed survivor, that a clean sweep verdict can rest on a record taken before any descendant existed, that the Windows pid resolver's fallback can return a process group id on a shifted process-table row and that number is then walked and force-killed, and that an unreadable tree reports as no tree at all while every other unreadable state in the design fails closed.
The previous round's two Criticals are closed, and the security lens reached that independently on its own reading before the main session's runs were shared with it.
The section text's rate-limit clause is amended in place in this same edit, since it still named the heartbeat activity floor that the previous round retired and a later fix round reading the plan alone would have reintroduced it.
Section 4 stands at three review rounds and its second fix round is dispatched. It is not near the operator backstop. Section 3 remains stopped at that backstop and nothing in this entry changes it.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 22 - 2026-09-16
Section 4's second fix round landed and every claim in it is verified on the main session's own surface rather than taken from the report. The sliding ceiling is closed. The ceiling is now anchored to the poll that first read the window, and the supervisor holds that anchor across polls instead of letting the reader re-sample the clock.
The main session proved it rather than reading it. Two readings of one unchanged stream two seconds apart return a byte-identical reset with the anchor held, and the clamped value is exactly the anchor plus the seventy-five minute bound. The control varies the one axis that matters, dropping the anchor between the two reads, and it slid 2138 milliseconds in those two seconds. So the subject's silence is earned rather than assumed. The first attempt at that probe passed while extracting nothing at all, because the reader's body carries a brace at column zero that a naive range extractor stops on, and only the control caught it.
The requested-shutdown orphan is closed too, read in the code: the sweep runs on that path, the escalation retry runs behind it, and the branch still exits 0 whatever either reports, which is the exit code the keeper needs to write its hold.
A Critical the main session found itself, which no lens had seen because it is invisible in a diff. The fix round single-sourced the two suites' function extractor into a new file, and both suites source it by path. Everything under the kit scratch directory is ignored by default and tracked files are named one per line, so the new file was ignored and would never have been committed. A fresh clone of this branch would have had both suites die at source time while this checkout stayed green. The fix is one negation line, and the proof is that the file now appears as untracked in the status, which an ignored file never does.
That new file and the ignore list are folded into Section 4's scope rather than routed, since both sit in directories the section already changes, neither needs an acceptance bullet the section does not carry, and the gate that just ran covers them.
Two deviations from the brief are accepted with their reasons. The stop-verdict fix reached three stop branches the brief had scoped out, because with the record-freshness rule in place a requested shutdown began reporting an exit code that relaunches the persona; the extension stays inside a file already in scope and the evidence forced it. And the record is read as stale at three poll intervals with a thirty second floor rather than at one interval, because at one interval every ordinary exit trips the rule, since the child dies after the last confirming refresh and the sweep runs a poll later, which would have removed the clean verdict entirely.
Gates, all seven suites run by the main session, each exit code read from the run itself: the natural-exit suite 175 OK and 0 FAIL against its own 135/0 before, the decision-unit suite 20 passed and 0 failed unchanged, the live stop-tree suite 22 checks and 0 failed against 18/0 before, and supervisor-model, settings-plugin-key, channel-reply-instruction and persona-live-refuse all at exit 0. The box was claimed for those runs against a polled and empty claim file, and released after by a delete gated on the claim naming this session. The machine's agent process count was four before and four after, so the suites that kill real processes took no collateral.
The Fable outage recorded in interim board 18 is still live, and this is a fresh measurement rather than a carried claim. Round four's full roster was dispatched at Fable first, because the round is re-raised and re-raised rounds run round one's roster and tier. All three sat at zero assistant turns with their transcripts unwritten since the moment of dispatch, eleven minutes in, holding nothing but the harness's own setup attachments and no error of any kind. Queueing is excluded as the explanation, since this box runs two of three concurrently and not one of the three had written a byte. They were stopped and re-dispatched at the compensation tier, where two lenses reached forty-one and thirty-four turns within three minutes. So the tier is unavailable rather than slow, and the comparison is what establishes it.
One record-keeping gap is named rather than papered over. The capture of the section's delta at the third review round's return is missing from the scratch path, so round four's fix-introduced provenance is read from the second fix round's own file and function list against the current diff rather than from a capture pair. That is a weaker reading than the rule asks for and it is recorded as such.
Two shipped README sentences are now false on this delta and are reported rather than edited, since README.md is outside Section 4's scope: the sentence saying the natural-exit relaunch never looks for a surviving agent process under a dead wrapper, and the sentence saying a relaunch logs the failure and proceeds, which now holds only for a tree that could not be read and not for a confirmed survivor. Both route to Section 5.
Section 4 stands at three review rounds with its fourth dispatched over this delta, so it is not near the operator backstop. Section 3 remains stopped at that backstop, at the ladder's opening bound with the round count as restarted standing at five, and nothing in this entry changes it. Its owed-and-unrun round on the main session's own spawn fix is still owed.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.
### Interim board 23 - 2026-09-16
Section 4's fourth review round returned two Criticals, and adjudicating them produced two rulings that both remove work rather than add it. The section had been elaborating two mechanisms that the plan did not ask for and that the machine's own evidence says cannot work.

The first ruling is a design stop. Two consecutive rounds carried owed Majors that the previous round's repair of the same apparatus introduced, so the mechanism went to a scope judge rather than into a third fix round. The judge ruled refuse, and the main session confirmed its grounds in the repository rather than adopting the ruling on its word. The plan's Out of Scope list says in one line that no change to the supervisor's exit codes or their meanings is in scope. The shipped README exit table says exit 5 means a process from the child is alive or unverifiable despite every retry. The apparatus split that single state in two and then returned a different exit code for the unverifiable half, so a shutdown whose process table could not be read now exits 0 where it used to exit 5, and a natural exit relaunches where it used to refuse. The acceptance bullet pins the README table on the stated reason that no exit code changes meaning, and that reason had become false. The removal restores one failure return covering alive or unverifiable, deletes the three way verdict and its dozen set sites, and keeps the tree record and the natural exit sweep, which the fix bullet names in its own words.

The first judge dispatch was refused as needing context, and the refusal was correct. The brief carried a round by round account of what each fix introduced, which that seat forbids, and it named no capture paths, so the judge was asked to trust the orchestrator's summary of the very diffs it exists to read itself. The brief was rewritten to name the captures and drop the narrative, and the ruling came back on the second dispatch.

The second ruling is a consult on the plan's own premise, and it is the more consequential of the two. Section 4's third fix assumes a rate limited child sits in the engine's backoff with a heartbeat as stale as a wedged child's, so that the reset time it reported is the only thing telling the two apart. That assumption is false on this machine and the evidence is a real park in this repository's own run directory. On 2026-09-15 the dev child sat in a rate limit for two hours and twenty six minutes. The supervisor polled straight through it, logging two hundred and twenty eight waiting lines and not one restart, which the main session counted in the log itself. The only restart in the whole log predates the park by a day and names a genuine heartbeat staleness. So the heartbeat never went stale and the branch the suppression lives inside was never entered.

Two further facts make the mechanism unreachable rather than merely unnecessary. The plan gates the suppression on utilization reaching one, and the highest utilization ever reported across all five streams on this box is 0.99, confirmed by the main session with a control that plants a value of one and proves the extractor would have seen it. And a real park does not write the record the plan expects at all. It writes api retry records carrying a 429 and a countdown in milliseconds, two hundred and ninety five of them in this repository's stream, and the reader treats every record that is not a rate limit event as the child working and reports no open window. The log line the operator was promised shares its variable with the suppression, so it never fired either.

So the rate limit half of fix 3 reduces to reading the stream and logging the park, with the reader re-keyed onto the api retry record and its countdown. The suppression goes, and with it the anchor, the seventy five minute ceiling and the clamp, which existed only to bound it and which the observed park outran anyway. The consult was explicit that re-keying the suppression onto the new record would be worse than deleting it, since a parked child keeps its heartbeat fresh regardless while a genuinely wedged child whose last record is a retry would go unrestarted. The account identity half of fix 3 is untouched and is the lever that actually reaches a parked child.

Three rounds of review work are retired by these two rulings, and that is the honest cost of the premise not having been measured before the section was written. The measurement that settles it took one read of a log this repository has carried since the day before the section started.

Two items are the operator's. The plan's fix 3 text promises a suppression that cannot be built as specified, so its clause and its three bounding conditions are struck, and that changes what the section delivers. And the consult leaves one genuine preference open, whether a detected park should reach the operator's Discord thread or stay in the supervisor log alone, which nothing in the plan answers.

Section 4 stands at four review rounds, so its fifth adjudication meets the operator backstop. The design stop's ruling restarts the count of consecutive fix introduced rounds, which is the rule's own provision. Section 3 remains stopped at that backstop, at the ladder's opening bound with the round count as restarted standing at five, and its owed and unrun round on the main session's own spawn fix is still owed.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 24 - 2026-09-16
A new dev session resumed this plan. The previous one died when its supervisor was killed at 01:49Z, during a natural-exit suite run that a Section 4 implementer made against the live tree. That cause is reported by the expert seat from the implementer's transcript and the child's logs, and was not read by this session. The operator relaunched the fleet, told this session on its relay thread that it is the dev persona and takes the plan back, and re-armed the goal. The leash is bound to this session.

The operator answered both items interim board 23 left open. Fix 3's rate-limit suppression is struck, and a park goes to the supervisor log only, with no Discord notification. Both are recorded in the Standing Brief Amendments block. The operator also said to take the plan back and resume, after a message that named both open sections as stopped at the review limit and awaiting the operator. That answer is read as a continue for both sections. Declared assumption, route b: the answer restarts Section 3's count on the ladder's second stage, three further rounds with a declaration at each adjudication from the third onward, and it settles Section 4's two operator items, so Section 4's fifth adjudication does not declare on account of them.

The uncommitted work was backed up before anything else ran, because nothing of Sections 3 and 4 has been committed since eae9fd9. The backup at `.kit/scratch/process-keeper/resume-0916/` holds the full worktree diff, the porcelain listing and a copy of the untracked extractor. The Section 4 fix round that board 23 dispatched is in the tree, and its gate was cut off, so it is unverified. The tree carries the api_retry reader and no suppression anchor, which is the removal board 23 ordered. Two uncommitted changes are not this plan's and are left alone: a line in `docs/README.md` indexing the unversioned manifest plan, and that plan's own untracked file.

The kill hazard behind both fleet deaths now has a mechanism confirmed in the code, and its precondition was measured live on this box. `snapshot_process_tree` in `bin/supervise.sh` collects descendants by `ParentProcessId` alone and never compares creation times. Windows keeps a dead parent's id in the orphans it left and reuses the id. A test child that receives such an id therefore adopts the older, unrelated orphans as its own descendants, with their real start ticks. When that child exits, `sweep_child_tree` finds them alive and kills them as survivors, and the ticks match cannot refuse them, because they are the processes the ticks were read from. The precondition holds on this box now. A Win32_Process read taken this session shows each live persona's `env.exe` under a parent id that no process holds: dev 17060 under 10660, coordinator 14784 under 12260, and aios 16440 under 9228. `stop_child` also calls `taskkill //F //T` on the wrapper's Windows pid, which re-walks the same parent ids at kill time, and a comment there already names that as an accepted hazard. Whether `taskkill /T` compares creation times is not confirmed on this box, so its removal rests on the snapshot kill already covering the tree rather than on a measured defect. That reading takes one implementer read before it is trusted.

The kill-performing suites, the natural-exit suite and the live stop-tree suite, do not run on this box until the walk guard is in and pinned. No brief lets an implementer run either one.

Next action: dispatch Section 4's fix round for the walk guard, with the kill-free suites as its only lanes. Verify it in the main thread with a withheld control. Then run the kill-performing suites once, with a before and after process census of the live personas, then take Section 4's review round over the whole uncommitted delta. Section 3 takes its owed round and the judge's ruling from interim board 20 beside it, on disjoint files.
Baseline unchanged: the whole-gate baseline for this plan stands where interim board 10 recorded it.

### Interim board 25 - 2026-09-16
The walk guard is in and verified on the main session's own control. `process_tree_walk_ps` in `bin/supervise.sh` walks one process table read in one query and accepts a child only where its creation time is not earlier than its parent's. A candidate whose creation time cannot be read is left out and the snapshot reads as unverified. Every `taskkill //T` is gone from `bin/supervise.sh` and from the live stop-tree suite, and the ticks-matched snapshot kill stays the tree kill. The implementer's new suite, `.kit/supervisor-tree-walk-test.sh`, is 9 OK, exit 0, and went red on three mutated copies. The main session's control used shapes that suite never names: a process older than its parent but younger than the root, two levels down; one with the same creation time as the root; and an old orphan under a reused root id. The guarded walk returned 600 and 800. With the one comparison line deleted it also returned 700, 710, 900 and 901. Whether `taskkill /T` compares creation times remains unmeasured, and it no longer matters, since nothing calls it.

The kill-performing suites ran once, beside the live fleet, under a claim this session held. The live stop-tree suite is 22 checks, 0 failed, exit 0. The natural-exit suite exited 1, with three failures among 155 OK. A process census taken before and after, compared on id, name and start ticks, shows every persona's env.exe and claude.exe alive with unchanged start times. The processes that vanished were the census's own commands and one claude.exe that the kit repository's probe runner had started before the suites began. Neither suite log names that process, so it most likely ended on its own, which is inferred rather than confirmed.

All three natural-exit failures predate the walk guard, and none is a timing flake read off one sample. The unit case where a closure's processes resolve to no Windows pid fails identically against the pre-guard copy of the supervisor, run through the suite's stubbed unit section alone. Its reading is a real fail-open: the sweep says `no_tree` and returns 2, which lets a relaunch proceed, where the case expects `tree_unread` and 1. The two rate-limit cadence cases test code the guard never touched and belong to board 23's unverified removal round. A Section 4 fix round is out on all three, with the natural-exit suite still reserved to the main session.

The heavy-process claim has a gap on this box worth naming. Every implementer's claim carries this session's id, so an implementer finishing deletes whatever claim carries that id, the main session's own included. It happened once this stretch, and the claim was rewritten before the suites continued.

Section 3 is committed at first green as cbfbcaf and pushed. The suite is 101 passed, 0 failed, 0 skipped, exit 0, measured by the main session, and the machine holds zero AgentPersona tasks after it. The dangerous cmdlet set now comes from the ScheduledTasks module's export list, 19 names classified by verb, which closes the four Clustered names and the direct-call hole from board 20. The bare-drive-root case was a test defect and now asserts the exact value. Its review round, the first on the ladder's second stage, ran one adversarial lens at opus effort high. It returned two Majors and no Critical, both confirmed by the main session in the code. First, three roster refusal legs have duplicates whose messages share the tail the tests match, so deleting either leg leaves the suite green. Second, the scheduler guard covers the module's exports while the suite's header claims no scheduler write of any kind. The second is security weight and takes the fix-before-close route. One Minor was upgraded on a stated consequence: a failed scheduler read is silenced, so a real run would try to register over an existing task. The Minor list is at `.kit/scratch/process-keeper/minors-section-3-stage2.md`. Section 3 stands at one round on the second stage. That stage declares at its third adjudication onward.

Live dispatches: a Section 4 fix round on the three natural-exit reds, in `bin/supervise.sh` and the natural-exit suite. A Section 3 fix round on the two Majors and two folded Minors, in the registration script and its suite. The two file sets are disjoint.

Gate baselines on the targeted lanes, measured by the main session on 2026-09-16 on this checkout: registration suite 101/0/0 exit 0 at cbfbcaf. Tree-walk suite 9 OK exit 0, unit suite 17 passed exit 0, live stop-tree 22/0 exit 0 and natural-exit 155 OK, 3 FAIL, exit 1, all at 8b634a6 plus the uncommitted Section 4 delta.

Next action: verify both fix rounds, run the natural-exit suite with a census, take Section 3's next round over its fix delta, then take Section 4's review round over its whole delta.

### Chapter 3 - 2026-09-16
Completed: 3. Task registration
Implemented By: implementer-sonnet through fix round 3, escalated to implementer-opus from the fix round interim board 15 records; the operator's refusal gate, its removal on Option D, the spawn-path fix and the close pass by the main session
Metrics: review rounds 12, counted as 5 before the operator's Option D answer, 5 on the ladder's opening stage after it, and 2 on its second stage; closed major-closed; provenance partly recorded: at least 5 fix-introduced, at least 7 spec-traceable, 1 new-requirement taken on the security carve-out, and the rest security findings whose provenance the interim boards did not record per finding; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT 0; escalations 1 (sonnet to opus); consults 2
Decisions / Surprises: `bin/Register-PersonaTasks.ps1` builds one definition per enabled roster entry with the action, AtStartup trigger, S4U principal and the seven settings the section names, prints them under -WhatIf, refuses unelevated before any scheduler call, updates an existing task in place, disables a disabled entry's task, reports an unlisted AgentPersona task and removes it only under -Prune, and now refuses the run when the read of existing tasks fails rather than reading a failure as an empty list. The only error read as "no tasks" is the module's no-match error, whose id starts CmdletizationQuery_NotFound, measured on this box by the implementer and again by the reviewer. The section's largest cost was the suite rather than the script. The class "a suite path can reach a real scheduler write" recurred across four rounds, and it closed only when the source of truth moved: the dangerous cmdlet set is read from the ScheduledTasks module's own export list, 19 names classified by verb, every spawn goes through one helper that writes the stubs and checks inside the engine that they took, and a text refusal covers seven named routes that bypass the exports, each with a control generated at run time. The suite header states that the text refusal covers named routes only and gives examples of routes it cannot see, among them the PSScheduledJob cmdlets and remoting sessions. The permission check the section built across its first five rounds was removed whole on the operator's Option D decision of 2026-09-15, recorded in interim boards 11 and 12. The idempotence check against a scratch task is cannot measure: this session is unelevated, so Operator Verification item 1's two elevated runs stand in for it.
Assumptions: the task name character class is `[A-Za-z0-9_-]+` and a path carrying a double quote is refused at the action-string boundary (assumed 2026-09-15, route b, section 3); the battery settings print through the CIM object's inverted property names (assumed 2026-09-15, route b, section 3); the operator's answer after interim board 23 restarts this section's count on the ladder's second stage (assumed 2026-09-16, route b, section 3, recorded on interim board 24).
Review Findings: `review: code pair and security at fable, Workflow` for round 1 before Option D; `review: one adversarial lens at opus effort high, Workflow` for every later round, except the backstop round interim board 20 records, which ran three lenses at opus. Criticals: one, the launcher writable by Authenticated Users, fixed and later retired with the permission check. Majors across the section: every one was confirmed by the main session against the code rather than taken from a lens; the security findings on the trust predicate retired with Option D, and the recurring real-scheduler-write class closed on the module export list as above. Backstop: fired twice, at interim boards 10 and 20, each declared with its phase analysis, and each answered by the operator. Stage 2 round 1 returned two Majors, duplicate roster refusal legs whose tests matched a shared message tail and a header overclaiming the guard's reach, both fixed in the round the main session verified at 110 passed. Stage 2 round 2, the closing round, returned APPROVED_WITH_CONCERNS with one Major, a claim finding on the header's list of routes the guard cannot see, held to the behavior bar as security weight and fixed as a comment edit, which owes no round. Minors: 3 fixed in the close pass (the double-quote path test deleted, a stale console-wrap comment deleted, the route-control comment narrowed to the alternatives a form exercises), 1 upgraded on a stated consequence and fixed (the silenced scheduler read), 4 left with the reason (the elevation check sitting after the roster read, which meets the acceptance as written; duplicate definition tests, whose deletion costs churn for no finding; English binding-error text and exact temp paths in assertions, both inside the plan's this-machine assumption). List at `.kit/scratch/process-keeper/minors-section-3-stage2.md`, with the earlier stages' lists beside it. The close-pass delta is comments only and was read by its author, not by a review round.
Stamps: adjudicated 11 (7 project, 4 operator), stamped 1 operator (a-killed-process-tree-does-not-name-its-killer, which shaped reading the census as survival rather than as attribution); the rest were read for other work in the window and did not shape this section.
Gate: targeted lane, `node .kit/keeper-register-test.mjs`, 110 passed, 0 failed, 0 skipped, exit 0, read from the run itself, measured by the main session on 2026-09-16 on this checkout at 0f027ff plus the close pass's comment edits and Section 4's unstaged work, box claimed, no foreign runner. Baseline on this lane: 101/0/0 exit 0 at cbfbcaf; the delta is nine cases the stage 2 fix round added, no failure either side. A scheduler read after the run returns zero AgentPersona tasks. Contention lane: not run; the section's delta writes no machine-shared state, and no suite path reaches the scheduler past the stubs.
Next: 4. Supervisor self-heal fixes the keeper depends on
Commit Model: Branch-and-PR
Delta: measured by the main session on 2026-09-16 on this checkout, worktree 0f027ff plus unstaged work, no machine or contention of its own since the verb reads the tree.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 26 - 2026-09-16
Section 3 is closed. Its Chapter is above, committed with the closing comment edits as 78f20b6 and pushed. Its fix round before that landed as 0f027ff.

Section 4 is committed at first green as 5b75823 and pushed, with all eight of its files. The two natural-exit rate-limit cadence cases and the unit case interim board 25 named are green on the main session's own run.

A defect in the last red fix was found by the main session's close-gate run and fixed before the commit. The supervisor model suite's launch control exited 5 where it expects 3, in 3 of 13 runs on this box. The supervisor log from a preserved failing run shows a child that exited at once with a closure naming the launch pid and a short-lived process under it, both gone by the Windows pid lookup, read as a tree that could not be read. The rule looked only at the launch pid. It now reads an unresolved closure as a failed read only where some process in it still runs. A Section 4 implementer made the change with a red-first unit case and a mutation that turned three cases red. After it the model suite passed 8 of 8.

One natural-exit run of the fixed tree had a single failure, case (y), by timeout (rc 124) rather than by a wrong exit code. It ran in the same minute both the coordinator and aios supervisors relaunched their children. The re-run passed 164 OK, 0 FAIL, exit 0. It is recorded as not reproduced rather than as a flake, and the round now reviewing the delta can weigh it.

The process census across that run showed the coordinator's and aios's agent processes gone. The main session traced it before anything else. Both supervisors are alive, and both logs read `RESTART_PASSIVE: account_changed` at 13:18Z followed by a relaunch. That is Section 4's account-identity check firing live when the account autoswitch moved accounts, not the suites. No persona supervisor was killed in any run this stretch.

Live dispatches: Section 4 review round 5 through Workflow, three lenses at opus effort max over base 78f20b6. The adversarial and security lenses carry the plan and the trace target; the blind lens carries the commit range alone. The Fable tier is still ruled out on this account, per interim boards 18 and 22, so this is the compensation route. Round bracket `.kit/scratch/process-keeper/s4/round5-pre.txt` names only the two files that are not this plan's.

Section 4's ladder: this is its fifth review round. Per the assumption on interim board 24, the operator's answer settled its two open items, so if this round's adjudication leaves the terminal condition unmet, that adjudication meets the backstop and declares.

Gate baselines, measured by the main session on 2026-09-16 on this checkout, box claimed and released: registration suite 110/0/0 exit 0 at 78f20b6; natural-exit 164 OK 0 FAIL exit 0, supervisor-unit 17 passed exit 0, tree-walk PASS exit 0, supervisor-model 8 of 8 exit 0, settings-plugin-key, channel-reply-instruction and persona-live-refuse exit 0, live stop-tree 22 checks 0 failed exit 0, all at 78f20b6 plus the delta 5b75823 commits.

Rulings adopted: none this boundary.

Next action: adjudicate Section 4's round 5 and capture its delta at return, fix or declare per the backstop, write Chapter 4 routing the two README sentences from interim board 22 to Section 5, then Section 5.

### Interim board 27 - 2026-09-16
Section 4's fifth review round returned, and its adjudication leaves the terminal condition unmet, so the section meets the operator backstop. The adversarial and blind lenses returned CHANGES_REQUIRED and the security lens returned CONCERNS, all at opus effort max through Workflow on the compensation route. The round bracket is clean and the reviewed delta is captured at `.kit/scratch/process-keeper/s4/fix-round-5.diff`. The adjudication, with every finding's provenance, is at `.kit/scratch/process-keeper/s4/round5-adjudication.md`.

The Critical is confirmed live, and this session is the evidence. A stop never reaches `claude.exe`. `stop_child` walks only from the coproc wrapper's Windows pid, and `claude.exe` sits under `env.exe`, whose parent is a dead fork intermediate. The dev supervisor's log reads `RESTART: hung`, TERM to pid 582 and `EXIT child-1 code=143` at 11:57Z, then a gate timeout at 11:59Z. The child it named is this main session, still writing its stream at 14:12Z. The gap predates the section's diff and sits in the words of fix 1, "snapshot its tree on every stop". It and one security-weight Major, a walk root not tied to the process table its age guard judged, take the fix-before-declaration route, and an implementer is out on both, barred from the kill-performing suites.

Nine Majors are held after merging duplicates across lenses. One is from the spec's first cut: one account change relaunched each persona twice, the identity flipping to cdae0019 and back within 91 seconds, confirmed in the aios log at 13:16:55Z and 13:18:26Z. One is pre-existing in a suite: a bare-pid cleanup kill at `.kit/live-stopprocesstree-test.sh:500`. Seven were introduced by fix rounds on the tree-record apparatus: the walk root tie, the shutdown exit-code split (exit 5 on the decide path, present at base, against exit 0 on the natural-exit path, added in the board 22 round), the wall-clock staleness bound, a record never re-walked for native children, case (u) not reaching its injected failure, four untested refusal legs, and assertions pinned on log prose.

Pre-BLOCKED acts: the expert ask went to the agent_persona expert seat, and a consult is out at opus effort max on whether the tree apparatus and the account check should continue or narrow. The design stop of interim board 23 ruled on the three-way verdict, another mechanism, so it does not stand in for this consult. The journal carries `kit.review.cap fail` for this section at 5 rounds.

Owed and unrun: the review round over the Critical fix's delta, taken on the re-arm before anything else.

Section 4's ladder: the opening bound, with the round count at five. Section 3 is closed.

Gate baselines unchanged from interim board 26.

Next action: verify the Critical fix on the main session's own runs with a census, record its delta, receive the consult ruling, then declare the backstop with its phase analysis.

### Interim board 28 - 2026-09-16
This plan changed hands. The session the leash was bound to died at 11:18 local, mid-flight, and this session took it over on the operator's instruction relayed over the Discord channel. The takeover ran through the kit-goal CLI under --self-armed rather than by editing the goal state, so the record says plainly that this run made the arming invocation. The authority is the plan's own committed Dispatch Authorization, which names any session holding the plan and the dev persona as its executor, and this session holds that persona. The queue now carries this plan and the steward-architect plan behind it, on the operator's naming of both.

The dead run's evidence survived it and is the reason no work was repeated. Its round 5 adjudication, its captured delta and its pre-BLOCKED consult all sit under .kit/scratch/process-keeper/s4/. The consult had returned before the session died and nobody had read it. Its ruling is Option C: land the Critical and the two security-weight Majors now, then declare the backstop recommending a continue, with a per-finding disposition for each of the nine held Majors rather than one ruling over them as a class.

The Critical fix the dead run left is not green, and this is the finding of the boundary. Its delta is uncommitted in the working tree and adds build_stop_snapshot, which unions the wrapper's walk, a walk from every Windows pid in the MSYS closure and the tree record, and calls refresh_child_tree on entry to stop_child. Run against the natural-exit suite it returns 171 OK, 10 FAIL, exit 1, against the baseline of 164 OK, 0 FAIL, exit 0 that interim board 26 recorded. The failures concentrate in two places: three shutdown cases return exit 5 where they must return 0, and the whole of case (h), which is the case that proves a real surviving process is named and killed. The log is kept at .kit/scratch/process-keeper/s4/c1-regression.log.

That reading was nearly lost to the harness. The suite ran as a background task and its completion notification reported exit code 0, which is the wrapper's exit and not the run's. Only the marker the run wrote for itself carried exit 1. A notification read as a result would have recorded a ten-failure regression as a clean pass.

The machine's heavy-process claim was still held by the dead session, written at 15:11Z with an expected 1500 seconds, and was over an hour past that when this session read it. Its holder had no heartbeat and no claim entry in the commons store while six other sessions heartbeat within seconds. This session replaced it with its own, kept the stale one at .kit/rescue-a581ce96/heavy-process.stale.md, and released its own before dispatching. A claim whose only permitted releaser is the session that died is a box claimed forever, and that is worth a kit note rather than a local workaround.

Every uncommitted file the dead session left was copied to .kit/rescue-a581ce96/ before anything ran, with a full patch beside it, and each copy was verified against the tree by diff rather than by inspection.

One file left that set. docs/README.md carried an uncommitted line announcing the unversioned-manifest plan as Ready and queued. That plan merged and was archived on main in pull requests 33 through 35, so the line states something untrue and describes a plan this branch does not own. It is outside Section 4's Files in scope, so it was reverted to its committed state rather than folded, and the dispatched implementer is told not to touch that file.

Live dispatches: implementer-opus on the ten-case regression, over bin/supervise.sh and the supervisor suites, told to keep the Critical fix rather than revert to walking the wrapper pid alone, and given the regression hypothesis explicitly as unconfirmed. Its first-turn reading was taken and resolved healthy at 18 assistant turns with no synthetic line.

Gate baselines, measured by this session on 2026-09-16 on this checkout at 5feaa3c plus the uncommitted Section 4 delta, box claimed and released, no foreign runner named: supervisor-unit 17 passed 0 failed exit 0; supervisor-tree-walk PASS exit 0; supervisor-natural-exit 171 OK 10 FAIL exit 1 against interim board 26's 164 OK 0 FAIL exit 0 at 78f20b6 plus 5b75823. The kill-performing runs took a process census before them; the after census is owed with the implementer's return.

Rulings adopted since the last boundary: none of this session's own. The dead run's consult ruling is read and held for the declaration rather than adopted piecemeal.

Section 4's ladder: unchanged at the opening bound with the round count at five. The backstop declaration is owed and is held only by the carve-out fix, which must land before it.

Owed and unrun, carried forward from interim board 27: the review round over the Critical fix's delta.

Next action: adjudicate the implementer's return, re-run the natural-exit suite with a census on this session's own surface, fold the live-cleanup security Major into the same delta as the consult directs, then declare the backstop with its phase analysis.

### Interim board 29 - 2026-09-16
This plan changed hands again. The session interim board 28 records died at 13:09 local, its last transcript entry a system line rather than a finished turn, and this session took it over on the operator's instruction relayed over the Discord channel. The operator typed the arming invocation into that channel, so this session ran the kit-goal CLI for it under `--self-armed`, which is the flag the skill gives a relayed instruction. Both plans are in the queue, this one first. The CLI recorded that the steward-architect plan carries no `## Dispatch Authorization` section; it is armed on the operator's own word and that is its whole authority.

The machine's heavy-process claim was still held by the dead session, written at 17:09Z with an expected 5400 seconds and over an hour past that when this session read it. This session did not take it. The role skill states that an aged claim is proceeded past unclaimed and that naming the contention never includes writing the claim, so the stale claim stands and every suite reading below was taken unclaimed under a named contention. Interim board 28 records the previous session replacing a stale claim instead, which that rule does not permit. A process poll before the runs found no foreign test runner in any engine, and the resident load is the live fleet's own claude processes.

The dead run's implementer finished after its parent died. Its artifacts are at `.kit/scratch/process-keeper/s4/fix-natexit.log` and `.exit`, written at 14:07 local, reporting 185 OK, 0 FAIL, exit 0. The carve-out delta is in the working tree and unchanged since: `build_stop_snapshot` for the Critical, the MSYS-pid re-check and ticks discard for M-root, and `kill_process_snapshot` in place of the bare force kill for M-live-cleanup.

That reported green was not taken on its word, and re-running it found the contention hazard rather than a regression. This session's first run of the natural-exit suite returned 181 OK, 4 FAIL, exit 1, read from its own marker; the harness's completion notification for that same run reported exit 0, which is the wrapper's exit and not the run's. The second run against a byte-identical tree returned 185 OK, 0 FAIL, exit 0. The failures moved between attempts, so the first run is contention rather than a result.

The shape of that contention is worth more than its verdict, because one of the four failures is a held Major reproducing live. Three failures were rc=124 timeouts. The fourth was case (n) exiting 5 where the case expects 0, which is M-stale: `child_tree_record_state` reads the record as stale on a wall-clock bound of three poll intervals with a thirty second floor (`bin/supervise.sh:1463-1465`), compared against `date +%s`, so a box loaded enough to slow the poll body ages the record past the bound while polls are still confirming it, the sweep returns 1 and the natural-exit path exits 5. M-stale was held at round 5 as a live false exit 5 inferred from reading. It is now confirmed on this box under ordinary fleet load, which is evidence the backstop declaration should carry. The consult's disposition for it, counting staleness in polls rather than wall seconds, is unchanged by this.

Live dispatches: Section 4's owed review round, the one interim boards 27 and 28 both carry as owed and unrun, over the carve-out delta alone. It is a re-raised round, round 5 having returned a Critical that survived adjudication, so it runs round 1's roster at round 1's tier: adversarial, blind and security lenses, all at fable through the Agent tool. Fable is available again, which is a fresh measurement rather than a carried claim: all three transcripts were live and past a quarter megabyte within minutes of dispatch, where interim board 22's outage reading was three transcripts at zero assistant turns eleven minutes in. The delta capture is `.kit/scratch/process-keeper/s4/fix-round-6.diff`, taken against 5b75823, which is the state round 5 reviewed. The pre-round tree capture is `round6-pre.txt`.

Gate baseline, measured by this session on 2026-09-16 on this checkout at 58b3b98 plus the uncommitted Section 4 delta, box not claimed and the contention named, no foreign test runner in the poll: natural-exit 185 OK, 0 FAIL, exit 0, against interim board 26's 164 OK, 0 FAIL, exit 0 at 78f20b6 plus 5b75823. No failure either side; the count grew by the removal-red cases the carve-out added. The unit, tree-walk and live stop-tree lanes are owed and were deliberately not run beside three live agents, the last of them because it kills real processes and could take an agent's own children as collateral.

Rulings adopted since the last boundary: none of this session's own. The dead run's consult ruling stands as interim board 28 records it, re-read from `backstop-consult.json` this session rather than carried from that summary.

Section 4's ladder: at the opening bound with the round count at five, the backstop fired at round 5's adjudication and its declaration owed. This owed round is the carve-out's, taken before the declaration as the rule directs. Its own adjudication meets the bound with the count at six.

Next action: adjudicate the round, fix any Critical or security-weight Major it returns, run the owed lanes with a process census once no agent is live, then declare the backstop with its phase analysis and its per-finding dispositions.

### Interim board 30 - 2026-09-16
Round 6 returned and is adjudicated at `.kit/scratch/process-keeper/s4/round6-adjudication.md`. Verdicts: adversarial CHANGES_REQUIRED, blind CHANGES_REQUIRED, security CONCERNS. The bracket held: `round6-pre.txt` and `round6-post.txt` are identical and the diff exited 0, so no tree change happened during the round. Four distinct Majors after merging the lenses, no Critical.

Two of the four are security findings of Major weight on the process-execution surface, so both took the carve-out route and were fixed before this declaration rather than held. Both were confirmed in code by this session before any fix was written, not taken on a lens's word.

The first is C1's own failure shape reopened by C1's own fix. `build_stop_snapshot` continued on walk rc 3 and rc 4 alike and still returned a verified snapshot, while `walk_msys_process_tree`'s own contract says rc 4 means the MSYS process is still running under a different Windows pid and so is part of the tree that this walk says nothing about. `refresh_child_tree` already encoded that difference and kept the record behind on rc 4. So a live closure member sat at an unwalked Windows pid, in neither the record nor the union, and the stop reported clean. The natural-exit `moved` case asserted RC=0 for exactly that shape, so the suite pinned the defect.

The second is the walked root being dropped when the process table has no row for it. That skip is right for a descendant and fail-open for the root, because `walk_msys_process_tree` independently proves the root alive and still resolving to the same Windows pid on both sides of the enumeration, and a process alive across its own enumeration must have had a row. An absent row there is a partial enumeration, not an empty tree. The tree-walk suite pinned that fail-open as intended too.

Round 7 fixed both, dispatched to implementer-opus at the section's own tier. rc 3 alone now continues and rc 4 fails the build. A new `ROOTUNJUDGED` marker joins the three the walk already had, checked after `CIMFAIL`, `WALKUNVERIFIED` and `TICKSMISMATCH` so a CIM failure still refuses under its own name. Both refusal legs carry removal-red proof under amendment 72: deleting the rc 4 leg flips the `moved` case from RC=1 STOP_PATH=unverified back to RC=0 STOP_PATH=term, and deleting the `ROOTUNJUDGED` leg flips the absent-root case from rc 1 to rc 0. Both probes ran through the real `stop_child` path rather than by calling the guard directly. A new control sits beside the root case where the root is absent from the table and nothing live holds the id, which stays a completed walk over an empty tree, and that control is what shows the leg discriminates rather than refusing every absent root. The delta capture is `fix-round-7.diff` against 5b75823, the same base as `fix-round-6.diff`, so round 7's own lines are the difference between the two.

Round 7's own review round is OWED AND UNRUN. Its delta reaches process execution, which is a fix-delta bar trigger on its own, so the round is owed. The backstop opens no round, so it is named here and is the first thing to run on a continue, before anything else.

Gate, measured by this session on this checkout at f074492 plus the uncommitted Section 4 delta, box not claimed and the contention named: `.kit/supervisor-unit-test.mjs` 17 passed 0 failed exit 0; `.kit/supervisor-tree-walk-test.sh` 16 OK 0 FAIL exit 0, against 14 OK before this round; `.kit/supervisor-natural-exit-test.sh` 186 OK 0 FAIL exit 0, against interim board 29's 185 OK 0 FAIL exit 0; `.kit/supervisor-model-test.sh`, `.kit/settings-plugin-key-test.sh`, `.kit/channel-reply-instruction-test.sh` and `.kit/persona-live-refuse-test.sh` each exit 0. The count grew by the three assertions this round added and nothing was removed. Every exit code was read from the run's own marker rather than from a completion notification. A process census before the gate found no foreign test runner in any engine; the resident load is the live fleet's own processes, 12 bash, 3 claude, 15 node and 5 powershell, and the machine's heavy-process claim was still the dead session's and more than two hours past its expected duration, so it was left standing and every run was taken unclaimed under a named contention.

The seventh acceptance suite, `.kit/live-stopprocesstree-test.sh`, is OWED AND UNRUN. It kills real processes and the operator's fleet is up, so running it beside the fleet risks taking a live persona down. It needs the fleet quiet, which is the operator's own act under Operator Verification. It is also the suite that would settle two of the frozen Minors: whether WMI truncates rather than rounds a creation time across a DST boundary, and whether the new `ROOTUNJUDGED` leg leaves the live `CIMFAIL` case alone.

Rulings adopted since the last boundary: none. No new-requirement Major was returned, so no judge was convened and nothing was held.

The design stop was considered and did not fire, and the reasoning is recorded here so a later session can overturn it rather than re-derive it. Rounds 5 and 6 each carry fix-introduced Majors in the tree-walk and stop-snapshot apparatus, which looks like the trigger's pair. It is not, because the two classes the design stop never counts are a Critical and a security finding of Critical or Major weight, and round 5's M-root together with round 6's two carve-out findings are all security findings of Major weight on the process-execution surface. What is left outside the carve-out is M-stale in round 5 and the retry-staleness Major in round 6. Those are the same unfixed staleness bound seen from two callers rather than a mechanism a fix round is building: no fix round has yet run on that bound, so there is no repair loop for a stop to interrupt, and the two findings sit in different passages, the bound itself and the newly written retry escalation path.

Section 4's ladder: at the opening bound, count now at six, and the declaration owed from round 5's adjudication goes out at this one carrying rounds 1 through 6. A continue buys the section three further rounds, and from the third of those onward each adjudication that still leaves the terminal condition unmet declares again.

Next action: the declaration is the next act, and nothing else in Section 4 proceeds until it is answered. On a continue, the order is round 7's owed review round first, then the two owed Majors this round leaves, then the live stop-tree suite once the fleet is quiet, then the Minor close pass.

### Interim board 31 - 2026-09-16
This plan changed hands a third time. The session interim board 30 records is gone and this session, the dev persona's supervised session, took the plan back. The operator's answer to the backstop declaration arrived as a coordinator decision record carrying the operator's own word and delegated authority: CONTINUE. Both plans were re-armed through the kit-goal CLI under `--self-armed`, this one first, and the leash is bound to this session. The CLI recorded that the steward-architect plan carries no `## Dispatch Authorization` section and armed it on the operator's word alone, which is its whole authority.

The operator's answer settles two things that go in Chapter 4. The kill remains the last rung only. And a follow-on plan, the supervisor as a peer with four-signal liveness, negotiated restarts through inbox records and a child that survives a supervisor restart, will be written by the coordinator seat after this section lands. Section 4 is not widened toward it. Where a held finding's fix would be reshaped by that plan, and M-account is the one, the fix is written inside fix 3's own words now and the follow-on is noted here.

**Section 4's ladder restarts on the operator's answer, at the second stage.** A continue past the opening bound buys three further rounds, and each adjudication from the third of those onward that still leaves the terminal condition unmet declares again. This round is the first of the three, so the count on this stage stands at one.

Round 7's owed review round ran and is adjudicated at `.kit/scratch/process-keeper/s4/round8-adjudication.md`. It ran round 1's roster at round 1's tier on the steer's own direction, which is a stricter read than the later-round row would give: adversarial, blind and security lenses, all at fable through the Agent tool. Fable answered on all three, which is a fresh measurement rather than a carried claim; interim boards 18 and 22 record an outage that is over. Verdicts: adversarial APPROVED_WITH_CONCERNS, blind APPROVED_WITH_CONCERNS, security CLEAR. No Critical from any lens. The bracket held, `round8-pre.txt` and `round8-post.txt` identical with diff exit 0.

The round was scoped to round 7's own lines, which took a piece of construction worth recording. One commit, afa87f5, carries rounds 6 and 7 together, so round 7's lines had no commit boundary to review against. A synthetic base commit was built in a scratch worktree from base 5b75823 plus `fix-round-6.diff`, and the delta under review is `git diff 01ae93de afa87f5` over the three code files, 173 lines. Before the round went out, the committed range 5b75823..afa87f5 was confirmed byte-identical to `fix-round-7.diff` with `cmp`, so the capture and the commit are the same delta.

**The steer's stop condition was tested and does not fire.** The steer said that if this round returned the same fail-open shape again, an unjudged process read as absent in the round 7 code, the section stops and the restructure option goes to the operator rather than a fourth patch. One finding is the candidate and it is a different class. The adversarial lens's Major at `bin/supervise.sh:898-900` needs `Get-Process -Id` to return null for a process that is running, and the lens could name no mechanism by which it would, which is why it rated its own confidence low. The code does not leave the root unjudged: it judges it with a second instrument and refuses on `ROOTUNJUDGED` when that instrument says the process lives. The silent path needs three instrument failures at once, since a child naming a rootless parent already raises `WALKUNVERIFIED` through `$parentCreated` being null, confirmed at `Select-ProcessTree` lines 748 to 754. The security lens read the same lines with the fail-open question as its explicit mandate and returned CLEAR. The reasoning is written out in the adjudication file so a later session can overturn it rather than re-derive it.

No design stop, and the reading is recorded because the shape looks like one again. Round 6's two carve-out findings were security findings of Major weight, which the trigger never counts, and round 6's one counting fix-introduced Major was the retry-staleness bound, a different passage from the walk's root branch this round's Major sits in. The trigger needs both in one mechanism.

The one owed Major is taken rather than justified-not-fixed, and the ground is that its fix closes a class instead of an instance. `walk_msys_process_tree` already proves independently that the MSYS pid is alive and still resolves to the walked Windows pid on both sides of the enumeration. A walk that returns rc 0 having named nothing at all, not even its own root, while that check says the root lives, is a contradiction the code currently accepts. Reading it as unverified removes the dependence on `Get-Process` being truthful and adds no mechanism.

One security finding of Minor weight is folded into the same fix round. Phase 3 of `stop_child` runs `taskkill //F //PID` at `bin/supervise.sh:1982` on a Windows pid resolved at stop entry, after two grace windows, with no re-resolve and no ticks match. It is pre-existing and outside the round 7 delta, and it is the same MSYS-moved shape round 7 now refuses for walks, so the fix re-resolves immediately before the kill and falls to the MSYS kill where the pid no longer matches.

Six Minors go to the close pass and one routes out. The routed one is that the only pin on the new `ROOTUNJUDGED` refusal lives in `.kit/supervisor-tree-walk-test.sh`, which the README's supervisor suite list does not name, so the acceptance clause gating every offline suite the README lists never runs it. Section 5 owns that list and already carries the job of adding this plan's new suites to it, so it goes there. The rest are at the adjudication file, among them one the two lenses split on, where the blind lens is right: a comment says `refresh_child_tree` gives the same answer to the two rc codes, where the answers differ and only the reading matches.

**Foreign uncommitted work sits in this section's own file, and it will block the commit.** The working tree carries steward-and-architect work that is not this plan's: two hunks in `bin/supervise.sh` rewriting the coordinator role instruction, a README rewrite of the coordinator launch example into a steward launch, about forty added lines in `.kit/channel-reply-instruction-test.sh`, and a Status flip in that plan's own doc. Git cannot split `bin/supervise.sh`, so while those hunks stand there is no commit form for it: a commit with no path list sweeps them under a process-keeper message, and one naming the file takes the working-tree content, which carries them too. Nothing was touched, and this round read the committed range rather than the working tree, so the round is unaffected. It went to the coordinator seat as a finding to route, with the two obvious routes named. Section 4 has a fix round, the owed lanes and a close pass ahead of its commit, so there is room.

Gate: no suite ran this boundary. The lanes stand where interim board 30 measured them, and the live stop-tree suite remains OWED AND UNRUN until the operator gives a quiet-fleet window, which is asked for when the work reaches it.

Rulings adopted: none. No new-requirement Major was returned, so no judge was convened and nothing is held.

Next action: the fix round, in the order the operator's answer set. M-account first, with the launch identity read just before the coproc launch and `restart_passive` only where the changed identity holds on every poll for 180 seconds, a revert inside the window cancelling, relaunches still counting against the restart budget, and the red cases per the consult at `.kit/scratch/process-keeper/s4/backstop-consult.json`. Then M-stale and R6-M-retry together, with staleness counted in polls as three consecutive failed confirms rather than in wall seconds. Then this round's A-M1 and S-M1, M-legs' remaining two legs, M-case-u, the M-rewalk narrowing with its backlog entry, and M-shutdown-code justified-not-fixed with its backlog entry. M-prose goes to the close pass and R6-M-suite routes to Section 5 as adjudicated. Then the owed lanes, then the live stop-tree suite once the fleet is quiet, then the close pass, Chapter 4 and Section 5.

### Interim board 32 - 2026-09-17
This plan changed hands a fourth time, and this time the cause is known rather than guessed. The session interim board 31 records, the dev persona's supervised session, was killed by its own supervisor at 06:00:32Z. It was not hung. `run/supervisor.log` records `RESTART: hung: heartbeat lastSeen 1789624726303 older than 90000ms` and then `EXIT child-6 code=143 (term)`, and that session's transcript ends mid-turn on a `tool_use` with no `tool_result`, which is what a kill looks like from the transcript side. This session is the child-7 the same supervisor launched at 06:03:22Z. Both plans were re-armed through the kit-goal CLI under `--self-armed`, this one first, exit 0, bound to this session. The CLI recorded again that the steward-architect plan carries no `## Dispatch Authorization`.

**The hang check is killing healthy sessions, and the mechanism is confirmed rather than inferred.** `bin/supervise.sh` reads the heartbeat at `"$WORKDIR/.agentic-heartbeat.json"`, `WORKDIR` being the absolute launch argument fixed at line 104. The plugin that stamps that file writes it relative to the harness's current working directory, which a `cd` inside a Bash tool call moves and which the harness announces in an environment-update notice. So a session working out of a subdirectory keeps stamping into a file nothing watches while the watched file goes still, and ninety seconds later the supervisor kills it. This session reproduced it: its own cwd entered `.kit/scratch/process-keeper/s4/gate8/`, a heartbeat carrying this session's id appeared there, and on return to the root the root file resumed advancing while the gate8 one froze.

All three `RESTART: hung` kills in `run/supervisor.log` carry the fingerprint, each killed session having left a stray heartbeat stamped later than the value it was killed on: `2a0f4775` at 20:49:45Z on 2026-09-14 against a stray 1470 seconds newer, `a581ce96` at 11:55:57Z on 2026-09-16 against one 120 seconds newer, and `1866157a` against one 120 seconds newer. Fifteen stray heartbeat files sit across `.kit/`, so the exposure is routine rather than exceptional. The gate8 file no longer holds the dead session's copy, this session's own cwd having overwritten it; the reading above was taken before that.

Section 4 is not widened toward this. Interim board 31 records the operator's decision that the four-signal liveness work belongs to the coordinator seat's follow-on plan. The finding went to the coordinator for that plan, and one narrow question went to the operator: whether a single corroboration check, reading an instrument the child does not write, goes into Section 4 now or waits. The recommendation given was to take it now and narrowly. Nothing waits on the answer; this board does not hold for it. The durable record is the project memory `heartbeat-writer-follows-cwd-supervisor-reads-fixed-path`, anchored to `bin/supervise.sh` and `bin/agentic-common.sh`.

**Tree attribution, confirmed hunk by hunk rather than taken from the handover.** The working tree carries seven modified paths in two hands. The steward-architect work is `README.md`, `.kit/channel-reply-instruction-test.sh`, that plan's own doc, and inside `bin/supervise.sh` exactly one hunk, the single-line `COORDINATOR_ROLE_INSTRUCTION` rewrite at the pre-image's line 2508, which adds the fleet-keeping duty, the Coordinator seat and the architect routing. A count of the diff's `ROLE_INSTRUCTION` assignment lines returns two, the minus and the plus of that one line, which is what establishes there is no second foreign hunk in the file. Every other hunk in `bin/supervise.sh` is the previous session's Section 4 fix-round work, along with `.kit/supervisor-natural-exit-test.sh`, `.kit/supervisor-tree-walk-test.sh` and `docs/backlog.md`. Nothing foreign has been touched and nothing foreign enters any staged list. Whether those hunks are rescued to a branch is with the operator.

Gate: the previous session's gate8 run is read from its own exit files rather than from any count. `.kit/supervisor-unit-test.mjs`, `.kit/supervisor-tree-walk-test.sh`, `.kit/supervisor-model-test.sh`, `.kit/settings-plugin-key-test.sh` and `.kit/persona-live-refuse-test.sh` each wrote exit 0. `.kit/supervisor-natural-exit-test.sh` wrote no exit file at all and its log stops at 122 OK, 0 FAIL against the 186 OK of interim board 30, so that leg died with its session and is unrun rather than failed. The whole gate is therefore owed again on the current tree. `.kit/live-stopprocesstree-test.sh` remains OWED AND UNRUN pending a quiet-fleet window, which is now asked of the operator. The machine's heavy-process claim is still a dead session's, from 2026-09-16T17:09:10Z with an expected 5400 seconds, so it is left standing and any run is taken unclaimed under a named contention.

Live dispatches: none. Rulings adopted since the last boundary: none.

**Section 4's ladder is unchanged at the second stage, count at one.** No review round was adjudicated at this boundary, and a round nobody ran is a round no ladder counts.

Next action: verify the previous session's uncommitted fix-round hunks against the consult dispositions at `.kit/scratch/process-keeper/s4/backstop-consult.json` and the fix-round order interim board 31 set, treating them as an unreviewed attempt. Then finish the fix round's remaining items, which that board lists as A-M1, S-M1, M-case-u, the M-rewalk narrowing and M-shutdown-code. Then the owed review round over the fix delta, then the owed lanes, then the live stop-tree suite once the fleet is quiet, then the Minor close pass, Chapter 4 and Section 5.
