# Steward and architect: split the coordinator persona into a Sonnet fleet keeper and a Fable designer on demand

Status: In Progress
Commit Model: Branch-and-PR
Disjoint: no
Created: 2026-09-15

## Goal

When this is done, the always-on coordinator persona is replaced by two personas. The steward runs Sonnet on a five-minute tick and keeps the fleet running: it routes commons-inbox traffic between workers and the operator as the coordinator does today, watches each persona's keeper state and heartbeat and reports one that is held, backing off or stale, holds the kit's Coordinator seat and runs that seat's reconciliation pass, and wakes the architect when a record needs design judgment. The architect runs Fable with no standing goal and no home repository, and is woken by the steward or the operator for design work: assessments, specs written into the target repository, consults, plan reviews, and the finishing judgment on a high-stakes effort. Workers are unchanged. It matters because today one Fable persona ticks every minute doing mostly clerical work at the top tier's cost, while the design work that tier exists for arrives a few times a day.

## Approach

**What the coordinator persona costs today, measured.** The coordinator child's transcript at `D:/personas/coordinator/run/child-1/stdout.jsonl` showed, when read on 2026-09-15, a run cost of 8.75 dollars across 150 assistant turns, 52 shell calls, 2 subagent dispatches, 10 replies to the operator, 1 inbound priming prompt and 0 steers sent to a worker. The work was status reading and reporting. That is the clerical load the steward takes at Sonnet.

**Steward.** The coordinator persona's launch shape, kept whole, with four changes. The persona is named `steward` and the plugin's `coordinatorPersona` setting names it, so the plugin's reach rule (the coordinator persona's holder may address any persona, and workers may address the coordinator persona) applies to the steward with no plugin change. The model is Sonnet. The controller tick is 300000 milliseconds; a delivered record still lands on the next quiet tick, and a worker's urgent record breaks into a running turn, so a slower tick costs response time only for a steward that is idle, and five minutes is the bound the fleet-health duty needs anyway. The standing instruction the supervisor writes for the coordinator persona (`COORDINATOR_ROLE_INSTRUCTION` in `bin/supervise.sh`) gains three duties beside the ones it carries today:

1. Fleet health. The plugin watches fleet health itself and submits a prompt labelled `[FLEET]` carrying only the personas whose health class changed; the steward reports each of them to the operator on its own thread. It polls nothing, because a duty written as a per-tick habit has no trigger: the controller tick starts no turn on a persona holding no active goal leaf and an empty inbox, which is exactly the quiet fleet on which a crashed persona most needs reporting. The classes are held, backing off, stale, no live claim while the roster enables it, and healthy. The fleet status tool Section 3 adds stays available as an on-demand read, and Section 6 builds the watcher.
2. The kit's Coordinator seat. At priming, take the seat with the kit's role skill, which writes the registry entry and reads the board. The reconciliation pass the kit's coordinator skill states, including the registry prune of exited entries and the claim probe, runs when a prompt labelled `[RECONCILE]` arrives and at no other time; that prompt fires on the owning skill's own four-hour cadence, because the probe's window is one full cadence and the prune's staleness test is twice it, so a five-minute run could fire nothing a four-hourly run misses. At the end of each turn whose state is on disk, run the kit's compaction checkpoint CLI's boundary verb as the coordinator instruction already says, so the next automatic compaction lands at a declared point.
3. Waking the architect. A record that turns on a design decision (an operator design question, a worker escalation the worker's plan does not cover, a request for a spec, a plan review or a consult) goes to the architect through `agentic_say` with the persona argument set to `architect`, carrying the ask and the repository it concerns, and the steward tells the operator it did so. A delivered record starts a turn on the architect's next tick, which is how the wake works, and no new mechanism is added. The architect answers the steward the way a worker does, with a record addressed to the steward's persona, and the steward relays the answer to the worker that escalated as a coordinator record, so the architect never addresses a worker directly.

**Architect.** A new persona `architect`, model Fable, effort high through the roster's `effort` field, which the process keeper plan maps to the supervisor's `EFFORT` variable, on a 60000 millisecond tick, which is cheap while idle because a tick with no goal and an empty inbox does nothing. Its home directory is `D:/personas/architect`, not a repository: it is the roaming seat the operator asked for. The supervisor takes any directory as a workdir, the persona store lives there, and the commons store every persona claims into is machine-global under the installed plugin, so the architect holds a commons entry and receives records like any other persona. When an ask names a repository, it cuts a worktree of that repository under its own directory, does the work there on a branch, commits and pushes, and reports the branch and the filename to the steward and the operator. A new standing instruction, `ARCHITECT_ROLE_INSTRUCTION`, built when the launch's persona matches a new `ARCHITECT_PERSONA` setting the way the coordinator instruction is built today, states its charter: design work only, no standing goal, specs go into the target repository's `docs/plans` on a branch, it never executes a plan, and a plan it writes is handed to a worker through the steward. One limitation stands until the kit-resolution plan lands: the kit resolves a session's instructions, memory and leash from the session's launch directory, so the architect's kit memory is its own directory's rather than the target repository's, and it reads a repository's memory index explicitly when it needs it.

**Workers.** Unchanged in this plan. They keep their launch shapes and their roster entries; the only thing that moves is the name their steer instruction targets, which is the `coordinatorPersona` value, so each worker picks up the steward's name at its next launch.

**What retires.** The `coordinator` persona: its roster entry is replaced by `steward`, its launcher becomes the steward's, its thread stays as history, and its store entries in the shared `D:/agent_persona` store are released by its own shutdown. The backlog entry "A pending record to an unheld coordinator is lost when its writer restarts before the coordinator's first tick" is re-read here against a steward that runs under the process keeper: Section 5 retires it with receipts if the keeper closes the window, and otherwise re-dates it with what remains.

**Why not one persona with a model switch.** A persona is one session with one model for its life; the supervisor sets the model at launch. Two jobs with different tiers and different cadences are two personas, and the commons inbox already carries traffic between named personas, so the split costs one instruction, one roster entry and one tool.

**Sweep.** The surfaces that speak the coordinator persona's contract, read directly in the design session: `bin/supervise.sh` (the worker steer instruction and the coordinator role instruction around lines 1540 to 1600, the `COORDINATOR_PERSONA` export at 359 to 370); `hooks/index.ts` (`coordinatorPersona` resolution at 820 to 823, the reach checks at 4420 and 4462, the delivery ground at 1586 and 1687); `hooks/operator.ts` (`mayReachPersonaIn` and `deliveryGroundIn`, the reach rule itself); `.kit/settings-plugin-key-test.sh` (the `coordinatorPersona` round trip); `README.md` (the coordinator launch paragraph at 48, Delivery and Trust boundary at 434 to 452, the coordinator standing instructions the archived coordinator plan's Section 8 states); `.kit/channel-reply-instruction-test.sh`; `docs/archive/agent_persona_coordinator_v2.md` (the persona this plan splits); the launchers under `D:/personas`; the kit's role and coordinator skills, whose seat this plan folds; and the machine's registry under the kit's coordinator directory, which shows the kit's Coordinator seat taken only by interactive sessions so far and never by the coordinator persona.

**Order.** After the process keeper plan, because the fleet-health duty reads the roster and `keeper.json` that plan creates. Section 1 and Section 2 in either order, then 3, then 4, then 5.

## Standing Brief Amendments

- The steward is woken by the plugin, not by a cadence in its own instruction. A duty that says "on each tick" states a trigger the runtime does not have: the controller tick submits no prompt when the persona holds no active goal leaf and its inbox is empty, so a quiet fleet never wakes the steward at all. Every duty is written as a response to a labelled prompt the plugin submits, or to a delivered record, and never as something the steward does on a schedule of its own.
- A prompt the plugin submits reaches the session only once it is idle, and repeated submits accumulate rather than replacing one another. So any submission the controller makes on a recurring timer is gated on a state comparison, and the state advances at the moment of submission rather than when the model reports, or one slow turn collects a pile of identical prompts. The state the health comparison holds lives in session memory and reaches no file, because a persona the steward watches can write the store and a reading read back from it would let that persona decide the steward's silence about itself. The reconciliation cadence stamp is the one field still read back from the store, since that cadence is four hours and a steward relaunched more often would otherwise restart its wait at every launch.
- The reconciliation pass runs on the kit coordinator skill's own four-hour cadence and never more often. The claim probe's window is one full cadence and the registry prune's staleness test is twice the cadence, so a five-minute run of either can fire nothing a four-hourly run misses, and the skill states that the probe adds no timer to the seat.
- A persona's health classes are held, backing off, stale, no live claim while the roster enables it, and healthy. The fourth is not optional: an enabled persona that never comes up has no commons entry at all, so it reports a null heartbeat age and satisfies none of the other conditions.
- A check whose acceptance is an empty or absent result asserts, first and separately, that its subject was produced at all. Otherwise the step that produces the subject can fail and the check passes on the very failure it exists to exclude, which is indistinguishable from a true clean result. Assert the producing command's exit status and the artifact's existence, then assert the absence. This is stated as a class because the plan has produced it twice on unrelated surfaces: a class sweep whose only failure path ran inside a loop that an empty read skipped, and a round-trip leg that accepted an empty read back out of a file its own emit step might never have written.
- The producers of a guarded channel are a set. A guard placed on a channel is finished only when every producer reaching that channel passes it, and when the party reading the guard's mark has been told what the mark means. Closing the producer that prompted a finding is not closing the channel, and a check written against the prompting producer alone reports clean on every other one.

## Sections of Work

### 1. The steward's launch shape and standing instruction
Model: opus

In `bin/supervise.sh`, extend `COORDINATOR_ROLE_INSTRUCTION` with the three duties in the Approach, in the same register and length discipline as the sentences it already carries, and keep every existing sentence. Change nothing in how the instruction is selected: it is still built when the launch's persona equals the `coordinatorPersona` value, so a launch under the name `steward` with that setting gets it. Update `README.md`'s coordinator launch paragraph to the steward's shape (Sonnet, the five-minute tick, the name), and the archived coordinator plan is left as history.

Acceptance: `.kit/channel-reply-instruction-test.sh` gains cases pinning that the persona named by `coordinatorPersona` receives the three new duty sentences and a named worker does not; the existing cases pass unchanged; the README paragraph states the steward's shape.

Files in scope: `bin/supervise.sh`, `.kit/channel-reply-instruction-test.sh`, `README.md`.
Tests: at minimum, lock each new duty sentence present for the coordinator persona and absent for a worker and for `default`, since a duty sentence leaking into a worker's priming would have workers probing the registry.

### 2. The architect's launch shape and standing instruction
Model: opus

In `bin/supervise.sh`, add an `ARCHITECT_PERSONA` setting beside `coordinatorPersona`, emitted into the settings file the supervisor writes and read back from a provided one by the same two branches, and an `ARCHITECT_ROLE_INSTRUCTION` built when the launch's persona equals it, stating the charter in the Approach. The instruction names the two ways an ask arrives (a `[COORDINATOR ...]` record from the steward, or the operator on the architect's own thread), the worktree rule, the commit-and-report rule, and the never-execute rule. Add the persona to `README.md` beside the steward.

Acceptance: `.kit/channel-reply-instruction-test.sh` pins the architect instruction present for the architect persona and absent for every other; `.kit/settings-plugin-key-test.sh` pins the new setting emitted and read back under both plugin ids as `coordinatorPersona` is; a launch with `ARCHITECT_PERSONA` unset builds no architect instruction for any persona.

Files in scope: `bin/supervise.sh`, `bin/agentic-common.sh`, `.kit/channel-reply-instruction-test.sh`, `.kit/settings-plugin-key-test.sh`, `README.md`, `docs/backlog.md`.
Tests: at minimum, lock the instruction's presence and absence per persona and the setting's round trip through the settings file, both directions, since a mis-set persona name would launch a Fable session with a worker's charter.

### 3. The fleet status tool
Model: opus

Add a `fleet_status` tool to the plugin (`hooks/index.ts`, beside `agentic_inbox`), available under the plugin's reach rule with the coordinator persona as the target: the caller holds the coordinator persona or a live reader claim on it, which is what `mayReachPersonaIn` in `hooks/operator.ts` already states. It reads the roster at the path a new `fleetRoster` setting names, each roster persona's `keeper.json` under that persona's run directory, enabled or not, and each persona's commons entry, and returns one row per roster persona: name, enabled, keeper action and delay, hold reason if any, last supervisor exit, commons claim held or not, heartbeat age, and turn state. `keeper.json` is the process keeper's state file and carries the persona name, launch count, last start and end, last exit code, current delay and the hold reason if any; the commons entry carries the claim's last-seen stamp, from which the heartbeat age is derived, and the turn state. The implementer opens `docs/archive/agent_persona_process-keeper_v1.md` for the roster and state-file layout. A missing roster or a missing `keeper.json` is a row that says so, never an error that hides the other rows.

Acceptance: a unit test drives the tool with fixture files for a healthy persona, a held one, one backing off, one with no `keeper.json`, and one absent from the commons, and checks each row; the tool is denied to a session that does not hold the coordinator persona, with the deny text naming the rule.

Files in scope: `hooks/index.ts`, `hooks/operator.ts` (the reach rule), a new `.kit/fleet-status-unit-test.mjs`, `README.md` (the tool list under the operator channel section).
Tests: at minimum, lock every row shape above and the deny direction: a session owning a named persona of its own holds a WORKER ground on the coordinator persona, which reaches it to send a record and is refused here, so fleet state is read only by that persona's holder or by a live reader claim on it.

### 4. The kit Coordinator seat under the steward, proven live
Model: opus

A live check that launches a real child, so it runs under the README's rule for live suites: only with the operator's yes while the fleet is up. Write `D:/personas/steward/launch.sh` in the shape of the coordinator's launcher with the steward's model, tick and name, launch it by hand, and watch the child take the kit's Coordinator seat. The seat's artifacts live under the kit's coordinator directory in the memory store, `~/.claude/coordinator/<machine>/`: `board.md` is the board and `registry/<session-id>.md` is one entry per registered session, per the kit's role skill. Acceptance, each read from the artifact: a registry entry under the kit's coordinator directory whose `Name:` is the machine's Coordinator seat name and whose `Session:` is the steward child's session; a board line from the steward's first reconciliation pass; and a registry prune of at least one exited entry with its board line, the machine's registry holding dozens of exited entries at the time of writing. Where the role skill cannot be invoked from a `claude -p` child, the Chapter says what failed and the seat stays with interactive sessions, which is a design stop for the operator rather than a fix round.

Files in scope: none in the tree beyond the Chapter; `D:/personas/steward/launch.sh` (machine state, kept by Section 5); reads of the kit's role and coordinator skills and the machine's coordinator directory.

### 5. Cutover, roster, and the retired persona
Model: sonnet

Update `bin/fleet.example.json` and the machine's `D:/personas/fleet.json`: replace the `coordinator` entry with `steward` (model `sonnet`, `controllerTickMs` 300000, `coordinatorPersona` `steward`, `channelName` `steward`) and add `architect` (model `fable`, `effort` `high`, workdir `D:/personas/architect`, `channelName` `architect`). Keep the steward's launcher Section 4 wrote and write `D:/personas/architect/launch.sh` beside it, as the manual fallbacks in the shape of the existing launchers. Re-adjudicate the pending-record backlog entry named in the Approach and either retire it into the quarter's backlog snapshot, `docs/archive/backlog-2026-Q3.md`, with receipts, per the curating-docs prune path, or re-date it. Update `docs/README.md`'s index line for this plan.

Both roster entries also carry `architectPersona` `architect`, and the keeper's roster-to-environment map gains `architectPersona` to `ARCHITECT_PERSONA` beside the four fields it already maps. Section 2 added the setting and the supervisor reads it, and nothing on the keeper's launch path can supply it: `Build-SupervisorInvocation` in `bin/keeper-functions.ps1` maps `model`, `effort`, `controllerTickMs` and `coordinatorPersona` and no others, so without this a keeper-launched steward builds no design-escalation clause and a keeper-launched architect runs with no charter. This is the same shape as the roster gap Section 6 was appended to close, found by review round 2 of Section 2 rather than in design.

Acceptance: `bin/Register-PersonaTasks.ps1 -WhatIf`, the process keeper's registration script, prints a definition for `steward` and `architect` and none for `coordinator`; each launcher matches its roster entry by eye, recorded in the Chapter; a keeper-launched architect's settings file carries `architectPersona`, read from a real launch rather than from the map by eye; the backlog entry is retired or re-dated with the reason.

Files in scope: `bin/fleet.example.json`, `bin/keeper-functions.ps1`, `docs/backlog.md`, `docs/README.md`, `D:/personas/fleet.json` and `D:/personas/architect/launch.sh` (machine state).

### 6. The fleet wake: the roster setting the launcher writes, and the plugin's change-driven prompt
Model: opus

Appended during execution. Sections 1 and 3 both shipped against a wake mechanism that does not exist, which round 1 found from three lenses and a consult ruled on. Two gaps, and they are one piece of work because neither is worth anything alone.

First, nothing writes the `fleetRoster` option. `emit_settings_json` in `bin/agentic-common.sh` writes a fixed option set ending at `coordinatorPersona`, and the process keeper's roster-to-environment map carries no roster path either, so a supervised steward always reads an empty setting and `fleet_status` reports that it has no roster. Add a `FLEET_ROSTER` environment variable to the emitted options and a read-back beside `read_settings_coordinator_persona`, by the same two branches and under both plugin ids.

Second, the controller tick submits no prompt on a quiet fleet, so a duty written "on each tick" never runs. In the tick, after the inbox drain and before the no-active-leaf return, add a block that runs only for the coordinator persona and only with a roster configured: compute the fleet rows with the module-scope helpers Section 3 wrote, reduce each row to one of the five health classes the Standing Brief Amendments name, compare against a persisted health map, and where any persona's class changed, persist the new map and submit a plugin-kind expected turn labelled `[FLEET]` carrying only the changed rows. Add a `reconcileEveryMs` option defaulting to 14400000, and submit a `[RECONCILE]` turn when that long has passed since the last one, coordinator persona only. Persist both state fields at submission rather than at report.

Two hazards round 3 found in Section 3 land here, because this section is the second producer of that tool's text and the first consumer of its `action` field.

The bracket guard is a property of the output channel rather than of the field that first needed it. Section 3 neutralizes square brackets in the persona-writable text, which is contained today by the JSON framing of a tool result. This section splices those rows into a submitted turn, where a delivery label is the trust signal and that framing is gone. So the neutralization happens at this splice over every field the prompt carries, `name`, `holdReasonSource`, `problem` and `problems` among them, rather than by trusting the per-field guard upstream. One exported helper owns the rule, and `bracketSafeProblem` in `hooks/operator.ts` is the rule's other implementation and the natural home.

The health-class reduction reads more than `action`. A row reading `running` says a live session holds the persona and says nothing about the keeper: an unreadable keeper state and a climbed relaunch ladder both reduce to `running`, so a reduction over `action` alone would report both as healthy, against the Goal's "watches each persona's keeper state". The reduction reads `nextDelaySeconds` and `note` beside it.

Acceptance: `.kit/controller-tick-test.mjs` pins that a tick over an unchanged fleet submits nothing, that a persona changing class submits one `[FLEET]` prompt carrying that persona and not the unchanged ones, that a second tick with no further change submits nothing more, and that `[RECONCILE]` fires on the interval and not before; `.kit/settings-plugin-key-test.sh` pins `fleetRoster` emitted from `FLEET_ROSTER` and read back under both plugin ids, as `coordinatorPersona` is; a launch with `FLEET_ROSTER` unset emits no roster and the tick block stays silent.

Files in scope: `hooks/index.ts`, `hooks/operator.ts`, `bin/agentic-common.sh`, `.claude-plugin/plugin.json`, `.kit/controller-tick-test.mjs`, `.kit/settings-plugin-key-test.sh`, `README.md`.
Tests: at minimum, lock the silence on an unchanged fleet and the single submission on a change, since the accumulating-submit behaviour means a missing change gate delivers one prompt per tick as a pile at the next idle moment.

## Out of Scope

- The kit resolving a session's instructions, memory and leash from the repository it works in rather than its launch directory. That is the next plan, for the session that maintains the `claude-kit` repository, and it is what lets the architect hold a repository's Expert seat properly.
- Any change to workers' charters or launch shapes.
- The steward relaunching a stuck persona itself. The process keeper owns relaunch; the steward reports.
- A model switch inside one persona's life.
- Moving the steward or the architect to another machine.

## Assumptions

- assumed 2026-09-15 (source: the operator's words on 2026-09-15, "Coordinator should fold into Steward. And it should probably run Sonnet"): the coordinator persona is renamed to `steward` rather than kept under its old name with new duties, since the name states the job; reversal: keep the name `coordinator` in the roster and the setting, and no worker needs relaunching for the steer target.
- assumed 2026-09-15 (default): the steward's tick is five minutes; reversal: one roster value.
- assumed 2026-09-15 (default): the architect is homed at `D:/personas/architect` with no repository, per the operator's words that the fleet should be a few workers with one or two homed; reversal: a workdir change in the roster.
- assumed 2026-09-15 (source: `README.md` Delivery, read 2026-09-15), and corrected during execution on 2026-09-15 by a consult that read the tick itself: a record delivered to a persona does start a turn on that persona's next quiet tick, and that is the whole wake mechanism for the architect, which is woken by a record. It is not the whole mechanism for the steward, whose fleet-health and seat duties are triggered by nothing and nobody: the tick submits no prompt at all when the persona holds no active goal leaf and its inbox is empty. Section 6 was appended to build the missing trigger as a plugin-side watcher. This correction is approval drift, recorded on interim board 2.
- assumed 2026-09-15 (default): fleet health is a plugin tool rather than prose telling the steward to read files, because a Sonnet session following a file-reading recipe every five minutes is the failure this plan exists to remove; reversal: drop Section 3 and the duty sentence reads the files by hand.
- assumed 2026-09-15 (default): the executing session runs on this machine, where the launchers under `D:/personas` and the kit's coordinator directory exist; reversal: none, since the paths are parameters of the process keeper's scripts.
- assumed 2026-09-15 (default): this plan is parked until the operator has read it, since the four decisions it rests on were given in outline and the section shapes above were not shown to the operator before writing; it carries no Dispatch Authorization section and arms on the operator's word.
- decided 2026-09-19 by the operator on this session's relay thread, answering the ownership fork board 22 sent and board 25 re-sent: the steward persona is homed in its own working directory, a subdirectory of `D:/personas`, which is also where it is configured from. The operator's words: "It should absolutely have its own working directory. It can be another sub folder in D:\personas, but that would be where it can be configured from." Rationale: the persona store is `<workdir>/.agentic-personas.json`, so two personas homed in one directory write one file, and that shared file is what makes the suppression class Section 6's rounds kept finding reachable at all. One home per persona gives each store a single writer. Reversal: a roster edit, at the cost of restoring the two-writer premise every later section rests on.

## Operator Verification

1. After the cutover, post a design ask on the architect's thread naming a repository. A spec lands in that repository's `docs/plans` on a branch within the hour, and the architect reports the branch and the filename on its thread.
2. Ask a worker, on its own thread, to escalate a question its plan does not cover. The steward's thread reports the escalation and that it went to the architect, and the architect's thread shows the record arriving.
3. Shut one worker down through its tool. Within one steward tick the steward's thread reports that persona as held, and reports nothing further about it until its state changes.

## Open Questions

- The rename to `steward` is assumed above and every section is written to it, so the plan is executable as it stands. The operator may reverse it before the plan arms, at the cost the assumption names. Recommended: keep the rename, because the name should state the job and the cost is one relaunch per worker at the cutover the process keeper plan already schedules.
- Whether the architect takes the kit's Expert seat for the repository it works in. Recommended: only once the kit-resolution plan lands, since until then the seat's registry entry would name the wrong repository. The kit session's call after that plan.
- Whether the steward should also hold the kit's Admin seat, whose inbox poll is a four-hour loop of the same clerical shape. Recommended: not in this plan; revisit after a month of the steward's boards.
- Section 2's second review-round backstop declaration, put to the operator at interim board 12. Decided 2026-09-18 by the operator on this session's channel: option (A), continue. Their words: "Go ahead and implement its removal, its test, its small findings, and do all of the stuff it suggested to pull that back out (since we don't need that piece that was kind of added along the way). Do one more round of review and checking to make sure that it's good before we continue." So the refused `architectPersona` precedence inversion is removed, the splice-site counter is fixed, the clone-by-existence reading is filed in the backlog entry, the twenty parked Minors take their close pass, and round 9 runs over that delta before the Chapter. The ladder's count restarts at this answer, and a continue from here buys three further rounds on the same terms.
- Whether the supervisor logs a line when a provided settings file names no architect, put to the operator at Section 2's ASK bucket (interim board 13). Decided 2026-09-18 by the operator on this session's channel: no. Their words: "I don't think we need it. The architect is there for me to interact with and talk to, it doesn't play a role in the active execution of plans or work. Therefore, if it doesn't start, I'll be going looking for it, trying to find out why it's not running. I don't see a concern here, the practical gap would be more than noise enough." So the silence the finding called invisible is not invisible to the party who would act on it, because the architect's absence surfaces as a missing conversational partner the operator is already looking for. No launch-time diagnostic is added, the scope adjudicator's ASK is disposed of as a refusal, and Section 2 closes without it.
- The architect's clone rule, put to the operator at Section 2's review backstop (interim board 7). Decided 2026-09-17 by the operator on the coordinator's channel: option (A), collapse the rule. The architect clones only a remote the operator named on the architect's own channel; a name arriving any other way is reported and never cloned; the relay leg through the coordinator persona, the steward's confirmation sentence and the README clause leave with their two pins. Rationale: the relay leg guards a no-channel launch this fleet never produces, and one rule the operator states on the thread is easier to audit than a chain of exceptions. The consultant recommended the same.
- Whether the steward reports a persona that a cleanly read roster no longer names, the sixth health class the code carries and the plan never asked for, put to the operator at Section 6. Decided 2026-09-19 by the operator on this session's relay thread: option (A), remove it. Their words: "On the recorded Blocker for Steward Architect, Option A, Remove It." So the departed-key mechanism comes out of `hooks/index.ts`, its cases come out of `.kit/controller-tick-test.mjs`, and the `README.md` paragraph and test-list clause that state it come out with them, returning the section to the five health classes the plan and the Standing Brief Amendments both name. Rationale: the plan states the five classes in two places, so the sixth was built without being asked for, and the fault that stopped the review exists only because the mechanism exists, so removing it removes the fault rather than buying another round. Reversal: restore the mechanism and its cases, at the cost of the outstanding fault and the bookkeeping the departed key needs. The cost accepted is that a persona vanishing from the roster between two ticks reaches the operator nowhere, and they find it by reading the roster themselves.

## Related

- `docs/archive/agent_persona_process-keeper_v1.md`: the process keeper, which this plan follows and whose roster and `keeper.json` the steward reads.
- `docs/archive/agent_persona_coordinator_v2.md`: the coordinator persona this plan splits.

## Chapters

### Interim board 1 - 2026-09-15

Written with Sections 1 and 3 at first green and their round 1 reviews in flight.

- **Run start.** The plan's `Status:` header read `Ready` and was normalized to `In Progress`, which is approval drift recorded here rather than silently. The plan carries no `## Dispatch Authorization` section and its own last assumption says it "arms on the operator's word"; that word is the operator's typed `/kit-goal ... --append` naming this exact path, so the plan is approved as written. The goal state records the arming as `self` because this run made the CLI invocation on the operator's relayed instruction, which is what the kit-goal flag rule requires; it does not mean the operator did not authorize the plan.
- **The preceding plan's blocker does not reach this one.** `agent_persona_process-keeper_v1` stopped on its own permission check at the review-round backstop. Sections 1 and 2 here touch only the supervisor's standing instructions, and Section 3 touches the plugin, so nothing in that stop bears on this work. Sections 4 and 5 do depend on the keeper and are named below.
- **Branch.** Work landed on a new branch `steward-architect`, cut from `5f7b66e` on `process-keeper` rather than from an earlier base. The reason is that the keeper plan's six modified files are uncommitted worktree state sitting on top of its committed code, so checking out an earlier base would have destroyed them. The cost is that this branch stacks on the keeper's commits, and the pull request is to be based on `process-keeper` rather than `main` at finishing. Those six files were confirmed untouched throughout: their modification times all predate the dispatch.
- **Section 1 (the steward's standing instruction) is at first green, committed as `01628b0` and pushed.** Three duties were appended to `COORDINATOR_ROLE_INSTRUCTION` in `bin/supervise.sh`, with no existing sentence changed and no change to how the instruction is selected. The instruction test gained eight cases across all five existing evaluations, extracting the block from the script by line range rather than hand-copying its prose.
- **Section 3 (the fleet status tool) is at first green, committed as `e8810f2` and pushed.** The tool, its five module-scope helpers, the `fleetRoster` setting and its manifest declaration, a 342-line suite and six fixtures. `hooks/operator.ts` is unmodified: the reach rule was reused and narrowed at the call site.
- **The orchestrator's brief was wrong on Section 3's gate and the implementer corrected it.** The brief said the needed gate "is exactly what `mayReachPersona` already decides". It is not: that rule's worker leg grants any session owning a named persona, because the leg exists to let a worker send the coordinator a record, so using it unchanged would have let every worker read fleet state. The implementer called `deliveryGroundIn` and admitted only the `COORDINATOR` and `READER:<coordinatorPersona>` grounds, which is the section's own sentence. Recorded because the correction is the implementer's, not this session's.
- **Route-b decisions the orchestrator locked before dispatch.** `.claude-plugin/plugin.json` was folded into Section 3, since the plan's Files in scope omitted it and the plugin reads its options from the manifest's `userConfig`; the implementer then confirmed empirically from the engine's own source that an undeclared key still reaches the plugin, so the declaration is worth keeping but was not required. `README.md` was carved out of both implementers' scope and written by the main thread, because both sections name it and they ran concurrently. Section 1's acceptance says "the three new duty sentences" while each duty is a paragraph, so one distinctive sentence per duty is pinned.
- **The keeper-action contract the orchestrator locked.** The plan's row shape asks for a keeper action, and `keeper.json` carries no such field: the keeper writes the action only to `keeper.log`'s `DECIDE` line. The implementer was given both sources and picked the `keeper.hold` marker plus `keeper.json`, because the marker is what actually stops a relaunch and a `holdReason` in the state file outlives a release. The log was rejected because it rotates at 5 MB while the plugin's read refuses anything over 4 MiB, which would take the action dark on the persona crash-looping hardest.
- **Folds and out-of-scope surfaces taken by the orchestrator after the implementers returned.** Two files the implementer edited outside its list are folded and named on Section 3's scope: `.kit/controller-tick-test.mjs` (two pins move because the tool list and count grew) and `.kit/.gitignore` (an allowlist entry, without which the new suite could never be committed). A sweep for other carriers of the reader-tier tool list found two stale ones and both were fixed: the manifest's arming description and a comment at `hooks/index.ts:1025`. The README trust-boundary paragraph's list of gate sites was incomplete and gained a sentence naming the tool's narrowing. The sweep's control matched a withheld instance, so its silence elsewhere is real.
- **Still owed as out-of-scope surfaces, not yet routed:** the implementer named a helper `hooks/commons.ts` should export, a `readAllEntries(store)` with no staleness filter and no collection, so the staleness predicate stops living in two files. `hooks/commons.ts` is outside every section's scope here. It is routed at Section 3's close.
- **Gate, measured by the orchestrator on a box it claimed and released, no contention, on this worktree at `e8810f2` plus the keeper plan's six foreign dirty files and the main thread's README and manifest edits:** the fleet status suite 48 passed, 0 failed, exit 0; the controller tick suite 785 passed, 0 failed, exit 0, against a recorded baseline of 785 taken before any edit, so no regression; the instruction test 33 passed, 0 failed, exit 0, against a recorded baseline of 25 before the change; `tsc -p tsconfig.json` exit 0 with zero output lines. Every count and exit code was read from the orchestrator's own run rather than from an implementer's report.
- **A hazard checked and cleared.** A stored lesson says a helper declared inside `register()` that closes over `$` passes both the typecheck and the harness while the plugin engine silently refuses to load the module at all, leaving the child with no tools. All eight declarations the diff adds are at module scope, `.kit/check-loader-rule.mjs` passes, and the typecheck is clean. The lesson's own stronger check, a real `--plugin-dir` load smoke, has not been run and is owed before this plan finishes.
- **Live dispatches:** the round 1 review workflow. Section 3 has an adversarial lens, a blind lens and a security lens, all at fable; Section 1 has an adversarial lens and a blind lens at fable. The security lens was asked specifically whether the tool achieves its intended narrowing, whether the shared rule was widened, and whether roster-named paths reach anything but reads. The five run in two batches to stay under the three-fable ceiling.
- **Rulings adopted since the last boundary:** none. No consult, no judge, no design stop.
- **Next action per section.** Sections 1 and 3: adjudicate round 1 when it returns, then the Minor close pass and the Chapters. Section 2 (the architect's launch shape and standing instruction) is unblocked and runs next; it was held back only because it edits the same two files as Section 1. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up, so it is an operator gate rather than work. Section 5 (cutover and roster) has an acceptance bullet that runs the blocked keeper plan's registration script, so it cannot close until that plan's decision lands.

### Interim board 2 - 2026-09-15

Written with Sections 1 and 3 fixed, committed and pushed, and their round 2 review in flight.

- **Round 1 adjudicated: five lenses, no Critical, eight Majors.** Three lenses independently found that nothing writes the `fleetRoster` option, so a supervised steward always reads an empty setting and the fleet status tool reports it has no roster. The orchestrator had confirmed the same from `emit_settings_json` before the round returned.
- **The one real design question went to a consult, and its ruling is adopted.** The steward's two per-tick duties had no runtime trigger: the controller tick starts no turn on a persona with no active goal leaf and an empty inbox, confirmed at `hooks/index.ts` step 4. The consult ruled the per-tick framing does not survive, and that the plugin should watch fleet health itself and wake the steward only when a persona's health class changes. It rejected the orchestrator's own lean, a standing goal, on evidence the orchestrator did not have: a nudge cap of three pauses such a leaf permanently, so the steward would wake three times and then go dark. It also rejected a per-tick prompt as 288 Sonnet turns a day, which is the spend this plan exists to remove. No operator fork survived.
- **A stored lesson sharpened the ruling.** A prompt the plugin submits reaches the session only once it is idle, and repeated submits accumulate rather than replacing one another, which is how one goal nudge once arrived more than fifty times. So the health map is persisted at submission rather than when the model reports. That is recorded as a standing brief amendment.
- **Four standing brief amendments were written at adoption**, above `## Sections of Work`, which is approval drift recorded here: the plugin wakes the steward rather than the instruction naming a cadence; a recurring submission is gated on a persisted state comparison persisted at submit time; the reconciliation pass runs on the owning skill's own four-hour cadence and never more often; and the health classes number five, the fourth being an enabled persona holding no live claim, which the old wording left unreported.
- **Section 6 was appended** to carry the two gaps neither section could close from inside its own scope: the `fleetRoster` plumbing and the plugin-side change-driven wake. That is approval drift by construction and is named to the operator.
- **Section 3's fixes.** The delay figure named the wrong thing: the row read `keeper.json`'s `currentDelay`, which the keeper writes from the next rung of its backoff ladder, and reported it as the wait in force, so a persona on its first crash waiting 300 seconds read as backing off for 600. Confirmed against HEAD rather than the worktree, since the keeper files carry another effort's uncommitted edits. The field is renamed and the tool now states the wait in force cannot be read from it. A signalled exit read as relaunching for a persona nothing would relaunch, and the suite pinned that wrong value. Hold-reason text, which a persona can write into its own run directory, now carries its source file and the same 2000-character bound `agentic_resolve` uses.
- **Section 1's fixes.** Both per-tick duties rewritten as responses to labelled prompts, the fourth health class added, and the architect duty now confirms a live architect before calling an ask routed, because `agentic_say` succeeds whether or not anything holds the persona.
- **Both implementers returned DONE_WITH_CONCERNS and both concerns are routed.** The fleet suite's first-crash case asserts the serialized row holds no occurrence of 300, a deliberately blunt pin, and Sections 1 and 3 now name prompt labels nothing submits until Section 6 lands. Both were put to the round 2 lens unrated, as questions.
- **Gate, measured by the orchestrator on an uncontended box, claim file empty, at worktree `267865f` plus the keeper plan's six foreign dirty files:** the fleet status suite 86 passed, 0 failed, exit 0, against a baseline of 48; the instruction test 41 passed, 0 failed, exit 0, against a baseline of 33; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `tsc -p tsconfig.json` exit 0 with zero output. Every count and exit code read from the orchestrator's own run.
- **The operator answered on the relay thread, twice, and the second message changed a call.** They chose to drop the process keeper's permission check whole, and then said the relay is their only channel, since this session is a headless process with no keyboard. The first reply had told them to repeat it at a keyboard, which was wrong and was corrected. The resolution recorded: the relay is the operator's channel, and what gates an act is its blast radius rather than which channel carried it, so an irreversible or outward-facing act still stops for a question. The keeper plan's own committed Dispatch Authorization section names that same relay thread as the operator's channel, which corroborates it.
- **The queue was re-armed to carry both plans**, steward first and the process keeper behind it, because a blocked plan sits in the finished list where an append refuses it. The replacement dropped the recorded blocked outcome from the goal state; that record lives in the keeper plan's interim board 11 and in the journal.
- **Live dispatches:** the round 2 review, one adversarial lens at opus and high effort through a workflow, which is what a later round takes over an opus writer. It carries both sections' fix deltas, the amendments in effect, the trace target, and the two implementer concerns as unrated questions.
- **Rulings adopted since the last boundary:** one, the consult's ruling on the wake mechanism. No judge, no design stop, no scope adjudication.
- **Next action per section.** Sections 1 and 3: adjudicate round 2, then the Chapters. Section 2 (the architect's launch shape) runs next; it edits the same two files as Section 1 so it was held until now. Section 6 (the fleet wake) follows Section 2, since it edits those files too. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last, and its acceptance runs the keeper plan's registration script, which the re-arm now puts back in reach.

### Interim board 3 - 2026-09-15

Written with Section 3 fixed twice more and its round 4 review in flight, and with Section 1 holding one frozen sentence pending a design ruling.

- **Round 2 adjudicated: no Critical, four Majors, six Minors.** All four Majors traced to the Goal or to Section 1's acceptance, so none was new-requirement and nothing went to a judge. All four sat in lines round 1's fix wrote, which makes them fix-introduced. That was the first fix-introduced round, since round 1 could carry none, so the design stop's two-consecutive-round trigger was not met and an ordinary fix round ran.
- **One round 2 Minor was upgraded to a security Major.** Hold-reason text out of a persona's own writable run directory reached the steward with no bracket neutralization, and Section 6 carries those rows into a submitted prompt where the JSON framing that contains it today is gone. A security finding at Major weight is never parked, so it was fixed before the section could close rather than held for the close pass.
- **Round 2's fixes, committed as `33e7af8` and pushed.** The tool's `action` field reported the keeper's last exit decision as the persona's present standing, because the keeper writes its state file only after a supervisor exit; a persona that crashed once and then ran healthy for a week still read as backing off. The keeper read now returns a standing and a new step settles the action against the commons half. The steward's fleet duty called a crashed persona one that had never come up. The architect liveness check reported every routed ask as undelivered whenever the roster was unset. The README asserted that the plugin submits the labelled prompts, which nothing in the tree does.
- **Round 3 adjudicated: no Critical, two Majors, eight Minors, and it fired the design stop.** Round 3's Major on the steward's fleet duty sits in the same sentence round 2's did, and both are fix-introduced. Two consecutive rounds of fix-introduced Majors in one passage is a design stop rather than a third repair.
- **What the design stop is about.** Round 2's fix re-keyed the never-came-up verdict from the absence of a live claim onto a null heartbeat age. Round 3 showed that is the same falsehood one branch over: `gcStaleClaims` in `hooks/commons.ts` deletes any commons entry older than the staleness threshold, and `readAllClaims` calls it on every read from five call sites, so a dead persona's entry is reaped within about ninety seconds and then does report a null heartbeat age. Confirmed by reading both functions. The stale branch the fix added is reachable only inside the pre-reap window.
- **The stop is held with the repository's live Expert seat, which is the judge the kit's rule names when the roster shows one.** The ask carries the mechanism, the round indices, the capture range, and the plan's Goal and Section 1 acceptance as the trace target, and deliberately carries no account of what happened inside the rounds and no lean. The frozen unit is that one sentence in `bin/supervise.sh` around line 1597. No fix round runs on it until the ruling lands. If nothing has arrived by the time Section 3's other work reaches its close gate, the kit's rule sends it to a scope adjudicator and the seat's later answer is recorded beside whatever that rules.
- **Four round 3 Minors were upgraded on their stated consequences and fixed, committed as `041406d` and pushed.** The bracket guard ran over whole fields, so a note reporting a run directory whose path contains square brackets reported a path that does not exist. A signalled exit was masked by a stale live claim for up to the staleness window, which the pre-fix code got right. A hold marker check that throws made a held persona read as running. And the byte-order-mark strip added in round 2 was a no-op, because `trim` already removes that character, so its new case could not have gone red.
- **Two round 3 Minors were routed forward into Section 6 rather than fixed**, and Section 6's text now carries both. The bracket guard belongs at the output channel rather than on two fields, because Section 6 is the second producer and splices these rows into a submitted turn. And Section 6's health-class reduction must read `nextDelaySeconds` and `note` beside `action`, because `running` collapses an unreadable keeper state and a climbed relaunch ladder into one value. That amendment is approval drift, recorded here.
- **Round count for the review-round backstop: four rounds dispatched, none of them a re-raise.** The bound is five. Nothing has fired it.
- **Gate, measured by the orchestrator on an uncontended box, claims directory empty, at worktree `041406d` plus the keeper plan's six foreign dirty files, a peer session's two docs files and this session's own plan doc edit:** the fleet status suite 118 passed, 0 failed, exit 0, against a baseline of 104; the instruction test 47 passed, 0 failed, exit 0, against a baseline of 47; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero output. Every count and exit code read from the orchestrator's own run rather than from an implementer's report.
- **Two out-of-scope surfaces are still owed and neither is routed yet.** `hooks/commons.ts` should export a `readAllEntries(store)` with no staleness filter and no collection, so the staleness predicate stops living in two files. And `hooks/operator.ts` should export the delivery-bracket rule as a character-set predicate, so its refusing form and the fleet lane's neutralizing form cannot drift on which characters count. Both are routed at Section 3's close.
- **A peer session is live in this working tree.** It added `docs/plans/agent_persona_unversioned-manifest_v1.md` and a line for it in `docs/README.md`, both uncommitted. Neither was read, staged, nor committed here. That index file also still describes this plan as Ready and parked, which is stale, and correcting it is held while the peer's uncommitted line sits in the same file.
- **The operator answered twice on the relay thread about the harness wrapper.** They asked for the wrapper's exact wording so the doctrine can be written to complement it rather than fight it, and both replies are on the thread. No decision was taken here and nothing in this plan changed.
- **Live dispatches:** the round 4 review, one adversarial lens at opus and high effort through a workflow, over commit `041406d` alone, carrying the amendments, the trace target, the three implementer concerns as unrated questions, and an explicit instruction that the frozen sentence is outside the review.
- **Rulings adopted since the last boundary:** none yet. The design stop's judge ask is outstanding.
- **Next action per section.** Section 3: adjudicate round 4, then the Minor close pass and the Chapter. Section 1: cannot close until the design ruling lands; everything else in it is fixed and green. Section 2 (the architect's launch shape) runs next, and it edits the same two files as Section 1. Section 6 (the fleet wake) follows Section 2. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last and its acceptance runs the keeper plan's registration script.

### Interim board 4 - 2026-09-15

Written with the design stop ruled and its removal performed, Section 3 fixed a third time, and round 5 in flight over both sections.

- **Round 4 adjudicated: one Critical, three Majors, two Minors.** The round covered Section 3 alone, over commit `041406d`.
- **The Critical was confirmed before it was acted on, and it was a second reversal of one passage.** Round 3's fix made a recorded signalled exit outrank a live commons claim unconditionally. The process keeper writes `keeper.json` at exactly one point, inside the relaunch loop after the supervisor returns, and nothing writes or clears it at launch, confirmed by reading `git show HEAD:bin/Start-Persona.ps1`. So after a signalled exit and a restart the recorded exit code stays at the signal value for the whole of the new run, and a live healthy persona reports as stopped the entire time. Round 2's ordering had the mirror defect bounded by the staleness window instead. Round 2 wrote the passage, round 3 reversed it, round 4 would have reversed it again, which is the second reversal the kit's rule sends to a consult.
- **The consult ruled and its ruling is adopted.** A recorded exit decides only when the live claim's heartbeat predates it. The keeper records the exit after the child tree has gone, so a dead session's last heartbeat necessarily precedes the stamp and a restarted session's necessarily follows it. Where the stamp cannot be read, the claim decides and the row's note says which reading was unavailable. The consult rejected the cheaper readings it was asked about: `claimedAt` is the same comparison with weaker validation and `launchCount` belongs to the previous keeper. No operator fork survived.
- **The design stop was ruled REFUSE by the scope adjudicator, and the removal is performed.** The Expert seat never answered, so the ask fell to the adjudicator when Section 1's other work was ready for its close gate, which is the window the kit's rule sets. The adjudicator found the mechanism departed from the form the plan asks for: the plan names five health classes and the instruction told the steward to make a claim about the persona's history and report it under a sixth label. The grounds were checked here before adoption. The instruction's own sentence lists the five classes and then names a class outside them, confirmed by reading `bin/supervise.sh` line 1597. The sentence now routes the condition to the class by its own name, and the two test fragments pinning the refused wording are replaced by an ordered pin that a rename reds.
- **One round 4 Major was discarded with the reason.** It objected to a justification recorded in a code comment, quoting a sentence about the roster being operator-side configuration rather than a persona's writable tree. That sentence is nowhere in the tree: the comment that is there says the opposite, that a run directory sits inside its persona's own writable tree. Confirmed by grep over the code and over the last three commit messages, with a control that matched a string known to be present. The permission fact the finding rested on is sound and is recorded here: every persona's scheduled task runs under one principal, so a persona could write the roster. The behaviour the finding asked to keep is already what the code does, and the splice-side neutralization is already routed into Section 6.
- **Two round 4 Majors and both Minors were fixed**, committed as `66d32f4` and pushed. The README paragraph claimed the cut mark carried the only square brackets left, which contradicted a clause two before it and the suite both. Five exact-wording pins on the tool description are retired, one of them a negative substring pin that went green on any rewording and so could never fail usefully. The roster read's caught error message now goes through the same neutralizing guard its three siblings use. And a comment that narrated the hold-marker check as deciding the action was corrected to say the unreadable case reaches the reader in the note.
- **Round counts for the review-round backstop.** Section 3 has taken four rounds and round 5 is in flight, so its next adjudication meets the bound of five: if that adjudication still leaves the terminal condition unmet, the section stops on the operator path rather than opening a sixth fix round. Section 1 has taken three rounds and round 5 is its fourth. The design stop's own count of consecutive fix-introduced rounds restarted at its ruling and is unrelated to this one.
- **Gate, measured by the orchestrator under its own box claim, claims directory empty on arrival, at worktree `66d32f4` plus the keeper plan's six foreign dirty files and a peer session's two:** the fleet status suite 123 passed, 0 failed, exit 0, against a baseline of 118; the instruction test 46 passed, 0 failed, exit 0, against a baseline of 47, the drop being the one anti-control case the design-stop removal retired; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. Every count and exit code read from the orchestrator's own run rather than from an implementer's report.
- **The implementer reported a tree delta it read as a peer session's, and it was this session's own.** `bin/supervise.sh` and `.kit/channel-reply-instruction-test.sh` became dirty while it worked, which was the design-stop removal being written in the main thread. Its own three files were untouched by that work and the file sets were disjoint throughout.
- **Two out-of-scope surfaces are still owed and neither is routed yet.** `hooks/commons.ts` should export a `readAllEntries(store)` with no staleness filter and no collection. `hooks/operator.ts` should export the delivery-bracket rule as a character-set predicate, so its refusing form and the fleet lane's neutralizing form cannot drift. Both are routed at Section 3's close. The implementer also named five sites in `hooks/` that interpolate a caught error's message raw and left them unchanged with the reason; those are put to round 5 as a question.
- **Live dispatches:** the round 5 review, one adversarial lens at opus and high effort through a workflow, over commit `66d32f4` alone, covering both sections, carrying the four amendments, the trace target, and six implementer and orchestrator concerns as unrated questions.
- **Rulings adopted since the last boundary: two.** The consult's ruling on the action precedence, and the scope adjudicator's REFUSE on the design stop with its removal performed. Both were checked against the code here before adoption.
- **Next action per section.** Section 3: adjudicate round 5, then the Minor close pass, the two owed out-of-scope surfaces, and the Chapter. Section 1: adjudicate round 5, then its Chapter; nothing else in it is outstanding. Section 2 (the architect's launch shape) runs next, and it edits the same two files as Section 1. Section 6 (the fleet wake) follows Section 2. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last and its acceptance runs the keeper plan's registration script.

### Chapter 1 - 2026-09-15
Completed: 1. The steward's launch shape and standing instruction
Implemented By: implementer-opus at first green; every fix round after that written in the main session, because the fixes were single sentences inside one instruction string and a brief would have cost more than the edit.
Metrics: review rounds 4, closed major-closed; provenance: the per-section split was not kept at rounds 1 through 3, whose boards record the counts across both sections together (round 1 eight Majors over five lenses, round 2 four, round 3 two), so only round 5's is per-section here, one Major, spec-traceable; rulings (1 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations none; consults 1
Decisions / Surprises: The section's own premise was wrong and a consult found it. Every duty had been written as something the steward does each tick, and the controller tick submits no prompt at all when a persona holds no active goal leaf and its inbox is empty, so a quiet fleet would never have woken it. The duties are now written as responses to labelled prompts, and Section 6 was appended to build the trigger that submits them. A design stop fired on one sentence of the fleet duty after two consecutive rounds of fix-introduced Majors landed in it, and the scope adjudicator refused the mechanism: the plan names five health classes and the sentence told the steward to judge whether a persona had ever come up and report it under a sixth label. The refusal was performed rather than recorded, so the sentence now routes that condition to the class by its own name. Round 5 then found the mirror gap, that the tool's action field carries values none of the five classes name, with `stopped` the row an operator most needs; the duty now says the action field is the keeper's record rather than a class and routes the off-list values by the claim and heartbeat it already reads.
Assumptions: none beyond the plan's own, which were written before execution.
Review Findings: design stop: the never-came-up verdict in the fleet duty, ruling refuse by the scope-adjudicator after the repository's live Expert seat did not answer inside the window; `review: adversarial and blind pair at fable, Agent tool` at round 1, then `review: adversarial lens at opus, Workflow` at rounds 2, 3 and 5. No Critical in any round. Majors addressed: the two per-tick duties rewritten as responses to labelled prompts; the fourth health class added; the architect liveness check added, because `agentic_say` succeeds whether or not anything holds the persona; the refused verdict removed; and round 5's, that three action values had no health class. Majors justified: none. Minors: 4 fixed across the rounds and the close pass, 1 upgraded on a stated consequence, 1 left with the reason. The one left is the full-sentence pin on the five class names in `.kit/channel-reply-instruction-test.sh`: it is an exact-wording pin on curated prose, which the retire class would ordinarily drop, but the five names are each too weak a substring to pin alone and nothing pins the three surfaces carrying this list byte-identical to one another, so it is the only instrument that catches a class going missing. Kept with that reason rather than retired.
Stamps: adjudicated 0, stamped 0; none surfaced, the window's reads all sitting on records the earlier boards already stamped.
Gate: targeted lane, run by the orchestrator under its own box claim, claims directory empty on arrival, at the worktree carrying this section's and Section 3's delta plus the process keeper plan's six foreign dirty files and a peer session's two, measured 2026-09-15: `.kit/channel-reply-instruction-test.sh` 47 passed, 0 failed, exit 0, against a baseline of 46 taken at `66d32f4`, the rise being this round's new ordered pin; `.kit/controller-tick-test.mjs` 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. The contention lane did not run, because this section's delta touches no machine-shared state. Every count and exit code read from the orchestrator's own run.
Next: 3. The fleet status tool
Commit Model: Branch-and-PR
Delta: measured 2026-09-15 on this worktree, no contention.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 2 - 2026-09-15
Completed: 3. The fleet status tool
Implemented By: implementer-opus at first green and for the round 2 and round 4 fix rounds; the round 3 and round 5 fixes and the close pass written in the main session.
Metrics: review rounds 5, closed major-closed; provenance: as in Chapter 1, the per-section split was not kept at rounds 1 through 3; round 4 was this section alone, one Critical and three Majors, of which the Critical and one Major were fix-introduced, one Major was spec-traceable and one was discarded as resting on a sentence that is nowhere in the tree; round 5 carried one Major, fix-introduced; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations none; consults 1
Decisions / Surprises: The tool reads two files that disagree about time, and every round but the first turned on that. `keeper.json` is written once, after a supervisor exit, and never at a launch, so it describes a decision the keeper has already carried out rather than the present. Reading it as the present made a persona that crashed once and then ran healthy for a week report as backing off. Making the live claim outrank it unconditionally made a signalled exit invisible. Making the signalled exit outrank the claim unconditionally made a restarted persona report as stopped for the whole of its next run. That third swing was the second reversal of one passage, which the kit routes to a consult rather than to a third repair, and the consult's rule is what stands: a recorded exit decides only where the live claim's heartbeat predates it. Round 5 then found that rule reading the wrong claim. Commons arbitration is first-claim-wins, and a dead session's entry survives until it passes the staleness threshold, so for up to ninety seconds after a restart the predecessor was both live and the earliest claim and the row followed it. The recorded exit now narrows the field before arbitration runs, which is the same rule applied at the selection rather than after it. Three things are recorded here that the interim boards carried forward. The two out-of-scope surfaces this section owed are routed: `hooks/commons.ts` now exports `readAllEntries(store)` with no staleness filter and no collection, which is a fold, since the duplicate store scan it replaces is one this section introduced and the close gate covers it; and `hooks/operator.ts` is added to Section 6's files in scope, because Section 6's own text already names `bracketSafeProblem` there as the home for the one exported bracket rule while its file list omitted the file, which is approval drift named here. The five sites in `hooks/` that interpolate a caught error's message raw are left unchanged with the reason: all five land in `sess.state.decisions[].detail`, whose only readers are a mechanical `includes` and an idle summary that renders `loop:action` and never `detail`, so no delivery label can ride out of them; the one site that did reach a model was the roster read, and round 4 fixed it. And the earlier window ran two implementers concurrently while one of them ran a tree-mutating probe, against this skill's exclusivity rule; the file sets were disjoint throughout and the tree-state brackets on both dispatches came back clean, so no contamination resulted, but the rule was breached rather than satisfied and the clean result is luck rather than evidence.
Assumptions: assumed 2026-09-15 (default, section 3): `.claude-plugin/plugin.json` is folded into this section's scope, since the plan's file list omitted it and the plugin reads its options from the manifest's `userConfig`; reversal: drop the declaration, which the implementer confirmed empirically is not required for an option to reach the plugin. assumed 2026-09-15 (source: the section's own acceptance sentence, section 3): a missing or unset roster returns no rows and a top-level problem rather than a row that says so, because no persona exists to name; reversal: none, the shape is the honest one.
Review Findings: `review: adversarial, blind and security lenses at fable, Agent tool` at round 1, then `review: adversarial lens at opus, Workflow` at rounds 2, 3, 4 and 5. One Critical, at round 4, confirmed against the code before it was acted on and routed to a consult rather than a fix because it was a second reversal. Majors addressed: the delay figure named the next rung of the keeper's ladder and was reported as the wait in force; the action field reported the keeper's last exit decision as the persona's present standing; the README asserted a submission nothing in the tree performs and later that the cut mark carried the only square brackets left; the roster read's caught error message bypassed the neutralizing guard its three siblings use; and round 5's, that the clock rule read the commons winner's heartbeat rather than the live session's. One Major discarded, orchestrator-made: it quoted a sentence about the roster being operator-side configuration that exists nowhere in the tree, confirmed by grep over the code and the last three commit messages with a control that matched a string known to be present. Majors justified: one, round 5's on the unreadable-stamp default, which asked the tool to fail toward `stopped` where the consult's adopted ruling says the claim decides; the finding's second half, that nothing told the steward to read `note`, was fixed in Section 1's duty instead. Minors: 9 fixed across the rounds and the close pass, 5 upgraded on a stated consequence, 2 left with the reason, and 2 routed forward into Section 6 rather than fixed here. One Minor was upgraded to a security Major and fixed before the section could close rather than parked: hold-reason text out of a persona's own writable run directory reached the steward with no bracket neutralization.
Stamps: adjudicated 0, stamped 0; none surfaced.
Gate: targeted lane, run by the orchestrator under its own box claim, claims directory empty on arrival, at the worktree carrying this section's and Section 1's delta plus the process keeper plan's six foreign dirty files and a peer session's two, measured 2026-09-15: `.kit/fleet-status-unit-test.mjs` 131 passed, 0 failed, exit 0, against a baseline of 123 taken at `66d32f4`; `.kit/controller-tick-test.mjs` 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression, which is the lane that covers the commons reach path this section's close-pass fold touched; `.kit/channel-reply-instruction-test.sh` 47 passed, 0 failed, exit 0, against a baseline of 46; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. The contention lane did not run, because this section's delta touches no machine-shared state. Every count and exit code read from the orchestrator's own run.
Next: 2. The architect's launch shape and standing instruction
Commit Model: Branch-and-PR
Delta: measured 2026-09-15 on this worktree, no contention.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 5 - 2026-09-15

Written with Section 2 built and three review rounds adjudicated, the third round's fixes green, and round 4 not yet dispatched.

- **Round 1: three lenses, one Major found by all three independently.** `review: adversarial, blind and security lenses at fable, Agent tool`. The shared finding: this section made the architect's name a setting while the steward's own instruction, written in Section 1, still routed design asks to the literal name `architect`. A fleet naming the seat anything else would have sent every ask to a persona nothing holds. Three more Majors beside it: the charter told the architect to cut a worktree and commit for every ask, including the consults and plan reviews the Goal names, which produce no file; the provided-settings branch discarded the launch environment's name silently, so a stale run directory launched a Fable session with no charter and no error; and nothing refused one persona named for both seats, which would have built both role instructions into one priming write.
- **Round 2: one lens at opus, six Majors, two of them about prose rather than code.** `review: adversarial lens at opus, Workflow`. The one that mattered most: the coordinator steer sentence is built for every launch and tells a session to put a coordinator record that ties to no goal node to the operator, or to decline it where no channel is attached. The architect holds no standing goal by design, so read literally it would have bounced every design ask the steward routed, which is the whole mechanism this section exists to build. The charter now names that rule and overrides it for this seat. Also: the read-side character class was wider than the emit side's over a value that the first round's own fix had just started splicing into the steward's instruction; the steward's routing list named four kinds where the Goal names five; and the charter said "cut a worktree" without naming which clone, whose likely reading sends the architect into a worker's live checkout.
- **Round 3: two lenses at opus, one Critical.** `review: adversarial and security lenses at opus, Workflow`. The Critical: the charter splices the coordinator's name, and on the provided-settings branch that name comes from a read whose class admits readable English. A hyphenated sentence passes it, so a persona able to write a settings file could plant prose in a top-privilege session's priming write. The fix holds the resolved name to the persona character class at the supervisor and refuses the launch when it fails, rather than narrowing the read, because the plugin resolves the wide-class name as the coordinator and narrowing would desynchronize the two. Beside it: the skill-load sentence sends every session to the plan-execution skill, which collides with the charter's own never-execute rule; the fleet carve-out said "two cases and no others" while the design clause called the liveness check a third; the clone clause still named no source, so a local clone would hardlink another persona's object store and push to it; and the pin guarding the headline fix could not red on the defect it exists to catch, because a bash case glob matched a second interpolation later in the same string.
- **Two findings routed rather than fixed.** Nothing on the process keeper's launch path can supply the new setting: its roster-to-environment map carries four fields and this is not one of them, so a keeper-launched steward would build no design clause and a keeper-launched architect no charter. That file belongs to the process keeper plan and is dirty in this worktree, so the gap is routed into Section 5, whose text and acceptance now name it. The second is recorded rather than built: a persona that can write another persona's run directory chooses who holds the design seat, and `docs/backlog.md` carries the hazard with a remedy that actually closes it, after a round found the remedy first recorded did not.
- **Two scope widenings, both declared.** `bin/agentic-common.sh` joined the section's Files in scope, because the settings emit and read both live there rather than in `bin/supervise.sh`, so the section's own sentence is unbuildable without it. `docs/backlog.md` joined it too, as the route for the hazard above.
- **Gate at this point, measured by the orchestrator on this worktree.** `.kit/channel-reply-instruction-test.sh` 88 passed, 0 failed, exit 0, against a baseline of 47 at `4eb8239`. `.kit/settings-plugin-key-test.sh` 95 passed, 0 failed, exit 0, against a baseline of 61. Both exit codes read from the runs. An em dash sweep over every added line returns zero, with a control line the same pattern counted as one.
- **Next action.** Round 4 over Section 2, then its Chapter. Round 3 carried two fix-introduced Majors, so a fourth round carrying more of them meets the design stop's condition and goes to the scope adjudicator rather than to a fifth repair.

### Interim board 6 - 2026-09-15

Written with Section 2's round 4 adjudicated, its fixes green, the design stop ruled, and round 5 not yet dispatched.

- **Round 4: one lens at opus, one Critical downgraded, five Majors, six Minors.** `review: adversarial lens at opus, Workflow`, carrying the security read over the one surface the round-3 fixes touched. The Critical said the charter's clone clause lets a worker-authored record name the repository as an `ext::` URL, which `git clone` would execute. Confirmed against the charter's text and then probed rather than believed: on this machine's git 2.55 a direct clone of an `ext::` URL refuses with `transport 'ext' not allowed`, no config setting it, and the same command under a forced `protocol.ext.allow=always` spawned a shell, which is the control that shows the refusal is git's default rather than a broken probe. The stated scenario does not reproduce, so the finding is downgraded at adjudication. What survives is a security Major on the same sentence and is fixed under the carve-out rather than parked: the charter's only guard fired before the push, after the clone had already run, and a hostile repository is read as context by a session at top privilege. The charter now holds the clone to a plain https or ssh remote URL, refuses any other shape, and waits for the operator's word before cloning a repository the operator did not name, with the coordinator persona's word as the no-channel fallback.
- **The other four Majors, all confirmed against the code before any edit.** The structural sweep that replaced round 3's broken pin read two literal shapes and missed a third that `bin/supervise.sh:1593` already carries; it now reads all three, its comment says it is an enumeration rather than a class read, and a new check counts the persona-variable interpolations in the script's own source and reds when a fifth appears in a shape the sweep does not read. The charter told the architect a launch prompt is information rather than an ask while the supervisor's own goal-prompt framing names that text the operator's trusted task; the charter now places the launch prompt as the operator's ask. The skills clause named no skill for a consult or a finishing judgment, two of the kinds the steward routes; it now names `claude-kit:consult` and `claude-kit:finishing-work`. And nothing wakes an architect on an ask it left half done, since it holds no goal node and the tick starts no turn on an empty inbox; the charter now has it report where it got to before ending such a turn. That last one traces to the Goal's spec-writing clause and sits in no fix round's lines, so it is spec-traceable; the other three are fix-introduced by round 3.
- **The design stop fired and was ruled.** Round 3's clone-clause Major and round 4's fetch-clause Major sit in one passage, the charter's repository-acquisition clause, and both are fix-introduced, which is the pair the stop keys on. The fetch Major was held, the expert ask went to the AP: Expert seat and had no answer inside the window, and the scope adjudicator ruled accept-and-declare: the Goal's "no home repository" and "specs written into the target repository" jointly require a way to obtain a copy, the passage is that way and nothing beyond it, and the round-4 narrowings add no channel or component. The grounds check on this session's surface: the Goal sentence at line 10 exists and covers the subject, and the diff adds no setting, tool or code path. On this shape a declare moves no bullet, so its record is this board and the Chapter's `Review Findings:` field. The held Major then re-entered the fix round: the charter said a fetch makes every branch start from a current trunk, which a fetch does not do, and it now says the branch is cut from the fetched remote-tracking trunk rather than the clone's local branch of that name. The README carried both that claim and the old push-only guard, and both moved with it. The count of consecutive fix-introduced rounds restarts at this ruling.
- **A second fix-introduced pair, named and not ruled.** Round 3's broken routing pin and round 4's sweep Major are also consecutive fix-introduced Majors in one mechanism, the test instrument guarding the routing target. It was not sent to the judge: the mechanism is the section's own Tests line verbatim, so a scope ruling on it has one possible answer, and the fix is the count backstop above. Named here so the record does not read as though only one pair fired.
- **Minors: 6 found, 6 fixed in the close pass.** The backlog entry now prices the denial-of-launch the two refusals make possible; the README no longer claims the two settings reads are pinned on the same six classes, and it no longer claims the splice sweep reads a class; the arrival clause's appositive names a persona other than the coordinator rather than any persona; the five hand-maintained absence lists, which three charter fragments had never been added to, are replaced by one sweep that reads every `ARCH_*_CONTROL` off the shell's variable table, and the default-persona eval on a fleet naming an architect now runs it too; and the plan-destination sentence is conditioned on a named repository. That class sweep caught a control of this session's own the moment it was written: `claude-kit:finishing-work` also appears in the skill-load sentence, so it was not withheld, and the control now uses the charter's own phrasing.
- **Every new instrument earned its green.** Three tree-mutating probes, each exclusive, each restored by copy and verified by `cmp` and by a `git status --porcelain` diff against the pre-probe capture, all three restores clean. A hardcoded name at the third splice shape: the sweep reds with `off-class= [steward]`. A fifth interpolation in a shape the sweep does not read: the sweep stays green on all three evals and the count reds at 5 against 4, which is the backstop's whole claim. A charter fragment the retired hand lists never carried, leaked into the skill-load sentence: all five absence cases red and name it.
- **Gate at this point, measured by the orchestrator on this worktree, claims directory empty on arrival.** `.kit/channel-reply-instruction-test.sh` 94 passed, 0 failed, exit 0, against 88 at `26242c9` and 47 at `4eb8239`. `.kit/settings-plugin-key-test.sh` 95 passed, 0 failed, exit 0, unchanged from `26242c9`. `bash -n` clean on both shell files. Both files 0 bare LF. Em dash sweep over the 140 added lines: 0, with a control line the same pattern counts as 1.
- **Next action.** Round 5 over Section 2. The delta is owed a round under the fix-delta bar, since the clone guard is an external boundary. Round 5 is the review-round backstop's number: where its adjudication leaves the terminal condition unmet, the section stops on the BLOCKED path with the phase analysis rather than opening round 6, and the design stop's ruling above stands as that declaration's consult on the repository-acquisition mechanism. Where it closes, the Chapter follows.

### Interim board 7 - 2026-09-15

Written with Section 2 stopped at the review-round backstop: round 5 adjudicated, its security-surface fixes committed, the round those fixes owe unrun, and the declaration going out.

- **Round 5: one lens at opus, no Critical, four Majors, six Minors.** `review: adversarial lens at opus, Workflow`, over base `4eb8239` with the delta at `31c2d86`, carrying the security read. All four Majors confirmed against the code before any edit, with one correction: the first Major cited a README line for the closure claim and that line is blank, so its README half is discarded and its comment half stands. Provenance: three fix-introduced, one spec-traceable, none new-requirement. The three fix-introduced sit on security surfaces (the persona-name refusal's comment, the clone gate's no-channel leg, the launch-prompt carriers), so they are carve-out findings, never counted toward a design stop and never held, and the fourth traces to the section's own Tests line. On the carve-out reading: the launch-prompt carriers are read as input handling, since the sentence decides which text a top-privilege session treats as an instruction, and the settings read is read as configuration; both readings are this session's and are recorded so the next reader can disagree.
- **The fixes, committed as `5a848f1` and pushed.** The settings read now refuses a present `architectPersona` outside the persona class, one naming `default`, or one that is not a string, with the value named, so the supervisor's existing could-not-read path exits 1 and the log says why; an empty string still reads as no architect. The pins that held the silent drop, a round-2 choice, are replaced by pins on the refusal and on the value it names, watched red on the old code first (8 red, then 100 green). The charter's clone gate says a no-channel launch waits for the operator's answer relayed through the coordinator persona and that persona's own word is not it, and the steward's design duty carries the matching half; two pins hold the pair, each watched red with its sentence removed, restore by copy verified by `cmp` and by a status diff. The README paragraph and the comment above the charter now place the launch prompt as the operator's ask, matching round 4's charter fix. The comment on the coordinator-name refusal now says what the refusal bounds (the shape, to one hyphenated token) rather than claiming it keeps a doctored instruction out; no length bound was added, since a cap with an arbitrary number would be the same closure claim in a different form.
- **Why the section stops here rather than opening round 6.** Every round-5 fix reaches a security-reviewer surface, so each fix delta owes a round under the fix-delta bar, and the terminal condition is unmet at the fifth round's adjudication, which is the backstop's number. The ladder stage: the opening bound is reached; a continue buys three further rounds, with each adjudication from the third of those onward that still leaves the condition unmet declaring again. The round count as restarted: 0 rounds taken past the bound.
- **Owed and unrun, written here so the re-arm finds it.** One review round over the round-5 fix delta (`31c2d86..5a848f1`), owed under the fix-delta bar because the delta changes the clone gate (an external boundary) and the settings read (configuration), and to be taken on the re-arm before anything else. Where the operator answers with the collapse below, its delta is read in the same round.
- **The pre-BLOCKED instruments.** The expert ask went to the AP: Expert seat and had no answer before the declaration; its answer, where one arrives, is recorded beside the ruling in force. The design stop's ruling from board 6 covers the repository-acquisition mechanism alone, and the phase analysis names other mechanisms too, so a consult was owed rather than substituted: `claude-kit:consultant` at fable, Agent tool. Ruling, checked on this surface: the generator is one clause, the trust rule for a repository name that reaches the architect from anyone but the operator, which the spec never drew and which each round's lens reconstructed one hop further; and the clone gate's relay leg guards a no-channel architect launch that nothing in this plan produces, confirmed here at `README.md:63` (`--channel-name architect`), the plan's Section 5 roster entry (`channelName architect`) and `bin/keeper-functions.ps1:234-235`. The consultant also found the fix-delta bar was not misfiring (the settings-read and clone-gate fixes each meet its security-surface trigger on their own) and that a mechanical clone guard would be a new component the Goal does not carry, so it is an ask for a follow-on rather than a remedy here. The fork that goes to the operator is therefore the trust rule rather than a round count, with the collapse recommended: the architect clones only a remote the operator named on the architect's own channel, anything arriving another way is reported and never cloned, and the relay sentences leave the charter, the steward's duty and the README.
- **Minors: 6 found in round 5, none fixed yet**, recorded in `.kit/scratch/steward-architect/minors-section-2.md` for the close pass, which runs after the terminal condition is met. One of them, the unbraced splice shape the count check does not read, is the same instrument family as round 4's sweep Major.
- **Gate at this point, measured by the orchestrator on this worktree, claims directory empty on arrival.** `.kit/channel-reply-instruction-test.sh` 96 passed, 0 failed, exit 0, against 94 at `31c2d86`. `.kit/settings-plugin-key-test.sh` 100 passed, 0 failed, exit 0, against 95 at `31c2d86`. `bash -n` clean on both shell files. Five files 0 bare LF. Em dash sweep over the 68 added lines: 0, control 1. Journal entry `kit.review.cap fail` logged with the provenance totals over five rounds: 13 spec-traceable, 11 fix-introduced, 0 new-requirement; rulings 0 refused, 1 declared, 0 asked (rounds 2 and 3 split as read from boards 5 and 6's prose).
- **Next action.** Wait on the operator's answer to the declaration. On (A), collapse the clone rule to the operator's own word on the architect's channel, delete the relay leg and the steward's confirmation sentence and the README clause with their two pins, take the owed round over `31c2d86..HEAD`, then the Minor close pass and the Chapter. On (B), take the owed round over `5a848f1` under the ladder. On (C), close without the owed round and record it as an author re-read. Remaining tree: the dirty keeper files and `docs/README.md` belong to the process-keeper plan and are not this session's.

### Interim board 8 - 2026-09-17

Written by the coordinator persona, not by an executing session, so that the session which resumes this plan finds the operator's answer and the two traps that stranded the plan for two days.

- **The declaration is answered: option (A).** Recorded under Open Questions with its rationale. Board 7's next action on (A) stands verbatim: collapse the clone rule to the operator's own word on the architect's channel, delete the relay leg, the steward's confirmation sentence and the README clause with their two pins, take the owed round over `31c2d86..HEAD`, then the Minor close pass and the Chapter.
- **This branch is the plan's only worked copy.** The `process-keeper` branch carries a copy of this document at `Status: Ready` with no boards, the parked spec as first written. On 2026-09-16 a session that resumed from that copy re-executed Section 1 from the original design, including the polling fleet-status tick board 2 corrected away, and left the result uncommitted in the keeper checkout when its supervisor killed it. Those edits were discarded rather than rescued, since the expert session and the coordinator each confirmed they duplicate superseded design; a copy of them stands at `.kit/scratch/coordinator/tree-after-child6-kill-2026-09-17T0600Z.patch` in the keeper checkout. So the resume rule for this plan: work only from this branch, never from the keeper branch's copy.
- **Resume sequence, after the process-keeper pull request merges.** Rebase this branch onto `main`, which by then carries the keeper's supervisor changes to the same files this plan edits, and resolve on this branch. Switch the checkout the leash runs in to this branch before the steward plan is armed or the queue advances to it. Then re-read board 7 and take the (A) path above. A session that finds itself in a checkout whose copy of this document has no boards is in the wrong checkout and stops before editing anything.

### Interim board 9 - 2026-09-18

Written by the session that resumed the queue and fell into the trap board 8 names, so the record of it sits beside the warning rather than only in a transcript.

- **The trap fired a second time, and board 8 is why it was caught.** This session armed the three-plan queue, cut a branch from the integration branch, and re-executed Section 1 from the parked spec. It ran three review rounds and two fix rounds against work Chapter 1 closed on 2026-09-15. The memory record `the-steward-plans-worked-copy-lives-on-its-branch-and-the-clone-rule-is-option-a` fired on a glob trigger at the first edit to this document and stopped it. Nothing was pushed. The duplicate sits at `87eee1c` on the local branch `plan/steward-architect`, kept for its findings and never to be merged.
- **What the wrong-base run is worth keeping for.** Its rounds found two defects in the design it was re-implementing. A coordinator told to relay an architect answer to a worker as its own record lets any local session claim the unheld architect name and reach a worker with delegated authority. The check that was meant to prove a duty sentence never reaches a worker matched one variable rather than the text the priming write sends, so it went green while sweeping almost nothing. The first is moot under option (A), which deletes the relay leg. The second is a test shape worth checking against this branch before Section 2 closes.
- **The checkout is now this branch and main is merged into it.** `cc3ac81` brings the keeper's supervisor and README changes onto the worked copy. Board 8 asked for a rebase; this is a merge, because the branch is already on the remote and only a force push could publish a rebase. Two README conflicts were resolved as unions, keeping this branch's architect charter and `fleet_status` reach sentences. The archived keeper plan's delete was accepted.
- **Gate at the merge.** `.kit/channel-reply-instruction-test.sh` 96 passed, 0 failed, exit 0, checked against 96 distinct check names so a concatenating merge could not inflate it. `.kit/fleet-status-unit-test.mjs` 0 failures, exit 0. `bash -n bin/supervise.sh` exit 0. Read from the runs themselves, on a box where a peer session holds the heavy-process claim for the claude-kit repository.
- **Next action, unchanged from board 7 and board 8.** Take the option (A) path on Section 2: collapse the clone rule to the operator's own word on the architect's channel, delete the relay leg, the steward's confirmation sentence and the README clause with their two pins, take the owed round, then the Minor close pass and the Chapter.

### Interim board 10 - 2026-09-18

Written with the operator's option (A) performed and committed, its owed review round adjudicated, and the fix round it owes not yet started.

- **The clone rule is collapsed and committed as `e07efdc`.** The architect clones only a repository the operator named on its own channel, and a name that reaches it any other way is reported and never cloned. The relay leg through the coordinator persona, the steward's matching confirmation sentence and the README clause are gone with their two pins. The gate's own pin is rewritten as an ordered pair over the rule and its refusal, and both halves were watched red on their own, each probe restored from a copy taken before the first mutation and verified byte-identical.
- **The owed round ran over two ranges rather than one.** Board 7 owed a round over `31c2d86..HEAD`, but the merge at `cc3ac81` now sits inside that range and brings about 1600 lines of the already-merged process keeper into `bin/supervise.sh`. So the round was dispatched over `31c2d86..5a848f1`, the round-5 fix delta, and `cc3ac81..HEAD`, the collapse, named as one delta with the merge excluded. One adversarial lens at opus and high effort through a workflow, carrying the security read. It ran wholly at its assigned tier: 71 of 71 non-synthetic assistant turns read `claude-opus-5`, no substitution. The tree-state bracket was identical before and after.
- **Round 6: no Critical, six Majors, three Minors.** Four Majors are fix-introduced and all four sit on security surfaces, the clone gate and the settings read, so every one of them is a carve-out finding: fixed before the section closes, never held, never counted toward a design stop. Round 5's fix-introduced Majors were carve-out findings for the same reason, so no pair has formed and no design stop fires.
- **Two Majors are defects this session's own collapse introduced.** The charter tells the architect that a launch prompt is the operator's ask worked as a message on its channel, and the new gate admits only a naming on that channel, which a launch prompt is not. The pre-collapse wording had no such conflict. And on a launch with no channel the collapsed rule leaves no reachable state at all: the gate's only admissible source does not exist, the refusal instructs a report to an operator with no reply tool, and the one no-channel fallback sentence is attached to the branch-and-filename report two sentences away rather than to the refusal. The relay leg used to cover that case. Removing it left the case undefined rather than decided, which is a gap in the text and not a reason to bring the leg back.
- **A third is the pin this session wrote overclaiming its own reach.** The comment says the refusal is what makes the operator's channel the only admissible source. It is not: the pin is an ordered substring pair, so a charter keeping both sentences verbatim and appending one exception satisfies both literals and goes green with the chain of exceptions restored. The reviewer supplied the defeating sentence. The instrument proves the two sentences are present and ordered and nothing more.
- **The fourth is the settings precedence, and its premise was confirmed here rather than taken.** The provided-settings branch lets the file win over the launch environment and justifies it with the coordinator's rationale, that the file is what outlives the launch. That rationale rests on the plugin reading the same key and needing to agree with it. Confirmed on this session's own surface with a control that speaks: `coordinatorPersona` appears 16 times in `hooks/index.ts` and 7 in `hooks/operator.ts`, while `architectPersona` appears zero times across `hooks/` and the plugin manifest, both clean exits rather than errored greps. The supervisor is this key's only consumer, so there is no second reader to agree with. The decision taken: the launch environment wins where it is set, falling back to the file where it is not. That closes a steward relaunched into any rundir written before this key existed silently building no design-escalation clause, and it adds no new refusal, unlike the alternative of refusing on a disagreement. It departs from the section's own parity-with-`coordinatorPersona` sentence, which is declared here rather than assumed.
- **The fifth Major is a real but narrow claim and rates below the bar it arrived at.** The round-5 refusal added a third way a written settings file can deny a launch, and the backlog entry's enumeration still names two. The cost itself is already priced in that entry, and its stated remedy is already the precedence change above. So the trigger list is short by one sentence and nothing else. It goes to the close pass with the Minors.
- **The sixth Major's premise holds and its worst case is already ruled out by this plan's own record.** `architectPersona` is written into the settings file under both plugin ids and is not declared in the manifest's `userConfig`, where `coordinatorPersona` and `fleetRoster` both are. Confirmed here by reading the manifest's own key list. The reviewer could not settle what the engine does with an undeclared key and named that as unverified; Chapter 2 records that Section 3's implementer confirmed empirically from the engine's own source that an undeclared key still reaches the plugin, so the rejection case the finding feared does not arise. The declaration is still owed on the section's own acceptance, which asks that the setting travel as `coordinatorPersona` does. `.claude-plugin/plugin.json` is folded into this section, and the fold is declared with its departure named: the fold predicate asks for a file in the same directory as one the section changed, and this file is not, while the other two clauses hold, the acceptance already carrying it and this section's own settings suite gating it.
- **One Minor is upgraded on a stated consequence, and it is the shape board 9 told this session to look for.** The absence check on the unset-architect path is a hand-maintained alternation of `ROLE_*` literals, and this session's own collapse edited that list by hand. Its `ARCH_*` sibling was given a sweep that reads the class off the shell's variable table precisely because a hand list had been carried in five places with three fragments never added to any of them. The consequence: a sixth duty sentence added to the design clause is swept by nothing, while every absence case there is labelled as proving no charter clause reaches the priming write. Board 9 flagged this shape and this session checked the `ARCH_*` family, found it already fixed, and stopped at the instance rather than the class. The round found it four lines away.
- **What the board-9 check did establish, and it stands.** The `ARCH_*` absence sweep on this branch is the strong form. It reads the fragment class off the variable table rather than a hand list, it sweeps the derived five-variable priming concatenation rather than one variable, and a live pin holds that the real priming write in `bin/supervise.sh` joins exactly those five variables, so a sixth instruction variable reds rather than slipping past an absence case that never looks at it. The defect the wrong-base run rediscovered does not exist on this branch on that axis.
- **Gate at this point, measured on this worktree at `e07efdc`.** `.kit/channel-reply-instruction-test.sh` 94 passed, 0 failed, exit 0 read from the run, against a baseline of 96 measured on the same worktree with the change stashed out; the drop of two is the change's own arithmetic, three pins removed and one added, and the 94 was counted distinct so no duplicate hides a loss. `bash -n` exit 0 on both shell files. A sweep for every carrier of the removed sentences matched nothing across the three files, against a control counting 10, 3 and 1 in their pre-edit copies; its members are a closed list this change itself wrote, so it is a swept list rather than a swept class. Measured on a box where a peer session holds the heavy-process claim for another repository, these being sub-second offline shell scripts.
- **Round counts.** Section 2 has now taken six rounds. The review-round backstop's opening bound was reached at board 7 and the operator's continue bought three further rounds, of which this is the first. A declaration is owed again from the third of those onward, so rounds 6 and 7 do not declare and round 8 would. The design stop's count of consecutive fix-introduced rounds stands where board 6's ruling left it, since every fix-introduced Major in rounds 5 and 6 is a carve-out finding.
- **Live dispatches:** none. Round 6 has returned and its workflow is complete.
- **Rulings adopted since the last boundary:** none. No consult, no judge, no design stop, no scope adjudication.
- **Next action.** Fix round 6's four carve-out Majors and the upgraded Minor, in one pass: name the launch prompt in the gate sentence, state the no-channel case outright, reword the pin comment to what the pair actually pins, flip the architect settings precedence to the environment where it is set, declare the manifest key, and give the `ROLE_*` absence check the same class sweep its `ARCH_*` sibling has. That delta reaches the clone gate and the settings read, so it owes round 7 under the fix-delta bar. Then the Minor close pass over the twelve parked from rounds 4 and 5 plus round 6's two remaining, then the Chapter. Sections 6, 4 and 5 follow in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 11 - 2026-09-18

Written with round 6's fixes committed, round 7 run and adjudicated, its fixes and the Minor close pass made, and the round that delta owes not yet run.

- **This session widened a rule the operator decided, and the widening is reverted.** Round 6 found the collapsed clone gate conflicting with the charter's own sentence that a launch prompt is the operator's ask. This session resolved that by admitting the launch prompt as a second clone source. That was the wrong side to move. The operator's decision of 2026-09-17 reads that the architect clones only a remote the operator named on its own channel and that a name arriving any other way is reported and never cloned, and a launch prompt is any other way. Round 7 caught it, the decision was re-read verbatim from the Open Questions entry, and the gate is back to one source. The conflict is closed the other way instead: the charter now says outright that a repository named in a launch prompt is not a repository the operator named on the channel.
- **The lesson is about which side of a conflict moves.** A review finding that names a contradiction does not say which sentence is wrong. Where one of the two sentences is an operator decision recorded verbatim, it is the other one that moves, and no reasoning about the conflict's merits changes that. Nothing in round 6's finding was wrong; the repair chose the surface that was not this session's to change.
- **Round 7's third Major is why the widening mattered rather than merely being out of order.** The launch prompt is not held in the supervisor's memory. It is written to a file in the run directory and read back tens of seconds later, after the child's own startup, and the run directory is the surface the backlog already names as writable by a persona. Admitting it as a clone source would have made a credentialed clone and push depend on that file's integrity. Reverting removes the credentialed act rather than adding a guard for it.
- **Round 7: no Critical, six Majors, seven Minors, all thirteen adjudicated.** It ran wholly at its assigned tier: every non-synthetic assistant turn read `claude-opus-5`, no substitution, 51 tool calls over about 25 minutes.
- **The first Major is the gap the collapse left on the main routing path, and it is now stated rather than inferred.** With a channel attached, one charter sentence ordered a worktree cut and a push for a repository a coordinator record named, and another forbade cloning it, and no sentence said what happens. That is the ordinary shape of a design ask. The charter now says a record naming a repository the architect holds no clone of is reported and not cloned, that it cuts no worktree and pushes nothing, and that the ask waits until the operator names that repository on the channel. The worktree sentence is bounded to a repository already cloned, so the contradiction is removed at both ends.
- **The fourth Major is the vacuous-green shape reappearing inside the instrument written to prevent it.** The control added for the design-clause class sweep passed on an empty class read, because its only failure path ran inside the loop the empty read skipped. The sweep and its control were therefore both silent in exactly the case the control existed to catch. Both classes now declare a floor and every read asserts it, and a single check runs both floors once per suite. That is the class fix rather than the instance fix: the charter sweep had the identical hole and was repaired in the same pass.
- **The remaining two Majors are drift this session's own fix created.** The README stated the settings precedence backwards in three places, and the precedence was pinned only on the library function while the supervisor's call site was covered by nothing. The supervisor now logs the value it resolved, and the two driven cases assert that value rather than asserting that both names appear in a line whose wording is fixed.
- **The Minor close pass ran over all twelve parked from rounds 4 and 5 plus round 7's seven.** Five parked Minors closed with receipts: their premises no longer hold at HEAD, the fragments they named having been swept by the class read, the arrival clause reworded and the two plan-destination clauses brought into agreement. One had already been discharged by the round 6 fix commit. One is retired with its reason: merging the two settings readers into one helper widens the blast radius of a security-relevant read for no gain the callers need. The rest are fixed here, among them the splice-site counter reading only braced spellings, the design duty leaving an operator's own design question with no relay leg back to the operator, and the clone rule leaving credentials, query and fragment undefined in the URL shape it admits.
- **The splice-site counter's fix found three sites and none of them was a splice.** Counting the unbraced spelling raised the count from four to seven. All three additions were the snippet's own shell comparisons, which read the same variables and splice nothing. The counter now drops the snippet's control flow before counting, rather than absorbing the difference by raising the expected number, which would have retired the check's whole purpose.
- **Gates at this point.** `.kit/channel-reply-instruction-test.sh` 104 passed, 0 failed, exit 0 read from the run, against 96 at the previous commit. `.kit/settings-plugin-key-test.sh` 112 passed, 0 failed, exit 0, against 106. Both counts are distinct names, so no duplicate hides a loss. `bash -n` exits 0 on all four shell files and the manifest parses. Measured on a box carrying no foreign test runner.
- **Round counts, and the declaration that is now close.** Section 2 has taken seven rounds. The operator's continue at the backstop bought rounds 6, 7 and 8, and a declaration is owed from the third of those. This delta reaches the clone gate and the settings read, so it owes round 8, and round 8 is that third round. So the round after this one is the last that runs without going back to the operator.
- **No design stop fires, and the pair that would form it is now one round away.** Rounds 5 and 6 produced only carve-out Majors. Round 7 is the first round of this section whose fix-introduced Majors are not all carve-outs: three of its six are ordinary correctness and documentation findings on the instrument and the README. A round 8 with the same shape would be the second consecutive one in a single mechanism.
- **Live dispatches:** none. Round 7 returned and its workflow is complete.
- **Rulings adopted since the last boundary:** none. No consult, no judge, no design stop, no scope adjudication.
- **Next action.** Run round 8 over this fix delta, declaring it to the operator as the backstop requires, then the Chapter with its `memq unstamped` adjudication. Sections 6, 4 and 5 follow in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 13 - 2026-09-18

Written with the operator's continue performed, round 9 run and adjudicated, its three confirmed Majors fixed, and one Major held on a judge's ASK that goes to the operator.

- **The operator answered the backstop: option (A), continue.** Recorded under Open Questions with their words. The ladder's count restarts at that answer. Round 9 is the first of the three rounds the continue buys, so rounds 9, 10 and 11 run and a declaration is owed again from the third of those onward. This board's declaration is not that one: it is the ASK bucket below, which is a different instrument.
- **The refused removal is performed and committed as `98a4772`.** `resolve_architect_persona` is gone from `bin/agentic-common.sh` with its header entry, and the provided branch in `bin/supervise.sh` now reads the architect's name from the settings file exactly as it reads the coordinator's. `read_settings_architect_persona` already held that value to the persona character class and refused `default`, confirmed by reading it, so the removal drops no validation.
- **Two consequences the recorded remedy did not predict, both found by reading the code rather than the record.** Removing the environment capture left `ARCHITECT_PERSONA_GIVEN` dangling in the both-seats refusal, which composed its message out of where each name came from; with one source that machinery is dead and the refusal names the file. And nothing logs the resolved architect name on a clean read, exactly as nothing logs the coordinator's, so the two driven pins that read the old NOTE line had nothing left to assert. They are replaced by two that pin the call site through an existing observable: a file naming its own coordinator as its architect refuses only if the launch took the file's value, and a file naming no architect reaches the gate only if it did. The two read orders end the launch with different exit codes, so neither pin passes under the other rule.
- **The splice-site counter is fixed twice, once by the fix and once by round 9.** Its filter dropped whole control-flow lines, so a splice sharing a line with the condition guarding it escaped both the counter and the enumeration it backstops. The strip now removes the control-flow expression and keeps what it guarded. Round 9 then found the replacement had its own silent zero: a greedy `.*` reached to the last `; then` on the line, so a splice whose own text carries `; then` was deleted with the condition. Reproduced here before the fix, remainder empty and count 0 against a true count of 1. The condition is now matched with `[^;]*`. Both shapes carry a control, and all three readings were taken: 0 under the old whole-line drop, 1 correct, 3 with no strip at all.
- **Round 9: no Critical, four Majors, four Minors.** One adversarial lens at opus and high effort through a workflow, carrying the security read, over `973e72d..98a4772`. It ran wholly at its assigned tier: 37 of 37 non-synthetic assistant turns read `claude-opus-5`, no substitution, 28 tool calls. The tree-state bracket was identical before and after. Every Major was confirmed against the code here before adjudication.
- **Three Majors were confirmed and are fixed.** A second precedence paragraph survived in `README.md` that the fix commit's own message claimed to have removed, an overclaim corrected here. The greedy strip above. And the clone-by-existence filing asserted that one fix closes both surfaces, which is false: the planted clone sits in the architect's workdir while the stated remedy hardens the run directory, and `RUNDIR` defaults to `$WORKDIR/run`, so containment runs inward and never outward. The backlog entry now names both directories as the closure condition.
- **The fourth Major is held, and the judge ruled ASK.** It asks the supervisor to log a NOTE when a provided settings file names no architect, so that "this fleet has no architect" is distinguishable from "nobody noticed the key was missing". The scope adjudicator's first dispatch returned NEEDS_CONTEXT against a brief that carried the plan's Approach, a prior ruling and a fix narrative, all of which its charter forbids; that was a defect in the brief and the one re-dispatch this step allows was made clean. Its ruling: the NOTE is named by no acceptance bullet and no Goal sentence, so it is a new mechanism, and it is not off the goal path either, because a steward that comes up with no design-escalation clause defeats the Goal's own "wakes the architect" clause.
- **The judge's grounds were checked here before adoption, and its sharpest point holds.** `coordinatorPersona` carries a default of `coordinator`, so its own silent missing-key path is harmless; `architectPersona` carries no default, so the acceptance bullet's "as `coordinatorPersona` is" mirrored the silence across without the default that made it safe. Confirmed by reading both readers in `bin/agentic-common.sh`.
- **One claim the judge marked reported is confirmed here, and it is broader than the finding said.** No live settings file on this box carries `architectPersona`: not `run/settings.json`, not `D:/personas/coordinator/run/settings.json`, and not `D:/personas/aios/run/settings.json`. Read directly from the three files. Nothing is broken today because the architect persona is not deployed until Section 5, so this is a transition hazard rather than a live fault.
- **The Minor record was wrong and is corrected.** Board 12's "twenty parked" does not hold. Nineteen were dispositioned at board 11, and round 8's eight stood open, of which exactly one was ever written down. The other seven never reached the Minor file and did not survive the previous session's compaction. They were not reconstructed from memory and were delegated to round 9, which read the current tree and restated none of them. Two of round 9's four Minors are fixed here and two are left with their reasons, all four recorded in the Minor file.
- **One Standing Brief Amendment was added, so the remaining sections inherit the guard.** A check whose acceptance is an empty or absent result must first assert that its subject was produced at all. The plan has now produced that defect twice on unrelated surfaces, which is what makes it a class rather than an instance. This is approval drift and is recorded as such.
- **Shared state altered to get this done.** None in the tree. `bin/supervise.sh` is executing right now under three personas, and bash reads a script incrementally by byte offset, so deleting lines from it in place can make a running copy resume mid-text when its loop exits. Both shell files were edited as copies under the gitignored scratch path and swapped in by rename, which leaves every running process on the original inode. One edit to that file also failed to match on its first attempt because the file carries CRLF endings while the replacement string carried LF; nothing was written on that attempt.
- **Gate at this point.** `.kit/channel-reply-instruction-test.sh` exit 0 read from the run, all passed, 107 distinct check names against 105 at board 12, the rise being the splice counter's two controls. `.kit/settings-plugin-key-test.sh` exit 0, PASS, 107 distinct against 112, the fall being six resolver pins removed, the two driven precedence blocks replaced by two tighter ones, and two new round-trip checks added. Both counted distinct so no duplicate hides a loss. `bash -n` exit 0 on all four shell files and the manifest parses. The machine's claim file carried no live claim on arrival; these are sub-second offline shell scripts, so that reading is that nobody has claimed the box rather than that the box is clear.
- **Live dispatches:** none. Round 9's workflow and both scope-adjudicator dispatches have returned.
- **Rulings adopted since the last boundary: one.** The scope adjudicator's ASK on the logging finding, its grounds checked here. An ask disposes of nothing, so Section 2 stays open at the finding step and its Chapter is not written.
- **Next action.** The declaration goes out and the section holds. On an answer of yes, add the NOTE on the provided branch and take the round that delta owes, then the Chapter. On no, record the refusal in the Chapter and write the Chapter. Either way Sections 6, 4 and 5 follow in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 12 - 2026-09-18

Written with round 8 adjudicated, its one carve-out finding fixed, a second design stop ruled REFUSE, and the review-round backstop firing again.

- **Round 8: no Critical, four Majors, eight Minors.** One adversarial lens at opus and high effort through a workflow, over `86c76ea..b226620`, carrying the security read. It ran wholly at its assigned tier: 62 of 62 non-synthetic assistant turns read `claude-opus-5`, no substitution, 34 tool calls. The tree-state bracket was identical before and after. All four Majors were confirmed against the code here before any adjudication.
- **One Major is a carve-out and is fixed.** The clone gate bound the repository the operator named and never the remote URL, while the charter separately says a repository is obtained by cloning its remote URL and never says where that URL came from. So a composite arrival passed: the operator names a project on the channel and a coordinator record supplies the URL for it. The gate now binds the URL itself, and a following sentence says outright that a repository the operator named without writing its URL is not cloned. An ordered pin holds the pair, watched red with the provenance sentence removed, the restore verified byte-identical by `cmp` with `git status --porcelain` unchanged. This does not touch the operator decision of 2026-09-17, which fixes who may name a repository; it makes the charter say that the URL itself is what the operator must have written.
- **The second design stop fired, and the judge ruled REFUSE.** Rounds 7 and 8 each carry a fix-introduced Major in one mechanism, the `architectPersona` precedence rule and the prose across the tree that states it. Round 7 found the README stating it backwards; round 8 found the same claim still wrong in the supervisor's own comment and self-contradictory in the very paragraph round 7 rewrote. The scope adjudicator ruled that the mechanism departs from the form the section's own acceptance asks for. Its grounds were checked here before adoption: the acceptance at line 58 asks that the setting be read back as `coordinatorPersona` is; `coordinatorPersona`'s provided-file branch reads the file alone and says so in its own comment at `bin/supervise.sh:359-370`; and `e07efdc`, the base of the held range, carried that same file-governs form. So the environment-first precedence this session adopted at board 10 is an added part no bullet named, and the ruling orders it removed.
- **The removal is owed and unrun, because this adjudication also fires the backstop.** The backstop freezes the stopping round's fixes save the carve-out's, and a removal a refuse orders is frozen like any other fix. What the removal takes off, from the judge's own grounds and the consult's reading: the environment-first line in `resolve_architect_persona`, the call site's precedence branch and its disagreement NOTE, the comment that justifies the inversion, the README's three precedence sentences, the `architectPersona` description in the manifest, and the six resolver pins and two driven pins that hold it. `docs/backlog.md` names the inversion as half-closing a hazard, and that paragraph becomes false with it.
- **Also owed and unrun: the splice-site counter's Major.** Its filter drops whole lines, so a splice written on a control-flow line escapes both the counter and the enumeration it backstops. Nothing in the tree hits it today, confirmed by the lens's read-only simulation over the real snippet. The fix strips the control-flow expression rather than the line.
- **And the eight round-8 Minors, parked with the twelve the close pass has already handled.** One of them is recorded here because its disposition is a rule rather than a repair: the charter trusts any clone already sitting under the architect's directory, since the trust anchor is a channel message that does not survive a relaunch while the directory does. Only a persona able to write that directory could plant one, which is exactly the hazard `docs/backlog.md` already records with its remedy. So it goes to that entry rather than to the charter, and a later finding resting on the same premise goes there too.
- **The consult ran and overturned this session's framing.** The design stop's ruling covers the precedence mechanism alone while the phase analysis names three, so a consult was owed rather than substituted: `claude-kit:consultant` at fable, Agent tool. This session's framing was that the round count is what reviewing a security boundary written in English costs. The consultant refused that as the central claim and named three generators instead. The largest is this session's own spec departure at board 10, which produced five Majors across rounds 6 to 8 and which the judge has now refused. The second is the test instrument, a bespoke sweep growing a hole per round, which guards regressions rather than attackers. Only the third is the prose gate, and it is one reading from its floor, that reading being the recorded hazard above. The ruling is adopted: the fork put to the operator is the ladder's own continue rather than a narrowing of what the architect may do, and the consultant's reason for not offering the narrowing is that workers already hold the same credentials and the same directory writability on this box, so moving the credentialed act buys no blast-radius reduction.
- **Gate at this point, measured on this worktree with the carve-out fix in place.** `.kit/channel-reply-instruction-test.sh` exit 0 read from the run, all tests passed, 105 distinct check names against 104 at `b226620`, the rise being this board's one new ordered pin. `.kit/settings-plugin-key-test.sh` exit 0, PASS, 112 distinct names, unchanged. `bash -n` exit 0 on all four shell files. `bin/supervise.sh` and `README.md` both 0 bare LF. The claims directories on both machines were empty on arrival; the process poll behind that reading is weak on this box, where a process listing prints no command line, so the reading is that no claim stands rather than that the box is clear. These are sub-second offline shell scripts.
- **Round counts and the ladder stage.** Section 2 has taken eight rounds. The opening bound was reached at board 7 and the operator's continue bought rounds 6, 7 and 8; round 8 is the third of those, so this adjudication declares. The round count as restarted: 3 rounds taken past the bound. A continue from here buys three further rounds on the same terms, with each adjudication from the third of those onward declaring again. The design stop's own count of consecutive fix-introduced rounds restarts at this ruling.
- **Live dispatches:** none. Round 8's workflow, the scope adjudicator and the consultant have all returned.
- **Rulings adopted since the last boundary: two.** The scope adjudicator's REFUSE on the precedence mechanism, its grounds checked here, with the removal owed. And the consultant's ruling on the framing, which changed what goes to the operator.
- **Next action.** The declaration goes out. On a continue: perform the refused removal, fix the splice counter, file the clone-by-existence reading in the backlog entry, run the Minor close pass over the twenty parked, then round 9 over that delta, then the Chapter with its `memq unstamped` adjudication. Sections 6, 4 and 5 follow in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Chapter 3 - 2026-09-18
Completed: 2. The architect's launch shape and standing instruction
Implemented By: implementer-opus for the first green and the early fix rounds; rounds 6 through 9 and every fix from board 10 onward written in the main session, because each turned on a judgment about what the section was allowed to contain rather than on code to be written from a brief.
Metrics: review rounds 9, closed refuse-then-ask; provenance: rounds 1 through 5 shared with Section 1, rounds 6 through 9 this section alone; rulings (1 refused, 0 declared, 1 asked, the ask answered no by the operator); NEEDS_CONTEXT count 1, on a scope-adjudicator brief this session wrote carrying three charter-forbidden inputs and no base ref, re-dispatched clean; escalations 1, the review-round backstop at board 12; consults 2.
Decisions / Surprises: The section shipped an architect settings key, a charter, and the supervisor's reading of both, and the expensive part was none of those. It was a mechanism this session added along the way and then had to remove. At board 10 the provided-settings branch was given an environment-first precedence for the architect name, mirroring nothing the acceptance asked for. Rounds 6, 7 and 8 each found the prose describing that precedence wrong on a different surface, and a scope adjudicator ruled the mechanism itself refused: the acceptance asks that the key be read back as `coordinatorPersona` is, and `coordinatorPersona`'s provided-file branch reads the file alone. The removal took `resolve_architect_persona` out of `bin/agentic-common.sh` entirely, which is safe because `read_settings_architect_persona` already enforces the same name shape and already refuses `default`. Two surprises rode with it. The provided branch now mirrors the coordinator's silent missing-key path, but `coordinatorPersona` carries a default and `architectPersona` does not, so the mirror carried the silence across without the default that made it harmless. That is what became the ask below. And `bin/supervise.sh` was executing in eight live processes while it was edited; bash reads a script incrementally by byte offset, so the edit was made on a copy under the gitignored scratch path and swapped in by rename, leaving every running process on the original inode. A separate trap: that file carries CRLF, so an exact-string replacement written with `\n` matched nothing and silently wrote nothing, which a line-based replacement preserving each line's own ending fixed.
Decisions the operator made: two. At board 12 the review-round backstop fired and they chose option (A), continue, with the refused precedence removed and one more review round taken before the Chapter. At board 13 the scope adjudicator's ASK went up, whether the supervisor should log a line when a provided settings file names no architect, and they answered no. Their reason is better than the finding's: the architect exists for the operator to talk to and plays no part in executing plans, so an architect that does not start is something they go looking for. The gap is its own signal and no launch-time diagnostic is added. That refusal is recorded in Open Questions with their words.
Assumptions: none new in this section beyond those the plan already carries.
Review Findings: nine rounds. No Critical survived to the close. Rounds 6, 7 and 8 each carried a fix-introduced Major in the one precedence mechanism, which is what fired the backstop and produced the refusal above. Round 8's separate carve-out Major was real and fixed: the clone gate bound the repository name the operator wrote rather than the remote URL, so a composite arrival could have had the operator name a project while a record supplied its URL. The gate now binds the URL itself. Round 9 confirmed three and all three are closed. A second README paragraph still published the removed precedence, in the architect launch recipe, and the prior commit message had overclaimed its own removal. The splice-site counter's replacement filter carried its own silent zero, matching a control-flow condition greedily so that a splice whose own quoted text held a terminator was deleted before counting; it now stops at the first terminator, proven against three readings, 0 under the old whole-line drop, 1 correct, 3 with no strip. And a backlog filing asserted that one fix closed two exposures when it closes neither of the other: a planted clone sits in the architect's workdir while the recorded remedy hardens the run directory, and `RUNDIR` defaults to `$WORKDIR/run`, so containment runs inward and never outward. Minors: 23 raised across the section. 19 from rounds 4, 5 and 7 were dispositioned at the close pass recorded in board 11, six of them spot-checked against the code before that record was trusted. Round 8 raised 8, of which one was filed to the backlog and seven were never written down and did not survive a compaction; they were delegated to round 9 over the current tree rather than reconstructed from memory, and round 9 restated none of them. Round 9's own four: two fixed, two left with the reason, both recorded in the scratch file the section keeps for this.
Approval drift: one, and it is an addition to `## Standing Brief Amendments` rather than a section edit. A check whose acceptance is an empty or absent result must first assert that its subject was produced at all, because otherwise the step that produces the subject can fail and the check passes on exactly the failure it exists to exclude. This is written as a class the remaining sections inherit rather than fixed as an instance, because the plan produced it twice on unrelated surfaces.
Stamps: `memq unstamped` run at this boundary returned 5 project-tier and 12 operator-tier records read and unstamped in the window. Two were adjudicated as genuinely applied in this stretch and stamped: `crlf-per-file-in-windows-checkouts` in the operator tier, which is what diagnosed the silent zero-match replacement on `bin/supervise.sh`, and `the-steward-plans-worked-copy-lives-on-its-branch-and-the-clone-rule-is-option-a` in the project tier, which carries the clone rule this section's charter implements. The remaining fifteen were read for orientation earlier in the effort and none of them changed an act taken here, so none was stamped.
Gate: targeted lane, run in the main session, claims directories on both machines empty on arrival, at this worktree with the section's whole delta in place, measured 2026-09-18: `.kit/channel-reply-instruction-test.sh` exit 0 read from the run, all tests passed, 107 distinct check names against a baseline of 105 taken at interim board 12, the rise being the splice counter's two new controls; `.kit/settings-plugin-key-test.sh` exit 0, PASS, 107 distinct check names against a baseline of 112 at the same board, the fall being six `resolve_architect_persona` unit pins retired with the function, the two driven precedence blocks replaced by two that discriminate on exit code, and two new round-trip checks added. Both counted distinct, so no duplicate name hides a loss. `bash -n` exit 0 on all four shell files and `.claude-plugin/plugin.json` parses. The contention lane did not run, because this section's delta touches no machine-shared state; the in-place edit of a running script was avoided by the rename described above rather than performed.
Next: 6. The fleet wake: the roster setting the launcher writes, and the plugin's change-driven prompt
Commit Model: Branch-and-PR

### Interim board 14 - 2026-09-18

Written with Section 6 built, its first review round adjudicated, and a fix round in flight.

- **Section 6 is built and green, and the green is not the story.** An opus implementer built both halves: the `fleetRoster` option emitted from `FLEET_ROSTER` in `bin/agentic-common.sh` with a read-back helper beside the architect's, and the controller tick's change-driven wake in `hooks/index.ts` that reduces each fleet row to a health class, compares against a map, and submits a `[FLEET]` turn carrying only the rows that moved, plus a `[RECONCILE]` turn on a four-hour cadence. Five lanes were re-run in the main session rather than accepted from the report, all exit 0 read from their own status: the tick suite, the settings-key suite, the channel-reply suite, `npx tsc`, and the fleet-status unit suite the implementer added to the floor because it consumes the reading this section extracted.
- **Round 1 ran three lenses at fable and returned one Critical and five Majors.** Adversarial, blind and security, all at high effort. The security lens ruled BLOCK and both others CHANGES_REQUIRED. Three findings were reached independently by two or more lenses, which is weight rather than proof, so the Critical was confirmed against the code here before the fix round was briefed.
- **The Critical is the exact threat this section was appended to close, arriving by a door nobody watched.** The section's own text says the bracket guard must move to the splice, because a submitted turn loses the JSON framing that contained persona-written text upstream. That was done: every field the prompt carries passes through `bracketSafeText`, confirmed across ten call sites. But `bracketSafeText` is `text.replace(/\[/g, "(").replace(/\]/g, ")")` and nothing else, while the `[FLEET]` prompt is a line-structured list joined with newlines. `hooks/index.ts:742` takes `state.holdReason.trim()`, which keeps interior newlines. So a persona writing a newline into its own `keeper.json` forges whole report lines about another persona, carrying no bracket at all, in a turn whose label is the trust signal and whose instruction tells the steward to report each line to the operator. This repository already exports the guard for it, `quoteContinuationLines` at `hooks/operator.ts:720`, whose own comment names this failure; the fleet splice never reached it. The lesson is general and is why this is recorded rather than just fixed: moving a guard to a channel means moving every guard that channel needs, and the one that was named is not the whole set.
- **The five Majors.** A dropped submit is lost for good, because both new sites write their state before the await, correctly, and then discard the outcome that says whether the submit landed. The watcher goes silent exactly when the roster breaks, since an unreadable roster yields zero rows, zero rows means nothing changed, and the line that would report the problem sits inside the branch that requires rows. A flapping persona can drive one submit per tick and short-circuit the rest of the tick. "Persisted" was read as in-session module state, so every steward relaunch re-reports every already-unhealthy persona and a steward relaunched faster than the cadence never reconciles at all, which breaks Operator Verification item 3 in this plan's own words. And one new test leg accepts two silences without asserting that the tick reached the block under test.
- **That last one is the fifth Standing Brief Amendment's class, appearing for the third time in this plan.** It was written into the amendments at Section 2's close precisely so the remaining sections would inherit it, and it still shipped. The amendment is not at fault and is not being rewritten; what this shows is that the class survives being known, so the section's reviewers are what catch it rather than the author's own care.
- **One scope decision was made rather than asked, and it is approval drift.** Fixing the persistence Major needs `hooks/agent-state.ts`, which Section 6's Files in scope does not name. It is authorized for that finding alone, because the section's own text says "a persisted health map" and an acceptance sentence of this plan fails without it. The fixer was told to confirm empirically that `AgentState` survives a relaunch on this box before building on the premise, and to stop and report rather than build if it does not.
- **One hypothesised Critical was ruled out rather than fixed.** A judgment sidecar flagged the roster path's backslash handling as not doubling, which would have made every supervised steward's settings file unparseable. It does not hold. Confirmed here by running the substitution on this box's bash and parsing the result, and independently re-confirmed by two lenses running the real emitter: a Windows path round-trips byte-equal, and a quote or control character is refused with no file written. The sidecar's reading came from its own probe, where the Bash tool halves a doubled backslash before bash sees it, so the probe under-reports.
- **Gate at this point**, all five read from their own exit codes in the main session at the post-implementation tree: `node .kit/controller-tick-test.mjs` exit 0 against a baseline of 835 `OK:` lines at `143d23b`; `bash .kit/settings-plugin-key-test.sh` exit 0 against 107 distinct; `bash .kit/channel-reply-instruction-test.sh` exit 0 at 107 distinct, unchanged; `npx tsc -p tsconfig.json` exit 0 with zero bytes; `node .kit/fleet-status-unit-test.mjs` exit 0. Claims directories on both machines were empty on arrival. The contention lane did not run, because this section's delta touches no machine-shared state.
- **Live dispatches:** one, the fix round. Its first-turn reading was taken and resolved: 49 assistant turns, every one `claude-opus-5`, no substitution and no synthetic placeholder. The three reviewers have all returned.
- **Rulings adopted since the last boundary: none.** No scope adjudicator or consultant was convened this round. The Critical is a defect against the section's own stated intent rather than a question about what the section may contain, so neither seat had a question to answer.
- **Next action.** Take the fix round's report, re-run the five lanes here rather than accepting them, then round 2 over the fix delta. Section 6's Chapter follows. Then Sections 4 and 5 in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 15 - 2026-09-18

Written with Section 6's first fix round landed, its second review round adjudicated, and a second fix round in flight.

- **Fix round 1 closed all seven of round 1's findings and the five lanes are green, re-run here rather than accepted.** Every exit code was read from its own unpiped run: the tick suite exit 0 at 934 checks against a baseline of 835, the settings-key suite exit 0 at 134 distinct against 107, the channel-reply suite exit 0 at 107 distinct and untouched, the fleet-status unit suite exit 0 at 131, and `npx tsc -p tsconfig.json` exit 0 with zero bytes. Four judgment-sidecar alerts claimed earlier gate readings took their exit code from an `echo` rather than from the suite. That class of error is real and is the reason this re-run was taken; these readings are `$?` captured directly after each unpiped command, corroborated by each suite's own `PASS` line.
- **Round 2 ran two fresh-context lenses at fable and both returned CHANGES_REQUIRED.** The two reached the same Major independently, which is the strongest signal this plan has produced from a review round. Every finding acted on below was confirmed against the code here before the fix was briefed.
- **The Critical's remedy is complete in bytes and silent to the reader, which is the third guard the channel needs.** Round 1 moved the bracket guard to the splice and missed the line-break guard. Fix round 1 added the line-break guard, so a persona-written newline now opens a line prefixed `> ` while a plugin-composed line opens `- `. The forgery is distinguishable in bytes. But the `[FLEET]` header tells the steward to report each line below on its own channel, and nothing in that header or in the steward's standing instruction says what a `> ` line is. So the forged line is distinguishable to a byte comparison and indistinguishable to the only reader that matters. The lesson generalises the one board 14 recorded: a guard that marks a hazard is finished only when the party reading the mark has been told what it means.
- **The suppression memo stores the last observed class rather than the last reported one.** Both lenses found this. A persona reported as held, flipping back to healthy inside the quiet window, then settling, produces the line "still healthy" to an operator who was last told held, and the real transition is named nowhere. The remedy is a second field holding the class actually emitted, with the observed class kept only for counting.
- **The quiet window as built breaks this plan's own acceptance.** Operator Verification item 3 requires a held persona reported within one steward tick. The window suppressed any class change inside ten minutes, so a shutdown three minutes after a restart report waited two ticks. The ruling is to suppress only a return to a class already reported inside the window, and to report a move to a not-yet-reported class at once. That keeps the flood bound the window exists for and restores the acceptance item.
- **The inbox drain starves the whole Section 6 block.** The drain delivers one record per tick and returns, and Section 6 was appended below it, so a coordinator holding a backlog reports no fleet reading and advances no reconcile stamp for as many ticks as the backlog holds. The ruling is to hoist the fleet block above the drain. A fleet change is rare and time-critical while a queued record is durable and merely delayed, and with the corrected window fleet lines are bounded to one per key per window, so the delay this imposes on delivery is bounded in turn.
- **Two rulings were made rather than asked, and both are declared here.** The quiet window's narrowed semantics and the block's new position above the drain are design calls inside the section's own intent, not scope additions, so neither went to the operator. The fixer was told they are calls rather than suggestions and told to escalate rather than substitute.
- **Nine minors accepted.** Within-line forgery, closed by moving every persona-written field onto its own `> ` line so a row line carries only plugin text. A false code comment claiming a name check that is never called. A prompt that can say the roster is unreadable and that every entry has a row again, in the same breath. A stored memo missing a field making the window's arithmetic NaN and suppressing that key forever. A missing turn-open re-check letting one tick queue three prompts. A stopped persona with no claim reading as backing off, which promises a relaunch that will not happen. A single shutdown reported twice. A problems comparison keyed on roster position rather than content. And a thin control running on a different harness than the check it certifies.
- **Live dispatches:** one, fix round 2. Both round 2 reviewers have returned and are closed.
- **Next action.** Take fix round 2's report, re-run the five lanes here rather than accepting them, then round 3 over that delta. Section 6's Chapter follows. Then Sections 4 and 5 in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 16 - 2026-09-18

Written with Section 6's second fix round landed, its third review round adjudicated, a third fix round in flight, and the operator's re-ordered plan queue armed.

- **The queue changed on the operator's word and now holds four plans.** They asked on the relay channel for this plan first, then the context-budget removal, then a lean-injection plan that had merged to main minutes earlier, then the supervisor-peer plan. The lean-injection plan was unreachable from this branch, so main was merged in and pushed before the arm. A merge takes the whole gate rather than a targeted lane, because a clean merge can redden a suite by itself with both parents green, so all five lanes ran on the merged tree before the push and came back at identical counts. One consequence was named back to the operator rather than left to surprise them later: the supervisor-peer plan's arming gate requires every plan before it closed, so moving it last makes it wait on three rather than two.
- **Fix round 2 built all thirteen rulings and the five lanes are green, re-run here rather than accepted.** Each exit code read from its own unpiped run: the tick suite exit 0 at 1015 checks, the fleet-status unit suite exit 0 at 132, the settings-key suite exit 0 at 134 distinct, the channel-reply suite exit 0 at 108 distinct, and `npx tsc` exit 0 with zero bytes.
- **Round 3 ran three lenses and all three returned CHANGES_REQUIRED.** The security lens, which ruled BLOCK in round 1, moved to CONCERNS. Three findings were reached by two lenses independently. The three highest-impact claims were confirmed against the code here before the fix round was briefed.
- **The most important finding of this section is the inverse attack, and it was not looked for until round 3 asked for it by name.** Every guard through rounds 1 and 2 assumed an attacker who wants to add a false line. The security lens was briefed to ask what a persona gains by suppressing a true one, and found two working paths. The steward's health memo about a worker is persisted in a state file that sits in the worker's own working directory, because the live roster gives both personas the same one. A worker writes itself a memo with a timestamp in the far future and a full class window, and the quiet window never expires, so the worker can crash and no line is ever sent. Separately, a persona writes a disabled flag into its own roster entry and exits: it reads healthy before, its commons entry ages out within a tick, and it reads healthy after, so it leaves the fleet with no line at all. A watcher that the watched can silence is worse than no watcher, because it is trusted. The remedy is validation of the stored value in the parse, never a check of who wrote the file, which a standing operator decision forbids.
- **The forgery channel is closed for persona-written fields and was still open for roster-written ones.** Round 2 moved every persona-written field onto its own carried line so a composed line holds only plugin text. The roster is a second producer of file text that the round did not move, so a refused roster name still rides inline on a composed line the header has just told the steward to trust. The same field never passes the length bound the hold reason and note both pass. This is the lesson of board 14 arriving a third time: the producers of a guarded channel are a set, and closing the one that prompted the finding is not closing the channel.
- **Two classifier defects would have shown on the first real fleet rather than in any test.** Any note under a live claim reads stale, and a persona on its first launch has no keeper file yet, so a fresh fleet reports every persona stale until its first exit. The fixture always seeds that file, so no test could see it. That is the class of defect a green suite is structurally blind to.
- **Scope was ruled here rather than asked.** Three findings went to the backlog rather than into the section: a persona forging its own heartbeat from a detached writer, which the security lens said itself has no in-section remedy; the shared state store's write race, which is pre-existing and only made visible here; and comment debris in the supervisor script, not worth a second swap of a file running in live processes. Everything else was ruled in, because it is a defect in code this section wrote.
- **The fifth Standing Brief Amendment's class has now shipped four times in this plan.** A check whose acceptance is an empty result, passing on the failure of the step that produces its subject. Written into the amendments at Section 2's close, restated in every brief since, and still arriving. The amendment is not at fault. What this run demonstrates is that the class survives being known by the author, so the reviewers are the control rather than the brief.
- **A process defect of the orchestrator's own, recorded because it cost review strength.** The blind reviewer's brief carried a sentence describing the shape of the diff. A blind review gets the diff and nothing else, and that sentence was an intent story. The reviewer caught it, said so, disregarded it, and then found the premise was wrong anyway. No finding was lost, and the round was weaker than it should have been.
- **Live dispatches:** one, fix round 3. All three round 3 reviewers have returned and are closed.
- **Next action.** Take fix round 3's report, re-run the five lanes here rather than accepting them, then a fourth review round over that delta. Close on a round that finds no Major. Section 6's Chapter follows, and must carry the amendment that the block now sits above the inbox drain rather than below it, which the section text still describes the old way. Then Sections 4 and 5 in that order, Section 4 needing a real child launch and the operator's yes while the fleet is up.

### Interim board 17 - 2026-09-18

Written with Section 6's third fix round landed and re-gated here, a gate policy adopted, the queue grown to five plans, and a small fourth fix round in flight.

- **A gate policy is now in force, and it is the operator's own decision of 2026-09-18.** Runs that hold the box are deferred to one end-run after the last queued plan merges. That set is `.kit/live-all.sh`, every `.kit/live-*-test.sh`, `.kit/supervisor-natural-exit-test.sh`, and any other check that launches a `claude` child or runs past two minutes of wall clock. Everything else still runs where the plan and the executing-work skill place it, and the fresh-context reviewer pair still runs, since it holds no box time. The policy reaches this plan from its finishing pass onward rather than from now, so Section 6's own lanes are unaffected, all six of them being mock-driven and finishing in minutes. `docs/plans/agent_persona_deferred-gate-run_v1.md` owns the policy and carries the end-run.
- **The policy arrived as a peer's claim and was not acted on until it was verified.** Two coordinator messages relayed it, each asserting the operator's authority, and one asked for the policy to be written into this document as the operator's decision and for the finishing pass to proceed under it. A peer message is a colleague's claim rather than operator steering, whatever authority it names, so the deferral was honored as a stop, which costs nothing and reverses easily, while the two durable acts were held: writing a decision into a permanent record under the operator's name, and signing a plan off with its whole gate unrun. The operator was asked on the channel that reaches this session and confirmed it is theirs. The claim is corroborated twice over now, since the new plan's own Dispatch Authorization quotes the operator verbatim. The general rule this run applied: honor a relayed stop, hold a relayed act, and ask.
- **The policy settles Section 4 better than the objection raised against it.** This session recommended keeping the live section outside the deferral, on the ground that deferring a live acceptance skips it rather than proving it. The plan already answers that: where a section's acceptance names a deferred suite, that clause is met by the end-run, and the Chapter must say so in those words rather than record a pass. The recommendation was withdrawn. Section 4 therefore closes with its live acceptance deferred and named as deferred, never as passed.
- **Fix round 3 built all seven Majors and six Minors, and the gate was re-run here rather than accepted.** Six lanes, each exit code read from its own unpiped invocation: the tick suite exit 0 at 1111 checks, the fleet-status unit suite exit 0 at 132, the channel-reply suite exit 0 at 114 distinct, the settings-key suite exit 0 at 134 distinct, `npx tsc` exit 0 with zero bytes, and the loader-rule check exit 0.
- **A sixth lane was added because a stored memory said the other five are blind to a whole failure mode.** The plugin engine judges `hooks/index.ts` under loader rules that both `tsc` and the tick harness pass straight over, and a violation refuses the entire module, so the persona comes up with no tools and the only trace is a line in a debug log. This section adds many helpers to that file, which is exactly the class the record names. `.kit/check-loader-rule.mjs` now runs after any edit to `hooks/` and is green. It is a pattern check over the shapes seen so far rather than the loader itself, so the real one-shot load smoke belongs to the deferred end-run.
- **The settings suite reddened here and it was a real defect, not the contention the fix round took it for.** Two checks failed, reproducibly and in isolation, so the flake reading did not survive. The failing pair predate this section: they are byte-identical at HEAD, confirmed against a control showing the same instrument reports this section's own additions as absent there. Root cause is a time-coupled fixture. The holder is stamped sixty seconds old and read against a ninety-second bound, which gives the suite a thirty-second budget to travel a few lines that each spawn a process. On a box carrying five personas that budget is gone, the stamp ages past the bound, and the reading flips from live to stale for a reason the check is not about. Fixed by re-stamping immediately before each large-bound read, taken as a flagged bonus outside the section's scope because the lane cannot yield a trustworthy green without it. The small-bound legs need no re-stamp, any age at all being past a one-millisecond bound. Undo: restore the two re-stamp lines from the byte copy at `.kit/scratch/steward-architect/settings-test.pre`.
- **The round escalated a residue rather than building past its brief, which is the behavior wanted.** Closing the first-launch defect meant reducing an absent keeper file under a live claim to healthy. That opened a narrower silence: a persona that deletes the keeper file in its own run directory buys silence on exactly one class, because the row keeps the note while the class stops moving, and a note reaches only an operator who asks for it by hand. The round proved this with a real run rather than by reading, and did not build the remedy because it sat outside the seven Majors. It is ruled in now and is the fourth fix round's first item, taking the same composite-value shape the disabled-roster flag already uses.
- **The queue is five plans and was re-armed twice.** The operator added the lean-injection plan and then the deferred-gate-run plan, each of which had merged to main minutes before, so main was merged into this branch twice to make the paths reachable. The first re-arm was also corrected: it recorded the arming as typed by the operator when this session ran the CLI on a relayed instruction, which takes the self-armed flag. Two plans carry no committed Dispatch Authorization, this one and the lean-injection plan, which the CLI named and which the arming survives.
- **Next action.** Take fix round 4's report, re-run the six lanes here rather than accepting them, then a final review round over the whole Section 6 delta. Close on a round that finds no Major. Section 6's Chapter follows and owes three things: the amendment that the block now sits above the inbox drain rather than below it, the three findings ruled to the backlog with their reasons, and the gate policy named for every run it defers. Then Sections 5 and 4, with Section 4's live acceptance deferred under the policy.

### Interim board 18 - 2026-09-18

Written with Section 6 committed at first green, main merged in on the operator's word, its fourth review round adjudicated, and a fifth fix round in flight.

- **Section 6's delta is committed at last, as `bdfb5df`, and pushed.** It had carried four fix rounds as uncommitted worktree state, which is a gap rather than a choice: Branch-and-PR commits a section's verified state at first green and layers review fixes on after, and that moment passed unmarked when the section first went green at interim board 14. What forced the correction is that the operator asked for main to be merged, and main rewrote `bin/supervise.sh`, which this section also edits. A merge cannot run over a dirty tree, so the commit came first. The lesson is that a first-green commit is not bookkeeping: skipping it leaves a section with no recovery point for as long as its review takes, and the cost lands on whatever unrelated thing next needs the tree clean.
- **Main is merged as `32062c1` and pushed, on the operator's request relayed on this session's channel.** They asked for it so the box can be restarted onto a supervisor performance fix another session landed: the poll now reads each cycle's facts in one process instead of fourteen, extracted into `bin/supervise-poll.mjs`. The merge was clean, including in `bin/supervise.sh`, where main removed a hundred and fifty lines around the block this section edits. Both of this section's edits to that file survived, confirmed by grep, and the file's CRLF is intact at 3259 carriage returns against 3259 lines. **The merge's whole gate is owed and unrun.** A clean merge can redden a suite by itself with both parents green, and that redness sits in files neither parent changed, so no lane derived from the merge's own diff reads it. The box was held by another repository's live claim when the merge landed and is held by this section's own fix round now. The gate runs when that round returns, before anything else.
- **Round 4 ran the adversarial and blind lenses at opus and high effort, and both returned CHANGES_REQUIRED with a Critical apiece.** Three Criticals in total, every one confirmed against the code here before the fix round was briefed. Two of the three are the suppression class this section has now met at every round since the third.
- **The most serious finding is a guard whose own comment is the bug.** `hooks/index.ts:2598` reads `if (!await persist($)) return;`, and its comment asserts that a false return means nothing reached the store. That is true of one of `persist`'s two false paths and false of the other: the commons-arbitration branch writes `sess.state` to disk and then returns false. Because the advanced reading was assigned at `:2563`, that write banks every changed key stamped as reported with no prompt ever submitted, and the successor session reads them as already told and stays silent for good. What makes this worth recording is that the code rolls the reading back correctly on both of its other exits, at `:2577` and `:2605`. The one path with no rollback is the one whose comment explains why a rollback is unnecessary. A false premise stated confidently in a comment is harder to see than no comment at all, because it answers the question a reader was about to ask. The same shape repeats for the reconcile stamp at `:2665`.
- **The second Critical is the disabled-roster guard defeated by deleting the entry instead of disabling it.** The rows loop at `:2455` walks only the keys the current reading produced, so a persona that removes its own roster entry and exits produces no line at any point: the key count moves, the reading is persisted, and nothing is said. The section had already closed the weaker form of this, where a persona writes `"enabled": false`, by folding the roster flag into the compared value. Deleting the entry beats that guard completely, because there is no entry left to carry a tail. This is the producers-are-a-set lesson from board 16 arriving again on the same channel: closing the case that prompted the finding is not closing the class.
- **A Major is routed to Section 5 rather than built here.** Nothing on the process keeper's launch path can set `FLEET_ROSTER`: `Build-SupervisorInvocation` in `bin/keeper-functions.ps1` maps four fields and no roster path, so the emitter this section added never fires for a keeper-launched steward and the whole watcher is inert on the real fleet. That file is Section 5's scope and Section 5 already edits that exact map to add `architectPersona`. The finding is recorded here so it cannot be lost, and Section 5's map edit now has two fields rather than one.
- **The round-count bound is one round away.** This section has taken four review rounds. The fifth review round's adjudication is the operator's backstop: if it still leaves an owed Major, the section stops on the blocked path with a phase analysis rather than opening a sixth fix round.
- **Gate at this point**, all six lanes re-run in the main session rather than accepted from the fix round's report, each exit code read from its own unpiped invocation, claims directories on both machines empty on arrival: the tick suite exit 0 at 1132 checks against a baseline of 1111, the settings-key suite exit 0 at 134 distinct, the channel-reply suite exit 0 at 119 distinct against 114, the fleet-status unit suite exit 0 at 132, `npx tsc` exit 0 with zero bytes, and the loader-rule check exit 0. Those readings are from before the merge. The whole gate named above is what reads the merged tree.
- **Live dispatches:** one, fix round 5, building the two persist rollbacks, the departed-roster-key line, and three small findings, with the keeper map confirmed and left untouched. Both round 4 reviewers have returned and are closed.
- **Rulings adopted since the last boundary:** none. No consult, no judge and no design stop. Every round 4 finding is a defect in code this section wrote, against the section's own stated intent, so no seat had a scope question to answer.
- **Next action.** Take fix round 5's report, re-run the six lanes, then run the merge's whole gate on the merged tree, which is owed. Then the fifth review round, which is the backstop round. Section 6's Chapter follows and owes four things: the amendment that the block sits above the inbox drain rather than below it, the three findings ruled to the backlog with their reasons, the gate policy named for every run it defers, and the roster-path Major routed to Section 5. Then Sections 5 and 4, with Section 4's live acceptance deferred under the policy.

### Interim board 19 - 2026-09-18

Written with Section 6's fifth review round adjudicated, a consult ruled and adopted, and the repair it ordered in flight. The review-round backstop has fired and its declaration is owed.

- **The fifth review round ran the full roster and the section did not close.** Round 4's three Criticals re-raised the round, so round 5 took round 1's roster: adversarial, blind and security, all at Fable. The adversarial lens returned APPROVED_WITH_CONCERNS and confirmed all three of round 4's Criticals closed against the code. The blind lens returned CHANGES_REQUIRED on a Major. The security lens returned BLOCK on two Criticals, both confirmed here against the code before anything was acted on.
- **This is the backstop's first firing, at five rounds, and the count restarts at the operator's answer.** A continue past it buys three further rounds, and each adjudication from the third of those onward declares again. The declaration is a decision ask and not a kill: nothing is discarded and the section stands where the last fix round left it.
- **The two security Criticals do not freeze with the rest, so they are being fixed before the declaration goes out.** That is the carve-out's own rule and the reason a fix round is running during a stop that otherwise opens none. The first is a throw bypassing the rollback built last round: `persist` parses and writes the store with no try, so an exception leaves the advanced reading in place with nothing submitted and the next comparison finds nothing moved. The second is the fourth suppression path this section has met.
- **A consult ruled on the second Critical and threw out both options this session had prepared.** The finding is that a memo whose stored class already equals the truth silences that key for good, with no window consulted and no expiry, and every field of a silencing memo is a value the watcher itself legitimately produces, so no parse-time check can refuse it. This session was going to ask the operator to choose between re-reporting unhealthy personas at each steward start and accepting the residual. The consultant ruled that the operator had already decided it twice, in the Goal sentence requiring the steward to report a held persona and in the standing decision that no guard may rest on who wrote a file, and that those two together leave no version of the accept option available. What remained was a record duty rather than a decision.
- **The ruling's substance is better than either option and is adopted.** Stop persisting the health map across steward sessions, hold it in session memory, and let the existing first-reading path run at every start. The reasoning: persistence is the whole attack surface, every bound-the-memo variant fails against an adversary who controls relaunch timing, and there is nowhere on this box a persona cannot write. Re-reporting at start would still have read the previous class out of a seeded memo, so a persona that was always healthy could plant one and make the steward print a transition that never happened. Only removing the stored value closes the class.
- **Four facts the ruling rests on were verified here rather than taken from the report.** Two roster entries share the steward's working directory. Every roster entry runs in bypass permission mode, so no path on this machine is beyond a persona's reach. The README already names the trust boundary as any plugin-loaded process on this machine, in two places. And the validator the ruling deletes has exactly one reader.
- **This reverses a Major from round 2 and narrows an Operator Verification item, both declared rather than asked.** Round 2 added the persistence to satisfy a reviewer citing Operator Verification item 3. That reviewer was right about the mechanism and wrong about the cost, and the cost is now known to be a silenceable watcher. Item 3's clause that the steward reports nothing further until a persona's state changes now holds within one steward run rather than across a relaunch. The price is that the steward restates each currently unhealthy persona once when it starts, and that a recovery happening while the steward was down goes unreported. Reversal is a one-field change back to persisting the reading, at the cost of reopening the hole.
- **The rule the four rounds converged on is being written down rather than instanced a fifth time.** Nothing the watcher reads from a file may decide silence about an unhealthy persona, and a file-supplied fact may add a line and never remove one. Three rounds each closed one instance of that rule by folding a borrowable fact into the compared value, and a fourth instance arrived each time. The rule goes into the README as a present-tense property of the design.
- **A fix round was stopped and re-dispatched rather than briefed around.** Its brief told it to harden the memo key validation, which the adopted ruling deletes outright. An agent faithfully executing an invalidated contract is killed and re-dispatched, so it was, at its baseline step with the tree untouched and nothing lost. Its claim on the box carried this session's id and was released.
- **The expert ask went out and is unanswered.** It asked the repository's live Expert seat whether a recorded decision already exists about re-reporting at relaunch, and whether the shared working directory has been settled elsewhere. It never gated: the consult ran beside it and the ruling stands on its own evidence. A late answer is recorded beside the ruling rather than reopening it.
- **One review round is owed and unrun, and it is named here rather than only in the declaration.** The repair now in flight carries a delta that reaches a surface the security trigger names, so it owes a round under the fix-delta bar. The backstop opens none. That round is taken on the re-arm, before anything else in this section.
- **Gate at this point:** the whole gate ran on the merged tree before this round, sixteen suites, every exit code 0 read from its own unpiped invocation on a box whose claim file was empty on arrival and whose fleet the operator had stood down. That reading covers the merge and the state the section stood at when round 5 was dispatched. It does not cover the repair now in flight, which re-gates when it lands.
- **Live dispatches:** one, the re-dispatched fix round, building the health map's move out of persisted state, the throw-path rollback, and three small findings. All three round 5 reviewers and the consultant have returned and are closed.
- **Rulings adopted since the last boundary:** one, the consultant's, on the fourth suppression path. No scope adjudicator was convened and no design stop fired: every round 5 finding is a defect against the section's own stated intent rather than a question about what the section may contain.
- **Next action.** Take the repair's report, re-run the six lanes, then declare the backstop with its phase analysis and this board's owed round named as owed-and-unrun. The section does not close and Sections 5 and 4 do not start until the operator answers.

### Interim board 20 - 2026-09-18

Written with the consult's repair landed and re-gated here, and the review-round backstop's declaration going out.

- **The repair is built, verified against the code here rather than accepted from its report, and committed as `898b4a8`.** All five items of the adopted ruling hold. The reading now lives on the session object and is written to no file, confirmed at `hooks/index.ts:353` where the field is declared and `:2444` and `:2679` where it is read and advanced. `parseState` drops any `fleetHealth` key the store carries, unconditionally, and the memo shape check, its text ceiling and the class validator are gone, confirmed by reading the diff of `hooks/agent-state.ts`. `persistOrRollBack` at `hooks/index.ts:1423` runs the same rollback on a throw and rethrows. `dropDecision` at `:1438` takes the audit line back out on every non-submitting path, matched by identity because `persist` pushes decisions of its own in that window. And a roster parse failure's message now rides a `> ` carried line rather than a composed one.
- **The false comment that was round 4's worst finding is re-derived rather than patched.** The guard at the submit no longer claims that a false return means nothing reached the store. It now says what is true: the lost-claim path writes the state as it gives the persona up, so the rollback goes with the call rather than after it, and an exception unwinds past every line below the call for the same reason. Read and confirmed at `hooks/index.ts:2716-2733`.
- **One defect in the repair's own README edit was found here and fixed.** Its new sentence about the parse failure's message was joined to the next with a lowercase "and" after a full stop. That is a claim finding on a published contract surface that contradicts no acceptance bullet and leaves no pointer aimed at nothing, so it rates below the behavior bar and was fixed in place rather than routed. A sweep for live references to the three deleted symbols returns hits only under the gitignored scratch path, which are earlier fix rounds' file copies rather than code.
- **Gate, six lanes re-run in the main session under this session's own box claim, claims directories on both machines empty on arrival, each exit code read from its own unpiped invocation:** the controller tick suite exit 0 at 1171 checks against a baseline of 1165, the settings key suite exit 0 at 134 distinct and unchanged, the channel reply suite exit 0 at 119 distinct and unchanged, the fleet status unit suite exit 0 at 132 and unchanged, `npx tsc -p tsconfig.json` exit 0 with zero bytes, and the loader-rule check exit 0. The tick suite's net rise of six is 54 checks retired and 60 added: the retired ones are the memo-validation pins and the state reads of a reading no longer written, which the ruling deletes the subject of. The counting predicate itself was corrected here after a first read returned zero on every lane, the suites printing their `OK:` lines indented.
- **The implementer's own concern is carried rather than closed, and it rides the owed round.** Removing the stored reading removed the silent branch's `persist` call with it, that call's only job having been writing the advanced map to disk. So a tick where the reading moves but nothing is reported now performs no store write, and a rival's commons claim is noticed at a later persist rather than on that tick. That is a timing change in arbitration detection rather than a correctness loss, since the reading it protected no longer exists, and the guarded persist before a submit is untouched. It is put to the owed round as an unrated question.
- **The ladder stage and the round count as restarted.** Section 6 has taken five review rounds and this is the backstop's first firing on this section. The opening bound is reached; the round count as restarted is 0 rounds taken past the bound. A continue buys three further rounds, and each adjudication from the third of those onward declares again.
- **Owed and unrun, written here so the re-arm finds it rather than only in the declaration.** One review round over the repair's delta (`bdfb5df..898b4a8`), owed under the fix-delta bar because that delta reaches a surface the security trigger names. The backstop opens no round, so it is taken on the re-arm before anything else in this section.
- **The pre-BLOCKED instruments both ran.** The expert ask went to the repository's live Expert seat and is still unanswered; it never gated, and a late answer is recorded beside the ruling rather than reopening it. The consultant's ruling from board 19 covers the mechanism this declaration's phase analysis names, so it stands as the pre-BLOCKED consult and no second one was dispatched. The journal entry `kit.review.cap fail` is logged with the provenance totals.
- **Live dispatches:** none. The repair has returned and its box claim, which carried this session's id, was released by the agent itself; this session wrote and holds its own claim for the six lanes above.
- **Rulings adopted since the last boundary:** none new. Board 19's consultant ruling is the one in force and this board records its construction rather than a fresh adoption.
- **Next action.** The declaration goes out and the section holds. On a continue: take the owed round over `bdfb5df..898b4a8`, then the Minor close pass, then the Chapter, which owes five things: the amendment that the block sits above the inbox drain rather than below it, the three backlog rulings with their reasons, the gate policy named for every deferred run, the roster-path Major routed to Section 5, and the Operator Verification item 3 narrowing with its reversal cost. Then Sections 5 and 4 in that order, with Section 4's live acceptance deferred under the gate policy.


### Interim board 21 - 2026-09-18

Written with the plan re-armed in a new session, Section 6's owed review round run and adjudicated, a consult dispatched on its two hardest findings, and no fix round started.

- **The run resumed under a new session and a re-armed leash.** The session that declared board 20's backstop ended, and the leash stayed bound to it, so this session could not take the plan up until the operator re-armed. They answered the declaration with option (A), continue, and relayed the arming invocation on this session's channel. This run made the CLI invocation itself, which is what the self-armed flag records; the operator's authorization of the plan is unchanged. The queue is unchanged at five plans with this one first.
- **The owed round's range was narrowed, and the narrowing is a correction rather than a preference.** Board 20 named the round over `bdfb5df..898b4a8`. That range straddles the merge of main at `32062c1`, which carries another effort's already-merged supervisor poll extraction into it: 1432 insertions across the range against 821 in the repair itself, including two files the repair never touched. The round therefore ran over `741be86..898b4a8`, which is the repair commit's own diff and the whole of what the consult ordered. This is board 10's trap arriving a second time on the same branch, and the range is stated here so a third reader does not re-derive it.
- **The round is the re-raised roster rather than a decayed one.** Round 5's two security Criticals survived adjudication, which re-raises the round after them, so round 6 ran round 1's three lenses at Fable rather than the single lens a later round would take. All three were confirmed to be running at their assigned tier before anything was read from them: 12, 13 and 14 assistant turns respectively, every one `claude-fable-5-1`, no substitution and no synthetic placeholder.
- **Round 6: no Critical, three Majors, ten Minors.** The adversarial and blind lenses returned CHANGES_REQUIRED, the security lens CONCERNS. The tree-state bracket taken before the dispatch and again at the return was identical, so no agent wrote to the tree. Every Major below was confirmed against the code here before anything was briefed. The three lenses' Major texts and this session's verification of each sit at `.kit/scratch/steward-architect/section-6/round6-findings.md`.
- **Two of the three Majors are one channel in two forms, and the channel is the steward's own persona state store.** That store is a file inside a persona's working directory, and the live roster gives two personas the same one. The adversarial lens found the `[FLEET]` submit still gated on a write to it: a persona that keeps the file unparseable makes the write throw on every tick, the rollback fires, the submit never runs, and nothing reaches the operator about any persona for as long as the file stays broken. The security lens found the second form: a persona that writes a mismatched `epoch` under the steward's key makes `shouldYield` fire, the steward gives the persona up, every later tick returns at its owner check, and the promotion path cannot recover it because the heartbeat still names the steward's own session. Confirmed at `hooks/agent-state.ts:824-833`, `hooks/index.ts:2326` and `hooks/index.ts:2730`. Both paths contradict the sentence this very commit shipped to the README, that nothing the watcher reads out of a file may decide silence about an unhealthy persona.
- **The third Major is a plain in-section defect and is not in dispute.** The departure line and the re-arrival it pairs with both bypass the quiet window: a departed key is deliberately left out of the advanced reading, so the next tick that sees the name again reads it as never seen and reports unconditionally, and the departure reports unconditionally too. A persona deleting and re-adding its own roster entry on alternate ticks therefore drives one prompt per tick, which is the pile the window exists to bound. Confirmed by reading the block whole at `hooks/index.ts:2653-2668`, whose own comment states the single-departure intent the flap defeats.
- **No design stop fires and the backstop does not declare.** Of the three Majors, two are security findings at Major weight, which the carve-out keeps out of the design stop's held unit and out of its count. The third sits in a mechanism round 5 raised no Major in, so no pair of consecutive rounds of fix-introduced Majors in one mechanism has formed. On the ladder, board 20's declaration was answered with a continue, which bought three further rounds; this is the first of them, so rounds 6 and 7 do not declare and round 8 would.
- **A consult is out on the two store findings, because the remedies differ by an order of magnitude in blast radius.** The question put to it: what this section fixes, what it declares, and what leaves the plan. Three candidates were named and this session's lean was labelled as an instinct to test, that the throw path is this section's to fix while the epoch path is seat theft that predates the plan and reaches every persona, so the honest remedy there is to narrow the README claim rather than to change arbitration semantics nothing in the Goal asks about. The constraint it was told it cannot waive: a security Major is never parked, so if the epoch path leaves the plan it goes to the operator as a decision rather than to a backlog file.
- **The recurrence rule fired and its remedy is a sixth Standing Brief Amendment rather than a fifth instance fix.** The class is that the producers of a guarded channel are a set, and it has now arrived at four consecutive rounds of this section on four different producers. The amendment is written above `## Sections of Work` and is approval drift recorded here.
- **Gate: none run this turn, and none was owed.** No file under test changed: the only edits are this plan document's. The baseline in force is board 20's, measured 2026-09-18 on this worktree under this session's own box claim with both machines' claims directories empty on arrival: the controller tick suite exit 0 at 1171 checks, the settings-key suite exit 0 at 134 distinct, the channel-reply suite exit 0 at 119 distinct, the fleet-status unit suite exit 0 at 132, `npx tsc -p tsconfig.json` exit 0 with zero bytes, and the loader-rule check exit 0. The fix round this section now owes re-gates against those figures.
- **One coverage gap in the round, named rather than left implicit.** The blind lens reported that its own charter bars it from reading under `.kit/`, so it skipped `.kit/controller-tick-test.mjs` and its findings say nothing about the 758 changed lines of that file. The adversarial and security lenses both read the test file's assertion names, and the adversarial lens read one new case by name, so the file is not unreviewed; it is reviewed by two lenses rather than three.
- **Live dispatches:** one, the consultant on the two store findings. All three round 6 reviewers have returned and are closed.
- **Rulings adopted since the last boundary:** none. The consult is in flight and no scope adjudicator was convened, the two disputed Majors being questions about what the section may contain rather than findings a judge rules on for scope alone.
- **Next action.** Take the consult's ruling, check its grounds against the code here rather than adopting it from the report, then brief one fix round carrying the adopted remedy, the departure-flap fix, and the ten Minors. That delta reaches a security surface, so it owes round 7 under the fix-delta bar. Then the Minor close pass and Section 6's Chapter, which owes five things: the amendment that the block sits above the inbox drain rather than below it, the three findings ruled to the backlog with their reasons, the gate policy named for every run it defers, the roster-path Major routed to Section 5, and the Operator Verification item 3 narrowing with its reversal cost. Then Sections 5 and 4 in that order, with Section 4's live acceptance deferred under the gate policy.

### Interim board 22 - 2026-09-18

Written with the consult's ruling adopted and checked, Section 6's fix round over review round 6 shipped and pushed, and review round 7 in flight. No section closed.

- **The consult ruled and its grounds were checked here rather than adopted from the report.** Its conclusion holds and two pieces of its evidence do not. Confirmed against the code: there is no `process.exit` anywhere under `hooks/`, with the same grep returning three hits in `bin/` as its control; the ownership arbitration landed in `d8586dd` on 2026-09-07 against this plan's creation in `cf9e903` on 2026-09-15; `yieldNow` at `hooks/index.ts:534-548` leaves the process alive; `persist`'s parse at `:1348` precedes its yield check at `:1357`; the controller tick returns at `:2417` on a lost seat; and the live roster homes `coordinator` and `dev` both at `D:/agent_persona` under bypass permissions. The two errors: the ruling put the controller tick at 300 seconds against a 30-second heartbeat, where the code default is 30 seconds at `hooks/index.ts:1616` and the live roster sets the coordinator to 60, so the ratio it reasoned from does not exist; and it attributed the heartbeat yield to `2ef521d`, which a `-S` search over that symbol does not reproduce. Neither touches the conclusion, which rests on where the yield check sits rather than on how often each tick runs.
- **The ruling's scope call is adopted.** The epoch path is not this section's and cannot be closed by this plugin, because a party able to write the steward's state store can equally stop the process or leave a keeper hold marker. It left the plan as an operator decision rather than a backlog note, which is what a security Major at this weight is owed. The fork went to the operator on the relay channel with three options and a recommendation of homing the steward in its own directory now and taking the yield contract as its own plan later. It blocks Section 5 and nothing before it.
- **One adopted remedy was changed on evidence the consult did not hold.** The ruling ended its fix by rethrowing after the submit, so a broken store still failed the tick visibly. Round 6's third security Minor had flagged that nobody knew what a rejected interval callback does to the harness, and the delta's own test pinned the rethrow as intended without that being settled. It is settled now: `TimerCall` is typed `(ms: number, fn: () => void) => Timer` at `.claude/types/claude-code.d.ts:6124`, so the host does not await the callback and a rejection cannot reach the timer. What it becomes instead is an unhandled promise rejection whose consequence belongs to the host process and is not readable from here. The fix therefore swallows and logs, which is also how the heartbeat tick already treats this same store failure at `hooks/index.ts:2283`.
- **The fix round shipped at `1ec8772` and is pushed.** It closes both in-section Majors and two Minors upgraded to ride with them: the fleet submit and the reconcile submit now report on a store throw rather than going silent, `session.start` comes up on a default state and says the store was unreadable, departure is compared as a class so a roster flap is held by the quiet window, and `persist` no longer returns true after the commons branch gave the persona up. The README paragraph that claimed none of this could happen is replaced, and two further sentences in the same section that this delta made false are corrected.
- **One change is wider than the four items and is named here because it widens the section.** Submitting on a throw at both blocks did not stop the tick rejecting, because every write in the tick reads the same store first and the cost summary's write throws as readily; the implementer captured the stack that proves it. The tick body is now held in a named function and registered inside a catch that logs through `$.ui.log`. It folds under the out-of-scope route's three conditions: the file is already in scope, it needs no acceptance criterion the section does not carry, and the gate that ran covers it. What it costs is that a genuine bug anywhere in the controller tick becomes a logged line, which is weighed against a rejection that reaches no caller at all. The section's `Files in scope:` line is unchanged, the file having been listed from the start.
- **Two findings the round's author raised are recorded rather than fixed.** Departed keys now stay in the session reading for the life of the session, so a persona writing a stream of distinct roster names grows that map without bound; prompt volume is unchanged and the same party already holds cheaper attacks inside the trust boundary the README draws, so this is left with the reason rather than capped. And the turn-open branch still writes through a bare `persist`, where a broken store now ends the tick under the wrapper with nothing lost, that branch having already rolled its reading back. Both were handed to round 7 as things to judge independently rather than as settled.
- **The Minor list exists now and should have from round 1.** Twelve Minors were printed across round 6's three lenses and two are cross-lens duplicates, so ten distinct, which is the count board 21 recorded. They are at `.kit/scratch/steward-architect/minors-section-6.md` with the two upgraded into the fix round marked and the reason stated for each. The close pass reads that file.
- **Gate, run in this session rather than read from the implementer's report.** `npx tsc -p tsconfig.json` exit 0 with zero bytes of output, and `node .kit/controller-tick-test.mjs` exit 0 at 1203 OK and 0 FAIL, both measured 2026-09-18 on this worktree at `1ec8772` with the tree otherwise clean, under this session's own box claim, written and released with the claims directory empty on arrival and on departure. The process poll before the claim found no foreign test runner: the node processes on the box are the Discord relay, the sidecar daemon, the statusline widget and a kit hook, read with command lines attached. Against board 21's baseline of exit 0 at 1171 checks, that is thirty-two checks added and none lost. The new baseline for this section is exit 0 at 1203.
- **The em dash sweep over the three changed files matched nothing, against a control that matched one.** The predicate was `grep -c` for the em dash character over `hooks/index.ts`, `README.md` and `.kit/controller-tick-test.mjs`; the control was a scratch file holding one em dash, against which the same predicate returned 1.
- **Live dispatches:** one, review round 7, the adversarial lens alone over `8ce7493..1ec8772` at opus and `high` effort through the Workflow route, that being an effort the Agent tool cannot set. Round 6 returned no Critical, so this round decays to one lens; the security lens joins no decayed round and its read of this delta is the finishing security pass. The range was confirmed to be exactly the three code files, the commits between the repair and the fix carrying plan documents alone.
- **Where the ladder stands.** Board 20's backstop declaration was answered with a continue, which bought three rounds. Round 6 was the first and round 7 is the second, so neither declares; round 8's adjudication would. The design stop has not fired: round 6's three Majors were two security findings the carve-out keeps out of the count and one in a mechanism round 5 raised no Major in.
- **Rulings adopted since the last boundary:** the consult's, in full on scope and with the rethrow replaced by a swallow on the evidence above. No scope adjudicator was convened.
- **Next action.** Take round 7's findings, capture the section delta at its return as `fix-round-7.diff` before adjudicating, and read every Critical and Major against the code here before briefing anything. Then the Minor close pass over the eight remaining in the list file, which includes correcting Standing Brief Amendment 2's wording, then Section 6's Chapter. That Chapter owes six things: the amendment that the block sits above the inbox drain rather than below it, the three findings ruled to the backlog with their reasons, the gate policy named for every run it defers, the roster-path Major routed to Section 5, the Operator Verification item 3 narrowing with its reversal cost, and the tick wrapper named as a fold. Then Section 5, which waits on the operator's answer to the ownership fork, and Section 4 after it with its live acceptance deferred under the gate policy.

### Interim board 23 - 2026-09-18

Written with Section 6's review round 7 adjudicated, a design stop fired and its ask out to the repository's Expert seat, and a fix round in flight over everything the held mechanism does not touch. No section closed.

- **Round 7 returned one Critical, five Majors and five Minors, one adversarial lens at opus and high effort through the Workflow route.** Its first-turn reading was taken and resolved before anything was read from it: 22 non-synthetic assistant lines, every one `claude-opus-5`, no substitution and no synthetic placeholder. The section delta was captured to `.kit/scratch/steward-architect/section-6/fix-round-7.diff` at its return and before adjudication, 792 lines over the seven files in scope, every one of them tracked so no untracked path needed appending. That capture is the first this section has taken, the earlier rounds having taken none, so the convention it sets is the round's own base rather than the section base and a later provenance read compares against it on those terms.
- **The Critical is confirmed end to end on this session's own surface, and it is the suppression class arriving inside the fix that closed its fourth instance.** On an unreadable store the session-start fallback leaves `existing` at `{}`, so `existingPersona` is undefined and control reaches the `else` at `hooks/index.ts:2234-2244`: `createDefaultState`, `isOwner = true`, a `persona_create` decision, and no `writeClaimDirect`. The claim branch at `:2205-2220` does call it at `:2220`. Line `:2285` then stamps the owner heartbeat because `isOwner` is true. When the store parses again the heartbeat tick at `:2339` finds `shouldYield` true on the `activeSessionId` mismatch alone (`hooks/agent-state.ts:824-833`) and `yieldNow` at `:534-548` sets `isOwner` false without clearing the heartbeat sidecar. So at the promotion branch `:2369-2381` the holder is this very session, `holderIsSelf` is permanently true, `holderIsStale && !holderIsSelf` can never fire, and `controllerTick` returns at its step-1 owner check for the life of the process. No `[FLEET]` and no `[RECONCILE]` ever go out and `startStoreProblem` is never drained, while the process keeper reads a live process. A transient read failure at start is enough.
- **A design stop fired, and it was not in the reviewer's report.** Round 6's departure Major and round 7's unbounded-growth Major are consecutive fix-introduced Majors in one mechanism, the handling of a roster key that has departed from the fleet health map. The capture is what established it: its lines 412 to 415 show the round-6 fix rewriting that handling from the departed key being left out of the reading to it being held in the reading as a class, so round 7's finding sits in lines round 6's fix wrote. Both trace to the same acceptance bullet, that a second tick with no further change submits nothing more. Neither is a Critical and neither is a security finding, so the carve-out excludes neither and the pair stands.
- **The held finding's premise was checked here rather than taken from the report.** `personaNameProblem` at `hooks/operator.ts:142-146` refuses a non-string, an empty string, a name carrying a colon and a bracket-unsafe name, and imposes no length bound at all, read from the function's whole body. So a roster-supplied key is unbounded in size as well as in count, which is a stronger claim than the one this session recorded at board 22 and left with its reason. That earlier disposition is superseded by the stop rather than standing beside it.
- **The ask went to the repository's live Expert seat, which is the judge the rule names while that seat is up.** `AP: Expert` is on the roster and idle. The ask carries the mechanism named in plain words, the round indices, the capture range and the trace target, and deliberately carries no account of what happened inside the rounds and no lean. It never gates: the fix round below was dispatched beside it. If nothing has arrived by the time the section's other work is ready for its close gate, it falls to a scope adjudicator and the seat's later answer is recorded beside whatever that rules.
- **The fix round in flight carries everything the held mechanism does not touch,** and the brief tells it outright that the departed-key mechanism is held and not to be touched even if a fix appears to need it. Six items: the Critical, the `null` store that still aborts `session.start` because `hooks/index.ts:2166` sits outside the new try while `JSON.parse("null")` throws nothing, the store-failure decision line left standing when the submit that follows fails, three bare persists at `:2791`, `:2915` and `:2937` that can end a tick early on the very path the wrapper exists for, a tick failure that reaches nobody, and one Minor upgraded because it is why the Critical was green in the suite.
- **The Critical's remedy was deliberately left open to the implementer.** Two candidates were named with their evidence, taking the claim on the fallback path or not coming up as owner at all, and the brief says which cost this session can see in each and asks for the choice and its reason back. The reviewer offered both; neither was adopted here, because whether `writeClaimDirect` can succeed against the store that just failed to read is a fact this session has not established.
- **One Major's premise is unverified and the brief says so rather than building on it.** The reviewer's fifth Major rests on an unhandled rejection killing the host process having been the signal that got a wedged steward relaunched, and rates itself medium for that reason. Neither the reviewer nor this session can confirm what this host does with an unhandled rejection. So the brief refuses that premise and asks only for the cheap honest part, carrying a tick failure to the operator through the notes channel the fleet block already has, in the shape `startStoreProblem` already uses, with no counter and no new component.
- **Where the ladder stands, and round 8 is the round that declares.** Board 20's backstop declaration was answered with a continue, which bought rounds 6, 7 and 8. Round 7 is the second, so it does not declare. The fix round's delta reaches a security surface, so it owes round 8 under the fix-delta bar, and round 8 is the third of the three: its adjudication declares again if the terminal condition is still unmet. Round 7's Critical survived adjudication, which re-raises the round after it, so round 8 runs round 1's full roster rather than the single lens a decayed round takes.
- **Gate: none run this turn, and none was owed.** No file under test changed by this session: the only edits are this plan document, the section's Minor list and the capture, all three outside the suites. The baseline in force is board 22's, measured 2026-09-18 on this worktree at `1ec8772` under this session's own box claim with the claims directory empty on arrival and on departure: `node .kit/controller-tick-test.mjs` exit 0 at 1203 OK and 0 FAIL, and `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. The fix round now in flight re-gates against those figures.
- **A tooling fact worth recording, because a later session will hit it too.** `TaskOutput` does not exist in this build. The executing-work skill's in-turn wait loop is written around a blocking `TaskOutput` call, and the deferred-tool index returns `TaskStop`, `SendMessage`, `PushNotification`, `WebSearch`, `CronCreate` and `Monitor` and no task-status tool, searched twice by exact name and by description. So the in-turn hold that skill names cannot be run here and the `WAITING:` park is the only shape available once a dispatch's first-turn reading is resolved.
- **The plan document was re-read whole at this session's compaction rather than resumed from a summary.** All 562 lines including every Chapter, which closes the gap the previous turn declared: Chapters 1 through 3 were being carried as reported and are now confirmed on this session's own surface.
- **Live dispatches:** one, the fix round over round 7 at opus, carrying the Critical, four Majors and one upgraded Minor, with the departed-key mechanism named as held and out of bounds. Round 7's reviewer has returned and its workflow is complete. The Expert seat's ask is out and unanswered.
- **Rulings adopted since the last boundary:** none. The design stop's ask is in flight and no scope adjudicator has been convened, the Expert seat being live and the rule sending it there first.
- **Next action.** Take the fix round's report, verify it against the code here rather than accepting it, re-run the gate lanes in this session rather than reading them from the report, and take the Expert seat's ruling on the held mechanism or fall to a scope adjudicator if the close gate arrives first. Then round 8 over the fix delta at round 1's full roster, whose adjudication declares to the operator if the terminal condition is still unmet. Then the Minor close pass over the eight from round 6 plus round 7's four remaining, which includes correcting Standing Brief Amendment 2's wording, then Section 6's Chapter. That Chapter owes seven things: the amendment that the block sits above the inbox drain rather than below it, the three findings ruled to the backlog with their reasons, the gate policy named for every run it defers, the roster-path Major routed to Section 5, the Operator Verification item 3 narrowing with its reversal cost, the tick wrapper named as a fold, and this design stop with its mechanism, its seat and its ruling. Then Section 5, which waits on the operator's answer to the ownership fork, and Section 4 after it with its live acceptance deferred under the gate policy.

### Interim board 24 - 2026-09-19

Written with Section 6's fix round over review round 7 verified, committed and pushed, and review round 8 returned from all three lenses. No section closed. Round 8 is the third of the three the continue bought and its terminal condition is unmet, so a declaration is owed to the operator and is being prepared with a consult ruling attached.

- **The fix round returned DONE_WITH_CONCERNS and its report was checked against the code rather than adopted.** The delta was captured to `.kit/scratch/steward-architect/section-6/fix-round-8-delta.diff` at its return and before adjudication, 734 lines over three files, and every hunk was read before the commit so no content from a session sharing this tree could ride in.
- **The Critical's remedy was left open to the implementer and both of its reasons for rejecting the alternatives were confirmed here.** `writeClaimDirect` at `hooks/index.ts:555-573` parses the store before it writes, so taking the claim on the session-start fallback path throws against the very store that just failed to read. And `holderIsStale` at `hooks/index.ts:2463` is falsy when the heartbeat sidecar holds no entry for the persona, so refusing to come up as owner would leave a genuinely new persona reporting nothing at all for ever. What shipped is neither: the claim is taken at the first heartbeat read that parses, with the existing commons arbitration deciding against a live rival.
- **Gates were run in this session under its own box claim, with the claims directory empty on arrival and on departure.** Every exit code read from its own unpiped run. `node .kit/controller-tick-test.mjs` exit 0 at 1247 OK and 0 FAIL, against board 22's baseline of 1203 OK and 0 FAIL on the same lane. `npx tsc -p tsconfig.json` exit 0 with zero bytes. `node .kit/check-loader-rule.mjs`, `node .kit/fleet-status-unit-test.mjs` and `bash .kit/settings-plugin-key-test.sh` all exit 0. The process list was polled before the claim was taken and carried no foreign test runner.
- **The red reading was reproduced here without mutating the tree.** HEAD's `hooks/` was extracted by `git archive` into a scratch directory carrying the final test file, and the suite run there returned exit 21 at 1224 OK and 21 failing, every fix item represented. The scratch copy was removed and the tree confirmed to carry only the three expected modified files. The copy route was taken because three reviewers were about to read this tree and a red probe run in place is exclusive of any agent reading it.
- **One defect was found in the delta and fixed here before the commit.** The README sentence counting the non-persona entries the watcher holds was raised from two to three while the sentence after it still read "Both are reported when they move". It now reads "All three". No suite pins that sentence, checked with a control that matched a string those files demonstrably hold.
- **The em dash sweep on the delta returned zero added lines against a control that returned eight,** so the instrument speaks on this diff rather than being silent for the wrong reason.
- **The fix is committed at `239c274` and pushed.** The staged list was read as its own step and held exactly the three target files, the landed file list was read after the commit, and the unpushed list held exactly one commit. `gh pr list --head steward-architect --state all` returned empty, so the push landed on a branch with no pull request open.
- **Round 8 ran round 1's full roster and all three lenses returned.** Adversarial, blind and security at Fable over `5d3628d..239c274`. All three first-turn readings were taken and resolved before anything was read from them: 23, 16 and 26 non-synthetic assistant lines, every one `claude-fable-5-1`, zero synthetic. Verdicts CHANGES_REQUIRED, CHANGES_REQUIRED and CONCERNS.
- **All three lenses independently found the same defect, and it is confirmed here against the code.** The `claimUnpublished` arbitration the fix added lives only in the heartbeat tick at `hooks/index.ts:2397-2420`. `persist` carries the identical `onDisk && shouldYield(...)` test at `hooks/index.ts:1368-1372` and calls `yieldNow` with no knowledge of that flag. So a steward that came up on an unreadable store yields at whichever persist fires first after the store parses again, `isOwner` goes false, the claim block sits behind an owner check that never passes again, the sidecar names this session at the promotion test, and the watcher is silent for the life of the process. That is the identical outcome round 7's Critical named. The adversarial lens adds the reachability that makes it the likely case rather than the edge: `session.start` persists at `:2350` and `turn.start` persists at `:4852` and `:4874`, and the priming prompt opens a turn inside the first heartbeat interval, so on the transient read failure the Critical was premised on the persist is the probable first reader.
- **This is the sixth instance of this section's recurring defect class,** a persona the steward watches finding a way to make the steward stay silent about that persona, and the third consecutive round in which the instance arrived inside the fix that closed the one before it.
- **Two of round 8's findings are one diagnosis, and it is Standing Brief Amendment 6's own class.** A guard closed at the producer that prompted the finding rather than at the channel every producer uses. The yield guard was closed at one of the two callers of the yield decision rather than at the decision. The store shape check was closed at one of four readers that parse that file, the other three being `persist` at `:1358`, `writeClaimDirect` at `:557` and the heartbeat tick at `:2375`. The adversarial lens's concrete input for the second is a store holding an empty array, which every reader accepts, which `persist` then assigns a property onto and serializes back as an empty array, so every write is silently dropped with nothing throwing and no store-refused note ever composing.
- **One finding calls this section's own green into question and is recorded rather than smoothed over.** The three legs of the new refused-write case accept an absence, that the tick did not end early, and no leg asserts that the refusal it configured actually fired. The helper exposes no flag and the third leg never checks its counter reached two. So if the branch under test never wrote at all, each leg is green on precisely the failure it exists to exclude. That is Standing Brief Amendment 5's class. The trailing control proves the instrument logs rather than that each leg's write was reached. The 1247 figure above is therefore a true count of checks that passed and a weaker claim about coverage than it looks, and the correction belongs in the next fix round.
- **Findings carried but not yet adjudicated,** pending the declaration: the claim block's own catch falling through to a yield, so two transient failures in a row become permanent silence; the claim publish writing a default state over a healed store that carries the predecessor's memory, goals and decisions; a commons store that throws ending the tick before the new tick-failure line can be composed; a non-numeric epoch in the persona-writable store producing NaN through the max; an unsliced decision detail interpolating an attacker-controlled session id; and the tick-failure flag being cleared by a tick that returned early rather than one that ran.
- **The security lens confirmed the two surfaces this delta was asked about and cleared both.** The new carried line is bounded and neutralized the way its siblings are, built from `safeErrorText` and `boundedText` and neutralized again at the splice. Nothing in the delta grows without bound: the tick-failure slot is one value overwritten per tick and its compared memo sits under the existing six-class window cap. It also confirmed independently that `bracketSafeProblem` refuses whitespace, so no roster persona can collide with the new key, which board 23 recorded from the same read.
- **A tooling correction, because it produced a false reading in this session before it was caught.** The `.output` path a task notification advertises is a zero-byte placeholder for a local agent dispatch, never written after dispatch. A first-turn tally taken against it returns zero lines and zero synthetic for a healthy agent, which is exactly the never-started shape that authorizes a kill, and all three of round 8's lenses read that way at first. The real artifact is `~/.claude/projects/<slug>/<parent-session-id>/subagents/agent-<task-id>.jsonl`, which is where the readings above were taken. An operator-tier memory already held this fact and the recognition nudge surfaced it.
- **Live dispatches:** the consult on this section's framing, and the ask to the repository's Expert seat. The three round 8 lenses and the fix round have all returned.
- **Rulings adopted since the last boundary:** none. The Expert seat has not answered the design stop's ask sent at board 23, and that ask still has its window because the section's close gate has not arrived.
- **Next action.** Take the consult's ruling, check it against the code here rather than adopting it, and declare to the operator with the ruling attached, because round 8 is the third of the three the continue bought and its terminal condition is unmet. The declaration's fork is whether this section keeps buying rounds or changes how it is being fixed. Whatever the answer, the fix round after it carries the findings listed above plus the test-leg correction, and it is briefed to close each at the channel rather than at the producer, which is the diagnosis two of round 8's findings share.

### Interim board 25 - 2026-09-19

Written with the consult ruled and the declaration to the operator going out. No section closed. Section 6's close now turns on the ownership fork board 22 sent and the operator has not yet answered.

- **The consult ruled option 3: the round ladder is a symptom and the section's premise is what the rounds keep exposing.** It ruled against this session's own lean, which was another fix round briefed to close at the channel. Its ruling is that the defect the last three rounds chased is reachable only because two personas share one working directory and therefore one state store file, and that the question governing it was already asked at board 22 and scoped there as blocking Section 5 alone.
- **The consult corrected a claim this session made in its own brief, and the correction is confirmed here.** The brief said four call sites parse the persona store. There are nine, at `hooks/index.ts` lines 558, 1359, 1411, 2183, 2375, 2489, 2517, 5412 and 5446, read by grep over `JSON.parse` filtered to the store path, against a control of 27 total parse sites in the file, so the filter is doing work rather than matching everything. That undercount is itself Standing Brief Amendment 6's class arriving in the brief before any fixer had touched it, which the consult named as its strongest evidence that a round 9 as conceived would ship instance seven.
- **The live trigger is confirmed on this session's own read of machine state.** `D:/personas/fleet.json` homes both `coordinator` and `dev` at `D:/agent_persona`, both under bypassed permissions. The store is `<workdir>/.agentic-personas.json`, so the two personas write one file. The consult's further claim, that every writer of the store parses it first and so the steward's own process can never heal an unparseable store, means the transient read failure both round 7's and round 8's Criticals are premised on requires a second writer to exist at all.
- **What the ruling makes of the current code under a single-writer deployment.** The session comes up as owner on a default state, no read ever parses so the heartbeat tick never yields, the controller tick runs, and the fleet report goes out carrying the store-refused line. That is degraded but audible, which is the opposite of the silence the rounds have been chasing. The consult marked this medium confidence because it traced the behaviour by reading rather than by running a session against a truncated store.
- **The declaration to the operator carries this ruling rather than a bare ask.** Round 8 was the third of the three the continue bought and its terminal condition is unmet, so the backstop declares. What it declares is not a request for a fourth round: it is the board 22 ownership fork re-sent as the one question, now named as governing Section 6's close rather than Section 5's alone.
- **The route the ruling lays out if the answer is a separate working directory.** Section 6's remaining delta is the three test legs that accept an absence without asserting their condition fired, plus a README sentence writing the single-writer premise into the trust boundary as a present-tense property, plus one decayed review round over that delta, and then the close. The arbitration hardening, meaning one shape-checked store reader across the nine parse sites and one home for the ownership decision across the yield sites, goes to its own plan with round 8's finding as its first item, paired with the yield-contract plan board 22 already proposed and the write-race backlog entry board 16 filed.
- **The route if the answer is that the steward stays beside `dev`.** Section 6 absorbs that restructuring before it can close, its scope is formally widened from the fleet wake to the ownership arbitration, and every persona's start-up path changes under a section whose goal is the wake. The consult named this as the alternative rather than as an equal option, and this session agrees on the evidence above.
- **The `claimUnpublished` block stays in the tree under either answer.** Its one live trigger under a single-writer deployment is an operator hand-repairing the store while the steward runs, and the safe operator move after such a repair is to restart the persona. The correct in-code answer belongs to the arbitration plan rather than to this section.
- **The security Major from round 8 is routed as an operator decision rather than as a backlog line,** which is the bound this plan set for itself. This declaration is that decision ask.
- **Live dispatches:** none. All three round 8 lenses, the fix round and the consult have returned. The ask to the repository's Expert seat on the departed-key design stop is still unanswered, and a second ask went to it on whether this codebase ever had a single home for the yield decision. Neither gates.
- **Rulings adopted since the last boundary:** the consult's, adopted after its two load-bearing facts were confirmed here against the code and against machine state rather than taken from its report.
- **Next action.** The declaration is out. On an answer of a separate working directory, run the three-item close the ruling lays out and open the arbitration plan. On an answer of staying beside `dev`, rewrite Section 6's scope in the plan before any further round. Either way the departed-key design stop still needs the Expert seat's ruling or a scope adjudicator in its place before the section closes.

### Interim board 26 - 2026-09-19

Written at a resume under a re-armed leash, with the operator's departed-key answer recorded and the removal dispatched. No section closed. Section 6 is the only section in flight.

- **The run resumed under a new session and a re-armed leash.** The session that held the leash died mid-run on an authentication expiry, not on a decision: its last transcript line is `Failed to authenticate: OAuth session expired and could not be refreshed`, read from its own transcript. The operator typed the arming invocation naming all five plans on this session's relay thread, which rebound the leash here. The queue is unchanged at five plans with this one first, and the goal state records the arming as the operator's. The persona store at `.agentic-personas.json` names this session as `dev`'s `activeSessionId`, which is what establishes the predecessor no longer holds the persona rather than any reading of a transcript's modification time.
- **The operator answered the departed-key fork: option (A), remove it.** Recorded under Open Questions with their words and the cost they accepted. That answer also disposes of the departed-key design stop, which board 25 left needing the Expert seat's ruling or a scope adjudicator in its place: the mechanism the stop held is now removed on the operator's own decision, which is the authority the stop was escalating toward, so no judge is convened for it.
- **The predecessor died parked on a dispatched Minor close pass, and that agent died with it.** The pass was working Section 6's twelve remaining findings. Its partial work is in the worktree unstaged and unproven: comment corrections and a `reconcileStoreProblem` carried line in `hooks/index.ts`, three refusal-reading assertions in `.kit/controller-tick-test.mjs`, a single-writer paragraph in `README.md`, and a backing-off clause change in `bin/supervise.sh`. The whole delta was captured to `.kit/scratch/steward-architect/section-6/pre-removal-inherited.diff` before anything was dispatched, with the porcelain status beside it, so the removal's own hunks are attributable against it. None of it has been gated in this session and none of its figures are this session's.
- **The 1250 baseline this section resumes against is reported rather than confirmed.** The predecessor recorded it from its own reading of a returned diff rather than from a run it made, and said so. It rides into the removal's brief marked that way, and the removal's own run is what settles it.
- **The box is held by another repository.** `C:/Users/LocalAdmin/.claude/coordinator/SCOTT-CLAUDE/claims/heavy-process.md` carries a live claim: seat `KIT: Worker`, repo `D:/claude-kit`, `node --test test/*.test.js`, expected 1200 seconds. A command-line process poll confirms that runner live. No suite was started beside it, and the removal's brief carries the read half and the wait instruction.
- **Live dispatches:** one, the departed-key removal at opus over `hooks/index.ts`, `.kit/controller-tick-test.mjs` and `README.md`, briefed to leave the predecessor's inherited hunks untouched and to wait the box out before gating.
- **Rulings adopted since the last boundary:** none. The two asks to the repository's Expert seat that board 25 left open are still unanswered, and the departed-key one is moot on the operator's answer.
- **Gate baseline in force:** the reported one above, `node .kit/controller-tick-test.mjs` exit 0 at 1250 OK and 0 FAIL, measured by a dead session on this worktree at `7074f32` with the inherited delta applied. Nothing was run in this session.
- **Next action.** Take the removal's report, check its greps and its exit codes here rather than adopting them, then run the owed review round over the combined delta, the inherited hunks included, since no round has ever read those. Then the Minor close pass the predecessor never finished, then Section 6's Chapter.

### Interim board 27 - 2026-09-19

Written with the removal verified and committed at first green and the owed review round dispatched. No section closed. Section 6 is still the only section in flight.

- **The removal returned and its claims were re-measured here rather than adopted.** Every figure below is this session's own unpiped run. `npx tsc -p tsconfig.json` exit 0 with no output. `node .kit/controller-tick-test.mjs` exit 0 at 1233 OK and 0 FAIL. `node .kit/fleet-status-unit-test.mjs` exit 0 at 132 OK and 0 FAIL. `node .kit/check-loader-rule.mjs` exit 0, which a `hooks/index.ts` change earns because the plugin engine's loader rules are judged statically over the whole file and refuse it silently, and neither the type check nor the test harness reads them.
- **The absence check ran against a control that spoke.** The predicate `FLEET_ROSTER_DEPARTED|no longer in the roster` over the three files as they stand at `cac0ea3`, taken by `git show HEAD:` into `.kit/scratch/steward-architect/section-6/control-head/`, matched nine lines: five in `.kit/controller-tick-test.mjs`, three in `hooks/index.ts`, one in `README.md`. The same predicate over the working tree matches nothing, exit 1 read unpiped. The removal's own report put the control at eight lines; the difference is which tree each was measured on and neither reading changes the result.
- **The 1250 baseline is now reconciled rather than still merely reported.** Seventeen `check(` calls left the suite and three arrived with the inherited hunks, counted off the diff. The reported 1250 was measured with those three already applied, so 1250 minus 17 is the 1233 measured here. The arithmetic closes, which is evidence for the reported figure rather than proof of it.
- **The five classes are confirmed at their source.** `FLEET_HEALTH` in `hooks/agent-state.ts` at line 168 holds exactly held, backing off, stale, no live claim while the roster enables it, and healthy, read here rather than taken from the removal's report.
- **The first-green commit landed and pushed as `09457ea`,** carrying four files: `hooks/index.ts`, `.kit/controller-tick-test.mjs`, `README.md` and `bin/supervise.sh`. The staged list was read as its own step and held exactly those four. `git log @{u}..HEAD` named one commit, this session's, before the push, and `gh pr list --head steward-architect --state all` returned an empty array at exit 0, so the branch still carries no pull request in any state.
- **`bin/supervise.sh` is a fold and widens the section.** Section 6's `Files in scope:` line does not name it. Its one inherited hunk is the backing-off clause of the steward's own role instruction, which is this section's subject, and it disposes of the blind lens's round 6 claim Minor at `bin/supervise.sh:2712`. The `Files in scope:` line is updated with the Chapter, and the fold is recorded there as the approval drift it is.
- **The predecessor's Minor close pass got further than board 26 credited it with.** Reading the inherited hunks against `.kit/scratch/steward-architect/minors-section-6.md`, they dispose of the round 6 README test-inventory claim, the round 6 positional-reference claim in `readFleetRows`, the `bin/supervise.sh` duty-text claim, and round 7's M1, M2, M3 and the M4 class of producing-step assertion. The README's replacement clause was checked against the suite and both cases it now names exist and are green. What is left for the close pass is the `agentic_identity` reading not cleared on a persona switch, the `dropDecision` gap, the Standing Brief Amendment 2 wording, the first-tick-on-an-unreadable-roster noise, and the security Minor at the bare `JSON.parse`.
- **Live dispatches:** two, the section's owed review round, run as one round at opus and `high` effort through `Workflow` because the Agent tool on this build takes no effort parameter. The adversarial lens has the spec, the section name, the trace target and the six Standing Brief Amendments. The blind lens has the base ref `cac0ea3` and the changed-file list alone. A blind lens is not owed on a decayed round and was added here because no round has ever read the inherited hunks. Both were confirmed running at `claude-opus-5` with no synthetic turns, so the model override took.
- **The box was claimed and released.** The claim `D:/claude-kit` held at board 26 was gone and the claims directory was empty, with no foreign runner in the process list. This session wrote its own claim, ran the four gates, and deleted it after confirming the `Session:` line was its own.
- **Rulings adopted since the last boundary:** none. The two asks to the repository's Expert seat remain unanswered and neither gates.
- **Gate baseline in force:** `node .kit/controller-tick-test.mjs` exit 0 at 1233 OK and 0 FAIL, measured in this session on this worktree at `09457ea` with a clean tree, at 2026-09-19T12:44Z, with the box uncontended.
- **Next action.** Take the round's findings, adjudicate each for provenance and class before any fix, and re-read the tree-state capture at `.kit/scratch/steward-architect/section-6/round-pre.txt` against the tree when the round returns. Then the Minor close pass over what the list above says is left, then the close gate, then Section 6's Chapter with the fold recorded.

### Interim board 28 - 2026-09-19

Written with the review round returned, its Majors confirmed here and the fix round dispatched. No section closed. Section 6 is still the only section in flight.

- **The round returned three Majors and four Minors, and both lenses converged on two of the Majors independently.** That convergence is itself the signal worth recording: two reviewers landing on one structural claim usually means a shared premise is wrong rather than that the code holds two unrelated defects. The tree at the round's return is byte-identical to the capture taken before dispatch, so no lens wrote into the tree and no finding is suspect on that ground.
- **The first Major is a regression the removal itself introduced, and it is confirmed on this session's own reading of the code.** In `compare` at `hooks/index.ts:2644-2657`, a key with no memo takes `FLEET_UNSEEN` and returns a change unconditionally; the quiet window is consulted only on the branch below, where a memo exists. The departed-key loop was the only thing keeping a departed persona's key in `current`, and the carry-forward at `2726` runs only when the roster could not be read. So on a clean roster read the key now drops out of the reading at `2841`, and a persona that deletes its roster entry and writes it back on alternate ticks drives one `[FLEET]` prompt per return with nothing holding it. Standing Brief Amendment 2 forbids exactly that, because submitted prompts accumulate rather than replacing one another.
- **The operator's accepted cost does not cover this, which is why it is fixed rather than declared.** Their words accepted that a persona vanishing from the roster reaches them nowhere. An unbounded prompt pile on its return is a different cost and was never put to them. The fix keeps the removal intact: nothing about a departure is reported and no sixth class returns. What changes is that a departed key carries forward in the reading, so the departure stays silent and a return is compared against the class the persona was last in, which makes an unchanged return silent and a changed one a flap the window governs like any other.
- **The second Major is a false claim on a published contract surface, also confirmed here.** The Trust boundary paragraph the dead session added to `README.md` asserts that each persona is homed in its own working directory and so one store has one writer. Both `bin/fleet.example.json` and the live `D:/personas/fleet.json` give `coordinator` and `dev` the same `workdir` of `D:/agent_persona`, read here. Two comments in `hooks/index.ts` state the opposite in the same tree. One home per persona is the operator's 2026-09-19 decision and a Section 5 cutover item, so the paragraph writes a future property as a present one. It is rated at the behavior bar under the claim-class rule twice over: it sits on a security boundary and on a published contract surface.
- **The third Major is the inherited `reconcileStoreProblem` mechanism, which no test exercises.** Four paths ship untested, and the one existing reconcile case runs with an empty roster, which is the single configuration where the fleet block never runs and the line never drains. The blind lens also found its clear on the prompt-failure path unconditional where the decision drop beside it is guarded, which discards a line composed on an earlier tick.
- **Provenance, read at adjudication and before any fix existed.** All three are spec-traceable. The first traces to Standing Brief Amendment 2, the second to the Goal's account of what the steward reports and to the amendment naming the five classes, the third to Section 6's acceptance. None is new-requirement, so none is held and no judge is convened. No design stop: that needs two consecutive rounds of fix-introduced Majors in one mechanism and this is one round.
- **Round ladder stage.** This is the first round since the count restarted at the operator's 2026-09-19 working-directory answer, and the stage in force from that answer buys three rounds before the backstop declares again.
- **Live dispatches:** one, the fix round at opus over `hooks/index.ts`, `.kit/controller-tick-test.mjs` and `README.md`, briefed on all three Majors, on the folded wording Minor, and on watching each new case fail before fixing.
- **Rulings adopted since the last boundary:** none.
- **Gate baseline in force:** `node .kit/controller-tick-test.mjs` exit 0 at 1233 OK and 0 FAIL, measured in this session on this worktree at `09457ea` with a clean tree, at 2026-09-19T12:44Z, with the box uncontended.
- **Next action.** Take the fix round's report, re-run the four gates here rather than adopting its figures, and read its new cases for whether they actually fail against the unfixed code. The fix delta owes a round of its own under the fix-delta bar: it adds no module and makes no outward call, but it reaches the prompt-composition path the security trigger names. Then the Minor close pass over the three parked in round 9's list plus what round 6 and round 7 left, then the close gate, then Section 6's Chapter with the `bin/supervise.sh` fold recorded and the `Files in scope:` line corrected.

### Interim board 29 - 2026-09-19

**Section 6 stage.** The fix round for the three Majors returned DONE_WITH_CONCERNS and its work is verified here rather than adopted. The round the fix delta owes is in flight.

**Gates, each exit code read from its own unpiped run in this session.** `npx tsc -p tsconfig.json` exit 0 with no output. `node .kit/controller-tick-test.mjs` exit 0 at 1276 OK and 0 FAIL, against the 1233 measured here at `09457ea`, so +43, which is exactly the implementer's added checks and no pre-existing check moved. `node .kit/fleet-status-unit-test.mjs` exit 0 at 132 OK and 0 FAIL. `node .kit/check-loader-rule.mjs` exit 0. The claims directory was empty before the run, the claim was written with this session's id and deleted after, and no contention was observed.

**The red probe was re-run here.** The pre-probe copy of `hooks/index.ts` went to `.kit/scratch/steward-architect/section-6/probe2/`, `git show HEAD:hooks/index.ts` replaced it, and the suite came back exit 9 at 1267 OK and 9 FAIL: seven in the departed-persona case and two in the reconcile surviving-line case, which is the implementer's own account reproduced. The restore was from the filesystem copy and `diff` confirmed it byte-identical, `git status --porcelain` matched its pre-probe capture, and the re-run came back exit 0. A first count of ten failures was a grep artifact, the runner's own `FAIL: 9 failure(s)` summary line matching the same pattern.

**The concern about an unbounded reading is confirmed pre-existing.** The implementer left `sess.fleetHealth` uncapped and said the removed code had the same property. That holds: the departed-key loop at `cac0ea3:hooks/index.ts:2843` called `compare(key, ...)`, which writes the key into the current reading, so a departed key never left there either. Choosing an eviction rule is a design decision this plan does not make, so it stays out.

**Parked Minors re-confirmed against the code, read-only beside the running round.** The round 6 security Minor at the bare `JSON.parse` in `session.start` is already fixed and is retired with receipts: the parse sits inside a try at `hooks/index.ts:2199-2215` with a shape check that refuses null, an array and a scalar, and the refusal is carried to the first `[FLEET]` prompt. The `agentic_identity` Minor still holds: `sess.persona` is reassigned at `hooks/index.ts:5424` and nothing clears `sess.fleetHealth`, whose every write sits inside the fleet block. R9-M1 still holds: the drain at `hooks/index.ts:2852` sits inside the fleet block's `sess.persona === coordinatorPersona && fleetRoster !== ""` gate while the reconcile block that composes the line is gated on the persona alone. R9-M2 still holds at `.kit/controller-tick-test.mjs:9984`.

**Live dispatch.** One adversarial lens over the fix delta, at opus and `high` effort through `Workflow`, which the reviewer effort table calls for on a later round over an opus writer and which the Agent tool cannot set. Its first-turn reading is resolved at 17 real assistant lines on `claude-opus-5` with none synthetic. The fix-delta capture for provenance is `.kit/scratch/steward-architect/section-6/fix-round-10.diff`. `TaskOutput` is absent from this session, so the in-turn blocking loop is unavailable and the round is awaited on its completion notification.

**Next action.** Adjudicate the round. Then the Minor close pass over the four still standing, then the close gate, then Section 6's Chapter with the `bin/supervise.sh` fold recorded and the `Files in scope:` line corrected.

### Interim board 30 - 2026-09-19

**Section 6 stage.** The round the fix delta owed returned CHANGES_REQUIRED with three Majors and six Minors and no Critical. One adversarial lens at opus and `high` effort through `Workflow`, run `wf_d962d634-01d`. The tree-state capture before the round and after it are identical, so no incident. The round count for the backstop ladder stands at two since the operator's 2026-09-18 continue, which bought three.

**Two Majors are confirmed here and are straightforward.** The first is a claim the fix round's own prose made: both `README.md:474` and the new comment at `hooks/index.ts:2745-2747` say a changed return is a flap the quiet window holds back, and `compare` at `hooks/index.ts:2687-2691` suppresses only a return into a class the window already carries. A move into a class the window does not carry goes out at once, which the README's very next paragraph states correctly and the delta's own new test asserts. Provenance fix-introduced, since the sentence is one this fix round wrote. The second is behavioural: `firstReading` at `hooks/index.ts:2625` reads as false whenever any reading exists, and a steward whose first tick meets an unreadable roster writes the two roster-level keys, so its next clean tick reports every persona as newly seen. Confirmed at that line. This is round 6's parked Minor raised to a Major because the fix round's new comment claims the carry covers the case, so fixing it closes both. Provenance spec-traceable.

**The third Major is the operator's to answer, and the reason is in this plan's own ruling.** The round found that the unconditional carry means `sess.fleetHealth` gains a key for every persona name the roster has ever named and loses none, where the state at `02e35af` rebuilt the reading from the roster's current rows and so was bounded by them. That is confirmed. What makes it more than a tradeoff is the `## Open Questions` entry recording the 2026-09-19 removal decision: it names the declined reversal's costs as "the outstanding fault and the bookkeeping the departed key needs". This fix re-incurs exactly that bookkeeping, for a different benefit than the reversal offered and without any sixth reported class. The operator listed that cost as a reason not to go this way, so they get to weigh it now that it is being paid.

**The scope check the round raised is answered rather than parked.** The re-added carry is not a partial reversal of the removal ruling. `FLEET_ROSTER_DEPARTED` is gone, confirmed by an absence check against a control, no sixth class is reported, and a departure still reaches the operator nowhere, so the accepted cost stands exactly as recorded. What the carry changes is only what a return is compared against, which the operator was never asked about.

**My own read, offered for checking rather than as a conclusion.** Bounded memory, a bounded prompt rate and reporting every genuinely new persona cannot all hold when the adversary picks the names, because any eviction rule hands an evicted name back to `compare`'s no-memo branch, which reports unconditionally. So one of the two resources stays unbounded, and memory at a few hundred bytes per name looks cheaper than prompts, which cost tokens, wake the session and accumulate rather than replacing one another. `readFleetRows` caps no roster entry count either, so a single large roster write is a cheaper attack on the same file that this delta neither introduced nor touches.

**Live dispatches.** A fresh-context consult on that framing, at fable and `high` effort through `Workflow`, run `wf_21e6410d-3c7`, asked to attack the trilemma rather than ratify it. And an expert ask to the repository's live `AP: Expert` seat, sent before any declaration under the blocker path's own ordering, asking whether a recorded decision, an existing cap or a fourth option already exists. The ask gates nothing. Anything either returns is checked against the code here before it is acted on.

**No tree edit runs while they read.** The two confirmed Majors sit in the exact regions the consult is reading, so their fixes wait for it rather than shifting the tree under its analysis.

**Next action.** Take the consult's ruling and the expert's answer, decide the third Major, and declare it to the operator if it stays theirs. Then fix the two confirmed Majors, then the Minor close pass over round 10's six and the four still standing from rounds 6, 7 and 9, then the close gate, then Section 6's Chapter with the `bin/supervise.sh` fold recorded and the `Files in scope:` line corrected.
