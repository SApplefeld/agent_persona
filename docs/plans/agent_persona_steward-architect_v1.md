# Steward and architect: split the coordinator persona into a Sonnet fleet keeper and a Fable designer on demand

Status: In Progress
Commit Model: Branch-and-PR
Disjoint: no
Created: 2026-09-15

## Goal

When this is done, the always-on coordinator persona is replaced by two personas. The steward runs Sonnet on a five-minute tick and keeps the fleet running: it routes commons-inbox traffic between workers and the operator as the coordinator does today, watches each persona's keeper state and heartbeat and reports one that is held, backing off or stale, holds the kit's Coordinator seat and runs that seat's reconciliation pass, and wakes the architect when a record needs design judgment. The architect runs Fable with no standing goal and no home repository, and is woken by the steward or the operator for design work: assessments, specs written into the target repository, consults, plan reviews, and the finishing judgment on a high-stakes effort. Workers are unchanged. It matters because today one Fable persona ticks every minute doing mostly clerical work at the top tier's cost, while the design work that tier exists for arrives a few times a day.

## Approach

**What the coordinator persona costs today, measured.** The coordinator child's transcript at `D:/personas/coordinator/run/child-1/stdout.jsonl` showed, when read on 2026-09-15, a run cost of 8.75 dollars across 150 assistant turns, 52 shell calls, 2 subagent dispatches, 10 replies to the operator, 1 inbound priming prompt and 0 steers sent to a worker. The work was status reading and reporting. That is the clerical load the steward takes at Sonnet.

**Steward.** The coordinator persona's launch shape, kept whole, with four changes. The persona is named `steward` and the plugin's `coordinatorPersona` setting names it, so the plugin's reach rule (the coordinator persona's holder may address any persona, and workers may address the coordinator persona) applies to the steward with no plugin change. The model is Sonnet. The controller tick is 300000 milliseconds; a delivered record still lands on the next quiet tick, and a worker's urgent record breaks into a running turn, so a slower tick costs response time only for a steward that is idle, and five minutes is the bound the fleet-health duty needs anyway. The standing instruction the supervisor writes for the coordinator persona (`COORDINATOR_ROLE_INSTRUCTION` in `bin/supervise.sh`) gains three duties beside the ones it carries today:

1. Fleet health. The plugin watches fleet health itself and submits a prompt labelled `[FLEET]` carrying only the personas whose health class changed; the steward reports each of them to the operator on its own thread. It polls nothing, because a duty written as a per-tick habit has no trigger: the controller tick starts no turn on a persona holding no active goal leaf and an empty inbox, which is exactly the quiet fleet on which a crashed persona most needs reporting. The classes are held, backing off, stale, no live claim while the roster enables it, and healthy. The fleet status tool Section 3 adds stays available as an on-demand read, and Section 6 builds the watcher.
2. The kit's Coordinator seat. At priming, take the seat with the kit's role skill, which writes the registry entry and reads the board. The reconciliation pass the kit's coordinator skill states, including the registry prune of exited entries and the claim probe, runs when a prompt labelled `[RECONCILE]` arrives and at no other time; that prompt fires on the owning skill's own four-hour cadence, because the probe's window is one full cadence and the prune's staleness test is twice it, so a five-minute run could fire nothing a four-hourly run misses. At the end of each turn whose state is on disk, run the kit's compaction checkpoint CLI's boundary verb as the coordinator instruction already says, so the next automatic compaction lands at a declared point.
3. Waking the architect. A record that turns on a design decision (an operator design question, a worker escalation the worker's plan does not cover, a request for a spec, a plan review or a consult) goes to the architect through `agentic_say` with the persona argument set to `architect`, carrying the ask and the repository it concerns, and the steward tells the operator it did so. A delivered record starts a turn on the architect's next tick, which is how the wake works, and no new mechanism is added. The architect answers the steward the way a worker does, with a record addressed to the steward's persona, and the steward relays the answer to the worker that escalated as a coordinator record, so the architect never addresses a worker directly.

**Architect.** A new persona `architect`, model Fable, effort high through the roster's `effort` field, which the process keeper plan maps to the supervisor's `EFFORT` variable, on a 60000 millisecond tick, which is cheap while idle because a tick with no goal and an empty inbox does nothing. Its home directory is `D:/personas/architect`, not a repository: it is the roaming seat the operator asked for. The supervisor takes any directory as a workdir, the persona store lives there, and the commons store every persona claims into is machine-global under the installed plugin, so the architect holds a commons entry and receives records like any other persona. When an ask names a repository, it cuts a worktree of that repository under its own directory, does the work there on a branch, commits and pushes, and reports the branch and the filename to the steward and the operator. A new standing instruction, `ARCHITECT_ROLE_INSTRUCTION`, built when the launch's persona matches a new `ARCHITECT_PERSONA` setting the way the coordinator instruction is built today, states its charter: design work only, no standing goal, specs go into the target repository's `docs/plans` on a branch, it never executes a plan, and a plan it writes is handed to a worker through the steward. One limitation stands until the kit-resolution plan lands: the kit resolves a session's instructions, memory and leash from the session's launch directory, so the architect's kit memory is its own directory's rather than the target repository's, and it reads a repository's memory index explicitly when it needs it.

**Workers.** Unchanged in this plan. They keep their launch shapes and their roster entries; the only thing that moves is the name their steer instruction targets, which is the `coordinatorPersona` value, so each worker picks up the steward's name at its next launch.

**What retires.** The `coordinator` persona: its roster entry is replaced by `steward`, its launcher becomes the steward's, its thread stays as history, and its store entries in the shared `D:/agent_persona` store are released by its own shutdown. The backlog entry "A pending record to an unheld coordinator is lost when its writer restarts before the coordinator's first tick" is re-read here against a steward that runs under the process keeper: Section 5 retires it with receipts if the keeper closes the window, and otherwise re-dates it with what remains.

**Why not one persona with a model switch.** A persona is one session with one model for its life; the supervisor sets the model at launch. Two jobs with different tiers and different cadences are two personas, and the commons inbox already carries traffic between named personas, so the split costs one instruction, one roster entry and one tool.

**Sweep.** The surfaces that speak the coordinator persona's contract, read directly in the design session: `bin/supervise.sh` (the worker steer instruction and the coordinator role instruction around lines 1540 to 1600, the `COORDINATOR_PERSONA` export at 359 to 370); `hooks/index.ts` (`coordinatorPersona` resolution at 820 to 823, the reach checks at 4420 and 4462, the delivery ground at 1586 and 1687); `hooks/operator.ts` (`mayReachPersonaIn` and `deliveryGroundIn`, the reach rule itself); `.kit/settings-plugin-key-test.sh` (the `coordinatorPersona` round trip); `README.md` (the coordinator launch paragraph at 48, Delivery and Trust boundary at 434 to 452, the coordinator standing instructions the archived coordinator plan's Section 8 states); `.kit/channel-reply-instruction-test.sh`; `docs/archive/agent_persona_coordinator_v2.md` (the persona this plan splits); the launchers under `D:/personas`; the kit's role and coordinator skills, whose seat this plan folds; and the machine's registry under the kit's coordinator directory, which shows the kit's Coordinator seat taken only by interactive sessions so far and never by the coordinator persona.

**Order.** After the process keeper plan, because the fleet-health duty reads the roster and `keeper.json` that plan creates. Section 1 and Section 2 in either order, then 3, then 4, then 5.

## Standing Brief Amendments

- The steward is woken by the plugin, not by a cadence in its own instruction. A duty that says "on each tick" states a trigger the runtime does not have: the controller tick submits no prompt when the persona holds no active goal leaf and its inbox is empty, so a quiet fleet never wakes the steward at all. Every duty is written as a response to a labelled prompt the plugin submits, or to a delivered record, and never as something the steward does on a schedule of its own.
- A prompt the plugin submits reaches the session only once it is idle, and repeated submits accumulate rather than replacing one another. So any submission the controller makes on a recurring timer is gated on a persisted state comparison, and the state is persisted at the moment of submission rather than when the model reports, or one slow turn collects a pile of identical prompts.
- The reconciliation pass runs on the kit coordinator skill's own four-hour cadence and never more often. The claim probe's window is one full cadence and the registry prune's staleness test is twice the cadence, so a five-minute run of either can fire nothing a four-hourly run misses, and the skill states that the probe adds no timer to the seat.
- A persona's health classes are held, backing off, stale, no live claim while the roster enables it, and healthy. The fourth is not optional: an enabled persona that never comes up has no commons entry at all, so it reports a null heartbeat age and satisfies none of the other conditions.

## Sections of Work

### 1. The steward's launch shape and standing instruction
Model: opus

In `bin/supervise.sh`, extend `COORDINATOR_ROLE_INSTRUCTION` with the three duties in the Approach, in the same register and length discipline as the sentences it already carries, and keep every existing sentence. Change nothing in how the instruction is selected: it is still built when the launch's persona equals the `coordinatorPersona` value, so a launch under the name `steward` with that setting gets it. Update `README.md`'s coordinator launch paragraph to the steward's shape (Sonnet, the five-minute tick, the name), and the archived coordinator plan is left as history.

Acceptance: `.kit/channel-reply-instruction-test.sh` gains cases pinning that the persona named by `coordinatorPersona` receives the three new duty sentences and a named worker does not; the existing cases pass unchanged; the README paragraph states the steward's shape.

Files in scope: `bin/supervise.sh`, `.kit/channel-reply-instruction-test.sh`, `README.md`.
Tests: at minimum, lock each new duty sentence present for the coordinator persona and absent for a worker and for `default`, since a duty sentence leaking into a worker's priming would have workers probing the registry.

### 2. The architect's launch shape and standing instruction
Model: opus

In `bin/supervise.sh`, add an `ARCHITECT_PERSONA` setting beside `coordinatorPersona`, emitted into the settings file the supervisor writes and read back from a provided one by the same two branches, and an `ARCHITECT_ROLE_INSTRUCTION` built when the launch's persona equals it, stating the charter in the Approach. The instruction names the two ways an ask arrives (a `[COORDINATOR ...]` record from the steward, or the operator on the architect's own thread), the worktree rule, the commit-and-report rule, and the never-execute rule. Add the persona to `README.md` beside the steward.

Acceptance: `.kit/channel-reply-instruction-test.sh` pins the architect instruction present for the architect persona and absent for every other; `.kit/settings-plugin-key-test.sh` pins the new setting emitted and read back under both plugin ids as `coordinatorPersona` is; a launch with `ARCHITECT_PERSONA` unset builds no architect instruction for any persona.

Files in scope: `bin/supervise.sh`, `bin/agentic-common.sh`, `.kit/channel-reply-instruction-test.sh`, `.kit/settings-plugin-key-test.sh`, `README.md`, `docs/backlog.md`.
Tests: at minimum, lock the instruction's presence and absence per persona and the setting's round trip through the settings file, both directions, since a mis-set persona name would launch a Fable session with a worker's charter.

### 3. The fleet status tool
Model: opus

Add a `fleet_status` tool to the plugin (`hooks/index.ts`, beside `agentic_inbox`), available under the plugin's reach rule with the coordinator persona as the target: the caller holds the coordinator persona or a live reader claim on it, which is what `mayReachPersonaIn` in `hooks/operator.ts` already states. It reads the roster at the path a new `fleetRoster` setting names, each roster persona's `keeper.json` under that persona's run directory, enabled or not, and each persona's commons entry, and returns one row per roster persona: name, enabled, keeper action and delay, hold reason if any, last supervisor exit, commons claim held or not, heartbeat age, and turn state. `keeper.json` is the process keeper's state file and carries the persona name, launch count, last start and end, last exit code, current delay and the hold reason if any; the commons entry carries the claim's last-seen stamp, from which the heartbeat age is derived, and the turn state. The implementer opens `docs/plans/agent_persona_process-keeper_v1.md` for the roster and state-file layout. A missing roster or a missing `keeper.json` is a row that says so, never an error that hides the other rows.

Acceptance: a unit test drives the tool with fixture files for a healthy persona, a held one, one backing off, one with no `keeper.json`, and one absent from the commons, and checks each row; the tool is denied to a session that does not hold the coordinator persona, with the deny text naming the rule.

Files in scope: `hooks/index.ts`, `hooks/operator.ts` (the reach rule), a new `.kit/fleet-status-unit-test.mjs`, `README.md` (the tool list under the operator channel section).
Tests: at minimum, lock every row shape above and the deny direction: a session owning a named persona of its own holds a WORKER ground on the coordinator persona, which reaches it to send a record and is refused here, so fleet state is read only by that persona's holder or by a live reader claim on it.

### 4. The kit Coordinator seat under the steward, proven live
Model: opus

A live check that launches a real child, so it runs under the README's rule for live suites: only with the operator's yes while the fleet is up. Write `D:/personas/steward/launch.sh` in the shape of the coordinator's launcher with the steward's model, tick and name, launch it by hand, and watch the child take the kit's Coordinator seat. The seat's artifacts live under the kit's coordinator directory in the memory store, `~/.claude/coordinator/<machine>/`: `board.md` is the board and `registry/<session-id>.md` is one entry per registered session, per the kit's role skill. Acceptance, each read from the artifact: a registry entry under the kit's coordinator directory whose `Name:` is the machine's Coordinator seat name and whose `Session:` is the steward child's session; a board line from the steward's first reconciliation pass; and a registry prune of at least one exited entry with its board line, the machine's registry holding dozens of exited entries at the time of writing. Where the role skill cannot be invoked from a `claude -p` child, the Chapter says what failed and the seat stays with interactive sessions, which is a design stop for the operator rather than a fix round.

Files in scope: none in the tree beyond the Chapter; `D:/personas/steward/launch.sh` (machine state, kept by Section 5); reads of the kit's role and coordinator skills and the machine's coordinator directory.

### 5. Cutover, roster, and the retired persona
Model: sonnet

Update `bin/fleet.example.json` and the machine's `D:/personas/fleet.json`: replace the `coordinator` entry with `steward` (model `sonnet`, `controllerTickMs` 300000, `coordinatorPersona` `steward`, `channelName` `steward`) and add `architect` (model `fable`, `effort` `high`, workdir `D:/personas/architect`, `channelName` `architect`). Keep the steward's launcher Section 4 wrote and write `D:/personas/architect/launch.sh` beside it, as the manual fallbacks in the shape of the existing launchers. Re-adjudicate the pending-record backlog entry named in the Approach and either retire it into the quarter's backlog snapshot, `docs/archive/backlog-2026-Q3.md`, with receipts, per the curating-docs prune path, or re-date it. Update `docs/README.md`'s index line for this plan.

Both roster entries also carry `architectPersona` `architect`, and the keeper's roster-to-environment map gains `architectPersona` to `ARCHITECT_PERSONA` beside the four fields it already maps. Section 2 added the setting and the supervisor reads it, and nothing on the keeper's launch path can supply it: `Build-SupervisorInvocation` in `bin/keeper-functions.ps1` maps `model`, `effort`, `controllerTickMs` and `coordinatorPersona` and no others, so without this a keeper-launched steward builds no design-escalation clause and a keeper-launched architect runs with no charter. This is the same shape as the roster gap Section 6 was appended to close, found by review round 2 of Section 2 rather than in design.

Acceptance: `bin/Register-PersonaTasks.ps1 -WhatIf`, the process keeper's registration script, prints a definition for `steward` and `architect` and none for `coordinator`; each launcher matches its roster entry by eye, recorded in the Chapter; a keeper-launched architect's settings file carries `architectPersona`, read from a real launch rather than from the map by eye; the backlog entry is retired or re-dated with the reason.

Files in scope: `bin/fleet.example.json`, `bin/keeper-functions.ps1`, `docs/backlog.md`, `docs/README.md`, `D:/personas/fleet.json` and `D:/personas/architect/launch.sh` (machine state).

### 6. The fleet wake: the roster setting the launcher writes, and the plugin's change-driven prompt
Model: opus

Appended during execution. Sections 1 and 3 both shipped against a wake mechanism that does not exist, which round 1 found from three lenses and a consult ruled on. Two gaps, and they are one piece of work because neither is worth anything alone.

First, nothing writes the `fleetRoster` option. `emit_settings_json` in `bin/agentic-common.sh` writes a fixed option set ending at `coordinatorPersona`, and the process keeper's roster-to-environment map carries no roster path either, so a supervised steward always reads an empty setting and `fleet_status` reports that it has no roster. Add a `FLEET_ROSTER` environment variable to the emitted options and a read-back beside `read_settings_coordinator_persona`, by the same two branches and under both plugin ids.

Second, the controller tick submits no prompt on a quiet fleet, so a duty written "on each tick" never runs. In the tick, after the inbox drain and before the no-active-leaf return, add a block that runs only for the coordinator persona and only with a roster configured: compute the fleet rows with the module-scope helpers Section 3 wrote, reduce each row to one of the five health classes the Standing Brief Amendments name, compare against a persisted health map, and where any persona's class changed, persist the new map and submit a plugin-kind expected turn labelled `[FLEET]` carrying only the changed rows. Add a `reconcileEveryMs` option defaulting to 14400000, and submit a `[RECONCILE]` turn when that long has passed since the last one, coordinator persona only. Persist both state fields at submission rather than at report.

Two hazards round 3 found in Section 3 land here, because this section is the second producer of that tool's text and the first consumer of its `action` field.

The bracket guard is a property of the output channel rather than of the field that first needed it. Section 3 neutralizes square brackets in the persona-writable text, which is contained today by the JSON framing of a tool result. This section splices those rows into a submitted turn, where a delivery label is the trust signal and that framing is gone. So the neutralization happens at this splice over every field the prompt carries, `name`, `holdReasonSource`, `problem` and `problems` among them, rather than by trusting the per-field guard upstream. One exported helper owns the rule, and `bracketSafeProblem` in `hooks/operator.ts` is the rule's other implementation and the natural home.

The health-class reduction reads more than `action`. A row reading `running` says a live session holds the persona and says nothing about the keeper: an unreadable keeper state and a climbed relaunch ladder both reduce to `running`, so a reduction over `action` alone would report both as healthy, against the Goal's "watches each persona's keeper state". The reduction reads `nextDelaySeconds` and `note` beside it.

Acceptance: `.kit/controller-tick-test.mjs` pins that a tick over an unchanged fleet submits nothing, that a persona changing class submits one `[FLEET]` prompt carrying that persona and not the unchanged ones, that a second tick with no further change submits nothing more, and that `[RECONCILE]` fires on the interval and not before; `.kit/settings-plugin-key-test.sh` pins `fleetRoster` emitted from `FLEET_ROSTER` and read back under both plugin ids, as `coordinatorPersona` is; a launch with `FLEET_ROSTER` unset emits no roster and the tick block stays silent.

Files in scope: `hooks/index.ts`, `hooks/operator.ts`, `bin/agentic-common.sh`, `.claude-plugin/plugin.json`, `.kit/controller-tick-test.mjs`, `.kit/settings-plugin-key-test.sh`, `README.md`.
Tests: at minimum, lock the silence on an unchanged fleet and the single submission on a change, since the accumulating-submit behaviour means a missing change gate delivers one prompt per tick as a pile at the next idle moment.

## Out of Scope

- The kit resolving a session's instructions, memory and leash from the repository it works in rather than its launch directory. That is the next plan, for the session that maintains the `claude-kit` repository, and it is what lets the architect hold a repository's Expert seat properly.
- Any change to workers' charters or launch shapes.
- The steward relaunching a stuck persona itself. The process keeper owns relaunch; the steward reports.
- A model switch inside one persona's life.
- Moving the steward or the architect to another machine.

## Assumptions

- assumed 2026-09-15 (source: the operator's words on 2026-09-15, "Coordinator should fold into Steward. And it should probably run Sonnet"): the coordinator persona is renamed to `steward` rather than kept under its old name with new duties, since the name states the job; reversal: keep the name `coordinator` in the roster and the setting, and no worker needs relaunching for the steer target.
- assumed 2026-09-15 (default): the steward's tick is five minutes; reversal: one roster value.
- assumed 2026-09-15 (default): the architect is homed at `D:/personas/architect` with no repository, per the operator's words that the fleet should be a few workers with one or two homed; reversal: a workdir change in the roster.
- assumed 2026-09-15 (source: `README.md` Delivery, read 2026-09-15), and corrected during execution on 2026-09-15 by a consult that read the tick itself: a record delivered to a persona does start a turn on that persona's next quiet tick, and that is the whole wake mechanism for the architect, which is woken by a record. It is not the whole mechanism for the steward, whose fleet-health and seat duties are triggered by nothing and nobody: the tick submits no prompt at all when the persona holds no active goal leaf and its inbox is empty. Section 6 was appended to build the missing trigger as a plugin-side watcher. This correction is approval drift, recorded on interim board 2.
- assumed 2026-09-15 (default): fleet health is a plugin tool rather than prose telling the steward to read files, because a Sonnet session following a file-reading recipe every five minutes is the failure this plan exists to remove; reversal: drop Section 3 and the duty sentence reads the files by hand.
- assumed 2026-09-15 (default): the executing session runs on this machine, where the launchers under `D:/personas` and the kit's coordinator directory exist; reversal: none, since the paths are parameters of the process keeper's scripts.
- assumed 2026-09-15 (default): this plan is parked until the operator has read it, since the four decisions it rests on were given in outline and the section shapes above were not shown to the operator before writing; it carries no Dispatch Authorization section and arms on the operator's word.

## Operator Verification

1. After the cutover, post a design ask on the architect's thread naming a repository. A spec lands in that repository's `docs/plans` on a branch within the hour, and the architect reports the branch and the filename on its thread.
2. Ask a worker, on its own thread, to escalate a question its plan does not cover. The steward's thread reports the escalation and that it went to the architect, and the architect's thread shows the record arriving.
3. Shut one worker down through its tool. Within one steward tick the steward's thread reports that persona as held, and reports nothing further about it until its state changes.

## Open Questions

- The rename to `steward` is assumed above and every section is written to it, so the plan is executable as it stands. The operator may reverse it before the plan arms, at the cost the assumption names. Recommended: keep the rename, because the name should state the job and the cost is one relaunch per worker at the cutover the process keeper plan already schedules.
- Whether the architect takes the kit's Expert seat for the repository it works in. Recommended: only once the kit-resolution plan lands, since until then the seat's registry entry would name the wrong repository. The kit session's call after that plan.
- Whether the steward should also hold the kit's Admin seat, whose inbox poll is a four-hour loop of the same clerical shape. Recommended: not in this plan; revisit after a month of the steward's boards.

## Related

- `docs/plans/agent_persona_process-keeper_v1.md`: the process keeper, which this plan follows and whose roster and `keeper.json` the steward reads.
- `docs/archive/agent_persona_coordinator_v2.md`: the coordinator persona this plan splits.

## Chapters

### Interim board 1 - 2026-09-15

Written with Sections 1 and 3 at first green and their round 1 reviews in flight.

- **Run start.** The plan's `Status:` header read `Ready` and was normalized to `In Progress`, which is approval drift recorded here rather than silently. The plan carries no `## Dispatch Authorization` section and its own last assumption says it "arms on the operator's word"; that word is the operator's typed `/kit-goal ... --append` naming this exact path, so the plan is approved as written. The goal state records the arming as `self` because this run made the CLI invocation on the operator's relayed instruction, which is what the kit-goal flag rule requires; it does not mean the operator did not authorize the plan.
- **The preceding plan's blocker does not reach this one.** `agent_persona_process-keeper_v1` stopped on its own permission check at the review-round backstop. Sections 1 and 2 here touch only the supervisor's standing instructions, and Section 3 touches the plugin, so nothing in that stop bears on this work. Sections 4 and 5 do depend on the keeper and are named below.
- **Branch.** Work landed on a new branch `steward-architect`, cut from `5f7b66e` on `process-keeper` rather than from an earlier base. The reason is that the keeper plan's six modified files are uncommitted worktree state sitting on top of its committed code, so checking out an earlier base would have destroyed them. The cost is that this branch stacks on the keeper's commits, and the pull request is to be based on `process-keeper` rather than `main` at finishing. Those six files were confirmed untouched throughout: their modification times all predate the dispatch.
- **Section 1 (the steward's standing instruction) is at first green, committed as `01628b0` and pushed.** Three duties were appended to `COORDINATOR_ROLE_INSTRUCTION` in `bin/supervise.sh`, with no existing sentence changed and no change to how the instruction is selected. The instruction test gained eight cases across all five existing evaluations, extracting the block from the script by line range rather than hand-copying its prose.
- **Section 3 (the fleet status tool) is at first green, committed as `e8810f2` and pushed.** The tool, its five module-scope helpers, the `fleetRoster` setting and its manifest declaration, a 342-line suite and six fixtures. `hooks/operator.ts` is unmodified: the reach rule was reused and narrowed at the call site.
- **The orchestrator's brief was wrong on Section 3's gate and the implementer corrected it.** The brief said the needed gate "is exactly what `mayReachPersona` already decides". It is not: that rule's worker leg grants any session owning a named persona, because the leg exists to let a worker send the coordinator a record, so using it unchanged would have let every worker read fleet state. The implementer called `deliveryGroundIn` and admitted only the `COORDINATOR` and `READER:<coordinatorPersona>` grounds, which is the section's own sentence. Recorded because the correction is the implementer's, not this session's.
- **Route-b decisions the orchestrator locked before dispatch.** `.claude-plugin/plugin.json` was folded into Section 3, since the plan's Files in scope omitted it and the plugin reads its options from the manifest's `userConfig`; the implementer then confirmed empirically from the engine's own source that an undeclared key still reaches the plugin, so the declaration is worth keeping but was not required. `README.md` was carved out of both implementers' scope and written by the main thread, because both sections name it and they ran concurrently. Section 1's acceptance says "the three new duty sentences" while each duty is a paragraph, so one distinctive sentence per duty is pinned.
- **The keeper-action contract the orchestrator locked.** The plan's row shape asks for a keeper action, and `keeper.json` carries no such field: the keeper writes the action only to `keeper.log`'s `DECIDE` line. The implementer was given both sources and picked the `keeper.hold` marker plus `keeper.json`, because the marker is what actually stops a relaunch and a `holdReason` in the state file outlives a release. The log was rejected because it rotates at 5 MB while the plugin's read refuses anything over 4 MiB, which would take the action dark on the persona crash-looping hardest.
- **Folds and out-of-scope surfaces taken by the orchestrator after the implementers returned.** Two files the implementer edited outside its list are folded and named on Section 3's scope: `.kit/controller-tick-test.mjs` (two pins move because the tool list and count grew) and `.kit/.gitignore` (an allowlist entry, without which the new suite could never be committed). A sweep for other carriers of the reader-tier tool list found two stale ones and both were fixed: the manifest's arming description and a comment at `hooks/index.ts:1025`. The README trust-boundary paragraph's list of gate sites was incomplete and gained a sentence naming the tool's narrowing. The sweep's control matched a withheld instance, so its silence elsewhere is real.
- **Still owed as out-of-scope surfaces, not yet routed:** the implementer named a helper `hooks/commons.ts` should export, a `readAllEntries(store)` with no staleness filter and no collection, so the staleness predicate stops living in two files. `hooks/commons.ts` is outside every section's scope here. It is routed at Section 3's close.
- **Gate, measured by the orchestrator on a box it claimed and released, no contention, on this worktree at `e8810f2` plus the keeper plan's six foreign dirty files and the main thread's README and manifest edits:** the fleet status suite 48 passed, 0 failed, exit 0; the controller tick suite 785 passed, 0 failed, exit 0, against a recorded baseline of 785 taken before any edit, so no regression; the instruction test 33 passed, 0 failed, exit 0, against a recorded baseline of 25 before the change; `tsc -p tsconfig.json` exit 0 with zero output lines. Every count and exit code was read from the orchestrator's own run rather than from an implementer's report.
- **A hazard checked and cleared.** A stored lesson says a helper declared inside `register()` that closes over `$` passes both the typecheck and the harness while the plugin engine silently refuses to load the module at all, leaving the child with no tools. All eight declarations the diff adds are at module scope, `.kit/check-loader-rule.mjs` passes, and the typecheck is clean. The lesson's own stronger check, a real `--plugin-dir` load smoke, has not been run and is owed before this plan finishes.
- **Live dispatches:** the round 1 review workflow. Section 3 has an adversarial lens, a blind lens and a security lens, all at fable; Section 1 has an adversarial lens and a blind lens at fable. The security lens was asked specifically whether the tool achieves its intended narrowing, whether the shared rule was widened, and whether roster-named paths reach anything but reads. The five run in two batches to stay under the three-fable ceiling.
- **Rulings adopted since the last boundary:** none. No consult, no judge, no design stop.
- **Next action per section.** Sections 1 and 3: adjudicate round 1 when it returns, then the Minor close pass and the Chapters. Section 2 (the architect's launch shape and standing instruction) is unblocked and runs next; it was held back only because it edits the same two files as Section 1. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up, so it is an operator gate rather than work. Section 5 (cutover and roster) has an acceptance bullet that runs the blocked keeper plan's registration script, so it cannot close until that plan's decision lands.

### Interim board 2 - 2026-09-15

Written with Sections 1 and 3 fixed, committed and pushed, and their round 2 review in flight.

- **Round 1 adjudicated: five lenses, no Critical, eight Majors.** Three lenses independently found that nothing writes the `fleetRoster` option, so a supervised steward always reads an empty setting and the fleet status tool reports it has no roster. The orchestrator had confirmed the same from `emit_settings_json` before the round returned.
- **The one real design question went to a consult, and its ruling is adopted.** The steward's two per-tick duties had no runtime trigger: the controller tick starts no turn on a persona with no active goal leaf and an empty inbox, confirmed at `hooks/index.ts` step 4. The consult ruled the per-tick framing does not survive, and that the plugin should watch fleet health itself and wake the steward only when a persona's health class changes. It rejected the orchestrator's own lean, a standing goal, on evidence the orchestrator did not have: a nudge cap of three pauses such a leaf permanently, so the steward would wake three times and then go dark. It also rejected a per-tick prompt as 288 Sonnet turns a day, which is the spend this plan exists to remove. No operator fork survived.
- **A stored lesson sharpened the ruling.** A prompt the plugin submits reaches the session only once it is idle, and repeated submits accumulate rather than replacing one another, which is how one goal nudge once arrived more than fifty times. So the health map is persisted at submission rather than when the model reports. That is recorded as a standing brief amendment.
- **Four standing brief amendments were written at adoption**, above `## Sections of Work`, which is approval drift recorded here: the plugin wakes the steward rather than the instruction naming a cadence; a recurring submission is gated on a persisted state comparison persisted at submit time; the reconciliation pass runs on the owning skill's own four-hour cadence and never more often; and the health classes number five, the fourth being an enabled persona holding no live claim, which the old wording left unreported.
- **Section 6 was appended** to carry the two gaps neither section could close from inside its own scope: the `fleetRoster` plumbing and the plugin-side change-driven wake. That is approval drift by construction and is named to the operator.
- **Section 3's fixes.** The delay figure named the wrong thing: the row read `keeper.json`'s `currentDelay`, which the keeper writes from the next rung of its backoff ladder, and reported it as the wait in force, so a persona on its first crash waiting 300 seconds read as backing off for 600. Confirmed against HEAD rather than the worktree, since the keeper files carry another effort's uncommitted edits. The field is renamed and the tool now states the wait in force cannot be read from it. A signalled exit read as relaunching for a persona nothing would relaunch, and the suite pinned that wrong value. Hold-reason text, which a persona can write into its own run directory, now carries its source file and the same 2000-character bound `agentic_resolve` uses.
- **Section 1's fixes.** Both per-tick duties rewritten as responses to labelled prompts, the fourth health class added, and the architect duty now confirms a live architect before calling an ask routed, because `agentic_say` succeeds whether or not anything holds the persona.
- **Both implementers returned DONE_WITH_CONCERNS and both concerns are routed.** The fleet suite's first-crash case asserts the serialized row holds no occurrence of 300, a deliberately blunt pin, and Sections 1 and 3 now name prompt labels nothing submits until Section 6 lands. Both were put to the round 2 lens unrated, as questions.
- **Gate, measured by the orchestrator on an uncontended box, claim file empty, at worktree `267865f` plus the keeper plan's six foreign dirty files:** the fleet status suite 86 passed, 0 failed, exit 0, against a baseline of 48; the instruction test 41 passed, 0 failed, exit 0, against a baseline of 33; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `tsc -p tsconfig.json` exit 0 with zero output. Every count and exit code read from the orchestrator's own run.
- **The operator answered on the relay thread, twice, and the second message changed a call.** They chose to drop the process keeper's permission check whole, and then said the relay is their only channel, since this session is a headless process with no keyboard. The first reply had told them to repeat it at a keyboard, which was wrong and was corrected. The resolution recorded: the relay is the operator's channel, and what gates an act is its blast radius rather than which channel carried it, so an irreversible or outward-facing act still stops for a question. The keeper plan's own committed Dispatch Authorization section names that same relay thread as the operator's channel, which corroborates it.
- **The queue was re-armed to carry both plans**, steward first and the process keeper behind it, because a blocked plan sits in the finished list where an append refuses it. The replacement dropped the recorded blocked outcome from the goal state; that record lives in the keeper plan's interim board 11 and in the journal.
- **Live dispatches:** the round 2 review, one adversarial lens at opus and high effort through a workflow, which is what a later round takes over an opus writer. It carries both sections' fix deltas, the amendments in effect, the trace target, and the two implementer concerns as unrated questions.
- **Rulings adopted since the last boundary:** one, the consult's ruling on the wake mechanism. No judge, no design stop, no scope adjudication.
- **Next action per section.** Sections 1 and 3: adjudicate round 2, then the Chapters. Section 2 (the architect's launch shape) runs next; it edits the same two files as Section 1 so it was held until now. Section 6 (the fleet wake) follows Section 2, since it edits those files too. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last, and its acceptance runs the keeper plan's registration script, which the re-arm now puts back in reach.

### Interim board 3 - 2026-09-15

Written with Section 3 fixed twice more and its round 4 review in flight, and with Section 1 holding one frozen sentence pending a design ruling.

- **Round 2 adjudicated: no Critical, four Majors, six Minors.** All four Majors traced to the Goal or to Section 1's acceptance, so none was new-requirement and nothing went to a judge. All four sat in lines round 1's fix wrote, which makes them fix-introduced. That was the first fix-introduced round, since round 1 could carry none, so the design stop's two-consecutive-round trigger was not met and an ordinary fix round ran.
- **One round 2 Minor was upgraded to a security Major.** Hold-reason text out of a persona's own writable run directory reached the steward with no bracket neutralization, and Section 6 carries those rows into a submitted prompt where the JSON framing that contains it today is gone. A security finding at Major weight is never parked, so it was fixed before the section could close rather than held for the close pass.
- **Round 2's fixes, committed as `33e7af8` and pushed.** The tool's `action` field reported the keeper's last exit decision as the persona's present standing, because the keeper writes its state file only after a supervisor exit; a persona that crashed once and then ran healthy for a week still read as backing off. The keeper read now returns a standing and a new step settles the action against the commons half. The steward's fleet duty called a crashed persona one that had never come up. The architect liveness check reported every routed ask as undelivered whenever the roster was unset. The README asserted that the plugin submits the labelled prompts, which nothing in the tree does.
- **Round 3 adjudicated: no Critical, two Majors, eight Minors, and it fired the design stop.** Round 3's Major on the steward's fleet duty sits in the same sentence round 2's did, and both are fix-introduced. Two consecutive rounds of fix-introduced Majors in one passage is a design stop rather than a third repair.
- **What the design stop is about.** Round 2's fix re-keyed the never-came-up verdict from the absence of a live claim onto a null heartbeat age. Round 3 showed that is the same falsehood one branch over: `gcStaleClaims` in `hooks/commons.ts` deletes any commons entry older than the staleness threshold, and `readAllClaims` calls it on every read from five call sites, so a dead persona's entry is reaped within about ninety seconds and then does report a null heartbeat age. Confirmed by reading both functions. The stale branch the fix added is reachable only inside the pre-reap window.
- **The stop is held with the repository's live Expert seat, which is the judge the kit's rule names when the roster shows one.** The ask carries the mechanism, the round indices, the capture range, and the plan's Goal and Section 1 acceptance as the trace target, and deliberately carries no account of what happened inside the rounds and no lean. The frozen unit is that one sentence in `bin/supervise.sh` around line 1597. No fix round runs on it until the ruling lands. If nothing has arrived by the time Section 3's other work reaches its close gate, the kit's rule sends it to a scope adjudicator and the seat's later answer is recorded beside whatever that rules.
- **Four round 3 Minors were upgraded on their stated consequences and fixed, committed as `041406d` and pushed.** The bracket guard ran over whole fields, so a note reporting a run directory whose path contains square brackets reported a path that does not exist. A signalled exit was masked by a stale live claim for up to the staleness window, which the pre-fix code got right. A hold marker check that throws made a held persona read as running. And the byte-order-mark strip added in round 2 was a no-op, because `trim` already removes that character, so its new case could not have gone red.
- **Two round 3 Minors were routed forward into Section 6 rather than fixed**, and Section 6's text now carries both. The bracket guard belongs at the output channel rather than on two fields, because Section 6 is the second producer and splices these rows into a submitted turn. And Section 6's health-class reduction must read `nextDelaySeconds` and `note` beside `action`, because `running` collapses an unreadable keeper state and a climbed relaunch ladder into one value. That amendment is approval drift, recorded here.
- **Round count for the review-round backstop: four rounds dispatched, none of them a re-raise.** The bound is five. Nothing has fired it.
- **Gate, measured by the orchestrator on an uncontended box, claims directory empty, at worktree `041406d` plus the keeper plan's six foreign dirty files, a peer session's two docs files and this session's own plan doc edit:** the fleet status suite 118 passed, 0 failed, exit 0, against a baseline of 104; the instruction test 47 passed, 0 failed, exit 0, against a baseline of 47; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero output. Every count and exit code read from the orchestrator's own run rather than from an implementer's report.
- **Two out-of-scope surfaces are still owed and neither is routed yet.** `hooks/commons.ts` should export a `readAllEntries(store)` with no staleness filter and no collection, so the staleness predicate stops living in two files. And `hooks/operator.ts` should export the delivery-bracket rule as a character-set predicate, so its refusing form and the fleet lane's neutralizing form cannot drift on which characters count. Both are routed at Section 3's close.
- **A peer session is live in this working tree.** It added `docs/plans/agent_persona_unversioned-manifest_v1.md` and a line for it in `docs/README.md`, both uncommitted. Neither was read, staged, nor committed here. That index file also still describes this plan as Ready and parked, which is stale, and correcting it is held while the peer's uncommitted line sits in the same file.
- **The operator answered twice on the relay thread about the harness wrapper.** They asked for the wrapper's exact wording so the doctrine can be written to complement it rather than fight it, and both replies are on the thread. No decision was taken here and nothing in this plan changed.
- **Live dispatches:** the round 4 review, one adversarial lens at opus and high effort through a workflow, over commit `041406d` alone, carrying the amendments, the trace target, the three implementer concerns as unrated questions, and an explicit instruction that the frozen sentence is outside the review.
- **Rulings adopted since the last boundary:** none yet. The design stop's judge ask is outstanding.
- **Next action per section.** Section 3: adjudicate round 4, then the Minor close pass and the Chapter. Section 1: cannot close until the design ruling lands; everything else in it is fixed and green. Section 2 (the architect's launch shape) runs next, and it edits the same two files as Section 1. Section 6 (the fleet wake) follows Section 2. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last and its acceptance runs the keeper plan's registration script.

### Interim board 4 - 2026-09-15

Written with the design stop ruled and its removal performed, Section 3 fixed a third time, and round 5 in flight over both sections.

- **Round 4 adjudicated: one Critical, three Majors, two Minors.** The round covered Section 3 alone, over commit `041406d`.
- **The Critical was confirmed before it was acted on, and it was a second reversal of one passage.** Round 3's fix made a recorded signalled exit outrank a live commons claim unconditionally. The process keeper writes `keeper.json` at exactly one point, inside the relaunch loop after the supervisor returns, and nothing writes or clears it at launch, confirmed by reading `git show HEAD:bin/Start-Persona.ps1`. So after a signalled exit and a restart the recorded exit code stays at the signal value for the whole of the new run, and a live healthy persona reports as stopped the entire time. Round 2's ordering had the mirror defect bounded by the staleness window instead. Round 2 wrote the passage, round 3 reversed it, round 4 would have reversed it again, which is the second reversal the kit's rule sends to a consult.
- **The consult ruled and its ruling is adopted.** A recorded exit decides only when the live claim's heartbeat predates it. The keeper records the exit after the child tree has gone, so a dead session's last heartbeat necessarily precedes the stamp and a restarted session's necessarily follows it. Where the stamp cannot be read, the claim decides and the row's note says which reading was unavailable. The consult rejected the cheaper readings it was asked about: `claimedAt` is the same comparison with weaker validation and `launchCount` belongs to the previous keeper. No operator fork survived.
- **The design stop was ruled REFUSE by the scope adjudicator, and the removal is performed.** The Expert seat never answered, so the ask fell to the adjudicator when Section 1's other work was ready for its close gate, which is the window the kit's rule sets. The adjudicator found the mechanism departed from the form the plan asks for: the plan names five health classes and the instruction told the steward to make a claim about the persona's history and report it under a sixth label. The grounds were checked here before adoption. The instruction's own sentence lists the five classes and then names a class outside them, confirmed by reading `bin/supervise.sh` line 1597. The sentence now routes the condition to the class by its own name, and the two test fragments pinning the refused wording are replaced by an ordered pin that a rename reds.
- **One round 4 Major was discarded with the reason.** It objected to a justification recorded in a code comment, quoting a sentence about the roster being operator-side configuration rather than a persona's writable tree. That sentence is nowhere in the tree: the comment that is there says the opposite, that a run directory sits inside its persona's own writable tree. Confirmed by grep over the code and over the last three commit messages, with a control that matched a string known to be present. The permission fact the finding rested on is sound and is recorded here: every persona's scheduled task runs under one principal, so a persona could write the roster. The behaviour the finding asked to keep is already what the code does, and the splice-side neutralization is already routed into Section 6.
- **Two round 4 Majors and both Minors were fixed**, committed as `66d32f4` and pushed. The README paragraph claimed the cut mark carried the only square brackets left, which contradicted a clause two before it and the suite both. Five exact-wording pins on the tool description are retired, one of them a negative substring pin that went green on any rewording and so could never fail usefully. The roster read's caught error message now goes through the same neutralizing guard its three siblings use. And a comment that narrated the hold-marker check as deciding the action was corrected to say the unreadable case reaches the reader in the note.
- **Round counts for the review-round backstop.** Section 3 has taken four rounds and round 5 is in flight, so its next adjudication meets the bound of five: if that adjudication still leaves the terminal condition unmet, the section stops on the operator path rather than opening a sixth fix round. Section 1 has taken three rounds and round 5 is its fourth. The design stop's own count of consecutive fix-introduced rounds restarted at its ruling and is unrelated to this one.
- **Gate, measured by the orchestrator under its own box claim, claims directory empty on arrival, at worktree `66d32f4` plus the keeper plan's six foreign dirty files and a peer session's two:** the fleet status suite 123 passed, 0 failed, exit 0, against a baseline of 118; the instruction test 46 passed, 0 failed, exit 0, against a baseline of 47, the drop being the one anti-control case the design-stop removal retired; the controller tick suite 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. Every count and exit code read from the orchestrator's own run rather than from an implementer's report.
- **The implementer reported a tree delta it read as a peer session's, and it was this session's own.** `bin/supervise.sh` and `.kit/channel-reply-instruction-test.sh` became dirty while it worked, which was the design-stop removal being written in the main thread. Its own three files were untouched by that work and the file sets were disjoint throughout.
- **Two out-of-scope surfaces are still owed and neither is routed yet.** `hooks/commons.ts` should export a `readAllEntries(store)` with no staleness filter and no collection. `hooks/operator.ts` should export the delivery-bracket rule as a character-set predicate, so its refusing form and the fleet lane's neutralizing form cannot drift. Both are routed at Section 3's close. The implementer also named five sites in `hooks/` that interpolate a caught error's message raw and left them unchanged with the reason; those are put to round 5 as a question.
- **Live dispatches:** the round 5 review, one adversarial lens at opus and high effort through a workflow, over commit `66d32f4` alone, covering both sections, carrying the four amendments, the trace target, and six implementer and orchestrator concerns as unrated questions.
- **Rulings adopted since the last boundary: two.** The consult's ruling on the action precedence, and the scope adjudicator's REFUSE on the design stop with its removal performed. Both were checked against the code here before adoption.
- **Next action per section.** Section 3: adjudicate round 5, then the Minor close pass, the two owed out-of-scope surfaces, and the Chapter. Section 1: adjudicate round 5, then its Chapter; nothing else in it is outstanding. Section 2 (the architect's launch shape) runs next, and it edits the same two files as Section 1. Section 6 (the fleet wake) follows Section 2. Section 4 (the kit Coordinator seat proven live) needs a real child launch and the operator's yes while the fleet is up. Section 5 (cutover and roster) runs last and its acceptance runs the keeper plan's registration script.

### Chapter 1 - 2026-09-15
Completed: 1. The steward's launch shape and standing instruction
Implemented By: implementer-opus at first green; every fix round after that written in the main session, because the fixes were single sentences inside one instruction string and a brief would have cost more than the edit.
Metrics: review rounds 4, closed major-closed; provenance: the per-section split was not kept at rounds 1 through 3, whose boards record the counts across both sections together (round 1 eight Majors over five lenses, round 2 four, round 3 two), so only round 5's is per-section here, one Major, spec-traceable; rulings (1 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations none; consults 1
Decisions / Surprises: The section's own premise was wrong and a consult found it. Every duty had been written as something the steward does each tick, and the controller tick submits no prompt at all when a persona holds no active goal leaf and its inbox is empty, so a quiet fleet would never have woken it. The duties are now written as responses to labelled prompts, and Section 6 was appended to build the trigger that submits them. A design stop fired on one sentence of the fleet duty after two consecutive rounds of fix-introduced Majors landed in it, and the scope adjudicator refused the mechanism: the plan names five health classes and the sentence told the steward to judge whether a persona had ever come up and report it under a sixth label. The refusal was performed rather than recorded, so the sentence now routes that condition to the class by its own name. Round 5 then found the mirror gap, that the tool's action field carries values none of the five classes name, with `stopped` the row an operator most needs; the duty now says the action field is the keeper's record rather than a class and routes the off-list values by the claim and heartbeat it already reads.
Assumptions: none beyond the plan's own, which were written before execution.
Review Findings: design stop: the never-came-up verdict in the fleet duty, ruling refuse by the scope-adjudicator after the repository's live Expert seat did not answer inside the window; `review: adversarial and blind pair at fable, Agent tool` at round 1, then `review: adversarial lens at opus, Workflow` at rounds 2, 3 and 5. No Critical in any round. Majors addressed: the two per-tick duties rewritten as responses to labelled prompts; the fourth health class added; the architect liveness check added, because `agentic_say` succeeds whether or not anything holds the persona; the refused verdict removed; and round 5's, that three action values had no health class. Majors justified: none. Minors: 4 fixed across the rounds and the close pass, 1 upgraded on a stated consequence, 1 left with the reason. The one left is the full-sentence pin on the five class names in `.kit/channel-reply-instruction-test.sh`: it is an exact-wording pin on curated prose, which the retire class would ordinarily drop, but the five names are each too weak a substring to pin alone and nothing pins the three surfaces carrying this list byte-identical to one another, so it is the only instrument that catches a class going missing. Kept with that reason rather than retired.
Stamps: adjudicated 0, stamped 0; none surfaced, the window's reads all sitting on records the earlier boards already stamped.
Gate: targeted lane, run by the orchestrator under its own box claim, claims directory empty on arrival, at the worktree carrying this section's and Section 3's delta plus the process keeper plan's six foreign dirty files and a peer session's two, measured 2026-09-15: `.kit/channel-reply-instruction-test.sh` 47 passed, 0 failed, exit 0, against a baseline of 46 taken at `66d32f4`, the rise being this round's new ordered pin; `.kit/controller-tick-test.mjs` 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. The contention lane did not run, because this section's delta touches no machine-shared state. Every count and exit code read from the orchestrator's own run.
Next: 3. The fleet status tool
Commit Model: Branch-and-PR
Delta: measured 2026-09-15 on this worktree, no contention.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Chapter 2 - 2026-09-15
Completed: 3. The fleet status tool
Implemented By: implementer-opus at first green and for the round 2 and round 4 fix rounds; the round 3 and round 5 fixes and the close pass written in the main session.
Metrics: review rounds 5, closed major-closed; provenance: as in Chapter 1, the per-section split was not kept at rounds 1 through 3; round 4 was this section alone, one Critical and three Majors, of which the Critical and one Major were fix-introduced, one Major was spec-traceable and one was discarded as resting on a sentence that is nowhere in the tree; round 5 carried one Major, fix-introduced; rulings (0 refused, 0 declared, 0 asked); NEEDS_CONTEXT count 0; escalations none; consults 1
Decisions / Surprises: The tool reads two files that disagree about time, and every round but the first turned on that. `keeper.json` is written once, after a supervisor exit, and never at a launch, so it describes a decision the keeper has already carried out rather than the present. Reading it as the present made a persona that crashed once and then ran healthy for a week report as backing off. Making the live claim outrank it unconditionally made a signalled exit invisible. Making the signalled exit outrank the claim unconditionally made a restarted persona report as stopped for the whole of its next run. That third swing was the second reversal of one passage, which the kit routes to a consult rather than to a third repair, and the consult's rule is what stands: a recorded exit decides only where the live claim's heartbeat predates it. Round 5 then found that rule reading the wrong claim. Commons arbitration is first-claim-wins, and a dead session's entry survives until it passes the staleness threshold, so for up to ninety seconds after a restart the predecessor was both live and the earliest claim and the row followed it. The recorded exit now narrows the field before arbitration runs, which is the same rule applied at the selection rather than after it. Three things are recorded here that the interim boards carried forward. The two out-of-scope surfaces this section owed are routed: `hooks/commons.ts` now exports `readAllEntries(store)` with no staleness filter and no collection, which is a fold, since the duplicate store scan it replaces is one this section introduced and the close gate covers it; and `hooks/operator.ts` is added to Section 6's files in scope, because Section 6's own text already names `bracketSafeProblem` there as the home for the one exported bracket rule while its file list omitted the file, which is approval drift named here. The five sites in `hooks/` that interpolate a caught error's message raw are left unchanged with the reason: all five land in `sess.state.decisions[].detail`, whose only readers are a mechanical `includes` and an idle summary that renders `loop:action` and never `detail`, so no delivery label can ride out of them; the one site that did reach a model was the roster read, and round 4 fixed it. And the earlier window ran two implementers concurrently while one of them ran a tree-mutating probe, against this skill's exclusivity rule; the file sets were disjoint throughout and the tree-state brackets on both dispatches came back clean, so no contamination resulted, but the rule was breached rather than satisfied and the clean result is luck rather than evidence.
Assumptions: assumed 2026-09-15 (default, section 3): `.claude-plugin/plugin.json` is folded into this section's scope, since the plan's file list omitted it and the plugin reads its options from the manifest's `userConfig`; reversal: drop the declaration, which the implementer confirmed empirically is not required for an option to reach the plugin. assumed 2026-09-15 (source: the section's own acceptance sentence, section 3): a missing or unset roster returns no rows and a top-level problem rather than a row that says so, because no persona exists to name; reversal: none, the shape is the honest one.
Review Findings: `review: adversarial, blind and security lenses at fable, Agent tool` at round 1, then `review: adversarial lens at opus, Workflow` at rounds 2, 3, 4 and 5. One Critical, at round 4, confirmed against the code before it was acted on and routed to a consult rather than a fix because it was a second reversal. Majors addressed: the delay figure named the next rung of the keeper's ladder and was reported as the wait in force; the action field reported the keeper's last exit decision as the persona's present standing; the README asserted a submission nothing in the tree performs and later that the cut mark carried the only square brackets left; the roster read's caught error message bypassed the neutralizing guard its three siblings use; and round 5's, that the clock rule read the commons winner's heartbeat rather than the live session's. One Major discarded, orchestrator-made: it quoted a sentence about the roster being operator-side configuration that exists nowhere in the tree, confirmed by grep over the code and the last three commit messages with a control that matched a string known to be present. Majors justified: one, round 5's on the unreadable-stamp default, which asked the tool to fail toward `stopped` where the consult's adopted ruling says the claim decides; the finding's second half, that nothing told the steward to read `note`, was fixed in Section 1's duty instead. Minors: 9 fixed across the rounds and the close pass, 5 upgraded on a stated consequence, 2 left with the reason, and 2 routed forward into Section 6 rather than fixed here. One Minor was upgraded to a security Major and fixed before the section could close rather than parked: hold-reason text out of a persona's own writable run directory reached the steward with no bracket neutralization.
Stamps: adjudicated 0, stamped 0; none surfaced.
Gate: targeted lane, run by the orchestrator under its own box claim, claims directory empty on arrival, at the worktree carrying this section's and Section 1's delta plus the process keeper plan's six foreign dirty files and a peer session's two, measured 2026-09-15: `.kit/fleet-status-unit-test.mjs` 131 passed, 0 failed, exit 0, against a baseline of 123 taken at `66d32f4`; `.kit/controller-tick-test.mjs` 785 passed, 0 failed, exit 0, against a baseline of 785, so no regression, which is the lane that covers the commons reach path this section's close-pass fold touched; `.kit/channel-reply-instruction-test.sh` 47 passed, 0 failed, exit 0, against a baseline of 46; `npx tsc -p tsconfig.json` exit 0 with zero bytes of output. The contention lane did not run, because this section's delta touches no machine-shared state. Every count and exit code read from the orchestrator's own run.
Next: 2. The architect's launch shape and standing instruction
Commit Model: Branch-and-PR
Delta: measured 2026-09-15 on this worktree, no contention.

```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 5 - 2026-09-15

Written with Section 2 built and three review rounds adjudicated, the third round's fixes green, and round 4 not yet dispatched.

- **Round 1: three lenses, one Major found by all three independently.** `review: adversarial, blind and security lenses at fable, Agent tool`. The shared finding: this section made the architect's name a setting while the steward's own instruction, written in Section 1, still routed design asks to the literal name `architect`. A fleet naming the seat anything else would have sent every ask to a persona nothing holds. Three more Majors beside it: the charter told the architect to cut a worktree and commit for every ask, including the consults and plan reviews the Goal names, which produce no file; the provided-settings branch discarded the launch environment's name silently, so a stale run directory launched a Fable session with no charter and no error; and nothing refused one persona named for both seats, which would have built both role instructions into one priming write.
- **Round 2: one lens at opus, six Majors, two of them about prose rather than code.** `review: adversarial lens at opus, Workflow`. The one that mattered most: the coordinator steer sentence is built for every launch and tells a session to put a coordinator record that ties to no goal node to the operator, or to decline it where no channel is attached. The architect holds no standing goal by design, so read literally it would have bounced every design ask the steward routed, which is the whole mechanism this section exists to build. The charter now names that rule and overrides it for this seat. Also: the read-side character class was wider than the emit side's over a value that the first round's own fix had just started splicing into the steward's instruction; the steward's routing list named four kinds where the Goal names five; and the charter said "cut a worktree" without naming which clone, whose likely reading sends the architect into a worker's live checkout.
- **Round 3: two lenses at opus, one Critical.** `review: adversarial and security lenses at opus, Workflow`. The Critical: the charter splices the coordinator's name, and on the provided-settings branch that name comes from a read whose class admits readable English. A hyphenated sentence passes it, so a persona able to write a settings file could plant prose in a top-privilege session's priming write. The fix holds the resolved name to the persona character class at the supervisor and refuses the launch when it fails, rather than narrowing the read, because the plugin resolves the wide-class name as the coordinator and narrowing would desynchronize the two. Beside it: the skill-load sentence sends every session to the plan-execution skill, which collides with the charter's own never-execute rule; the fleet carve-out said "two cases and no others" while the design clause called the liveness check a third; the clone clause still named no source, so a local clone would hardlink another persona's object store and push to it; and the pin guarding the headline fix could not red on the defect it exists to catch, because a bash case glob matched a second interpolation later in the same string.
- **Two findings routed rather than fixed.** Nothing on the process keeper's launch path can supply the new setting: its roster-to-environment map carries four fields and this is not one of them, so a keeper-launched steward would build no design clause and a keeper-launched architect no charter. That file belongs to the process keeper plan and is dirty in this worktree, so the gap is routed into Section 5, whose text and acceptance now name it. The second is recorded rather than built: a persona that can write another persona's run directory chooses who holds the design seat, and `docs/backlog.md` carries the hazard with a remedy that actually closes it, after a round found the remedy first recorded did not.
- **Two scope widenings, both declared.** `bin/agentic-common.sh` joined the section's Files in scope, because the settings emit and read both live there rather than in `bin/supervise.sh`, so the section's own sentence is unbuildable without it. `docs/backlog.md` joined it too, as the route for the hazard above.
- **Gate at this point, measured by the orchestrator on this worktree.** `.kit/channel-reply-instruction-test.sh` 88 passed, 0 failed, exit 0, against a baseline of 47 at `4eb8239`. `.kit/settings-plugin-key-test.sh` 95 passed, 0 failed, exit 0, against a baseline of 61. Both exit codes read from the runs. An em dash sweep over every added line returns zero, with a control line the same pattern counted as one.
- **Next action.** Round 4 over Section 2, then its Chapter. Round 3 carried two fix-introduced Majors, so a fourth round carrying more of them meets the design stop's condition and goes to the scope adjudicator rather than to a fifth repair.
